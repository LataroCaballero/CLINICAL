---
phase: 01-infraestructura-async
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - frontend/src/app/dashboard/configuracion/page.tsx
  - frontend/src/app/dashboard/configuracion/components/WhatsappConfigTab.tsx
  - frontend/src/hooks/useWabaConfig.ts
  - frontend/src/hooks/useSaveWabaConfig.ts
  - frontend/src/hooks/useUpdateWhatsappOptIn.ts
  - frontend/src/app/dashboard/pacientes/components/WhatsappOptInToggle.tsx
  - frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx
autonomous: false
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "El usuario ADMIN/PROFESIONAL ve una tab 'WhatsApp' en Configuración con el formulario de conexión WABA"
    - "Al guardar credenciales válidas, se muestra el número formateado y el nombre del negocio (retornados por Meta)"
    - "Al guardar credenciales inválidas, se muestra el error descriptivo de Meta API"
    - "En el perfil del paciente, hay un toggle visible de consentimiento WhatsApp con estado y fecha de aceptación"
    - "El toggle actualiza el backend y refleja el cambio en UI sin recargar la página"
    - "SECRETARIA y FACTURADOR NO ven la tab de WhatsApp en Configuración"
  artifacts:
    - path: "frontend/src/app/dashboard/configuracion/components/WhatsappConfigTab.tsx"
      provides: "Formulario de configuración WABA con validación y feedback de Meta"
      min_lines: 80
    - path: "frontend/src/hooks/useWabaConfig.ts"
      provides: "TanStack Query hook para GET /whatsapp/config"
      exports: ["useWabaConfig"]
    - path: "frontend/src/hooks/useSaveWabaConfig.ts"
      provides: "Mutation hook para POST /whatsapp/config"
      exports: ["useSaveWabaConfig"]
    - path: "frontend/src/app/dashboard/pacientes/components/WhatsappOptInToggle.tsx"
      provides: "Toggle de consentimiento con fecha en perfil paciente"
      exports: ["WhatsappOptInToggle"]
    - path: "frontend/src/hooks/useUpdateWhatsappOptIn.ts"
      provides: "Mutation hook para PATCH /pacientes/:id/whatsapp-opt-in"
      exports: ["useUpdateWhatsappOptIn"]
  key_links:
    - from: "frontend/src/app/dashboard/configuracion/components/WhatsappConfigTab.tsx"
      to: "/api/whatsapp/config"
      via: "useSaveWabaConfig mutation hook"
      pattern: "useSaveWabaConfig"
    - from: "frontend/src/app/dashboard/pacientes/components/WhatsappOptInToggle.tsx"
      to: "/api/pacientes/:id/whatsapp-opt-in"
      via: "useUpdateWhatsappOptIn mutation hook"
      pattern: "useUpdateWhatsappOptIn"
    - from: "frontend/src/app/dashboard/configuracion/page.tsx"
      to: "WhatsappConfigTab"
      via: "tab condicional para ADMIN y PROFESIONAL"
      pattern: "WhatsApp"
    - from: "frontend/src/app/dashboard/pacientes/components/WhatsappOptInToggle.tsx"
      to: "frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx"
      via: "import y render del componente en la sección de datos de contacto del paciente"
      pattern: "WhatsappOptInToggle"
---

<objective>
Construir la UI de configuración WABA en la página de Settings (tab condicional visible solo para ADMIN/PROFESIONAL) y el toggle de consentimiento WhatsApp en el perfil del paciente. Ambas UIs consumen los endpoints del plan 01-02.

Purpose: INFRA-02 e INFRA-03 requieren que los usuarios puedan configurar credenciales y gestionar opt-ins. Sin UI, los endpoints del plan 01-02 no son accionables.
Output: Tab de WhatsApp en Configuración + toggle de opt-in en perfil paciente, conectados al backend via hooks TanStack Query.
</objective>

<execution_context>
@/Users/laucaballero/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-infraestructura-async/01-CONTEXT.md
@.planning/phases/01-infraestructura-async/01-02-SUMMARY.md

Codebase relevante:
@frontend/src/app/dashboard/configuracion/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hooks TanStack Query para WABA config y opt-in</name>
  <files>
    frontend/src/hooks/useWabaConfig.ts
    frontend/src/hooks/useSaveWabaConfig.ts
    frontend/src/hooks/useUpdateWhatsappOptIn.ts
  </files>
  <action>
Crear los 3 hooks siguiendo los patrones del proyecto (TanStack Query + axios instance de `@/lib/api`).

Verificar primero el patrón exacto de hooks existentes:
```bash
cat frontend/src/hooks/usePacientes.ts 2>/dev/null | head -30
# o algún hook de mutation existente
cat frontend/src/hooks/useCreatePaciente.ts 2>/dev/null | head -30
```

**1. `frontend/src/hooks/useWabaConfig.ts`:**
```typescript
import { useQuery } from '@tanstack/react-query';
import api from '@/lib/api';

interface WabaConfigResponse {
  phoneNumberId: string;
  displayPhone: string;
  verifiedName?: string;
  activo: boolean;
}

export function useWabaConfig() {
  return useQuery<WabaConfigResponse | null>({
    queryKey: ['waba-config'],
    queryFn: async () => {
      const res = await api.get('/whatsapp/config');
      return res.data;
    },
  });
}
```

**2. `frontend/src/hooks/useSaveWabaConfig.ts`:**
```typescript
import { useMutation, useQueryClient } from '@tanstack/react-query';
import api from '@/lib/api';

interface SaveWabaConfigInput {
  phoneNumberId: string;
  accessToken: string;
  wabaId?: string;
}

export function useSaveWabaConfig() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (data: SaveWabaConfigInput) => {
      const res = await api.post('/whatsapp/config', data);
      return res.data;
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['waba-config'] });
    },
  });
}
```

**3. `frontend/src/hooks/useUpdateWhatsappOptIn.ts`:**
```typescript
import { useMutation, useQueryClient } from '@tanstack/react-query';
import api from '@/lib/api';

interface UpdateOptInInput {
  pacienteId: string;
  optIn: boolean;
}

export function useUpdateWhatsappOptIn() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async ({ pacienteId, optIn }: UpdateOptInInput) => {
      const res = await api.patch(`/pacientes/${pacienteId}/whatsapp-opt-in`, { optIn });
      return res.data;
    },
    onSuccess: (_data, variables) => {
      // Invalidar query del paciente para refrescar opt-in status
      queryClient.invalidateQueries({ queryKey: ['paciente', variables.pacienteId] });
    },
  });
}
```

Ajustar los `queryKey` para que coincidan exactamente con los keys usados por los hooks existentes `usePaciente.ts` (para invalidación correcta).
  </action>
  <verify>
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend
npm run build 2>&1 | grep -E "error" | grep -v "\.next/types" | head -20
```
Cero errores TypeScript en los 3 nuevos hooks.
  </verify>
  <done>
- Los 3 hooks existen y compilan sin errores TypeScript
- Usan la instancia `api` de `@/lib/api` (no fetch directo)
- `useSaveWabaConfig` invalida `waba-config` query key en onSuccess
- `useUpdateWhatsappOptIn` invalida el query del paciente correcto en onSuccess
  </done>
</task>

<task type="auto">
  <name>Task 2: Componentes UI — WhatsappConfigTab, WhatsappOptInToggle y render en PacienteDetails</name>
  <files>
    frontend/src/app/dashboard/configuracion/components/WhatsappConfigTab.tsx
    frontend/src/app/dashboard/pacientes/components/WhatsappOptInToggle.tsx
    frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx
    frontend/src/app/dashboard/configuracion/page.tsx
  </files>
  <action>
**1. Crear `frontend/src/app/dashboard/configuracion/components/WhatsappConfigTab.tsx`:**

Diseño visual a discreción de Claude (user decision: "Diseño visual del formulario de Settings de WhatsApp"), usando componentes shadcn/ui del proyecto:
- Card con título "WhatsApp Business" y descripción
- Si ya hay configuración guardada (useWabaConfig retorna data): mostrar estado "Conectado" con número formateado y nombre verificado como badge verde; mostrar botón "Actualizar configuración"
- Si no hay configuración: mostrar formulario de conexión
- Formulario: campos "Phone Number ID" y "Access Token" (type="password" para el token), campo opcional "WABA ID"
- Agregar nota/disclaimer bajo el campo Access Token: "El token se cifra antes de guardarse. Solo ingresalo si estás reconectando."
- Botón "Conectar WhatsApp" — en estado loading mientras mutación pendiente
- Si la mutación falla: mostrar el error de Meta API debajo del botón (usar `toast` del proyecto si existe, o inline error)
- Si la mutación tiene éxito: mostrar el número formateado y nombre verificado retornados por la respuesta

```typescript
'use client';
import { useState } from 'react';
import { useWabaConfig } from '@/hooks/useWabaConfig';
import { useSaveWabaConfig } from '@/hooks/useSaveWabaConfig';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Badge } from '@/components/ui/badge';
// Completar con el patrón de manejo de errores del proyecto
```

**2. Crear `frontend/src/app/dashboard/pacientes/components/WhatsappOptInToggle.tsx`:**

Buscar primero si ya existe un componente de toggle/switch en el proyecto para el perfil del paciente, y replicar el patrón:
```bash
ls frontend/src/app/dashboard/pacientes/components/ 2>/dev/null
```

Componente recibe props: `{ pacienteId: string; optIn: boolean; optInAt?: string | null }`
- Usar componente `Switch` de shadcn/ui (de `@/components/ui/switch`)
- Label: "Acepta mensajes por WhatsApp"
- Si `optIn=true` y `optInAt` existe: mostrar fecha de aceptación como texto muted debajo (ej: "Aceptó el 15 feb 2026")
- Si `optIn=false`: mostrar texto muted "Sin consentimiento registrado"
- En estado loading (mutación pendiente): deshabilitar el switch
- Al cambiar el switch: llamar `useUpdateWhatsappOptIn` con el nuevo valor

**3. Actualizar `frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx`:**

Agregar `WhatsappOptInToggle` al perfil del paciente para que el toggle sea visible y editable:

```bash
# Verificar la estructura del componente antes de editar
head -100 frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx
```

- Importar el componente al inicio del archivo:
```typescript
import { WhatsappOptInToggle } from './WhatsappOptInToggle';
```

- Agregar el toggle en la sección de datos de contacto (después de teléfono/email, antes de los botones de acción). Buscar en el componente existente la sección de datos de contacto y añadir el toggle ahí usando los campos del paciente:
```tsx
<WhatsappOptInToggle
  pacienteId={paciente.id}
  optIn={paciente.whatsappOptIn ?? false}
  optInAt={paciente.whatsappOptInAt}
/>
```

**IMPORTANTE:** Si `whatsappOptIn` y `whatsappOptInAt` no están aún en el tipo `paciente` (porque el tipo TypeScript no se actualizó con la migración Prisma), usar `(paciente as any).whatsappOptIn` temporalmente y agregar un comentario `// TODO: actualizar tipo Paciente tras regenerar Prisma client`.

**4. Actualizar `frontend/src/app/dashboard/configuracion/page.tsx`:**

Agregar tab "WhatsApp" en AMBOS bloques de Tabs (ADMIN y PROFESIONAL):

Para ADMIN: agregar antes de `<TabsTrigger value="reportes">Reportes</TabsTrigger>`:
```tsx
<TabsTrigger value="whatsapp">WhatsApp</TabsTrigger>
```
Y en TabsContent:
```tsx
<TabsContent value="whatsapp" className="mt-6">
  <WhatsappConfigTab />
</TabsContent>
```

Para PROFESIONAL: misma adición. Ajustar el `grid-cols-7` a `grid-cols-8` si se agrega una tab más.

Importar `WhatsappConfigTab` al inicio del archivo.

**IMPORTANTE (locked decision de CONTEXT.md):**
- SOLO visible para ADMIN y PROFESIONAL — NO agregar para ningún otro rol
- El estado de conexión NO se agrega al sidebar ni a ningún otro lugar — SOLO en la página de Settings
- NO implementar wizard de onboarding (deferred)
  </action>
  <verify>
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend
npm run build 2>&1 | grep -E "error" | grep -v "\.next/types" | head -20
```
Build limpio. Verificar también que el `grid-cols` se actualizó si se cambió el número de tabs.
  </verify>
  <done>
- `WhatsappConfigTab` existe y muestra estado conectado/desconectado según la respuesta de `useWabaConfig`
- El formulario tiene campos phoneNumberId, accessToken (type=password), wabaId (opcional)
- Errores de Meta API se muestran al usuario en el formulario
- `WhatsappOptInToggle` muestra el switch con fecha de aceptación cuando optIn=true
- `PacienteDetails.tsx` importa y renderiza `WhatsappOptInToggle` con los campos `whatsappOptIn` y `whatsappOptInAt` del paciente
- Tab "WhatsApp" visible en Configuración para ADMIN y PROFESIONAL únicamente
- Build TypeScript sin errores en los nuevos archivos
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificar UI de WhatsApp (Settings + perfil paciente)</name>
  <action>Verificación humana de la UI construida en Tasks 1 y 2 de este plan.</action>
  <what-built>
    UI completa de Phase 1:
    1. Tab "WhatsApp" en Configuración (ADMIN y PROFESIONAL) con formulario de conexión WABA
    2. Toggle de consentimiento WhatsApp en perfil del paciente con fecha de aceptación
    3. Hooks TanStack Query conectados al backend
  </what-built>
  <how-to-verify>
    1. Ir a http://localhost:3000/dashboard/configuracion con rol ADMIN o PROFESIONAL
    2. Verificar que aparece la tab "WhatsApp" en la lista de tabs
    3. Hacer clic en la tab — verificar que se muestra el formulario con campos Phone Number ID y Access Token
    4. Verificar que SECRETARIA/FACTURADOR NO ven la tab (cambiar rol en sesión si es posible)
    5. Abrir el perfil de un paciente cualquiera
    6. Buscar el toggle de consentimiento WhatsApp — verificar que existe y es accionable
    7. Activar el toggle — verificar que el cambio persiste al recargar la página
    8. (Opcional si tienes credenciales reales de Meta) Ingresar credenciales inválidas → verificar mensaje de error; credenciales válidas → verificar número formateado mostrado
  </how-to-verify>
  <resume-signal>Escribir "aprobado" si la UI se ve y funciona correctamente, o describir los problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
1. Tab "WhatsApp" visible en Configuración para ADMIN y PROFESIONAL
2. Tab "WhatsApp" NO visible para SECRETARIA, FACTURADOR, PACIENTE
3. Formulario WABA con campos correctos y manejo de errores
4. Toggle opt-in en perfil paciente con estado y fecha
5. `PacienteDetails.tsx` renderiza `WhatsappOptInToggle` — el toggle es visible al abrir el drawer de un paciente
6. `npm run build` en frontend/ sin errores TypeScript en archivos nuevos
7. Estado de conexión SOLO en página Settings (no en sidebar ni topbar)
</verification>

<success_criteria>
- Tab de WhatsApp en Configuración visible y funcional para los roles correctos
- Toggle de opt-in en perfil paciente persistiendo cambios via API
- Formulario WABA validando con Meta API en tiempo real y mostrando errores descriptivos
- Zero badges o indicadores de WhatsApp fuera de la página de Settings
</success_criteria>

<output>
Después de completar, crear `.planning/phases/01-infraestructura-async/01-03-SUMMARY.md` con:
- Confirmación del resultado del checkpoint humano
- Lista de componentes creados
- Ajustes realizados al patrón de configuracion/page.tsx (número de grid-cols si se cambió)
</output>
