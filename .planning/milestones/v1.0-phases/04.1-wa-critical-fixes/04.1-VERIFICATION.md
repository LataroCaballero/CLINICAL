---
phase: 04.1-wa-critical-fixes
verified: 2026-03-02T00:00:00Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---

# Phase 4.1: WA Critical Fixes — Verification Report

**Phase Goal:** Close three critical WhatsApp integration gaps: SECRETARIA role causes NotFoundException on all WA endpoints, SendWAMessageModal email channel always 404s, and BACKEND_URL is undocumented causing production PDF URLs to resolve to localhost.
**Verified:** 2026-03-02
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SECRETARIA can POST /whatsapp/mensajes, GET /whatsapp/mensajes/:pacienteId, POST /whatsapp/mensajes/free-text, POST /whatsapp/mensajes/:id/retry, POST /whatsapp/presupuesto/:id/send, GET /whatsapp/unread without NotFoundException | VERIFIED | All 7 endpoints carry `@Auth('ADMIN', 'PROFESIONAL', 'SECRETARIA')` + null-guard pattern; each resolves `profesionalId` via DB lookup or returns graceful response |
| 2 | GET /whatsapp/templates returns BadRequestException with a clear message when SECRETARIA calls it | VERIFIED | Lines 70-79 of controller: `@Auth('ADMIN', 'PROFESIONAL', 'SECRETARIA')`, null-guard at line 73 throws `BadRequestException('SECRETARIA debe estar asociada a un profesional para listar templates')` |
| 3 | GET /whatsapp/unread returns empty object {} for SECRETARIA role (no crash, no NotFoundException) | VERIFIED | Lines 164-171: `if (!profesionalId) return {};` — explicit early return documented in JSDoc comment |
| 4 | SendWAMessageModal shows only the WhatsApp channel — no email toggle, no email branch, no Mail import, no isSendingEmail state | VERIFIED | grep for `Channel\|isSendingEmail\|enviar-email\|Mail\|channel === 'email'` returns no matches; file has only `MessageSquare` and `Check` from lucide-react; `isConfirming = sendWA.isPending` (no email state) |
| 5 | BACKEND_URL is documented in CLAUDE.md under Environment Variables (Backend) and exists in backend/.env.example with usage comment | VERIFIED | CLAUDE.md line 110 contains full BACKEND_URL documentation with production warning; `backend/.env.example` confirmed in git (commit 70cfb9d) with all 6 vars and `# MUST be a publicly accessible URL in production — Meta cannot fetch localhost URLs` comment |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/src/modules/whatsapp/whatsapp.controller.ts` | Null-guarded WhatsappController with PrismaService injected | VERIFIED | File is 237 lines. `private readonly prisma: PrismaService` at line 24. 12 null-guard checks (`if (!profesionalId)`) across all 7 SECRETARIA endpoints. No `NEXT_PUBLIC_API_URL` in backend runtime. `BACKEND_URL ?? 'http://localhost:3001'` with `Logger.warn` at lines 222-227. |
| `frontend/src/components/whatsapp/SendWAMessageModal.tsx` | WA-only send modal without email channel (min 100 lines) | VERIFIED | File is 147 lines (above min_lines: 100). Imports: only `useState` from react, `toast` from sonner, `MessageSquare, Check` from lucide-react, dialog/button/skeleton UI components, `useWATemplates, useSendWATemplate, WATemplate` hooks. No `Mail`, no `Channel` type, no `isSendingEmail`, no `api` import, no email branch. |
| `CLAUDE.md` | BACKEND_URL documentation | VERIFIED | Line 110 contains `BACKEND_URL` with full description, example value, and production warning about Meta not reaching localhost. |
| `backend/.env.example` | All required backend env vars with comments | VERIFIED | Confirmed in git at commit 70cfb9d. Contains: DATABASE_URL, JWT_SECRET, ENCRYPTION_KEY, BACKEND_URL (with production usage warning comment), REDIS_HOST, REDIS_PORT — 6 vars total. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `whatsapp.controller.ts` | `this.prisma.paciente.findUnique` | `private readonly prisma: PrismaService` in constructor | WIRED | Line 24: `private readonly prisma: PrismaService`. Used at lines 90, 114, 138, 210 for paciente lookups, and line 182 for `mensajeWhatsApp` lookup. |
| `whatsapp.controller.ts` | `profesionalId` fallback | null-guard pattern `if (!profesionalId)` | WIRED | 12 occurrences of `if (!profesionalId)` confirmed. Pattern matches Phase 2.1 established standard from `pacientes.controller.ts`. |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| WA-01 | 04.1-01-PLAN.md | El coordinador puede enviar un mensaje WhatsApp a un paciente desde su perfil usando templates aprobados por Meta | SATISFIED | SECRETARIA null-guard enables all send endpoints; email channel removal makes modal WA-only with no broken 404 path |
| WA-05 | 04.1-01-PLAN.md | Si un mensaje falla por WhatsApp, el sistema notifica al coordinador con opción de reintentar o enviar por email | SATISFIED | POST /whatsapp/mensajes/:id/retry now null-guarded for SECRETARIA via `mensajeWhatsApp.profesionalId` lookup; retry logic itself was already implemented in Phase 4 |
| PRES-04 | 04.1-01-PLAN.md | El coordinador puede enviar el presupuesto por WhatsApp (como documento PDF) directamente desde la plataforma | SATISFIED | sendPresupuesto endpoint null-guarded; `BACKEND_URL ?? 'http://localhost:3001'` with Logger.warn replaces broken `NEXT_PUBLIC_API_URL` fallback; BACKEND_URL documented so production deployments can configure it correctly |

**Orphaned requirements check:** REQUIREMENTS.md traceability table maps WA-01, WA-05, and PRES-04 to Phase 4.1. All three are claimed in the PLAN frontmatter. No orphaned requirements.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `whatsapp.controller.ts` | 31 | `TODO: remover antes de Phase 4 o mover a admin-only route dedicada` (test-queue endpoint) | Info | Pre-existing TODO from Phase 1, unrelated to Phase 4.1 scope. Endpoint is ADMIN-only so no SECRETARIA exposure. No impact on phase goal. |

No blocker or warning anti-patterns found in Phase 4.1 modified code.

---

### Human Verification Required

None. All automated checks passed and the three gap closures are verifiable by code inspection:

1. The null-guard pattern is exhaustive and matches the established Phase 2.1 approach exactly.
2. The email channel removal is complete — no residual email code exists in the modal.
3. Documentation is in place in both CLAUDE.md and backend/.env.example.

The only item that could benefit from manual smoke testing is the end-to-end SECRETARIA flow in a running instance, but this is runtime verification (not a code gap). The code path is complete.

---

### Gaps Summary

No gaps found. All five must-have truths are verified:

- All 7 SECRETARIA-accessible WhatsApp endpoints have null-guards that resolve `profesionalId` via DB entity lookups. The `/whatsapp/unread` endpoint returns `{}` explicitly. The `/whatsapp/templates` endpoint returns `BadRequestException` with a developer-clear message.
- `SendWAMessageModal.tsx` contains zero email-related code. The file is clean WA-only at 147 lines.
- `BACKEND_URL` is documented in CLAUDE.md with a production warning and exists in `backend/.env.example` (confirmed in git) with a comment about Meta's inability to fetch localhost URLs.
- The `NEXT_PUBLIC_API_URL` (frontend-only env var) has been removed from the backend PDF URL fallback chain, replaced with `BACKEND_URL ?? 'http://localhost:3001'` plus `Logger.warn`.

Phase 4.1 goal is fully achieved. WA-01, WA-05, and PRES-04 requirements are satisfied. Phase 5 can begin.

---

_Verified: 2026-03-02_
_Verifier: Claude (gsd-verifier)_
