---
phase: 05-dashboard-de-conversion
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/hooks/usePeriodoFilter.ts
  - frontend/src/components/crm/PeriodoSelector.tsx
  - frontend/src/hooks/useCRMFunnel.ts
  - frontend/src/hooks/useCRMKpis.ts
  - frontend/src/hooks/useCRMLossReasons.ts
  - frontend/src/hooks/useCRMPipelineIncome.ts
  - frontend/src/hooks/useCoordinatorPerformance.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "usePeriodoFilter persiste la selección en localStorage y la restaura al montar"
    - "PeriodoSelector renderiza tres botones (Esta semana / Este mes / Este trimestre) y resalta el activo"
    - "Cada hook de datos acepta profesionalId + periodo y hace fetch al endpoint correspondiente"
    - "Los hooks retornan { data, isLoading, error } mediante TanStack Query"
  artifacts:
    - path: "frontend/src/hooks/usePeriodoFilter.ts"
      provides: "Hook usePeriodoFilter(storageKey, defaultPeriodo) → [periodo, setPeriodo]"
      exports: ["usePeriodoFilter", "Periodo"]
    - path: "frontend/src/components/crm/PeriodoSelector.tsx"
      provides: "Componente UI de 3 botones para seleccionar período"
      exports: ["PeriodoSelector"]
    - path: "frontend/src/hooks/useCRMFunnel.ts"
      provides: "Hook GET /reportes/crm/funnel"
      exports: ["useCRMFunnel"]
    - path: "frontend/src/hooks/useCRMKpis.ts"
      provides: "Hook GET /reportes/crm/kpis"
      exports: ["useCRMKpis"]
    - path: "frontend/src/hooks/useCRMLossReasons.ts"
      provides: "Hook GET /reportes/crm/motivos-perdida"
      exports: ["useCRMLossReasons"]
    - path: "frontend/src/hooks/useCRMPipelineIncome.ts"
      provides: "Hook GET /reportes/crm/pipeline-income"
      exports: ["useCRMPipelineIncome"]
    - path: "frontend/src/hooks/useCoordinatorPerformance.ts"
      provides: "Hook GET /reportes/crm/coordinator-performance — retorna CoordinatorPerformanceData con array coordinadores"
      exports: ["useCoordinatorPerformance", "CoordinadorRow", "CoordinatorPerformanceData"]
  key_links:
    - from: "frontend/src/hooks/useCRMFunnel.ts"
      to: "/reportes/crm/funnel"
      via: "api.get con query params"
      pattern: "reportes/crm/funnel"
    - from: "frontend/src/components/crm/PeriodoSelector.tsx"
      to: "frontend/src/hooks/usePeriodoFilter.ts"
      via: "import Periodo type"
      pattern: "from.*usePeriodoFilter"
---

<objective>
Crear la infraestructura de datos del frontend: hook reutilizable de filtro de período con persistencia localStorage, componente UI PeriodoSelector y los 5 hooks TanStack Query que conectan con los nuevos endpoints del backend.

Purpose: Los widgets del Plan 03 necesitan hooks tipados y el selector de período antes de ser construidos. Separar esta capa de datos de los componentes UI evita que el Plan 03 sea demasiado grande.
Output: 7 archivos nuevos (2 utilitarios compartidos + 5 hooks de datos) listos para ser importados por los widgets.
</objective>

<execution_context>
@/Users/laucaballero/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-de-conversion/05-01-SUMMARY.md
@.planning/phases/05-dashboard-de-conversion/05-RESEARCH.md
@frontend/src/hooks/useCRMMetrics.ts
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook usePeriodoFilter y componente PeriodoSelector</name>
  <files>
    frontend/src/hooks/usePeriodoFilter.ts
    frontend/src/components/crm/PeriodoSelector.tsx
  </files>
  <action>
**`frontend/src/hooks/usePeriodoFilter.ts`:**

```typescript
import { useState, useEffect } from 'react';

export type Periodo = 'semana' | 'mes' | 'trimestre';

export function usePeriodoFilter(storageKey: string, defaultPeriodo: Periodo = 'mes') {
  const [periodo, setPeriodo] = useState<Periodo>(() => {
    if (typeof window === 'undefined') return defaultPeriodo;
    const stored = localStorage.getItem(storageKey);
    return (stored === 'semana' || stored === 'mes' || stored === 'trimestre')
      ? stored
      : defaultPeriodo;
  });

  useEffect(() => {
    localStorage.setItem(storageKey, periodo);
  }, [storageKey, periodo]);

  return [periodo, setPeriodo] as const;
}
```

**`frontend/src/components/crm/PeriodoSelector.tsx`:**

```tsx
"use client";

import { Periodo } from '@/hooks/usePeriodoFilter';

const OPCIONES: { value: Periodo; label: string }[] = [
  { value: 'semana', label: 'Esta semana' },
  { value: 'mes', label: 'Este mes' },
  { value: 'trimestre', label: 'Este trimestre' },
];

interface Props {
  value: Periodo;
  onChange: (p: Periodo) => void;
}

export function PeriodoSelector({ value, onChange }: Props) {
  return (
    <div className="flex gap-1 text-xs">
      {OPCIONES.map(op => (
        <button
          key={op.value}
          type="button"
          onClick={() => onChange(op.value)}
          className={`px-2 py-1 rounded transition-colors ${
            value === op.value
              ? 'bg-blue-50 text-blue-700 font-medium'
              : 'text-gray-500 hover:text-gray-700'
          }`}
        >
          {op.label}
        </button>
      ))}
    </div>
  );
}
```
  </action>
  <verify>
Verificar que los archivos existen y exportan los symbols correctos:
```bash
grep -n "export" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/hooks/usePeriodoFilter.ts
grep -n "export" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/components/crm/PeriodoSelector.tsx
```
  </verify>
  <done>`usePeriodoFilter` y `Periodo` exportados desde el hook. `PeriodoSelector` exportado desde el componente. Ambos con TypeScript correcto.</done>
</task>

<task type="auto">
  <name>Task 2: Los 5 hooks de datos TanStack Query</name>
  <files>
    frontend/src/hooks/useCRMFunnel.ts
    frontend/src/hooks/useCRMKpis.ts
    frontend/src/hooks/useCRMLossReasons.ts
    frontend/src/hooks/useCRMPipelineIncome.ts
    frontend/src/hooks/useCoordinatorPerformance.ts
  </files>
  <action>
Crear los 5 hooks siguiendo el patrón de `useCRMMetrics.ts` (useQuery + api.get + queryKey con profId + periodo).

**`frontend/src/hooks/useCRMFunnel.ts`:**
```typescript
import { useQuery } from '@tanstack/react-query';
import api from '@/lib/api';

export interface FunnelEtapa {
  etapa: string;
  count: number;
}

export interface TasaPaso {
  de: string;
  a: string;
  tasa: number | null;
}

export interface FunnelData {
  etapas: FunnelEtapa[];
  tasasPaso: TasaPaso[];
  perdidos: {
    total: number;
    porMotivo: { motivo: string; count: number }[];
  };
}

export function useCRMFunnel(profesionalId: string | null) {
  return useQuery<FunnelData>({
    queryKey: ['crm-funnel', profesionalId],
    queryFn: () =>
      api.get(`/reportes/crm/funnel?profesionalId=${profesionalId}`).then(r => r.data),
    enabled: !!profesionalId,
    staleTime: 1000 * 60 * 5,
  });
}
```

**`frontend/src/hooks/useCRMKpis.ts`:**
```typescript
import { useQuery } from '@tanstack/react-query';
import api from '@/lib/api';
import { Periodo } from './usePeriodoFilter';

export interface CRMKpisData {
  periodo: { inicio: string; fin: string };
  nuevos: number;
  confirmados: number;
  totalActivos: number;
  tasaConversion: number;
}

export function useCRMKpis(profesionalId: string | null, periodo: Periodo = 'mes') {
  return useQuery<CRMKpisData>({
    queryKey: ['crm-kpis', profesionalId, periodo],
    queryFn: () =>
      api.get(`/reportes/crm/kpis?profesionalId=${profesionalId}&periodo=${periodo}`).then(r => r.data),
    enabled: !!profesionalId,
    staleTime: 1000 * 60 * 5,
  });
}
```

**`frontend/src/hooks/useCRMLossReasons.ts`:**
```typescript
import { useQuery } from '@tanstack/react-query';
import api from '@/lib/api';
import { Periodo } from './usePeriodoFilter';

export interface MotivoPerdida {
  motivo: string;
  count: number;
  porcentaje: number;
}

export interface LossReasonsData {
  total: number;
  motivos: MotivoPerdida[];
}

export function useCRMLossReasons(profesionalId: string | null, periodo: Periodo = 'mes') {
  return useQuery<LossReasonsData>({
    queryKey: ['crm-loss-reasons', profesionalId, periodo],
    queryFn: () =>
      api.get(`/reportes/crm/motivos-perdida?profesionalId=${profesionalId}&periodo=${periodo}`).then(r => r.data),
    enabled: !!profesionalId,
    staleTime: 1000 * 60 * 5,
  });
}
```

**`frontend/src/hooks/useCRMPipelineIncome.ts`:**
```typescript
import { useQuery } from '@tanstack/react-query';
import api from '@/lib/api';
import { Periodo } from './usePeriodoFilter';

export interface PipelineIncomeData {
  total: number;
  count: number;
}

export function useCRMPipelineIncome(profesionalId: string | null, periodo: Periodo = 'mes') {
  return useQuery<PipelineIncomeData>({
    queryKey: ['crm-pipeline-income', profesionalId, periodo],
    queryFn: () =>
      api.get(`/reportes/crm/pipeline-income?profesionalId=${profesionalId}&periodo=${periodo}`).then(r => r.data),
    enabled: !!profesionalId,
    staleTime: 1000 * 60 * 5,
  });
}
```

**`frontend/src/hooks/useCoordinatorPerformance.ts`:**
```typescript
import { useQuery } from '@tanstack/react-query';
import api from '@/lib/api';
import { Periodo } from './usePeriodoFilter';

export interface CoordinadorRow {
  nombre: string;
  interacciones: number;
  pacientesContactados: number;
  porcentajeConversion: number | null;
}

export interface CoordinatorPerformanceData {
  periodo: { inicio: string; fin: string };
  coordinadores: CoordinadorRow[];
}

export function useCoordinatorPerformance(profesionalId: string | null, periodo: Periodo = 'semana') {
  return useQuery<CoordinatorPerformanceData>({
    queryKey: ['coordinator-performance', profesionalId, periodo],
    queryFn: () =>
      api.get(`/reportes/crm/coordinator-performance?profesionalId=${profesionalId}&periodo=${periodo}`).then(r => r.data),
    enabled: !!profesionalId,
    staleTime: 1000 * 60 * 5,
  });
}
```
  </action>
  <verify>
```bash
ls /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/hooks/useCRM*.ts /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/hooks/useCoordinator*.ts 2>&1
```
Los 5 archivos existen.
  </verify>
  <done>Los 5 hooks existen, exportan su función principal y los tipos de datos, usan `enabled: !!profesionalId`, staleTime de 5 minutos, y queryKey incluye profId + periodo para cache correcto.</done>
</task>

</tasks>

<verification>
1. Los 7 archivos nuevos existen en las rutas indicadas
2. `usePeriodoFilter` tiene lazy init de localStorage (evita SSR crash con `typeof window === 'undefined'`)
3. `PeriodoSelector` tiene `type="button"` en cada botón (evita submit de form accidental)
4. Cada hook tiene `enabled: !!profesionalId` para no disparar fetch con null
5. Los queryKeys incluyen `periodo` para cache independiente por período
</verification>

<success_criteria>
- 7 nuevos archivos creados
- `Periodo` type y `usePeriodoFilter` exportados correctamente
- `PeriodoSelector` componente listo para uso
- 5 hooks de datos con tipos correctos y queryKeys diferenciados por período
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-de-conversion/05-02-SUMMARY.md`
</output>
