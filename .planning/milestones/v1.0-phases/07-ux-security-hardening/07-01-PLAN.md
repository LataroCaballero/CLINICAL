---
phase: 07-ux-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/dashboard/pacientes/components/ContactosSection.tsx
  - backend/src/main.ts
  - backend/src/modules/whatsapp/guards/whatsapp-hmac.guard.ts
  - backend/src/modules/whatsapp/whatsapp-webhook.controller.ts
  - backend/src/modules/whatsapp/whatsapp.module.ts
  - backend/.env.example
autonomous: true
requirements:
  - LOG-02
  - WA-04

must_haves:
  truths:
    - "El botón 'Ver todos (N)' en ContactosSection expande la lista en el lugar mostrando todos los contactos del paciente (no solo los primeros 5)"
    - "POST /webhook/whatsapp rechaza con 403 cualquier request cuya firma x-hub-signature-256 no coincida con HMAC-SHA256 del body firmado con META_APP_SECRET"
    - "GET /webhook/whatsapp (Meta challenge handshake) sigue respondiendo sin que el guard HMAC interfiera"
    - "Webhooks legítimos con firma válida siguen procesándose correctamente a través de BullMQ"
  artifacts:
    - path: "frontend/src/app/dashboard/pacientes/components/ContactosSection.tsx"
      provides: "showAll state toggle + expanded contact list rendering"
      contains: "useState(false)"
    - path: "backend/src/modules/whatsapp/guards/whatsapp-hmac.guard.ts"
      provides: "HMAC-SHA256 signature verification guard"
      exports: ["WhatsappHmacGuard"]
    - path: "backend/src/main.ts"
      provides: "rawBody: true para NestFactory"
      contains: "rawBody: true"
    - path: "backend/src/modules/whatsapp/whatsapp-webhook.controller.ts"
      provides: "Guard aplicado solo al POST handler"
      contains: "UseGuards(WhatsappHmacGuard)"
  key_links:
    - from: "ContactosSection.tsx"
      to: "useContactos(pacienteId, undefined)"
      via: "showAll === true → no limit param"
      pattern: "showAll \\? undefined : 5"
    - from: "whatsapp-webhook.controller.ts POST handler"
      to: "WhatsappHmacGuard"
      via: "@UseGuards(WhatsappHmacGuard) on @Post only"
      pattern: "@UseGuards\\(WhatsappHmacGuard\\)"
    - from: "WhatsappHmacGuard"
      to: "req.rawBody"
      via: "NestFactory rawBody: true option"
      pattern: "rawBody: true"
---

<objective>
Cierra los dos últimos requirements abiertos del milestone CRM Conversión v1:

- LOG-02: El perfil del paciente permite ver el historial completo de contactos (no solo los primeros 5) mediante un toggle in-place en ContactosSection.
- WA-04: El webhook POST de Meta verifica la firma HMAC-SHA256 (x-hub-signature-256) antes de encolar en BullMQ, rechazando payloads no firmados por Meta con 403.

Purpose: Cierra el milestone — una vez ejecutado este plan, todos los 35 requirements v1 están completos.
Output: ContactosSection con expand in-place + WhatsappHmacGuard en el POST webhook
</objective>

<execution_context>
@/Users/laucaballero/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ux-security-hardening/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LOG-02 — Expandir historial completo de contactos en ContactosSection</name>
  <files>frontend/src/app/dashboard/pacientes/components/ContactosSection.tsx</files>
  <action>
    Añadir state local `showAll` (boolean, false por defecto) al componente ContactosSection.
    Cambiar la llamada a `useContactos` para pasar `undefined` como limit cuando `showAll === true`:

    ```typescript
    const [showAll, setShowAll] = useState(false);
    const { data, isLoading } = useContactos(pacienteId, showAll ? undefined : 5);
    ```

    Añadir `useState` al import de React si no está ya importado.

    Reemplazar el botón stub "Ver todos ({total})" — que tiene un comentario `/* future */` vacío — con un onClick real:

    ```tsx
    {total > 5 && !showAll && (
      <button
        type="button"
        className="w-full text-xs text-primary hover:underline py-1"
        onClick={() => setShowAll(true)}
      >
        Ver todos ({total})
      </button>
    )}
    {showAll && total > 5 && (
      <button
        type="button"
        className="w-full text-xs text-muted-foreground hover:underline py-1"
        onClick={() => setShowAll(false)}
      >
        Mostrar menos
      </button>
    )}
    ```

    Importante:
    - El query key en useContactos es `["contactos", pacienteId]` — al cambiar el limit parameter, TanStack Query refetchea con los nuevos params. No crear un segundo query key.
    - NO usar Dialog ni Sheet para esta expansión — in-place es suficiente y evita el conflicto z-index de Drawer-in-Sheet ya documentado en STATE.md [02-02].
    - La nota "c.nota" ya se trunca con `truncate` en el CSS — cuando se expande la lista full, quitar el `truncate` para mostrar notas completas (cambiar clase a `text-xs text-muted-foreground mt-0.5 whitespace-pre-wrap break-words`).
  </action>
  <verify>
    `cd frontend && npm run build` completa sin errores TypeScript.
    Inspección visual: abrir perfil de paciente con más de 5 contactos → aparece botón "Ver todos (N)" → al hacer click se expanden todos → aparece "Mostrar menos" → al hacer click vuelve a 5.
  </verify>
  <done>
    ContactosSection.tsx tiene `useState(false)` para showAll.
    `useContactos` recibe `showAll ? undefined : 5`.
    El botón "Ver todos" tiene onClick funcional que llama `setShowAll(true)`.
    El botón "Mostrar menos" aparece cuando showAll=true y total > 5.
    Build del frontend pasa sin errores.
  </done>
</task>

<task type="auto">
  <name>Task 2: WA-04 — HMAC guard + rawBody para verificación de webhook de Meta</name>
  <files>
    backend/src/main.ts
    backend/src/modules/whatsapp/guards/whatsapp-hmac.guard.ts
    backend/src/modules/whatsapp/whatsapp-webhook.controller.ts
    backend/src/modules/whatsapp/whatsapp.module.ts
    backend/.env.example
  </files>
  <action>
    **1. main.ts — añadir rawBody: true**

    Cambiar la línea de NestFactory.create para habilitar rawBody:
    ```typescript
    const app = await NestFactory.create(AppModule, { rawBody: true });
    ```
    CRÍTICO: No añadir ningún `app.use(json())` o `app.use(urlencoded())` manual — anularía el rawBody wiring interno de NestJS. El proyecto actualmente no tiene custom body parsers, mantener así.

    **2. Crear backend/src/modules/whatsapp/guards/whatsapp-hmac.guard.ts**

    ```typescript
    import {
      CanActivate,
      ExecutionContext,
      Injectable,
      Logger,
      UnauthorizedException,
    } from '@nestjs/common';
    import * as crypto from 'crypto';
    import { RawBodyRequest } from '@nestjs/common';
    import { Request } from 'express';

    @Injectable()
    export class WhatsappHmacGuard implements CanActivate {
      private readonly logger = new Logger(WhatsappHmacGuard.name);

      canActivate(context: ExecutionContext): boolean {
        const request = context
          .switchToHttp()
          .getRequest<RawBodyRequest<Request>>();

        const signature = request.headers['x-hub-signature-256'] as string | undefined;
        const rawBody = request.rawBody;
        const appSecret = process.env.META_APP_SECRET;

        if (!appSecret) {
          this.logger.warn(
            'META_APP_SECRET not set — skipping HMAC verification (dev mode)',
          );
          return true;
        }

        if (!signature || !rawBody) {
          this.logger.warn('Missing x-hub-signature-256 or rawBody');
          throw new UnauthorizedException('Missing webhook signature');
        }

        const expected = `sha256=${crypto
          .createHmac('sha256', appSecret)
          .update(rawBody)
          .digest('hex')}`;

        const sigBuffer = Buffer.from(signature);
        const expectedBuffer = Buffer.from(expected);

        if (sigBuffer.length !== expectedBuffer.length) {
          throw new UnauthorizedException('Invalid webhook signature');
        }
        if (!crypto.timingSafeEqual(sigBuffer, expectedBuffer)) {
          throw new UnauthorizedException('Invalid webhook signature');
        }

        return true;
      }
    }
    ```

    Notas de implementación:
    - `crypto.timingSafeEqual` previene timing attacks — nunca usar `===` para comparar firmas.
    - Verificar longitud ANTES de timingSafeEqual — si los buffers tienen distinto length, timingSafeEqual lanza RangeError.
    - Dev fallback (return true cuando falta META_APP_SECRET) facilita desarrollo local. En producción la variable debe estar presente.

    **3. whatsapp-webhook.controller.ts — aplicar guard SOLO al POST**

    Añadir imports y decorator `@UseGuards(WhatsappHmacGuard)` SOLO en el método `handleWebhook` (POST). NO aplicar a nivel de clase ni al método `verifyWebhook` (GET) — el GET de Meta challenge no incluye HMAC y fallaría el registro del webhook:

    ```typescript
    import { Controller, Get, Post, Query, Body, Res, HttpCode, Logger, UseGuards } from '@nestjs/common';
    // ...existing imports...
    import { WhatsappHmacGuard } from './guards/whatsapp-hmac.guard';

    // GET handler — sin guard (Meta challenge no tiene HMAC)
    @Get('webhook/whatsapp')
    verifyWebhook(@Query() query: any, @Res() res: Response) { ... }

    // POST handler — guard aquí solamente
    @Post('webhook/whatsapp')
    @HttpCode(200)
    @UseGuards(WhatsappHmacGuard)
    async handleWebhook(@Body() body: any): Promise<string> { ... }
    ```

    **4. whatsapp.module.ts — registrar guard en providers**

    Añadir WhatsappHmacGuard a providers:
    ```typescript
    import { WhatsappHmacGuard } from './guards/whatsapp-hmac.guard';

    @Module({
      // ...
      providers: [WhatsappService, WhatsappMessageProcessor, EncryptionService, WhatsappHmacGuard],
      // ...
    })
    ```

    **5. backend/.env.example — documentar META_APP_SECRET**

    Añadir la variable al .env.example (si el archivo existe, añadir al final de la sección WhatsApp):
    ```
    META_APP_SECRET=your_meta_app_secret_from_meta_developer_dashboard
    ```
    Si .env.example no existe, crearlo con solo esta entrada precedida de un comentario.
  </action>
  <verify>
    `cd backend && npm run build` completa sin errores TypeScript.

    Test HMAC inválido (sin META_APP_SECRET seteada, el guard debe pasar en dev):
    ```bash
    curl -s -o /dev/null -w "%{http_code}" \
      -X POST http://localhost:3001/webhook/whatsapp \
      -H "Content-Type: application/json" \
      -d '{"test": true}'
    ```
    Debe retornar 200 (META_APP_SECRET no seteada en dev → fallback skip).

    Verificar que el GET challenge sigue funcionando:
    ```bash
    curl -s -w "%{http_code}" \
      "http://localhost:3001/webhook/whatsapp?hub.mode=subscribe&hub.verify_token=TEST&hub.challenge=CHALLENGE_VALUE"
    ```
    Debe retornar el challenge o 403 (dependiendo del WA_WEBHOOK_VERIFY_TOKEN), pero no 401 del guard.
  </verify>
  <done>
    backend/src/modules/whatsapp/guards/whatsapp-hmac.guard.ts existe y exporta WhatsappHmacGuard.
    main.ts tiene `rawBody: true` en NestFactory.create.
    whatsapp-webhook.controller.ts tiene @UseGuards(WhatsappHmacGuard) en el @Post handler (no en @Get, no en clase).
    whatsapp.module.ts incluye WhatsappHmacGuard en providers.
    backend/.env.example documenta META_APP_SECRET.
    `npm run build` en backend pasa sin errores.
    POST sin META_APP_SECRET seteada retorna 200 (dev fallback activo).
  </done>
</task>

</tasks>

<verification>
Verificación completa del phase:

1. **LOG-02**: Abrir perfil de paciente con más de 5 contactos registrados → aparece "Ver todos (N)" → click lo expande en el lugar → todas las notas visibles (sin truncate) → aparece "Mostrar menos" → click vuelve a 5 entradas.

2. **WA-04**:
   - `npm run build` del backend sin errores.
   - Archivo `backend/src/modules/whatsapp/guards/whatsapp-hmac.guard.ts` existe.
   - `main.ts` contiene `rawBody: true`.
   - `whatsapp-webhook.controller.ts` tiene `@UseGuards(WhatsappHmacGuard)` en el POST (verificar con grep).
   - GET /webhook/whatsapp responde normalmente (no bloqueado por guard).

3. **No regresiones**: `npm run build` en frontend y backend ambos pasan.
</verification>

<success_criteria>
1. ContactosSection muestra todos los contactos al hacer click en "Ver todos" y vuelve a 5 con "Mostrar menos" — sin dialogs ni rutas nuevas.
2. POST /webhook/whatsapp con META_APP_SECRET presente rechaza requests sin firma válida con 403; con firma válida retorna 200 y encola en BullMQ.
3. GET /webhook/whatsapp no es afectado por el guard HMAC.
4. Builds de frontend y backend pasan sin errores.
5. backend/.env.example documenta META_APP_SECRET.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux-security-hardening/07-01-SUMMARY.md` following the summary template at @/Users/laucaballero/.claude/get-shit-done/templates/summary.md
</output>
