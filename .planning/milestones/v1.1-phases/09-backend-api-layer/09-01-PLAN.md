---
phase: 09-backend-api-layer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/finanzas/utils/month-boundaries.ts
  - backend/src/modules/finanzas/utils/month-boundaries.spec.ts
  - backend/src/modules/finanzas/finanzas.service.ts
  - backend/src/modules/finanzas/finanzas.service.spec.ts
autonomous: true
requirements:
  - LMIT-02
  - LIQ-03

must_haves:
  truths:
    - "getMonthBoundariesART('2026-03').start equals 2026-03-01T03:00:00.000Z"
    - "A practice timestamped 2026-03-01T02:30:00Z is excluded from March's emitido total"
    - "crearLoteLiquidacion creates LiquidacionObraSocial and sets liquidacionId + PAGADO on all practice rows atomically"
    - "getLimiteDisponible returns {limite, emitido, disponible} using ART boundaries"
    - "getPracticasPendientesAgrupadas returns rows grouped by obraSocialId without N+1 queries"
  artifacts:
    - path: "backend/src/modules/finanzas/utils/month-boundaries.ts"
      provides: "getMonthBoundariesART() pure function"
      exports: ["getMonthBoundariesART", "MonthBoundaries"]
    - path: "backend/src/modules/finanzas/utils/month-boundaries.spec.ts"
      provides: "Unit tests for timezone boundary arithmetic"
    - path: "backend/src/modules/finanzas/finanzas.service.ts"
      provides: "Five new service methods"
    - path: "backend/src/modules/finanzas/finanzas.service.spec.ts"
      provides: "Unit tests for crearLoteLiquidacion and getLimiteDisponible (mocked Prisma)"
  key_links:
    - from: "finanzas.service.ts#getLimiteDisponible"
      to: "month-boundaries.ts#getMonthBoundariesART"
      via: "import + call on every invocation"
      pattern: "getMonthBoundariesART"
    - from: "finanzas.service.ts#crearLoteLiquidacion"
      to: "prisma.$transaction"
      via: "interactive callback form"
      pattern: "prisma\\.\\$transaction\\(async"
---

<objective>
Create the timezone utility and five new FinanzasService methods that power the FACTURADOR's billing-limit and lote-settlement features.

Purpose: LMIT-02 requires a timezone-correct available-limit calculation (UTC-3, no DST). LIQ-03 requires an atomic lote-creation transaction. Both are pure service logic — no controller exposure yet.

Output:
- `utils/month-boundaries.ts` — `getMonthBoundariesART()` with full unit coverage
- Five new methods in `finanzas.service.ts`:
  1. `getLimiteDisponible(profesionalId, mes)` → limit/emitido/disponible
  2. `setLimiteMensual(profesionalId, mes, limite)` → upsert LimiteFacturacionMensual
  3. `getPracticasPendientesAgrupadas(profesionalId)` → grouped by OS, no N+1
  4. `getPracticasPendientesPorOS(profesionalId, obraSocialId)` → list for lote prep
  5. `crearLoteLiquidacion(dto, usuarioId?)` → atomic $transaction
- Unit test files for Wave 0 scaffold (all tests red before implementation, green after)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backend-api-layer/09-RESEARCH.md
@.planning/phases/09-backend-api-layer/09-VALIDATION.md
</context>

<interfaces>
<!-- Key types the executor needs — extracted from codebase. -->

From backend/src/modules/finanzas/finanzas.service.ts (existing — do not remove):
- getDashboard, getIngresosPorDia, getIngresosPorObraSocial, getPagos, createPago
- getFacturas, createFactura, anularFactura, getPracticasPendientes (N+1 — keep but do not copy pattern)
- marcarPracticasPagadas (non-atomic — keep for backward compat, mark @deprecated in JSDoc)
- getCierreMensual, getReporteIngresos

From backend/src/prisma/schema.prisma (confirmed present after Phase 8):
```
model LimiteFacturacionMensual {
  id            String    @id @default(uuid())
  profesionalId String
  mes           String    // 'YYYY-MM'
  limite        Decimal?
  @@unique([profesionalId, mes])
}

model LiquidacionObraSocial {
  id           String              @id @default(uuid())
  obraSocialId String
  periodo      String              // 'YYYY-MM'
  fechaPago    DateTime?
  montoTotal   Decimal
  usuarioId    String?
  facturaId    String?
  practicas    PracticaRealizada[]  // via liquidacionId FK
}

model PracticaRealizada {
  id               String
  profesionalId    String
  pacienteId       String
  obraSocialId     String?
  codigo           String
  descripcion      String
  monto            Decimal
  coseguro         Decimal
  montoPagado      Decimal?   // Added in Phase 8 (SCHEMA-01)
  estadoLiquidacion EstadoLiquidacion  // PENDIENTE | PAGADO
  liquidacionId    String?    // FK → LiquidacionObraSocial
  fecha            DateTime
}

model Factura {
  id                  String
  profesionalId       String
  fecha               DateTime
  total               Decimal
  estado              EstadoFactura    // EMITIDA | ANULADA | ...
  condicionIVAReceptor CondicionIVA   // Added Phase 8 (SCHEMA-03)
  tipoCambio          Decimal?         // Added Phase 8 (SCHEMA-03)
}
```

From backend/src/modules/finanzas/dto/finanzas.dto.ts (existing patterns to mirror):
```typescript
export class MarcarPracticasPagadasDto {
  @IsArray()
  @IsUUID('4', { each: true })
  practicaIds: string[];
}
```
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Wave 0 — Write failing test scaffolds (RED)</name>
  <files>
    backend/src/modules/finanzas/utils/month-boundaries.spec.ts,
    backend/src/modules/finanzas/finanzas.service.spec.ts
  </files>
  <behavior>
    month-boundaries.spec.ts:
    - Test: getMonthBoundariesART('2026-03').start.toISOString() === '2026-03-01T03:00:00.000Z'
    - Test: getMonthBoundariesART('2026-03').end.toISOString() === '2026-04-01T02:59:59.999Z'
    - Test: practice at 2026-03-01T02:30:00Z has date < getMonthBoundariesART('2026-03').start (excluded from March)
    - Test: getMonthBoundariesART('2026-12') correctly rolls to next year (end in 2027-01)
    - Test: getMonthBoundariesART('2026-01').start.toISOString() === '2026-01-01T03:00:00.000Z'

    finanzas.service.spec.ts (mocked Prisma via jest.fn()):
    - Test: getLimiteDisponible returns {limite: 100000, emitido: 30000, disponible: 70000} when mock returns matching records
    - Test: getLimiteDisponible returns {limite: null, emitido: 0, disponible: null} when no LimiteFacturacionMensual exists
    - Test: crearLoteLiquidacion calls prisma.$transaction with async callback
    - Test: crearLoteLiquidacion resolves with the created liquidacion object
  </behavior>
  <action>
    Create both spec files with FAILING tests first (source files don't exist yet — imports will fail, which is the correct RED state).

    For finanzas.service.spec.ts, mock PrismaService and CuentasCorrientesService using jest.fn(). Pattern from existing project specs. The service has no spec file yet — create from scratch.

    Run `cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run test -- --testPathPattern="month-boundaries|finanzas.service" --passWithNoTests 2>&1 | tail -20` to confirm RED state.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run test -- --testPathPattern="month-boundaries|finanzas.service" --passWithNoTests 2>&1 | tail -30</automated>
  </verify>
  <done>Both spec files exist. Tests run (may fail due to missing source). Confirmed RED or PASS-WITH-NO-TESTS before implementation begins.</done>
</task>

<task type="tdd">
  <name>Task 2: Implement getMonthBoundariesART + five service methods (GREEN)</name>
  <files>
    backend/src/modules/finanzas/utils/month-boundaries.ts,
    backend/src/modules/finanzas/finanzas.service.ts
  </files>
  <behavior>
    month-boundaries.ts:
    - exports MonthBoundaries interface and getMonthBoundariesART(mes: string) function
    - Uses fixed UTC-3 offset (3 * 60 * 60 * 1000 ms) — no DST, no external library
    - December edge case: nextMonth=1, nextYear=year+1

    finanzas.service.ts additions:
    - getLimiteDisponible(profesionalId, mes): uses getMonthBoundariesART, parallel Promise.all for limiteRecord + factura.aggregate, returns {limite, emitido, disponible}
    - setLimiteMensual(profesionalId, mes, limite): prisma.limiteFacturacionMensual.upsert with @@unique compound key
    - getPracticasPendientesAgrupadas(profesionalId): select + single findMany for pacientes (no N+1), group by obraSocialId, return array of {obraSocialId, nombre, count, total}
    - getPracticasPendientesPorOS(profesionalId, obraSocialId): findMany where profesionalId + estadoLiquidacion=PENDIENTE + obraSocialId, include paciente select
    - crearLoteLiquidacion(dto: CreateLoteDto, usuarioId?): prisma.$transaction(async tx => { create LiquidacionObraSocial, compute montoTotal server-side from practicaIds (sum montoPagado ?? monto), updateMany practicas with PAGADO + liquidacionId, return liquidacion })

    For crearLoteLiquidacion: compute montoTotal inside the transaction by fetching practices with tx.practicaRealizada.findMany({where:{id:{in:dto.practicaIds}},select:{id,monto,montoPagado}}) then sum montoPagado ?? monto. Do NOT trust client-provided montoTotal.

    Add CreateLoteDto to dto/finanzas.dto.ts:
    - @IsUUID() profesionalId
    - @IsUUID() obraSocialId
    - @IsString() @Matches(/^\d{4}-\d{2}$/) periodo
    - @IsArray() @IsUUID('4', { each: true }) practicaIds

    Add @deprecated JSDoc to marcarPracticasPagadas (keep method body intact for backward compat).
  </behavior>
  <action>
    1. Create `backend/src/modules/finanzas/utils/month-boundaries.ts` with the implementation from the research doc (Pattern 1).
    2. Add `CreateLoteDto` to `backend/src/modules/finanzas/dto/finanzas.dto.ts` — append to existing file, do not rewrite.
    3. Add five new methods to `finanzas.service.ts` — append after `getReporteIngresos`, do not touch existing methods. Import `getMonthBoundariesART` at the top of the service file.
    4. Run tests to confirm GREEN.

    Anti-patterns to avoid (from research):
    - Never use `new Date(year, month-1, 1)` — that's midnight UTC (= 21:00 ART previous day)
    - Use interactive callback form `$transaction(async tx => {...})` — NOT array form — because liquidacionId FK must reference the created record's id
    - Never derive profesionalId from JWT in service methods (all methods accept it as explicit param)
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run test -- --testPathPattern="month-boundaries|finanzas.service" 2>&1 | tail -20</automated>
  </verify>
  <done>
    All tests green. Specifically:
    - getMonthBoundariesART('2026-03').start === '2026-03-01T03:00:00.000Z'
    - Practice at 02:30Z excluded from March
    - crearLoteLiquidacion resolves with liquidacion object
    - getLimiteDisponible returns correct shape with null handling
    `cd backend && npm run test -- --testPathPattern="month-boundaries|finanzas.service"` exits 0.
  </done>
</task>

</tasks>

<verification>
Full backend test suite must stay green after this plan:
`cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run test 2>&1 | tail -10`

TypeScript compilation must succeed:
`cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -20`
</verification>

<success_criteria>
- `month-boundaries.ts` exists with exported `getMonthBoundariesART` and `MonthBoundaries`
- `month-boundaries.spec.ts` has 5 tests, all green
- `finanzas.service.spec.ts` has 4+ tests for getLimiteDisponible + crearLoteLiquidacion, all green
- `finanzas.service.ts` has five new methods appended (existing methods untouched)
- `finanzas.dto.ts` has `CreateLoteDto` appended
- `npm run test -- --testPathPattern="month-boundaries|finanzas.service"` exits 0
- `npm run build` exits 0 (no TypeScript errors)
</success_criteria>

<output>
After completion, create `.planning/phases/09-backend-api-layer/09-01-SUMMARY.md`
</output>
