---
phase: 52-preop-hc-form-chip-catalogs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/schema.prisma
  - backend/src/modules/catalogo-hc/catalogo-hc.seed-data.ts
autonomous: true
requirements: [PREOP-03, PREOP-04]
must_haves:
  truths:
    - "An AntecedenteCatalogoPro table exists in the database after migration (D-01, D-02)"
    - "Profesional has an antecedentesCatalogo inverse relation (D-01)"
    - "The antecedente seed list mirrors the 10 PREDEFINED conditions used in the frontend (D-03)"
  artifacts:
    - path: "backend/src/prisma/schema.prisma"
      provides: "AntecedenteCatalogoPro model + inverse relation on Profesional"
      contains: "model AntecedenteCatalogoPro"
    - path: "backend/src/modules/catalogo-hc/catalogo-hc.seed-data.ts"
      provides: "SEED_ANTECEDENTES flat constant"
      contains: "SEED_ANTECEDENTES"
    - path: "backend/prisma/migrations"
      provides: "new migration add_antecedente_catalogo_pro"
      contains: "AntecedenteCatalogoPro"
  key_links:
    - from: "AntecedenteCatalogoPro.profesionalId"
      to: "Profesional.id"
      via: "@relation FK"
      pattern: "profesional\\s+Profesional\\s+@relation"
---

<objective>
Add the missing per-professional catalog model `AntecedenteCatalogoPro` (D-01) following the exact pattern of the existing `AlergiaCatalogoPro` / `MedicamentoCatalogoPro`, seed its system rows from the frontend `PREDEFINED` condition list (D-03), and run the single new migration this phase requires (D-02).

Purpose: PREOP-03/04 require antecedentes to be selectable from a per-professional catalog with auto-learning. The research only planned alergias/medicación catalogs; this plan closes that schema gap so the rest of the phase can build on a real table.
Output: New Prisma model + inverse relation, a `SEED_ANTECEDENTES` constant, and an applied migration with idempotent seed data shape.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/52-preop-hc-form-chip-catalogs/52-CONTEXT.md
@.planning/phases/52-preop-hc-form-chip-catalogs/52-PATTERNS.md

<interfaces>
Existing analog models (schema.prisma lines 1432-1458) — copy field-for-field, rename only:
- AlergiaCatalogoPro / MedicamentoCatalogoPro: { id String @id @default(uuid)), nombre String, activo Boolean @default(true), esSistema Boolean @default(false), profesionalId String, profesional Profesional @relation(fields:[profesionalId], references:[id]), createdAt, updatedAt, @@unique([nombre, profesionalId]), @@index([profesionalId, activo]) }
- Profesional already has: alergiasCatalogo AlergiaCatalogoPro[] / medicamentosCatalogo MedicamentoCatalogoPro[] (schema lines ~137-138)
- Frontend PREDEFINED source: frontend/src/components/CondicionesChips.tsx lines 11-22 → Hipertensión, Diabetes, Asma, Enfermedad cardíaca, Obesidad, Artritis, Alergia severa, Hipotiroidismo, Cannabis, Epilepsia
- normalizarNombre already exported from catalogo-hc.seed-data.ts (lines 25-27)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AntecedenteCatalogoPro model + inverse relation + seed constant</name>
  <read_first>
    - backend/src/prisma/schema.prisma (read AlergiaCatalogoPro/MedicamentoCatalogoPro at lines 1430-1458 and Profesional relations at lines ~130-140)
    - backend/src/modules/catalogo-hc/catalogo-hc.seed-data.ts (read SEED_ZONAS + normalizarNombre, lines 1-119)
    - frontend/src/components/CondicionesChips.tsx (read PREDEFINED list, lines 11-22 — source of truth for seed values)
  </read_first>
  <action>In schema.prisma, add a new model `AntecedenteCatalogoPro` immediately after `MedicamentoCatalogoPro`, copying every field, the `@@unique([nombre, profesionalId])` and `@@index([profesionalId, activo])` verbatim, with the relation `profesional Profesional @relation(fields: [profesionalId], references: [id])`. Add the inverse relation line `antecedentesCatalogo AntecedenteCatalogoPro[]` to the `Profesional` model right after `medicamentosCatalogo MedicamentoCatalogoPro[]`. In catalogo-hc.seed-data.ts, export a flat constant `SEED_ANTECEDENTES: string[]` containing exactly these 10 values in order: Hipertensión, Diabetes, Asma, Enfermedad cardíaca, Obesidad, Artritis, Alergia severa, Hipotiroidismo, Cannabis, Epilepsia. Do not modify normalizarNombre or SEED_ZONAS.</action>
  <verify>
    <automated>cd backend && npx prisma format && grep -q "model AntecedenteCatalogoPro" src/prisma/schema.prisma && grep -q "antecedentesCatalogo AntecedenteCatalogoPro\[\]" src/prisma/schema.prisma && grep -q "SEED_ANTECEDENTES" src/modules/catalogo-hc/catalogo-hc.seed-data.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - schema.prisma contains `model AntecedenteCatalogoPro` with `@@unique([nombre, profesionalId])` and `@@index([profesionalId, activo])`
    - Profesional model contains `antecedentesCatalogo AntecedenteCatalogoPro[]`
    - `npx prisma format` exits 0 (schema is valid)
    - SEED_ANTECEDENTES is an exported string[] of length 10 matching the frontend PREDEFINED list
  </acceptance_criteria>
  <done>Model, inverse relation and seed constant present; schema validates.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Run Prisma migration + regenerate client</name>
  <read_first>
    - backend/src/prisma/schema.prisma (confirm AntecedenteCatalogoPro present from Task 1)
    - backend/prisma/migrations (inspect existing migration folder naming to match convention)
  </read_first>
  <action>From `backend/`, run `npx prisma migrate dev --name add_antecedente_catalogo_pro` to create and apply the migration for the new table. If interactive prompts block in non-TTY, fall back to `npx prisma migrate dev --name add_antecedente_catalogo_pro --create-only` followed by `npx prisma migrate deploy`. Then run `npx prisma generate` so the `antecedenteCatalogoPro` delegate is available on PrismaClient. This is a pure additive table creation — no `--accept-data-loss` is expected; if Prisma ever requests it, STOP and surface the drift rather than forcing it. Do NOT hand-write a seed script here — the rows are seeded lazily per-professional by the service layer (Plan 02) using SEED_ANTECEDENTES.</action>
  <verify>
    <automated>cd backend && npx prisma migrate status && grep -rq "AntecedenteCatalogoPro" prisma/migrations && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma migrate status` exits 0 with no pending migrations
    - A migration directory under backend/prisma/migrations contains a CREATE TABLE referencing AntecedenteCatalogoPro
    - `npx prisma generate` completes and `prisma.antecedenteCatalogoPro` is a valid delegate (no TS error when referenced)
    - No `--accept-data-loss` flag was required
  </acceptance_criteria>
  <done>Migration applied, client regenerated, table exists at runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Prisma migration → live DB | Schema change applied to production-shaped DB; risk of unintended data loss |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Denial of Service | prisma migrate | mitigate | Additive-only table create; if Prisma requests `--accept-data-loss`, stop and surface drift instead of forcing |
| T-52-02 | Tampering | AntecedenteCatalogoPro scope | mitigate | `@@unique([nombre, profesionalId])` enforces per-professional isolation at DB level; no cross-tenant rows possible |
| T-52-SC | Tampering | npm/prisma installs | accept | No new packages installed in this plan; prisma already vendored |
</threat_model>

<verification>
- `npx prisma migrate status` clean
- `npx prisma validate` passes
- Backend still builds: `cd backend && npm run build`
</verification>

<success_criteria>
- AntecedenteCatalogoPro table exists with the correct unique constraint and index
- Profesional.antecedentesCatalogo inverse relation present
- SEED_ANTECEDENTES exported with the 10 canonical values
- Migration applied with no pending migrations and no data loss
</success_criteria>

<output>
Create `.planning/phases/52-preop-hc-form-chip-catalogs/52-01-SUMMARY.md` when done.
</output>
