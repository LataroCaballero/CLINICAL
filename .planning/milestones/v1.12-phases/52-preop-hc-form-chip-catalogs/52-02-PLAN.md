---
phase: 52-preop-hc-form-chip-catalogs
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - backend/src/modules/catalogo-hc/catalogo-hc.service.ts
  - backend/src/modules/catalogo-hc/catalogo-hc.controller.ts
autonomous: true
requirements: [PREOP-03, PREOP-04, PREOP-06, PREOP-07]
must_haves:
  truths:
    - "A professional can GET their antecedentes/alergias/medicamentos catalogs, lazily seeded on first access"
    - "A new name learned via Otro is upserted into the calling professional's catalog only (D-06)"
    - "System rows cannot be renamed or deleted"
    - "All catalog reads/writes are scoped to the profesionalId derived from the authenticated JWT"
  artifacts:
    - path: "backend/src/modules/catalogo-hc/catalogo-hc.service.ts"
      provides: "flat-catalog seed/get/learning methods for the 3 sections"
      contains: "aprenderDesdePreoperatorio"
    - path: "backend/src/modules/catalogo-hc/catalogo-hc.controller.ts"
      provides: "GET endpoints for antecedentes/alergias/medicamentos"
      contains: "antecedentes"
  key_links:
    - from: "catalogo-hc.controller getProfesionalId"
      to: "JWT req.user"
      via: "scope resolution"
      pattern: "getProfesionalId\\(req\\.user"
    - from: "aprenderDesdePreoperatorio"
      to: "normalizarNombre"
      via: "case/accent-insensitive dedup"
      pattern: "normalizarNombre"
---

<objective>
Add flat per-professional catalog support for the three chip sections (antecedentes, alergias, medicación) to the existing `catalogo-hc` module: lazy idempotent seeding, list getters, an auto-learning upsert method reused by the PREOP HC branch, and JWT-scoped GET endpoints.

Purpose: PREOP-03/06/07 need chips sourced from per-professional catalogs; PREOP-04 needs new "Otro" values to persist (learning). This mirrors the F46 learning pattern 1:1 (D-06).
Output: Service methods `seed{Antecedentes,Alergias,Medicamentos}Inicial`, `get{...}ConSeed`, `aprenderDesdePreoperatorio`, plus controller GET endpoints, all scoped by `getProfesionalId` (PITFALL 12).
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-preop-hc-form-chip-catalogs/52-CONTEXT.md
@.planning/phases/52-preop-hc-form-chip-catalogs/52-PATTERNS.md
@.planning/phases/46-auto-aprendizaje-via-otros/46-CONTEXT.md

<interfaces>
From catalogo-hc.service.ts (existing analogs):
- seedCatalogoInicial (lines 102-159) — idempotent seed via count-then-createMany skipDuplicates
- getCatalogoConSeed (lines 166-221) — lazy-seed list getter
- aprenderDesdeZonas (lines 236-434) — best-effort upsert, normalizarNombre dedup, reactivates inactive
- esSistema guards (lines 449-599), P2002 conflict on rename (lines 452-465)
From catalogo-hc.controller.ts:
- @Auth('ADMIN','PROFESIONAL','SECRETARIA') class guard (line 19)
- getProfesionalId(user, targetProfesionalId?) — JWT scope resolver (lines 27-55)
- @Get() getCatalogo(@Req,@Query('profesionalId')) → service.getCatalogoConSeed(pid) (lines 254-259)
From catalogo-hc.seed-data.ts: SEED_ANTECEDENTES (Plan 01), normalizarNombre (lines 25-27)
New Prisma delegates (Plan 01): prisma.antecedenteCatalogoPro; existing prisma.alergiaCatalogoPro, prisma.medicamentoCatalogoPro
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add flat seed + list + learning service methods</name>
  <read_first>
    - backend/src/modules/catalogo-hc/catalogo-hc.service.ts (read seedCatalogoInicial 102-159, getCatalogoConSeed 166-221, aprenderDesdeZonas 236-434, esSistema guards 449-599)
    - backend/src/modules/catalogo-hc/catalogo-hc.seed-data.ts (SEED_ANTECEDENTES + normalizarNombre)
    - backend/src/modules/historia-clinica/historia-clinica.service.ts lines 313-336 (how aprenderDesdeZonas is invoked best-effort — the learning contract Plan 03 will reuse)
  </read_first>
  <behavior>
    - Calling getAntecedentesConSeed for a professional with an empty catalog seeds the 10 SEED_ANTECEDENTES rows with esSistema:true then returns them
    - Calling it again does NOT create duplicates (count guard)
    - aprenderDesdePreoperatorio with a new name creates an esSistema:false row scoped to profesionalId
    - aprenderDesdePreoperatorio with a name that normalizes (case/accent-insensitive) to an existing active row is a no-op
    - aprenderDesdePreoperatorio with a name matching an inactive row reactivates it (activo:true)
    - aprenderDesdePreoperatorio never throws (best-effort) and learns only the names passed in
  </behavior>
  <action>In catalogo-hc.service.ts add: `seedAntecedentesInicial`, `seedAlergiasInicial`, `seedMedicamentosInicial` (each: `count` by profesionalId, return if >0, else `createMany` from the matching SEED_* constant with esSistema:true, skipDuplicates:true). Add `getAntecedentesConSeed`, `getAlergiasConSeed`, `getMedicamentosConSeed` (findMany where profesionalId+activo:true ordered by esSistema desc then nombre asc; if empty, call the matching seed then re-query). Add a generic private helper `aprenderDesdeFlat(profesionalId, nombres, modelo)` that loads a snapshot (id,nombre,activo), builds a normalizarNombre→{id,activo} map, and for each incoming name: create if absent (esSistema:false), reactivate if present-but-inactive, no-op if active. Add the public `aprenderDesdePreoperatorio(profesionalId, { antecedentes, alergias, medicacion })` that calls aprenderDesdeFlat for each non-empty array against its model delegate name. Import SEED_ANTECEDENTES and reuse existing SEED imports for alergias/medicamentos (those seed constants already exist from F51 — reuse them; do not redefine). Do NOT throw from learning methods; the caller wraps them in try/catch.</action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json && grep -q "aprenderDesdePreoperatorio" src/modules/catalogo-hc/catalogo-hc.service.ts && grep -q "getAntecedentesConSeed" src/modules/catalogo-hc/catalogo-hc.service.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Service exports `aprenderDesdePreoperatorio`, `getAntecedentesConSeed`, `getAlergiasConSeed`, `getMedicamentosConSeed`, and the three `seed*Inicial` methods
    - aprenderDesdeFlat uses `normalizarNombre` for matching and writes `esSistema:false` for learned rows
    - All writes include `profesionalId` from the method argument (never from a request body)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Flat-catalog seed/list/learning methods compile and follow the F46 learning contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add JWT-scoped GET endpoints for the three catalogs</name>
  <read_first>
    - backend/src/modules/catalogo-hc/catalogo-hc.controller.ts (read getProfesionalId 27-55 and the existing @Get() 254-259)
  </read_first>
  <action>In catalogo-hc.controller.ts add `@Get('antecedentes')`, `@Get('alergias')`, `@Get('medicamentos')` handlers, each accepting `@Req() req` and optional `@Query('profesionalId') profesionalId?`, resolving the scope with the existing `getProfesionalId(req.user, profesionalId)` helper, and delegating to the matching `get*ConSeed(pid)` service method. Keep the class-level `@Auth('ADMIN','PROFESIONAL','SECRETARIA')` guard — do not relax it. Do NOT trust any profesionalId from the request body; only the @Query override path already vetted inside getProfesionalId (SECRETARIA/ADMIN) is allowed.</action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json && grep -q "@Get('antecedentes')" src/modules/catalogo-hc/catalogo-hc.controller.ts && grep -q "getProfesionalId(req.user" src/modules/catalogo-hc/catalogo-hc.controller.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Three new GET endpoints exist: `/catalogo-hc/antecedentes`, `/catalogo-hc/alergias`, `/catalogo-hc/medicamentos`
    - Each resolves scope via `getProfesionalId(req.user, ...)` — no profesionalId read from request body
    - Class still guarded by `@Auth('ADMIN','PROFESIONAL','SECRETARIA')`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Endpoints return per-professional, lazily-seeded catalogs scoped to the JWT context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → catalog API | Untrusted profesionalId override could leak/poison another professional's catalog |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03 | Information Disclosure / Tampering | catalog GET + learning | mitigate | PITFALL 12 — scope resolved via `getProfesionalId(req.user)`; profesionalId never read from request body; SECRETARIA/ADMIN override path already vetted inside the helper |
| T-52-04 | Elevation of Privilege | system catalog rows | mitigate | esSistema guard blocks rename/delete of seeded rows (reuse existing ForbiddenException pattern) |
| T-52-05 | Tampering | catalog cross-tenant write | mitigate | `@@unique([nombre, profesionalId])` + every write passes profesionalId from the resolved scope, not the payload |
| T-52-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `cd backend && npm run build` succeeds
- Manual smoke (optional): GET `/catalogo-hc/antecedentes` as PROFESIONAL returns 10 seeded items on first call
</verification>

<success_criteria>
- Per-professional catalogs for the 3 sections are readable and lazily seeded
- Learning upsert is best-effort, dedup-safe, and JWT-scoped
- System rows protected; no cross-tenant access possible
</success_criteria>

<output>
Create `.planning/phases/52-preop-hc-form-chip-catalogs/52-02-SUMMARY.md` when done.
</output>
