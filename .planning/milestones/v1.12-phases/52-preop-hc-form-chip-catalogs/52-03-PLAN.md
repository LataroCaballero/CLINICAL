---
phase: 52-preop-hc-form-chip-catalogs
plan: 03
type: execute
wave: 3
depends_on: ["52-02"]
files_modified:
  - backend/src/modules/historia-clinica/dto/crear-entrada.dto.ts
  - backend/src/modules/historia-clinica/historia-clinica.service.ts
autonomous: true
requirements: [PREOP-04, PREOP-05, PREOP-06, PREOP-07, PREOP-08, PREOP-09, PREOP-10]
must_haves:
  truths:
    - "Creating a pre_quirurgico HC entry stores antecedentes/alergias/medicacion/estudios/consentimiento in the entry contenido JSONB"
    - "Confirmed antecedentes/alergias/medicacion are union-dedup merged into the patient profile arrays (not replaced) (D-09)"
    - "Estudios complementarios persist in the dedicated estudiosComplementarios Json column in the stable shape (D-10)"
    - "Consentimiento informado writes a timestamp into the entry JSONB, never into Paciente.consentimientoFirmadoAt (D-11)"
    - "New Otro values are learned into the professional catalogs best-effort after the transaction (D-06)"
  artifacts:
    - path: "backend/src/modules/historia-clinica/dto/crear-entrada.dto.ts"
      provides: "PREOP request fields"
      contains: "consentimientoInformado"
    - path: "backend/src/modules/historia-clinica/historia-clinica.service.ts"
      provides: "pre_quirurgico branch in crearEntrada"
      contains: "pre_quirurgico"
  key_links:
    - from: "crearEntrada pre_quirurgico branch"
      to: "catalogoHc.aprenderDesdePreoperatorio"
      via: "best-effort post-transaction learning"
      pattern: "aprenderDesdePreoperatorio"
    - from: "pre_quirurgico merge"
      to: "paciente.condiciones/alergias/medicacion"
      via: "union-dedup update"
      pattern: "Set\\(\\[\\.\\.\\."
---

<objective>
Extend the HC create DTO with PREOP fields and add the `pre_quirurgico` branch to `crearEntrada()`: build the contenido JSONB, persist estudios in the dedicated column (D-10), write the consentimiento-informado timestamp into the JSONB (D-11, NOT consentimientoFirmadoAt), union-dedup merge the confirmed chips into the patient profile (D-09), and trigger best-effort catalog learning (D-06) using the method from Plan 02.

Purpose: PREOP-04/05/06/07/08/09/10 — server-side persistence and learning for the prequirúrgico form.
Output: DTO fields + a fully wired `pre_quirurgico` branch that mirrors the `primera_vez` branch structure.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-preop-hc-form-chip-catalogs/52-CONTEXT.md
@.planning/phases/52-preop-hc-form-chip-catalogs/52-PATTERNS.md
@.planning/phases/51-schema-foundation-chat-fix/51-CONTEXT.md

<interfaces>
From historia-clinica.service.ts:
- crearEntrada(pacienteId, dto, profesionalId) — dispatch by dto.tipo (lines 96-113); primera_vez branch is the model (lines 79-339)
- patient profile update inside $transaction (lines 251-260)
- best-effort learning call site (lines 313-336)
- this.catalogoHc: CatalogoHCService (injected) → now exposes aprenderDesdePreoperatorio(profesionalId, {antecedentes,alergias,medicacion}) [Plan 02]
From crear-entrada.dto.ts:
- CreateEntradaDto.tipo union already includes 'pre_quirurgico' (line ~387); optional fields use @IsOptional()+@IsArray() (lines 67-98)
From schema (already present, do NOT add): Paciente.condiciones[], Paciente.alergias[], Paciente.medicacion[]; HistoriaClinicaEntrada.estudiosComplementarios Json?, contenido Json?, tipoEntrada TipoEntradaHC?
Controller passes profesionalId from req.user?.profesionalId (JWT) — do not read it from body.
Estudios shape (D-10, locked F51 D-09): { laboratorio: boolean, ecg: boolean, imagenes: string[] } where imagenes ⊆ {Ecografía, Tomografía, Mamografía, Otro}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PREOP fields to CreateEntradaDto</name>
  <read_first>
    - backend/src/modules/historia-clinica/dto/crear-entrada.dto.ts (read existing optional-field pattern lines 42-98)
  </read_first>
  <action>In crear-entrada.dto.ts add optional validated fields following the existing `@IsOptional()` style: `antecedentes?: string[]`, `alergias?: string[]`, `medicacion?: string[]` (each `@IsArray()` + `@IsString({ each: true })`); `estudiosComplementarios?` as an object `{ laboratorio: boolean; ecg: boolean; imagenes: string[] }` (`@IsOptional()` + `@IsObject()` or a nested class with `@ValidateNested()`); and `consentimientoInformado?: boolean` (`@IsOptional()` + `@IsBoolean()`). Confirm the `tipo` union already contains `'pre_quirurgico'`; if it does, leave it. Do not remove or repurpose any existing field.</action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json && grep -q "consentimientoInformado" src/modules/historia-clinica/dto/crear-entrada.dto.ts && grep -q "estudiosComplementarios" src/modules/historia-clinica/dto/crear-entrada.dto.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - DTO declares antecedentes/alergias/medicacion (string[]), estudiosComplementarios (laboratorio/ecg/imagenes shape), consentimientoInformado (boolean), all @IsOptional
    - `tipo` union includes 'pre_quirurgico'
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PREOP DTO fields validated and compiling.</done>
</task>

<task type="auto">
  <name>Task 2: Add pre_quirurgico branch to crearEntrada</name>
  <read_first>
    - backend/src/modules/historia-clinica/historia-clinica.service.ts (read dispatch 96-113, primera_vez contenido build, profile update 251-260, learning call 313-336)
    - backend/src/modules/catalogo-hc/catalogo-hc.service.ts (confirm aprenderDesdePreoperatorio signature from Plan 02)
  </read_first>
  <action>In crearEntrada() add a `dto.tipo === 'pre_quirurgico'` branch that builds `contenido` = { tipo:'pre_quirurgico', antecedentes: dto.antecedentes ?? [], alergias: dto.alergias ?? [], medicacion: dto.medicacion ?? [], estudiosComplementarios: dto.estudiosComplementarios ?? null, consentimientoInformadoAt: dto.consentimientoInformado ? new Date().toISOString() : null, optional zonas if present, comentario }. Persist the entry with `tipoEntrada: 'PREOPERATORIO'`, writing the estudios object ALSO into the dedicated `estudiosComplementarios` Json column (D-10) so it is queryable (PREOP-09). Inside the same `$transaction`, after creating the entry, load the patient's `condiciones`, `alergias`, `medicacion` and write back the union-dedup merge (`Array.from(new Set([...existing, ...incoming]))`) — MERGE, never replace (D-09). Do NOT touch `consentimientoFirmadoAt` or any `*AutoReportada(o)s` staging fields. After the transaction commits, call `this.catalogoHc.aprenderDesdePreoperatorio(profesionalId, { antecedentes, alergias, medicacion })` wrapped in try/catch with `this.logger.warn(...)` on failure — learning must never block the save. Use the `profesionalId` argument passed into crearEntrada (JWT-derived), never a value from the body.</action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json && grep -q "pre_quirurgico" src/modules/historia-clinica/historia-clinica.service.ts && grep -q "aprenderDesdePreoperatorio" src/modules/historia-clinica/historia-clinica.service.ts && grep -q "consentimientoInformadoAt" src/modules/historia-clinica/historia-clinica.service.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - crearEntrada has a pre_quirurgico branch persisting contenido JSONB with the locked shape + tipoEntrada 'PREOPERATORIO'
    - estudiosComplementarios written to the dedicated Json column in shape {laboratorio,ecg,imagenes[]} (PREOP-09 queryable)
    - Profile merge is union-dedup into condiciones/alergias/medicacion; a pre-existing value is preserved (not dropped) and incoming-only values are added
    - consentimientoInformado=true writes an ISO timestamp into contenido.consentimientoInformadoAt; consentimientoFirmadoAt is untouched (grep confirms it is not assigned in this branch)
    - Learning runs post-transaction in try/catch with logger.warn; throwing in learning does not roll back the entry
    - profesionalId used for learning is the crearEntrada argument (JWT), not dto
  </acceptance_criteria>
  <done>pre_quirurgico entries persist, merge to profile, store estudios queryably, timestamp consent, and learn best-effort.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → crearEntrada | Untrusted DTO arrays + flags; profesionalId must come from JWT not body |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06 | Tampering | learning scope | mitigate | PITFALL 12 — aprenderDesdePreoperatorio receives the JWT-derived profesionalId argument; dto never supplies it |
| T-52-07 | Tampering | profile arrays | mitigate | Union-dedup merge preserves existing values; no destructive overwrite of condiciones/alergias/medicacion |
| T-52-08 | Repudiation | consent audit | mitigate | consentimientoInformadoAt ISO timestamp recorded in immutable entry JSONB for audit; distinct from firmado |
| T-52-09 | Denial of Service | learning failure | accept | Learning is best-effort post-transaction; failure logged (warn) and does not block or roll back the HC save |
| T-52-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `cd backend && npm run build` succeeds
- `cd backend && npm test` — existing HC tests still pass
</verification>

<success_criteria>
- pre_quirurgico HC entries persist all PREOP data with the locked shapes
- Profile arrays merged not replaced; estudios queryable; consent timestamped in JSONB
- Catalog learning fires best-effort and JWT-scoped
</success_criteria>

<output>
Create `.planning/phases/52-preop-hc-form-chip-catalogs/52-03-SUMMARY.md` when done.
</output>
