---
phase: 52-preop-hc-form-chip-catalogs
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/pacientes/pacientes.service.ts
  - backend/src/modules/pacientes/pacientes.controller.ts
  - backend/src/modules/pacientes/pacientes.module.ts
  - backend/src/modules/pacientes/portal-email.service.ts
autonomous: true
requirements: [PREOP-11, PREOP-12]
must_haves:
  truths:
    - "A professional can generate the patient portal link; the DB stores only the sha256 hash (D-12)"
    - "Re-requesting a link for a patient that already has a token does NOT re-hash or regenerate it (D-12)"
    - "The professional can email the portal link only when SMTP is configured (D-13)"
    - "If the patient has no email, the professional can set it at share time"
  artifacts:
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "generarPortalLink (idempotent sha256 token)"
      contains: "createHash"
    - path: "backend/src/modules/pacientes/portal-email.service.ts"
      provides: "SMTP-aware portal link emailer with isSmtpConfigured()"
      contains: "isSmtpConfigured"
    - path: "backend/src/modules/pacientes/pacientes.controller.ts"
      provides: "portal-link + email endpoints"
      contains: "portal-link"
  key_links:
    - from: "generarPortalLink"
      to: "Paciente.portalToken (hash)"
      via: "sha256 persisted, raw uuid only in URL"
      pattern: "createHash\\('sha256'\\)"
    - from: "portal email endpoint"
      to: "isSmtpConfigured"
      via: "gate email option on SMTP presence"
      pattern: "isSmtpConfigured"
---

<objective>
Generate the real, stable patient-portal token now (D-12) and expose share endpoints: a JWT-scoped `portal-link` generator that stores only the `sha256(uuid)` hash (PITFALL 1), an email-send path gated on SMTP configuration (D-13/PREOP-12), and an at-share email update for patients without an email on file.

Purpose: PREOP-11/12 + Success Criteria #5 — the professional can copy/WhatsApp/QR/email the portal link from the prequirúrgico template. F54 reuses this exact token without regenerating.
Output: `generarPortalLink` service method, a dedicated `PortalEmailService` (SMTP-aware), controller endpoints, and module wiring.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-preop-hc-form-chip-catalogs/52-CONTEXT.md
@.planning/phases/52-preop-hc-form-chip-catalogs/52-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
From schema (already present, F51): Paciente.portalToken String? @unique (SHA-256 hash), Paciente.portalTokenGeneradoAt DateTime?, Paciente.email
From presupuesto-email.service.ts (token+url pattern): const token = crypto.randomUUID(); const frontendUrl = this.configService.get('FRONTEND_URL','http://localhost:3000'); url = `${frontendUrl}/presupuesto/${token}`
From reportes/services/email.service.ts (SMTP detect pattern, lines 28-46): reads SMTP_HOST/SMTP_PORT(587)/SMTP_USER/SMTP_PASS; if !host||!user||!pass → not configured; transporter = nodemailer.createTransport({host,port,secure:port===465,auth:{user,pass}}); SMTP_FROM default
From pacientes.controller.ts: class @Auth('ADMIN','PROFESIONAL','SECRETARIA','FACTURADOR') (line 36); existing @Patch(':id') update pattern
ConfigModule is global (app.module isGlobal:true) — ConfigService injectable without importing ConfigModule
crypto: import * as crypto from 'crypto'; sha256 → crypto.createHash('sha256').update(rawUuid).digest('hex') = 64-char hex
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: generarPortalLink + PortalEmailService</name>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.ts (existing service patterns + prisma usage)
    - backend/src/modules/presupuestos/presupuesto-email.service.ts lines 95-103 (randomUUID + FRONTEND_URL pattern)
    - backend/src/modules/reportes/services/email.service.ts lines 21-80 (SMTP transporter init + isConfigured guard + sendEmail)
    - .planning/research/PITFALLS.md (pitfall 1 token hashed, pitfall 12 JWT scope)
  </read_first>
  <action>In pacientes.service.ts add `async generarPortalLink(pacienteId: string)`: fetch the patient's `portalToken`, `email`; if `portalToken` is already set, return `{ url: null, alreadyGenerated: true }` WITHOUT regenerating or re-hashing (D-12 idempotent). If absent, `crypto.randomUUID()` → `crypto.createHash('sha256').update(rawUuid).digest('hex')`, persist that hash into `portalToken` plus `portalTokenGeneradoAt: new Date()`, and return `{ url: ` + "`${FRONTEND_URL}/portal/${rawUuid}`" + `, alreadyGenerated: false }`. The raw UUID is NEVER persisted — only the hash. Also add `async setEmailSiFalta(pacienteId, email)` that updates Paciente.email only when currently empty. Create a new `backend/src/modules/pacientes/portal-email.service.ts` `@Injectable() PortalEmailService` modeled on reportes EmailService: build the nodemailer transporter from SMTP_HOST/PORT/USER/PASS via the global ConfigService, expose `isSmtpConfigured(): boolean` (true only if host+user+pass present), and `async enviarLinkPortal(to, url, pacienteNombre)` that returns false if not configured (never throws). Import `import * as crypto from 'crypto'` in the service.</action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json && grep -q "createHash('sha256')" src/modules/pacientes/pacientes.service.ts && grep -q "isSmtpConfigured" src/modules/pacientes/portal-email.service.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - generarPortalLink stores a value matching /^[0-9a-f]{64}$/ in Paciente.portalToken; the raw UUID never appears in any DB write
    - When portalToken already exists, generarPortalLink returns alreadyGenerated:true and performs NO update (grep: no prisma update inside the already-set branch)
    - portalTokenGeneradoAt set only on fresh generation
    - PortalEmailService.isSmtpConfigured() returns false when any of SMTP_HOST/USER/PASS missing; enviarLinkPortal returns false (no throw) when not configured
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Idempotent hashed token generation + SMTP-aware emailer implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Portal-link + email endpoints + module wiring</name>
  <read_first>
    - backend/src/modules/pacientes/pacientes.controller.ts (read class @Auth line 36 + existing @Patch(':id') handler)
    - backend/src/modules/pacientes/pacientes.module.ts (providers list)
  </read_first>
  <action>In pacientes.controller.ts add `@Post(':id/portal-link')` that calls `service.generarPortalLink(id)` and returns `{ url, alreadyGenerated, smtpConfigured: portalEmail.isSmtpConfigured() }` so the frontend knows whether to show the email option. Add `@Post(':id/portal-link/email')` accepting `@Body() { email?: string }`: if email provided, call `setEmailSiFalta(id, email)`; resolve the portal URL (generate if absent), then `portalEmail.enviarLinkPortal(...)`; return `{ enviado: boolean }`. Keep the existing class `@Auth(...)` guard — these endpoints are staff-only; the patient never calls them. Use the `id` path param as the patient scope; do NOT accept a patient id from the body. Register `PortalEmailService` in pacientes.module.ts providers and inject it into both the controller and pacientes.service as needed.</action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json && grep -q "portal-link" src/modules/pacientes/pacientes.controller.ts && grep -q "PortalEmailService" src/modules/pacientes/pacientes.module.ts && cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - POST `/pacientes/:id/portal-link` returns { url, alreadyGenerated, smtpConfigured }
    - POST `/pacientes/:id/portal-link/email` sets the email when missing then sends; returns { enviado }
    - Both endpoints are under the staff `@Auth` guard; patient id taken from path param, never body
    - PortalEmailService registered in module; `npm run build` succeeds
  </acceptance_criteria>
  <done>Share endpoints live and SMTP-gated; module wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → portal-link API | Token must never leak in plaintext at rest; only staff may mint links |
| backend → patient email | Email send must fail closed when SMTP absent |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-10 | Information Disclosure | portalToken at rest | mitigate | PITFALL 1 — store sha256 hash (64-hex) only; raw UUID returned once in URL, never persisted |
| T-52-11 | Tampering | token regeneration | mitigate | D-12 idempotency — existing token never re-hashed/regenerated, keeping F54's link valid |
| T-52-12 | Elevation of Privilege | who can mint links | mitigate | Endpoints under staff `@Auth('ADMIN','PROFESIONAL','SECRETARIA','FACTURADOR')`; patient id from path, not body (PITFALL 12) |
| T-52-13 | Information Disclosure | email misfire | mitigate | enviarLinkPortal returns false when SMTP unconfigured; option hidden client-side via smtpConfigured flag |
| T-52-SC | Tampering | npm installs | accept | nodemailer already vendored; no new packages |
</threat_model>

<verification>
- `cd backend && npm run build` succeeds
- Smoke: POST `/pacientes/:id/portal-link` returns a url with a UUID path segment; DB portalToken is 64-hex
- Second POST returns alreadyGenerated:true with unchanged portalToken
</verification>

<success_criteria>
- Real, stable, hashed portal token generated idempotently
- Email share gated on SMTP; at-share email update supported
- All endpoints staff-scoped, patient id from path only
</success_criteria>

<output>
Create `.planning/phases/52-preop-hc-form-chip-catalogs/52-04-SUMMARY.md` when done.
</output>
