---
phase: 52-preop-hc-form-chip-catalogs
plan: 06
type: execute
wave: 4
depends_on: ["52-03", "52-05"]
files_modified:
  - frontend/src/components/live-turno/tabs/hc/PreoperatorioForm.tsx
  - frontend/src/components/live-turno/tabs/hc/HCCreatorForm.tsx
autonomous: true
requirements: [PREOP-01, PREOP-02]
must_haves:
  truths:
    - "Selecting the 'Pre Quirúrgico' template renders a structured, seccioned PREOPERATORIO form (not a free textarea) (D-04, D-05)"
    - "The form has chip sections for antecedentes, alergias and medicación, each with an 'Otro' Enter→chip input loaded from the professional's per-pro catalog (D-07)"
    - "On open, chips are pre-selected from the patient profile (condiciones/alergias/medicacion)"
    - "An optional 'Agregar diagnóstico/tratamiento' check expands the reused zona/diagnóstico/tratamiento selector, collapsed by default"
    - "An estudios complementarios checklist and a consentimiento informado check are present and bubble into the saved payload"
    - "Saving sends tipo 'pre_quirurgico' with antecedentes/alergias/medicacion/estudiosComplementarios/consentimientoInformado through useCreateHistoriaClinicaEntry"
  artifacts:
    - path: "frontend/src/components/live-turno/tabs/hc/PreoperatorioForm.tsx"
      provides: "Structured seccioned prequirúrgico form component (PREOP-01/02 UI)"
      contains: "PreoperatorioForm"
    - path: "frontend/src/components/live-turno/tabs/hc/HCCreatorForm.tsx"
      provides: "Swap to PreoperatorioForm on pre_quirurgico + pre_quirurgico save branch"
      contains: "pre_quirurgico"
  key_links:
    - from: "HCCreatorForm tipoSeleccionado === 'pre_quirurgico'"
      to: "PreoperatorioForm"
      via: "render swap (mirrors primera_vez block)"
      pattern: "tipoSeleccionado === 'pre_quirurgico'"
    - from: "PreoperatorioForm antecedentes/alergias/medicación sections"
      to: "useAntecedentesCatalogo/useAlergiasCatalogo/useMedicamentosCatalogo"
      via: "per-pro catalog chips"
      pattern: "useAntecedentesCatalogo"
    - from: "PreoperatorioForm dx/tratamiento section"
      to: "PrimeraConsultaForm"
      via: "reused zona/dx/tratamiento selector (D-08)"
      pattern: "PrimeraConsultaForm"
---

<objective>
Build the structured PREOPERATORIO form (PREOP-01) and wire it into `HCCreatorForm` so the 'Pre Quirúrgico' button renders it instead of the generic textarea fallback. The form is ONE seccioned page (D-04, NOT a wizard): three chip sections (antecedentes/alergias/medicación) with 'Otro' Enter→chip and patient-profile pre-load (D-09), an optional collapsible diagnóstico/tratamiento selector reusing `PrimeraConsultaForm` (D-08/PREOP-02), an estudios complementarios checklist (D-10), and a consentimiento informado check (D-11). The "Compartir link" section is added by Plan 07.

Purpose: PREOP-01 (structured seccioned form) + PREOP-02 (optional dx/tratamiento selector); the UI surface for the chips/estudios/consent persisted by Plans 03/05.
Output: new `PreoperatorioForm.tsx` + a `pre_quirurgico` render+save branch in `HCCreatorForm.tsx`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-preop-hc-form-chip-catalogs/52-CONTEXT.md
@.planning/phases/52-preop-hc-form-chip-catalogs/52-PATTERNS.md

<interfaces>
HCCreatorForm.tsx:
- Props `HCCreatorFormProps { pacienteId: string; ...; obraSocialId?: string }` (line 23)
- `tipoSeleccionado` state (line 60); `pvState` bubbled from PrimeraConsultaForm via onChange (line 63)
- `handleGuardar` branches by tipo (lines ~107-160): builds the mutation payload `{ pacienteId, tipo, tipoEntrada: PLANTILLA_TO_TIPO_ENTRADA[tipoSeleccionado], ... }`
- Render swap blocks: primera_vez (lines 229-236), tratamiento_en_consultorio (line 238), generic textarea fallback for all other tipos (line 335 — pre_quirurgico currently lands here; replace with the new branch)
- `PLANTILLA_TO_TIPO_ENTRADA['pre_quirurgico']` already returns 'PREOPERATORIO' (no change needed)

PrimeraConsultaForm.tsx:
- `export function PrimeraConsultaForm({ profesionalId, onChange, onGenerarPresupuesto, obraSocialId }: Props)` (line 66); bubbles `PrimeraConsultaFormState { zonas: ZonaSeleccionDto[]; comentario?; ... }` via `onChange`
- local `Chip({ label, selected, dashed, onClick })` (lines 35-64): `dashed` = border-dashed style for not-yet-saved "Otro" items — REPLICATE this Chip in PreoperatorioForm
- `handleDxNuevoEnter` (lines 202-240): Enter on the "Otro" input → dedup (lowercase) → push to selected + nuevos → clear input, keep section open — REPLICATE per chip section

Catalog hooks (Plan 05): `useAntecedentesCatalogo(profesionalId?)`, `useAlergiasCatalogo`, `useMedicamentosCatalogo` → each returns `CatalogoItem[] { id; nombre; esSistema }`; query-key constants exported (`ANTECEDENTES_CATALOGO_QUERY_KEY`, etc.)
Create hook (Plan 05): `useCreateHistoriaClinicaEntry` DTO accepts `antecedentes?: string[]`, `alergias?: string[]`, `medicacion?: string[]`, `estudiosComplementarios?: { laboratorio: boolean; ecg: boolean; imagenes: string[] }`, `consentimientoInformado?: boolean`, plus existing `tipo`, `tipoEntrada`, `zonas`, `comentario`.
Patient profile (D-09 pre-load): `usePaciente(pacienteId)` (frontend/src/hooks/usePaciente.ts) returns the patient incl. `condiciones[]`, `alergias[]`, `medicacion[]` (backend fields). Use these to pre-select chips on open.
Seed source: `frontend/src/components/CondicionesChips.tsx` `PREDEFINED` (line 11) is the antecedentes seed (matches backend Plan 01 seed).
Estudios shape (D-10, locked): `{ laboratorio: boolean; ecg: boolean; imagenes: string[] }` where `imagenes ⊆ { Ecografía, Tomografía, Mamografía, Otro }`.
Professional context: `useEffectiveProfessionalId` / professional-context store (see CLAUDE.md) supplies `profesionalId` for the catalog hooks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PreoperatorioForm.tsx (chips + Otro + profile pre-load)</name>
  <read_first>
    - frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx (Chip lines 35-64, handleDxNuevoEnter lines 202-240, onChange bubbling pattern)
    - frontend/src/components/CondicionesChips.tsx (PREDEFINED list + selected-merge pattern lines 11-33)
    - frontend/src/hooks/usePaciente.ts (patient profile shape — condiciones/alergias/medicacion)
    - frontend/src/hooks/useAntecedentesCatalogo.ts (Plan 05 — catalog hook + query key)
  </read_first>
  <action>Create `PreoperatorioForm.tsx` exporting `PreoperatorioForm` and a `PreoperatorioFormState` type `{ antecedentes: string[]; alergias: string[]; medicacion: string[]; estudiosComplementarios: { laboratorio: boolean; ecg: boolean; imagenes: string[] }; consentimientoInformado: boolean; zonas: ZonaSeleccionDto[]; comentario?: string }`. Props: `{ pacienteId: string; profesionalId?: string; obraSocialId?: string; onChange: (s: PreoperatorioFormState) => void }`. Replicate the local `Chip` component (with the `dashed` prop) from PrimeraConsultaForm. Render a single seccioned page (accordion or stacked section headers, collapsed/expand optional — NOT a multi-step wizard) with section headers: Antecedentes · Alergias · Medicación · (later sections in Task 2). For each of the three chip sections, load options from the matching catalog hook (`useAntecedentesCatalogo`/`useAlergiasCatalogo`/`useMedicamentosCatalogo` with the effective `profesionalId`), render the catalog items as chips, plus an 'Otro' Input below the group whose Enter replicates `handleDxNuevoEnter`: dedup case-insensitively, add to that section's `selected[]` and `nuevos[]`, render new items as `dashed` chips, clear the input. On mount, pre-select chips from `usePaciente(pacienteId)` profile arrays — `condiciones`→antecedentes, `alergias`→alergias, `medicacion`→medicacion (union with catalog matches; values not in the catalog still appear as selected dashed chips). Bubble the full `PreoperatorioFormState` up via `onChange` on every change (mirror PrimeraConsultaForm's onChange pattern).</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "PreoperatorioForm" src/components/live-turno/tabs/hc/PreoperatorioForm.tsx && grep -q "useAntecedentesCatalogo" src/components/live-turno/tabs/hc/PreoperatorioForm.tsx && grep -q "usePaciente" src/components/live-turno/tabs/hc/PreoperatorioForm.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - PreoperatorioForm.tsx exports PreoperatorioForm and PreoperatorioFormState with the locked field shape (antecedentes/alergias/medicacion string[], estudiosComplementarios {laboratorio,ecg,imagenes[]}, consentimientoInformado boolean, zonas, comentario?)
    - Three chip sections each source options from their per-pro catalog hook and render an 'Otro' input where Enter adds a dashed chip without saving
    - On open, a patient with condiciones=["Hipertensión"] has the Hipertensión antecedente chip pre-selected (and likewise for alergias/medicacion)
    - A typed 'Otro' value deselected before save is NOT included in the bubbled state
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PreoperatorioForm renders the three learning chip sections with profile pre-load and bubbles its state.</done>
</task>

<task type="auto">
  <name>Task 2: Add optional dx/tratamiento selector, estudios checklist, consentimiento check</name>
  <read_first>
    - frontend/src/components/live-turno/tabs/hc/PreoperatorioForm.tsx (the file from Task 1)
    - frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx (component reuse + PrimeraConsultaFormState.zonas)
  </read_first>
  <action>In PreoperatorioForm.tsx add three more sections below the chip sections. (1) Optional dx/tratamiento (PREOP-02/D-08): a checkbox "Agregar diagnóstico/tratamiento" (default unchecked, section collapsed); when checked, render the EXISTING `<PrimeraConsultaForm profesionalId={profesionalId} obraSocialId={obraSocialId} onChange={...} />` and copy its `zonas` into `PreoperatorioFormState.zonas`; when unchecked, `zonas` is `[]`. Do NOT fork PrimeraConsultaForm — reuse it. (2) Estudios complementarios (D-10): checkboxes for `laboratorio` and `ecg`, and an "Imágenes" multi-select of `Ecografía / Tomografía / Mamografía / Otro` writing into `estudiosComplementarios.imagenes: string[]`; keep the locked shape `{ laboratorio, ecg, imagenes }`. (3) Consentimiento informado (D-11): a single checkbox "Paciente informado del consentimiento" setting `consentimientoInformado: boolean` — label/wording must make clear this is "informado", NOT a signature. Every change re-bubbles `onChange`.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "PrimeraConsultaForm" src/components/live-turno/tabs/hc/PreoperatorioForm.tsx && grep -q "estudiosComplementarios" src/components/live-turno/tabs/hc/PreoperatorioForm.tsx && grep -q "consentimientoInformado" src/components/live-turno/tabs/hc/PreoperatorioForm.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The "Agregar diagnóstico/tratamiento" checkbox is unchecked by default and only renders PrimeraConsultaForm when checked; its zonas flow into PreoperatorioFormState.zonas; unchecking clears zonas to []
    - Estudios section produces exactly the shape { laboratorio: boolean, ecg: boolean, imagenes: string[] } with imagenes ⊆ {Ecografía,Tomografía,Mamografía,Otro}
    - Consentimiento section sets a single consentimientoInformado boolean and is labelled as "informado" (no signature wording)
    - PrimeraConsultaForm is reused (imported), not duplicated
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Optional dx/tratamiento selector, estudios checklist, and consentimiento check complete the form body.</done>
</task>

<task type="auto">
  <name>Task 3: Swap HCCreatorForm to PreoperatorioForm + pre_quirurgico save branch</name>
  <read_first>
    - frontend/src/components/live-turno/tabs/hc/HCCreatorForm.tsx (render swap blocks lines 229-238 + 335 fallback; handleGuardar branches lines 107-160)
    - frontend/src/components/live-turno/tabs/hc/PreoperatorioForm.tsx (PreoperatorioFormState export)
    - frontend/src/hooks/useCreateHistoriaClinicaEntry.ts (DTO PREOP fields from Plan 05)
  </read_first>
  <action>In HCCreatorForm.tsx: import `PreoperatorioForm` and `PreoperatorioFormState`; add `const [preopState, setPreopState] = useState<PreoperatorioFormState | null>(null)`. Add a render branch `{tipoSeleccionado === 'pre_quirurgico' && <PreoperatorioForm pacienteId={pacienteId} profesionalId={...} obraSocialId={obraSocialId} onChange={setPreopState} />}` mirroring the primera_vez block (lines 229-236) — and ensure `pre_quirurgico` no longer falls into the generic textarea fallback at line 335 (exclude it like primera_vez/tratamiento_en_consultorio are excluded). In `handleGuardar`, add an `else if (tipoSeleccionado === 'pre_quirurgico')` branch that builds the mutation payload: `{ pacienteId, tipo: 'pre_quirurgico', tipoEntrada: PLANTILLA_TO_TIPO_ENTRADA['pre_quirurgico'] ?? 'PREOPERATORIO', antecedentes: preopState?.antecedentes ?? [], alergias: preopState?.alergias ?? [], medicacion: preopState?.medicacion ?? [], estudiosComplementarios: preopState?.estudiosComplementarios, consentimientoInformado: preopState?.consentimientoInformado ?? false, zonas: preopState?.zonas ?? [], comentario: preopState?.comentario }`. Reuse the existing mutation (`useCreateHistoriaClinicaEntry`) and the existing success/close handling used by the other branches. Do not change unrelated branches.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "PreoperatorioForm" src/components/live-turno/tabs/hc/HCCreatorForm.tsx && grep -q "tipoSeleccionado === 'pre_quirurgico'" src/components/live-turno/tabs/hc/HCCreatorForm.tsx && cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npm run build && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - Selecting 'Pre Quirúrgico' renders <PreoperatorioForm/> and NOT the generic textarea fallback
    - handleGuardar's pre_quirurgico branch sends tipo 'pre_quirurgico', tipoEntrada 'PREOPERATORIO', and the antecedentes/alergias/medicacion/estudiosComplementarios/consentimientoInformado/zonas fields from preopState through useCreateHistoriaClinicaEntry
    - Existing primera_vez and tratamiento_en_consultorio branches are unchanged
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>The 'Pre Quirúrgico' template renders the structured form and saves the full PREOP payload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| frontend form → create-entry API | DTO arrays/flags are advisory; backend (Plan 03) re-derives profesionalId from JWT and authorizes scope |
| frontend → catalog API | Read-only; profesionalId is a non-secret scoping hint, authorized server-side |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-16 | Tampering | catalog scope | mitigate | profesionalId comes from the professional-context store/JWT, not user-entered; backend (Plan 02) re-scopes server-side (PITFALL 12) |
| T-52-17 | Information Disclosure | consent semantics | mitigate | UI labels the check "informado" only; never writes/implies a signature (consentimientoFirmadoAt is backend-untouched per Plan 03/D-11) |
| T-52-18 | Tampering | profile pre-load | accept | Pre-load is read-only display; the authoritative union-dedup merge happens server-side (Plan 03) |
| T-52-SC | Tampering | npm installs | accept | No new packages in this plan (QR dependency is added in Plan 07) |
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds
- Manual: choosing 'Pre Quirúrgico' shows a seccioned form with antecedentes/alergias/medicación chips, an optional dx/tratamiento toggle, estudios checklist, and consentimiento informado check — no free textarea
</verification>

<success_criteria>
- PREOP-01: structured seccioned PREOPERATORIO form rendered from HCCreatorForm
- PREOP-02: optional, collapsed dx/tratamiento selector reusing PrimeraConsultaForm
- Chips learn via 'Otro', pre-load from profile, and the full payload reaches the create-entry mutation
</success_criteria>

<output>
Create `.planning/phases/52-preop-hc-form-chip-catalogs/52-06-SUMMARY.md` when done.
</output>
