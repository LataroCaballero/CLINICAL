---
phase: 52-preop-hc-form-chip-catalogs
plan: 07
type: execute
wave: 5
depends_on: ["52-04", "52-06"]
files_modified:
  - frontend/src/components/live-turno/tabs/hc/SharePortalPanel.tsx
  - frontend/src/components/live-turno/tabs/hc/PreoperatorioForm.tsx
  - frontend/src/hooks/usePortalLink.ts
  - frontend/package.json
autonomous: false
requirements: [PREOP-11, PREOP-12]
must_haves:
  truths:
    - "From the prequirúrgico form the professional can copy the portal link, open a WhatsApp share link, and show a scannable QR of the same URL"
    - "The Email option appears only when the backend reports SMTP is configured"
    - "If the patient has no email, the professional can enter one at share time before sending"
    - "The UI uses the URL returned by the backend (raw-uuid path); it never sees or constructs the sha256 hash"
  artifacts:
    - path: "frontend/src/components/live-turno/tabs/hc/SharePortalPanel.tsx"
      provides: "Copy / WhatsApp / QR / Email share panel for the portal link"
      contains: "SharePortalPanel"
    - path: "frontend/src/hooks/usePortalLink.ts"
      provides: "Mutations for POST /pacientes/:id/portal-link and /portal-link/email"
      contains: "portal-link"
  key_links:
    - from: "usePortalLink"
      to: "POST /pacientes/:id/portal-link"
      via: "generate/fetch link + smtpConfigured flag"
      pattern: "portal-link"
    - from: "SharePortalPanel email option"
      to: "smtpConfigured flag from portal-link response"
      via: "conditional render (D-13)"
      pattern: "smtpConfigured"
---

<objective>
Add the "Compartir link" section to the prequirúrgico form (Success Criteria #5 / PREOP-11/12): copy the portal link, a WhatsApp share link, a scannable QR, and an SMTP-gated email send. The link/URL comes from the Plan 04 endpoints; the DB stores only the sha256 hash, so the frontend strictly consumes the URL the backend returns (raw UUID in the path) and never handles the hash. A QR library must be added (none installed).

Purpose: PREOP-11 (copy/WhatsApp/QR) + PREOP-12 (email gated on SMTP, set email at share time) — the share affordance of SC#5.
Output: `usePortalLink` hook, `SharePortalPanel.tsx`, a QR dependency, and a "Compartir link" section wired into PreoperatorioForm.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-preop-hc-form-chip-catalogs/52-CONTEXT.md
@.planning/phases/52-preop-hc-form-chip-catalogs/52-04-PLAN.md

<interfaces>
Backend endpoints (Plan 04):
- POST `/pacientes/:id/portal-link` → `{ url: string | null, alreadyGenerated: boolean, smtpConfigured: boolean }` (url is the FRONTEND_URL/portal/<rawUuid> link; when alreadyGenerated and a fresh url is not returned, re-call to obtain it / backend returns the stable url)
- POST `/pacientes/:id/portal-link/email` with body `{ email?: string }` → `{ enviado: boolean }`
api client: `frontend/src/lib/api.ts` (axios + JWT interceptor); mutation pattern: see existing hooks in `frontend/src/hooks/` (useMutation).
WhatsApp share: `https://wa.me/?text=${encodeURIComponent(url)}` (no number — opens contact picker) is acceptable.
Copy: `navigator.clipboard.writeText(url)`.
QR: no library installed (D / Claude's Discretion) — add `qrcode.react` (React component, lightweight) and render `<QRCodeSVG value={url} />`.
PreoperatorioForm.tsx (Plan 06): exports PreoperatorioForm with `pacienteId` prop; add the share section at the bottom.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QR dependency</name>
  <read_first>
    - frontend/package.json (dependencies block + existing versions style)
  </read_first>
  <action>Add `qrcode.react` to frontend dependencies and install it (`cd frontend && npm install qrcode.react`). This is the QR library choice left to discretion in CONTEXT.md. Marked autonomous:false because installing a package may require network/lockfile review.</action>
  <verify>
    <automated>cd frontend && node -e "require.resolve('qrcode.react')" && grep -q "qrcode.react" package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - qrcode.react present in package.json dependencies and resolvable
    - package-lock.json updated
  </acceptance_criteria>
  <done>QR library available for the share panel.</done>
</task>

<task type="auto">
  <name>Task 2: usePortalLink hook</name>
  <read_first>
    - frontend/src/hooks/useCreatePaciente.ts (an existing useMutation hook for shape/error pattern)
    - frontend/src/lib/api.ts (axios instance)
    - .planning/phases/52-preop-hc-form-chip-catalogs/52-04-PLAN.md (endpoint contracts)
  </read_first>
  <action>Create `usePortalLink.ts` exposing two mutations: `useGenerarPortalLink()` → `POST /pacientes/${id}/portal-link` returning `{ url, alreadyGenerated, smtpConfigured }`, and `useEnviarPortalLinkEmail()` → `POST /pacientes/${id}/portal-link/email` with body `{ email?: string }` returning `{ enviado }`. Type the responses. The patient id is passed as a function argument and goes in the URL path only — never accept it from a free-form body field in the hook surface.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "portal-link" src/hooks/usePortalLink.ts && grep -q "smtpConfigured" src/hooks/usePortalLink.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - usePortalLink.ts exports a generate mutation (returns url/alreadyGenerated/smtpConfigured) and an email mutation (accepts optional email, returns enviado)
    - id is used only in the URL path
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Typed mutations for the portal-link endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: SharePortalPanel + wire into PreoperatorioForm</name>
  <read_first>
    - frontend/src/components/live-turno/tabs/hc/PreoperatorioForm.tsx (Plan 06 — where to mount the section + pacienteId prop)
    - frontend/src/hooks/usePortalLink.ts (the Task-2 hook)
    - frontend/src/components/ui (existing Button/Input/Dialog primitives to reuse)
  </read_first>
  <action>Create `SharePortalPanel.tsx` (props `{ pacienteId: string; pacienteEmail?: string }`). On first interaction (e.g. a "Generar link" / "Compartir" button), call the generate mutation; once `url` is available render: a "Copiar link" button (`navigator.clipboard.writeText(url)` + copied feedback), a WhatsApp link button (`https://wa.me/?text=${encodeURIComponent(url)}`), a QR via `<QRCodeSVG value={url} />` (toggle to show), and — ONLY when the response `smtpConfigured` is true (D-13) — an "Enviar por email" control. The email control: if `pacienteEmail` is empty, show an Input to enter one and pass it as `{ email }` to the email mutation; otherwise send to the existing email; show enviado/error feedback. The panel must use the backend-returned `url` verbatim; do NOT build the URL from any token/hash client-side. Then add a "Compartir link" section at the bottom of `PreoperatorioForm.tsx` rendering `<SharePortalPanel pacienteId={pacienteId} pacienteEmail={...from usePaciente} />`.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "SharePortalPanel" src/components/live-turno/tabs/hc/SharePortalPanel.tsx && grep -q "QRCodeSVG" src/components/live-turno/tabs/hc/SharePortalPanel.tsx && grep -q "smtpConfigured" src/components/live-turno/tabs/hc/SharePortalPanel.tsx && grep -q "SharePortalPanel" src/components/live-turno/tabs/hc/PreoperatorioForm.tsx && cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npm run build && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - SharePortalPanel offers Copiar link, WhatsApp link, and a scannable QR of the backend-returned url
    - The Email option is rendered only when smtpConfigured is true; when the patient has no email, an input lets the professional enter one before sending
    - The QR/copy/WhatsApp all use the exact url string returned by POST /pacientes/:id/portal-link (no client-side hash/token construction)
    - PreoperatorioForm renders the "Compartir link" section via SharePortalPanel
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Professionals can copy/WhatsApp/QR/email the portal link from the prequirúrgico template, SMTP-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| frontend → portal-link API | Only staff (JWT) mint/fetch the link; patient id is a path param, never a body field |
| frontend display → portal URL | The URL contains the raw UUID once; the sha256 hash never reaches the client |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-19 | Information Disclosure | portal token in client | mitigate | UI consumes only the backend-returned url (raw-uuid path); the sha256 hash is server-only (Plan 04 / PITFALL 1); do not log the url to analytics/console |
| T-52-20 | Information Disclosure | email misfire | mitigate | Email control hidden unless smtpConfigured=true (D-13); send fails closed server-side (Plan 04) |
| T-52-21 | Tampering | patient scope | mitigate | usePortalLink takes the patient id in the URL path only; backend endpoints are under the staff @Auth guard (PITFALL 12) |
| T-52-SC | Tampering | npm installs | mitigate | Adds qrcode.react (Task 1, autonomous:false → reviewed); QR renders locally, no network calls |
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds
- Manual: from a prequirúrgico form, "Compartir link" generates a link, copy works, the QR renders, WhatsApp opens with the url, and the Email option appears only on an SMTP-configured backend
</verification>

<success_criteria>
- PREOP-11: copy link, WhatsApp link, and scannable QR available from the template
- PREOP-12: email send gated on SMTP; missing patient email can be set at share time
- Frontend never handles the hashed token — only the backend-returned URL
</success_criteria>

<output>
Create `.planning/phases/52-preop-hc-form-chip-catalogs/52-07-SUMMARY.md` when done.
</output>
