---
phase: 52-preop-hc-form-chip-catalogs
plan: 10
type: execute
wave: 2
depends_on: ["52-09"]
gap_closure: true
autonomous: true
requirements: [PREOP-12]
files_modified:
  - frontend/src/hooks/usePortalLink.ts
  - frontend/src/components/live-turno/tabs/hc/SharePortalPanel.tsx
  - backend/src/modules/pacientes/portal-email.service.ts
  - backend/src/modules/pacientes/pacientes.controller.ts

must_haves:
  truths:
    - "Al abrir el share panel de un paciente con link previo, se muestra la url existente con Copiar/WhatsApp/QR — sin spinner muerto ni pantalla de Generar"
    - "Para un paciente sin token (o legacy sin columna cifrada) el panel muestra el botón Generar, no un spinner colgado; al generar aparece la url"
    - "(Opcional) cuando el envío de email falla, el banner muestra el código real de error SMTP (EAUTH/ECONNECTION/...) sin filtrar credenciales ni host"
  artifacts:
    - path: "frontend/src/hooks/usePortalLink.ts"
      provides: "Hook useQuery que hace GET portal-link en mount (sólo lectura, no genera)"
      contains: "useObtenerPortalLink"
    - path: "frontend/src/components/live-turno/tabs/hc/SharePortalPanel.tsx"
      provides: "Render del link existente al montar; sin estado atascado en Generar"
      contains: "useObtenerPortalLink"
  key_links:
    - from: "frontend/src/components/live-turno/tabs/hc/SharePortalPanel.tsx"
      to: "GET /pacientes/:id/portal-link"
      via: "useObtenerPortalLink (useQuery en mount)"
      pattern: "useObtenerPortalLink"
---

<objective>
Cierra el lado frontend de Gap B (52-UAT Test 13): el share panel queda atascado en la pantalla
"Generar link" para pacientes que ya generaron su link, porque la url sólo vivía en estado local de
React y se perdía al recargar. Con el backend de 52-09 devolviendo la url recuperable (cifrada) vía
un GET de sólo lectura, el panel ahora debe consultar el link existente al montar y mostrarlo.

Incluye una tarea OPCIONAL de baja prioridad (Gap A ya está RESUELTO por infra/DNS — NO se planifica
nada de delivery de email): surfacing del código real de error SMTP para que un futuro misconfig sea
diagnosticable desde la UI en vez del banner genérico.

Purpose: Que un link ya generado siempre se muestre y se pueda compartir; nunca un dead spinner.
Output: Hook GET de sólo lectura, SharePortalPanel que carga el link en mount, y (opcional) banner
de email con el código de error real.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-preop-hc-form-chip-catalogs/52-UAT.md
@.planning/phases/52-preop-hc-form-chip-catalogs/52-09-PLAN.md

@frontend/src/hooks/usePortalLink.ts
@frontend/src/components/live-turno/tabs/hc/SharePortalPanel.tsx
@backend/src/modules/pacientes/portal-email.service.ts
@backend/src/modules/pacientes/pacientes.controller.ts

<interfaces>
<!-- Contrato del backend tras 52-09 (este plan lo consume). -->

GET /pacientes/:id/portal-link (nuevo en 52-09, sólo lectura, NO genera):
```typescript
// respuesta
{ url: string | null; alreadyGenerated: boolean; legacy: boolean; smtpConfigured: boolean }
// url presente  → mostrar vista de link
// url null + legacy true  → token viejo sin cifrar → mostrar botón Generar (regenera/backfillea, rota el link)
// url null + alreadyGenerated false → sin token → mostrar botón Generar
```

POST /pacientes/:id/portal-link (modificado en 52-09): ahora devuelve la url estable también para
tokens ya generados (cifrados); para legacy rota y devuelve url nueva. Respuesta:
`{ url: string | null; alreadyGenerated: boolean; smtpConfigured: boolean }`.

From frontend/src/hooks/usePortalLink.ts (estado actual):
```typescript
export interface GenerarPortalLinkResponse { url: string | null; alreadyGenerated: boolean; smtpConfigured: boolean; }
export interface EnviarPortalLinkEmailResponse { enviado: boolean; motivo?: 'sin_destinatario' | 'envio_fallido'; }
export function useGenerarPortalLink(): UseMutationResult<...>;   // POST, mantener
export function useEnviarPortalLinkEmail(): UseMutationResult<...>; // POST email, mantener
```

From frontend/src/components/live-turno/tabs/hc/SharePortalPanel.tsx (estado actual):
- `const [url, setUrl] = useState<string|null>(null)` arranca null en cada montaje → causa del Gap B.
- `if (!url) return (<pantalla Generar/spinner>)` (línea ~103). `handleGenerar` hace POST.
- Bloque email gateado por `smtpConfigured` (D-13); banners por `emailErrorMotivo`.
- Props: `{ pacienteId: string; pacienteEmail?: string }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend — cargar el link existente en mount y mostrarlo</name>
  <files>frontend/src/hooks/usePortalLink.ts, frontend/src/components/live-turno/tabs/hc/SharePortalPanel.tsx</files>
  <action>
En `usePortalLink.ts`:
- Agregar `export interface ObtenerPortalLinkResponse { url: string | null; alreadyGenerated: boolean; legacy: boolean; smtpConfigured: boolean; }`.
- Agregar `export function useObtenerPortalLink(pacienteId: string)` usando `useQuery` (TanStack Query)
  con queryKey `['portal-link', pacienteId]`, queryFn que hace `api.get<ObtenerPortalLinkResponse>(\`/pacientes/${pacienteId}/portal-link\`)`.
  `enabled: !!pacienteId`. NO usar mutation para el GET (es lectura idempotente, no debe generar token).
- Mantener intactos useGenerarPortalLink (POST) y useEnviarPortalLinkEmail.

En `SharePortalPanel.tsx`:
- Llamar `const { data: actual, isLoading } = useObtenerPortalLink(pacienteId)` al tope del componente.
- Inicializar el estado a partir de `actual` cuando llega: si `actual.url` → setUrl(actual.url),
  setAlreadyGenerated(actual.alreadyGenerated), setSmtpConfigured(actual.smtpConfigured)
  (usar un useEffect que dependa de `actual`, o derivar directo). El estado local sigue usándose
  para la url generada por el botón (handleGenerar) — no romper ese flujo.
- Estados de render:
  - `isLoading` (primer fetch) → spinner ACOTADO con texto "Cargando link…" (no infinito; termina cuando resuelve la query).
  - resuelto con `url` (de actual o de generar) → vista de link existente (Copiar/WhatsApp/QR + bloque email gateado por smtpConfigured), tal cual hoy.
  - resuelto SIN url (sin token o legacy) → pantalla "Generar link" actual; handleGenerar (POST) sigue funcionando y al volver con url la muestra. Para legacy, el botón Generar efectivamente recupera/rota el link (backend 52-09).
- Tras un generar exitoso, invalidar o setear la query `['portal-link', pacienteId]` para mantener consistencia (opcional pero preferible vía queryClient.setQueryData).
- Eliminar la posibilidad de quedar en spinner muerto: el spinner sólo se muestra mientras la query
  está realmente pendiente; al resolver, SIEMPRE cae en vista-link o vista-generar.
Respetar shadcn/ui y los componentes ya importados (Button, Input, iconos lucide).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep -c "error TS" | grep -qx 0 && grep -q "useObtenerPortalLink" src/components/live-turno/tabs/hc/SharePortalPanel.tsx</automated>
  </verify>
  <done>Al montar el panel para un paciente con link previo se muestra la url existente con las opciones de compartir; para sin-token/legacy se muestra el botón Generar; no hay rama que deje un spinner colgado tras resolver la query; compila sin errores de tipo.</done>
</task>

<task type="auto">
  <name>Task 2 (OPCIONAL, baja prioridad): surface del código real de error SMTP</name>
  <files>backend/src/modules/pacientes/portal-email.service.ts, backend/src/modules/pacientes/pacientes.controller.ts, frontend/src/hooks/usePortalLink.ts, frontend/src/components/live-turno/tabs/hc/SharePortalPanel.tsx</files>
  <action>
OPCIONAL — implementar sólo si hay presupuesto de contexto; no bloquea el cierre del gap. Gap A
(delivery) ya está RESUELTO por infra/DNS: NO tocar la lógica de envío ni isSmtpConfigured más allá
de exponer el código de error.

Objetivo: cuando `transporter.sendMail` falla, surfacing del `error.code` de nodemailer
(EAUTH, ECONNECTION, ETIMEDOUT, ETLS, EENVELOPE, etc.) para que el operador diagnostique, en vez del
banner genérico "Intentá nuevamente en unos minutos".

Backend `portal-email.service.ts`: en el catch de `enviarLinkPortal` (líneas ~81-84), además de
`logger.error`, retornar el código. Cambiar la firma a `Promise<{ enviado: boolean; codigo?: string }>`
devolviendo `{ enviado:false, codigo: (error as { code?: string })?.code ?? 'UNKNOWN' }` en el catch
y `{ enviado:true }` en éxito. SÓLO el `error.code` (enum corto) — NUNCA el mensaje completo, host,
usuario ni credenciales (T-52-12).

Controller `enviarPortalLinkEmail` (línea ~281): adaptar al nuevo retorno; cuando `enviado:false`
con codigo, devolver `{ enviado:false, motivo:'envio_fallido', codigo }`. Mantener validarPortalUrl
+ CR-01/T-52-01 EXACTAMENTE como están.

Frontend `usePortalLink.ts`: agregar `codigo?: string` a `EnviarPortalLinkEmailResponse`.
`SharePortalPanel.tsx`: cuando `emailErrorMotivo === 'envio_fallido'`, si hay `codigo`, mostrarlo en
el banner (ej. "No se pudo enviar el email (SMTP: EAUTH). Revisá la configuración."). Guardar el
codigo en estado junto a emailErrorMotivo.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <done>Ante fallo de sendMail el endpoint devuelve motivo:'envio_fallido' + codigo (sólo el error.code, sin host/credenciales); el banner muestra el código; el path de delivery e isSmtpConfigured quedan funcionalmente intactos. Si no se implementa por presupuesto, dejar nota en el SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client(staff) → GET portal-link | El panel consume la url recuperable; sólo staff autenticado (guard heredado en 52-09) |
| backend → banner de error SMTP | El código de error no debe filtrar credenciales/host del SMTP |
| client → email controller body | CR-01/T-52-01: la url provista se valida same-origin antes de reflejarse en el email |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-12 | Information Disclosure | surfacing de error SMTP (tarea opcional) | mitigate | Sólo se devuelve `error.code` (enum corto: EAUTH/ECONNECTION/...); nunca el mensaje completo, host, usuario ni password. |
| T-52-01 (regression guard) | Tampering | email controller (validarPortalUrl) | mitigate | La tarea opcional NO toca validarPortalUrl ni CR-01; la url sigue validándose same-origin antes de reflejarse en el email. |
| T-52-SC | Tampering | npm installs | accept | No se agregan paquetes (TanStack Query, nodemailer ya vendored). |
</threat_model>

<verification>
- `npx tsc --noEmit` en frontend y backend sin errores nuevos.
- Manual (post-deploy del backend 52-09): abrir el share panel de un paciente con link previo → se
  muestra la url existente sin spinner colgado; de un paciente sin token → botón Generar; al generar → url.
- Si se hace la tarea opcional: forzar un fallo SMTP en dev y confirmar que el banner muestra el código.
</verification>

<success_criteria>
- El share panel nunca queda atascado en spinner para un paciente con link existente; la url se carga en mount y es compartible.
- Para sin-token/legacy se muestra el botón Generar y al generar aparece la url.
- (Opcional) el banner de fallo de email muestra el código SMTP real sin filtrar secretos.
</success_criteria>

<output>
Create `.planning/phases/52-preop-hc-form-chip-catalogs/52-10-SUMMARY.md` when done
</output>
