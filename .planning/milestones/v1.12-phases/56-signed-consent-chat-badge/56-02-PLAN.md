---
phase: 56-signed-consent-chat-badge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/mensajes-internos/mensajes-internos.service.ts
  - backend/src/modules/catalogo-hc/catalogo-hc.service.ts
  - backend/src/main.ts
autonomous: true
requirements: [CHAT-03, CONS-07, CONS-04]
must_haves:
  truths:
    - "D-13: The mensajes-internos DTO exposes origenPaciente for each message so the chat can distinguish patient-origin messages"
    - "actualizarIndicacionesUrl rejects non-http(s) and over-length URLs before persisting (CR-01 stored-XSS blocker closed)"
    - "The Express JSON body limit is raised to 2mb so a base64 signature PNG POST does not 413"
  artifacts:
    - path: "backend/src/modules/mensajes-internos/mensajes-internos.service.ts"
      provides: "origenPaciente in select + DTO map"
      contains: "origenPaciente"
    - path: "backend/src/modules/catalogo-hc/catalogo-hc.service.ts"
      provides: "imperative URL protocol + length validation"
      contains: "new URL"
    - path: "backend/src/main.ts"
      provides: "2mb express json/urlencoded body limit"
      contains: "limit: '2mb'"
  key_links:
    - from: "mensajes-internos.service map"
      to: "MensajeInterno.origenPaciente"
      via: "select + spread into DTO"
      pattern: "origenPaciente: m.origenPaciente"
---

<objective>
Land the three independent backend enablers this phase needs before the portal signature flow and chat badge can work: expose `origenPaciente` from the mensajes-internos DTO (CHAT-03 backend), close the CR-01 stored-XSS blocker by validating `indicacionesUrl` imperatively at the write point, and raise the Express body limit to 2mb so the base64 signature POST does not return 413.

Purpose: These are prerequisites — CHAT-03 frontend needs the field, the portal indicaciones link (CONS-07) may not be exposed to patients until CR-01 is fixed, and the signature POST (CONS-04) fails without the larger body limit.
Output: Three small, targeted edits with no schema dependency.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/56-signed-consent-chat-badge/56-RESEARCH.md
@.planning/phases/56-signed-consent-chat-badge/56-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose origenPaciente in mensajes-internos DTO (CHAT-03 backend)</name>
  <files>backend/src/modules/mensajes-internos/mensajes-internos.service.ts</files>
  <read_first>
    - backend/src/modules/mensajes-internos/mensajes-internos.service.ts — `findByPaciente` (lines 108-139): the typed `select` block (~112) and the `.map()` DTO shape (~135-139)
    - .planning/phases/56-signed-consent-chat-badge/56-RESEARCH.md §"Pattern 6" (lines 550-576) and Pitfall G (lines 865-867)
    - .planning/phases/56-signed-consent-chat-badge/56-PATTERNS.md §mensajes-internos.service.ts (lines 185-210)
  </read_first>
  <action>
    In `findByPaciente`, add `origenPaciente: true` to the `select` block (immediately after `esSistema: true`), and add `origenPaciente: m.origenPaciente` to the object returned by `.map()` (alongside `esPropio`/`leido`). `origenPaciente` already exists as a Boolean on the `MensajeInterno` model and is already written `true` by `paciente-portal.service.ts crearConsulta` — this only surfaces it. Do not change any other field. Both edits are required together (Pitfall G: if the field is not in `select`, the map spread yields `undefined`).
  </action>
  <acceptance_criteria>
    - `grep -c "origenPaciente" backend/src/modules/mensajes-internos/mensajes-internos.service.ts` returns >= 2
    - Both the `select` block and the `.map()` return include `origenPaciente`
    - `cd backend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit</automated>
  </verify>
  <done>findByPaciente selects and returns origenPaciente for every message; type check passes.</done>
</task>

<task type="auto">
  <name>Task 2: [CR-01 BLOCKER] Validate indicacionesUrl at the write point</name>
  <files>backend/src/modules/catalogo-hc/catalogo-hc.service.ts</files>
  <read_first>
    - backend/src/modules/catalogo-hc/catalogo-hc.service.ts — `actualizarIndicacionesUrl` (lines 760-773); confirm `BadRequestException` is already imported
    - .planning/phases/56-signed-consent-chat-badge/56-RESEARCH.md §"Pattern 5" (lines 493-524) — the exact validation block; §"Don't Hand-Roll" URL row (line 626)
    - .planning/phases/56-signed-consent-chat-badge/56-CONTEXT.md §Reviewed Todos (CR-01, lines 150-151) — this is a mandatory, non-foldable blocker
  </read_first>
  <action>
    Prepend imperative validation to `actualizarIndicacionesUrl`, BEFORE the existing ownership guard: when `indicacionesUrl !== null`, reject with `BadRequestException` if `length > 2048`, parse with `new URL(indicacionesUrl)` inside try/catch (throw `BadRequestException('URL de indicaciones inválida')` on parse failure), and reject unless `parsed.protocol` is `http:` or `https:` (blocks `javascript:` / `data:` stored-XSS vectors). Preserve the existing null path (allow clearing the link). The existing `@IsUrl` decorator is dead code here because there is no global ValidationPipe — this imperative check at the write point is the real defense.
  </action>
  <acceptance_criteria>
    - `grep -c "new URL" backend/src/modules/catalogo-hc/catalogo-hc.service.ts` returns >= 1
    - The method rejects protocols other than http/https and lengths > 2048 while still allowing `null`
    - `grep "protocol" backend/src/modules/catalogo-hc/catalogo-hc.service.ts` matches an http/https allow-list check
    - `cd backend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit</automated>
  </verify>
  <done>actualizarIndicacionesUrl validates protocol (http/https only) + max length before persisting, and still permits clearing to null.</done>
</task>

<task type="auto">
  <name>Task 3: Raise Express JSON/urlencoded body limit to 2mb</name>
  <files>backend/src/main.ts</files>
  <read_first>
    - backend/src/main.ts (lines 12-46) — the `bootstrap` function; `NestFactory.create` (~15, note `rawBody: true`) and `app.listen` (~45)
    - .planning/phases/56-signed-consent-chat-badge/56-RESEARCH.md §"Pattern 3" body-limit note (lines 413-420) and Pitfall D (lines 853-855); Assumption A2 (line 940)
  </read_first>
  <action>
    After `NestFactory.create(...)` and before `app.listen(...)`, register `express.json({ limit: '2mb' })` and `express.urlencoded({ extended: true, limit: '2mb' })` via `app.use(...)` (import express as needed). This lifts the default 100KB limit so a ~200KB base64 signature PNG POST does not return 413. The existing `rawBody: true` flag is a separate concern and stays unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "limit: '2mb'" backend/src/main.ts` returns >= 1 (present in both json and urlencoded)
    - `rawBody: true` remains present in `NestFactory.create`
    - `cd backend && npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npm run build</automated>
  </verify>
  <done>main.ts applies a 2mb body limit for json + urlencoded; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staff client → catalogo-hc API | `indicacionesUrl` is untrusted input that will later be rendered as a link to the patient |
| Patient browser → portal API | Large base64 body crosses into the app; body-limit tuning affects DoS surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-04 | Injection/XSS | catalogo-hc.service.ts actualizarIndicacionesUrl | mitigate | Imperative `new URL()` parse + http/https-only protocol allow-list + maxLength 2048 at the write point (CR-01); frontend adds a second render-time `^https?://` guard (Plan 06) |
| T-56-05 | Denial of Service | main.ts body limit | mitigate | Limit capped at 2mb (not unbounded); the firma route additionally enforces a per-buffer PNG size guard (Plan 05) and strict throttling from F53 |
| T-56-06 | Information disclosure | mensajes-internos DTO | accept | `origenPaciente` is a non-sensitive Boolean already scoped to the requesting user's patient thread; no new data class exposed |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` pass in backend.
- `origenPaciente` present in both select and map of `findByPaciente`.
- `actualizarIndicacionesUrl` rejects `javascript:`/`data:` and over-length URLs, allows null.
- main.ts shows `limit: '2mb'` for json and urlencoded.
</verification>

<success_criteria>
The chat DTO carries `origenPaciente`, the CR-01 stored-XSS blocker is closed at the write point, and the signature POST body limit is raised — all three prerequisites for the phase's downstream plans are met.
</success_criteria>

<output>
Create `.planning/phases/56-signed-consent-chat-badge/56-02-SUMMARY.md` when done.
</output>
