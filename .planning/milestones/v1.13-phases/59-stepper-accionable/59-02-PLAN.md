---
phase: 59-stepper-accionable
plan: 02
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - frontend/src/components/crm/CardActionsSheet.tsx
  - frontend/src/app/dashboard/turnos/SurgeryAppointmentModal.tsx
  - frontend/src/components/patient/PatientDrawer/views/HCCreatorDialog.tsx
autonomous: true
requirements: [STEPPER-03, STEPPER-04, STEPPER-05, STEPPER-06]
decisions_covered: [D-07, D-08, D-09, D-10, D-12]
must_haves:
  truths:
    - "D-07: el prellenado del presupuesto usa ítems estructurados (descripcion + precio del catálogo), matcheando patient.procedimiento contra el catálogo del profesional — no solo el string; confirmado por el usuario como resolución para Phase 59"
    - "D-08: se reusa GenerarPresupuestoModal (acepta initialItems + picker de catálogo) en lugar del diálogo de PresupuestosView sin prefill — cláusula de escape de D-08, aprobado por el usuario"
    - "D-09: agendar cirugía abre un modal inline con el paciente pre-seleccionado, sin navegar fuera del CRM"
    - "D-10: el turno de cirugía queda registrado como Cirugia (POST /turnos/cirugia) → pasos.cirugia pasa a 'completo' y Confirmado a verde"
    - "D-12: el botón Registrar HC abre HCCreatorDialog gateado por pasos.hc==='pendiente' y notifica su guardado para re-colorear"
    - "El sheet pasa pasos, flujo y onCirugiaClick al EtapaStepper"
    - "Click en el paso HC naranja abre HCCreatorDialog; al guardar la HC el stepper se re-colorea (crm-kanban invalidado)"
    - "Click en el paso presupuesto naranja abre un modal de presupuesto prellenado con ítems del catálogo (cirugías/tratamientos) matcheados al procedimiento del paciente"
    - "Click en 'Agendar cirugía' abre el modal de cirugía con el paciente pre-seleccionado y crea el turno vía POST /turnos/cirugia (queda registrado como Cirugia)"
    - "Al programar la cirugía, se invalida ['crm-kanban'] y el paso Confirmado pasa a verde"
    - "Al enviar/crear el presupuesto se invalida ['crm-kanban'] y el paso presupuesto pasa a verde"
  artifacts:
    - path: "frontend/src/components/crm/CardActionsSheet.tsx"
      provides: "Orquestación de los 3 modales de acción + invalidación de crm-kanban + prefill de presupuesto"
      contains: "crm-kanban"
    - path: "frontend/src/app/dashboard/turnos/SurgeryAppointmentModal.tsx"
      provides: "Pre-selección de paciente + invalidación de crm-kanban al programar"
      contains: "crm-kanban"
  key_links:
    - from: "CardActionsSheet.tsx"
      to: "EtapaStepper"
      via: "props pasos/flujo/onCirugiaClick"
      pattern: "pasos=\\{patient.pasos\\}"
    - from: "CardActionsSheet.tsx onCirugiaClick"
      to: "POST /turnos/cirugia"
      via: "SurgeryAppointmentModal con paciente pre-seleccionado"
      pattern: "SurgeryAppointmentModal"
    - from: "acciones exitosas"
      to: "['crm-kanban']"
      via: "queryClient.invalidateQueries"
      pattern: "invalidateQueries.*crm-kanban"
---

<objective>
Cablear las 3 quick-actions del stepper desde `CardActionsSheet.tsx` reutilizando modales
existentes, resolviendo las dos brechas load-bearing: (1) el turno de cirugía debe quedar
registrado como `Cirugia` — se logra reusando `SurgeryAppointmentModal` que ya usa
`POST /turnos/cirugia` (no `NuevoTurnoModal`, cuyo select de tipos excluye `esCirugia` y NUNCA
podría crear una `Cirugia`); y (2) cada acción que cambia un `pasos.*` debe invalidar
`['crm-kanban']` para que el stepper se re-coloree.

Purpose: STEPPER-03 (HC wizard), STEPPER-04 (presupuesto prellenado desde catálogo),
STEPPER-05/06 (agenda de cirugía que crea `Cirugia` y pinta Confirmado verde).
Output: `CardActionsSheet.tsx` orquestando HC, presupuesto y cirugía con invalidación correcta;
`SurgeryAppointmentModal.tsx` con paciente pre-seleccionable + invalidación crm-kanban;
`HCCreatorDialog.tsx` con callback opcional `onSaved`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-stepper-accionable/59-CONTEXT.md
@.planning/phases/59-stepper-accionable/59-PATTERNS.md

<interfaces>
<!-- Contratos que el executor consume directamente. -->

EtapaStepper (tras Plan 59-01) acepta: pasos?: PasosCrm; flujo?: KanbanPatient['flujo']; onCirugiaClick?: () => void.

SurgeryAppointmentModal (frontend/src/app/dashboard/turnos/SurgeryAppointmentModal.tsx):
  props actuales: { open: boolean; onOpenChange: (open: boolean) => void; defaultDate?: Date }
  POST /turnos/cirugia con payload { pacienteId, profesionalId, fecha, horaInicio, procedimiento, ... }
  → backend crea Cirugia (turnos.service.ts crearTurnoCirugia) → pasos.cirugia = 'completo'.
  onSuccess actual: qc.invalidateQueries({ queryKey: ['turnos'] }).

GenerarPresupuestoModal (frontend/src/components/live-turno/tabs/hc/GenerarPresupuestoModal.tsx):
  props: { open; onClose; pacienteId; profesionalId; pacienteEmail?; initialItems: PresupuestoItemInput[]; onCreated: (p) => void }
  Ya acepta initialItems (prefill) y tiene picker "Agregar del catálogo" (cirugías + tratamientos).

PresupuestoItemInput (useCreatePresupuesto.ts): { descripcion: string; precioTotal: number }.
useCirugiasCatalogo(profesionalId?) → CirugiaCatalogo[] (nombre, precioARS, precioUSD).
useTratamientosProfesional(false, profesionalId?) → TratamientoConInsumos[] (nombre, precio).

pasos (crm-steps.helper.ts): presupuesto='completo' solo con estado ENVIADO/ACEPTADO;
hc='completo' con entrada FINALIZED tipoEntrada CONSULTA_CIRUGIA; cirugia='completo' con Cirugia.

Query key a invalidar: ['crm-kanban', profesionalId] (useCRMKanban.ts:95) — invalidar por prefijo ['crm-kanban'].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-selección de paciente en SurgeryAppointmentModal + callback onSaved en HCCreatorDialog + invalidación crm-kanban</name>
  <files>frontend/src/app/dashboard/turnos/SurgeryAppointmentModal.tsx, frontend/src/components/patient/PatientDrawer/views/HCCreatorDialog.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/turnos/SurgeryAppointmentModal.tsx (props Props líneas 60-64, AutocompletePaciente 200-216, createMutation onSuccess 149-159)
    - frontend/src/components/patient/PatientDrawer/views/HCCreatorDialog.tsx (props 6-12, HCCreatorForm onSaved 27-33)
    - backend/src/modules/turnos/turnos.controller.ts (POST /turnos/cirugia, @Auth ADMIN/PROFESIONAL/SECRETARIA)
    - frontend/src/hooks/useCRMKanban.ts (query key ['crm-kanban', profesionalId])
  </read_first>
  <action>
    En `SurgeryAppointmentModal.tsx`: agregar dos props opcionales `pacienteId?: string` y
    `pacienteNombre?: string`. Cuando `pacienteId` está presente, sembrar el formulario con ese
    paciente (setear `pacienteId`/`pacienteNombre` en los defaultValues o vía `useEffect` cuando
    `open` cambia a true, análogo al `useEffect` de `defaultDate` en líneas 110-114) y ocultar (o
    dejar en modo lectura) el `AutocompletePaciente` para no permitir cambiar el paciente en este
    flujo (D-09: paciente pre-seleccionado desde el CRM). En `createMutation.onSuccess`, AÑADIR
    `qc.invalidateQueries({ queryKey: ['crm-kanban'] })` junto al `invalidateQueries(['turnos'])`
    existente (Gap #2: sin esto el stepper no se re-colorea). No cambiar el endpoint ni el payload
    (ya postea a `/turnos/cirugia` → crea `Cirugia` → resuelve Gap #1 sin tocar el backend).
    En `HCCreatorDialog.tsx`: agregar prop opcional `onSaved?: () => void` a `HCCreatorDialogProps`
    y en el `onSaved` que hoy hace `() => onOpenChange(false)`, llamar también a `props.onSaved?.()`
    (para que el sheet pueda invalidar crm-kanban tras guardar la HC — Gap #2). Comportamiento por
    defecto sin la prop: idéntico al actual (backward-compatible).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | rg "SurgeryAppointmentModal|HCCreatorDialog" || echo "OK: tipos limpios"</automated>
  </verify>
  <acceptance_criteria>
    - `SurgeryAppointmentModal.tsx` declara props `pacienteId?` y `pacienteNombre?` y, cuando `pacienteId` está seteado, el paciente queda fijado (AutocompletePaciente oculto o bloqueado)
    - `SurgeryAppointmentModal` `onSuccess` invalida `['crm-kanban']` (grep: `invalidateQueries` con `crm-kanban`) además de `['turnos']`
    - `HCCreatorDialog.tsx` acepta `onSaved?: () => void` y lo invoca al guardar, manteniendo `onOpenChange(false)`
    - `POST /turnos/cirugia` y el payload permanecen sin cambios
    - `npx tsc --noEmit` sin errores nuevos en ambos archivos
  </acceptance_criteria>
  <done>Cirugía crea Cirugia con paciente fijo e invalida crm-kanban; HCCreatorDialog notifica su guardado.</done>
</task>

<task type="auto">
  <name>Task 2: Cablear onCirugiaClick + pasar pasos/flujo al stepper + invalidar crm-kanban tras HC</name>
  <files>frontend/src/components/crm/CardActionsSheet.tsx</files>
  <read_first>
    - frontend/src/components/crm/CardActionsSheet.tsx (estado hcOpen línea 52, montaje de dialogs 172-236, call site EtapaStepper 120-126, HCCreatorDialog 188-193)
    - frontend/src/app/dashboard/turnos/SurgeryAppointmentModal.tsx (tras Task 1: props pacienteId/pacienteNombre)
    - .planning/phases/59-stepper-accionable/59-PATTERNS.md (patrón de montaje de modal dentro del Sheet; handler gating)
  </read_first>
  <action>
    Importar `useQueryClient` de `@tanstack/react-query` y crear `const qc = useQueryClient()`.
    Importar `SurgeryAppointmentModal` desde `@/app/dashboard/turnos/SurgeryAppointmentModal`.
    Agregar estado `const [turnoOpen, setTurnoOpen] = useState(false)` (sibling de `hcOpen`).
    Extender el call site de `<EtapaStepper>` (líneas 120-126) para pasar `pasos={patient.pasos}`,
    `flujo={patient.flujo}` y `onCirugiaClick={profesionalId ? () => setTurnoOpen(true) : undefined}`
    (idiom de gating por `profesionalId` como en `onHCClick`). Montar `<SurgeryAppointmentModal>`
    como sibling dentro de `<SheetContent>` (junto a los otros dialogs, ~línea 193) con
    `open={turnoOpen}`, `onOpenChange={setTurnoOpen}`, `pacienteId={patient.id}` y
    `pacienteNombre={patient.nombreCompleto}` (Radix DialogPortal escapa a document.body, sin
    conflicto de z-index — ver comentario línea 172). Para la re-coloreo tras HC (Gap #2): pasar al
    `<HCCreatorDialog>` la nueva prop `onSaved={() => qc.invalidateQueries({ queryKey: ['crm-kanban'] })}`.
    (La invalidación tras la cirugía ya la hace SurgeryAppointmentModal internamente por Task 1.)
    No eliminar la navegación manual ni las acciones de contacto/lista de espera/archivar
    existentes (D-06).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | rg "CardActionsSheet" ; npm run lint 2>&1 | rg "CardActionsSheet" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - El call site de `<EtapaStepper>` incluye `pasos={patient.pasos}`, `flujo={patient.flujo}` y `onCirugiaClick=` gateado por `profesionalId`
    - `<SurgeryAppointmentModal>` está montado dentro de `<SheetContent>` con `pacienteId={patient.id}` y `pacienteNombre={patient.nombreCompleto}`
    - `<HCCreatorDialog>` recibe `onSaved` que invalida `['crm-kanban']`
    - Las acciones existentes (contacto, lista de espera, archivar, navegación de etapa) permanecen
    - `npx tsc --noEmit` y `npm run lint` sin errores nuevos en CardActionsSheet.tsx
  </acceptance_criteria>
  <done>onCirugiaClick abre el modal de cirugía con paciente fijo; stepper recibe pasos/flujo; HC re-colorea al guardar.</done>
</task>

<task type="auto">
  <name>Task 3: Presupuesto prellenado desde el catálogo (STEPPER-04) montado en el sheet + invalidación</name>
  <files>frontend/src/components/crm/CardActionsSheet.tsx</files>
  <read_first>
    - frontend/src/components/crm/CardActionsSheet.tsx (handlePresupuestoClick actual líneas 94-97 — navega fuera; reemplazar por modal inline)
    - frontend/src/components/live-turno/tabs/hc/GenerarPresupuestoModal.tsx (props: open/onClose/pacienteId/profesionalId/initialItems/onCreated; picker de catálogo)
    - frontend/src/hooks/useCirugiasCatalogo.ts (useCirugiasCatalogo → nombre/precioARS/precioUSD)
    - frontend/src/hooks/useTratamientosProfesional.ts (useTratamientosProfesional → nombre/precio)
    - frontend/src/hooks/useCreatePresupuesto.ts (PresupuestoItemInput { descripcion, precioTotal })
    - .planning/phases/59-stepper-accionable/59-CONTEXT.md (D-07 fuente del prellenado; D-08 reuso de modal existente)
  </read_first>
  <action>
    Reemplazar la navegación de `handlePresupuestoClick` (que hoy cierra el sheet y enruta al drawer)
    por la apertura de un modal inline (D-08/D-09: no navegar fuera del CRM). Agregar estado
    `const [presupuestoOpen, setPresupuestoOpen] = useState(false)`. Cargar los catálogos del
    profesional con `useCirugiasCatalogo(profesionalId ?? undefined)` y
    `useTratamientosProfesional(false, profesionalId ?? undefined)`. Construir `initialItems:
    PresupuestoItemInput[]` (D-07: catálogo estructurado, "no solo el string procedimiento")
    matcheando `patient.procedimiento` (comparación case-insensitive por `includes`) contra el
    `nombre` de cada cirugía y tratamiento del catálogo; por cada match, push
    `{ descripcion: nombre, precioTotal: precioARS ?? precio ?? 0 }`. Si no hay ningún match pero
    `patient.procedimiento` existe, sembrar un único ítem
    `{ descripcion: patient.procedimiento, precioTotal: 0 }` (ítem libre editable) para no perder la
    intención; si `procedimiento` es null, `initialItems` puede ser `[{ descripcion: '', precioTotal: 0 }]`.
    Montar `<GenerarPresupuestoModal>` dentro de `<SheetContent>` con `open={presupuestoOpen}`,
    `onClose={() => { setPresupuestoOpen(false); qc.invalidateQueries({ queryKey: ['crm-kanban'] }); }}`,
    `pacienteId={patient.id}`, `profesionalId={profesionalId ?? ''}`, `initialItems={...}` y
    `onCreated={() => qc.invalidateQueries({ queryKey: ['crm-kanban'] })}`. Nota (Gap #2 + condición
    real de paso): `pasos.presupuesto` sólo pasa a 'completo' cuando el presupuesto queda ENVIADO/
    ACEPTADO — `GenerarPresupuestoModal` continúa al flujo de envío (EnviarPresupuestoModal) tras
    crear; invalidar crm-kanban tanto en `onCreated` como al cerrar (`onClose`) cubre el caso enviado.
    Cambiar `handlePresupuestoClick` (usado por `onPresupuestoClick` del stepper) para que sólo haga
    `setPresupuestoOpen(true)` en lugar de navegar. `onPresupuestoClick` sigue gateado en el stepper
    por `pasos.presupuesto === 'pendiente'` (Plan 59-01).
    Documentar en el SUMMARY: se usa `GenerarPresupuestoModal` (que ya acepta `initialItems` y tiene
    picker de catálogo) en vez de refactorizar el diálogo de estado interno de `PresupuestosView`,
    aplicando la cláusula de escape de D-08 (el diálogo de PresupuestosView no tiene prop de prefill).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | rg "CardActionsSheet" ; npm run lint 2>&1 | rg "CardActionsSheet" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `handlePresupuestoClick` ya NO llama a `onOpenDrawerWithView`; abre el modal inline (`setPresupuestoOpen(true)`)
    - `<GenerarPresupuestoModal>` está montado en el sheet con `initialItems` derivados del matcheo de `patient.procedimiento` contra `useCirugiasCatalogo` + `useTratamientosProfesional`
    - Un procedimiento que matchea un ítem de catálogo aparece como ítem con su `precioTotal` del catálogo (no 0); sin match, aparece el string procedimiento como ítem libre
    - `onCreated` y `onClose` del modal de presupuesto invalidan `['crm-kanban']`
    - `npx tsc --noEmit` y `npm run lint` sin errores nuevos en CardActionsSheet.tsx
  </acceptance_criteria>
  <done>El botón de presupuesto abre un modal inline prellenado con ítems estructurados del catálogo y re-colorea el stepper tras enviar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente (CRM sheet) → API `/turnos/cirugia` | Crea `Cirugia` + `Turno`. Endpoint ya existente y guardado. |
| cliente (CRM sheet) → API `/presupuestos` (+ envío) | Crea/ envía presupuesto. Endpoints ya existentes y guardados. |
| cliente → API `/pacientes/kanban` (re-fetch tras invalidación) | Lectura ya scopeada por profesional/tenant (Phase 57). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02 | Elevation of Privilege | POST /turnos/cirugia (turnos.controller) | accept | Endpoint YA guardado con `@Auth('ADMIN','PROFESIONAL','SECRETARIA')`; este plan lo reutiliza sin abrir nuevas rutas. NO se modifica `/tipos-turno` ni se expone `esCirugia` (Gap #1 se resuelve reusando el endpoint de cirugía existente), por lo que NO se agrega superficie de autorización nueva. |
| T-59-03 | Tampering | payload de `/turnos/cirugia` y `/presupuestos` desde el sheet | mitigate | El `pacienteId`/`profesionalId` provienen del contexto ya autenticado (`useEffectiveProfessionalId`, `patient.id` del kanban tenant-scoped); la validación de DTO (class-validator en `CreateCirugiaTurnoDto`) y las reglas de negocio del backend permanecen intactas. |
| T-59-04 | Information Disclosure | prefill del presupuesto (catálogos del profesional) | accept | `useCirugiasCatalogo`/`useTratamientosProfesional` ya filtran por profesional; no se expone catálogo de otros tenants. |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | Sin instalación de paquetes: solo se reutilizan componentes/hooks existentes (REQUIREMENTS "Out of Scope": nuevas dependencias prohibidas). |

**Nota de superficie:** fase de wiring frontend que reutiliza modales/endpoints ya autenticados,
con roles y tenant scoping ya aplicados por los endpoints reusados. No se introducen endpoints
nuevos ni cambios de autorización en el backend.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` sin errores nuevos.
- `cd frontend && npm run lint` sin errores nuevos en los 3 archivos.
- Source: el sheet pasa pasos/flujo/onCirugiaClick; monta SurgeryAppointmentModal (paciente fijo) y GenerarPresupuestoModal (prefill); invalida crm-kanban en HC, presupuesto y cirugía.
</verification>

<success_criteria>
- STEPPER-03: paso HC naranja → abre HCCreatorDialog; al guardar, stepper se re-colorea.
- STEPPER-04: paso presupuesto naranja → modal prellenado con ítems del catálogo.
- STEPPER-05: Confirmado sin cirugía → "Agendar cirugía" abre modal que crea `Cirugia`.
- STEPPER-06: tras agendar, `pasos.cirugia` = completo, Confirmado verde (crm-kanban invalidado).
</success_criteria>

<output>
Create `.planning/phases/59-stepper-accionable/59-02-SUMMARY.md` when done
</output>
