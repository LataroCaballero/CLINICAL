---
phase: 60-estad-sticas-sobre-registros-reales
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/reportes/services/crm-dashboard.service.ts
  - backend/src/modules/reportes/services/crm-dashboard.service.spec.ts
autonomous: true
requirements: [STATS-01, STATS-02]

must_haves:
  truths:
    - "El conteo de cirugías realizadas se calcula sobre filas de Cirugia (fecha pasada, estado no cancelado/suspendido), no sobre etapaCRM"
    - "El conteo de tratamientos realizados se calcula sobre entradas HC con tipoEntrada TRATAMIENTO, no sobre etapaCRM"
    - "Cambiar la etapaCRM de un paciente a PERDIDO no altera ninguno de los dos conteos"
    - "Ambos conteos siguen filtrados por profesionalId (aislamiento multi-tenant preservado)"
  artifacts:
    - path: "backend/src/modules/reportes/services/crm-dashboard.service.ts"
      provides: "getKpis extendido con cirugiasRealizadas y tratamientosRealizados basados en registros reales"
      contains: "prisma.cirugia.count"
    - path: "backend/src/modules/reportes/services/crm-dashboard.service.spec.ts"
      provides: "Test del invariante etapa-no-afecta-conteo y del scoping por profesionalId"
      contains: "describe('CrmDashboardService"
  key_links:
    - from: "crm-dashboard.service.ts getKpis"
      to: "prisma.cirugia"
      via: "count con fecha<now y estado notIn CANCELADA/SUSPENDIDA"
      pattern: "cirugia\\.count"
    - from: "crm-dashboard.service.ts getKpis"
      to: "prisma.historiaClinicaEntrada"
      via: "count con tipoEntrada TRATAMIENTO y historiaClinica.profesionalId"
      pattern: "historiaClinicaEntrada\\.count"
---

<objective>
Reemplazar el origen etapa-CRM de los conteos "realizados" por registros efectivos. Se extiende `getKpis` (crm-dashboard.service.ts) para devolver `cirugiasRealizadas` (desde `Cirugia`) y `tratamientosRealizados` (desde entradas HC de tratamiento), robustos a cambios de etapaCRM posteriores, preservando el filtrado por `profesionalId`.

Purpose: Satisface STATS-01 y STATS-02 en la capa de datos — el conteo deja de depender de la etapa actual del paciente.
Output: `getKpis` amplía su respuesta con dos conteos basados en registros + spec que fija el invariante "cambiar etapa no cambia el conteo".
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del código. Usar directamente, sin explorar. -->

Fuente de verdad de "cirugía realizada" — backend/src/modules/pacientes/cirugia-realizada-scheduler.service.ts:
  prisma.cirugia.findMany({ where: { fecha: { lt: ahora }, estado: { notIn: ['CANCELADA', 'SUSPENDIDA'] } } })
  // Replicar EXACTAMENTE esta definición para cirugiasRealizadas (no inventar una nueva).

Enums (backend/src/prisma/schema.prisma):
  EstadoCirugia = { PROGRAMADA, EN_CURSO, COMPLETADA, CANCELADA, SUSPENDIDA }
  TipoEntradaHC = { CONSULTA_CIRUGIA, TRATAMIENTO, CONTROL, SEGUIMIENTO, PREOPERATORIO }
  EstadoEntradaHC = { DRAFT, FINALIZED }

Modelo Cirugia: tiene profesionalId y pacienteId directos, fecha DateTime, estado EstadoCirugia.
Modelo HistoriaClinicaEntrada: NO tiene profesionalId directo. Scoping vía relación historiaClinica → HistoriaClinica.profesionalId (HistoriaClinica sí tiene profesionalId). Campos: tipoEntrada TipoEntradaHC?, status EstadoEntradaHC (default FINALIZED).

Firma actual (crm-dashboard.service.ts):
  async getKpis(profesionalId: string, periodo: string): Promise<{ periodo, nuevos, confirmados, totalActivos, tasaConversion }>
  // getKpis ya usa un Promise.all con 3 prisma.paciente.count. Extender ese Promise.all.
  // totalActivos ya es acumulativo (no acotado a periodo) — los nuevos conteos siguen ese mismo patrón acumulativo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir cirugiasRealizadas y tratamientosRealizados a getKpis (basados en registros)</name>
  <files>backend/src/modules/reportes/services/crm-dashboard.service.ts</files>
  <read_first>
    - backend/src/modules/reportes/services/crm-dashboard.service.ts (método getKpis, líneas ~119-159, y los imports de @prisma/client en la cabecera)
    - backend/src/modules/pacientes/cirugia-realizada-scheduler.service.ts (definición canónica de "cirugía realizada": fecha < ahora, estado notIn CANCELADA/SUSPENDIDA)
    - backend/src/prisma/schema.prisma (models Cirugia, HistoriaClinicaEntrada, HistoriaClinica; enums EstadoCirugia, TipoEntradaHC, EstadoEntradaHC)
  </read_first>
  <behavior>
    - cirugiasRealizadas cuenta filas Cirugia del profesional con fecha < ahora y estado no CANCELADA/SUSPENDIDA
    - tratamientosRealizados cuenta entradas HC del profesional con tipoEntrada TRATAMIENTO y status FINALIZED
    - Cambiar etapaCRM de un paciente (a PERDIDO u otra) NO cambia ninguno de los dos conteos (no leen etapaCRM)
    - Un profesionalId distinto no ve las cirugías/tratamientos de otro profesional (scoping preservado)
  </behavior>
  <action>
    En `getKpis`, extender el `Promise.all` existente agregando dos consultas de conteo por registro real, cumulativas (mismo patrón acumulativo que `totalActivos`, NO acotadas a `inicio`/`fin`):
    - `cirugiasRealizadas` = `prisma.cirugia.count` con `where: { profesionalId, fecha: { lt: ahora }, estado: { notIn: [EstadoCirugia.CANCELADA, EstadoCirugia.SUSPENDIDA] } }`. Definir `const ahora = new Date()` al inicio del método. Esta condición replica EXACTAMENTE la del scheduler `cirugia-realizada-scheduler.service.ts` — no inventar otra definición (p.ej. NO filtrar por `estado: COMPLETADA` solamente).
    - `tratamientosRealizados` = `prisma.historiaClinicaEntrada.count` con `where: { tipoEntrada: TipoEntradaHC.TRATAMIENTO, status: EstadoEntradaHC.FINALIZED, historiaClinica: { profesionalId } }`. El scoping multi-tenant va por la relación `historiaClinica.profesionalId` (HistoriaClinicaEntrada no tiene profesionalId directo).
    Importar `EstadoCirugia`, `TipoEntradaHC`, `EstadoEntradaHC` desde `@prisma/client` (junto a los imports existentes). Añadir ambos campos (`cirugiasRealizadas`, `tratamientosRealizados`) al objeto de retorno de `getKpis`. NO tocar `getFunnelSnapshot` (el funnel es una vista de pipeline por etapa y debe seguir agrupando por etapaCRM — coexiste con los nuevos conteos acumulativos). NINGUNA de las dos consultas nuevas debe leer, filtrar ni agrupar por `etapaCRM`.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^$' | grep -i "crm-dashboard" ; test $? -ne 0 && echo "TYPECHECK_OK_NO_ERRORS_IN_FILE"</automated>
  </verify>
  <acceptance_criteria>
    - `getKpis` en crm-dashboard.service.ts referencia `prisma.cirugia.count` y `prisma.historiaClinicaEntrada.count`
    - Ninguna de las dos consultas nuevas contiene la palabra `etapaCRM`
    - La condición de cirugía usa `fecha: { lt: ... }` y `estado: { notIn: [...CANCELADA...SUSPENDIDA...] }` (idéntica al scheduler)
    - La consulta de tratamientos usa `tipoEntrada: TipoEntradaHC.TRATAMIENTO` y scoping `historiaClinica: { profesionalId }`
    - El objeto retornado por getKpis incluye las claves `cirugiasRealizadas` y `tratamientosRealizados`
    - `cd backend && npx tsc --noEmit` no reporta errores nuevos en crm-dashboard.service.ts
  </acceptance_criteria>
  <done>getKpis devuelve cirugiasRealizadas y tratamientosRealizados calculados sobre Cirugia y HistoriaClinicaEntrada, con scoping por profesionalId y sin leer etapaCRM.</done>
</task>

<task type="auto">
  <name>Task 2: Spec del invariante "cambiar etapa no cambia el conteo" + scoping multi-tenant</name>
  <files>backend/src/modules/reportes/services/crm-dashboard.service.spec.ts</files>
  <read_first>
    - backend/src/modules/reportes/services/crm-dashboard.service.ts (getKpis ya modificado en Task 1)
    - backend/src/modules/reportes/services/reportes-dashboard.service.spec.ts (patrón de mock de PrismaService: providers con { provide: PrismaService, useValue: mockPrismaService } y jest.fn por modelo)
    - backend/src/modules/pacientes/cirugia-realizada-scheduler.service.spec.ts (patrón de aserción sobre argumentos de where con expect.objectContaining)
  </read_first>
  <action>
    Crear `crm-dashboard.service.spec.ts` siguiendo el patrón de `reportes-dashboard.service.spec.ts` (Test.createTestingModule con `CrmDashboardService` y un `mockPrismaService`). Mockear `paciente.count`, `paciente.groupBy`, `presupuesto.findMany`, `cirugia.count` y `historiaClinicaEntrada.count` como `jest.fn()`. Escribir tests sobre `getKpis`:
    - Test A (invariante etapa): `getKpis` NO invoca `cirugia.count` ni `historiaClinicaEntrada.count` con ningún `where` que contenga `etapaCRM`. Afirmar con `expect(mock.cirugia.count).toHaveBeenCalledWith(expect.objectContaining({ where: expect.not.objectContaining({ etapaCRM: expect.anything() }) }))` (y equivalente para historiaClinicaEntrada). Esto codifica que un cambio de etapa a PERDIDO no puede alterar los conteos porque las consultas no dependen de etapaCRM.
    - Test B (definición de cirugía realizada): `cirugia.count` se llama con `where` que incluye `fecha` con operador `lt` y `estado` con `notIn` conteniendo 'CANCELADA' y 'SUSPENDIDA'.
    - Test C (scoping multi-tenant): con `profesionalId = 'prof-X'`, `cirugia.count` recibe `where.profesionalId === 'prof-X'` y `historiaClinicaEntrada.count` recibe `where.historiaClinica.profesionalId === 'prof-X'`. Afirmar que ninguna de las dos consultas omite el scoping por profesional.
    - Test D (mapeo de salida): configurar `cirugia.count` → 7 y `historiaClinicaEntrada.count` → 4; afirmar que el resultado de getKpis tiene `cirugiasRealizadas === 7` y `tratamientosRealizados === 4`.
    Mockear los `paciente.count` (nuevos/confirmados/totalActivos) devolviendo 0 para que el resto de getKpis no rompa.
  </action>
  <verify>
    <automated>cd backend && npm run test -- crm-dashboard.service.spec 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Existe backend/src/modules/reportes/services/crm-dashboard.service.spec.ts
    - Contiene un test que afirma que las consultas de conteo NO usan etapaCRM (invariante STATS-01/STATS-02)
    - Contiene un test que afirma scoping por profesionalId en ambas consultas (cirugia.profesionalId y historiaClinica.profesionalId)
    - `cd backend && npm run test -- crm-dashboard.service.spec` termina con exit 0 (todos los tests verdes)
  </acceptance_criteria>
  <done>Spec verde que fija el invariante etapa-no-afecta-conteo y el aislamiento multi-tenant de ambos conteos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente→GET /reportes/crm/kpis | `profesionalId` llega como query param no confiable y define el scope de datos devuelto |
| service→Prisma | Cada `count` debe incluir el filtro de tenant (profesionalId) o filtra datos de otras clínicas/profesionales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Information Disclosure | getKpis cirugiasRealizadas | mitigate | `prisma.cirugia.count` DEBE incluir `where.profesionalId` — Test C lo verifica; sin ese filtro se cuentan cirugías de otros profesionales |
| T-60-02 | Information Disclosure | getKpis tratamientosRealizados | mitigate | scoping obligatorio vía `where.historiaClinica.profesionalId` (HistoriaClinicaEntrada no tiene profesionalId directo); Test C lo verifica |
| T-60-03 | Tampering | conteo derivado de etapaCRM | mitigate | consultas NO leen etapaCRM; un cambio de etapa (manual o del scheduler) no puede inflar/desinflar el conteo — Test A lo fija |
| T-60-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala paquetes nuevos (RESTRICCIÓN v1.13: sin nuevas dependencias); no aplica gate de legitimidad |
</threat_model>

<verification>
- `cd backend && npm run test -- crm-dashboard.service.spec` exit 0
- `cd backend && npx tsc --noEmit` sin errores nuevos en crm-dashboard.service.ts
- Revisión manual: ninguna de las dos consultas nuevas menciona `etapaCRM`
</verification>

<success_criteria>
- getKpis devuelve `cirugiasRealizadas` (desde Cirugia) y `tratamientosRealizados` (desde HC TRATAMIENTO) calculados sobre registros reales
- Ambos conteos scopeados por profesionalId (sin fuga cross-tenant)
- Spec verde que prueba el invariante "cambiar etapa a PERDIDO no cambia el conteo"
</success_criteria>

<output>
Create `.planning/phases/60-estad-sticas-sobre-registros-reales/60-01-SUMMARY.md` when done
</output>
