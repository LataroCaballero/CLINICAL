---
phase: 60-estad-sticas-sobre-registros-reales
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - frontend/src/hooks/useCRMKpis.ts
  - frontend/src/app/dashboard/components/CRMKpiCards.tsx
  - frontend/src/app/dashboard/page.tsx
autonomous: true
requirements: [STATS-01, STATS-02]

must_haves:
  truths:
    - "El dashboard muestra un conteo 'Cirugías realizadas' proveniente de crm/kpis (registros Cirugia)"
    - "El dashboard muestra un conteo 'Tratamientos realizados' proveniente de crm/kpis (entradas HC de tratamiento)"
    - "Los nuevos conteos consumen el mismo endpoint crm/kpis del plan 60-01 (sin nuevo endpoint)"
  artifacts:
    - path: "frontend/src/hooks/useCRMKpis.ts"
      provides: "CRMKpisData extendido con cirugiasRealizadas y tratamientosRealizados"
      contains: "cirugiasRealizadas"
    - path: "frontend/src/app/dashboard/components/CRMKpiCards.tsx"
      provides: "Tarjetas KPI 'Cirugías realizadas' y 'Tratamientos realizados'"
      contains: "Cirugías realizadas"
  key_links:
    - from: "CRMKpiCards.tsx"
      to: "useCRMKpis"
      via: "data.cirugiasRealizadas / data.tratamientosRealizados"
      pattern: "cirugiasRealizadas"
---

<objective>
Exponer en el dashboard los dos conteos basados en registros que el plan 60-01 añadió a `crm/kpis`: "Cirugías realizadas" y "Tratamientos realizados". Se extiende el tipo del hook `useCRMKpis` y se agregan dos `KpiCard` en `CRMKpiCards`, ajustando el grid del dashboard para que las 6 tarjetas se acomoden.

Purpose: Cierra STATS-01/STATS-02 en la UI — el usuario ve conteos "realizados" estables ante cambios de etapa CRM.
Output: Dashboard con dos KPI adicionales alimentados desde registros reales.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/60-estad-sticas-sobre-registros-reales/60-01-PLAN.md

<interfaces>
<!-- Contratos ya existentes. Usar directamente. -->

Hook actual — frontend/src/hooks/useCRMKpis.ts:
  export interface CRMKpisData { periodo, nuevos, confirmados, totalActivos, tasaConversion }
  useCRMKpis(profesionalId, periodo) -> GET /reportes/crm/kpis
  // El plan 60-01 amplía la respuesta del endpoint con cirugiasRealizadas y tratamientosRealizados (números).

Componente actual — frontend/src/app/dashboard/components/CRMKpiCards.tsx:
  Renderiza 4 <KpiCard title value subtitle isLoading /> dentro de un fragment.
  KpiCard props: { title: string; value: string; subtitle?: string; trend?: 'up'|'neutral'|'down'; isLoading?: boolean }

Dashboard — frontend/src/app/dashboard/page.tsx línea ~90:
  <div className="grid grid-cols-2 md:grid-cols-4 gap-4"> <CRMKpiCards ... /> </div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender CRMKpisData y renderizar los dos KPI en CRMKpiCards</name>
  <files>frontend/src/hooks/useCRMKpis.ts, frontend/src/app/dashboard/components/CRMKpiCards.tsx</files>
  <read_first>
    - frontend/src/hooks/useCRMKpis.ts (interface CRMKpisData completa)
    - frontend/src/app/dashboard/components/CRMKpiCards.tsx (estructura de las 4 KpiCard existentes)
    - frontend/src/app/dashboard/components/KpiCard.tsx (props exactas de KpiCard)
  </read_first>
  <action>
    En `useCRMKpis.ts`: añadir `cirugiasRealizadas: number` y `tratamientosRealizados: number` a la interface `CRMKpisData` (opcionales `?` NO — el backend 60-01 siempre los devuelve). No cambiar la queryKey ni el endpoint.
    En `CRMKpiCards.tsx`: agregar dos `<KpiCard>` nuevas después de las existentes:
    - title "Cirugías realizadas", value `data ? String(data.cirugiasRealizadas) : "—"`, subtitle "registro efectivo".
    - title "Tratamientos realizados", value `data ? String(data.tratamientosRealizados) : "—"`, subtitle "registro efectivo".
    Pasar `isLoading={loading}` como las demás. No introducir nuevas dependencias ni componentes (RESTRICCIÓN v1.13). El subtitle debe dejar claro que el origen es el registro real (no la etapa CRM), pero sin lenguaje de "v1"/"placeholder".
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "useCRMKpis|CRMKpiCards" ; test $? -ne 0 && echo "NO_TS_ERRORS_IN_TOUCHED_FILES"</automated>
  </verify>
  <acceptance_criteria>
    - `CRMKpisData` incluye `cirugiasRealizadas: number` y `tratamientosRealizados: number`
    - `CRMKpiCards.tsx` renderiza una KpiCard con title "Cirugías realizadas" ligada a `data.cirugiasRealizadas`
    - `CRMKpiCards.tsx` renderiza una KpiCard con title "Tratamientos realizados" ligada a `data.tratamientosRealizados`
    - `cd frontend && npx tsc --noEmit` no reporta errores en useCRMKpis.ts ni CRMKpiCards.tsx
  </acceptance_criteria>
  <done>El hook tipa los dos conteos y CRMKpiCards muestra las dos tarjetas nuevas alimentadas por crm/kpis.</done>
</task>

<task type="auto">
  <name>Task 2: Ajustar el grid del dashboard para 6 tarjetas KPI</name>
  <files>frontend/src/app/dashboard/page.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/page.tsx (línea ~90, el div grid que envuelve <CRMKpiCards />)
  </read_first>
  <action>
    Cambiar la clase del `div` contenedor de `CRMKpiCards` de `grid-cols-2 md:grid-cols-4` a un layout que acomode 6 tarjetas de forma pareja (p.ej. `grid-cols-2 md:grid-cols-3 lg:grid-cols-6`), manteniendo `gap-4`. No modificar la lógica ni otros widgets del dashboard; sólo la clase del grid de KPIs.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "dashboard/page" ; test $? -ne 0 && echo "NO_TS_ERRORS_IN_PAGE"</automated>
  </verify>
  <acceptance_criteria>
    - El grid contenedor de CRMKpiCards ya no usa `md:grid-cols-4` fijo para 4 tarjetas; acomoda 6 tarjetas sin desbordar en fila incompleta
    - `cd frontend && npx tsc --noEmit` sin errores en dashboard/page.tsx
    - `cd frontend && npm run build` compila (o al menos `npx tsc --noEmit` global sin errores nuevos)
  </acceptance_criteria>
  <done>Las 6 tarjetas KPI (4 existentes + 2 nuevas) se muestran en un grid ordenado en el dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard→GET /reportes/crm/kpis | El frontend consume conteos ya scopeados por profesionalId en el backend (60-01); no introduce scoping propio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Information Disclosure | useCRMKpis / CRMKpiCards | accept | El aislamiento multi-tenant se garantiza en backend (T-60-01/T-60-02); el frontend sólo muestra valores ya filtrados por profesionalId (profId de useEffectiveProfessionalId). No hay lógica de scope en cliente que pueda romperlo. |
| T-60-SC | Tampering | npm installs | accept | Sin nuevas dependencias (RESTRICCIÓN v1.13); no aplica gate de legitimidad de paquetes |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` sin errores en los 3 archivos tocados
- Revisión visual del dashboard: aparecen "Cirugías realizadas" y "Tratamientos realizados" con valores numéricos
</verification>

<success_criteria>
- El dashboard muestra los dos conteos "realizados" provenientes de registros reales (crm/kpis)
- Sin nuevas dependencias; layout de KPIs ordenado con 6 tarjetas
</success_criteria>

<output>
Create `.planning/phases/60-estad-sticas-sobre-registros-reales/60-02-SUMMARY.md` when done
</output>
