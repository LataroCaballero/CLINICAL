---
phase: 61-backend-schema-decoupling-e-indicaciones
plan: 04
type: execute
wave: 3
depends_on: ["61-03"]
gap_closure: true
files_modified:
  - backend/src/modules/pacientes/pacientes.service.ts
  - backend/src/modules/pacientes/pacientes.service.spec.ts
autonomous: true
requirements: [INDIC-04]
must_haves:
  truths:
    - "getKanban entrega TODAS las filas ConsentimientoFirmado del paciente (no solo la mas reciente) a computePasosCrm, para que el Fallback 1 (.some sobre indicacionesLeidasAt) vea la fila legacy v1.12 aunque no sea la ultima firmada"
    - "Un paciente con indicacionesLeidasAt legacy en una fila NO-ultima que luego firma otra zona (fila nueva NULL) mantiene indicacionesPreop = completo (no regresa a pendiente)"
    - "Existe un test que ejercita la forma real del select de getKanban contra computePasosCrm y falla si se reintroduce la truncacion (take:1) en consentimientosFirmados"
    - "El Paso 4 (consentimiento, .some sobre firmadoAt) sigue completo con el select sin take:1 — sin regresion en el paso consentimiento"
  artifacts:
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "getKanban consentimientosFirmados select sin take:1"
      contains: "consentimientosFirmados"
    - path: "backend/src/modules/pacientes/pacientes.service.spec.ts"
      provides: "test de frontera getKanban -> computePasosCrm que cubre multi-zona con legacy receipt en fila no-ultima"
      contains: "indicacionesPreop"
  key_links:
    - from: "pacientes.service.ts getKanban consentimientosFirmados select"
      to: "computePasosCrm Fallback 1 (.some sobre c.indicacionesLeidasAt)"
      via: "array completo de filas firmadas (sin take:1)"
      pattern: "consentimientosFirmados"
    - from: "pacientes.service.spec.ts getKanban boundary test"
      to: "service.getKanban(profesionalId) -> pasos.indicacionesPreop"
      via: "prisma.paciente.findMany mock con multiples consentimientosFirmados"
      pattern: "indicacionesPreop"
---

<objective>
Cierra el gap SC#3/INDIC-04 (WR-01 de 61-REVIEW.md): el select de consentimientosFirmados en getKanban usa take: 1 con orderBy firmadoAt desc, por lo que solo la fila firmada mas reciente llega a computePasosCrm. El Fallback 1 del Paso 5 recorre un array con .some sobre c.indicacionesLeidasAt, pero solo ve esa unica fila. Un paciente con un indicacionesLeidasAt legacy v1.12 en una fila NO-ultima que luego firma cualquier zona nueva (filas nuevas siempre NULL por D-01/D-02) pierde el receipt legacy: indicacionesPreop regresa de completo a pendiente. Este plan remueve la truncacion y agrega un test de frontera que atrapa futuras regresiones a este limite.

Purpose: Restaurar la garantia de no-regresion de pacientes v1.12 (D-04, sin backfill) que 61-03-PLAN.md se compromete a entregar pero que la truncacion del select rompe en el sistema cableado. El helper puro ya es correcto; el defecto vive en el select de Prisma, invisible a los unit tests del helper.
Output: getKanban select de consentimientosFirmados sin take:1; un test de frontera en pacientes.service.spec.ts que ejercita la forma real del select contra computePasosCrm.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-CONTEXT.md
@.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-VERIFICATION.md
@.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-REVIEW.md

<interfaces>
Estado actual del select y del helper. El executor NO debe explorar; usar esto directo.

getKanban consentimientosFirmados select actual (pacientes.service.ts 674-678) — CON truncacion a corregir. Contiene: select de firmadoAt/indicacionesLeidasAt, luego take: 1, luego orderBy firmadoAt desc. La linea take: 1 (y su orderBy) es el defecto.

computePasosCrm consume ambos campos del array (crm-steps.helper.ts):
- Paso 4 (97-99): consentimientosFirmados.some sobre c.firmadoAt distinto de null, OR consentimientoFirmadoLegacy.
- Paso 5 Fallback 1 (105-108): OR consentimientosFirmados.some sobre c.indicacionesLeidasAt distinto de null, OR indicacionesEnviadasLegacy.
Ambos usan .some sobre el array; ninguno depende del orden ni del limite de 1.

getKanban es el UNICO consumidor de computePasosCrm (confirmado en 61-CONTEXT integration points).

Patron del spec existente (pacientes.service.spec.ts) — reutilizar tal cual:
- TestingModule con providers: PacientesService, PrismaService (mockPrisma), ConfigService (mockConfigService), EncryptionService (mockEnc).
- mockPrisma.paciente hoy tiene findUnique + update como jest.fn(); hay que AGREGAR findMany: jest.fn().
- Constructor de PacientesService (46-50): prisma, configService, encryption — los 3 ya estan provistos por el spec.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover take:1 de consentimientosFirmados en getKanban (WR-01, SC#3, INDIC-04)</name>
  <files>backend/src/modules/pacientes/pacientes.service.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.ts (getKanban select 620-681; en especial el bloque consentimientosFirmados 674-678; la llamada computePasosCrm 742-750)
    - backend/src/modules/pacientes/crm-steps.helper.ts (Paso 4 lineas 97-99 usa firmadoAt via .some; Paso 5 Fallback 1 lineas 105-108 usa indicacionesLeidasAt via .some — confirmar que ninguno depende de take:1 ni del orden)
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-REVIEW.md (WR-01, fix propuesto lineas 45-58)
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-VERIFICATION.md (gap missing item 1)
  </read_first>
  <action>
    En el bloque consentimientosFirmados del select de getKanban (pacientes.service.ts 674-678): eliminar las claves take: 1 y orderBy firmadoAt desc, dejando solo el select de firmadoAt y indicacionesLeidasAt. Este es exactamente el fix primario de 61-REVIEW.md WR-01: el helper computePasosCrm recorre el array con .some en el Paso 4 (firmadoAt) y el Paso 5 Fallback 1 (indicacionesLeidasAt), por lo que necesita ver TODAS las filas firmadas del paciente, no solo la mas reciente. Sin take:1 el array queda acotado naturalmente por la cantidad de zonas firmadas del paciente (una fila por zona), sin over-fetch relevante.
    Reemplazar el comentario del bloque (si lo hay) por uno que documente el motivo: que la truncacion rompia el Fallback 1 legacy v1.12 (regresion de indicacionesPreop) y referencia WR-01/SC#3. Seleccionar unicamente los dos campos que el helper lee (firmadoAt, indicacionesLeidasAt) — no ampliar el select a otros campos de ConsentimientoFirmado (mitigacion T-61-10 de over-fetch de datos forenses append-only).
    NO tocar ninguna otra parte de getKanban: el select top-level indicacionesLeidasAt true, el call arg indicacionesLeidasAt de p.indicacionesLeidasAt, y los demas relations quedan como estan. NO modificar el Paso 4 ni el helper.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && rg -n -A4 "consentimientosFirmados: \{" src/modules/pacientes/pacientes.service.ts | rg -q "take: 1" && echo TAKE_STILL_PRESENT_FAIL || echo NO_TAKE_PASS; rg -q "firmadoAt: true, indicacionesLeidasAt: true" src/modules/pacientes/pacientes.service.ts && echo FIELDS_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source: el bloque consentimientosFirmados del select de getKanban NO contiene take: 1 (rg -A4 del bloque no matchea take: 1; el verify imprime NO_TAKE_PASS)
    - Source: el bloque consentimientosFirmados conserva el select de firmadoAt true e indicacionesLeidasAt true (no se amplia a otros campos; el verify imprime FIELDS_PASS)
    - Source: no queda orderBy firmadoAt desc dentro del bloque consentimientosFirmados (la clave desaparece junto con take:1)
    - Source: el select top-level indicacionesLeidasAt true (v1.14) y el call arg indicacionesLeidasAt de p.indicacionesLeidasAt siguen presentes sin cambios
    - Tipos: el tipo del array cambia de 1 elemento a N; compatible con PacientePasosInput.consentimientosFirmados que ya es Array (sin errores de tipo nuevos en el archivo)
  </acceptance_criteria>
  <done>El select entrega el array completo de filas firmadas al helper; Paso 4 y Paso 5 Fallback 1 ven todas las filas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test de frontera getKanban -> computePasosCrm (regresion multi-zona legacy receipt)</name>
  <files>backend/src/modules/pacientes/pacientes.service.spec.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.spec.ts (patron completo del spec: mockEncryptionService 20-29; beforeEach con TestingModule 47-74; mockPrisma.paciente 48-52 — hoy solo findUnique/update)
    - backend/src/modules/pacientes/pacientes.service.ts (getKanban 620-754: forma del where, del select, del retorno agrupado por etapaCRM y del map que aplica computePasosCrm)
    - backend/src/modules/pacientes/crm-steps.helper.ts (Paso 5 Fallback 1 105-108; PasosCrm shape 20-26 — indicacionesPreop es 'completo' o 'pendiente')
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-VERIFICATION.md (gap missing item 2)
  </read_first>
  <behavior>
    - Test A (regresion — el que atrapa el bug): un paciente con indicacionesLeidasAt null a nivel Paciente, indicacionesEnviadas false, y DOS filas consentimientosFirmados — la MAS reciente (firmadoAt mayor) con indicacionesLeidasAt null, y una fila mas antigua con indicacionesLeidasAt fecha legacy — produce indicacionesPreop igual a 'completo'. Con take:1 este test falla porque solo llegaria la fila NULL mas reciente.
    - Test B (no-regresion Paso 4): el mismo paciente multi-zona tiene consentimiento igual a 'completo' (ambas filas tienen firmadoAt; .some sobre firmadoAt sigue verdadero sin take:1).
    - Test C (pendiente real): un paciente con indicacionesLeidasAt null, indicacionesEnviadas false, y filas firmadas todas con indicacionesLeidasAt null produce indicacionesPreop igual a 'pendiente' (el fix no marca completo espuriamente).
    - El test lee indicacionesPreop del payload retornado por getKanban (retorno = array de columnas; aplanar con result.flatMap sobre c.pacientes y buscar el paciente por id).
  </behavior>
  <action>
    Extender pacientes.service.spec.ts con un nuevo describe: getKanban -> computePasosCrm boundary (WR-01/SC#3). Reutilizar el setup existente: agregar findMany como jest.fn() al objeto mockPrisma.paciente (junto a findUnique/update) en el beforeEach del bloque, provisto por un TestingModule local con los mismos 3 providers (PacientesService, PrismaService=mockPrisma, ConfigService=mockConfigService, EncryptionService=mockEnc). ConfigService/EncryptionService no se usan en getKanban pero deben estar provistos para que el modulo compile.
    Construir el objeto que prisma.paciente.findMany resuelve como un array de 1 paciente con la forma EXACTA que getKanban selecciona, incluyendo todas las claves que el map de retorno lee para no romper: id, nombreCompleto, fotoUrl, etapaCRM, temperatura, scoreConversion, tratamiento, lugarIntervencion, updatedAt, enListaEspera, comentarioListaEspera, flujo, presupuestos vacio, turnos vacio, contactos vacio, autorizaciones vacio, consentimientoFirmado false, indicacionesEnviadas segun caso, indicacionesLeidasAt segun caso, cirugias vacio, historiasClinicas vacio, consentimientosFirmados con filas segun caso. Usar etapaCRM de un valor valido del map de columnas, p.ej. CONFIRMADO.
    Test A data: indicacionesLeidasAt null (Paciente), indicacionesEnviadas false, consentimientosFirmados con dos filas: la antigua con firmadoAt new Date 2025-01-01 e indicacionesLeidasAt new Date 2025-01-01, y la reciente con firmadoAt new Date 2026-06-01 e indicacionesLeidasAt null.
    Llamar await service.getKanban de un profesionalId, aplanar result.flatMap sobre c.pacientes, encontrar el paciente por id y assertar: indicacionesPreop igual a 'completo' (Test A) y consentimiento igual a 'completo' (Test B) sobre el mismo mock; en un caso separado con ambas filas indicacionesLeidasAt null e indicacionesEnviadas false, assertar indicacionesPreop igual a 'pendiente' (Test C).
    Nota de entorno: el jest full-suite cuelga en este entorno; correr SOLO este archivo con invocacion targeted (ver acceptance_criteria) y, si cuelga, usar el fallback documentado (transpile + ejecucion manual del escenario) tal como se hizo en 61-01/61-02/61-03, dejando registro en el SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && rg -q "getKanban" src/modules/pacientes/pacientes.service.spec.ts && rg -q "indicacionesPreop" src/modules/pacientes/pacientes.service.spec.ts && rg -q "findMany" src/modules/pacientes/pacientes.service.spec.ts && npx jest --runTestsByPath src/modules/pacientes/pacientes.service.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: el spec contiene un describe getKanban -> computePasosCrm boundary con al menos los 3 casos (A regresion, B paso 4, C pendiente)
    - Source: mockPrisma.paciente incluye findMany como jest.fn() y el test lo mockea con resolvedValue de la forma del select de getKanban
    - Behavior (Test A): paciente con indicacionesLeidasAt null, indicacionesEnviadas false, dos filas consentimientosFirmados (reciente NULL, antigua con receipt) produce indicacionesPreop igual a 'completo' — este assert FALLA si se reintroduce take:1
    - Behavior (Test B): el mismo mock produce consentimiento igual a 'completo' (paso 4 no regresiona)
    - Behavior (Test C): con ambas filas indicacionesLeidasAt null e indicacionesEnviadas false, indicacionesPreop igual a 'pendiente'
    - Test: npx jest --runTestsByPath del spec pasa (o, si el entorno cuelga, el fallback transpile+ejecucion-manual documentado en el SUMMARY confirma los 3 asserts, igual que 61-03)
  </acceptance_criteria>
  <done>Test de frontera cubre el bug WR-01 (falla con take:1, pasa sin el); paso 4 y caso pendiente cubiertos; el spec corre aislado del resto del archivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profesional (staff JWT) -> getKanban | getKanban ya filtra por profesionalId en el where; el cambio del select no cruza tenants ni agrega nuevas entradas de usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-10 | Information Disclosure | getKanban consentimientosFirmados select (ahora sin take:1, N filas por paciente) | mitigate | El select sigue limitado a exactamente 2 campos (firmadoAt, indicacionesLeidasAt); NO se seleccionan hash SHA-256, ip, userAgent ni el PDF forense. El cardinal del array queda acotado por la cantidad de zonas firmadas del paciente (unas pocas), no por datos externos. Sin over-fetch de campos forenses append-only. |
| T-61-11 | Tampering (logica derivada) | computePasosCrm Paso 5 Fallback 1 alimentado por el array completo | mitigate | El fix restaura el contrato que el helper espera (array completo), preservando ambos fallbacks legacy => no-regresion v1.12/pre-v1.12 (D-04). El test de la Task 2 falla si se reintroduce la truncacion, bloqueando futuras regresiones. Funcion pura, sin efectos secundarios. |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | No hay instalacion de paquetes en este gap (solo cambio de select + test). Sin superficie de supply-chain. |
</threat_model>

<verification>
- Task 1: el bloque consentimientosFirmados del select ya no tiene take:1 ni orderBy; conserva solo select de firmadoAt/indicacionesLeidasAt.
- Task 2: el spec corre aislado y el Test A (multi-zona legacy receipt en fila no-ultima) da indicacionesPreop 'completo'; Test C da 'pendiente'; Test B mantiene consentimiento 'completo'.
- Regla anti-regresion: si alguien reintroduce take:1 en el select, el Test A del spec falla.
</verification>

<success_criteria>
- SC#3 (ROADMAP) restaurado en el sistema cableado: computePasosCrm marca indicacionesPreop completo a partir del acuse, y el Fallback 1 legacy v1.12 vuelve a funcionar para pacientes multi-zona (no regresion, D-04, sin backfill).
- INDIC-04 pasa de PARTIALLY SATISFIED a SATISFIED: el gap de wiring del select queda cerrado y protegido por un test de frontera.
</success_criteria>

<output>
Create `.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-04-SUMMARY.md` when done.
</output>
