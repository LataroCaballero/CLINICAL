---
phase: 62-portal-staff-frontend-gate-de-firma-secciones-separadas-y-si
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useCRMKanban.ts
  - backend/src/modules/pacientes/pacientes.service.ts
  - frontend/src/components/crm/CardActionsSheet.tsx
  - frontend/src/components/crm/EtapaStepper.tsx
autonomous: true
requirements: [INDIC-05, EMBUDO-06]

must_haves:
  truths:
    - "getKanban expone indicacionesLeidasAt en el objeto de respuesta de cada paciente (campo ya seleccionado, sólo se agrega al .map)"
    - "El tipo KanbanPatient incluye indicacionesLeidasAt: string | null"
    - "El stepper del sheet muestra la fecha de lectura de indicaciones cuando indicacionesLeidasAt != null (formato es-AR)"
    - "El coloreo verde/naranja del dot 'Indicaciones preop' lo sigue gobernando pasos.indicacionesPreop (sin cambio de derivación)"
    - "El board CRM refetchea al volver el foco a la pestaña (refetchOnWindowFocus:true) con staleTime bajo, sin recarga manual"
    - "D-08: getKanban expone indicacionesLeidasAt en el .map (campo ya seleccionado) y KanbanPatient lo tipa"
    - "D-09: EtapaStepper renderiza la fecha de lectura en el sub-indicador Indicaciones preop cuando indicacionesLeidasAt != null (formato es-AR); el coloreo del dot sigue gobernado por pasos.indicacionesPreop"
    - "D-10: useCRMKanban activa refetchOnWindowFocus:true y baja staleTime al rango 0–5_000"
    - "D-11: el query key ['crm-kanban', profesionalId] no se modifica"
  artifacts:
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "indicacionesLeidasAt en el .map de getKanban (sibling de flujo)"
      contains: "indicacionesLeidasAt"
    - path: "frontend/src/hooks/useCRMKanban.ts"
      provides: "campo en KanbanPatient + refetchOnWindowFocus + staleTime bajo"
      contains: "refetchOnWindowFocus"
    - path: "frontend/src/components/crm/EtapaStepper.tsx"
      provides: "render de fecha en sub-indicador Indicaciones preop"
      contains: "toLocaleDateString"
    - path: "frontend/src/components/crm/CardActionsSheet.tsx"
      provides: "thread de indicacionesLeidasAt a EtapaStepper"
      contains: "indicacionesLeidasAt"
  key_links:
    - from: "backend getKanban .map"
      to: "KanbanPatient.indicacionesLeidasAt"
      via: "campo expuesto en el objeto de respuesta"
      pattern: "indicacionesLeidasAt: p.indicacionesLeidasAt"
    - from: "CardActionsSheet.tsx"
      to: "EtapaStepper"
      via: "prop indicacionesLeidasAt={patient.indicacionesLeidasAt}"
      pattern: "indicacionesLeidasAt=\\{patient"
    - from: "useCRMKanban query"
      to: "board freshness"
      via: "refetchOnWindowFocus:true + staleTime bajo"
      pattern: "refetchOnWindowFocus:\\s*true"
---

<objective>
Exponer la fecha de lectura de indicaciones en el stepper del staff (INDIC-05) y cerrar la deuda W-1: el board CRM se sincroniza sin recarga manual vía refetch on window focus (EMBUDO-06).

Purpose: INDIC-05 requiere que el staff vea la fecha en que el paciente leyó las indicaciones. El backend de Phase 61 ya selecciona `indicacionesLeidasAt` en `getKanban` pero no lo expone en la respuesta; sólo falta agregarlo al `.map` y tiparlo. EMBUDO-06 cierra la deuda W-1 de v1.13 activando `refetchOnWindowFocus` con `staleTime` bajo. Ambos cambios comparten `useCRMKanban.ts`, por eso van juntos.
Output: campo expuesto en backend + tipado + threaded al stepper con render de fecha; query del board con freshness.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-portal-staff-frontend-gate-de-firma-secciones-separadas-y-si/62-CONTEXT.md
@.planning/phases/62-portal-staff-frontend-gate-de-firma-secciones-separadas-y-si/62-PATTERNS.md
@.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-CONTEXT.md

<interfaces>
<!-- Contratos existentes verificados por el pattern mapper + scout. -->

backend pacientes.service.ts getKanban:
  El select ya trae `indicacionesLeidasAt` (línea 662) y ya lo pasa a computePasosCrm (línea ~752). El objeto de respuesta del .map (líneas 715-754) tiene un sibling `flujo: p.flujo ?? null,` (línea 743) — el nuevo campo se agrega al lado, mismo estilo: `indicacionesLeidasAt: p.indicacionesLeidasAt,`. computePasosCrm NO devuelve la fecha cruda (sólo el estado 'completo'/'pendiente'), por eso hay que exponerla directo. NO cambiar la derivación de pasos.indicacionesPreop.

frontend useCRMKanban.ts:
  interface KanbanPatient (líneas 36-61) — agregar `indicacionesLeidasAt: string | null;` cerca de los campos de Phase 58 (pasos/todosCompletos).
  useQuery options (líneas 93-105): staleTime actual 30_000 → bajar (0–5_000, elegir exacto); agregar `refetchOnWindowFocus: true`. queryKey ["crm-kanban", profesionalId] NO se cambia (D-11).

frontend EtapaStepper.tsx:
  interface EtapaStepperProps (líneas 25-34) tiene `pasos?: PasosCrm;` y `flujo?: KanbanPatient["flujo"];`. Agregar `indicacionesLeidasAt?: string | null;`. Destructurar en la firma (líneas 36-45).
  Sub-indicador "Indicaciones preop" (líneas 213-225): el span muestra el texto; agregar `{indicacionesLeidasAt && ` · leídas ${new Date(indicacionesLeidasAt).toLocaleDateString("es-AR")}`}`. El dot verde/naranja sigue gobernado por pasos!.indicacionesPreop.

frontend CardActionsSheet.tsx:
  Único caller de <EtapaStepper> (línea 158). Recibe `patient: KanbanPatient`. Ya pasa pasos={patient.pasos} flujo={patient.flujo}. Agregar indicacionesLeidasAt={patient.indicacionesLeidasAt}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Exponer indicacionesLeidasAt (backend getKanban + tipo KanbanPatient) y activar board sync</name>
  <files>backend/src/modules/pacientes/pacientes.service.ts, frontend/src/hooks/useCRMKanban.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.ts (foco getKanban: select ~línea 662, objeto de respuesta del .map ~líneas 715-754, sibling flujo línea 743)
    - frontend/src/hooks/useCRMKanban.ts (interface KanbanPatient líneas 36-61, useQuery options líneas 93-105)
    - .planning/phases/62-portal-staff-frontend-gate-de-firma-secciones-separadas-y-si/62-PATTERNS.md (secciones useCRMKanban.ts y pacientes.service.ts)
    - .planning/phases/62-portal-staff-frontend-gate-de-firma-secciones-separadas-y-si/62-CONTEXT.md (D-08, D-10, D-11)
  </read_first>
  <action>
    Tres cambios de contrato (backend expone → frontend tipa → query sincroniza), per D-08/D-10/D-11:
    (1) Backend `pacientes.service.ts` getKanban: en el objeto de respuesta del `.map` (líneas 715-754), agregar el campo `indicacionesLeidasAt: p.indicacionesLeidasAt,` como sibling de `flujo: p.flujo ?? null,` (línea 743). El `select` ya trae el campo (línea 662) — NO agregar select nuevo, NO tocar la llamada a `computePasosCrm` ni la derivación de `pasos.indicacionesPreop`. El campo viaja sólo para display de fecha (D-08).
    (2) Frontend `useCRMKanban.ts`: agregar `indicacionesLeidasAt: string | null;` a la interfaz `KanbanPatient` (cerca de `pasos`/`todosCompletos`, líneas 36-61). Comentar que es display-only (INDIC-05) y NO gobierna `pasos.indicacionesPreop`.
    (3) Frontend `useCRMKanban.ts`: en las opciones de `useQuery` (líneas 93-105) bajar `staleTime` de `30_000` a un valor bajo dentro del rango D-10 (elegir `0`; freshness sobre volumen de requests, cierre W-1) y agregar `refetchOnWindowFocus: true`. NO cambiar `queryKey: ["crm-kanban", profesionalId]` (D-11). NO tocar las invalidaciones existentes en otros hooks.
  </action>
  <acceptance_criteria>
    - `pacientes.service.ts`: el `.map` de getKanban contiene `indicacionesLeidasAt: p.indicacionesLeidasAt`
    - `useCRMKanban.ts`: la interfaz `KanbanPatient` contiene `indicacionesLeidasAt: string | null`
    - `useCRMKanban.ts` contiene `refetchOnWindowFocus: true`
    - `useCRMKanban.ts` ya no contiene `staleTime: 30_000` (bajado a valor 0–5000)
    - `useCRMKanban.ts` conserva `queryKey: ["crm-kanban", profesionalId]` sin cambios
    - `cd backend && npm run build` termina con exit code 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "indicacionesLeidasAt: p.indicacionesLeidasAt" backend/src/modules/pacientes/pacientes.service.ts && grep -q "indicacionesLeidasAt: string | null" frontend/src/hooks/useCRMKanban.ts && grep -q "refetchOnWindowFocus: true" frontend/src/hooks/useCRMKanban.ts && ! grep -q "staleTime: 30_000" frontend/src/hooks/useCRMKanban.ts && cd backend && npm run build</automated>
  </verify>
  <done>Backend expone la fecha; `KanbanPatient` la tipa; el query del board hace refetch on focus con staleTime bajo y queryKey intacto; build backend en verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Threadear indicacionesLeidasAt y renderizar la fecha en el sub-indicador (CardActionsSheet + EtapaStepper)</name>
  <files>frontend/src/components/crm/CardActionsSheet.tsx, frontend/src/components/crm/EtapaStepper.tsx</files>
  <read_first>
    - frontend/src/components/crm/EtapaStepper.tsx (interface EtapaStepperProps líneas 25-34, firma líneas 36-45, sub-indicador Indicaciones preop líneas 213-225)
    - frontend/src/components/crm/CardActionsSheet.tsx (invocación de EtapaStepper línea 158, recibe patient: KanbanPatient)
    - .planning/phases/62-portal-staff-frontend-gate-de-firma-secciones-separadas-y-si/62-PATTERNS.md (secciones EtapaStepper.tsx + Date formatting convention)
    - .planning/phases/62-portal-staff-frontend-gate-de-firma-secciones-separadas-y-si/62-CONTEXT.md (D-09)
  </read_first>
  <action>
    Threadear el campo del parent al stepper y renderizar la fecha, per D-09:
    (1) `EtapaStepper.tsx`: agregar `indicacionesLeidasAt?: string | null;` a la interfaz `EtapaStepperProps` (junto a `pasos?`/`flujo?`, líneas 25-34) y destructurarlo en la firma de la función (junto a `pasos,`, líneas 36-45).
    (2) `EtapaStepper.tsx`: en el sub-indicador "Indicaciones preop" (líneas 213-225), extender el `<span>` para que muestre la fecha cuando exista: sufijo ` · leídas DD/MM/AAAA` usando `new Date(indicacionesLeidasAt).toLocaleDateString("es-AR")` sólo si `indicacionesLeidasAt` es truthy. Mantener el dot verde/naranja gobernado por `pasos!.indicacionesPreop === "completo"` (sin cambios en el coloreo).
    (3) `CardActionsSheet.tsx`: en la invocación `<EtapaStepper ...>` (línea 158) agregar el prop `indicacionesLeidasAt={patient.indicacionesLeidasAt}`, junto a `pasos={patient.pasos}` y `flujo={patient.flujo}`.
    Micro-copy del sufijo de fecha a discreción (consistente con el formato es-AR del resto del board/portal).
  </action>
  <acceptance_criteria>
    - `EtapaStepper.tsx`: `EtapaStepperProps` contiene `indicacionesLeidasAt?: string | null`
    - `EtapaStepper.tsx`: el sub-indicador "Indicaciones preop" usa `toLocaleDateString("es-AR")` condicionado a `indicacionesLeidasAt`
    - `EtapaStepper.tsx`: el dot sigue coloreado por `pasos!.indicacionesPreop` (sin nueva lógica de color)
    - `CardActionsSheet.tsx`: la invocación de `EtapaStepper` pasa `indicacionesLeidasAt={patient.indicacionesLeidasAt}`
    - `cd frontend && npm run build` termina con exit code 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "indicacionesLeidasAt" src/components/crm/EtapaStepper.tsx && grep -q "toLocaleDateString" src/components/crm/EtapaStepper.tsx && grep -q "indicacionesLeidasAt={patient.indicacionesLeidasAt}" src/components/crm/CardActionsSheet.tsx && npm run build</automated>
  </verify>
  <done>El stepper del sheet muestra "Indicaciones preop · leídas DD/MM/AAAA" cuando el paciente leyó; coloreo del dot intacto; build frontend en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → staff (getKanban) | El payload del board viaja al staff autenticado vía api.ts (JWT staff) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-07 | Information Disclosure | `indicacionesLeidasAt` expuesto en getKanban | mitigate | Campo no sensible (timestamp de acuse), viaja por `api.ts` que ya autentica al staff (JWT + guards existentes de pacientes.controller). getKanban ya está scoped por `profesionalId`. Sin nueva superficie de exposición pública. |
| T-62-08 | Denial of Service | `refetchOnWindowFocus:true` + `staleTime` bajo | accept | Trade-off aceptado (D-10): más requests al board a cambio de datos frescos. El volumen es acotado (un query por focus de pestaña, key scoped por profesional). Cierre de deuda W-1. Sin riesgo de DoS real. |

Sin amenazas de severidad alta sin mitigar.
</threat_model>

<verification>
- `cd backend && npm run build` y `cd frontend && npm run build` terminan en exit 0.
- Manual/UAT (checkpoint de fase): en el board, abrir el sheet de un paciente con indicaciones leídas → el sub-indicador muestra la fecha; hacer un acuse desde el portal, volver el foco a la pestaña del board → el board refresca sin recarga manual y el dot pasa a verde.
</verification>

<success_criteria>
- INDIC-05: el staff ve en el stepper si el paciente leyó las indicaciones, con la fecha.
- EMBUDO-06: el board refleja consentimiento firmado / indicaciones leídas sin recarga manual (refetch on window focus activo en `['crm-kanban']`).
</success_criteria>

<output>
Create `.planning/phases/62-portal-staff-frontend-gate-de-firma-secciones-separadas-y-si/62-03-SUMMARY.md` when done
</output>
