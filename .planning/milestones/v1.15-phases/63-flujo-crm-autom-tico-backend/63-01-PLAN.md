---
phase: 63-flujo-crm-autom-tico-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/pacientes/pacientes.service.ts
  - backend/src/modules/pacientes/pacientes.service.spec.ts
autonomous: true
requirements: [EMBUDO-07]
must_haves:
  truths:
    - "D-01: al crear un paciente, el registro persiste con `etapaCRM = NUEVO_LEAD` (nunca null)"
    - "D-01: `create()` setea `etapaCRM = NUEVO_LEAD` de forma incondicional — el `CreatePacienteDto` del endpoint create hoy NO transporta `etapaCRM`, por lo que no hay override que reconciliar aquí; el path import/otro flujo (que sí podría traer override) vive fuera de este endpoint"
    - "D-03: el lead recién creado queda con `flujo = null`, por lo que pasa el filtro `OR:[{flujo:CIRUGIA},{flujo:null}]` de getKanban y cae en la columna NUEVO_LEAD"
    - "D-02: la lógica solo afecta a pacientes creados desde ahora (default en `create()`); no hay backfill ni update masivo de históricos"
  artifacts:
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "Default etapaCRM=NUEVO_LEAD + flujo=null en create()"
      contains: "EtapaCRM.NUEVO_LEAD"
    - path: "backend/src/modules/pacientes/pacientes.service.spec.ts"
      provides: "Regresión de default de create() + visibilidad en getKanban"
  key_links:
    - from: "pacientes.service.ts::create()"
      to: "prisma.paciente.create data literal"
      via: "etapaCRM: EtapaCRM.NUEVO_LEAD ; flujo: null"
      pattern: "etapaCRM.*NUEVO_LEAD"
---

<objective>
EMBUDO-07: un paciente recién creado entra al kanban en la columna "Nuevo Lead" (`etapaCRM = NUEVO_LEAD`) en vez de quedar "Sin clasificar".

Purpose: Que la secretaria vea a todo lead nuevo en el board sin clasificarlo a mano — primer eslabón del embudo automático de v1.15.
Output: `pacientes.service.ts::create()` aplica el default y el lead es visible en getKanban; regresión en el spec.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/63-flujo-crm-autom-tico-backend/63-CONTEXT.md
@.planning/phases/63-flujo-crm-autom-tico-backend/63-PATTERNS.md

<interfaces>
<!-- Enums ya importados en pacientes.service.ts (L20). NO agregar imports nuevos. -->
enum EtapaCRM { NUEVO_LEAD, TURNO_AGENDADO, CONSULTADO, PRESUPUESTO_ENVIADO, PROCEDIMIENTO_REALIZADO, CONFIRMADO, PERDIDO }
enum FlujoPaciente { CIRUGIA, TRATAMIENTO, PENDIENTE }   // schema default de Paciente.flujo = PENDIENTE

// CreatePacienteDto (backend/src/modules/pacientes/dto/create-paciente.dto.ts):
//   NO declara etapaCRM ni flujo (sólo `estado?: EstadoPaciente`). => el default en create() es INCONDICIONAL.
// create() actual (L53-78): construye `data = { ...dto, fechaNacimiento?, fechaIndicaciones? }`
//   y NO setea etapaCRM ni flujo → hoy toma flujo @default(PENDIENTE) y etapaCRM null.
// getKanban() (L620-705): where `OR:[{flujo:CIRUGIA},{flujo:null}]`; buckets por etapaCRM;
//   existe la columna NUEVO_LEAD (L689); etapaCRM null cae en SIN_CLASIFICAR.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Default etapaCRM=NUEVO_LEAD y flujo=null en create()</name>
  <files>backend/src/modules/pacientes/pacientes.service.ts, backend/src/modules/pacientes/pacientes.service.spec.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.ts (L1-78: imports y método create())
    - backend/src/modules/pacientes/dto/create-paciente.dto.ts (confirmar que NO existe campo etapaCRM/flujo)
    - backend/src/modules/pacientes/pacientes.service.spec.ts (L1-40: patrón de mock TestingModule + PrismaService)
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-CONTEXT.md (D-01, D-02, D-03)
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-PATTERNS.md (sección "EMBUDO-07": house-style del `data` literal y análisis D-03 opción 1)
  </read_first>
  <behavior>
    - Test A: create() con un DTO típico → prisma.paciente.create recibe `data.etapaCRM === EtapaCRM.NUEVO_LEAD` (default incondicional D-01).
    - Test B: create() con un DTO que trae otros campos poblados (p. ej. `estado: EstadoPaciente.ACTIVO`) → `data.etapaCRM` SIGUE siendo `EtapaCRM.NUEVO_LEAD` (ningún otro campo del DTO altera el default; el DTO no puede pasar etapaCRM).
    - Test C: create() con un DTO típico → `data.flujo === null` (D-03 opción 1, para pasar el filtro de getKanban).
    - Test D: create() → `prisma.paciente.create` se invoca exactamente una vez y el `data` literal contiene simultáneamente `etapaCRM=NUEVO_LEAD` y `flujo=null`.
  </behavior>
  <action>
    En `create()` (L53-78), dentro del `data` literal que hoy sólo deriva `fechaNacimiento`/`fechaIndicaciones`, agregar dos campos con valor fijo (el `CreatePacienteDto` NO transporta estos campos, así que el default es incondicional): `etapaCRM: EtapaCRM.NUEVO_LEAD` (D-01) y `flujo: null` (D-03). NO usar `dto.etapaCRM ?? ...` ni `dto.flujo ?? ...` — esas propiedades no existen en el DTO y romperían el gate `tsc` (TS2339). NO tocar getKanban ni getListaAccion (opción 1 de PATTERNS.md — flujo=null ya pasa su filtro; menor blast radius, no surfacea históricos con flujo=PENDIENTE). `EtapaCRM` y `FlujoPaciente` ya están importados; no agregar imports. Escribir los tests A-D en pacientes.service.spec.ts mockeando `prisma.paciente.create` con `jest.fn()` y aseverando el argumento recibido (`create.mock.calls[0][0].data`). Aprovechar para eliminar el `console.log('DTO RECIBIDO', dto)` (tech debt: expone PII en logs) sólo si no rompe otros tests.
  </action>
  <verify>
    <automated>cd backend && npx jest src/modules/pacientes/pacientes.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "etapaCRM: EtapaCRM.NUEVO_LEAD" backend/src/modules/pacientes/pacientes.service.ts` devuelve una línea dentro de create().
    - `grep -n "flujo: null" backend/src/modules/pacientes/pacientes.service.ts` devuelve una línea dentro de create().
    - `grep -c "dto.etapaCRM" backend/src/modules/pacientes/pacientes.service.ts` NO aparece en create() (no se referencia una propiedad inexistente del DTO).
    - `cd backend && npx tsc --noEmit -p tsconfig.build.json` pasa sin TS2339.
    - Los 4 tests (A-D) pasan; el spec suite no regresiona.
    - getKanban/getListaAccion NO fueron modificados (`git diff` no muestra cambios en esos métodos).
  </acceptance_criteria>
  <done>create() aplica el default NUEVO_LEAD + flujo=null de forma incondicional (sin referenciar campos inexistentes del DTO), con tests A-D verdes y tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 2: Regresión de visibilidad en getKanban para el lead nuevo</name>
  <files>backend/src/modules/pacientes/pacientes.service.spec.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.ts (L620-705: getKanban where-filter y bucketing por etapaCRM)
    - backend/src/modules/pacientes/pacientes.service.spec.ts (patrón de mock de findMany)
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-PATTERNS.md (invariante board/action-list filter)
  </read_first>
  <action>
    Agregar un test que mockee `prisma.paciente.findMany` para devolver un paciente `{ etapaCRM: EtapaCRM.NUEVO_LEAD, flujo: null, ... }` y verifique que getKanban lo ubica en la columna `NUEVO_LEAD` (no en `SIN_CLASIFICAR`) — bloqueando el Success Criterion #1. Complementariamente, aseverar que el objeto `where` usado en el findMany conserva el filtro `OR:[{flujo:CIRUGIA},{flujo:null}]` (invariante v1.13) para dejar constancia de que la visibilidad se apoya en `flujo:null`, no en un cambio de filtro. Este task es sólo de test (no hay cambio de producción: flujo=null ya pasa el filtro).
  </action>
  <verify>
    <automated>cd backend && npx jest src/modules/pacientes/pacientes.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Existe un test cuyo nombre menciona NUEVO_LEAD/getKanban y asevera que el paciente cae en la columna `NUEVO_LEAD`.
    - El test falla si alguien cambia el default de create() a un flujo que NO pase el filtro (ej. PENDIENTE) — es decir, prueba el criterio end-to-end.
  </acceptance_criteria>
  <done>Test de regresión verde que ancla la visibilidad del lead nuevo en la columna NUEVO_LEAD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente→API `POST /pacientes` (create) | El DTO es input no confiable; NO expone `etapaCRM`/`flujo` (no declarados en CreatePacienteDto), por lo que el default no es manipulable por el cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Tampering | `create()` default de etapaCRM/flujo | accept | El default es un valor fijo del servidor (`NUEVO_LEAD` / `null`), no derivado del DTO → el cliente NO puede inyectar una etapa/flujo arbitrarios por este endpoint. No hay escalación de privilegios (no es campo de rol/tenant). |
| T-63-02 | Information Disclosure | `console.log('DTO RECIBIDO', dto)` en create() | mitigate | Eliminar el log que expone PII del paciente (tech debt STATE.md) si no rompe tests. |
| T-63-03 | Spoofing/Tenant | scoping por `profesionalId` en getKanban | accept | create() no cambia el scoping multi-tenant existente; getKanban ya filtra por `profesionalId`. Sin cambios de autorización en este plan. |
| T-63-SC | Tampering | npm installs | accept | No se instalan paquetes en este plan (edición de archivos existentes). |
</threat_model>

<verification>
- `cd backend && npx jest src/modules/pacientes/pacientes.service.spec.ts` → verde.
- `cd backend && npx tsc --noEmit -p tsconfig.build.json` → sin errores de tipo.
- Un paciente creado sin etapaCRM aparece en la columna NUEVO_LEAD del board (aseverado por test de Task 2).
</verification>

<success_criteria>
- create() setea `etapaCRM = NUEVO_LEAD` y `flujo = null` por default de forma incondicional (D-01/D-03), sin referenciar campos inexistentes del DTO.
- Ningún backfill ni update de históricos (D-02): sólo el default en el path de creación.
- getKanban ubica al lead nuevo en NUEVO_LEAD (Success Criterion #1 del roadmap).
</success_criteria>

<output>
Create `.planning/phases/63-flujo-crm-autom-tico-backend/63-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
