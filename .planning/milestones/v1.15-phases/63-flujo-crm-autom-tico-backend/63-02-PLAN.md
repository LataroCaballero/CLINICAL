---
phase: 63-flujo-crm-autom-tico-backend
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - backend/src/modules/turnos/turnos.service.ts
  - backend/src/modules/turnos/turnos.service.spec.ts
  - backend/src/modules/pacientes/pacientes.service.ts
  - backend/src/modules/pacientes/pacientes.service.spec.ts
autonomous: true
requirements: [EMBUDO-08]
must_haves:
  truths:
    - "D-04: al crear un turno de cirugía (crearTurnoCirugia, esCirugia=true) el paciente pasa a `etapaCRM = CONFIRMADO` dentro de la misma transacción, sin depender de presupuesto aceptado"
    - "D-05: un turno nuevo NO degrada una etapa avanzada (CONFIRMADO / PROCEDIMIENTO_REALIZADO) a TURNO_AGENDADO"
    - "D-06: EXCEPCIÓN a D-05 — cuando el turno es de tipo Consulta (`tipoTurno.nombre === 'Consulta'`) SÍ reinicia el ciclo a TURNO_AGENDADO (nuevo ciclo del embudo)"
    - "D-05: un turno nuevo sobre etapa NO avanzada (incluye PERDIDO) sigue reactivando a TURNO_AGENDADO"
    - "D-07: al cancelar un turno de cirugía el paciente MANTIENE `CONFIRMADO` (no se degrada), se marca `temperatura = CALIENTE` y la cirugía asociada pasa a CANCELADA"
    - "D-07: un paciente CONFIRMADO con cirugía CANCELADA/SUSPENDIDA y sin cirugía PROGRAMADA futura reaparece en getListaAccion con `requiereRecontacto = true` (derivado, schema-free)"
  artifacts:
    - path: "backend/src/modules/turnos/turnos.service.ts"
      provides: "CONFIRMADO en crearTurnoCirugia; guard selectivo en crearTurno; recontacto en cancelarTurno"
      contains: "EtapaCRM.CONFIRMADO"
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "getListaAccion surfacea el caso recontacto vía query derivada + flag computado"
      contains: "requiereRecontacto"
    - path: "backend/src/modules/turnos/turnos.service.spec.ts"
      provides: "Tests de D-04/D-05/D-06/D-07 (archivo nuevo)"
  key_links:
    - from: "turnos.service.ts::crearTurnoCirugia() $transaction"
      to: "tx.paciente.update etapaCRM=CONFIRMADO"
      via: "escritura dentro de la tx tras crear el turno"
      pattern: "etapaCRM: EtapaCRM.CONFIRMADO"
    - from: "turnos.service.ts::crearTurno() guard"
      to: "tipoTurno.nombre === 'Consulta'"
      via: "select nombre + condición esConsulta || !etapaAvanzada"
      pattern: "nombre === 'Consulta'"
    - from: "turnos.service.ts::cancelarTurno()"
      to: "pacientes.service.ts::getListaAccion() rama recontacto"
      via: "cirugia.estado=CANCELADA + temperatura=CALIENTE → query derivada"
      pattern: "requiereRecontacto"
---

<objective>
EMBUDO-08: el estado del embudo refleja el ciclo quirúrgico automáticamente — agendar cirugía confirma al paciente, un turno nuevo no rompe una etapa avanzada (salvo Consulta = ciclo nuevo), y cancelar una cirugía deja al paciente CONFIRMADO + caliente + en la lista de acción para recontacto.

Purpose: Que el board y la lista de acción sean fiel reflejo del ciclo real del paciente, respetando la naturaleza cíclica del embudo (D-05) y sin perder pacientes con cirugía caída (D-07).
Output: transiciones CRM inline en turnos.service.ts + surfacing de recontacto en getListaAccion; tests nuevos.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/63-flujo-crm-autom-tico-backend/63-CONTEXT.md
@.planning/phases/63-flujo-crm-autom-tico-backend/63-PATTERNS.md
@.planning/phases/63-flujo-crm-autom-tico-backend/63-01-SUMMARY.md

<interfaces>
<!-- Enums ya importados en turnos.service.ts (L11-19). NO agregar imports nuevos. -->
enum EtapaCRM { NUEVO_LEAD, TURNO_AGENDADO, CONSULTADO, PRESUPUESTO_ENVIADO, PROCEDIMIENTO_REALIZADO, CONFIRMADO, PERDIDO }
enum EstadoCirugia { PROGRAMADA, EN_CURSO, COMPLETADA, CANCELADA, SUSPENDIDA }
enum EstadoTurno { PENDIENTE, CONFIRMADO, CANCELADO, AUSENTE, FINALIZADO, EN_ESPERA, SIENDO_ATENDIDO }
enum TemperaturaPaciente { CALIENTE, TIBIO, FRIO }
enum TipoContacto { ... SISTEMA }

// ContactoLog (schema): profesionalId es NON-NULL (obligatorio). Turno.profesionalId existe (non-null)
//   => cualquier contactoLog.create debe pasar profesionalId (del turno o del dto).
// Analog canónico de transición CRM inline en este archivo: cerrarSesion() L881-911
//   (nuevaEtapa guardado + paciente.update + contactoLog SISTEMA con profesionalId).
// crearTurnoCirugia() L661-772: $transaction crea cirugia (estado PROGRAMADA) luego turno (esCirugia=true, cirugiaId). dto.profesionalId disponible.
// crearTurno() L131-141: HOY setea etapaCRM=TURNO_AGENDADO INCONDICIONAL. tipoTurno select L52-55 = {id,duracionDefault,flujoPaciente} (falta nombre). pacienteCRM findUnique ya selecciona etapaCRM.
// cancelarTurno() L215-240: select {id,estado}; sólo hace turno.update estado=CANCELADO. Sin side-effects CRM. Turno.profesionalId/pacienteId/cirugiaId/esCirugia disponibles si se amplía el select.
// getListaAccion() L884-893: where etapaCRM notIn ['CONFIRMADO','PERDIDO'] + OR:[{flujo:CIRUGIA},{flujo:null}]; mapea items con score.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: crearTurnoCirugia → CONFIRMADO (D-04)</name>
  <files>backend/src/modules/turnos/turnos.service.ts, backend/src/modules/turnos/turnos.service.spec.ts</files>
  <read_first>
    - backend/src/modules/turnos/turnos.service.ts (L661-772 crearTurnoCirugia; L881-911 cerrarSesion como analog de la escritura CRM)
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-CONTEXT.md (D-04)
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-PATTERNS.md (EMBUDO-08 → crearTurnoCirugia D-04)
    - backend/src/modules/pacientes/pacientes.service.spec.ts (patrón de TestingModule + mock PrismaService, para replicar en el spec nuevo)
  </read_first>
  <behavior>
    - Test: crearTurnoCirugia(dto válido) → dentro de la `$transaction` se invoca `tx.paciente.update` con `where.id === dto.pacienteId` y `data.etapaCRM === EtapaCRM.CONFIRMADO`.
    - Test: la escritura NO depende de que exista presupuesto aceptado (el dto no incluye presupuesto y aún así confirma).
  </behavior>
  <action>
    Crear el archivo `turnos.service.spec.ts` (no existe) replicando el patrón de TestingModule + PrismaService mock de pacientes.service.spec.ts; mockear `prisma.$transaction` para ejecutar el callback con un `tx` cuyos `cirugia.create`/`turno.create`/`paciente.update`/`contactoLog.create` sean `jest.fn()`. En crearTurnoCirugia (L726-771), tras el `tx.turno.create`, agregar `await tx.paciente.update({ where: { id: dto.pacienteId }, data: { etapaCRM: EtapaCRM.CONFIRMADO } })` (D-04, dentro de la MISMA tx — nunca abrir tx anidada) y, para consistencia con el analog cerrarSesion, un `tx.contactoLog.create({ data: { pacienteId: dto.pacienteId, profesionalId: dto.profesionalId, tipo: TipoContacto.SISTEMA, nota: 'Cirugía agendada — confirmado' } })` (ContactoLog.profesionalId es non-null → siempre pasar profesionalId). `EtapaCRM.CONFIRMADO` y `TipoContacto.SISTEMA` ya están importados. Mantener el `return turno`.
  </action>
  <verify>
    <automated>cd backend && npx jest src/modules/turnos/turnos.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "etapaCRM: EtapaCRM.CONFIRMADO" backend/src/modules/turnos/turnos.service.ts` aparece dentro de crearTurnoCirugia.
    - La escritura usa el cliente `tx` de la transacción existente (no `this.prisma`), verificado por lectura del diff.
    - Tests de D-04 verdes.
  </acceptance_criteria>
  <done>Agendar una cirugía deja al paciente CONFIRMADO transaccionalmente, sin depender de presupuesto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard selectivo de degradación en crearTurno (D-05/D-06)</name>
  <files>backend/src/modules/turnos/turnos.service.ts, backend/src/modules/turnos/turnos.service.spec.ts</files>
  <read_first>
    - backend/src/modules/turnos/turnos.service.ts (L36-158 crearTurno; foco L52-55 select de tipoTurno y L131-141 degradación incondicional actual)
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-CONTEXT.md (D-05, D-06 y "embudo cíclico")
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-PATTERNS.md (EMBUDO-08 → D-05/D-06 sketch)
  </read_first>
  <behavior>
    - Test A: paciente `etapaCRM=CONFIRMADO` + turno NO Consulta → paciente.update de etapaCRM NO se invoca (se mantiene CONFIRMADO).
    - Test B: paciente `etapaCRM=PROCEDIMIENTO_REALIZADO` + turno NO Consulta → etapaCRM se mantiene (no degrada).
    - Test C: paciente `etapaCRM=CONFIRMADO` + turno `tipoTurno.nombre==='Consulta'` → etapaCRM se setea a TURNO_AGENDADO (nuevo ciclo, D-06).
    - Test D: paciente `etapaCRM=CONSULTADO` (no avanzada) + turno cualquiera → etapaCRM se setea a TURNO_AGENDADO.
    - Test E: paciente `etapaCRM=PERDIDO` + turno NO Consulta → etapaCRM se setea a TURNO_AGENDADO (PERDIDO no es "avanzada", sigue reactivando).
  </behavior>
  <action>
    En el select de `tipoTurno` (L52-55) agregar `nombre: true`. Reemplazar la escritura incondicional de L138-141 por un guard: computar `esConsulta = tipoTurno.nombre === 'Consulta'` (D-06 — `nombre` es @unique, no existe flag esConsulta) y `etapaAvanzada = pacienteCRM?.etapaCRM === EtapaCRM.CONFIRMADO || pacienteCRM?.etapaCRM === EtapaCRM.PROCEDIMIENTO_REALIZADO`; ejecutar el `paciente.update({ data: { etapaCRM: EtapaCRM.TURNO_AGENDADO } })` sólo si `esConsulta || !etapaAvanzada`. Actualizar el comentario L131-133 (que hoy dice que el guard forward-only fue eliminado) para reflejar el guard selectivo de D-05/D-06. NO tocar el bloque de flujo auto-update L143-158 ni el contactoLog posterior. Añadir los tests A-E al spec.
  </action>
  <verify>
    <automated>cd backend && npx jest src/modules/turnos/turnos.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "nombre === 'Consulta'" backend/src/modules/turnos/turnos.service.ts` devuelve una línea en crearTurno.
    - El `tipoTurno.findUnique` select incluye `nombre: true`.
    - Los 5 tests A-E pasan; ninguno degrada CONFIRMADO/PROCEDIMIENTO_REALIZADO salvo el caso Consulta.
  </acceptance_criteria>
  <done>Un turno nuevo respeta etapas avanzadas salvo Consulta (reinicia ciclo); PERDIDO/CONSULTADO siguen reactivando.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cancelarTurno mantiene CONFIRMADO + recontacto surfacing (D-07)</name>
  <files>backend/src/modules/turnos/turnos.service.ts, backend/src/modules/turnos/turnos.service.spec.ts, backend/src/modules/pacientes/pacientes.service.ts, backend/src/modules/pacientes/pacientes.service.spec.ts</files>
  <read_first>
    - backend/src/modules/turnos/turnos.service.ts (L215-240 cancelarTurno; L861-869 cerrarSesion como ejemplo de widen del select antes de un side-effect CRM)
    - backend/src/modules/pacientes/pacientes.service.ts (L872-901 getListaAccion where + inicio del map)
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-CONTEXT.md (D-07 completo)
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-PATTERNS.md (EMBUDO-08 → D-07 surfacing derivado, "No Analog Found")
  </read_first>
  <behavior>
    - Test A (cancelarTurno, cirugía): turno.esCirugia=true → dentro de una tx se setea `turno.estado=CANCELADO`, la cirugía asociada `estado=CANCELADA`, `paciente.temperatura=CALIENTE`, se escribe un `contactoLog` con `profesionalId === turno.profesionalId`, y NO se escribe etapaCRM (se mantiene CONFIRMADO).
    - Test B (cancelarTurno, no cirugía): turno.esCirugia=false → comportamiento actual (sólo cancela el turno), sin tocar cirugía/temperatura/etapaCRM.
    - Test C (getListaAccion): paciente CONFIRMADO con cirugía CANCELADA y sin cirugía PROGRAMADA futura → aparece en la lista con `requiereRecontacto === true`.
    - Test D (getListaAccion): paciente CONFIRMADO con cirugía CANCELADA pero con una cirugía PROGRAMADA futura → NO aparece por la rama recontacto (fue reprogramada).
    - Test E (getListaAccion): paciente CONFIRMADO "normal" (sin cirugía cancelada) → sigue excluido (no aparece).
  </behavior>
  <action>
    En cancelarTurno (L215-240): ampliar el select a `{ id, estado, esCirugia, cirugiaId, pacienteId, profesionalId }` (incluir `profesionalId` es OBLIGATORIO — el contactoLog.create lo requiere: ContactoLog.profesionalId es non-null y Turno.profesionalId existe); envolver la mutación en `this.prisma.$transaction([...])` sólo cuando `turno.esCirugia` para: (1) `turno.update estado=CANCELADO`, (2) si `turno.cirugiaId`, `cirugia.update({ where:{id:cirugiaId}, data:{ estado: EstadoCirugia.CANCELADA } })` (hace queryable la señal), (3) `paciente.update({ where:{id:pacienteId}, data:{ temperatura: TemperaturaPaciente.CALIENTE } })` — NO escribir etapaCRM (mantener CONFIRMADO, D-07), (4) `contactoLog.create({ data: { pacienteId: turno.pacienteId, profesionalId: turno.profesionalId, tipo: TipoContacto.SISTEMA, nota: 'Cirugía cancelada — requiere recontacto' } })`. Cuando NO es cirugía, mantener el `turno.update` simple actual.
    En getListaAccion (pacientes.service.ts L884-893): reestructurar el `where` a `OR` de dos ramas manteniendo `profesionalId`, `crmArchivado:false` y el `NOT: contactos gte hoy` al nivel superior: rama normal = `{ etapaCRM: { notIn: ['CONFIRMADO','PERDIDO'] }, OR:[{flujo:CIRUGIA},{flujo:null}] }`; rama recontacto = `{ etapaCRM: 'CONFIRMADO', cirugias: { some: { estado: { in: [EstadoCirugia.CANCELADA, EstadoCirugia.SUSPENDIDA] } } }, NOT: { cirugias: { some: { estado: EstadoCirugia.PROGRAMADA, fecha: { gte: ahora } } } } }`. Incluir `cirugias` (select estado+fecha) en el `include`. En el `.map`, computar y exponer `requiereRecontacto: boolean` derivado (true sólo cuando el paciente matchea la condición de la rama recontacto) — flag COMPUTADO, no persistido (schema-free, Out of Scope prohíbe migraciones). El TEXTO del badge es Phase 64; acá sólo se expone el estado. Añadir tests A-E a los specs correspondientes.
  </action>
  <verify>
    <automated>cd backend && npx jest src/modules/turnos/turnos.service.spec.ts src/modules/pacientes/pacientes.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TemperaturaPaciente.CALIENTE" backend/src/modules/turnos/turnos.service.ts` aparece en cancelarTurno.
    - El select ampliado de cancelarTurno incluye `profesionalId` (y `pacienteId`, `cirugiaId`, `esCirugia`) — verificado por lectura del diff; el `contactoLog.create` pasa `profesionalId: turno.profesionalId`.
    - cancelarTurno NO contiene una escritura de `etapaCRM` (verificado por lectura del diff — se mantiene CONFIRMADO).
    - `grep -n "requiereRecontacto" backend/src/modules/pacientes/pacientes.service.ts` devuelve al menos la definición del flag computado.
    - getListaAccion usa `EstadoCirugia.CANCELADA`/`SUSPENDIDA` y excluye por cirugía PROGRAMADA futura.
    - `cd backend && npx tsc --noEmit -p tsconfig.build.json` pasa (el contactoLog.create satisface el profesionalId non-null).
    - Tests A-E verdes.
  </acceptance_criteria>
  <done>Cancelar una cirugía deja al paciente CONFIRMADO + caliente y lo devuelve a la lista de acción con requiereRecontacto; reprogramar lo saca.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente→API turnos (`crearTurno`, `crearTurnoCirugia`, `cancelarTurno`) | El `turnoId`/`pacienteId` del request dispara side-effects de estado CRM sobre un Paciente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-04 | Tampering (state integrity) | Degradación de etapa en crearTurno | mitigate | Guard D-05/D-06 evita que un turno cualquiera degrade CONFIRMADO/PROCEDIMIENTO_REALIZADO; sólo Consulta reinicia deliberadamente. Se aplica al `dto.pacienteId` del turno — nunca cross-paciente (update por id específico). |
| T-63-05 | Tampering (state integrity) | Escrituras CRM en crearTurnoCirugia | mitigate | `tx.paciente.update` usa `dto.pacienteId` validado (findUnique previo L664-667); la escritura vive en la misma $transaction que crea cirugía+turno → atomicidad, no puede confirmar a otro paciente ni dejar estado parcial. |
| T-63-06 | Tampering / cross-tenant | cancelarTurno cascada a cirugía/paciente | mitigate | El pacienteId/cirugiaId/profesionalId provienen del propio turno (select ampliado), no de input del cliente → imposible cancelar/confirmar la cirugía de otro paciente ni atribuir el contactoLog a otro profesional vía el turnoId. Sólo se actúa cuando `turno.esCirugia`. |
| T-63-07 | Elevation / data exposure | getListaAccion rama recontacto | mitigate | La query mantiene el scope `profesionalId` al nivel superior del where → la rama recontacto sólo surfacea pacientes del profesional autenticado; `requiereRecontacto` es derivado (sin persistir estado sensible). |
| T-63-SC | Tampering | npm installs | accept | No se instalan paquetes (edición de archivos existentes). |
</threat_model>

<verification>
- `cd backend && npx jest src/modules/turnos/turnos.service.spec.ts src/modules/pacientes/pacientes.service.spec.ts` → verde.
- `cd backend && npx tsc --noEmit -p tsconfig.build.json` → sin errores de tipo.
- Escenario manual (opcional): agendar cirugía → CONFIRMADO; cancelar → sigue CONFIRMADO + caliente + aparece en lista de acción; reprogramar → sale de la lista.
</verification>

<success_criteria>
- crearTurnoCirugia confirma al paciente sin depender de presupuesto (D-04, Success Criterion #2).
- crearTurno respeta etapas avanzadas salvo Consulta (D-05/D-06); PERDIDO/CONSULTADO reactivan.
- cancelarTurno mantiene CONFIRMADO + CALIENTE y surfacea recontacto sin cambios de schema (D-07).
</success_criteria>

<output>
Create `.planning/phases/63-flujo-crm-autom-tico-backend/63-02-SUMMARY.md` when done.
</output>
</content>
