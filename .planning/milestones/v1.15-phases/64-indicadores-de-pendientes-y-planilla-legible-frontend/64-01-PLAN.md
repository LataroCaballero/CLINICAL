---
phase: 64-indicadores-de-pendientes-y-planilla-legible-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/historia-clinica/historia-clinica.contenido.helpers.ts
  - backend/src/modules/historia-clinica/historia-clinica.contenido.spec.ts
  - backend/src/modules/turnos/turnos.service.ts
autonomous: true
requirements: [TRAT-07]

must_haves:
  truths:
    - "D-07: GET /turnos/rango expone un NUEVO campo array `tratamientos: string[]` con la lista completa de nombres, sin colapsar"
    - "D-07: `ultimoTratamiento` (string colapsado) y `formatearResumen` quedan SIN CAMBIOS — no se rompe ningún consumidor existente"
    - "El helper deriva los nombres del mismo `entradaHC.contenido` ya seleccionado (sin nuevo select, sin migración Prisma)"
    - "Contenido free-text devuelve el texto completo sin cortar a TEXTO_LIMITE; contenido vacío/nulo devuelve []"
  artifacts:
    - path: "backend/src/modules/historia-clinica/historia-clinica.contenido.helpers.ts"
      provides: "listarTratamientosDeContenido(contenido): string[] + private extraerNombresTratamiento"
      contains: "export function listarTratamientosDeContenido"
    - path: "backend/src/modules/turnos/turnos.service.ts"
      provides: "campo tratamientos en el mapper de /turnos/rango"
      contains: "listarTratamientosDeContenido"
    - path: "backend/src/modules/historia-clinica/historia-clinica.contenido.spec.ts"
      provides: "cobertura del nuevo helper + regresión de resumirTratamientosDeContenido"
      contains: "listarTratamientosDeContenido"
  key_links:
    - from: "backend/src/modules/turnos/turnos.service.ts"
      to: "listarTratamientosDeContenido"
      via: "import desde ../historia-clinica/historia-clinica.contenido.helpers"
      pattern: "listarTratamientosDeContenido\\(entradaHC"
---

<objective>
Exponer la lista completa de nombres de tratamiento del turno en el payload de `GET /turnos/rango`, sin tocar el string colapsado existente. Es el touch de backend que habilita TRAT-07 (la celda "Último tratamiento" del frontend debe mostrar TODOS los tratamientos, no "primero +N-1").

Purpose: El colapso "primero +N-1" se arma hoy en el backend (`formatearResumen`) y el frontend solo recibe el string ya truncado. Para que la planilla muestre todo, el backend debe entregar el array crudo.
Output: Nuevo helper puro `listarTratamientosDeContenido` + campo `tratamientos: string[]` en la respuesta de `/turnos/rango` + tests.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-CONTEXT.md
@.planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-PATTERNS.md

<interfaces>
<!-- Contratos existentes — usar directamente, sin exploración -->

Existente en historia-clinica.contenido.helpers.ts (NO modificar su salida):
  export function resumirTratamientosDeContenido(contenido: unknown): string | null
  function formatearResumen(nombres: string[]): string | null   // "uno" | "uno +N-1"
  const TEXTO_LIMITE  // corte del branch free-text (solo aplica al string colapsado)

Formas de `contenido` (JSONB) que el helper debe cubrir (3 prioridades):
  1. v1.9 zona-grouped:  { zonas: [{ tratamientos: [{ nombre: string }] }] }
  2. legacy flat:        { tratamientos: [{ nombre: string }] }
  3. free-text:          { texto: string }

Existente en turnos.service.ts (~578-598, builder de /turnos/rango):
  select entradaHC { tipoEntrada, contenido }   // contenido YA seleccionado
  return turnos.map((t) => ({ ...rest,
    ultimoTratamiento: resumirTratamientosDeContenido(entradaHC?.contenido ?? null),
    tipoEntradaHC: entradaHC?.tipoEntrada ?? null }))
  import ya presente (línea 27): resumirTratamientosDeContenido
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper listarTratamientosDeContenido + extractor compartido + tests</name>
  <files>backend/src/modules/historia-clinica/historia-clinica.contenido.helpers.ts, backend/src/modules/historia-clinica/historia-clinica.contenido.spec.ts</files>
  <read_first>
    - backend/src/modules/historia-clinica/historia-clinica.contenido.helpers.ts (líneas 93-154: resumirTratamientosDeContenido, formatearResumen, TEXTO_LIMITE — fuente de las 3 ramas)
    - backend/src/modules/historia-clinica/historia-clinica.contenido.spec.ts (aserciones fijas existentes tipo "Lipoaspiración +2" — NO tocar)
    - .planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-PATTERNS.md (sección helpers, opción 1: extraer "collect nombres[]")
  </read_first>
  <behavior>
    - listarTratamientosDeContenido con contenido zona-grouped ({zonas:[{tratamientos:[{nombre:"Lipoaspiración"},{nombre:"Bichectomía"},{nombre:"Otro"}]}]}) devuelve ["Lipoaspiración","Bichectomía","Otro"] (array COMPLETO, sin colapsar)
    - listarTratamientosDeContenido con contenido flat legacy ({tratamientos:[{nombre:"A"},{nombre:"B"}]}) devuelve ["A","B"]
    - listarTratamientosDeContenido con free-text ({texto: <string más largo que TEXTO_LIMITE>}) devuelve [textoCompleto] SIN cortar a TEXTO_LIMITE
    - listarTratamientosDeContenido con null / undefined / {} / {texto:""} / {tratamientos:[]} devuelve []
    - Regresión: resumirTratamientosDeContenido con el mismo contenido de 3 nombres sigue devolviendo exactamente "Lipoaspiración +2" (salida colapsada INTACTA)
  </behavior>
  <action>
    Extraer la lógica de "colectar nombres" de las prioridades 1 (zonas) y 2 (flat `tratamientos[]`) de `resumirTratamientosDeContenido` a un helper privado `extraerNombresTratamiento(contenido: unknown): string[]` (misma guarda `typeof contenido === 'object'`, mismo trim + filter de vacíos). Refactorizar `resumirTratamientosDeContenido` para llamar a `extraerNombresTratamiento` y luego colapsar con `formatearResumen` — su salida pública NO cambia (D-07). Crear y exportar `listarTratamientosDeContenido(contenido: unknown): string[]`: usa `extraerNombresTratamiento`; si devuelve nombres, retorna el array completo; si no hay nombres pero existe la rama free-text (`typeof c.texto === 'string'` y `.trim().length > 0`), retorna `[texto.trim()]` SIN aplicar `TEXTO_LIMITE` (decisión de Claude's Discretion: el detalle free-text es el texto completo); en cualquier otro caso retorna `[]`. NO modificar `formatearResumen` ni la firma/salida de `resumirTratamientosDeContenido`. En el spec, agregar bloques `describe`/`it` nuevos para `listarTratamientosDeContenido` (casos zona-grouped, flat, free-text sin corte, vacíos) y un `it` de regresión que verifique que `resumirTratamientosDeContenido` sigue produciendo "Lipoaspiración +2"; no editar las aserciones existentes.
  </action>
  <verify>
    <automated>cd backend && npx jest historia-clinica.contenido.spec --silent</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npx jest historia-clinica.contenido.spec` exits 0
    - historia-clinica.contenido.helpers.ts contiene `export function listarTratamientosDeContenido` y `function extraerNombresTratamiento`
    - grep confirma que `formatearResumen` no fue modificado en su cuerpo de colapso ("+" con `nombres.length - 1` intacto)
    - El spec incluye una aserción de regresión que exige salida "Lipoaspiración +2" de resumirTratamientosDeContenido
  </acceptance_criteria>
  <done>listarTratamientosDeContenido exportado y probado; resumirTratamientosDeContenido sin cambios de salida; todos los tests del spec en verde</done>
</task>

<task type="auto">
  <name>Task 2: Wire tratamientos: string[] en el mapper de /turnos/rango</name>
  <files>backend/src/modules/turnos/turnos.service.ts</files>
  <read_first>
    - backend/src/modules/turnos/turnos.service.ts (líneas 27 import, 578-598 builder de /turnos/rango)
    - backend/src/modules/historia-clinica/historia-clinica.contenido.helpers.ts (firma de listarTratamientosDeContenido creada en Task 1)
    - .planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-PATTERNS.md (sección turnos.service.ts, "Recommended new cell shape" del mapper)
  </read_first>
  <action>
    En `turnos.service.ts` agregar un import hermano de `listarTratamientosDeContenido` junto al import existente de `resumirTratamientosDeContenido` (línea ~27, mismo módulo `../historia-clinica/historia-clinica.contenido.helpers`). En el `.map()` del builder de `/turnos/rango` (~589-598), agregar en el object literal retornado un nuevo campo `tratamientos: listarTratamientosDeContenido(entradaHC?.contenido ?? null)`, MANTENIENDO sin cambios `ultimoTratamiento` y `tipoEntradaHC`. NO tocar el bloque `select` de `entradaHC` (578-583): `contenido` ya está seleccionado y es la única fuente. Usar el nombre de campo exacto `tratamientos` (elección de Claude's Discretion sobre D-07; el frontend en 64-03 tipa exactamente este nombre).
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - turnos.service.ts /turnos/rango mapper devuelve `tratamientos` derivado de `listarTratamientosDeContenido(entradaHC?.contenido ?? null)`
    - `ultimoTratamiento` sigue mapeado vía `resumirTratamientosDeContenido` (sin cambios)
    - El bloque `select` de entradaHC no fue modificado (sin nuevo campo Prisma, sin migración)
    - `cd backend && npx tsc --noEmit` sin errores nuevos
  </acceptance_criteria>
  <done>La respuesta de GET /turnos/rango incluye `tratamientos: string[]` junto al inalterado `ultimoTratamiento`; compila limpio</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /turnos/rango | Cliente autenticado consulta el rango; entrada = query params de rango, salida = filas de turno |
| entradaHC.contenido (JSONB) → mapper | Contenido HC (parcialmente texto ingresado por profesional) parseado en helper puro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Information Disclosure | GET /turnos/rango (nuevo campo `tratamientos`) | accept | El array deriva del MISMO `entradaHC.contenido` que ya alimenta `ultimoTratamiento` — no expone datos nuevos ni de otro tenant; el endpoint ya está detrás del JWT guard y del scoping por profesional existente (sin cambios de authz en esta fase) |
| T-64-02 | Tampering / Injection | extraerNombresTratamiento / listarTratamientosDeContenido | mitigate | Parsing puramente por `typeof` guards y `Array.isArray`; sin `eval`/`Function`; entradas no-objeto o malformadas retornan `[]`. Cualquier string se trata como texto plano (no se ejecuta) |
| T-64-03 | Denial of Service | free-text sin corte TEXTO_LIMITE en el array | accept | El texto completo ya se persiste en `contenido`; devolverlo entero es un solo string por fila, cota práctica dada por el límite de longitud de la HC. El colapso truncado (`ultimoTratamiento`) sigue disponible para consumidores sensibles a tamaño |
| T-64-SC | Tampering (supply chain) | instalaciones npm | accept | Esta fase NO instala paquetes nuevos (solo helper puro + wiring); sin superficie de supply chain |
</threat_model>

<verification>
- `cd backend && npx jest historia-clinica.contenido.spec` en verde (nuevo helper + regresión colapsado).
- `cd backend && npx tsc --noEmit` sin errores.
- Inspección: la respuesta de `/turnos/rango` incluye `tratamientos: string[]` y `ultimoTratamiento` sin cambios.
</verification>

<success_criteria>
- Habilita ROADMAP SC3 (backend): el frontend puede recibir TODOS los tratamientos del turno sin el colapso "primero +N-1".
- `ultimoTratamiento` y `formatearResumen` inalterados (cero rotura de consumidores).
- Sin cambio de schema Prisma ni migración.
</success_criteria>

<output>
Create `.planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-01-SUMMARY.md` when done
</output>
