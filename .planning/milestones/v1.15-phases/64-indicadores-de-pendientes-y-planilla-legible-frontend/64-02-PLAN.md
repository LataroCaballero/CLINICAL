---
phase: 64-indicadores-de-pendientes-y-planilla-legible-frontend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/crm/PatientCard.tsx
autonomous: true
requirements: [CONTACTO-03, CONTACTO-04]

must_haves:
  truths:
    - "D-02/D-04: La card de un paciente con `columnId === 'NUEVO_LEAD'` muestra el badge de pendiente con texto exacto 'Dar turno' (CONTACTO-03)"
    - "D-02/D-04: La card de un paciente con `columnId === 'TURNO_AGENDADO'` muestra el badge de pendiente con texto exacto 'Ser atendido' (CONTACTO-04)"
    - "D-01: El indicador nuevo usa el componente shadcn `Badge` (no un `<span>` a mano); los spans existentes de la card NO se refactorizan"
    - "D-03: El badge se inserta en la zona inferior de registro de contacto/pendientes (junto a Espera / Aut. pendiente / Última interacción)"
    - "D-08: NO se agrega badge de 'Cirugía cancelada, recontactar' (ya cubierto por Phase 63 vía ultimoContactoNota)"
  artifacts:
    - path: "frontend/src/components/crm/PatientCard.tsx"
      provides: "Badge de pendiente por etapa en la zona inferior"
      contains: "Dar turno"
  key_links:
    - from: "frontend/src/components/crm/PatientCard.tsx"
      to: "@/components/ui/badge"
      via: "import { Badge }"
      pattern: "import \\{ Badge \\}"
---

<objective>
Mostrar en la card del kanban un badge de "acción pendiente" según la etapa: "Dar turno" para leads nuevos, "Ser atendido" para consultas agendadas. La secretaria ve de un vistazo qué falta por paciente (CONTACTO-03 / CONTACTO-04).

Purpose: El embudo ya pobla `NUEVO_LEAD`/`TURNO_AGENDADO` automáticamente (Phase 63); esta card los vuelve accionables visualmente.
Output: Badge condicional por `columnId` en `PatientCard.tsx`, usando el componente shadcn `Badge`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-CONTEXT.md
@.planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-PATTERNS.md

<interfaces>
<!-- Contratos existentes — usar directamente -->

PatientCard.tsx: `columnId: string` YA es prop (interface línea ~35). La card ya ramifica badges por
columnId (ej. `columnId === "PROCEDIMIENTO_REALIZADO"`, `columnId === "CONFIRMADO"`) en la zona
inferior (~120-147). Valores de columna relevantes: "NUEVO_LEAD", "TURNO_AGENDADO".

Badge (frontend/src/components/ui/badge.tsx): CVA, variants default|secondary|destructive|outline.
Convención del repo (ProximosTurnos.tsx): `variant="outline"` + clases Tailwind de color
(`className="bg-X-50 text-X-700 border-X-200"`), NO variants sólidos.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge de pendiente por etapa en PatientCard</name>
  <files>frontend/src/components/crm/PatientCard.tsx</files>
  <read_first>
    - frontend/src/components/crm/PatientCard.tsx (interface con `columnId` ~línea 35; zona inferior de badges ~104-156, ver bloques `columnId === "PROCEDIMIENTO_REALIZADO"` y `columnId === "CONFIRMADO"`)
    - frontend/src/components/ui/badge.tsx (componente Badge + variants)
    - frontend/src/app/dashboard/reportes/components/ProximosTurnos.tsx (líneas 4, 19-38 — idiom `variant="outline"` + clases de color para badges por estado)
    - .planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-PATTERNS.md (Analog A + Analog B de PatientCard)
  </read_first>
  <action>
    Importar `Badge` desde `@/components/ui/badge`. En la zona inferior de badges/pendientes de `PatientCard.tsx` (junto a los bloques existentes de Espera / Aut. pendiente / `columnId === "CONFIRMADO"`, respetando D-03), agregar dos renders condicionales por `columnId`: cuando `columnId === "NUEVO_LEAD"` renderizar `<Badge>` con el texto exacto `Dar turno` (CONTACTO-03); cuando `columnId === "TURNO_AGENDADO"` renderizar `<Badge>` con el texto exacto `Ser atendido` (CONTACTO-04). Usar `variant="outline"` + clases Tailwind de color que comuniquen "acción pendiente" (ámbar/naranja, coherente con los acentos ámbar de los spans Espera existentes en la misma card) — elección de estilo dentro de Claude's Discretion (D-01/D-04). NO refactorizar a `Badge` los spans existentes (Espera, Aut. pendiente): D-01 aplica solo al indicador nuevo. NO agregar ningún badge de recontacto/"Cirugía cancelada" (D-08: ya lo cubre `ultimoContactoNota` de Phase 63). No hace falta prop nueva: `columnId` ya está disponible.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint -- --file src/components/crm/PatientCard.tsx 2>/dev/null || cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - PatientCard.tsx contiene `import { Badge }` desde `@/components/ui/badge`
    - Existe un branch `columnId === "NUEVO_LEAD"` que renderiza un `<Badge>` con la cadena exacta `Dar turno`
    - Existe un branch `columnId === "TURNO_AGENDADO"` que renderiza un `<Badge>` con la cadena exacta `Ser atendido`
    - Los spans existentes (Espera / Aut. pendiente) permanecen como `<span>` (no convertidos a Badge)
    - No existe ningún texto "recontactar"/"Cirugía cancelada" nuevo en la card (D-08)
    - `cd frontend && npx tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>La card muestra "Dar turno" en NUEVO_LEAD y "Ser atendido" en TURNO_AGENDADO vía el componente Badge, en la zona inferior; compila y lint limpio</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos de /pacientes/kanban → DOM (React) | La card renderiza campos servidos por el backend; el badge nuevo usa texto estático |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-04 | Injection / XSS | PatientCard badge nuevo | accept | El texto del badge ("Dar turno"/"Ser atendido") es una cadena literal estática, no input de usuario; React escapa por defecto el contenido JSX; sin `dangerouslySetInnerHTML` |
| T-64-05 | Information Disclosure | render condicional por columnId | accept | `columnId` deriva de la columna del kanban ya autorizada; no expone datos adicionales — solo muestra/oculta una etiqueta estática |
| T-64-SC | Tampering (supply chain) | instalaciones npm | accept | Sin instalación de paquetes: `Badge` ya existe en el repo |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` sin errores.
- Inspección visual (UAT, cubierta por el checkpoint de fase): lead nuevo muestra "Dar turno"; consulta agendada muestra "Ser atendido".
</verification>

<success_criteria>
- ROADMAP SC1: card en "Nuevo Lead" muestra el badge "Dar turno" en la zona inferior.
- ROADMAP SC2: card en "Consulta Agendada" (`TURNO_AGENDADO`) muestra el badge "Ser atendido".
</success_criteria>

<output>
Create `.planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-02-SUMMARY.md` when done
</output>
