---
phase: 64-indicadores-de-pendientes-y-planilla-legible-frontend
plan: 03
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - frontend/src/hooks/useTurnosRangos.ts
  - frontend/src/app/dashboard/pacientes/components/TratamientosTab.tsx
autonomous: true
requirements: [TRAT-07]

must_haves:
  truths:
    - "D-05: La celda 'Último tratamiento' muestra TODOS los nombres del turno coma-separados en una línea, truncados por CSS (`truncate max-w-[200px]`), no 'primero +N-1'"
    - "D-06: El `title` nativo se reemplaza por el componente Radix `Tooltip`; al hover revela la lista completa de tratamientos"
    - "D-07: El frontend consume el nuevo campo `tratamientos: string[]` de TurnoRango; `ultimoTratamiento` se conserva (lo usa el filtro fuente-B en la misma tabla)"
    - "Estado vacío (sin tratamientos): la celda muestra el guion '—' (Claude's Discretion)"
  artifacts:
    - path: "frontend/src/hooks/useTurnosRangos.ts"
      provides: "campo tratamientos en el tipo TurnoRango"
      contains: "tratamientos"
    - path: "frontend/src/app/dashboard/pacientes/components/TratamientosTab.tsx"
      provides: "celda con lista completa truncada + Radix Tooltip"
      contains: "TooltipContent"
  key_links:
    - from: "frontend/src/app/dashboard/pacientes/components/TratamientosTab.tsx"
      to: "turno.tratamientos"
      via: "join(', ') en trigger y content del Tooltip"
      pattern: "turno\\.tratamientos"
    - from: "frontend/src/app/dashboard/pacientes/components/TratamientosTab.tsx"
      to: "@/components/ui/tooltip"
      via: "import Tooltip/TooltipTrigger/TooltipContent"
      pattern: "TooltipContent"
---

<objective>
Hacer legible la columna "Último tratamiento" de la planilla: mostrar TODOS los tratamientos del turno (no el colapso "primero +N-1"), truncados en la celda por CSS, y revelar el texto completo con un tooltip Radix al hover (TRAT-07, Success Criteria #3 y #4).

Purpose: Hoy la celda muestra el string colapsado con `title` nativo; la secretaria pierde información silenciosamente. Con el nuevo campo `tratamientos: string[]` (64-01) el frontend arma el display.
Output: campo tipado en `TurnoRango` + celda reescrita con `join(", ")` truncado + Radix Tooltip en `TratamientosTab.tsx`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-CONTEXT.md
@.planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-PATTERNS.md
@.planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-01-SUMMARY.md

<interfaces>
<!-- Contrato producido por 64-01 (backend) — consumir directamente -->

GET /turnos/rango ahora devuelve por fila (además de lo existente):
  tratamientos: string[]   // lista completa de nombres sin colapsar; [] si no hay
  ultimoTratamiento?: string | null   // SIN CAMBIOS (colapsado) — NO renombrar/remover

TurnoRango actual (useTurnosRangos.ts ~4-14): { id, inicio, fin, estado, observaciones?,
  paciente{id,nombreCompleto}, tipoTurno{...}, ultimoTratamiento?: string|null, tipoEntradaHC?: string|null }

Tooltip (frontend/src/components/ui/tooltip.tsx): el componente `Tooltip` YA se autoenvuelve en
`TooltipProvider` internamente — NO hace falta un TooltipProvider externo. Forma mínima:
  <Tooltip><TooltipTrigger asChild>{trigger}</TooltipTrigger><TooltipContent>{full}</TooltipContent></Tooltip>

CUIDADO — TratamientosTab.tsx línea ~65 filtra: `isFuenteB(t) && t.ultimoTratamiento != null`.
NO remover ni renombrar `ultimoTratamiento`; solo la CELDA de display cambia a `tratamientos`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipar tratamientos: string[] en TurnoRango</name>
  <files>frontend/src/hooks/useTurnosRangos.ts</files>
  <read_first>
    - frontend/src/hooks/useTurnosRangos.ts (tipo TurnoRango, líneas 4-14)
    - frontend/src/hooks/useCRMKanban.ts (líneas 36-64 — convención de comentario `// Phase NN (REQ) — ...` al sumar campos)
    - .planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-01-SUMMARY.md (nombre exacto del campo backend: `tratamientos`)
  </read_first>
  <action>
    Agregar al tipo `TurnoRango` el campo `tratamientos?: string[]` (mismo nombre que expone el backend en 64-01), junto a `ultimoTratamiento`. Preceder con un comentario siguiendo la convención del repo: `// Phase 64 (TRAT-07) — lista completa de nombres sin colapsar; ultimoTratamiento se mantiene para compatibilidad`. NO remover ni renombrar `ultimoTratamiento` (lo usa el filtro fuente-B de TratamientosTab).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - useTurnosRangos.ts `TurnoRango` incluye `tratamientos?: string[]`
    - `ultimoTratamiento?: string | null` sigue presente sin cambios
    - `cd frontend && npx tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>El tipo TurnoRango expone `tratamientos: string[]`; compila limpio</done>
</task>

<task type="auto">
  <name>Task 2: Celda "Último tratamiento" con lista completa truncada + Radix Tooltip</name>
  <files>frontend/src/app/dashboard/pacientes/components/TratamientosTab.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/pacientes/components/TratamientosTab.tsx (celda a reemplazar ~272-292; header "Último tratamiento" ~222-224; filtro fuente-B con ultimoTratamiento ~65)
    - frontend/src/components/ui/tooltip.tsx (Tooltip autoprovee TooltipProvider ~21-29)
    - frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx (líneas 24-26, 311-328 — forma mínima Tooltip/TooltipTrigger asChild/TooltipContent sin provider externo)
    - .planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-PATTERNS.md ("Recommended new cell shape")
  </read_first>
  <action>
    Importar `Tooltip`, `TooltipTrigger`, `TooltipContent` desde `@/components/ui/tooltip` (forma mínima de `PacienteDetails.tsx`; NO agregar `TooltipProvider` externo — el componente ya lo autoprovee). Reemplazar la celda actual de "Último tratamiento" (~272-292): condicionar por `turno.tratamientos && turno.tratamientos.length > 0`. Cuando hay tratamientos, envolver el `<button>` existente (que conserva su `onClick` de abrir el drawer en vista "historia" y sus clases `truncate max-w-[200px] block`) en `<Tooltip><TooltipTrigger asChild>...</TooltipTrigger>`, mostrando `turno.tratamientos.join(", ")` como contenido del botón, y `<TooltipContent>` con el mismo `turno.tratamientos.join(", ")` (lista completa). Eliminar el atributo `title` nativo (lo reemplaza el Tooltip, D-06). Estado vacío (`tratamientos` ausente o `[]`): mantener el `<span>` con guion "—" (Claude's Discretion). Para un solo tratamiento, el tooltip muestra el mismo nombre (comportamiento aceptado, sin lógica especial). NO tocar el filtro fuente-B de la línea ~65 ni el campo `ultimoTratamiento` en el resto del archivo.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - TratamientosTab.tsx importa `Tooltip`, `TooltipTrigger`, `TooltipContent` desde `@/components/ui/tooltip`
    - La celda renderiza `turno.tratamientos.join(", ")` dentro de un `<TooltipTrigger asChild>` con clases `truncate max-w-[200px]`
    - `<TooltipContent>` contiene la lista completa `turno.tratamientos.join(", ")`
    - Ya no existe `title={turno.ultimoTratamiento}` en esa celda (reemplazado por Tooltip)
    - El estado vacío sigue mostrando "—"
    - El filtro `isFuenteB(t) && t.ultimoTratamiento != null` (~línea 65) permanece intacto
    - `cd frontend && npx tsc --noEmit` sin errores
  </acceptance_criteria>
  <done>La celda muestra todos los tratamientos coma-separados truncados por CSS con tooltip Radix que revela el texto completo; compila limpio</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tratamientos (nombres de HC) → DOM (React) | Los nombres pueden incluir texto ingresado por el profesional (free-text branch) y se renderizan en celda + tooltip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-06 | Injection / XSS | render de `turno.tratamientos.join(", ")` en celda y TooltipContent | mitigate | Se renderiza como contenido de texto JSX (`{...}`), que React escapa por defecto; sin `dangerouslySetInnerHTML`; el Tooltip de Radix renderiza children como texto — nombres de tratamiento tratados como texto plano |
| T-64-07 | Information Disclosure | tooltip revela la lista completa | accept | Es la misma data de la fila (turno del paciente ya autorizado en la tabla); no cruza tenant ni agrega datos nuevos respecto de lo ya servido por /turnos/rango |
| T-64-SC | Tampering (supply chain) | instalaciones npm | accept | Sin paquetes nuevos: `Tooltip` (Radix) ya está en el repo y en uso en ~10 archivos |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` sin errores.
- Inspección/UAT (cubierta por checkpoint de fase): fila con múltiples tratamientos muestra la lista coma-separada truncada; hover revela el texto completo; fila sin tratamientos muestra "—".
</verification>

<success_criteria>
- ROADMAP SC3: la columna "Último tratamiento" muestra todos los tratamientos truncados en la celda (no "primero +N-1").
- ROADMAP SC4: al hover, el tooltip revela el texto completo de todos los tratamientos.
</success_criteria>

<output>
Create `.planning/phases/64-indicadores-de-pendientes-y-planilla-legible-frontend/64-03-SUMMARY.md` when done
</output>
