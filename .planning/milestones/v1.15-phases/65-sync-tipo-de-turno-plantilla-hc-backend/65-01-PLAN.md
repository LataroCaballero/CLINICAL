---
phase: 65-sync-tipo-de-turno-plantilla-hc-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts
  - backend/src/modules/historia-clinica/historia-clinica.service.ts
  - backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts
autonomous: true
requirements: [HCSYNC-01, HCSYNC-02, HCSYNC-03]

must_haves:
  truths:
    - "D-01/D-07 (HCSYNC-01): al guardar una entrada con plantilla `primera_vez` sobre un turno de rango menor (Control/null/no-mapeado), el `Turno.tipoTurnoId` queda en 'Consulta'; si ya es Consulta o superior (Tratamiento/Pre-Quirúrgico), no cambia"
    - "D-01/D-07 (HCSYNC-02): al guardar plantilla `tratamiento_en_consultorio` sobre un turno 'Consulta' (o 'Control'/null), el `Turno.tipoTurnoId` cambia a 'Tratamiento'; sobre 'Pre-Quirúrgico' no degrada"
    - "D-01/D-07 (HCSYNC-03): al guardar plantilla `pre_quirurgico` sobre un turno no-cirugía, el `Turno.tipoTurnoId` queda en 'Pre-Quirúrgico' (tope de la escalera); si ya es Pre-Quirúrgico es no-op"
    - "D-02: si el turno actual es de cirugía (`esCirugia===true`), su tipo NUNCA se toca, sea cual sea la plantilla (sentinel protegido de máxima prioridad)"
    - "D-01 no-downgrade: la escalera Consulta(1)<Tratamiento(2)<Pre-Quirúrgico(3) solo aplica el destino si su rango es mayor que el actual; nunca baja un tipo ya avanzado; order-independent (D-04)"
    - "D-03: cualquier tipo actual no mapeado (Control/null/otro) cuenta rango 0 y es sobrescribible por cualquiera de las 3 plantillas"
    - "D-09 guard (SC#4): sin `dto.turnoId` no se ejecuta ningún query ni update de turno (entrada retroactiva desde PatientDrawer no toca ningún tipo de turno)"
    - "D-05: el update escribe `Turno.tipoTurnoId` (FK resuelta por `nombre`) y sincroniza el flag denormalizado `Turno.esCirugia` con el `esCirugia` del tipo destino"
    - "D-08: los `TipoTurno` destino se resuelven por su `nombre` (`@unique`) — literales canónicos `'Consulta'`/`'Tratamiento'`/`'Pre-Quirúrgico'` (seed-tipos-turno.ts) — vía `tx.tipoTurno.findUnique({ where: { nombre } })`; la comparación del tipo actual del turno también es por `nombre` (`turnoCtx.tipoTurno.nombre`)"
    - "D-06: el sync NO re-dispara efectos sobre `Paciente.flujo` ni `etapaCRM`; el bloque `resolverNuevoFlujo`/`tx.paciente.update` existente (crearEntrada :270-293) queda intacto e independiente del write de turno"
    - "Idempotencia + defensa: se saltea el `tx.turno.update` si el `tipoTurnoId` destino ya coincide con el actual; si el TipoTurno destino no existe por `nombre`, se saltea el sync silenciosamente sin romper el guardado de la HC"
  artifacts:
    - path: "backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts"
      provides: "función pura resolverTipoTurnoSync (escalera de prioridad D-01/D-02/D-03/D-07)"
      contains: "export function resolverTipoTurnoSync"
    - path: "backend/src/modules/historia-clinica/historia-clinica.service.ts"
      provides: "write guardado de turno dentro del $transaction de crearEntrada"
      contains: "tx.turno.update"
    - path: "backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts"
      provides: "tabla de casos de resolverTipoTurnoSync + tests de wiring del sync de turno"
      contains: "resolverTipoTurnoSync"
  key_links:
    - from: "historia-clinica.service.ts::crearEntrada"
      to: "resolverTipoTurnoSync"
      via: "named import desde ./historia-clinica.flujo.helpers"
      pattern: "resolverTipoTurnoSync\\("
    - from: "historia-clinica.service.ts::crearEntrada"
      to: "prisma.tipoTurno (lookup por nombre @unique)"
      via: "tx.tipoTurno.findUnique({ where: { nombre } })"
      pattern: "tipoTurno\\.findUnique"
    - from: "historia-clinica.service.ts::crearEntrada"
      to: "Turno.tipoTurnoId / Turno.esCirugia"
      via: "tx.turno.update guardado por dto.turnoId (D-09) y por resultado no-null del resolver"
      pattern: "tx\\.turno\\.update"
---

<objective>
Sincronizar automáticamente el `tipoTurno` de un turno con la plantilla de HC que se carga sobre él, dentro de `crearEntrada`, siguiendo una escalera de prioridad pura (Consulta<Tratamiento<Pre-Quirúrgico) que nunca degrada, protege turnos de cirugía y no toca nada si no hay turno asociado.

Purpose: Elimina la corrección manual del tipo de turno en la agenda (core value: automatizar para no-técnicos). Cubre HCSYNC-01/02/03.
Output:
- Nuevo helper puro `resolverTipoTurnoSync` + suite de casos.
- Write guardado `tx.turno.update` dentro de la transacción existente de `crearEntrada`.
- Tests de wiring (mock PrismaService) verificando disparo/no-op/guard/protección.

Backend puro: sin cambios de schema, migraciones, DTO, controller ni frontend. Los 3 archivos ya existen (tocados en Phase 63); se extienden in-place.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-sync-tipo-de-turno-plantilla-hc-backend/65-CONTEXT.md
@.planning/phases/65-sync-tipo-de-turno-plantilla-hc-backend/65-PATTERNS.md

<interfaces>
<!-- Contratos que el executor usa directamente — no explorar el codebase. -->

Helper analog existente — backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts:
- `resolverNuevoFlujo(tipoEntrada, flujoActual, esCirugia): 'CIRUGIA'|'TRATAMIENTO'|null` — patrón guard-first (chequea el sentinel `esCirugia` primero, early-return null, luego discrimina). MIRROR para el nuevo helper.
- `resolverTipoEntrada(tipo, tipoEntradaDto): string|undefined` — patrón "map dto.tipo string literal → nombre canónico" (if/return plano).

Firma objetivo del nuevo helper (Claude's Discretion, per 65-PATTERNS.md):
  `resolverTipoTurnoSync(plantilla: string|undefined, currentTipoNombre: string|null|undefined, currentEsCirugia: boolean): string | null`

Mapa de plantilla→destino (D-07) y rangos (D-01/D-03):
- `primera_vez` → 'Consulta' (rango 1)
- `tratamiento_en_consultorio` → 'Tratamiento' (rango 2)
- `pre_quirurgico` → 'Pre-Quirúrgico' (rango 3)
- `control` / `practica` / `libre` / undefined → null (sin sync)
- rango de currentTipoNombre: 'Consulta'=1, 'Tratamiento'=2, 'Pre-Quirúrgico'=3, cualquier otro/null='Control'/no-mapeado = 0
- Regla: si `currentEsCirugia===true` → null (D-02, chequeado PRIMERO). Si plantilla no mapea → null. Devolver el nombre destino solo si `rankDestino > rankActual`, si no null (cubre no-op cuando rangos iguales).

Nombres canónicos de TipoTurno (backend/src/prisma/seed-tipos-turno.ts:11-37, @unique): 'Consulta', 'Control', 'Pre-Quirúrgico', 'Tratamiento', 'Cirugía'.

Punto de enganche — crearEntrada (historia-clinica.service.ts):
- Pre-fetch turno existente (:227-233): `dto.turnoId ? prisma.turno.findUnique({ where:{id:dto.turnoId}, select:{ esCirugia:true } }) : null` → const `turnoCtx`.
- Transacción existente `this.prisma.$transaction(async (tx) => {...})` abre en :244, `return entrada` en :370.
- Bloque flujo del paciente (`resolverNuevoFlujo` + `tx.paciente.update`) en :270-293 — NO tocar, queda independiente (D-06).
- Import block a extender (:1-16): named import desde `./historia-clinica.flujo.helpers`.

Shape del update scoped-by-id (single-tenant, sin filtro de tenant): `tx.turno.update({ where:{ id: dto.turnoId }, data:{ tipoTurnoId, esCirugia } })`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper puro resolverTipoTurnoSync + tabla de casos (TDD)</name>
  <files>backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts, backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts</files>
  <read_first>
    - backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts (patrón de helper puro guard-first, JSDoc style de resolverNuevoFlujo :6-17)
    - backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts (bloque describe('resolverNuevoFlujo',...) :10-71 — mirror exacto de estilo: un it() por regla, nombre en español input→output, expect(...).toBe/.toBeNull)
    - backend/src/prisma/seed-tipos-turno.ts (:11-37 — nombres canónicos fuente de verdad)
    - .planning/phases/65-sync-tipo-de-turno-plantilla-hc-backend/65-CONTEXT.md (tabla "Resultado por plantilla frente al tipo actual" :26-29 = spec de casos)
  </read_first>
  <behavior>
    RED primero: escribir el bloque `describe('resolverTipoTurnoSync', ...)` con estos casos antes de implementar:
    - `resolverTipoTurnoSync('primera_vez', 'Control', false)` → 'Consulta' (HCSYNC-01, D-03 rango 0)
    - `resolverTipoTurnoSync('primera_vez', null, false)` → 'Consulta'
    - `resolverTipoTurnoSync('primera_vez', 'Consulta', false)` → null (no-op, ya está)
    - `resolverTipoTurnoSync('primera_vez', 'Tratamiento', false)` → null (no degrada, D-01)
    - `resolverTipoTurnoSync('primera_vez', 'Pre-Quirúrgico', false)` → null
    - `resolverTipoTurnoSync('tratamiento_en_consultorio', 'Consulta', false)` → 'Tratamiento' (HCSYNC-02)
    - `resolverTipoTurnoSync('tratamiento_en_consultorio', 'Control', false)` → 'Tratamiento' (extra intencional D-01/specifics)
    - `resolverTipoTurnoSync('tratamiento_en_consultorio', null, false)` → 'Tratamiento'
    - `resolverTipoTurnoSync('tratamiento_en_consultorio', 'Tratamiento', false)` → null (no-op)
    - `resolverTipoTurnoSync('tratamiento_en_consultorio', 'Pre-Quirúrgico', false)` → null (no degrada)
    - `resolverTipoTurnoSync('pre_quirurgico', 'Consulta', false)` → 'Pre-Quirúrgico' (HCSYNC-03)
    - `resolverTipoTurnoSync('pre_quirurgico', 'Control', false)` → 'Pre-Quirúrgico'
    - `resolverTipoTurnoSync('pre_quirurgico', 'Pre-Quirúrgico', false)` → null (no-op tope)
    - Cirugía protegido (D-02): `resolverTipoTurnoSync('pre_quirurgico', 'Consulta', true)` → null; `resolverTipoTurnoSync('tratamiento_en_consultorio', 'Control', true)` → null; `resolverTipoTurnoSync('primera_vez', null, true)` → null
    - Plantillas sin sync (D-07): `resolverTipoTurnoSync('control', 'Consulta', false)` → null; `resolverTipoTurnoSync('practica', 'Consulta', false)` → null; `resolverTipoTurnoSync('libre', 'Control', false)` → null; `resolverTipoTurnoSync(undefined, 'Consulta', false)` → null
  </behavior>
  <action>
    Agregar `resolverTipoTurnoSync(plantilla, currentTipoNombre, currentEsCirugia): string | null` a historia-clinica.flujo.helpers.ts, con JSDoc describiendo la escalera D-01/D-02/D-03/D-07 (mismo estilo que el doc-comment sobre resolverNuevoFlujo). Estructura guard-first idéntica a resolverNuevoFlujo: chequear `currentEsCirugia` PRIMERO (return null — sentinel protegido D-02); mapear `plantilla` (dto.tipo) a nombre destino y su rango (`primera_vez`→'Consulta'/1, `tratamiento_en_consultorio`→'Tratamiento'/2, `pre_quirurgico`→'Pre-Quirúrgico'/3, resto→null); calcular rango de `currentTipoNombre` ('Consulta'=1,'Tratamiento'=2,'Pre-Quirúrgico'=3, resto/null=0); devolver el nombre destino solo si `rankDestino > rankActual`, si no null. Usar if/return plano o un objeto de rangos const — a discreción, respetando house style. Sin imports de Prisma/Nest. Escribir primero el describe en el spec (RED, debe fallar), luego implementar (GREEN). Extender el named import del spec (:1-8) para incluir `resolverTipoTurnoSync`.
  </action>
  <verify>
    <automated>cd backend && npx jest historia-clinica.flujo.spec.ts -t "resolverTipoTurnoSync" -x</automated>
  </verify>
  <acceptance_criteria>
    - historia-clinica.flujo.helpers.ts exporta `resolverTipoTurnoSync` sin imports de @prisma/client ni @nestjs/*.
    - Todos los casos del bloque `<behavior>` pasan; en particular `resolverTipoTurnoSync('tratamiento_en_consultorio','Consulta',false)` === 'Tratamiento', `resolverTipoTurnoSync('primera_vez','Tratamiento',false)` === null, y todo caso con `currentEsCirugia===true` === null.
    - El comando de test sale con código 0.
  </acceptance_criteria>
  <done>Helper puro implementado y su tabla de casos verde; escalera + protección cirugía + no-downgrade + plantillas sin-sync cubiertas.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring del sync en crearEntrada (pre-fetch + lookup + tx.turno.update guardado)</name>
  <files>backend/src/modules/historia-clinica/historia-clinica.service.ts</files>
  <read_first>
    - backend/src/modules/historia-clinica/historia-clinica.service.ts (:1-16 import block; :227-233 pre-fetch turnoCtx; :244-293 apertura del $transaction y bloque paciente.update de flujo; :352-371 cierre de tx / return entrada)
    - backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts (firma final de resolverTipoTurnoSync tras Task 1)
    - .planning/phases/65-sync-tipo-de-turno-plantilla-hc-backend/65-PATTERNS.md (§ historia-clinica.service.ts — pre-fetch extension :104, tx.turno.update shape :142-151, lookup por nombre :133-140, anti-patrón D-06 :153-167)
  </read_first>
  <action>
    (1) Extender el named import de `./historia-clinica.flujo.helpers` (:5-8) para incluir `resolverTipoTurnoSync`.
    (2) Extender el pre-fetch `turnoCtx` (:227-233) para seleccionar además `tipoTurnoId: true` y `tipoTurno: { select: { nombre: true } }` (evita un segundo round-trip; mismo comentario "pgBouncer pattern"). Mantener el guard ternario `dto.turnoId ? ... : null`.
    (3) Dentro del `$transaction` (tras el bloque paciente.update de flujo, :293, antes del bloque pre_quirurgico :295), agregar el sync de turno GUARDADO por `dto.turnoId` presente (D-09) — sin turnoId, ningún query/update. Resolver `const targetNombre = resolverTipoTurnoSync(dto.tipo, turnoCtx?.tipoTurno?.nombre, turnoCtx?.esCirugia ?? false)`. Si `targetNombre` es no-null: buscar el destino `const destino = await tx.tipoTurno.findUnique({ where: { nombre: targetNombre }, select: { id: true, esCirugia: true } })`. Aplicar `tx.turno.update({ where: { id: dto.turnoId }, data: { tipoTurnoId: destino.id, esCirugia: destino.esCirugia } })` SOLO si `destino` existe (defensa: si no existe, skip silencioso, no throw — no romper el guardado de HC) Y `destino.id !== turnoCtx?.tipoTurnoId` (idempotencia: no escribir si ya coincide, D-05/Discretion). Usar `tx.turno.update` (no `this.prisma`) para atomicidad. NO escribir `paciente.flujo`/`etapaCRM` desde este bloque (D-06); el bloque :270-293 queda intacto.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json && npm run lint -- src/modules/historia-clinica/historia-clinica.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `historia-clinica.service.ts` importa `resolverTipoTurnoSync` desde `./historia-clinica.flujo.helpers`.
    - El pre-fetch `turnoCtx` incluye `tipoTurnoId` y `tipoTurno.select.nombre`; sigue guardado por `dto.turnoId`.
    - Existe una única llamada `tx.turno.update` dentro del `$transaction`, scoped `{ id: dto.turnoId }`, escribiendo solo `tipoTurnoId` y `esCirugia`, condicionada a: `dto.turnoId` presente, `resolverTipoTurnoSync(...)` no-null, `destino` encontrado, y `destino.id !== turnoCtx.tipoTurnoId`.
    - grep confirma que NO se agregó ningún `paciente.update` / `etapaCRM` / `flujo` nuevo en el bloque de sync (D-06): `grep -n "turno.update" historia-clinica.service.ts` devuelve la nueva línea y `grep -c "resolverTipoTurnoSync" historia-clinica.service.ts` >= 1.
    - `tsc --noEmit` y lint salen con código 0.
  </acceptance_criteria>
  <done>crearEntrada ajusta atómicamente el tipoTurno del turno según la plantilla, bajo guard de turnoId, idempotente y defensivo, sin acoplar efectos de paciente.</done>
</task>

<task type="auto">
  <name>Task 3: Tests de wiring del sync de turno (mock PrismaService)</name>
  <files>backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts</files>
  <read_first>
    - backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts (bloque describe('HistoriaClinicaService.crearEntrada — wiring ...',...) :99-230 — mock PrismaService, $transaction que corre el callback contra el mock :198-200, aserciones sobre call args)
    - backend/src/modules/historia-clinica/historia-clinica.service.ts (forma final del sync tras Task 2)
    - .planning/phases/65-sync-tipo-de-turno-plantilla-hc-backend/65-PATTERNS.md (§ spec — casos de wiring a cubrir :210)
  </read_first>
  <action>
    Extender el mock `mockPrisma` (:104-131) con `turno: { findUnique: jest.fn(), update: jest.fn() }` y `tipoTurno: { findUnique: jest.fn() }`. Agregar tests de wiring del sync (dentro del describe de wiring existente o uno nuevo `describe('crearEntrada — sync tipoTurno (HCSYNC-01/02/03, D-09)')`). Configurar `mockPrisma.turno.findUnique` para devolver el `turnoCtx` con `esCirugia`/`tipoTurnoId`/`tipoTurno.nombre` según el caso, y `mockPrisma.tipoTurno.findUnique` para devolver `{ id, esCirugia:false }` del destino. Cubrir: (a) `turnoId` presente + `tratamiento_en_consultorio` sobre 'Consulta' → `mockPrisma.turno.update` llamado con `data` conteniendo `tipoTurnoId` del destino 'Tratamiento'; (b) no-op: destino.id === tipoTurnoId actual (o rango no mayor) → `turno.update` NO llamado; (c) guard D-09: `dto.turnoId` ausente → ni `turno.findUnique` ni `turno.update` llamados; (d) cirugía protegida: `turnoCtx.esCirugia===true` con cualquier plantilla → `turno.update` NO llamado. Usar el patrón existente `mockPrisma.$transaction.mockImplementation((cb)=>cb(mockPrisma))` y `expect(...).toHaveBeenCalledWith(expect.objectContaining(...))` / `not.toHaveBeenCalled()`.
  </action>
  <verify>
    <automated>cd backend && npx jest historia-clinica.flujo.spec.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - El spec incluye los 4 casos de wiring (a)(b)(c)(d) descritos, cada uno como `it(...)` con nombre en español.
    - Caso (a): asserta `mockPrisma.turno.update` llamado con `data` que contiene el `tipoTurnoId` del destino 'Tratamiento'.
    - Casos (b),(c),(d): assertan `mockPrisma.turno.update` NO llamado; el caso (c) además asserta `mockPrisma.turno.findUnique` NO llamado (guard D-09).
    - `npx jest historia-clinica.flujo.spec.ts` completo sale con código 0 (incluye la tabla de Task 1 y los wiring de Phase 63 preexistentes sin regresión).
  </acceptance_criteria>
  <done>Wiring del sync verificado end-to-end vía mock: disparo, no-op, guard de turnoId y protección de cirugía cubiertos; suite completa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente (LiveTurno) → crearEntrada | `dto.turnoId` y `dto.tipo` llegan del cliente autenticado (profesional/secretaria) y cruzan al service |
| crearEntrada → Turno row | write a `Turno.tipoTurnoId`/`esCirugia` scoped solo por `{ id: dto.turnoId }` (single-tenant, sin filtro de tenant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering / Elevation (IDOR) | `tx.turno.update({ where:{ id: dto.turnoId } })` en crearEntrada | accept | Modelo single-tenant sin columna de tenant: no existe frontera cross-tenant que violar; el scoping es por `profesionalId` a nivel de aplicación y el pre-fetch/estampado de `dto.turnoId` (`OrdenConsumo.turnoId`, `entradaHCId`) ya opera sobre el mismo id sin filtro adicional (postura de autz preexistente y consistente con turnos.service). El write se limita a 2 columnas no destructivas dentro del conjunto mapeado; la escalera (D-01 no-downgrade + D-02 cirugía protegida) impide sacar de agenda un turno quirúrgico o degradar un tipo avanzado. Sin escalada de privilegio ni exposición de PII. |
| T-65-02 | Tampering / Injection | lookup `tipoTurno.findUnique({ where:{ nombre } })` | mitigate | El `nombre` destino NUNCA proviene directo de `dto`: se deriva server-side de un mapa fijo (`primera_vez`/`tratamiento_en_consultorio`/`pre_quirurgico` → 3 literales canónicos). El cliente no puede inyectar un `nombre` arbitrario; plantillas fuera del mapa devuelven null y no disparan lookup. `nombre @unique` + `findUnique` = match exacto, sin ambigüedad. |
| T-65-03 | Denial of Service / Integrity | destino TipoTurno inexistente en el entorno | mitigate | Null-check defensivo: si `findUnique` no encuentra el destino, se saltea el sync silenciosamente (no throw), preservando el guardado de la HC. El guard `dto.turnoId` (D-09) y la idempotencia (`destino.id !== tipoTurnoId`) evitan queries/writes innecesarios. |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | Esta fase no instala paquetes nuevos (extiende 3 archivos existentes con la stack presente); no aplica gate de legitimidad de paquetes ni slopcheck de instalación. |
</threat_model>

<verification>
- `cd backend && npx jest historia-clinica.flujo.spec.ts` → toda la suite verde (tabla resolverTipoTurnoSync + wiring nuevo + wiring Phase 63 sin regresión).
- `cd backend && npx tsc --noEmit` → sin errores de tipos.
- `cd backend && npm run lint` sobre los 3 archivos tocados → sin nuevos errores.
- Revisión manual: el helper no importa Prisma/Nest; el `tx.turno.update` es único, guardado por `dto.turnoId` + resolver no-null + destino encontrado + idempotencia; el bloque `paciente.update`/`resolverNuevoFlujo` (:270-293) quedó intacto (D-06).
</verification>

<success_criteria>
- HCSYNC-01: `primera_vez` sobre turno de rango menor → tipoTurnoId='Consulta'; sin degradar tipos superiores.
- HCSYNC-02: `tratamiento_en_consultorio` sobre 'Consulta'/'Control'/null → tipoTurnoId='Tratamiento'; sin degradar 'Pre-Quirúrgico'.
- HCSYNC-03: `pre_quirurgico` sobre turno no-cirugía → tipoTurnoId='Pre-Quirúrgico'; no-op si ya lo es.
- SC#4 (D-09): sin `dto.turnoId` → ningún query/update de turno.
- D-02: turno de cirugía nunca se toca.
- D-06: sin efectos nuevos sobre `Paciente.flujo`/`etapaCRM`.
- Escalera order-independent (D-04): el rango mayor siempre queda, sin importar el orden de guardado de entradas.
</success_criteria>

<output>
Crear `.planning/phases/65-sync-tipo-de-turno-plantilla-hc-backend/65-01-SUMMARY.md` al terminar.
</output>
