---
phase: 65-sync-tipo-de-turno-plantilla-hc-backend
plan: 02
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - backend/src/modules/historia-clinica/historia-clinica.service.ts
  - backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts
autonomous: true
requirements: [HCSYNC-01, HCSYNC-02, HCSYNC-03]

must_haves:
  truths:
    - "El sync de tipoTurno NUNCA rompe el guardado de la HC — cuando `dto.turnoId` es stale / apunta a un turno ya eliminado (el pre-fetch devuelve `turnoCtx=null`), el bloque de sync se saltea COMPLETO: no se llama `tx.turno.update` contra una fila inexistente, no se lanza Prisma P2025, y `crearEntrada` resuelve (la entrada de HC + diagnóstico/tratamiento + flujo/etapaCRM se persisten normalmente)"
    - "El guard del bloque de sync es `if (dto.turnoId && turnoCtx)` (o equivalente que exija turnoCtx no-null), mirror del bloque de flujo del paciente inmediatamente anterior que ya tolera `turnoCtx == null` vía `turnoCtx?.esCirugia ?? false`; un `turnoId` presente que resuelve (turnoCtx no-null) sigue disparando el sync exactamente como antes"
    - "No hay regresión sobre las 11 must-haves ya verificadas de 65-01: la tabla pura de `resolverTipoTurnoSync`, los wiring (a)(b)(c)(d) del sync, y los wiring de Phase 63 quedan verdes; HCSYNC-01/02/03 siguen satisfechos con turno resoluble"
  artifacts:
    - path: "backend/src/modules/historia-clinica/historia-clinica.service.ts"
      provides: "guard endurecido del bloque de sync de turno dentro del $transaction de crearEntrada"
      contains: "if (dto.turnoId && turnoCtx)"
    - path: "backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts"
      provides: "test de regresión: turnoId stale (turno.findUnique→null, tipoTurno.findUnique→destino truthy) → turno.update NO llamado y crearEntrada resuelve sin lanzar"
      contains: "turno.findUnique.mockResolvedValue(null)"
  key_links:
    - from: "historia-clinica.service.ts::crearEntrada (bloque sync)"
      to: "tx.turno.update"
      via: "guard compuesto dto.turnoId && turnoCtx (short-circuit cuando el pre-fetch no resuelve el turno)"
      pattern: "if \\(dto\\.turnoId && turnoCtx\\)"
---

<objective>
Cerrar la única gap confirmada de la verificación de Phase 65 (65-VERIFICATION.md, status: gaps_found, 11/12): el bloque de sync de tipoTurno dentro de `crearEntrada` guarda solo en `if (dto.turnoId)`, no en si el pre-fetch realmente resolvió el turno (`turnoCtx`). Cuando `dto.turnoId` es stale / apunta a un turno eliminado, `turnoCtx=null` pero el bloque igual corre, `resolverTipoTurnoSync` devuelve un destino real, el lookup de catálogo (global) lo encuentra, y `tx.turno.update({ where:{ id: dto.turnoId } })` se ejecuta contra una fila inexistente → Prisma P2025 → se aborta TODA la `$transaction` → falla el guardado de la HC. Esto contradice el propio comentario inline ("never break HC save") y el threat model T-65-03.

Purpose: Restaurar la defensa declarada — el sync nunca debe romper el guardado de la HC. Cambio mínimo y de bajo riesgo (un guard compuesto de una línea + un test de regresión), ya especificado por el verificador y el code review (WR-01).
Output:
- Guard del bloque de sync endurecido a `if (dto.turnoId && turnoCtx)` en historia-clinica.service.ts.
- Test de wiring de regresión que reproduce el turnoId stale y asserta que `turno.update` no se llama y `crearEntrada` resuelve.

Backend puro: sin cambios de schema, migraciones, DTO, controller ni frontend. Se extienden in-place los 2 archivos ya existentes. NO se toca 65-01-PLAN.md ni el helper `resolverTipoTurnoSync` (correcto tal cual). Fuera de alcance (no planear): WR-02 (write muerto de `esCirugia`) y la integridad del test de wiring del caso (d) de cirugía — no son gaps, quedan en tracking.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-sync-tipo-de-turno-plantilla-hc-backend/65-CONTEXT.md
@.planning/phases/65-sync-tipo-de-turno-plantilla-hc-backend/65-VERIFICATION.md
@.planning/phases/65-sync-tipo-de-turno-plantilla-hc-backend/65-PATTERNS.md

<interfaces>
<!-- Estado actual del código relevante (leído directamente del archivo). El executor lo usa tal cual. -->

Bloque a corregir — historia-clinica.service.ts, dentro del `$transaction` de crearEntrada (~:302-329), estado actual:
- Guard actual (~:306): `if (dto.turnoId) {`  ← EL BUG: no verifica turnoCtx.
- Dentro: `const targetNombre = resolverTipoTurnoSync(dto.tipo, turnoCtx?.tipoTurno?.nombre, turnoCtx?.esCirugia ?? false);`
- Luego: `const destino = await tx.tipoTurno.findUnique({ where:{ nombre: targetNombre }, select:{ id:true, esCirugia:true } });`
- Luego: `if (destino && destino.id !== turnoCtx?.tipoTurnoId) { await tx.turno.update({ where:{ id: dto.turnoId }, data:{ tipoTurnoId: destino.id, esCirugia: destino.esCirugia } }); }`

Fuente de verdad del patrón a espejar — el bloque de flujo del paciente INMEDIATAMENTE ANTERIOR (~:277-300) ya tolera `turnoCtx == null` así:
- `const nuevoFlujo = resolverNuevoFlujo(tipoEntradaResuelto, pac?.flujo, turnoCtx?.esCirugia ?? false);`
  El sync debe adoptar la misma postura: si el turno referenciado no resuelve, no operar sobre él.

Pre-fetch que produce turnoCtx (~:231-240): `dto.turnoId ? await this.prisma.turno.findUnique({ where:{id:dto.turnoId}, select:{ esCirugia:true, tipoTurnoId:true, tipoTurno:{ select:{ nombre:true } } } }) : null` → devuelve `null` cuando el turno no existe (id stale/eliminado) O cuando no hay `dto.turnoId`.

Bloque de tests de wiring del sync — historia-clinica.flujo.spec.ts, `describe('crearEntrada — sync tipoTurno (HCSYNC-01/02/03, D-09)')` (:351-488):
- Mock `mockPrisma` con `turno: { findUnique: jest.fn(), update: jest.fn().mockResolvedValue({}) }` y `tipoTurno: { findUnique: jest.fn() }` (:392-396).
- `$transaction.mockImplementation((cb) => cb(mockPrisma))` (:399-401) — el callback corre síncrono contra el mock.
- Constantes: `PACIENTE_ID`, `PROFESIONAL_ID`, `TURNO_ID='turno-1'`, `TIPO_TURNO_DESTINO_ID='tipo-turno-tratamiento'`.
- Caso (a) :420-443 — el patrón a espejar para el destino truthy: mockea AMBOS `mockPrisma.turno.findUnique.mockResolvedValue({ esCirugia:false, tipoTurnoId:TIPO_TURNO_ACTUAL_ID, tipoTurno:{ nombre:'Consulta' } })` Y `mockPrisma.tipoTurno.findUnique.mockResolvedValue({ id: TIPO_TURNO_DESTINO_ID, esCirugia: false })`. El nuevo caso (e) reutiliza esa misma `tipoTurno.findUnique` mock (misma forma `{ id: TIPO_TURNO_DESTINO_ID, esCirugia: false }`), pero con `turno.findUnique.mockResolvedValue(null)`.
- Casos existentes (a)(b)(c)(d) :420-487 — patrón: mockear `turno.findUnique`/`tipoTurno.findUnique`, invocar `service.crearEntrada(PACIENTE_ID, { tipo, turnoId } as never, PROFESIONAL_ID)`, assertar `mockPrisma.turno.update` con `toHaveBeenCalledWith(expect.objectContaining(...))` o `.not.toHaveBeenCalled()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endurecer el guard del bloque de sync a `if (dto.turnoId && turnoCtx)`</name>
  <files>backend/src/modules/historia-clinica/historia-clinica.service.ts</files>
  <read_first>
    - backend/src/modules/historia-clinica/historia-clinica.service.ts (:227-240 pre-fetch turnoCtx — confirmar que devuelve null cuando el turno no existe; :277-300 bloque de flujo del paciente que ya tolera turnoCtx null vía `turnoCtx?.esCirugia ?? false` — PATRÓN A ESPEJAR; :302-329 bloque de sync con el guard `if (dto.turnoId)` a corregir)
    - .planning/phases/65-sync-tipo-de-turno-plantilla-hc-backend/65-VERIFICATION.md (gap + `missing`: cambiar el guard a `if (dto.turnoId && turnoCtx)`)
  </read_first>
  <action>
    Cambiar el guard de apertura del bloque de sync de tipoTurno (~:306) de `if (dto.turnoId) {` a `if (dto.turnoId && turnoCtx) {`. Un solo cambio de guard: cuando `dto.turnoId` está presente pero el pre-fetch no resolvió el turno (`turnoCtx === null`, id stale/eliminado), el bloque completo se saltea — no se llama `resolverTipoTurnoSync`, ni `tipoTurno.findUnique`, ni `tx.turno.update` — mirroring exacto de cómo el bloque de flujo inmediatamente anterior ya tolera `turnoCtx == null`. NO tocar el interior del bloque (resolver, lookup de catálogo, idempotencia `destino.id !== turnoCtx?.tipoTurnoId`, el update) — solo el guard. NO tocar el pre-fetch, el bloque de flujo del paciente (:277-300), ni el bloque pre_quirurgico (:335+). Opcional/trivial: actualizar el comentario inline (~:303-305) para reflejar que el guard ahora también cubre turnoId stale. No introducir `paciente.update`/`etapaCRM`/`flujo` nuevos (D-06 intacto).
  </action>
  <verify>
    <automated>cd backend && grep -n "if (dto.turnoId && turnoCtx)" src/modules/historia-clinica/historia-clinica.service.ts && npx tsc --noEmit -p tsconfig.build.json && npx eslint src/modules/historia-clinica/historia-clinica.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "if (dto.turnoId && turnoCtx)" historia-clinica.service.ts` devuelve exactamente una línea (el guard nuevo); `grep -c "if (dto.turnoId) {" historia-clinica.service.ts` sobre el bloque de sync = 0 (el guard viejo ya no existe).
    - El interior del bloque de sync queda idéntico: sigue existiendo una única `tx.turno.update` scoped `{ id: dto.turnoId }`, condicionada por `destino && destino.id !== turnoCtx?.tipoTurnoId`.
    - No se agregó ningún `paciente.update` / `etapaCRM` / `flujo` nuevo en el bloque de sync (D-06).
    - `npx tsc --noEmit -p tsconfig.build.json` sale con código 0.
    - `npx eslint historia-clinica.service.ts` sale con código 0 (sin nuevos errores).
  </acceptance_criteria>
  <done>El bloque de sync se cortocircuita cuando el turno referenciado no resuelve (turnoCtx null), preservando el guardado de la HC; el disparo normal con turno resoluble queda intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Test de regresión — turnoId stale (turno.findUnique→null) no rompe el guardado de la HC</name>
  <files>backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts</files>
  <read_first>
    - backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts (:351-488 describe `crearEntrada — sync tipoTurno` completo: mock setup :370-418, patrón de los casos (a)(b)(c)(d) :420-487 — copiar estilo de mock/invocación/aserción; en particular el caso (a) :420-443 que mockea `tipoTurno.findUnique` con `{ id: TIPO_TURNO_DESTINO_ID, esCirugia: false }`)
    - backend/src/modules/historia-clinica/historia-clinica.service.ts (forma final del guard tras Task 1)
    - .planning/phases/65-sync-tipo-de-turno-plantilla-hc-backend/65-VERIFICATION.md (Behavioral Spot-Check :72 — la reproducción independiente: mock turno.findUnique→null, turno.update→throw P2025, se esperaba resolve; ese es el escenario a codificar como test permanente)
  </read_first>
  <action>
    Agregar un caso `it('(e) turnoId stale / turno inexistente (findUnique→null) → turno.update NO llamado y crearEntrada resuelve sin lanzar', ...)` dentro del describe existente `crearEntrada — sync tipoTurno (HCSYNC-01/02/03, D-09)` (:351-488), como último caso tras (d). Configurar los TRES mocks siguientes dentro del `it`:
    1. `mockPrisma.turno.findUnique.mockResolvedValue(null)` — el pre-fetch NO resuelve el turno (id stale/eliminado), por lo que `turnoCtx=null`.
    2. `mockPrisma.tipoTurno.findUnique.mockResolvedValue({ id: TIPO_TURNO_DESTINO_ID, esCirugia: false })` — OBLIGATORIO: mockear el lookup de catálogo para que devuelva un `destino` TRUTHY, espejando exactamente la mock del caso (a) (misma forma/constante `TIPO_TURNO_DESTINO_ID`). SIN esta mock, el `jest.fn()` sin configurar del `beforeEach` devuelve `undefined`, `destino` sería falsy, el inner guard `if (destino && …)` cortocircuitaría, `tx.turno.update` NUNCA se alcanzaría, y el test PASARÍA idéntico con el guard viejo Y el nuevo (test vacuo, cero protección de regresión). Con `destino` truthy, bajo el guard viejo `if (dto.turnoId)` el bloque SÍ corre, `destino.id (!= turnoCtx?.tipoTurnoId undefined)` es true, y `tx.turno.update` SÍ se alcanza → rechaza con P2025.
    3. `mockPrisma.turno.update.mockRejectedValue(new Error('P2025'))` — para reproducir fielmente el P2025 original: si el guard fuese incorrecto, el update correría y la promesa rechazaría.
    Invocar `service.crearEntrada(PACIENTE_ID, { tipo: 'tratamiento_en_consultorio', turnoId: TURNO_ID } as never, PROFESIONAL_ID)` (turnoId presente pero stale). Assertar dos cosas: (1) la promesa resuelve — `await expect(service.crearEntrada(...)).resolves.toBeDefined()` (o `.resolves.not.toThrow()` equivalente); (2) `expect(mockPrisma.turno.update).not.toHaveBeenCalled()`. No modificar los casos (a)(b)(c)(d) existentes ni el mock setup salvo lo estrictamente necesario dentro del nuevo `it` (los overrides de mock son locales al caso via las `mockResolvedValue`/`mockRejectedValue` invocadas dentro del test, respetando el `beforeEach`).
  </action>
  <verify>
    <automated>cd backend && npx jest historia-clinica.flujo.spec.ts -t "turnoId stale" -x && npx jest historia-clinica.flujo.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - El spec incluye el nuevo `it('(e) ...turnoId stale...')` con `mockPrisma.turno.findUnique.mockResolvedValue(null)`.
    - El caso mockea `mockPrisma.tipoTurno.findUnique.mockResolvedValue({ id: TIPO_TURNO_DESTINO_ID, esCirugia: false })` (destino truthy, misma forma que el caso (a)) — REQUERIDO para que, en la ruta pre-fix (guard viejo `if (dto.turnoId)`), el inner guard `if (destino && …)` no cortocircuite y `tx.turno.update` SÍ se alcance. Sin esta mock el test sería vacuo (pasaría con guard viejo y nuevo por igual).
    - El caso mockea `mockPrisma.turno.update.mockRejectedValue(new Error('P2025'))` para reproducir el fallo transaccional original.
    - El caso asserta que `crearEntrada` resuelve (no rechaza) Y que `mockPrisma.turno.update` NO fue llamado.
    - El caso es un guard de regresión genuino: con el guard viejo `if (dto.turnoId)` (destino truthy vía la mock anterior) `tx.turno.update` SÍ se alcanzaría y rechazaría con P2025 → el test FALLA pre-fix; con el guard endurecido de Task 1 el bloque se saltea → `turno.update` no se llama y la promesa resuelve → el test PASA post-fix.
    - `npx jest historia-clinica.flujo.spec.ts` completo sale con código 0: la tabla pura de `resolverTipoTurnoSync`, los wiring (a)(b)(c)(d), el nuevo (e), y los wiring de Phase 63 quedan verdes sin regresión.
  </acceptance_criteria>
  <done>La defensa contra turnoId stale queda cubierta por un test de wiring permanente que reproduce el escenario P2025 (con destino truthy para alcanzar realmente el update en la ruta pre-fix) y prueba que la HC se guarda igual; suite completa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente (LiveTurno / PatientDrawer) → crearEntrada | `dto.turnoId` llega del cliente autenticado y puede ser stale (turno eliminado o cache viejo) al cruzar al service |
| crearEntrada → Turno row | write a `Turno.tipoTurnoId`/`esCirugia` scoped por `{ id: dto.turnoId }` dentro del `$transaction` que también persiste la HC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-03 | Denial of Service / Integrity | bloque de sync de turno en crearEntrada con `dto.turnoId` stale | mitigate | ESTA gap es la realización correcta de T-65-03. El guard endurecido `if (dto.turnoId && turnoCtx)` cortocircuita el bloque cuando el pre-fetch no resuelve el turno, evitando `tx.turno.update` contra una fila inexistente (Prisma P2025) que abortaría toda la transacción y rompería el guardado de la HC. El test (e) fija la defensa permanentemente. Reduce superficie de fallo, no la agrega. |
| T-65-NEW | — (sin nueva superficie) | cambio de un guard + un test | accept | Sin nuevos endpoints, DTOs, dependencias, columnas ni queries. El write sigue scoped a las mismas 2 columnas no destructivas; el cambio solo agrega una condición de skip. Postura de autz de T-65-01 (single-tenant, IDOR aceptado) sin cambios. No se instalan paquetes (no aplica gate de legitimidad ni slopcheck). |
</threat_model>

<verification>
- `cd backend && npx jest historia-clinica.flujo.spec.ts` → toda la suite verde, incluyendo el nuevo caso (e) de turnoId stale y sin regresión de los casos previos ni de Phase 63.
- `cd backend && npx tsc --noEmit -p tsconfig.build.json` → sin errores de tipos.
- `cd backend && npx eslint src/modules/historia-clinica/historia-clinica.service.ts src/modules/historia-clinica/historia-clinica.flujo.spec.ts` → sin nuevos errores.
- Revisión manual: el guard es `if (dto.turnoId && turnoCtx)`; el interior del bloque de sync y el bloque `paciente.update`/`resolverNuevoFlujo` (:277-300) quedaron intactos (D-06); no se tocó 65-01-PLAN.md ni el helper `resolverTipoTurnoSync`.
</verification>

<success_criteria>
- El sync de tipoTurno nunca rompe el guardado de la HC: con `dto.turnoId` stale (turnoCtx null) la entrada de HC se persiste y `tx.turno.update` no se ejecuta (sin P2025).
- Guard endurecido `if (dto.turnoId && turnoCtx)` presente y único; disparo normal con turno resoluble intacto (HCSYNC-01/02/03 sin regresión).
- Test de regresión (e) verde reproduciendo el escenario stale-turnoId; suite completa verde.
- Sin regresión de las 11 must-haves ya verificadas de 65-01.
</success_criteria>

<output>
Crear `.planning/phases/65-sync-tipo-de-turno-plantilla-hc-backend/65-02-SUMMARY.md` al terminar.
</output>
</content>
</invoke>
