---
phase: 67-tel-fono-opcional-y-guards-de-env-o-backend
plan: 02
type: execute
wave: 2
depends_on: ["67-01"]
files_modified:
  - backend/src/modules/pacientes/pacientes.service.ts
  - backend/src/modules/paciente-portal/paciente-portal.service.ts
autonomous: true
requirements: [TEL-01]

must_haves:
  truths:
    - "D-01: un helper privado normalizeTelefono(value) en PacientesService hace trim, devuelve null si queda vacío y valida el largo mínimo sólo cuando hay valor — la normalización vive en el service, no en el DTO ni en el frontend, porque el frontend manda telefono: '' y no undefined"
    - "D-02: la misma regla aplica en create() y en update(); vaciar el teléfono de un paciente que sí tenía número lo deja en null, porque vaciar el campo es una forma legítima de corregir un número mal cargado"
    - "D-05: updateContacto se relaja pero sigue validando forma — sin valor guarda null, con valor sigue exigiendo 6 caracteres o más, de modo que editar la sección Contacto de un paciente sin número no devuelve 400"
    - "D-06: el umbral de 6 caracteres existe en un solo lugar, el helper normalizeTelefono(), usado por create, update y updateContacto — no se duplica ni se mueve al DTO"
    - "D-07: updateEmergencia queda intacto; contactoEmergenciaNombre, contactoEmergenciaRelacion y contactoEmergenciaTelefono siguen siendo los tres obligatorios"
    - "D-08: pickPresent() del portal conserva su comportamiento y su test WR-02 — sólo se reescribe el comentario a 'el portal nunca borra datos de contacto'; la asimetría staff-puede-vaciar / paciente-no-puede es deliberada"
    - "El autosuggest sigue devolviendo pacientes sin teléfono que matchean por nombre o DNI, y el score no se rompe por el LIKE sobre una columna nullable"
    - "El parámetro de búsqueda de suggest() sigue pasando como parámetro de Prisma, sin concatenación de strings en el SQL"
  artifacts:
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "normalizeTelefono() + create/update/updateContacto normalizados + suggest() null-safe"
      contains: "private normalizeTelefono"
    - path: "backend/src/modules/paciente-portal/paciente-portal.service.ts"
      provides: "pickPresent() con el comentario WR-02 recontextualizado, comportamiento sin cambios"
      contains: "value !== undefined && value !== null"
  key_links:
    - from: "backend/src/modules/pacientes/pacientes.service.ts create()/update()/updateContacto()"
      to: "normalizeTelefono()"
      via: "llamada al helper privado antes de persistir"
      pattern: "this\\.normalizeTelefono\\("
    - from: "backend/src/modules/pacientes/pacientes.service.ts suggest()"
      to: "columna Paciente.telefono nullable"
      via: "COALESCE(p.telefono, '') en el WHERE y en el CASE del score"
      pattern: "COALESCE\\(p\\.telefono"
---

<objective>
Cerrar el lado de escritura de TEL-01 en `PacientesService`: un único helper `normalizeTelefono()` que define qué es "teléfono válido" para todo el módulo, aplicado en `create()`, `update()` y `updateContacto()`; y dejar `suggest()` a salvo de la semántica de NULL en SQL. En el portal, recontextualizar el comentario de `pickPresent()` sin tocar su comportamiento.

Purpose: D-01, D-02, D-05, D-06, D-07 y D-08 del CONTEXT.md. Sin esto, un `telefono: ""` enviado por el frontend se persistiría como string vacío, el guard de envío del plan 67-03 no dispararía, y editar la sección Contacto de un paciente sin número devolvería 400.
Output: helper de normalización, tres call sites normalizados, `suggest()` null-safe, comentario del portal actualizado.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-CONTEXT.md
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-PATTERNS.md
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-01-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contratos y firmas que el ejecutor necesita. Detalle completo con excerpts en 67-PATTERNS.md. -->

Ya disponible tras el plan 67-01:
  - `Paciente.telefono` es `string | null` en el cliente Prisma
  - `PacienteSuggest.telefono` y `PacienteListaDto.telefono` son `string | null`
  - el genérico de `$queryRaw` de `suggest()` ya declara `telefono: string | null`

Firmas relevantes en backend/src/modules/pacientes/pacientes.service.ts:
  - `async create(dto: CreatePacienteDto)` — líneas 54-85, arma `const data = { ...dto, fechaNacimiento: ..., fechaIndicaciones: ..., etapaCRM, flujo }` y llama `this.prisma.paciente.create({ data })`
  - `async update(id: string, dto: UpdatePacienteDto)` — líneas 209-215, passthrough puro `data: dto`
  - `private async updateContacto(id: string, data: any)` — líneas 426-449, whitelist `{ telefono, telefonoAlternativo, email }` + check `typeof patch.telefono !== 'string' || patch.telefono.trim().length < 6` -> `BadRequestException('Teléfono inválido')`
  - `private async updateEmergencia(id: string, data: any)` — líneas 451-470, NO SE TOCA (D-07)
  - `private async ensureExists(id: string)` — líneas 332-335, convención de helper privado a imitar
  - `async suggest(q: string, profesionalId?: string): Promise<PacienteSuggest[]>` — líneas 337-399
  - `BadRequestException` ya importado desde `@nestjs/common`

Firmas relevantes en backend/src/modules/paciente-portal/paciente-portal.service.ts:
  - `private pickPresent<T extends object>(input: T, allowed: readonly string[]): Record<string, unknown>` — líneas 690-703
  - `async updateContacto(pacienteId: string, dto: UpdateContactoPortalDto)` — líneas 249-268, llama a `pickPresent`
  - `getDatos()` expone `contacto.telefono` en líneas 141-150 (tipo inferido, sin anotación explícita)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Helper normalizeTelefono() + aplicarlo en create() y update()</name>
  <files>backend/src/modules/pacientes/pacientes.service.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.ts (líneas 30-50: imports y la constante `EMAIL_SHAPE` con su comentario "No global ValidationPipe is active, so the decorator on the DTO is inert — we MUST re-check at runtime"; líneas 54-85: `create()`; líneas 209-215: `update()`; líneas 332-335: `ensureExists()` como convención de helper privado; líneas 426-449: `updateContacto()` de donde sale la regla de 6 caracteres)
    - .planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-CONTEXT.md (D-01, D-02, D-06)
    - .planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-PATTERNS.md (secciones `normalizeTelefono()`, `create()`, `update()`)
    - backend/src/modules/pacientes/dto/create-paciente.dto.ts y dto/update-paciente.dto.ts (estado post plan 67-01)
  </read_first>
  <action>
    Agregar a `PacientesService` un método privado `normalizeTelefono(value: unknown): string | null`, ubicado junto a los otros helpers privados del service (misma zona que `ensureExists`, líneas 332-335), con un comentario de cabecera al estilo del bloque `EMAIL_SHAPE` que explique que es la única definición de "teléfono válido" del módulo (D-06). Comportamiento exacto:
      - `null` o `undefined` -> devuelve `null`
      - string: se le hace `trim()`; si el resultado es cadena vacía -> devuelve `null`; si tiene menos de 6 caracteres -> lanza `BadRequestException('Teléfono inválido')` (mismo mensaje literal que hoy usa `updateContacto`, para no cambiar el contrato de error visible); si tiene 6 o más -> devuelve el valor ya trimmeado
      - cualquier otro tipo (número, objeto, array) -> lanza `BadRequestException('Teléfono inválido')`
    El umbral 6 aparece una sola vez en todo el módulo, dentro de este helper (D-06). No crear un DTO por sección ni mover la regla al DTO.

    En `create()` (líneas 54-85): dentro del objeto `data`, después del spread `...dto`, agregar `telefono: this.normalizeTelefono(dto.telefono)` siguiendo la misma forma de override-post-spread que ya usan `fechaNacimiento` y `fechaIndicaciones`. Con esto, un alta sin teléfono persiste `null` explícito y un `telefono: ""` del frontend también (D-01). El resto de `create()` —`etapaCRM: EtapaCRM.NUEVO_LEAD`, `flujo: null`, el `catch` con `P2002`/`ConflictException`— queda igual.

    En `update()` (líneas 209-215): hoy es un passthrough `data: dto`. Cambiar a un objeto que preserve el passthrough y sólo intervenga cuando el campo viene presente, usando spread condicional del estilo `...(dto.telefono !== undefined ? { telefono: this.normalizeTelefono(dto.telefono) } : {})` sobre `...dto`. La distinción es crítica: `telefono` ausente NO debe forzarse a `null` (todos los campos de `UpdatePacienteDto` son opcionales y el editor manda parciales), pero `telefono: ""` presente SÍ debe quedar en `null` (D-02). Mantener la llamada previa a `this.ensureExists(id)`.

    No agregar `as any` ni non-null assertions. No tocar `updateContacto` ni `updateEmergencia` en este task.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && grep -n 'private normalizeTelefono' src/modules/pacientes/pacientes.service.ts && grep -c 'this.normalizeTelefono(' src/modules/pacientes/pacientes.service.ts && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `pacientes.service.ts` contiene exactamente una declaración `private normalizeTelefono(` y su firma devuelve `string | null`
    - Source: la subcadena literal `6` como umbral de largo de teléfono aparece únicamente dentro de `normalizeTelefono` (grep de `length < 6` devuelve una sola ocurrencia en el archivo tras la Task 2)
    - Source: `create()` incluye `telefono: this.normalizeTelefono(dto.telefono)` dentro del objeto `data`, después del spread `...dto`
    - Source: `update()` aplica `normalizeTelefono` sólo cuando `dto.telefono !== undefined`
    - Behavior: `normalizeTelefono(undefined)` y `normalizeTelefono('   ')` devuelven `null`; `normalizeTelefono(' 1123456789 ')` devuelve `'1123456789'`; `normalizeTelefono('123')` lanza `BadRequestException` con mensaje `Teléfono inválido`
    - CLI: `cd backend && npm run build` sale 0
  </acceptance_criteria>
  <done>El helper existe, es la única definición del umbral, y `create()`/`update()` normalizan el teléfono antes de persistir.</done>
</task>

<task type="auto">
  <name>Task 2: Relajar updateContacto (D-05/D-06), dejar updateEmergencia intacto (D-07) y recontextualizar el comentario WR-02 del portal (D-08)</name>
  <files>backend/src/modules/pacientes/pacientes.service.ts, backend/src/modules/paciente-portal/paciente-portal.service.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.ts (líneas 401-424: `updatePacienteSection`, el dispatcher por sección; 426-449: `updateContacto` con el whitelist `{telefono, telefonoAlternativo, email}` y la validación a reemplazar; 451-470: `updateEmergencia`, el contraste "qué NO cambiar")
    - backend/src/modules/paciente-portal/paciente-portal.service.ts (líneas 141-150: `getDatos()` expone `contacto.telefono`; 249-268: `updateContacto` del portal llamando a `pickPresent`; 690-703: `pickPresent()` con el comentario `// WR-02:` a reescribir)
    - backend/src/modules/paciente-portal/paciente-portal.service.spec.ts (líneas 275-292: el test `'WR-02: drops an explicit null so it never reaches a non-nullable column'` que DEBE seguir pasando sin editarse)
    - .planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-CONTEXT.md (D-05, D-06, D-07, D-08 y la nota de `<specifics>` sobre los dos sitios distintos)
  </read_first>
  <action>
    En `pacientes.service.ts`, dentro de `updateContacto` (líneas 426-449): reemplazar el bloque de validación actual —el `if (typeof patch.telefono !== 'string' || patch.telefono.trim().length < 6) throw new BadRequestException('Teléfono inválido')`— por una asignación que pase el valor por `this.normalizeTelefono(data.telefono)` al armar `patch.telefono`. Resultado: sin valor (ausente, `null` o cadena en blanco) guarda `null`; con valor de 6 o más caracteres guarda el string trimmeado; con valor de menos de 6 caracteres sigue lanzando `BadRequestException('Teléfono inválido')` desde el helper (D-05, D-06). Mantener sin cambios el resto de la función: el whitelist explícito de tres campos, el `telefonoAlternativo: data.telefonoAlternativo ?? null`, el `email: data.email ?? null`, el check de email y el `prisma.paciente.update({ where: { id }, data: patch })`. El whitelist es la defensa contra mass-assignment: no ampliarlo.

    NO tocar `updateEmergencia` (líneas 451-470) ni su mensaje `'Datos de emergencia inválidos'` (D-07): `contactoEmergenciaNombre`, `contactoEmergenciaRelacion` y `contactoEmergenciaTelefono` siguen siendo obligatorios los tres. NO tocar el dispatcher `updatePacienteSection`.

    En `paciente-portal.service.ts`, dentro de `pickPresent()` (líneas 690-703): reescribir ÚNICAMENTE el comentario `// WR-02: ...`. La justificación actual ("forwarding null into a non-nullable column (e.g. telefono) throws at the DB layer (500)") deja de ser cierta ahora que la columna es nullable; el nuevo texto debe decir que el portal nunca borra datos de contacto — el staff sí puede vaciar `telefono` vía `PacientesService.updateContacto` (D-02), pero el paciente desde el portal no, porque el teléfono es el canal por el que la clínica lo contacta, y esa asimetría es deliberada (D-08). La línea `if (value !== undefined && value !== null) data[key] = value;` NO cambia, la firma NO cambia, el allow-list de la llamada en `updateContacto` (líneas 249-258) NO cambia, y el test `paciente-portal.service.spec.ts:280-288` NO se edita.

    Verificar además —sin editar salvo que el compilador lo exija— que la lectura `contacto.telefono` de `getDatos()` (líneas 141-150) sigue compilando: el tipo es inferido, así que un `string | null` fluye sin anotación que corregir.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -10 && npm run test -- paciente-portal.service.spec.ts 2>&1 | tail -15 && git diff --stat src/modules/paciente-portal/paciente-portal.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `updateContacto` de `pacientes.service.ts` ya no contiene `typeof patch.telefono !== 'string'` y arma `telefono` vía `this.normalizeTelefono(`
    - Source: `updateContacto` conserva las tres claves del whitelist (`telefono`, `telefonoAlternativo`, `email`) y el check de email
    - Source: `git diff` NO muestra cambios en `updateEmergencia` ni en `updatePacienteSection`
    - Source: `paciente-portal.service.ts` conserva literalmente `if (value !== undefined && value !== null) data[key] = value;` y su comentario ya no menciona "non-nullable column"
    - Source: `git diff --stat` NO lista `backend/src/modules/paciente-portal/paciente-portal.service.spec.ts`
    - Test command: `cd backend && npm run test -- paciente-portal.service.spec.ts` sale 0 (incluye el test WR-02)
    - Behavior: `updateContacto(id, { telefono: '', email: 'a@b.com' })` persiste `telefono: null` en vez de lanzar 400
    - Behavior: `updateContacto(id, { telefono: '123' })` sigue lanzando `BadRequestException` con mensaje `Teléfono inválido`
    - CLI: `cd backend && npm run build` sale 0
  </acceptance_criteria>
  <done>La sección Contacto de la ficha se puede guardar sin teléfono, la de emergencia sigue exigiéndolo, y el portal conserva su comportamiento con el comentario actualizado.</done>
</task>

<task type="auto">
  <name>Task 3: suggest() a prueba de NULL en el LIKE y en el score</name>
  <files>backend/src/modules/pacientes/pacientes.service.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.ts (líneas 337-399: `suggest()` completo — el genérico de `$queryRaw` ya ensanchado por el plan 67-01, el `CASE WHEN p.telefono LIKE '%' || ${query} || '%' THEN 0.5 ELSE 0 END` del score, el `OR p.telefono LIKE '%' || ${query} || '%'` del WHERE, el `${profesionalFilter}` construido con `Prisma.sql` y el `try/catch` que devuelve `[]`)
    - backend/src/common/types/paciente-suggest.type.ts (estado post plan 67-01)
    - .planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-PATTERNS.md (sección `suggest()` raw SQL)
    - .planning/STATE.md ("Ojo en la Phase 67: el LIKE sobre p.telefono en suggest() devuelve NULL (no false) con teléfono nulo")
  </read_first>
  <action>
    En el `$queryRaw` de `suggest()`, reemplazar las dos apariciones de `p.telefono LIKE '%' || ${query} || '%'` por `COALESCE(p.telefono, '') LIKE '%' || ${query} || '%'`: una en el `CASE WHEN ... THEN 0.5 ELSE 0 END` del cálculo de score y otra en el `OR` del `WHERE`. Motivo: en SQL `NULL LIKE 'x'` evalúa a NULL, no a false — en el `WHERE` un OR con NULL sigue siendo TRUE si otra rama matchea (nombre o DNI), pero la expresión queda ambigua y frágil; con `COALESCE` el predicado es explícitamente false para los pacientes sin teléfono y el score suma 0 en vez de depender del comportamiento del `ELSE` frente a un WHEN NULL.

    Restricción de seguridad no negociable: `${query}` debe seguir siendo un parámetro del template tag de Prisma. No convertir la query a concatenación de strings de JavaScript, no interpolar `query` dentro de un `Prisma.raw(...)`, y no tocar `profesionalFilter`, que es el scope por profesional (`Prisma.sql\`AND p."profesionalId" = ${profesionalId}\``) y debe seguir aplicándose exactamente igual.

    No cambiar el `SELECT p.telefono`, ni el `ORDER BY score DESC`, ni el `LIMIT 10`, ni el `try/catch` que devuelve `[]`. No cambiar la firma ni el `if (!q || q.trim().length < 2) return []`.

    Probar el resultado contra la base real con un script temporal fuera del repo (por ejemplo bajo `/tmp`), usando `PrismaClient` dentro de un `$transaction` que al final lanza un error sentinela para forzar rollback, de modo que no quede data de prueba: dentro de la transacción crear un paciente con `telefono: null`, `nombreCompleto` y `dni` únicos e improbables, ejecutar la misma consulta de `suggest()` con un query que matchee ese nombre, y comprobar que la fila vuelve con `telefono: null` y `score > 0`. Repetir la consulta con un query numérico que no matchee nada y comprobar que ese paciente NO aparece. Borrar el script al terminar y reportar el resultado en el SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && grep -c "COALESCE(p.telefono, '')" src/modules/pacientes/pacientes.service.ts && ! grep -n "p.telefono LIKE" src/modules/pacientes/pacientes.service.ts && ! grep -n 'Prisma.raw' src/modules/pacientes/pacientes.service.ts && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `pacientes.service.ts` contiene exactamente 2 ocurrencias de `COALESCE(p.telefono, '')` y 0 ocurrencias de `p.telefono LIKE`
    - Source: el SQL de `suggest()` sigue usando `${query}` como parámetro del template tag; `grep 'Prisma.raw'` en el archivo no devuelve nada y no aparece concatenación JS de `query` en la query
    - Source: `${profesionalFilter}` sigue presente en el `WHERE` de `suggest()`
    - Behavior: contra la base real, un paciente con `telefono = NULL` buscado por su nombre aparece en los resultados con `score > 0`; buscado por un número que no le corresponde, no aparece. Resultado del probe registrado en el SUMMARY
    - Behavior: el script de prueba corre dentro de una transacción con rollback y no deja filas nuevas (`SELECT count(*) FROM "Paciente"` igual antes y después)
    - CLI: `cd backend && npm run build` sale 0 y `npm run lint` sale 0
  </acceptance_criteria>
  <done>El autosuggest devuelve pacientes sin teléfono cuando matchean por nombre o DNI, el score no se degrada, y el SQL sigue parametrizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP -> `POST /pacientes`, `PATCH /pacientes/:id`, editor por secciones | Payload no confiable con `telefono` ahora opcional; no hay ValidationPipe global, la validación real vive en el service |
| cliente HTTP -> `GET /pacientes/suggest?q=` | El término de búsqueda del usuario entra a una consulta `$queryRaw` |
| paciente autenticado -> portal (`PATCH` de contacto) | Superficie distinta del staff, con menos privilegios sobre los mismos campos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-05 | Tampering | `normalizeTelefono()` como único punto de validación de forma | mitigate | El helper rechaza tipos no-string con `BadRequestException('Teléfono inválido')` y mantiene el umbral de 6 caracteres; los tres call sites (`create`, `update`, `updateContacto`) pasan por él, sin ruta alternativa que persista `telefono` sin normalizar |
| T-67-06 | Tampering | `suggest()` `$queryRaw` con el término de búsqueda del usuario | mitigate | `${query}` permanece como parámetro del template tag de Prisma; prohibido `Prisma.raw` y la concatenación JS; `COALESCE` se agrega alrededor de la columna, no del parámetro. Gate automatizado: `grep 'Prisma.raw'` debe no devolver nada |
| T-67-07 | Information Disclosure | scope multi-tenant de `suggest()` | mitigate | `${profesionalFilter}` (`AND p."profesionalId" = ${profesionalId}`) se conserva intacto en el `WHERE`; criterio de aceptación explícito sobre su presencia |
| T-67-08 | Elevation of Privilege | `updateContacto` del staff vs. `pickPresent()` del portal | mitigate | Se preserva la asimetría de D-08: el portal sigue descartando `null` (el paciente no puede vaciar su teléfono) mientras el staff sí puede; el whitelist de tres campos de `updateContacto` no se amplía, manteniendo la defensa contra mass-assignment |
| T-67-09 | Repudiation | script de probe contra la base real en la Task 3 | mitigate | El probe corre dentro de un `$transaction` con rollback forzado y se borra al terminar; criterio de aceptación exige conteo de filas idéntico antes y después |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala ni actualiza dependencias. Si apareciera la necesidad, detener y correr el Package Legitimacy Gate antes de continuar |
</threat_model>

<verification>
- `cd backend && npm run build` sale 0
- `cd backend && npm run lint` sale 0
- `cd backend && npm run test -- paciente-portal.service.spec.ts` sale 0, con el test WR-02 sin editar
- Probe de `suggest()` contra la base real documentado en el SUMMARY, con rollback verificado
</verification>

<success_criteria>
- `normalizeTelefono()` es la única definición de "teléfono válido" del módulo y gobierna `create`, `update` y `updateContacto`
- Un `telefono: ""` enviado por el frontend termina como `null` en la base, tanto al crear como al editar
- La sección Contacto de la ficha se puede guardar sin teléfono; la de emergencia sigue exigiendo sus tres campos
- El portal conserva su comportamiento y su test WR-02, con el comentario recontextualizado
- El autosuggest sigue encontrando pacientes sin teléfono por nombre y DNI, con SQL parametrizado y scope por profesional intacto
</success_criteria>

<output>
Create `.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-02-SUMMARY.md` when done
</output>
