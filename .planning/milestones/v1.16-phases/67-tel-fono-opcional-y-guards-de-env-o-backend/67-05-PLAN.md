---
phase: 67-tel-fono-opcional-y-guards-de-env-o-backend
plan: 05
type: execute
wave: 3
depends_on: ["67-02", "67-03", "67-04"]
files_modified:
  - backend/src/modules/pacientes/pacientes.service.spec.ts
  - backend/src/modules/whatsapp/whatsapp.service.spec.ts
  - backend/src/modules/presupuestos/presupuestos.service.spec.ts
autonomous: true
requirements: [TEL-01, ENVIO-01, ENVIO-02]

must_haves:
  truths:
    - "SC#1: un create() con sólo nombreCompleto y dni persiste el paciente sin teléfono, probado en pacientes.service.spec.ts"
    - "D-03: hay tests que prueban el criterio falsy-tras-trim del guard — null, cadena vacía y sólo espacios bloquean por igual, y un teléfono corto pero presente NO bloquea el envío (el guard mira existencia, no validez)"
    - "D-08: el test WR-02 de paciente-portal.service.spec.ts sigue pasando sin haber sido editado"
    - "SC#3 y SC#4: existen tests que prueban que enviar WhatsApp y enviar un presupuesto por WhatsApp a un paciente sin teléfono lanzan BadRequestException sin encolar jobs ni crear registros de mensaje"
    - "SC#5: la suite completa del backend sigue pasando, y cualquier fallo preexistente queda documentado en vez de aceptado en silencio"
    - "Existe un test de camino feliz que prueba que un paciente CON teléfono sigue encolando el envío — el guard no bloquea de más"
  artifacts:
    - path: "backend/src/modules/whatsapp/whatsapp.service.spec.ts"
      provides: "Cobertura de los 4 guards de teléfono de WhatsappService"
      min_lines: 120
    - path: "backend/src/modules/presupuestos/presupuestos.service.spec.ts"
      provides: "Cobertura del passthrough de telefono nullable en generatePdf()"
      min_lines: 50
    - path: "backend/src/modules/pacientes/pacientes.service.spec.ts"
      provides: "Cobertura de normalizeTelefono en create/update/updateContacto"
      contains: "normaliz"
  key_links:
    - from: "backend/src/modules/whatsapp/whatsapp.service.spec.ts"
      to: "getQueueToken(WHATSAPP_QUEUE)"
      via: "provider mockeado para asertar que add() no se llama cuando el guard bloquea"
      pattern: "getQueueToken"
---

<objective>
Probar los cinco success criteria de la fase con tests automatizados: el alta sin teléfono, la normalización en las tres rutas de escritura, los cuatro guards de envío, el passthrough del PDF de presupuesto, y la suite existente en verde. Dos de los tres archivos son nuevos — no existía spec para `WhatsappService` ni para `PresupuestosService`.

Purpose: SC#1, SC#3, SC#4 y SC#5 del ROADMAP, y la verificación de D-03 y D-08. Sin estos tests, los guards del plan 67-03 quedarían sin ninguna prueba de que fallan cerrado.
Output: `pacientes.service.spec.ts` extendido, `whatsapp.service.spec.ts` y `presupuestos.service.spec.ts` nuevos, suite completa verde.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-CONTEXT.md
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-PATTERNS.md
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-02-SUMMARY.md
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-03-SUMMARY.md
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-04-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Constructores y tokens que los specs necesitan. Los analogs concretos están en 67-PATTERNS.md ("No Analog Found"). -->

`PacientesService` (constructor): `PrismaService`, `ConfigService`, `EncryptionService`.
  Analog de setup: `backend/src/modules/pacientes/pacientes.service.spec.ts:24-83` — `mockEncryptionService()`, `mockConfigService` con `FRONTEND_URL`, `mockPrisma` con `paciente.{findUnique,update,findMany,create}` y `contactoLog.count`.
  Analog de bloque describe: mismo archivo, `describe('create() — default etapaCRM=NUEVO_LEAD + flujo=null (D-01/D-03)')`, líneas 392-441.
  API pública para llegar a `updateContacto` (que es privado): `updatePacienteSection(id, { section: 'contacto', data: {...} })` — `pacientes.service.ts:401-424`.

`WhatsappService` (constructor): `@InjectQueue(WHATSAPP_QUEUE) Queue`, `PrismaService`, `EncryptionService`.
  Token de cola: `getQueueToken(WHATSAPP_QUEUE)` de `@nestjs/bullmq`; `WHATSAPP_QUEUE = 'whatsapp-messages'` se exporta desde `backend/src/modules/whatsapp/processors/whatsapp-message.processor.ts:7`.
  Analog de mock de cola: `backend/src/modules/finanzas/finanzas.service.spec.ts` líneas 2 y 55-90 (`{ provide: getQueueToken(CAE_QUEUE), useValue: mockCaeQueue }`, con `mockCaeQueue = { add: jest.fn().mockResolvedValue({ id: 'job-1' }) }`).
  Prisma a mockear: `configuracionWABA.findUnique` (lo consume `getDecryptedConfig`, `whatsapp.service.ts:157-179`, y lanza `NotFoundException` si devuelve null), `paciente.findUnique`, `mensajeWhatsApp.{create,findUnique,update}`.
  `EncryptionService.decrypt` debe devolver un token cualquiera para que `getDecryptedConfig` no explote.

`PresupuestosService` (constructor): `PrismaService`, `CuentasCorrientesService`, `PresupuestoPdfService` (`presupuestos.service.ts:50-54`). No tiene `@InjectQueue`: no hace falta mock de cola.
  `generatePdf(id)` — `presupuestos.service.ts:417-476` — arma `pdfData` y delega en `this.pdfService.generatePdfBuffer(...)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extender pacientes.service.spec.ts con la cobertura de normalizeTelefono</name>
  <files>backend/src/modules/pacientes/pacientes.service.spec.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.spec.ts (líneas 1-90: el header con la estrategia de mocks, `mockEncryptionService()`, el `beforeEach` con `Test.createTestingModule`; líneas 392-441: el bloque `describe('create() — default etapaCRM=NUEVO_LEAD + flujo=null (D-01/D-03)')` que es la plantilla exacta de estilo)
    - backend/src/modules/pacientes/pacientes.service.ts (estado post plan 67-02: `normalizeTelefono`, `create`, `update`, `updateContacto`, `updatePacienteSection`)
    - .planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-02-SUMMARY.md (qué quedó exactamente implementado)
    - .planning/ROADMAP.md (Phase 67, success criteria #1 y #5)
  </read_first>
  <action>
    Agregar a `pacientes.service.spec.ts` un bloque `describe` nuevo dedicado al teléfono opcional, siguiendo el estilo del bloque de `create()` de las líneas 392-441 (mismo patrón de `mockPrisma.paciente.create.mockResolvedValue(...)` + inspección de `mock.calls[0][0].data`). No modificar los describes existentes ni el `beforeEach`; si hiciera falta una clave nueva en `mockPrisma`, agregarla sin quitar las existentes.

    Casos a cubrir:
      - SC#1: `create({ nombreCompleto, dni })` sin la clave `telefono` no lanza, y el `data` que llega a `prisma.paciente.create` tiene `telefono: null`, conservando `etapaCRM: 'NUEVO_LEAD'` y `flujo: null` (no romper el contrato de EMBUDO-07)
      - `create({ ..., telefono: '' })` y `create({ ..., telefono: '   ' })` -> `data.telefono === null` (D-01: el frontend manda cadena vacía, no undefined)
      - `create({ ..., telefono: ' 1123456789 ' })` -> `data.telefono === '1123456789'` (trimmeado)
      - `create({ ..., telefono: '123' })` -> rechaza con `BadRequestException` y mensaje `Teléfono inválido`, sin llamar a `prisma.paciente.create`
      - `update(id, { nombreCompleto: 'X' })` sin la clave `telefono` -> el `data` enviado a `prisma.paciente.update` NO tiene la propiedad `telefono` (usar `expect(data).not.toHaveProperty('telefono')`); es la distinción ausente-vs-vacío de D-02
      - `update(id, { telefono: '' })` -> `data.telefono === null` (D-02: el staff puede vaciar el número)
      - `updatePacienteSection(id, { section: 'contacto', data: { telefono: '', email: 'a@b.com' } })` -> no lanza y persiste `telefono: null` (D-05)
      - `updatePacienteSection(id, { section: 'contacto', data: { telefono: '123' } })` -> `BadRequestException` con `Teléfono inválido` (D-06: el umbral sigue vivo, en un solo lugar)
      - `updatePacienteSection(id, { section: 'emergencia', data: { ... sin contactoEmergenciaTelefono } })` -> sigue lanzando `BadRequestException` con `Datos de emergencia inválidos` (D-07: la sección de emergencia no se relajó)

    Nombrar cada `it()` citando la decisión que prueba (por ejemplo `'D-02: vaciar el teléfono desde el staff lo deja en null'`), igual que hace el archivo con `(D-01/D-03)` en el describe existente.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run test -- pacientes.service.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `cd backend && npm run test -- pacientes.service.spec.ts` sale 0
    - Behavior: existe un test que llama a `create` con sólo `nombreCompleto` y `dni` y asserta `telefono: null` en el `data` persistido (SC#1)
    - Behavior: existe un test que asserta `expect(data).not.toHaveProperty('telefono')` para un `update` que no manda el campo
    - Behavior: existe un test que asserta `BadRequestException` con mensaje `Teléfono inválido` para un teléfono de menos de 6 caracteres, en `create` y en la sección `contacto`
    - Behavior: existe un test que asserta que la sección `emergencia` sigue rechazando con `Datos de emergencia inválidos`
    - Source: los describes y tests preexistentes del archivo no fueron modificados (el diff sólo agrega bloques)
  </acceptance_criteria>
  <done>La normalización de teléfono está cubierta en las tres rutas de escritura, incluido el caso SC#1 y el contraste con la sección de emergencia.</done>
</task>

<task type="auto">
  <name>Task 2: Crear whatsapp.service.spec.ts con la cobertura de los 4 guards</name>
  <files>backend/src/modules/whatsapp/whatsapp.service.spec.ts</files>
  <read_first>
    - backend/src/modules/whatsapp/whatsapp.service.ts (estado post plan 67-03: el helper `requireTelefonoParaEnvio`, `getDecryptedConfig` en 157-179, y los 4 métodos guardados)
    - backend/src/modules/finanzas/finanzas.service.spec.ts (líneas 1-10 y 55-90: import de `getQueueToken`, `mockCaeQueue = { add: jest.fn()... }` y su registro como provider — el patrón exacto de mock de cola BullMQ)
    - backend/src/modules/pacientes/pacientes.service.spec.ts (líneas 1-90: `Test.createTestingModule`, `mockEncryptionService()` y el estilo de mock de Prisma por test)
    - backend/src/modules/whatsapp/processors/whatsapp-message.processor.ts (línea 7: `export const WHATSAPP_QUEUE = 'whatsapp-messages'`)
    - .planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-03-SUMMARY.md (mensaje literal del guard y ubicación final en cada método)
  </read_first>
  <action>
    Crear `backend/src/modules/whatsapp/whatsapp.service.spec.ts` — primer spec del módulo. Setup compuesto: `Test.createTestingModule` con `WhatsappService` y tres providers mockeados — `{ provide: getQueueToken(WHATSAPP_QUEUE), useValue: { add: jest.fn().mockResolvedValue({ id: 'job-1' }) } }`, `{ provide: PrismaService, useValue: mockPrisma }` y `{ provide: EncryptionService, useValue: { decrypt: jest.fn(() => 'token-desencriptado') } }`. `mockPrisma` debe cubrir `configuracionWABA.findUnique` (devolviendo un objeto con `phoneNumberId`, `accessTokenEncrypted`, `wabaId`, `displayPhone`, para que `getDecryptedConfig` no lance), `paciente.findUnique`, y `mensajeWhatsApp.{create,findUnique,update}`. `jest.clearAllMocks()` en el `beforeEach`, como hace `finanzas.service.spec.ts`.

    Casos a cubrir, uno por método y por variante de valor falsy (D-03):
      - `sendTemplateMessage` con `paciente = { telefono: null, whatsappOptIn: true }` -> rechaza con `BadRequestException`, el mensaje contiene `no tiene un número de teléfono cargado`, y `queue.add` y `mensajeWhatsApp.create` reciben 0 llamadas (ENVIO-01, SC#3)
      - mismo método con `telefono: ''` y con `telefono: '   '` -> mismo resultado (D-03: falsy tras trim)
      - mismo método con `telefono: null` pero `telefonoAlternativo: '1123456789'` en el registro mockeado -> sigue rechazando (D-04: el alternativo no es canal de envío)
      - `sendFreeText` con `telefono: null` -> mismo rechazo, 0 encolados
      - `sendPresupuestoPdf` con `telefono: null` -> mismo rechazo, 0 encolados (ENVIO-02, SC#4)
      - `retryMessage` con `mensaje.paciente.telefono = null` y ownership válido -> rechaza y `mensajeWhatsApp.update` recibe 0 llamadas (el estado del mensaje no se pisa)
      - `retryMessage` con `profesionalId` distinto al del mensaje -> sigue lanzando `NotFoundException` con `Mensaje no encontrado`, incluso si el teléfono es null (el guard no adelanta información)
      - orden de guards preservado: un paciente con `whatsappOptIn: false` y `telefono: null` sigue fallando por el mensaje de consentimiento, no por el de teléfono
      - camino feliz: `sendTemplateMessage` con `telefono: '1123456789'` y `whatsappOptIn: true` encola exactamente una vez, y el payload del job lleva `telefono: '1123456789'` (prueba de que el guard no bloquea de más y de que se pasa el valor trimmeado, no `null`)
      - un teléfono corto pero presente (`'123'`) NO bloquea el envío: el guard mira existencia, no validez (D-03)

    Usar `await expect(...).rejects.toThrow(BadRequestException)` para las aserciones de excepción, y aserciones separadas sobre el mensaje. No mockear `axios`: los `send*` no llaman a Meta en el request (eso ocurre en el processor), y probar `queue.add` con 0 llamadas es la evidencia de "sin llegar a la API de Meta".
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run test -- whatsapp.service.spec.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `cd backend && npm run test -- whatsapp.service.spec.ts` sale 0 con al menos 10 tests ejecutados
    - Source: el archivo importa `getQueueToken` de `@nestjs/bullmq` y `WHATSAPP_QUEUE` desde `../whatsapp/processors/whatsapp-message.processor` (ruta relativa correcta desde el spec)
    - Behavior: para los 4 métodos, con teléfono ausente el mock de la cola registra `add` con 0 llamadas (`expect(mockQueue.add).not.toHaveBeenCalled()`)
    - Behavior: `sendTemplateMessage` y `sendPresupuestoPdf` con teléfono ausente registran `mensajeWhatsApp.create` con 0 llamadas
    - Behavior: `retryMessage` con teléfono ausente registra `mensajeWhatsApp.update` con 0 llamadas
    - Behavior: el test de camino feliz asserta `add` llamado 1 vez con `telefono: '1123456789'` en el payload
    - Behavior: el test de opt-in ausente falla con el mensaje de consentimiento, no con el de teléfono
  </acceptance_criteria>
  <done>Los 4 guards están probados: fallan cerrado, no encolan, no crean ni mutan registros, y no rompen el camino feliz ni el orden de guards.</done>
</task>

<task type="auto">
  <name>Task 3: Crear presupuestos.service.spec.ts y correr la verificación completa del backend</name>
  <files>backend/src/modules/presupuestos/presupuestos.service.spec.ts</files>
  <read_first>
    - backend/src/modules/presupuestos/presupuestos.service.ts (líneas 45-55: el constructor con `PrismaService`, `CuentasCorrientesService`, `PresupuestoPdfService`; líneas 417-476: `generatePdf()` completo, estado post plan 67-04)
    - backend/src/modules/presupuestos/presupuesto-pdf.service.ts (líneas 15-19: la interfaz `PresupuestoPdfData`; línea 143: el render condicional del teléfono)
    - backend/src/modules/pacientes/pacientes.service.spec.ts (líneas 1-90: patrón de `Test.createTestingModule` con Prisma mockeado, sin mock de cola)
    - .planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-04-SUMMARY.md (qué casts se quitaron)
    - .planning/ROADMAP.md (Phase 67, success criterion #5)
  </read_first>
  <action>
    Crear `backend/src/modules/presupuestos/presupuestos.service.spec.ts` — primer spec del módulo. Setup: `Test.createTestingModule` con `PresupuestosService` y tres providers mockeados: `PrismaService` (con `presupuesto.findUnique` devolviendo un presupuesto armado a mano, con `items: []`, `paciente`, `profesional` y lo que `generatePdf()` necesite para no explotar), `CuentasCorrientesService` (objeto vacío con `jest.fn()` donde haga falta) y `PresupuestoPdfService` con `generatePdfBuffer: jest.fn().mockResolvedValue(Buffer.from('pdf'))`.

    Casos a cubrir:
      - `generatePdf(id)` con un paciente `{ telefono: null }` no lanza, y el objeto que recibe `pdfService.generatePdfBuffer` tiene `paciente.telefono === null` (propagación limpia, sin coerción a cadena vacía ni a texto placeholder)
      - `generatePdf(id)` con un paciente `{ telefono: '1123456789' }` propaga el valor tal cual
      - `generatePdf(id)` con un `presupuesto.findUnique` que devuelve `null` sigue lanzando `NotFoundException('Presupuesto no encontrado')`

    Cerrar la fase corriendo la verificación completa desde `backend/`: `npm run build`, `npm run lint` y `npm run test` (suite entera, SC#5). Si `npm run test` se cuelga, reintentar con `npm run test -- --runInBand` antes de considerarlo un fallo. Si aparece algún test en rojo, determinar si es preexistente ejecutándolo sobre el commit base de la fase; los fallos preexistentes se documentan en el SUMMARY con su evidencia, los introducidos por la fase se arreglan. No marcar el plan como completo con fallos introducidos por la fase.

    Registrar en el SUMMARY el estado final de los 5 success criteria del ROADMAP, cada uno con el comando o test concreto que lo prueba.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -5 && npm run test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `cd backend && npm run test -- presupuestos.service.spec.ts` sale 0
    - Behavior: el test asserta que el payload recibido por `generatePdfBuffer` tiene `paciente.telefono === null` (estrictamente `null`, no `''` ni `undefined`)
    - Behavior: el test de presupuesto inexistente sigue obteniendo `NotFoundException` con `Presupuesto no encontrado`
    - CLI: `cd backend && npm run build` sale 0
    - CLI: `cd backend && npm run lint` sale 0
    - CLI: `cd backend && npm run test` termina sin fallos introducidos por la fase; cualquier fallo preexistente queda documentado en el SUMMARY con la evidencia de que ya fallaba en el commit base
    - SUMMARY: incluye una tabla de los 5 success criteria del ROADMAP con el comando o test que prueba cada uno
  </acceptance_criteria>
  <done>El passthrough del PDF está probado y la suite completa del backend queda en verde con los 5 success criteria trazados a evidencia concreta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suite de tests -> código de producción | Los tests son la única evidencia automatizada de que los guards fallan cerrado; un test mal construido produce un falso verde |
| specs -> servicios externos | Los mocks de cola, Prisma y encriptación reemplazan a BullMQ/Redis, Postgres y AES en los tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-20 | Repudiation | falso verde en los tests de guard | mitigate | Cada test de rechazo asserta además el conteo de llamadas a `queue.add`, `mensajeWhatsApp.create` y `mensajeWhatsApp.update`: no alcanza con que lance, tiene que no haber tenido efectos. Se incluye un camino feliz para descartar que el guard bloquee todo |
| T-67-21 | Tampering | tests preexistentes modificados para que pasen | mitigate | Criterio de aceptación explícito: el diff de `pacientes.service.spec.ts` sólo agrega bloques, y `paciente-portal.service.spec.ts` no aparece en `git diff --stat` (D-08) |
| T-67-22 | Information Disclosure | credenciales de prueba en los specs | mitigate | Los mocks usan valores obviamente ficticios (`'token-desencriptado'`); no se leen `.env*` ni se hardcodean secretos reales — `EncryptionService` se mockea entero, nunca se ejercita con una clave real |
| T-67-23 | Denial of Service | suite completa colgada | accept | Fallback documentado a `npm run test -- --runInBand`; si persiste, se reporta en vez de saltear la verificación |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala ni actualiza dependencias — Jest, `@nestjs/testing` y `@nestjs/bullmq` ya están en `package.json`. Si apareciera la necesidad de instalar algo, detener y correr el Package Legitimacy Gate antes de continuar |
</threat_model>

<verification>
- `cd backend && npm run test -- pacientes.service.spec.ts` sale 0
- `cd backend && npm run test -- whatsapp.service.spec.ts` sale 0
- `cd backend && npm run test -- presupuestos.service.spec.ts` sale 0
- `cd backend && npm run test -- paciente-portal.service.spec.ts` sale 0, con el spec sin editar
- `cd backend && npm run build` y `npm run lint` salen 0
- `cd backend && npm run test` (suite completa) sin fallos introducidos por la fase
</verification>

<success_criteria>
- SC#1 probado: alta con sólo `nombreCompleto` y `dni`
- SC#2 probado en el plan 67-01 (contraste de conteos pre/post migración), referenciado en el SUMMARY
- SC#3 y SC#4 probados: los 4 guards rechazan sin encolar, sin crear registros y sin contactar a Meta
- SC#5 probado: la suite completa del backend en verde, incluido el test WR-02 del portal sin editar
- Los tests citan las decisiones que prueban en los nombres de los `it()`
</success_criteria>

<output>
Create `.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-05-SUMMARY.md` when done
</output>
