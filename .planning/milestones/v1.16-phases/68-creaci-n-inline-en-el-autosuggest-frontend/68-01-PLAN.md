---
phase: 68-creaci-n-inline-en-el-autosuggest-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/InlineCreatePaciente.tsx
  - frontend/src/hooks/useCreatePaciente.ts
autonomous: true
requirements: [ALTA-02, ALTA-03, ALTA-05, ALTA-06]

must_haves:
  truths:
    - "D-05: el mini-form tiene exactamente tres campos — Nombre, DNI y Teléfono etiquetado (opcional) — y un teléfono vacío se envía como cadena vacía para que el service lo normalice a null"
    - "D-06: el payload del alta inline es el mismo de NewPacienteModal (estado ACTIVO, consentimientoFirmado false, indicacionesEnviadas false, profesionalId ?? undefined), sin ningún flag de ficha incompleta"
    - "D-09: un query cuyo texto, sin espacios ni puntos ni guiones, es todo dígitos precarga el campo DNI; cualquier otro query precarga el campo Nombre"
    - "D-10: el DNI queda reducido a sólo dígitos tanto en la precarga como en el valor validado y enviado"
    - "D-11: el nombre se capitaliza únicamente en la precarga y el campo queda editable, guardándose tal cual lo deje el usuario"
    - "D-12: un error 409 (o cuyo mensaje mencione DNI) se muestra bajo el campo DNI conservando lo ya cargado; cualquier otro error sale por toast.error"
    - "D-15: el schema Zod local valida nombreCompleto min 3, dni min 7 sobre el valor ya normalizado a dígitos, y telefono opcional que exige ≥6 caracteres sólo si tiene valor"
    - "ALTA-06: el paciente se crea con el profesionalId que llega por prop, sin resolverlo internamente"
    - "El mini-form no renderiza ningún elemento <form> ni ningún botón sin type=\"button\", para no submitear el turno del modal contenedor"
  artifacts:
    - path: "frontend/src/components/InlineCreatePaciente.tsx"
      provides: "Mini-form RHF+Zod de 3 campos que crea el paciente vía useCreatePaciente y devuelve el registro creado"
      min_lines: 120
      exports: ["InlineCreatePaciente", "buildPrefill"]
    - path: "frontend/src/hooks/useCreatePaciente.ts"
      provides: "Mutation POST /pacientes con invalidación de pacientes y pacientes-suggest"
      contains: "pacientes-suggest"
  key_links:
    - from: "frontend/src/components/InlineCreatePaciente.tsx"
      to: "frontend/src/hooks/useCreatePaciente.ts"
      via: "useCreatePaciente().mutate(payload, { onSuccess, onError })"
      pattern: "useCreatePaciente\\(\\)"
    - from: "frontend/src/components/InlineCreatePaciente.tsx"
      to: "el campo DNI"
      via: "setError('dni') en el branch 409"
      pattern: "setError\\(\"dni\""
---

<objective>
Construir el mini-form de alta inline como componente propio y autocontenido: `frontend/src/components/InlineCreatePaciente.tsx`. Recibe el query tipeado y el `profesionalId` por props, precarga el campo correcto, valida con Zod + React Hook Form, crea el paciente vía `useCreatePaciente` y devuelve el registro creado al padre.

Purpose: aislar toda la lógica de alta (schema, precarga, payload, ruteo de errores) fuera de `AutocompletePaciente.tsx`, para que el plan 02 sólo tenga que resolver el problema difícil de esta fase — la coordinación de dismiss entre Popover y Dialog — sin mezclarlo con la lógica de formulario.

Output: el componente nuevo + la extensión de invalidación de cache en `useCreatePaciente`. Este plan **no** monta el componente en ningún lado; eso es el plan 02.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-CONTEXT.md
@.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-PATTERNS.md

**Frontera de fase (no negociable):** este plan es 100% frontend. Nada bajo `backend/` se toca. `POST /pacientes` ya acepta un paciente sin teléfono (Phase 67, D-01) y ya devuelve `ConflictException('El DNI ingresado ya está registrado.')` en el duplicado.

**Baseline de lint (medida antes de planificar, 2026-08-18):** el repo tiene 221 errores de eslint pre-existentes en total. Por archivo tocado en este plan: `src/hooks/useCreatePaciente.ts` = 1 error / 0 warnings. `npm run lint` global **no** sale en 0 y no debe usarse como gate.

**Baseline de tipos:** `cd frontend && npx tsc --noEmit` sale limpio hoy (exit 0, sin output). Ese sí es un gate válido.

**No hay runner de tests en `frontend/`** (`package.json` sólo tiene `dev`, `build`, `start`, `lint`). La verificación automatizada de este plan es typecheck + eslint acotado a archivo + aserciones de fuente por grep.
</context>

<interfaces>
<!-- Contratos que el plan 02 va a consumir. Definirlos exactamente así. -->

Props públicas de `InlineCreatePaciente` (default export):

- `query: string` — el texto crudo tipeado en el autosuggest. Se lee **una sola vez, al montar**, para calcular la precarga.
- `profesionalId?: string | null` — el profesional del contexto del modal (D-08). Acepta `string`, `null` y `undefined` porque `QuickAppointment` lo tiene tipado `string` y los otros dos modales `string | null`.
- `onCreated: (paciente: PacienteCreado) => void` — se llama con la respuesta del POST.
- `onCancel: () => void` — el botón Cancelar.

Tipo exportado (evita `any`, que es error de eslint en este repo):

`export type PacienteCreado = { id: string; nombreCompleto: string; fotoUrl?: string | null; [key: string]: unknown }`

Helper exportado para poder auditarlo: `export function buildPrefill(query: string): { nombreCompleto: string; dni: string }`

Forma del error de axios (el repo no tiene códigos estructurados; Phase 67 D-10):
`type ApiError = { response?: { status?: number; data?: { message?: string } }; message?: string }`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Crear InlineCreatePaciente.tsx — precarga, schema Zod y los tres campos</name>

  <read_first>
    - `frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx` (molde entero: schema `:31-46`, wiring RHF `:60-71`, idiom de foco `:58`/`:76-81` y el merge de refs `:135-142`, markup de campo + `Alert variant="destructive"` `:150-165`, label de campo opcional `:184-187`, footer con `isPending` `:237-244`, y el uso de `watch`/`setValue` para el teléfono `:172-175`)
    - `frontend/src/components/AutocompletePaciente.tsx` (convención `type="button"` en `:59-68` y `:99-107`; tratamiento de modo foco con `useUIStore` en `:30` y `:78`)
    - `.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-CONTEXT.md` (D-05, D-09, D-10, D-11, D-15)
    - `.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-PATTERNS.md` (sección "The mini-form (component, CRUD)" y "Shared Patterns → No nested `<form>`")
    - `frontend/src/components/ui/input.tsx` y `frontend/src/components/ui/alert.tsx` (firmas de los componentes que se usan)
  </read_first>

  <files>frontend/src/components/InlineCreatePaciente.tsx</files>

  <action>
Crear el archivo nuevo `frontend/src/components/InlineCreatePaciente.tsx` con `"use client"` en la primera línea. Exporta por default `InlineCreatePaciente` con las props exactas del bloque `<interfaces>` de este plan, y además exporta el tipo `PacienteCreado` y la función `buildPrefill`.

Precarga (D-09/D-10/D-11) — implementarla como funciones puras exportables/auditables, no inline en el JSX:
- `stripSeparators(q)` quita espacios, puntos y guiones: reemplazo global sobre la clase `[\s.\-]`.
- La regla es "todo dígitos → DNI": si `stripSeparators(query)` es no vacío y matchea `/^\d+$/`, entonces `buildPrefill` devuelve `{ dni: <ese valor>, nombreCompleto: "" }`. En cualquier otro caso devuelve `{ dni: "", nombreCompleto: capitalizarNombre(query.trim()) }`. Casos que deben cumplirse: `"30.123.456"` → dni `"30123456"`; `"30123456"` → dni `"30123456"`; `"Juan 45"` → nombre; `"12 de Octubre"` → nombre; `""` → nombre vacío.
- `capitalizarNombre` pone en mayúscula la primera letra de cada palabra y en minúscula el resto (`"juan perez"` → `"Juan Perez"`), separando por espacios y colapsando los espacios repetidos. Se aplica **sólo** acá, en la precarga; el campo queda editable y lo que el usuario deje se envía tal cual (D-11).

Formulario (D-15) — `useForm<FormValues>` con `zodResolver(schema)` y `defaultValues` calculados **una sola vez al montar** a partir de la prop `query` (usar `useState(() => buildPrefill(query))` o `useRef`, de modo que si el usuario sigue tipeando en el input de búsqueda los campos ya cargados no se pisen). El schema local, con mensajes en español al estilo del repo:
- `nombreCompleto: z.string().min(3, "Mínimo 3 caracteres")`
- `dni: z.string().min(7, "Mínimo 7 dígitos")`
- `telefono`: `z.string().optional()` + `.refine(v => !v || v.trim() === "" || v.trim().length >= 6, { message: "Teléfono inválido" })`. **NO copiar** `NewPacienteModal.tsx:34` (`telefono: z.string().min(6)` obligatorio) — el molde correcto para "opcional pero validado si viene" es el campo `email` de `:35-41`. El umbral de 6 espeja `normalizeTelefono()` del backend (Phase 67, D-06).

Campo DNI (D-10) — **controlado**, no `register`: `value={watch("dni")}` y `onChange={(e) => setValue("dni", e.target.value.replace(/\D/g, ""), { shouldValidate: false })}`, con `inputMode="numeric"`. Es el mismo idiom `watch`/`setValue` que `NewPacienteModal.tsx:172-175` usa para el teléfono. Así el valor validado por `min(7)` y el valor enviado ya son sólo dígitos, sin normalización duplicada en el submit.

Campos Nombre y Teléfono — `{...register("nombreCompleto")}` y `{...register("telefono")}` (este último con `inputMode="tel"`). El teléfono usa `Input` plano, **no** `PhoneInput`: `frontend/src/components/PhoneInput.tsx:263-315` renderiza su propio `Popover`, y meterlo acá anidaría Popover dentro de Popover dentro de Dialog encima del riesgo de dismiss D-13/D-14 que resuelve el plan 02. Dejar esa razón como comentario de una línea junto al campo.

Labels: `Nombre completo *`, `DNI *`, y `Teléfono` seguido del span "(opcional)" con el markup de `NewPacienteModal.tsx:184-187`. Errores de cada campo con el bloque `{errors.<campo> && <Alert variant="destructive" className="py-2"><AlertDescription>{errors.<campo>.message}</AlertDescription></Alert>}`.

Foco al montar (D-04) — refs locales para Nombre y DNI, y un `useEffect` de montaje que enfoca el **primer campo vacío** en el orden Nombre → DNI: si la precarga llenó `nombreCompleto`, enfoca DNI; si llenó `dni`, enfoca Nombre. Usar el idiom `setTimeout(() => ref.current?.focus(), 50)` de `NewPacienteModal.tsx:76-81` y el merge de refs de `:135-142` para el campo registrado.

Contenedor y botones — la raíz del componente es un `<div>`, **nunca** un `<form>`: `PopoverContent` se portalea a `document.body` (`ui/popover.tsx:27`), pero los eventos sintéticos de React viajan por el árbol de React, así que un `submit` interno dispararía el `onSubmit` del `<form>` de `NewAppointmentModal`/`SurgeryAppointmentModal`. Footer con dos botones, ambos `type="button"`: "Cancelar" (`variant="outline"`, llama `onCancel`) y el de crear (llama `handleSubmit(onSubmit)` — el wiring del submit se completa en la Task 2; en esta task dejar el `onSubmit` como función que sólo arma el payload, sin `mutate` todavía). El `<div>` raíz lleva `className="p-3 grid gap-3"` porque `AutocompletePaciente` fuerza `p-0` en el `PopoverContent`.

Modo foco — llamar `useUIStore()` dentro del componente (`const { focusModeEnabled: fm } = useUIStore()`) y aplicar a los tres `Input` la misma clase condicional con `cn()` que usa `AutocompletePaciente.tsx:78`.

No usar el tipo `any` en ningún lado del archivo: `@typescript-eslint/no-explicit-any` es error en este repo.
  </action>

  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx eslint src/components/InlineCreatePaciente.tsx -f json | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const e=JSON.parse(d)[0].errorCount;console.log('errors='+e);process.exit(e===0?0:1)})"</automated>
    <automated>cd frontend && grep -c "<form" src/components/InlineCreatePaciente.tsx; test $(grep -c "<form" src/components/InlineCreatePaciente.tsx) -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `frontend/src/components/InlineCreatePaciente.tsx` existe, empieza con `"use client";` y tiene un `export default function InlineCreatePaciente`.
    - El archivo exporta `buildPrefill` y el tipo `PacienteCreado` (`grep -c "export function buildPrefill" ` ≥ 1 y `grep -c "export type PacienteCreado"` ≥ 1).
    - `grep -c "<form" src/components/InlineCreatePaciente.tsx` devuelve `0`.
    - Todo `<button>`/`<Button>` del archivo lleva `type="button"`: `grep -c 'type="button"'` ≥ 2 y `grep -cE '<(Button|button)( |$)' ` es igual a la cantidad de `type="button"`.
    - El archivo contiene la regla de dígitos del DNI: `grep -c 'replace(/\\D/g, "")'` ≥ 1.
    - El archivo contiene `z.string().min(3, "Mínimo 3 caracteres")` y `z.string().min(7, "Mínimo 7 dígitos")`.
    - El archivo **no** contiene `z.string().min(6` como regla obligatoria de teléfono; el campo `telefono` aparece con `.optional()` y un `.refine(`.
    - El archivo no importa `PhoneInput` (`grep -c "PhoneInput"` devuelve `0`) y contiene un comentario que menciona `Popover` como razón.
    - El archivo no contiene la palabra `any` como anotación de tipo (`grep -cE ':\s*any\b'` devuelve `0`).
    - `cd frontend && npx tsc --noEmit` sale con exit 0 y sin output.
    - `npx eslint src/components/InlineCreatePaciente.tsx` reporta `errorCount` 0.
  </acceptance_criteria>

  <done>El componente renderiza los tres campos con precarga, validación Zod y foco correcto, sin `<form>` interno, sin `any` y sin errores de lint ni de tipos. El botón de crear todavía no dispara el POST.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring del POST — payload, éxito con handoff y ruteo del 409</name>

  <read_first>
    - `frontend/src/components/InlineCreatePaciente.tsx` (el archivo tal como quedó en la Task 1)
    - `frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx:83-115` (forma del payload y el bloque `mutate` con `onSuccess`/`onError` — es el patrón a calcar)
    - `frontend/src/hooks/useCreatePaciente.ts` (archivo completo, 17 líneas)
    - `frontend/src/hooks/usePacienteSuggest.ts` (la queryKey es la tupla `["pacientes-suggest", debounced, profesionalId]`, `:10-11`)
    - `.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-CONTEXT.md` (D-06, D-07, D-08, D-12 y el bullet de discreción sobre la invalidación de cache)
    - `backend/src/modules/pacientes/pacientes.service.ts:54-100` (**sólo lectura**: confirmar el mensaje exacto del 409 y los defaults que fuerza el service; no editar nada de `backend/`)
  </read_first>

  <files>frontend/src/components/InlineCreatePaciente.tsx, frontend/src/hooks/useCreatePaciente.ts</files>

  <action>
En `InlineCreatePaciente.tsx`: traer `const { mutate, isPending } = useCreatePaciente()` y completar `onSubmit(data)`.

Payload (D-06) — exactamente estas claves, sin ninguna más y sin ningún flag de "ficha incompleta":
`nombreCompleto: data.nombreCompleto.trim()`, `dni: data.dni` (ya son sólo dígitos por la Task 1), `telefono: data.telefono?.trim() ?? ""`, `profesionalId: profesionalId ?? undefined`, `estado: "ACTIVO"`, `consentimientoFirmado: false`, `indicacionesEnviadas: false`. Mandar `telefono: ""` es correcto y deliberado: `normalizeTelefono()` del service lo convierte a `null` (Phase 67, D-01), y el DTO ya lo tiene `@IsOptional()`. `etapaCRM` y `flujo` **no** se mandan: el service los fuerza incondicionalmente.

`profesionalId ?? undefined` (D-08/ALTA-06) — mismo fallback que `NewPacienteModal.tsx:91`, pero el valor llega por prop en vez de resolverse con `useEffectiveProfessionalId()`. No agregar ningún guard que bloquee la creación cuando es `null`: ese caso ya está cortado aguas abajo por `NewAppointmentModal.tsx:132`.

`onSuccess` (D-07) — el primer argumento del callback es el body de la respuesta (`mutationFn` devuelve `response.data`, o sea el `Paciente` completo de Prisma, superset de `{ id, nombreCompleto, fotoUrl }`). Hacer: `toast.success(\`${creado.nombreCompleto} creado correctamente\`)` con `toast` de `sonner`, y después `onCreated(creado)`. No hacer `reset()` ni re-enfocar: el componente se desmonta cuando el padre cierra el mini-form.

`onError` (D-12) — calcar `NewPacienteModal.tsx:104-113`, tipando el error con el tipo local `ApiError` del bloque `<interfaces>` (no `any`): leer `error.response?.status` y `error.response?.data?.message ?? error.message`. Si `status === 409 || message?.includes("DNI")` → `setError("dni", { message: "Este DNI ya está registrado" })` y `return` sin tocar ningún otro campo (ALTA-05: se conserva todo lo cargado y se puede corregir). Cualquier otro caso → `toast.error(message || "Error al crear el paciente")`. No agregar un slot de error genérico dentro del popover.

Botón de crear — `disabled={isPending}` y label `{isPending ? "Creando..." : "Crear paciente"}`, siguiendo `NewPacienteModal.tsx:241-243`. El botón Cancelar también se deshabilita mientras `isPending`.

Enter dentro del mini-form — agregar `onKeyDown` en el `<div>` raíz: si `e.key === "Enter"`, llamar `e.preventDefault()` y `e.stopPropagation()` y después `handleSubmit(onSubmit)()`. Esto hace que Enter cree el paciente y garantiza que la tecla no llegue al modal contenedor. **No** interceptar `Escape` acá: el cierre por Escape lo maneja `PopoverContent` en el plan 02 (D-13).

En `frontend/src/hooks/useCreatePaciente.ts` — agregar, junto al `invalidateQueries({ queryKey: ["pacientes"] })` existente, una segunda invalidación `queryClient.invalidateQueries({ queryKey: ["pacientes-suggest"] })`. El prefijo matchea las tres posiciones de la tupla, así que cubre cualquier variante de debounce/profesional. Es el ítem de discreción de CONTEXT.md: no hace falta para ALTA-04 (el paciente se selecciona con la respuesta del POST), pero evita que una búsqueda inmediata posterior siga sin encontrarlo. No cambiar el import de `api` de este archivo (`@/lib/api`) — `usePacienteSuggest` usa `@/lib/axios` y esa divergencia se respeta por archivo.
  </action>

  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx eslint src/components/InlineCreatePaciente.tsx src/hooks/useCreatePaciente.ts -f json | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const r=JSON.parse(d);const t=r.reduce((a,f)=>a+f.errorCount,0);console.log('errors='+t);process.exit(t<=1?0:1)})"</automated>
    <automated>cd frontend && grep -q 'pacientes-suggest' src/hooks/useCreatePaciente.ts && grep -q 'setError("dni"' src/components/InlineCreatePaciente.tsx && grep -q 'consentimientoFirmado: false' src/components/InlineCreatePaciente.tsx && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `src/components/InlineCreatePaciente.tsx` contiene las siete claves del payload: `nombreCompleto`, `dni`, `telefono`, `profesionalId: profesionalId ?? undefined`, `estado: "ACTIVO"`, `consentimientoFirmado: false`, `indicacionesEnviadas: false`.
    - El archivo **no** contiene `etapaCRM`, `flujo` ni ninguna clave que insinúe ficha incompleta (`grep -ciE "etapaCRM|fichaIncompleta"` devuelve `0`).
    - El archivo contiene `status === 409` y `includes("DNI")` en la misma condición, seguida de `setError("dni"`.
    - El archivo contiene `toast.success(` y `toast.error(` importados de `sonner`.
    - El archivo contiene `onCreated(` dentro del callback `onSuccess`.
    - El archivo contiene `isPending` usado como `disabled` y el label `"Creando..."`.
    - El archivo maneja Enter: contiene `e.key === "Enter"` junto a `preventDefault()` y `stopPropagation()`.
    - El archivo **no** contiene `onEscapeKeyDown` ni intercepta `Escape` (`grep -c "Escape"` devuelve `0`).
    - `src/hooks/useCreatePaciente.ts` contiene exactamente dos llamadas a `invalidateQueries`, una con `["pacientes"]` y otra con `["pacientes-suggest"]`.
    - `src/hooks/useCreatePaciente.ts` sigue importando `api` desde `@/lib/api`.
    - `cd frontend && npx tsc --noEmit` sale con exit 0 y sin output.
    - `npx eslint src/components/InlineCreatePaciente.tsx src/hooks/useCreatePaciente.ts` reporta como máximo 1 error en total (el error pre-existente `no-explicit-any` de `useCreatePaciente.ts:8`), y 0 en el archivo nuevo.
    - `git status --porcelain backend/` no lista ningún archivo.
  </acceptance_criteria>

  <done>El mini-form crea el paciente contra `POST /pacientes`, devuelve el registro creado por `onCreated`, pinta el 409 bajo el campo DNI sin perder lo cargado, y cualquier otro error sale por toast. La cache de sugerencias se invalida junto con la de pacientes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `POST /pacientes` | El payload del mini-form cruza acá; el backend es la autoridad (validación DTO + índice único de DNI + defaults de CRM forzados en el service) |
| input del usuario → estado del form | Texto libre tipeado por la secretaria/profesional; se normaliza en cliente por integridad de datos, no como control de seguridad |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Elevation of Privilege | `POST /pacientes` desde el mini-form | accept | El endpoint ya está gateado por `@Auth('ADMIN','PROFESIONAL','SECRETARIA','FACTURADOR')` (`pacientes.controller.ts:38, 48-51`) y esos mismos 4 roles ya llegan a los modales de turno. **No se agrega gating extra en la UI**: sería un control redundante que no cambia la superficie real. |
| T-68-02 | Tampering | payload del alta inline | mitigate | El payload se limita a las 7 claves de D-06; `etapaCRM`/`flujo` no se mandan y el service los fuerza incondicionalmente (`pacientes.service.ts:69-70`), así que un cliente no puede sembrar un lead en una etapa arbitraria del embudo. |
| T-68-03 | Information Disclosure | mensajes de error del backend renderizados en el form | accept | Los errores son `BadRequestException`/`ConflictException` planos con texto en español ya pensado para el usuario final (Phase 67, D-10). No se renderiza stack ni objeto de error crudo: sólo `error.response.data.message`. |
| T-68-04 | Tampering (integridad de datos, no seguridad) | normalización de DNI a dígitos (D-10) | mitigate | La normalización en cliente evita que `30.123.456` y `30123456` convivan como identidades distintas y que el 409 nunca dispare. **Es una medida de integridad, no una frontera de seguridad** — el índice único de Postgres sigue siendo la autoridad y el servidor rechaza el duplicado aunque el cliente mande basura. |
| T-68-05 | Tampering | dependencias | accept | Este plan no instala ningún paquete: `react-hook-form`, `zod`, `@hookform/resolvers`, `sonner` y `lucide-react` ya están en `frontend/package.json`. Sin superficie de supply-chain nueva, no aplica el gate de legitimidad de paquetes. |

**Severidad:** ninguna amenaza de este plan llega a `high`. La razón es que el plan no agrega un endpoint, no cambia authz, no toca almacenamiento ni secretos, y no amplía qué datos puede escribir un rol que ya podía escribirlos por otra pantalla (`NewPacienteModal`). El peor caso realista es un paciente creado con datos mal tipeados, corregible desde la ficha.
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` → exit 0, sin output (baseline limpio, no puede regresionar).
2. `cd frontend && npx eslint src/components/InlineCreatePaciente.tsx` → `errorCount` 0.
3. `cd frontend && npx eslint src/hooks/useCreatePaciente.ts` → `errorCount` ≤ 1 (baseline).
4. `git status --porcelain backend/` → vacío (frontera de fase).
5. `grep -c "<form" frontend/src/components/InlineCreatePaciente.tsx` → 0.
</verification>

<success_criteria>
- Existe `frontend/src/components/InlineCreatePaciente.tsx` con la API pública del bloque `<interfaces>`, listo para que el plan 02 lo monte sin adaptador.
- La precarga cumple los 5 casos de D-09 y el DNI queda en dígitos (D-10); el nombre se capitaliza sólo al precargar (D-11).
- El schema Zod tiene el `telefono` opcional de D-15, no el obligatorio de `NewPacienteModal`.
- El 409 aterriza bajo el campo DNI conservando lo cargado (ALTA-05); el resto sale por toast.
- No hay `<form>` interno ni botones sin `type="button"`.
- Nada bajo `backend/` fue modificado.
</success_criteria>

<output>
Create `.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-01-SUMMARY.md` when done
</output>
