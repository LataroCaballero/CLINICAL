---
phase: 68-creaci-n-inline-en-el-autosuggest-frontend
plan: 03
type: execute
wave: 3
depends_on: ["68-02"]
files_modified:
  - frontend/src/app/dashboard/turnos/NewAppointmentModal.tsx
  - frontend/src/app/dashboard/turnos/SurgeryAppointmentModal.tsx
  - frontend/src/app/dashboard/components/QuickAppointment.tsx
autonomous: false
requirements: [ALTA-04, ALTA-06, ALTA-07]

must_haves:
  truths:
    - "D-07: crear el paciente desde cualquiera de los tres modales lo deja seleccionado en la cajita indigo con el popover cerrado, y el turno se confirma desde ahí sin pasos extra"
    - "D-08: cada modal pasa a `profesionalIdParaAlta` exactamente la misma expresión que ya usa para el `profesionalId` del turno — `effectiveProfessionalId` en `NewAppointmentModal` y `SurgeryAppointmentModal`, la prop `profesionalId` en `QuickAppointment`; ningún call site resuelve el profesional por su cuenta para el alta"
    - "ALTA-06: el paciente creado inline queda bajo el mismo profesional al que se le está agendando el turno, incluso en `QuickAppointment`, donde el filtro de búsqueda resuelve el profesional por otra vía (`useEffectiveProfessionalId` dentro de `usePacienteSuggest`) y puede diverger de la prop"
    - "ALTA-07: `PatientFilters.tsx` y `data-table-toolbar.tsx` no contienen `allowCreate` ni `profesionalIdParaAlta`, y no aparecen en el diff de este plan"
    - "ALTA-04: con el mini-form abierto dentro de `NewAppointmentModal` y `SurgeryAppointmentModal` —los dos únicos modales que tienen `<form onSubmit>`— crear el paciente (con Enter o con el botón) crea el paciente y **no** submitea el turno"
    - "En `SurgeryAppointmentModal` la creación se habilita únicamente en la rama `else` del ternario `pacienteIdProp`: con paciente preseleccionado desde CRM el bloque read-only queda idéntico"
    - "Los tres call sites typechequean con la firma de props que definió el plan 02, pese a que `QuickAppointment` pasa un `string` no nullable y los otros dos un `string | null`"
    - "El comportamiento de dismiss del Popover dentro del Dialog (D-13/D-14) queda verificado manualmente en los tres modales, no en uno solo, y el resultado queda registrado en el SUMMARY"
  artifacts:
    - path: "frontend/src/app/dashboard/turnos/NewAppointmentModal.tsx"
      provides: "Call site con creación inline habilitada y el profesional del turno propagado al alta"
      contains: "profesionalIdParaAlta={effectiveProfessionalId}"
    - path: "frontend/src/app/dashboard/turnos/SurgeryAppointmentModal.tsx"
      provides: "Call site con creación inline habilitada sólo en la rama sin paciente preseleccionado"
      contains: "profesionalIdParaAlta={effectiveProfessionalId}"
    - path: "frontend/src/app/dashboard/components/QuickAppointment.tsx"
      provides: "Call site con creación inline habilitada y la prop propia de profesional propagada al alta"
      contains: "profesionalIdParaAlta={profesionalId}"
  key_links:
    - from: "frontend/src/app/dashboard/turnos/NewAppointmentModal.tsx"
      to: "frontend/src/components/AutocompletePaciente.tsx"
      via: "allowCreate + profesionalIdParaAlta={effectiveProfessionalId}"
      pattern: "profesionalIdParaAlta=\\{effectiveProfessionalId\\}"
    - from: "frontend/src/app/dashboard/components/QuickAppointment.tsx"
      to: "frontend/src/components/AutocompletePaciente.tsx"
      via: "allowCreate + profesionalIdParaAlta={profesionalId} (la misma variable del payload del turno)"
      pattern: "profesionalIdParaAlta=\\{profesionalId\\}"
    - from: "el onCreated del mini-form"
      to: "el estado de paciente del modal"
      via: "onSelect existente → setValue(\"pacienteId\", pac.id) / setPaciente(p)"
      pattern: "setValue\\(\"pacienteId\", pac.id\\)"
---

<objective>
Encender la creación inline en los tres modales de turno y dejar demostrado que **no** se encendió en ningún otro lado. Este es el plan donde el opt-in que construyó el plan 02 se convierte en comportamiento observable: tres call sites reciben las dos props nuevas, cada uno con la expresión de profesional que ya usa para el turno, y los dos usos de filtro se auditan como no-regresión en vez de asumirse intactos.

Purpose: ALTA-07 no es "la prop tiene default false" —eso es un mecanismo—, es "aparece acá y no aparece allá", y sólo se puede probar en los call sites. ALTA-06 tampoco se realiza en el componente: se realiza en qué valor le pasa cada modal. Y ALTA-04 recién se vuelve verificable end-to-end cuando hay un modal real alrededor del autosuggest.

Output: los tres modales modificados, la auditoría del fence, y la verificación manual cross-modal del riesgo técnico central de la fase (Popover dentro de Dialog, D-13/D-14) que los planes 01 y 02 no podían ejecutar. Este plan **no** toca `AutocompletePaciente.tsx` ni `InlineCreatePaciente.tsx`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-CONTEXT.md
@.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-PATTERNS.md
@.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-01-SUMMARY.md
@.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-02-SUMMARY.md

**Frontera de fase:** frontend puro. Nada bajo `backend/` se modifica. `POST /pacientes` ya está gateado por `@Auth('ADMIN','PROFESIONAL','SECRETARIA','FACTURADOR')` (`pacientes.controller.ts:38, 48-51`) y esos cuatro roles ya llegan a los tres modales de turno — no hace falta ni corresponde agregar gating en la UI.

**Este plan no toca `AutocompletePaciente.tsx`.** Toda la lógica ya está en el componente (plan 02); acá sólo se pasan props. Esto también mantiene el archivo libre para la Phase 69, que modifica su línea `:121`.

**Baseline de lint por archivo (medida 2026-08-19, antes de tocar nada):**
- `src/app/dashboard/turnos/NewAppointmentModal.tsx` = 1 error / 1 warning
- `src/app/dashboard/turnos/SurgeryAppointmentModal.tsx` = 1 error / 1 warning
- `src/app/dashboard/components/QuickAppointment.tsx` = 2 errores / 0 warnings

Ese es el techo por archivo. `npm run lint` global tiene 221 errores pre-existentes en el repo y **no** sirve como gate.

**Baseline de tipos:** `cd frontend && npx tsc --noEmit` sale limpio (exit 0, sin output). Ese sí es un gate válido, y es el que prueba que las dos firmas de profesional (`string` en `QuickAppointment`, `string | null` en los otros dos) entran en la prop que definió el plan 02.

**No hay runner de tests en `frontend/`** (`package.json` sólo tiene `dev`, `build`, `start`, `lint`). La verificación automatizada de este plan es typecheck + eslint acotado por archivo + aserciones de fuente por grep + aserciones de `git status`. El comportamiento de dismiss Popover-dentro-de-Dialog no es demostrable por comando: va a la Task 3, que es un checkpoint humano (por eso `autonomous: false`).
</context>

<interfaces>
<!-- Contrato definido por el plan 02. Usarlo tal cual — los nombres son exactos, no aproximados. -->

`AutocompletePaciente` (default export de `@/components/AutocompletePaciente`) — props existentes, todas opcionales salvo `onSelect`:
- `onSelect: (paciente: any) => void`
- `value?: string`
- `avatarUrl?: string | null`
- `onClear?: () => void`

Props que agregó el plan 02 (ambas opcionales — de eso depende ALTA-07):
- `allowCreate?: boolean` — default `false` en el destructuring del componente. Es el interruptor: sin ella, la condición de apertura del popover y su contenido colapsan exactamente a los de antes.
- `profesionalIdParaAlta?: string | null` — el profesional bajo el que se crea el paciente (D-08). Acepta `string`, `null` y `undefined`.

Camino del paciente creado (D-07 / ALTA-04, ya cableado en el plan 02): el mini-form llama `onCreated(pacienteCreado)`, y `AutocompletePaciente` responde con `onSelect(pac)` + `setQuery("")` + `setCreating(false)`. O sea: **el call site no necesita ningún handler nuevo.** El `onSelect` que ya tiene recibe el paciente recién creado por el mismo camino que uno elegido de la lista, porque `POST /pacientes` devuelve el `Paciente` completo de Prisma, superset de `{ id, nombreCompleto, fotoUrl }`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Habilitar la creación inline en NewAppointmentModal y SurgeryAppointmentModal</name>

  <read_first>
    - `frontend/src/app/dashboard/turnos/NewAppointmentModal.tsx` (archivo completo: la declaración `const effectiveProfessionalId = useEffectiveProfessionalId()` en `:74`, el guard `if (!effectiveProfessionalId)` en `:131`, el guard `if (!data.pacienteId)` en `:136`, el `profesionalId: effectiveProfessionalId` del payload del turno en `:161`, el `<form onSubmit={handleSubmit(onSubmit)}>` en `:210` y el call site en `:214-227`)
    - `frontend/src/app/dashboard/turnos/SurgeryAppointmentModal.tsx` (archivo completo: `useEffectiveProfessionalId()` en `:86`, el `profesionalId: effectiveProfessionalId` del payload en `:146`, el guard en `:178`, el `<form onSubmit={handleSubmit(onSubmit)}>` en `:214` y el ternario `pacienteIdProp ? <div read-only> : <AutocompletePaciente …>` en `:218-237`)
    - `frontend/src/components/AutocompletePaciente.tsx` (**el archivo tal como lo dejó el plan 02**, no como estaba antes: leer el bloque `type Props` y el destructuring de la firma para confirmar los nombres reales de las dos props nuevas y sus tipos, en vez de asumirlos)
    - `.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-02-SUMMARY.md` (qué quedó efectivamente implementado y qué se desvió del plan, si algo)
    - `.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-CONTEXT.md` (D-07, D-08 y la lista de call sites de `<canonical_refs>`)
  </read_first>

  <files>frontend/src/app/dashboard/turnos/NewAppointmentModal.tsx, frontend/src/app/dashboard/turnos/SurgeryAppointmentModal.tsx</files>

  <action>
En `NewAppointmentModal.tsx`, en el `<AutocompletePaciente>` de `:214-227`, agregar exactamente dos props y nada más: `allowCreate` (en forma corta de JSX, sin `={true}`) y `profesionalIdParaAlta={effectiveProfessionalId}`. `effectiveProfessionalId` es la constante que ya existe en `:74` — **no** llamar `useEffectiveProfessionalId()` una segunda vez ni introducir una variable intermedia: D-08 exige una sola fuente de verdad por modal, y la prueba de ALTA-06 es que la expresión pasada al alta sea literalmente la misma que la de `profesionalId: effectiveProfessionalId` en el payload del turno (`:161`).

No tocar `value`, `avatarUrl`, `onClear` ni `onSelect`. En particular **no** agregar un handler nuevo para el paciente creado: el plan 02 cablea `onCreated` contra el `onSelect` existente, así que el `setValue("pacienteId", pac.id)` / `setValue("pacienteNombre", pac.nombreCompleto)` / `setPacienteFotoUrl(pac.fotoUrl || null)` que ya está es lo que satisface ALTA-04, y es lo que hace que el guard `if (!data.pacienteId)` de `:136` pase después de un alta inline.

En `SurgeryAppointmentModal.tsx`, mismas dos props en el `<AutocompletePaciente>` de `:223-236`, con `profesionalIdParaAlta={effectiveProfessionalId}` (la constante de `:86`). **Sólo en la rama `else` del ternario** `pacienteIdProp ? … : …`: cuando el paciente viene preseleccionado desde el CRM se renderiza el `<div>` read-only y no hay autosuggest, así que ese bloque no se toca ni se le agrega nada.

No agregar imports nuevos en ninguno de los dos archivos: `AutocompletePaciente` y `useEffectiveProfessionalId` ya están importados en ambos (`:37` y `:39` en `NewAppointmentModal`).

No introducir ningún `<form>` nuevo ni mover el existente. Cada archivo tiene exactamente un `<form onSubmit={handleSubmit(onSubmit)}>` y debe seguir teniendo exactamente uno: es la superficie contra la que se prueba, en la Task 3, que el mini-form no submitea el turno (los eventos sintéticos de React viajan por el árbol de React aunque el `PopoverContent` se portalee a `document.body`).
  </action>

  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && grep -q 'profesionalIdParaAlta={effectiveProfessionalId}' src/app/dashboard/turnos/NewAppointmentModal.tsx && grep -q 'profesionalIdParaAlta={effectiveProfessionalId}' src/app/dashboard/turnos/SurgeryAppointmentModal.tsx && echo OK</automated>
    <automated>cd frontend && npx eslint src/app/dashboard/turnos/NewAppointmentModal.tsx src/app/dashboard/turnos/SurgeryAppointmentModal.tsx -f json | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const r=JSON.parse(d);r.forEach(f=>console.log(f.filePath.split('/src/')[1],f.errorCount));const bad=r.some(f=>f.errorCount>1);process.exit(bad?1:0)})"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'allowCreate' src/app/dashboard/turnos/NewAppointmentModal.tsx` devuelve `1`, y lo mismo en `SurgeryAppointmentModal.tsx`.
    - `grep -c 'profesionalIdParaAlta={effectiveProfessionalId}'` devuelve `1` en cada uno de los dos archivos.
    - Ninguno de los dos archivos contiene `useEffectiveProfessionalId()` más de una vez (`grep -c 'useEffectiveProfessionalId()'` devuelve `1` en cada uno) — la prop reusa la constante existente.
    - El `onSelect` del call site queda intacto: `grep -c 'setValue("pacienteId", pac.id)'` devuelve `1` en cada archivo.
    - `SurgeryAppointmentModal.tsx` tiene exactamente un `<AutocompletePaciente` (`grep -c '<AutocompletePaciente'` devuelve `1`) y conserva el bloque read-only del ternario (`grep -c 'pacienteNombreProp || pacienteIdProp'` devuelve `1`), lo que prueba que las props nuevas cayeron en la rama `else`.
    - Cada archivo sigue teniendo exactamente un `<form` (`grep -c '<form'` devuelve `1` en cada uno).
    - No hay imports nuevos: `git diff -- src/app/dashboard/turnos/NewAppointmentModal.tsx src/app/dashboard/turnos/SurgeryAppointmentModal.tsx | grep -c '^+import'` devuelve `0`.
    - `cd frontend && npx tsc --noEmit` sale con exit 0 y sin output.
    - `npx eslint` reporta `errorCount` ≤ 1 en cada uno de los dos archivos (el baseline medido).
    - `git status --porcelain backend/` no lista ningún archivo.
  </acceptance_criteria>

  <done>Los dos modales con `<form>` habilitan la creación inline y le pasan al alta exactamente el mismo `effectiveProfessionalId` que le pasan al turno. El paciente creado entra por el `onSelect` que ya existía, sin adaptador ni handler nuevo.</done>
</task>

<task type="auto">
  <name>Task 2: Habilitar en QuickAppointment, documentar la divergencia de profesional y auditar el fence de ALTA-07</name>

  <read_first>
    - `frontend/src/app/dashboard/components/QuickAppointment.tsx` (archivo completo: `type Props = { profesionalId: string }` en `:147-149`, la firma `export default function QuickAppointment({ profesionalId }: Props)` en `:151`, `useAgenda(profesionalId)` en `:155`, los dos usos de `profesionalId` en los payloads de `:183` y `:211`, y el call site en `:363-367`. Confirmar también que el archivo **no** tiene ningún `<form>`)
    - `frontend/src/components/AutocompletePaciente.tsx` (tal como lo dejó el plan 02: los nombres y tipos reales de las dos props, y el comentario que el plan 02 dejó sobre la divergencia D-08/ALTA-06)
    - `frontend/src/hooks/usePacienteSuggest.ts` (archivo completo — la razón de la divergencia: resuelve el profesional internamente con `useEffectiveProfessionalId()`, no por prop)
    - `frontend/src/hooks/useEffectiveProfessionalId.ts` (archivo completo, 15 líneas: devuelve `selectedProfessionalId` para ADMIN/SECRETARIA/FACTURADOR y el propio `profesionalId` para PROFESIONAL)
    - `frontend/src/app/dashboard/pacientes/components/PatientFilters.tsx` (**sólo lectura** — el call site de filtro en `:38-42`)
    - `frontend/src/components/data-table/data-table-toolbar.tsx` (**sólo lectura** — el call site de filtro en `:66-81`)
    - `.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-PATTERNS.md` (el hallazgo #7 sobre la divergencia y la sección de `QuickAppointment`)
  </read_first>

  <files>frontend/src/app/dashboard/components/QuickAppointment.tsx</files>

  <action>
En `QuickAppointment.tsx`, en el `<AutocompletePaciente>` de `:363-367`, agregar `allowCreate` (forma corta de JSX) y `profesionalIdParaAlta={profesionalId}` — la prop del componente declarada en `:148`, tipada `string` no nullable. Es exactamente la misma variable que el archivo usa en los payloads de `:183` y `:211`, que es lo que D-08 pide ("cada modal le pasa exactamente el mismo valor que usa para el turno") y lo que hace verdadero ALTA-06 acá. No llamar `useEffectiveProfessionalId()` en este archivo: hoy no lo importa y no debe empezar a hacerlo.

No tocar `onSelect={(p) => setPaciente(p)}`, `value` ni `avatarUrl`. Este call site no pasa `onClear` y sigue sin pasarlo.

Dejar un comentario JSX de 2-3 líneas inmediatamente arriba del `<AutocompletePaciente>` que registre la divergencia del hallazgo #7, citando `D-08` y `ALTA-06` textualmente: la búsqueda del autosuggest filtra por el profesional que `usePacienteSuggest` resuelve internamente con `useEffectiveProfessionalId()`, mientras que el alta usa esta prop; en `NewAppointmentModal` y `SurgeryAppointmentModal` son el mismo valor, pero acá pueden diverger si el profesional seleccionado en el contexto global no es el de esta agenda. La resolución elegida es priorizar el profesional del turno (esta prop), porque el paciente debe quedar bajo quien lo va a atender; cambiar el filtro de búsqueda está fuera de alcance de la fase. El comentario es el entregable — no cambiar el hook ni el filtro.

Auditoría del fence (ALTA-07) — **verificación, no edición**. `PatientFilters.tsx` y `data-table-toolbar.tsx` no se abren para escribir. Correr y registrar en el SUMMARY:
- que ninguno de los dos contiene `allowCreate` ni `profesionalIdParaAlta`,
- que `git status --porcelain` sobre esos dos paths no devuelve nada,
- que `git diff --stat` sobre esos dos paths no devuelve nada.

Si alguna de las tres sale distinto, es un fallo del plan: revertir el archivo y volver a intentar sólo sobre `QuickAppointment.tsx`.
  </action>

  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && grep -q 'profesionalIdParaAlta={profesionalId}' src/app/dashboard/components/QuickAppointment.tsx && test $(grep -c 'useEffectiveProfessionalId' src/app/dashboard/components/QuickAppointment.tsx) -eq 0 && echo OK</automated>
    <automated>cd frontend && test $(grep -c 'allowCreate\|profesionalIdParaAlta' src/app/dashboard/pacientes/components/PatientFilters.tsx) -eq 0 && test $(grep -c 'allowCreate\|profesionalIdParaAlta' src/components/data-table/data-table-toolbar.tsx) -eq 0 && echo FENCE_OK</automated>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL && test -z "$(git status --porcelain frontend/src/app/dashboard/pacientes/components/PatientFilters.tsx frontend/src/components/data-table/data-table-toolbar.tsx)" && test -z "$(git diff --stat -- frontend/src/app/dashboard/pacientes/components/PatientFilters.tsx frontend/src/components/data-table/data-table-toolbar.tsx)" && echo FENCE_UNTOUCHED</automated>
    <automated>cd frontend && npx eslint src/app/dashboard/components/QuickAppointment.tsx -f json | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const e=JSON.parse(d)[0].errorCount;console.log('errors='+e);process.exit(e<=2?0:1)})"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'allowCreate' src/app/dashboard/components/QuickAppointment.tsx` devuelve `1` y `grep -c 'profesionalIdParaAlta={profesionalId}'` devuelve `1`.
    - `grep -c 'useEffectiveProfessionalId' src/app/dashboard/components/QuickAppointment.tsx` devuelve `0` (el profesional del alta sale de la prop, no del hook).
    - `grep -c 'onSelect={(p) => setPaciente(p)}'` devuelve `1` (el handler existente quedó intacto).
    - El archivo contiene un comentario que menciona a la vez `D-08` y `ALTA-06` (`grep -c 'D-08'` ≥ 1 y `grep -c 'ALTA-06'` ≥ 1).
    - `grep -c '<form' src/app/dashboard/components/QuickAppointment.tsx` sigue devolviendo `0` (este modal nunca tuvo `<form>` y no se le agrega uno).
    - `grep -cE 'allowCreate|profesionalIdParaAlta' src/app/dashboard/pacientes/components/PatientFilters.tsx` devuelve `0`.
    - `grep -cE 'allowCreate|profesionalIdParaAlta' src/components/data-table/data-table-toolbar.tsx` devuelve `0`.
    - `git status --porcelain` sobre esos dos paths devuelve cadena vacía, y `git diff --stat` sobre esos dos paths también.
    - El diff de este plan no incluye `frontend/src/components/AutocompletePaciente.tsx` ni `frontend/src/components/InlineCreatePaciente.tsx`.
    - `cd frontend && npx tsc --noEmit` sale con exit 0 y sin output — esto es lo que prueba que un `string` no nullable y un `string | null` entran ambos en `profesionalIdParaAlta`.
    - `npx eslint src/app/dashboard/components/QuickAppointment.tsx` reporta `errorCount` ≤ 2 (el baseline).
    - `git status --porcelain backend/` no lista ningún archivo.
  </acceptance_criteria>

  <done>Los tres call sites de turno tienen la creación habilitada con el profesional correcto por modal, la divergencia del hallazgo #7 queda registrada en el código, y los dos usos de filtro están auditados —no asumidos— como intactos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación manual cross-modal — dismiss, no-submit y turno confirmado</name>

  <read_first>
    - `.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-CONTEXT.md` (D-01, D-04, D-07, D-13, D-14 y la nota de `<code_context>`: "merece verificación manual en los tres modales, no sólo en uno")
    - `.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-PATTERNS.md` (sección "No Analog Found" — no hay precedente en el repo de un Popover que se niegue a cerrar, ni de `onEscapeKeyDown`/`onPointerDownOutside`; y el hallazgo #5: `QuickAppointment` no tiene `<form>`, así que no puede probar el punto del submit)
    - `frontend/src/app/dashboard/turnos/NewAppointmentModal.tsx` y `frontend/src/app/dashboard/turnos/SurgeryAppointmentModal.tsx` (tal como quedaron en la Task 1)
  </read_first>

  <what-built>
Los tres modales de turno (`QuickAppointment` en el dashboard, `NewAppointmentModal` y `SurgeryAppointmentModal` en Turnos) ahora ofrecen crear un paciente desde adentro del popover del buscador: fila `Crear paciente: "…"`, mini-form de Nombre + DNI + Teléfono (opcional), y el paciente creado queda seleccionado para seguir agendando. Los dos usos de filtro del autosuggest (Pacientes → barra de búsqueda, y la toolbar de las tablas) quedaron sin cambios.

Todo lo automatizable ya se verificó por comando: typecheck limpio, lint dentro del baseline por archivo, las props presentes en los tres call sites y ausentes en los dos de filtro, y los dos archivos del fence sin diff. Lo que queda es lo único que ningún comando puede probar en este repo (no hay runner de tests en `frontend/`): la interacción entre el Popover de Radix y el Dialog de Radix que lo contiene.
  </what-built>

  <how-to-verify>
Levantar `cd frontend && npm run dev` y recorrer los **tres** modales. Para cada uno, correr los 8 pasos:

**Modal A — `NewAppointmentModal`:** Turnos → botón de nuevo turno.
**Modal B — `SurgeryAppointmentModal`:** Turnos → "¿Querés programar una cirugía?" (sin paciente preseleccionado desde CRM).
**Modal C — `QuickAppointment`:** Dashboard → agendar turno rápido.

1. **Fila de crear (D-01/D-03):** escribir ≥3 caracteres de un nombre que no exista (ej. `Zzqx Prueba`). Esperar a que el spinner termine. Debe aparecer la fila `Crear paciente: "Zzqx Prueba"` al pie del popover. Con 2 caracteres **no** debe aparecer.
2. **Precarga (ALTA-03):** clickear la fila. Se despliega el mini-form dentro del mismo popover, con el campo **Nombre** precargado y capitalizado (`Zzqx Prueba`), el foco en el campo DNI, y el modal de turno todavía abierto detrás.
3. **Congelamiento (D-04):** escribir en el input de búsqueda de arriba. El mini-form debe seguir ahí, sin ser reemplazado por resultados.
4. **Click afuera (D-14):** clickear en el cuerpo del modal de turno, fuera del popover (ej. sobre la etiqueta "Tipo de turno"). El mini-form **no** se cierra.
5. **Escape (D-13):** cargar antes fecha / tipo de turno / observaciones; después, con el mini-form abierto, presionar Escape. Debe cerrarse **sólo** el mini-form. El modal de turno sigue abierto y con fecha, tipo y observaciones intactos.
6. **Crear con Enter, sin submitear el turno (ALTA-04, crítico en A y B):** reabrir el mini-form, cargar el DNI y presionar Enter. Debe aparecer el toast verde `… creado correctamente`, el popover cerrarse y el paciente quedar en la cajita indigo. **No** debe aparecer el toast `Turno creado correctamente` ni cerrarse el modal — eso sería el `<form>` del turno submiteando. Repetir con el botón "Crear paciente" en vez de Enter. En el modal C esto no puede fallar (no tiene `<form>`), pero se corre igual para confirmar que nada más se rompe.
7. **Confirmar el turno (ALTA-04):** con el paciente recién creado seleccionado, completar fecha y tipo y confirmar. El turno se crea sin pedir volver a elegir paciente.
8. **DNI duplicado (ALTA-05):** repetir el flujo con el DNI del paciente recién creado. El error debe salir **bajo el campo DNI**, dentro del popover, conservando el nombre y el teléfono ya cargados, y debe poder corregirse sin reabrir nada.

**Chequeo de profesional (ALTA-06/D-08)** — una sola vez, en el modal C: crear un paciente desde `QuickAppointment` de un profesional determinado, luego ir a Pacientes con **ese** profesional seleccionado en el contexto y confirmar que el paciente aparece en su lista. Si el contexto global tenía otro profesional seleccionado, este es exactamente el escenario del hallazgo #7: el paciente debe quedar bajo el profesional de la agenda de `QuickAppointment`, no bajo el del contexto global.

**Chequeo del fence (ALTA-07)** — dos lugares donde la creación **no** debe existir:
- Pacientes → barra de búsqueda de arriba (`PatientFilters`): escribir un nombre inexistente. **No** debe aparecer ninguna fila de crear; el popover se comporta igual que antes (se cierra con click afuera, no muestra nada cuando no hay resultados).
- Cualquier tabla con la toolbar de filtro (`data-table-toolbar`): mismo chequeo.
  </how-to-verify>

  <resume-signal>Escribir "approved" si los 8 pasos pasan en los tres modales y los dos chequeos de fence salen limpios. Si algo falla, describir cuál paso, en cuál modal y qué pasó en vez de lo esperado.</resume-signal>

  <acceptance_criteria>
    - El `68-03-SUMMARY.md` incluye una tabla con los 8 pasos × 3 modales (24 celdas) y el resultado registrado de cada una — no un "verificado OK" agregado.
    - El SUMMARY registra por separado el resultado del chequeo de profesional (ALTA-06/D-08) en `QuickAppointment`, indicando qué profesional tenía la agenda, cuál el contexto global, y bajo cuál quedó el paciente.
    - El SUMMARY registra el resultado de los dos chequeos de fence (`PatientFilters` y `data-table-toolbar`) con la respuesta explícita a "¿apareció la fila de crear?".
    - Los pasos 5 y 6 están registrados como pasados en **`NewAppointmentModal` y `SurgeryAppointmentModal`** específicamente; un resultado sólo de `QuickAppointment` no cuenta para el paso 6, porque ese archivo no tiene `<form>` y no puede exponer el fallo.
    - Cualquier paso fallado queda anotado con el modal y el síntoma, y se abre trabajo de corrección antes de cerrar la fase — no se acepta como "menor".
  </acceptance_criteria>

  <done>Los 8 pasos pasan en los tres modales, el paciente creado queda bajo el profesional correcto, y la creación no aparece en ninguno de los dos usos de filtro. El riesgo técnico central de la fase (Popover dentro de Dialog, sin precedente en el repo) queda cerrado con evidencia registrada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| call site → `AutocompletePaciente` | El valor que cada modal pasa en `profesionalIdParaAlta` determina bajo qué profesional queda el paciente creado; es la única entrada que decide el scoping del alta |
| superficie de UI → `POST /pacientes` | Este plan decide **en qué pantallas** existe un camino de escritura hacia el endpoint de alta de pacientes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-11 | Elevation of Privilege | `POST /pacientes` alcanzado desde los tres modales de turno | accept | `POST /pacientes` ya está gateado por `@Auth('ADMIN','PROFESIONAL','SECRETARIA','FACTURADOR')` (`pacientes.controller.ts:38, 48-51`), y **esos mismos cuatro roles ya llegan hoy a los tres modales de turno y ya pueden crear pacientes desde `NewPacienteModal`**. Habilitar la creación inline no agrega ningún rol nuevo a la superficie de escritura. Se deja constancia explícita de que **no se agrega gating en la UI**: sería un control redundante que no cambia quién puede escribir. |
| T-68-12 | Tampering (scoping multi-profesional) | `profesionalIdParaAlta` en `QuickAppointment` vs. el profesional interno de `usePacienteSuggest` | mitigate | Es el riesgo real de este plan y es el hallazgo #7. Cada modal pasa **la misma expresión que usa para el `profesionalId` del turno** (`effectiveProfessionalId` en `NewAppointmentModal:161` y `SurgeryAppointmentModal:146`; la prop `profesionalId` en `QuickAppointment:183/211`), de modo que el paciente queda bajo quien lo va a atender (ALTA-06). En `QuickAppointment` la búsqueda puede estar filtrada por otro profesional (el del contexto global) mientras el alta usa el de la agenda: la divergencia se resuelve a favor del profesional del turno, queda documentada en el código (Task 2) y se verifica manualmente (Task 3). Impacto si sale mal: el paciente aparece en la lista del profesional equivocado — error de scoping y de datos, no exposición de información ajena, porque el usuario que lo crea ya tenía acceso de lectura a ambas listas por su rol. |
| T-68-13 | Elevation of Privilege | el fence de ALTA-07: `PatientFilters.tsx` y `data-table-toolbar.tsx` | mitigate | Encender la creación por accidente en un uso de filtro abriría un camino de escritura desde UI que hoy es sólo de lectura, en pantallas donde el usuario no está en un flujo de alta y no espera crear nada. Mitigación: los dos archivos se auditan por comando —ausencia de `allowCreate`/`profesionalIdParaAlta`, `git status` vacío y `git diff --stat` vacío— y además se chequean a mano en la Task 3. No se asume el default `false`: se prueba. |
| T-68-14 | Denial of Service (local — pérdida del trabajo del usuario) | el `<form onSubmit>` de `NewAppointmentModal` / `SurgeryAppointmentModal` bajo el mini-form | mitigate | Un submit accidental del turno desde adentro del popover (Enter o botón sin `type`) crearía un turno a medio cargar o descartaría lo tipeado. Las defensas ya están en los planes 01 y 02 (sin `<form>` interno, todo botón con `type="button"`, Enter interceptado con `preventDefault`/`stopPropagation`), pero **acá es donde se prueban**: la Task 3 exige el paso 6 verificado específicamente en los dos modales que tienen `<form>`. `QuickAppointment` no tiene `<form>` y por eso no puede ser el único probado. Mismo razonamiento para Escape burbujeando al Dialog (D-13). |
| T-68-15 | Tampering | dependencias | accept | Este plan no instala ningún paquete y no agrega imports: sólo pasa props entre componentes que ya existen. Sin superficie de supply-chain nueva, no aplica el gate de legitimidad de paquetes. |

**Severidad:** ninguna amenaza de este plan llega a `high`, y no se inventa una para satisfacer el gate. La razón es concreta: el plan no crea endpoints, no cambia authz (T-68-11 lo documenta con la línea exacta del guard que ya existe), no toca almacenamiento ni secretos, y no le da a ningún rol una capacidad de escritura que no tuviera ya por otra pantalla. Las dos amenazas con impacto real —scoping de profesional (T-68-12) y submit accidental del turno (T-68-14)— son `medium`: producen datos mal asignados o pérdida de trabajo del usuario, ambos corregibles desde la UI, sin exposición de datos entre tenants ni escalada de rol.
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` → exit 0, sin output (prueba que las tres firmas de profesional entran en `profesionalIdParaAlta`).
2. `cd frontend && npx eslint src/app/dashboard/turnos/NewAppointmentModal.tsx` → `errorCount` ≤ 1.
3. `cd frontend && npx eslint src/app/dashboard/turnos/SurgeryAppointmentModal.tsx` → `errorCount` ≤ 1.
4. `cd frontend && npx eslint src/app/dashboard/components/QuickAppointment.tsx` → `errorCount` ≤ 2.
5. `grep -c 'allowCreate'` → `1` en cada uno de los tres modales.
6. `grep -cE 'allowCreate|profesionalIdParaAlta'` → `0` en `PatientFilters.tsx` y en `data-table-toolbar.tsx`.
7. `git status --porcelain` y `git diff --stat` sobre esos dos paths → salida vacía en ambos.
8. `git status --porcelain backend/` → vacío (frontera de fase).
9. El diff de este plan lista únicamente los tres modales — ni `AutocompletePaciente.tsx` ni `InlineCreatePaciente.tsx`.
10. Verificación manual de la Task 3: 8 pasos × 3 modales + chequeo de profesional + chequeo de fence, con resultados registrados en el SUMMARY.
</verification>

<success_criteria>
- Los tres modales de turno (`QuickAppointment`, `NewAppointmentModal`, `SurgeryAppointmentModal`) ofrecen la creación inline; `PatientFilters` y `data-table-toolbar` no, y eso está probado por comando y a mano, no asumido (ALTA-07 / SC#6 del roadmap).
- Cada modal pasa al alta la misma expresión de profesional que usa para el turno; en `QuickAppointment` la divergencia con el filtro de búsqueda está documentada en el código y verificada a mano (ALTA-06 / D-08).
- Un paciente creado inline queda seleccionado y el turno se confirma sin pasos adicionales, en los tres modales (ALTA-04 / D-07).
- Crear el paciente desde adentro de `NewAppointmentModal` o `SurgeryAppointmentModal` no submitea el turno, y Escape no cierra el Dialog.
- El typecheck sale limpio y ningún archivo supera su baseline de lint.
- Nada bajo `backend/` fue modificado, y `AutocompletePaciente.tsx` no aparece en el diff de este plan (frontera con la Phase 69).
</success_criteria>

<output>
Create `.planning/phases/68-creaci-n-inline-en-el-autosuggest-frontend/68-03-SUMMARY.md` when done
</output>
