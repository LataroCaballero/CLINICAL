---
phase: 69-consistencia-de-tel-fono-opcional-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/telefono.ts
  - frontend/src/types/pacients.ts
  - frontend/src/types/reportes.ts
  - frontend/src/hooks/useReportesFinancieros.ts
  - frontend/src/hooks/useListaEspera.ts
autonomous: true
requirements: [TEL-03, ENVIO-03]

must_haves:
  truths:
    - "D-01: el placeholder de teléfono ausente es `-` (guion simple), no `—` (em dash)"
    - "D-02: existe UN solo helper `formatTelefono(value)` que aplica el criterio *falsy tras trim* de 67 D-03 y devuelve `-`; ningún otro sitio define el placeholder"
    - "D-07: los tipos TS de teléfono de paciente son `string | null` en los 6 sitios listados, sin `as any` y sin placeholders dentro del tipo"
    - "D-12: existe UN solo predicado `tieneTelefono(value) = !!value?.trim()`, que NO mira `telefonoAlternativo` (67 D-04)"
    - "D-14: cuando falta el teléfono el motivo devuelto es el del teléfono, tenga o no opt-in — el teléfono tiene precedencia"
    - "D-15: el motivo de bloqueo se calcula en una sola función que devuelve `string | null`; `null` significa control habilitado y sin `TooltipContent`"
    - "D-16: el texto del motivo por falta de teléfono es exactamente `El paciente no tiene teléfono cargado`, sin la segunda oración del mensaje de backend"
  artifacts:
    - path: "frontend/src/lib/telefono.ts"
      provides: "formatTelefono, tieneTelefono, getMotivoBloqueoWhatsApp y los dos textos de motivo"
      exports: ["formatTelefono", "tieneTelefono", "getMotivoBloqueoWhatsApp", "MOTIVO_SIN_TELEFONO", "MOTIVO_SIN_OPTIN"]
    - path: "frontend/src/types/pacients.ts"
      provides: "PacienteListItem.telefono y PacienteDetalle.telefono como string | null"
      contains: "telefono: string | null"
  key_links:
    - from: "frontend/src/lib/telefono.ts"
      to: "getMotivoBloqueoWhatsApp"
      via: "reutiliza tieneTelefono para el chequeo de teléfono"
      pattern: "tieneTelefono\\("
---

<objective>
Crear la única definición de "teléfono ausente" del frontend (placeholder, predicado y motivo de bloqueo) y ensanchar los tipos TS de teléfono a `string | null`, espejando lo que la Phase 67 hizo en backend.

Purpose: todo el resto de la fase (placeholder TEL-03 y guards ENVIO-03) consume estos tres helpers. Si el placeholder y el guard salieran de definiciones distintas, podrían divergir — que es exactamente el bug que 67 D-06 evitó en backend con `normalizeTelefono()`.
Output: `frontend/src/lib/telefono.ts` + 6 declaraciones de tipo ensanchadas.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-CONTEXT.md
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear el módulo de helpers de teléfono</name>
  <files>frontend/src/lib/telefono.ts</files>
  <read_first>
    - frontend/src/lib/estadoTurno.ts (convención de helper puro del repo: JSDoc corto arriba de la función, módulo TypeScript sin imports de framework — es el analog de convención)
    - frontend/src/lib/utils.ts (para confirmar que hoy sólo re-exporta `cn` y que los helpers nuevos van en archivo propio, no ahí)
    - backend/src/modules/whatsapp/whatsapp.service.ts (localizar `requireTelefonoParaEnvio` con `rg -n` — es el criterio *falsy tras trim* de 67 D-03 que se espeja, y el mensaje largo que D-16 NO replica)
    - .planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-PATTERNS.md (sección 4 "Helpers nuevos" tiene el molde de las tres firmas)
  </read_first>
  <action>
Crear el archivo nuevo `frontend/src/lib/telefono.ts` (no tocar `lib/utils.ts`, que hoy sólo re-exporta `cn`). Es un módulo TypeScript puro, sin imports de React ni de librerías; cada export lleva un JSDoc corto de 2-3 líneas al estilo de `lib/estadoTurno.ts`, citando la decisión que lo origina.

Exports requeridos, con estos nombres exactos:

1. `MOTIVO_SIN_TELEFONO` — constante string con el valor literal exacto: El paciente no tiene teléfono cargado (D-16). Sin punto final, y sin la segunda oración "Agregá un teléfono en su ficha..." que sí usa el backend: el tooltip es una etiqueta de un renglón y en la ficha esa instrucción sería redundante.
2. `MOTIVO_SIN_OPTIN` — constante string con el valor literal exacto: El paciente no tiene opt-in para WhatsApp. Es el texto que hoy está hardcodeado y duplicado en los 4 archivos de controles WA; centralizarlo evita una quinta copia divergente (D-15).
3. `formatTelefono(value: string | null | undefined): string` — aplica el criterio *falsy tras trim* de 67 D-03: si `value` es `null`, `undefined`, string vacío o sólo espacios, devuelve el guion simple `-` (U+002D, NO em dash, D-01); si hay valor, devuelve el valor con `.trim()` aplicado. No formatea el número: no agrega separadores ni prefijo de país (diferido explícitamente en `<deferred>` de CONTEXT.md) — el helper sólo resuelve la ausencia.
4. `tieneTelefono(value: string | null | undefined): boolean` — devuelve `!!value?.trim()` (D-12). No mira `telefonoAlternativo` ni recibe ese parámetro: 67 D-04 estableció que el alternativo no es canal de envío.
5. `getMotivoBloqueoWhatsApp(telefono: string | null | undefined, whatsappOptIn: boolean | undefined): string | null` — implementa D-14 (precedencia) y D-15 (un motivo por vez). Orden obligatorio: si `!tieneTelefono(telefono)` devuelve `MOTIVO_SIN_TELEFONO` (aunque falte también el opt-in); si no, si `!whatsappOptIn` devuelve `MOTIVO_SIN_OPTIN`; si no, devuelve `null`. Debe invocar `tieneTelefono`, no reimplementar el chequeo, para que guard y placeholder no puedan divergir.

`null` como retorno significa control habilitado y sin `TooltipContent` — dejarlo escrito en el JSDoc de la función, porque es el contrato que consumen los planes 05 y 06.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/telefono.ts` existe y exporta los 5 identificadores: `grep -c "^export " frontend/src/lib/telefono.ts` devuelve 5 o más
    - `grep -F 'El paciente no tiene teléfono cargado' frontend/src/lib/telefono.ts` encuentra el string exacto
    - `grep -F 'El paciente no tiene opt-in para WhatsApp' frontend/src/lib/telefono.ts` encuentra el string exacto
    - El archivo NO contiene em dash: `grep -c '—' frontend/src/lib/telefono.ts` devuelve 0
    - El archivo NO menciona `telefonoAlternativo`: `grep -c 'telefonoAlternativo' frontend/src/lib/telefono.ts` devuelve 0
    - `getMotivoBloqueoWhatsApp` invoca `tieneTelefono`: `grep -A6 'export function getMotivoBloqueoWhatsApp' frontend/src/lib/telefono.ts | grep -c 'tieneTelefono('` devuelve 1 o más
    - El archivo no importa nada: `grep -c "^import" frontend/src/lib/telefono.ts` devuelve 0
    - Comportamiento: `formatTelefono("   ")` devuelve `-` (no devuelve los espacios) y `formatTelefono(" 1145 ")` devuelve `1145`
    - Comportamiento: `getMotivoBloqueoWhatsApp(null, true)` devuelve el texto de teléfono; `getMotivoBloqueoWhatsApp(null, false)` devuelve el MISMO texto de teléfono (precedencia D-14, no el de opt-in); `getMotivoBloqueoWhatsApp("1145", false)` devuelve el texto de opt-in; `getMotivoBloqueoWhatsApp("1145", true)` devuelve `null`
    - `cd frontend && npx tsc --noEmit` sale con código 0 (la baseline previa a este plan también es 0 — cualquier error es regresión de este plan)
  </acceptance_criteria>
  <done>El módulo existe con los 5 exports, el placeholder es `-`, el texto del motivo de teléfono es el corto de D-16, y `getMotivoBloqueoWhatsApp` da precedencia al teléfono sobre el opt-in.</done>
</task>

<task type="auto">
  <name>Task 2: Ensanchar a `string | null` los 6 tipos de teléfono de paciente</name>
  <files>frontend/src/types/pacients.ts, frontend/src/types/reportes.ts, frontend/src/hooks/useReportesFinancieros.ts, frontend/src/hooks/useListaEspera.ts</files>
  <read_first>
    - frontend/src/types/pacients.ts (declaraciones `PacienteListItem` y `PacienteDetalle` — el CONTEXT cita `:6` y `:38`; re-verificar con `rg -n "telefono" frontend/src/types/pacients.ts` antes de editar)
    - frontend/src/types/reportes.ts (`PacienteAusentista`, CONTEXT cita `:84`)
    - frontend/src/hooks/useReportesFinancieros.ts (`CuentaPorCobrar` y `CuentaMorosa`, CONTEXT cita `:60` y `:77`)
    - frontend/src/hooks/useListaEspera.ts (`PacienteListaEspera`, CONTEXT cita `:7`)
    - .planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-CONTEXT.md (D-01: el service normaliza el string vacío a `null`, por eso el tipo del cliente debe admitir `null`)
  </read_first>
  <action>
Ensanchar la declaración de `telefono` a `string | null` en las 6 declaraciones de tipo listadas, espejando lo que la Phase 67 ya hizo en backend (donde `Paciente.telefono` pasó a `String?` y 6 declaraciones de tipo se ensancharon sin `as any`).

Sitios (los números de línea del CONTEXT son del 2026-08-20 y pueden haber corrido — localizar cada uno con `rg -n "telefono" <archivo>` antes de editar, NO editar por número de línea a ciegas):
- `frontend/src/types/pacients.ts` → `PacienteListItem.telefono` y `PacienteDetalle.telefono`
- `frontend/src/types/reportes.ts` → `PacienteAusentista.telefono`
- `frontend/src/hooks/useReportesFinancieros.ts` → `CuentaPorCobrar.telefono` y `CuentaMorosa.telefono`
- `frontend/src/hooks/useListaEspera.ts` → `PacienteListaEspera.telefono`

Reglas de ensanchamiento (mismo criterio que 67 D-04 / plan 67-04): la unión es `string | null`, sin `as any`, sin valores por defecto ni placeholders dentro del tipo. Si un campo hoy ya es opcional (`telefono?: string`), pasa a `telefono?: string | null` conservando el `?`. Si es requerido (`telefono: string`), pasa a `telefono: string | null` — no lo conviertas en opcional, porque el backend siempre manda la clave, con `null` como valor.

`telefonoAlternativo` no se toca en ninguno de estos archivos (67 D-04: no es canal de envío).

Después de editar, correr `npx tsc --noEmit`. Si el compilador rompe en un archivo NO listado acá, ese sitio es un hallazgo de la fase, no un obstáculo: resolvelo usando `formatTelefono` de la Task 1 si es un sitio de display, o un null-check / `?? ""` si es lógica — nunca con `as any` — y registrá cada sitio extra en el SUMMARY con su ruta y cómo se resolvió.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Los 6 campos declaran la unión: `grep -rn "telefono[?]*: string | null" frontend/src/types/pacients.ts frontend/src/types/reportes.ts frontend/src/hooks/useReportesFinancieros.ts frontend/src/hooks/useListaEspera.ts | wc -l` devuelve 6
    - Ningún campo `telefono` quedó como `string` a secas en esos 4 archivos: `grep -rn "telefono[?]*: string;" frontend/src/types/pacients.ts frontend/src/types/reportes.ts frontend/src/hooks/useReportesFinancieros.ts frontend/src/hooks/useListaEspera.ts | wc -l` devuelve 0
    - No se introdujo ningún `as any`: `git diff -- frontend/src/types frontend/src/hooks | grep '^+' | grep -c 'as any'` devuelve 0
    - `telefonoAlternativo` no aparece en el diff de este plan: `git diff -- frontend/src | grep '^+' | grep -c 'telefonoAlternativo'` devuelve 0
    - `cd frontend && npx tsc --noEmit` sale con código 0
    - Si `tsc` obligó a tocar archivos fuera de `files_modified`, cada uno queda listado en el SUMMARY con ruta y resolución
  </acceptance_criteria>
  <done>Los 6 campos de teléfono aceptan `null` a nivel de tipos, el typecheck del frontend sale 0 y no se introdujo ningún `as any`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna nueva | Este plan agrega un módulo TypeScript puro sin I/O y ensancha declaraciones de tipo. No cruza red, no toca almacenamiento, no procesa entrada de usuario. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Information Disclosure | `MOTIVO_SIN_TELEFONO` / `MOTIVO_SIN_OPTIN` | accept | Los dos textos son etiquetas de UI sin datos del paciente (no interpolan nombre, DNI ni número). No filtran nada que el operador logueado no vea ya en la ficha. |
| T-69-02 | Tampering | `tieneTelefono` usado como si fuera control de seguridad | accept | El predicado es sólo UX. La autoridad real es `requireTelefonoParaEnvio()` en `whatsapp.service.ts` (67 D-03/D-10), que corre server-side en los 4 paths de envío. Un cliente modificado que ignore el helper sigue recibiendo 400 del backend. |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala ningún paquete: `telefono.ts` no tiene imports y los 4 archivos de tipos sólo editan anotaciones. Sin superficie de supply chain, no aplica el gate de legitimidad de paquetes. |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` sale 0 (baseline pre-plan verificada en 0 el 2026-08-20)
2. `cd frontend && npm run lint` no reporta errores nuevos en `src/lib/telefono.ts`
3. Los 5 exports del helper existen con los nombres exactos que consumirán los planes 04, 05 y 06
</verification>

<success_criteria>
- Existe una sola definición del placeholder `-`, del predicado *falsy tras trim* y de los dos textos de motivo, todas en `frontend/src/lib/telefono.ts`
- Los 6 tipos de teléfono admiten `null` sin `as any`
- El typecheck del frontend sigue limpio
</success_criteria>

<output>
Create `.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-01-SUMMARY.md` when done
</output>
