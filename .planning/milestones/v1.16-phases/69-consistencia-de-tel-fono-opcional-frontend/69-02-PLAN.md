---
phase: 69-consistencia-de-tel-fono-opcional-frontend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/turnos/turnos.service.ts
  - frontend/src/app/dashboard/turnos/page.tsx
  - frontend/src/app/dashboard/turnos/CalendarGrid.tsx
  - frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx
autonomous: true
requirements: [ENVIO-03]

must_haves:
  truths:
    - "D-13: `obtenerTurnosPorRango` devuelve `paciente.telefono` — sin ese dato, el guard visual del calendario deshabilitaría el botón de WhatsApp para TODOS los pacientes, incluidos los que sí tienen número"
    - "D-13: el teléfono viaja por la misma cadena que `whatsappOptIn` (select del service → mapeo de `turnos/page.tsx` → `CalendarEvent`), sin queries nuevas en el frontend"
    - "D-13: las TRES copias independientes de `interface CalendarEvent` (`CalendarGrid.tsx`, `turnos/page.tsx`, `AppointmentDetailModal.tsx`) declaran `telefono?: string | null`"
    - "D-13: este plan corre ANTES del guard visual de `AppointmentDetailModal` (plan 06), que depende de él por wave"
  artifacts:
    - path: "backend/src/modules/turnos/turnos.service.ts"
      provides: "telefono del paciente en la respuesta de GET /turnos/rango"
      contains: "telefono: true"
    - path: "frontend/src/app/dashboard/turnos/page.tsx"
      provides: "mapeo turnos → CalendarEvent con el teléfono poblado"
      contains: "telefono: t.paciente?.telefono ?? null"
  key_links:
    - from: "backend/src/modules/turnos/turnos.service.ts"
      to: "frontend/src/app/dashboard/turnos/page.tsx"
      via: "GET /turnos/rango → mapeo a CalendarEvent"
      pattern: "telefono: t\\.paciente\\?\\.telefono"
    - from: "frontend/src/app/dashboard/turnos/page.tsx"
      to: "frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx"
      via: "prop `event` tipada por las 3 copias de CalendarEvent"
      pattern: "telefono\\?: string \\| null"
---

<objective>
Hacer que el teléfono del paciente llegue al calendario de turnos, replicando exactamente el recorrido que ya hace `whatsappOptIn`: ensanchar el `select` de `obtenerTurnosPorRango`, poblarlo en el mapeo a `CalendarEvent` y declararlo en las tres copias de esa interfaz.

Purpose: es el único requisito de datos de la fase. El plan 06 deshabilita el botón de WhatsApp del detalle de turno cuando el paciente no tiene número; si ese guard corre sin este dato, el botón queda deshabilitado para todos los pacientes — un falso negativo total, no un caso borde. Por eso este plan está en la wave 1 y el 06 depende de él.
Output: teléfono disponible en `event.telefono` dentro de `AppointmentDetailModal`, sin fetch nuevo en el frontend.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-CONTEXT.md
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensanchar el `select` de `obtenerTurnosPorRango` con el teléfono del paciente</name>
  <files>backend/src/modules/turnos/turnos.service.ts</files>
  <read_first>
    - backend/src/modules/turnos/turnos.service.ts (localizar `obtenerTurnosPorRango` con `rg -n "obtenerTurnosPorRango" backend/src/modules/turnos/turnos.service.ts`; el `select` anidado de `paciente` estaba en `:567-572` al 2026-08-20 y trae `id`, `nombreCompleto`, `whatsappOptIn`)
    - backend/src/modules/turnos/turnos.controller.ts (el handler `GET rango` y el `@Auth('ADMIN','PROFESIONAL','SECRETARIA')` de la clase — contexto para el threat model, no se modifica)
  </read_first>
  <action>
Agregar `telefono: true` al `select` anidado de `paciente` dentro del método `obtenerTurnosPorRango` de `TurnosService`. Es un ensanchamiento aditivo de un `select` de Prisma: no cambia la firma del método, no cambia el contrato del endpoint para los consumidores existentes, sólo suma una clave al objeto `paciente` de cada turno devuelto.

Ubicalo junto a `whatsappOptIn: true`, dentro del mismo bloque `paciente: { select: { ... } }` que ya trae `id` y `nombreCompleto`. NO tocar el resto del `select` (`id`, `inicio`, `fin`, `estado`, `observaciones`, `tipoTurno`, ni ningún otro include), NO tocar `turnos.controller.ts`, y NO ensanchar otros métodos del service (`obtenerProximosTurnos`, `findAll`, etc.) — esta fase toca una sola línea de backend, deliberadamente.

Verificá con `rg -n "telefono: true" backend/src/modules/turnos/turnos.service.ts` que quedó una sola ocurrencia nueva y que está dentro de `obtenerTurnosPorRango`, no en otro método.
  </action>
  <verify>
    <automated>cd backend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "telefono: true" backend/src/modules/turnos/turnos.service.ts` devuelve exactamente 1 línea
    - Esa línea cae dentro del cuerpo de `obtenerTurnosPorRango`: verificar con `rg -n "obtenerTurnosPorRango|telefono: true" backend/src/modules/turnos/turnos.service.ts` que el número de línea de `telefono: true` es mayor que el de la declaración del método y menor que el de la siguiente declaración de método
    - El diff de este task toca exactamente 1 línea agregada y 0 eliminadas en `turnos.service.ts`: `git diff --numstat -- backend/src/modules/turnos/turnos.service.ts` reporta `1	0`
    - `backend/src/modules/turnos/turnos.controller.ts` no fue modificado: `git diff --name-only -- backend/src/modules/turnos/turnos.controller.ts` no devuelve nada
    - `cd backend && npm run build` sale con código 0
    - Comportamiento: una respuesta de `GET /turnos/rango?profesionalId=...&desde=...&hasta=...` incluye la clave `telefono` dentro de cada objeto `paciente` (con valor string o `null`)
  </acceptance_criteria>
  <done>El `select` de `obtenerTurnosPorRango` devuelve `paciente.telefono` y el build de backend sale 0.</done>
</task>

<task type="auto">
  <name>Task 2: Declarar `telefono` en las 3 copias de `CalendarEvent` y poblarlo en el mapeo</name>
  <files>frontend/src/app/dashboard/turnos/CalendarGrid.tsx, frontend/src/app/dashboard/turnos/page.tsx, frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/turnos/page.tsx (su copia local de `interface CalendarEvent`, ~`:41-56`, y el mapeo `turnos → CalendarEvent` ~`:281-300`, donde `:290` es la línea de `whatsappOptIn`)
    - frontend/src/app/dashboard/turnos/CalendarGrid.tsx (su copia de `interface CalendarEvent`, ~`:15-28`)
    - frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx (su copia de `interface CalendarEvent`, ~`:51-62` — SOLO la interfaz en este plan; el control de WhatsApp lo toca el plan 06)
    - .planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-PATTERNS.md (sección 2 y "Desviaciones" punto 2: las tres interfaces son copias locales independientes, ninguna se importa de la otra)
  </read_first>
  <action>
Hay TRES declaraciones independientes de `interface CalendarEvent` en `frontend/src/app/dashboard/turnos/` — confirmalo antes de editar con `rg -n "interface CalendarEvent" frontend/src/app/dashboard/turnos/`. Ninguna importa a las otras: son copias locales estructuralmente compatibles. Las tres necesitan el campo nuevo; si sólo se toca una, TypeScript no falla (el campo es opcional y las interfaces son estructurales) pero el dato no llega al componente que quedó sin declararlo.

Cambios:

1. En las tres copias (`CalendarGrid.tsx`, `page.tsx`, `AppointmentDetailModal.tsx`), agregar el campo `telefono?: string | null;` inmediatamente debajo de la declaración de `whatsappOptIn`, conservando el estilo de cada archivo. Nombre del campo: `telefono` (no `pacienteTelefono`) — dentro de `CalendarEvent` el resto de los campos del paciente ya viven sin prefijo cuando es un dato del turno mapeado.

2. En el mapeo `turnos → CalendarEvent` de `page.tsx` (el `.map((t) => ({ ... }))` que sigue al `.filter((t) => t.estado !== "CANCELADO")`), agregar la línea `telefono: t.paciente?.telefono ?? null,` inmediatamente al lado de la línea existente `whatsappOptIn: t.paciente?.whatsappOptIn ?? false,`. El default es `null`, no `""`: `null` es lo que el backend devuelve para un paciente sin teléfono desde la Phase 67 y lo que `tieneTelefono`/`formatTelefono` esperan.

No agregar ningún fetch, hook ni query nuevos: el dato llega en la misma respuesta de `GET /turnos/rango` que ya se consume. No tocar el control de WhatsApp de `AppointmentDetailModal` en este plan — el guard visual es del plan 06 y depende de que este plan ya esté aplicado.

Los números de línea del CONTEXT son del 2026-08-20 y pueden haber corrido: localizá cada punto de edición con `rg -n` antes de tocar el archivo.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Las 3 interfaces declaran el campo: `rg -c "telefono\?: string \| null" frontend/src/app/dashboard/turnos/CalendarGrid.tsx frontend/src/app/dashboard/turnos/page.tsx frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx` reporta 1 en cada uno de los 3 archivos
    - El mapeo puebla el campo: `rg -n "telefono: t\.paciente\?\.telefono \?\? null" frontend/src/app/dashboard/turnos/page.tsx` devuelve 1 línea
    - Esa línea es adyacente a la de `whatsappOptIn` en el mapeo (diferencia de 1 línea entre ambos números reportados por `rg -n "whatsappOptIn: t\.paciente|telefono: t\.paciente" frontend/src/app/dashboard/turnos/page.tsx`)
    - No se introdujo ningún fetch/hook nuevo: `git diff -- frontend/src/app/dashboard/turnos | grep '^+' | grep -cE "useQuery|api\.get|fetch\("` devuelve 0
    - El control de WhatsApp de `AppointmentDetailModal` sigue intacto en este plan: `git diff -- frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx | grep '^+' | grep -c "TooltipContent"` devuelve 0
    - `cd frontend && npx tsc --noEmit` sale con código 0
    - Comportamiento: abrir el calendario de turnos y, en el detalle de un turno de un paciente CON teléfono, inspeccionar el objeto `event` (React DevTools o un `console.log` temporal removido después) muestra `telefono` con el número; para un paciente sin teléfono muestra `null` (no `undefined`)
  </acceptance_criteria>
  <done>Las 3 copias de `CalendarEvent` declaran `telefono?: string | null`, el mapeo lo puebla desde la respuesta del endpoint y el typecheck sale 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → API (`GET /turnos/rango`) | Único cruce real de la fase: la respuesta del endpoint suma un campo de PII (`paciente.telefono`) que hoy no viaja por ahí. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-03 | Information Disclosure | `GET /turnos/rango` ahora devuelve `paciente.telefono` | accept | El controller lleva `@Auth('ADMIN','PROFESIONAL','SECRETARIA')` a nivel de clase: los tres roles ya acceden al teléfono del paciente por `GET /pacientes` y por la ficha. `PACIENTE` y `FACTURADOR` no alcanzan el endpoint. La respuesta ya incluía `nombreCompleto` y `whatsappOptIn`, así que no se abre un canal nuevo hacia un rol que hoy no tenga el dato. |
| T-69-04 | Information Disclosure | `obtenerPorRango` toma `profesionalId` de query sin pasar por `resolveScope` (a diferencia de `findAll`, que sí recibe `@Req`) | accept | Preexistente y fuera del alcance de esta fase. Un usuario autenticado con rol clínico puede pedir el rango de otro `profesionalId` y ya obtiene nombre y opt-in de esos pacientes; sumar el teléfono amplía marginalmente el PII de un path que ya está expuesto. **El executor debe registrar este hallazgo en el SUMMARY** para que quede trazado como deuda conocida — no corregirlo acá: agregar scoping al endpoint cambia el contrato del calendario y merece su propia fase. |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala paquetes: agrega una clave a un `select` de Prisma y un campo opcional a tres interfaces. Sin superficie de supply chain. |
</threat_model>

<verification>
1. `cd backend && npm run build` sale 0
2. `cd frontend && npx tsc --noEmit` sale 0
3. `rg -n "interface CalendarEvent" frontend/src/app/dashboard/turnos/` sigue devolviendo 3 archivos y los 3 declaran `telefono?: string | null`
4. El hallazgo de scoping de `GET /turnos/rango` (T-69-04) queda registrado en el SUMMARY
</verification>

<success_criteria>
- El teléfono del paciente llega desde Prisma hasta `event.telefono` en `AppointmentDetailModal`, sin fetch nuevo
- Las tres copias de `CalendarEvent` están alineadas
- El plan 06 puede implementar el guard visual sabiendo que el dato existe
</success_criteria>

<output>
Create `.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-02-SUMMARY.md` when done
</output>
