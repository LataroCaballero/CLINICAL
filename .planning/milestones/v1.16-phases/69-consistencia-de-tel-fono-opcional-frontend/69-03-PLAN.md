---
phase: 69-consistencia-de-tel-fono-opcional-frontend
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx
  - frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx
autonomous: true
requirements: [TEL-02]

must_haves:
  truths:
    - "D-08: `NewPacienteModal` guarda un paciente sin teléfono — el schema Zod calca el mini-form de 68 D-15 (vacío válido; con valor, ≥6 caracteres) y el label pierde el asterisco de obligatorio"
    - "D-08: el payload usa `data.telefono?.trim() ?? \"\"` — el `data.telefono.trim()` actual explota con `undefined` cuando el campo queda vacío"
    - "D-09: el `contactoSchema` de `DatosCompletos.tsx` relaja `telefono` (vacío válido; con valor, 6–20 chars), sin lo cual un paciente creado inline no puede corregir su email sin inventar un teléfono"
    - "D-09: `telefonoAlternativo` y los seeds `paciente.telefono ?? \"\"` no se tocan"
    - "D-10: `PatientFormModal.tsx` no se modifica — verificado como no-op, su campo ya acepta vacío"
  artifacts:
    - path: "frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx"
      provides: "alta completa de paciente sin teléfono"
      contains: "optional()"
    - path: "frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx"
      provides: "edición del bloque Contacto sin teléfono cargado"
      contains: "optional()"
  key_links:
    - from: "frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx"
      to: "POST /pacientes"
      via: "payload con telefono string vacío que el service normaliza a null (67 D-01)"
      pattern: "data\\.telefono\\?\\.trim\\(\\)"
---

<objective>
Relajar la validación cliente de teléfono en los dos formularios que hoy lo exigen: el alta completa (`NewPacienteModal`) y el bloque Contacto de la ficha (`DatosCompletos`), calcando el schema Zod que la Phase 68 ya resolvió contra el backend real.

Purpose: TEL-02 es literalmente esto en el alta completa. Y en `DatosCompletos`, es lo que vuelve observable el trabajo de 67 D-05 — el backend ya acepta editar Contacto sin teléfono, pero el Zod del cliente lo sigue bloqueando, así que hoy ese trabajo es invisible y un paciente creado inline en la Phase 68 no puede corregir su email.
Output: dos schemas relajados, un label sin asterisco y el crash latente del payload cerrado.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-CONTEXT.md
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alta completa sin teléfono en `NewPacienteModal`</name>
  <files>frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx (el schema Zod ~`:31-44`, el `defaultValues` ~`:70`, la construcción del payload ~`:87` y el bloque del campo Teléfono ~`:167-180`; localizar con `rg -n "telefono" <archivo>` antes de editar)
    - frontend/src/components/InlineCreatePaciente.tsx (el schema de la Phase 68, ~`:42-51` — es el molde EXACTO a calcar, ya validado contra el backend en producción)
    - .planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-CONTEXT.md (D-01: el service normaliza string vacío a `null`; D-06: el umbral ≥6 vive en `normalizeTelefono()`)
  </read_first>
  <action>
Tres ediciones en `NewPacienteModal.tsx`, más una que explícitamente NO se hace.

1. **Schema Zod**: `telefono: z.string().min(6, "Teléfono inválido")` pasa a la forma del mini-form de la Phase 68: `z.string()` + `.optional()` + un `.refine()` que acepta cuando el valor es falsy o su `trim()` está vacío, y exige `trim().length >= 6` cuando hay contenido, con el mensaje `Teléfono inválido` (mismo texto que hoy, no inventar uno nuevo). El umbral ≥6 espeja `normalizeTelefono()` del backend (67 D-06) para no comerse un 400 evitable. Copiar la forma de `InlineCreatePaciente.tsx`, no re-derivarla.

2. **Label**: en el bloque del campo Teléfono, quitar el `<span className="text-destructive">*</span>` y agregar el texto `(opcional)` a continuación de la palabra "Teléfono", siguiendo cómo el mismo modal marca hoy sus otros campos no obligatorios (mirar el label de `email` u `obraSocialId` en este archivo y usar la misma envoltura/clase que ya empleen ahí). El único requisito duro: que el campo deje de verse como obligatorio.

3. **Payload**: `telefono: data.telefono.trim()` pasa a `telefono: data.telefono?.trim() ?? ""`. Es un crash latente: con el campo vacío y el schema opcional, `data.telefono` llega `undefined` y `.trim()` tira TypeError antes de llegar al POST. Mandar string vacío es seguro y correcto — 67 D-01 lo normaliza a `null` en el service, para todos los callers.

4. **NO tocar** el `defaultValues` (`telefono: ""` sigue siendo el estado inicial correcto según D-08), ni el `PhoneInput`, ni los otros campos del schema, ni el bloque de `Alert`/`errors.telefono` (sigue funcionando: sólo dispara cuando hay valor y es corto).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - El schema ya no exige teléfono: `rg -c 'telefono: z\.string\(\)\.min\(6' frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx` devuelve 0
    - El schema es opcional con refine: `rg -n "optional\(\)" frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx` y `rg -n "refine" frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx` devuelven al menos 1 línea cada uno, dentro del bloque del schema
    - El mensaje de error conserva el texto original: `rg -F 'Teléfono inválido' frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx` sigue encontrando el string
    - El payload es null-safe: `rg -F 'data.telefono?.trim() ?? ""' frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx` devuelve 1 línea, y `rg -F 'data.telefono.trim()' <mismo archivo>` devuelve 0
    - El label ya no marca obligatorio: en el bloque del campo Teléfono no queda `text-destructive` sobre un asterisco — verificar con `rg -n -B2 -A2 'Teléfono' frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx` que el `<span className="text-destructive">*</span>` desapareció de ese bloque y aparece `(opcional)`
    - `defaultValues` intacto: `rg -F 'telefono: ""' frontend/src/app/dashboard/pacientes/components/NewPacienteModal.tsx` sigue devolviendo la línea del `defaultValues`
    - `cd frontend && npx tsc --noEmit` sale con código 0
    - Comportamiento: abrir el modal de alta, cargar sólo nombre completo y DNI, dejar el teléfono vacío y guardar → el paciente se crea (POST 201), sin error de validación en el cliente y sin TypeError en consola
    - Comportamiento anti-regresión: cargar un teléfono de 3 caracteres sigue mostrando el `Alert` con `Teléfono inválido` y NO envía el POST
  </acceptance_criteria>
  <done>El alta completa guarda un paciente sin teléfono, el label no lo marca obligatorio, un teléfono demasiado corto sigue siendo rechazado en el cliente y el payload no puede tirar TypeError.</done>
</task>

<task type="auto">
  <name>Task 2: Bloque Contacto de la ficha editable sin teléfono</name>
  <files>frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx</files>
  <read_first>
    - frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx (el `contactoSchema` ~`:106-108`, los seeds `paciente.telefono ?? ""` en ~`:27` y ~`:185`, el submit del bloque contacto ~`:203-212` y el `FieldRow` de Teléfono ~`:586-596`; localizar con `rg -n "telefono" <archivo>`)
    - frontend/src/components/InlineCreatePaciente.tsx (~`:42-51`, mismo molde de la Task 1)
    - .planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-CONTEXT.md (D-05: `updateContacto` en el backend ya se relajó — sin valor → `null`; con valor → sigue exigiendo ≥6)
  </read_first>
  <action>
Relajar sólo el campo `telefono` del `contactoSchema`, que hoy declara `z.string().min(6, "Teléfono inválido").max(20, "Teléfono inválido")` obligatorio.

Forma objetivo, misma regla que la Task 1 pero conservando el techo de 20: `z.string()` + `.optional()` + un `.refine()` que acepta cuando el valor es falsy o su `trim()` está vacío, y cuando hay contenido exige `trim().length >= 6 && trim().length <= 20`, con el mensaje `Teléfono inválido` (el mismo texto que hoy usan ambas validaciones). El piso ≥6 y el techo ≤20 espejan lo que `updateContacto` sigue validando server-side (67 D-05/D-06).

Si el `contactoSchema` se valida con un `safeParse` manual (no vía `zodResolver`) y el resultado alimenta `contactoErrors`, no cambies ese cableado: sólo cambia la declaración del campo.

NO tocar en este archivo:
- `telefonoAlternativo` (`:107`) — ya es `.optional().nullable()` y 67 D-04 lo deja fuera del canal de envío.
- Los seeds `telefono: paciente.telefono ?? ""` (`:27` y `:185`) — ya son null-safe.
- El `FieldRow` / `EditableInput` de Teléfono — el campo se sigue mostrando y editando igual; esta task no cambia la UI del bloque.
- El schema `email` ni ningún otro bloque de la ficha.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - El campo ya no es obligatorio: `rg -c 'telefono: z\.string\(\)\.min\(6' frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx` devuelve 0
    - El campo es opcional con refine: dentro del bloque `contactoSchema`, `rg -n -A8 'const contactoSchema' frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx | grep -c 'optional()'` devuelve 2 o más (el de `telefono` nuevo y el preexistente de `telefonoAlternativo`)
    - `telefonoAlternativo` quedó exactamente igual: `git diff -- frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx | grep '^[+-]' | grep -c 'telefonoAlternativo'` devuelve 0
    - Los seeds quedaron intactos: `git diff -- frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx | grep '^[+-]' | grep -c 'paciente.telefono ?? ""'` devuelve 0
    - El diff está acotado al schema: `git diff --numstat -- frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx` reporta menos de 15 líneas agregadas
    - `cd frontend && npx tsc --noEmit` sale con código 0
    - Comportamiento: abrir la ficha de un paciente SIN teléfono, editar el bloque Contacto cambiando sólo el email y guardar → el PATCH sale y el email queda persistido, sin error `Teléfono inválido` bloqueando el submit
    - Comportamiento anti-regresión: en un paciente con teléfono, escribir un valor de 3 caracteres en Teléfono sigue mostrando `Teléfono inválido` y no guarda
  </acceptance_criteria>
  <done>El bloque Contacto de la ficha se guarda con el teléfono vacío, sigue rechazando valores fuera del rango 6–20 cuando hay contenido, y `telefonoAlternativo` y los seeds quedaron sin tocar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| formulario → API (`POST /pacientes`, `PATCH /pacientes/:id` sección contacto) | Se relaja validación del lado cliente en ambos cruces. La validación autoritativa sigue siendo la del server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-05 | Tampering | Relajar el Zod de `telefono` en los dos formularios | accept | La validación cliente nunca fue el control: `normalizeTelefono()` en `PacientesService` (67 D-01/D-06) aplica `trim` → `null` si vacío → umbral ≥6 en `create`, `update` y `updateContacto`. Relajar el cliente sólo deja de bloquear lo que el server ya acepta; un payload malicioso llega igual al mismo helper server-side. |
| T-69-06 | Tampering | Payload `telefono: ""` enviado en vez de omitir la clave | accept | 67 D-01 decidió normalizar en el service precisamente porque el frontend manda string vacío. El valor se persiste como `null`, no como `""`, así que el guard de envío (67 D-03) y el placeholder (D-01 de esta fase) disparan correctamente. |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala paquetes: `zod` y `react-hook-form` ya son dependencias del proyecto y se usan en ambos archivos. Sin superficie de supply chain. |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` sale 0
2. `cd frontend && npm run lint` sin errores nuevos en los dos archivos
3. `frontend/src/app/dashboard/pacientes/components/PatientFormModal.tsx` NO aparece en el diff de la fase (D-10, no-op verificado)
</verification>

<success_criteria>
- Criterio de éxito #1 del ROADMAP cubierto: el alta completa permite guardar un paciente sin cargar teléfono
- Un paciente sin teléfono puede editar su bloque Contacto (67 D-05 se vuelve observable)
- Ambos formularios siguen rechazando teléfonos demasiado cortos cuando el usuario sí carga un valor
</success_criteria>

<output>
Create `.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-03-SUMMARY.md` when done
</output>
