---
phase: 69-consistencia-de-tel-fono-opcional-frontend
plan: 05
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - frontend/src/app/dashboard/pacientes/components/PatientDrawer.tsx
  - frontend/src/components/patient/PatientDrawer/views/PresupuestosView.tsx
  - frontend/src/components/patient/PatientDrawer/views/MensajesView.tsx
  - frontend/src/components/whatsapp/WAThreadView.tsx
autonomous: true
requirements: [ENVIO-03]

must_haves:
  truths:
    - "D-11: el guard cubre 3 de los 5 controles de la lista cerrada — el de presupuestos y los DOS de `WAThreadView` (template y free-text); `SendWAMessageModal` queda explícitamente sin guard propio"
    - "D-13: el teléfono se plumbea por props replicando la cadena de `whatsappOptIn` (`PatientDrawer` → `PresupuestosView` y `PatientDrawer` → `MensajesView` → `WAThreadView`), sin store ni fetch nuevos"
    - "D-15: el motivo se resuelve con `getMotivoBloqueoWhatsApp` — un motivo por vez, `null` significa habilitado y sin `TooltipContent`"
    - "D-16: cuando falta el teléfono el tooltip dice exactamente `El paciente no tiene teléfono cargado`, no el mensaje largo del backend"
    - "D-17: el patrón visual se reusa tal cual (`Tooltip > TooltipTrigger asChild > <span> > <Button disabled>`), y el `<span>` intermedio es obligatorio también en el wrapper nuevo del botón free-text"
  artifacts:
    - path: "frontend/src/components/patient/PatientDrawer/views/PresupuestosView.tsx"
      provides: "botón Enviar por WhatsApp deshabilitado con motivo cuando falta teléfono u opt-in"
      contains: "getMotivoBloqueoWhatsApp"
    - path: "frontend/src/components/whatsapp/WAThreadView.tsx"
      provides: "los dos controles de envío (template y free-text) gateados por teléfono + opt-in"
      contains: "getMotivoBloqueoWhatsApp"
  key_links:
    - from: "frontend/src/app/dashboard/pacientes/components/PatientDrawer.tsx"
      to: "frontend/src/components/whatsapp/WAThreadView.tsx"
      via: "prop pacienteTelefono a través de MensajesView"
      pattern: "pacienteTelefono"
    - from: "frontend/src/components/whatsapp/WAThreadView.tsx"
      to: "getMotivoBloqueoWhatsApp"
      via: "cálculo único del motivo para los dos controles del archivo"
      pattern: "getMotivoBloqueoWhatsApp\\("
---

<objective>
Aplicar el guard de teléfono a los tres controles de envío por WhatsApp que cuelgan del drawer de paciente: el botón "Enviar por WhatsApp" del presupuesto y los dos controles de `WAThreadView` (template y free-text), plumbeando el teléfono por la misma cadena de props que ya usa `whatsappOptIn`.

Purpose: ENVIO-03. Hoy esos tres controles sólo miran el opt-in, así que un paciente sin teléfono deja hacer clic y el error recién aparece como toast del backend (67 D-10). El guard lo vuelve visible antes del clic, con el motivo correcto.
Output: tres controles que se deshabilitan por teléfono ausente con el tooltip corto de D-16, sin store ni fetch nuevos.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-CONTEXT.md
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-PATTERNS.md
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-01-SUMMARY.md
</context>

<interfaces>
Contrato del plan 01 (`frontend/src/lib/telefono.ts`), a consumir tal cual:

- `getMotivoBloqueoWhatsApp(telefono: string | null | undefined, whatsappOptIn: boolean | undefined): string | null` — devuelve el motivo de teléfono si falta el número (aunque falte también el opt-in, D-14), el de opt-in si sólo falta ese, o `null` si el control debe quedar habilitado.
- `MOTIVO_SIN_TELEFONO`, `MOTIVO_SIN_OPTIN` — los dos textos, ya centralizados.

Nombre de prop acordado para toda la cadena: `pacienteTelefono?: string | null`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Plumbing del teléfono y guard del botón de presupuesto</name>
  <files>frontend/src/app/dashboard/pacientes/components/PatientDrawer.tsx, frontend/src/components/patient/PatientDrawer/views/MensajesView.tsx, frontend/src/components/patient/PatientDrawer/views/PresupuestosView.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/pacientes/components/PatientDrawer.tsx (el bloque ~`:129-146` donde se renderizan `PresupuestosView` con `pacienteOptIn={...}` y `MensajesView` con `whatsappOptIn={...}` — es el molde exacto del plumbing)
    - frontend/src/components/patient/PatientDrawer/views/MensajesView.tsx (archivo completo, ~23 líneas: eslabón puro que reenvía props a `WAThreadView`)
    - frontend/src/components/patient/PatientDrawer/views/PresupuestosView.tsx (el `type Props` ~`:43-48`, la firma de la función ~`:59` y el bloque Tooltip del botón "Enviar por WhatsApp" ~`:214-245`)
    - frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx (el bloque Tooltip ~`:311-328` es el patrón canónico del repo, con `<span>` intermedio y `TooltipContent` condicional)
    - frontend/src/lib/telefono.ts (helper del plan 01)
  </read_first>
  <action>
**`PatientDrawer.tsx`** — agregar la prop nueva en los DOS call sites, en la línea contigua a la del opt-in, sin cambiar nada más de esos bloques:
- al `<PresupuestosView ...>`, junto a `pacienteOptIn={(paciente as any).whatsappOptIn ?? false}`, agregar `pacienteTelefono={paciente.telefono ?? null}`
- al `<MensajesView ...>`, junto a `whatsappOptIn={(paciente as any).whatsappOptIn ?? false}`, agregar `pacienteTelefono={paciente.telefono ?? null}`

El objeto `paciente` del drawer ya trae el campo, así que no hace falta ningún fetch, hook ni store nuevo. Si el tipo de `paciente` en este archivo obliga a un cast para leer `telefono`, seguí el estilo que ya usa la línea vecina de `whatsappOptIn` en vez de introducir uno distinto — pero preferí acceder sin cast si el tipo ya lo permite (el plan 01 ensanchó `PacienteDetalle.telefono` a `string | null`).

**`MensajesView.tsx`** — es un eslabón puro: agregar `pacienteTelefono?: string | null;` a su `type Props`, desestructurarlo en la firma (con default `null`) y reenviarlo a `<WAThreadView pacienteTelefono={pacienteTelefono} ... />`. Ninguna lógica en este archivo.

**`PresupuestosView.tsx`** — dos cambios:
1. Agregar `pacienteTelefono?: string | null;` al `type Props` y desestructurarlo en la firma con default `null`, al lado de `pacienteOptIn`.
2. En el bloque del botón "Enviar por WhatsApp": calcular una sola vez, arriba del render de la lista, `const motivoBloqueoWA = getMotivoBloqueoWhatsApp(pacienteTelefono, pacienteOptIn);`. Después:
   - el `disabled` pasa de `!pacienteOptIn || sendingWAId === p.id` a `!!motivoBloqueoWA || sendingWAId === p.id` — **conservar la condición de loading tal cual**, sólo se reemplaza la parte del opt-in;
   - el `TooltipContent` pasa de estar condicionado por `!pacienteOptIn` con texto hardcodeado a `{motivoBloqueoWA && <TooltipContent>{motivoBloqueoWA}</TooltipContent>}`.

No tocar el `<span>` intermedio ni la estructura `Tooltip > TooltipTrigger asChild > <span> > <Button>` (D-17): el `<span>` es obligatorio porque un `<Button disabled>` no dispara eventos de puntero y sin él el tooltip nunca aparecería. No tocar el `onClick` ni el `api.post('/whatsapp/presupuesto/{id}/send')` inline.

Los números de línea son del 2026-08-20 — localizar cada punto con `rg -n` antes de editar.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - La prop viaja por los 3 archivos: `rg -c "pacienteTelefono" frontend/src/app/dashboard/pacientes/components/PatientDrawer.tsx frontend/src/components/patient/PatientDrawer/views/MensajesView.tsx frontend/src/components/patient/PatientDrawer/views/PresupuestosView.tsx` reporta 2 o más en `PatientDrawer.tsx` y 1 o más en los otros dos
    - `PresupuestosView` usa el helper y ya no hardcodea el texto: `rg -c "getMotivoBloqueoWhatsApp" frontend/src/components/patient/PatientDrawer/views/PresupuestosView.tsx` devuelve 1 o más, y `rg -cF 'El paciente no tiene opt-in para WhatsApp' frontend/src/components/patient/PatientDrawer/views/PresupuestosView.tsx` devuelve 0
    - La condición de loading del botón se conservó: `rg -n "sendingWAId === p.id" frontend/src/components/patient/PatientDrawer/views/PresupuestosView.tsx` sigue devolviendo la línea dentro del `disabled`
    - El `<span>` intermedio sigue presente: `rg -n -A3 "TooltipTrigger asChild" frontend/src/components/patient/PatientDrawer/views/PresupuestosView.tsx | grep -c "<span>"` devuelve 1 o más
    - No se introdujo fetch/hook/store nuevo: `git diff -- frontend/src/app/dashboard/pacientes/components/PatientDrawer.tsx frontend/src/components/patient/PatientDrawer/views | grep '^+' | grep -cE "useQuery|useState\(|api\.get|create\(\(set"` devuelve 0
    - `cd frontend && npx tsc --noEmit` sale con código 0
    - Comportamiento: abrir el drawer de un paciente SIN teléfono → sección Presupuestos → el botón "Enviar por WhatsApp" está deshabilitado y al pasar el mouse muestra `El paciente no tiene teléfono cargado`
    - Comportamiento (precedencia D-14): un paciente sin teléfono Y sin opt-in muestra el mensaje de teléfono, no el de opt-in
    - Comportamiento anti-regresión: un paciente CON teléfono y CON opt-in tiene el botón HABILITADO y sin tooltip; uno con teléfono pero sin opt-in sigue mostrando `El paciente no tiene opt-in para WhatsApp`
  </acceptance_criteria>
  <done>El teléfono llega por props a `PresupuestosView` y a `MensajesView`, y el botón de envío de presupuesto se deshabilita con el motivo correcto sin perder su condición de loading.</done>
</task>

<task type="auto">
  <name>Task 2: Guard en los dos controles de `WAThreadView`</name>
  <files>frontend/src/components/whatsapp/WAThreadView.tsx</files>
  <read_first>
    - frontend/src/components/whatsapp/WAThreadView.tsx (el `type Props` ~`:26-32`; el control template ~`:204-235`, que usa un TERNARIO `whatsappOptIn ? <Button> : <Tooltip>...`; el control free-text ~`:244-275`, con `Textarea` y botón `Send`, que HOY NO TIENE Tooltip)
    - .planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-PATTERNS.md (sección 1, subsección de `WAThreadView`: documenta que este archivo NO sigue el patrón `disabled={predicado}` de los otros tres sitios, y que el free-text no tiene wrapper que extender)
    - frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx (~`:311-328`, patrón canónico del `<span>` intermedio, para construir el wrapper nuevo)
    - frontend/src/lib/telefono.ts (helper del plan 01)
  </read_first>
  <action>
Agregar `pacienteTelefono?: string | null;` al `type Props` y desestructurarlo en la firma con default `null`. Calcular una sola vez en el cuerpo del componente `const motivoBloqueoWA = getMotivoBloqueoWhatsApp(pacienteTelefono, whatsappOptIn);` y usar ese valor en los dos controles.

**Control 1 — botón "Enviar mensaje" / template.** Ojo: este control NO usa `disabled={predicado}` como los otros tres sitios de la fase; es un ternario `whatsappOptIn ? <Button habilitado> : <Tooltip><span><Button disabled>...`. Adaptarse a esa forma, no convertirla al otro patrón: cambiar la condición del ternario de `whatsappOptIn` a `!motivoBloqueoWA`, y dentro de la rama deshabilitada reemplazar el string hardcodeado del `TooltipContent` por `{motivoBloqueoWA}`. La rama habilitada, sus clases y su `onClick` quedan idénticos.

**Control 2 — `Textarea` + botón `Send` de free-text.** Este control hoy tiene `disabled` crudo sin ninguna explicación visual: no hay Tooltip que extender, hay que construirlo.
- `Textarea`: sumar el motivo a su `disabled`, que pasa de `!canSendFreeText || !whatsappOptIn` a `!canSendFreeText || !!motivoBloqueoWA`. **No** envolver el `Textarea` en un Tooltip: no hay analog de eso en el repo y un textarea deshabilitado con tooltip propio sería un patrón nuevo.
- Botón `Send`: reemplazar el término `!whatsappOptIn` de su array de condiciones `disabled` por `!!motivoBloqueoWA`, conservando los otros tres términos (`!freeText.trim()`, `!canSendFreeText`, `sendFreeText.isPending`) tal cual. Envolverlo en el patrón canónico `Tooltip > TooltipTrigger asChild > <span> > <Button>` con `{motivoBloqueoWA && <TooltipContent>{motivoBloqueoWA}</TooltipContent>}`, copiando la estructura del Control 1 de este mismo archivo. El `<span>` intermedio es obligatorio (D-17): sin él, el `<Button disabled>` no dispara eventos de puntero y el tooltip nunca aparecería. Mantener las clases del botón (`size="icon"`, `h-9 w-9 shrink-0`) y cuidar que el `<span>` no rompa el layout flex del renglón — si hace falta, darle al `<span>` las clases mínimas de layout que ya tenía el botón para no alterar el alto ni el alineado.

El tooltip del free-text sólo muestra el motivo de teléfono/opt-in. La ausencia de ventana de 24h (`!canSendFreeText`) ya se comunica por el `placeholder` del `Textarea` y no entra en `getMotivoBloqueoWhatsApp` — no inventar un tercer motivo.

Los números de línea son del 2026-08-20 y el pattern mapper detectó corrimientos: localizar ambos controles con `rg -n "showTemplateModal|handleSendFreeText|canSendFreeText"` antes de editar.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - El motivo se calcula una sola vez: `rg -c "getMotivoBloqueoWhatsApp" frontend/src/components/whatsapp/WAThreadView.tsx` devuelve 1
    - Ya no queda ningún texto de bloqueo hardcodeado: `rg -cF 'El paciente no tiene opt-in para WhatsApp' frontend/src/components/whatsapp/WAThreadView.tsx` devuelve 0
    - `!whatsappOptIn` ya no aparece como término de habilitación en el archivo: `rg -c "!whatsappOptIn" frontend/src/components/whatsapp/WAThreadView.tsx` devuelve 0
    - El botón `Send` quedó envuelto: el archivo tiene ahora 2 ocurrencias de `TooltipTrigger` (`rg -c "TooltipTrigger" frontend/src/components/whatsapp/WAThreadView.tsx` devuelve 2) y 2 de `<span>` inmediatamente bajo un `TooltipTrigger asChild`
    - Las otras condiciones del botón `Send` se conservaron: `rg -n -A6 'onClick={handleSendFreeText}' frontend/src/components/whatsapp/WAThreadView.tsx` (o el bloque `disabled` equivalente) sigue conteniendo `freeText.trim()`, `canSendFreeText` y `sendFreeText.isPending`
    - El `Textarea` NO fue envuelto en Tooltip: `rg -n -B3 "<Textarea" frontend/src/components/whatsapp/WAThreadView.tsx | grep -c "TooltipTrigger"` devuelve 0
    - `cd frontend && npx tsc --noEmit` sale con código 0
    - Comportamiento: abrir el hilo de WhatsApp de un paciente SIN teléfono → el botón "Enviar mensaje" está deshabilitado y su tooltip dice `El paciente no tiene teléfono cargado`; el botón `Send` también está deshabilitado y muestra el mismo texto al hacer hover
    - Comportamiento anti-regresión: un paciente CON teléfono y CON opt-in ve el botón "Enviar mensaje" HABILITADO (rama habilitada del ternario, sin tooltip) y el botón `Send` habilitado cuando hay ventana de 24h y texto escrito
    - Comportamiento anti-regresión: con teléfono pero sin opt-in, ambos controles siguen deshabilitados con el texto de opt-in
  </acceptance_criteria>
  <done>Los dos controles de `WAThreadView` se deshabilitan por teléfono ausente con el motivo centralizado, el botón `Send` ganó un tooltip que antes no existía y ninguna condición preexistente (ventana de 24h, texto vacío, pending) se perdió.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → API (`POST /whatsapp/...`) | Los tres controles disparan envíos reales. Este plan sólo agrega un guard de UX **antes** del cruce; el cruce en sí no cambia. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-10 | Elevation of Privilege | Deshabilitar botones tomado como control de autorización | transfer | El control autoritativo es server-side: `requireTelefonoParaEnvio()` guarda los 4 paths de `WhatsappService` (67 D-03/D-10) antes de encolar en BullMQ. Este plan es UX preventiva; un cliente modificado que fuerce el POST sigue recibiendo `BadRequestException`. No se remueve ni se debilita ninguna validación de servidor. |
| T-69-11 | Information Disclosure | `TooltipContent` con el motivo de bloqueo | accept | El texto es fijo (`MOTIVO_SIN_TELEFONO` / `MOTIVO_SIN_OPTIN`), no interpola el número ni ningún dato del paciente, y sólo se muestra dentro del drawer, ya autenticado. |
| T-69-12 | Denial of Service | Guard mal calibrado que deshabilite envíos válidos | mitigate | Los criterios de aceptación incluyen escenarios anti-regresión explícitos para paciente CON teléfono + opt-in en los tres controles, y `getMotivoBloqueoWhatsApp` devuelve `null` en ese caso por contrato del plan 01. El riesgo real de falso negativo total está en el calendario y se aísla en el plan 02 + wave 3. |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala paquetes: `Tooltip`, `Button` y `Textarea` ya existen en el repo (shadcn/ui) y el helper es local. Sin superficie de supply chain. |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` sale 0
2. `cd frontend && npm run lint` sin errores nuevos en los 4 archivos
3. `frontend/src/components/whatsapp/SendWAMessageModal.tsx` NO aparece en el diff (D-11: fuera de alcance con razón — sólo se abre desde controles que ya bloquean aguas arriba)
4. El texto de opt-in ya no está duplicado en estos archivos: `rg -lF 'El paciente no tiene opt-in para WhatsApp' frontend/src` no lista `PresupuestosView.tsx` ni `WAThreadView.tsx`
</verification>

<success_criteria>
- Los 3 controles de este plan se deshabilitan cuando el paciente no tiene teléfono, con el tooltip corto de D-16
- El teléfono llega por la misma cadena de props que `whatsappOptIn`, sin store, hook ni fetch nuevos
- Ninguna condición preexistente de habilitación (loading, ventana de 24h, texto vacío) se perdió
</success_criteria>

<output>
Create `.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-05-SUMMARY.md` when done
</output>
