---
phase: 69-consistencia-de-tel-fono-opcional-frontend
plan: 06
type: execute
wave: 3
depends_on: ["69-01", "69-02", "69-04"]
files_modified:
  - frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx
  - frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx
autonomous: true
requirements: [ENVIO-03]

must_haves:
  truths:
    - "D-11: se cierran los 2 controles restantes de la lista cerrada de 5 — el botón WhatsApp de la ficha y el atajo WhatsApp del detalle de turno"
    - "D-13: el guard del calendario corre DESPUÉS de que el teléfono fluye (plan 02: select del backend + mapeo + 3 interfaces); si corriera antes, el botón quedaría deshabilitado para TODOS los pacientes"
    - "D-14: si falta el teléfono el tooltip dice lo del teléfono, tenga o no opt-in"
    - "D-17: se reusa el patrón visual tal cual — `Tooltip > TooltipTrigger asChild > <span> > <Button disabled>` con `TooltipContent` condicional; el `<span>` intermedio no se toca"
  artifacts:
    - path: "frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx"
      provides: "botón WhatsApp de la ficha gateado por teléfono + opt-in"
      contains: "getMotivoBloqueoWhatsApp"
    - path: "frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx"
      provides: "atajo WhatsApp del detalle de turno gateado por teléfono + opt-in"
      contains: "getMotivoBloqueoWhatsApp"
  key_links:
    - from: "frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx"
      to: "event.telefono"
      via: "campo poblado por el mapeo del plan 02, consumido por getMotivoBloqueoWhatsApp"
      pattern: "getMotivoBloqueoWhatsApp\\(\\s*event\\.telefono"
---

<objective>
Cerrar los dos controles de envío por WhatsApp que faltan: el de la grilla de acciones de la ficha del paciente y el atajo del detalle de turno del calendario.

Purpose: completa ENVIO-03 y el criterio de éxito #4 del ROADMAP. El del calendario es el entrypoint de envío más transitado y el único que dependía de que el dato llegara desde el backend — por eso este plan está en la wave 3, después del plan 02.
Output: los 5 controles de la lista cerrada de D-11 quedan gateados por teléfono, con precedencia sobre el opt-in.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-CONTEXT.md
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-PATTERNS.md
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-01-SUMMARY.md
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-02-SUMMARY.md
</context>

<interfaces>
Contrato del plan 01 (`frontend/src/lib/telefono.ts`):
- `getMotivoBloqueoWhatsApp(telefono: string | null | undefined, whatsappOptIn: boolean | undefined): string | null`

Contrato del plan 02 (`CalendarEvent`, declarado en las 3 copias de `frontend/src/app/dashboard/turnos/`):
- `telefono?: string | null` — poblado en el mapeo de `turnos/page.tsx` desde `t.paciente?.telefono ?? null`, que a su vez viene del `select` ensanchado de `obtenerTurnosPorRango`.

**Precondición dura de este plan:** `event.telefono` ya llega con datos reales. Antes de tocar el `disabled`, verificá que el plan 02 está aplicado con `rg -n "telefono: t\.paciente\?\.telefono" frontend/src/app/dashboard/turnos/page.tsx` y `rg -n "telefono: true" backend/src/modules/turnos/turnos.service.ts`. Si alguno de los dos no está, **detené el plan**: implementar el guard sin el dato deshabilita el botón para todos los pacientes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Guard del botón WhatsApp en la ficha del paciente</name>
  <files>frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx (el bloque Tooltip del botón "WhatsApp" de la grilla de acciones, ~`:311-328`; el archivo ya usa `paciente.telefono` en el display migrado por el plan 04, así que el dato está en scope sin plumbing)
    - frontend/src/lib/telefono.ts (helper del plan 01; el import de `formatTelefono` ya fue agregado por el plan 04 — extenderlo, no duplicar la línea de import)
  </read_first>
  <action>
Este componente ya tiene `paciente.telefono` en scope (lo usa el display migrado por el plan 04): cero plumbing.

Calcular una vez en el cuerpo del componente `const motivoBloqueoWA = getMotivoBloqueoWhatsApp(paciente.telefono, (paciente as any).whatsappOptIn);` — mantener el cast del opt-in tal como está hoy en el archivo, no cambiarlo en esta fase.

En el bloque del botón "WhatsApp":
- `disabled` pasa de `!(paciente as any).whatsappOptIn` a `!!motivoBloqueoWA`
- el render condicional del tooltip pasa de `{!(paciente as any).whatsappOptIn && <TooltipContent>El paciente no tiene opt-in para WhatsApp</TooltipContent>}` a `{motivoBloqueoWA && <TooltipContent>{motivoBloqueoWA}</TooltipContent>}`

No tocar el `onClick` (`setWaModalOpen(true)`), ni el ícono `MessageSquare`, ni las clases del botón, ni el `<span>` intermedio del `TooltipTrigger asChild` — el `<span>` es obligatorio (D-17) porque un `<Button disabled>` no dispara eventos de puntero. Extender la línea de import de `@/lib/telefono` que el plan 04 ya dejó en este archivo, en vez de agregar un segundo import del mismo módulo.

`SendWAMessageModal` no recibe guard propio ni el teléfono: sólo se abre desde este control y desde el de la Task 2, que ya bloquean aguas arriba (D-11).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Usa el helper: `rg -c "getMotivoBloqueoWhatsApp" frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx` devuelve 1
    - Ya no hardcodea el texto de opt-in: `rg -cF 'El paciente no tiene opt-in para WhatsApp' frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx` devuelve 0
    - Un solo import del módulo de teléfono: `rg -c "from \"@/lib/telefono\"|from '@/lib/telefono'" frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx` devuelve 1
    - El `<span>` intermedio sigue presente: `rg -n -A3 "TooltipTrigger asChild" frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx | grep -c "<span>"` devuelve 1 o más
    - El `onClick` del botón no cambió: `git diff -- frontend/src/app/dashboard/pacientes/components/PacienteDetails.tsx | grep '^[+-]' | grep -c 'setWaModalOpen'` devuelve 0
    - `cd frontend && npx tsc --noEmit` sale con código 0
    - Comportamiento: ficha de un paciente SIN teléfono → el botón "WhatsApp" de la grilla de acciones está deshabilitado y el tooltip dice `El paciente no tiene teléfono cargado`
    - Comportamiento anti-regresión: ficha de un paciente CON teléfono y CON opt-in → botón HABILITADO, sin tooltip, y el clic sigue abriendo el modal de envío
    - Comportamiento: paciente CON teléfono y SIN opt-in → deshabilitado con el texto de opt-in (sin cambios respecto de hoy)
  </acceptance_criteria>
  <done>El botón WhatsApp de la ficha se deshabilita por teléfono ausente con el motivo centralizado, sin haber tocado su onClick ni su estructura visual.</done>
</task>

<task type="auto">
  <name>Task 2: Guard del atajo WhatsApp en el detalle de turno</name>
  <files>frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx (su copia local de `interface CalendarEvent` ~`:51-62`, que el plan 02 ya ensanchó con `telefono?: string | null`; y el bloque del atajo WhatsApp ~`:366-386`, condicionado por `event.pacienteId`)
    - frontend/src/app/dashboard/turnos/page.tsx (confirmar que el mapeo puebla `telefono` — precondición de este plan)
    - backend/src/modules/turnos/turnos.service.ts (confirmar `telefono: true` en el `select` de `obtenerTurnosPorRango` — precondición de este plan)
    - frontend/src/lib/telefono.ts (helper del plan 01)
  </read_first>
  <action>
**Antes de editar**, verificar las dos precondiciones del plan 02 con los `rg` listados en `<interfaces>`. Si el `select` del backend o el mapeo no están aplicados, detener y reportar: sin el dato, este guard deshabilita el botón para todos los pacientes del calendario, incluidos los que sí tienen número. Ese es el falso negativo total que la wave 3 existe para evitar.

Calcular una vez en el cuerpo del componente `const motivoBloqueoWA = getMotivoBloqueoWhatsApp(event.telefono, event.whatsappOptIn);` y aplicarlo al bloque del atajo:
- `disabled` pasa de `!event.whatsappOptIn` a `!!motivoBloqueoWA`
- el tooltip pasa de `{!event.whatsappOptIn && <TooltipContent>El paciente no tiene opt-in para WhatsApp</TooltipContent>}` a `{motivoBloqueoWA && <TooltipContent>{motivoBloqueoWA}</TooltipContent>}`

Mantener intactos: la condición externa `{event.pacienteId && (...)}` que decide si el atajo se muestra, el `onClick` (`setWaModalOpen(true)`), el ícono `MessageSquare`, las clases del botón y el `<span>` intermedio del `TooltipTrigger asChild` (D-17).

No re-declarar `telefono` en la interfaz local: el plan 02 ya lo hizo en las tres copias. Si `rg -n "telefono" frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx` no muestra el campo en la interfaz, el plan 02 quedó incompleto — reportarlo en vez de parchear acá.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Usa el helper con el campo correcto: `rg -c "getMotivoBloqueoWhatsApp" frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx` devuelve 1 y esa llamada recibe `event.telefono` como primer argumento
    - Ya no hardcodea el texto de opt-in: `rg -cF 'El paciente no tiene opt-in para WhatsApp' frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx` devuelve 0
    - La condición de visibilidad del atajo no cambió: `git diff -- frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx | grep '^[+-]' | grep -c 'event.pacienteId'` devuelve 0
    - El `<span>` intermedio sigue presente: `rg -n -A3 "TooltipTrigger asChild" frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx | grep -c "<span>"` devuelve 1 o más
    - La cadena de datos está completa: `rg -c "telefono: true" backend/src/modules/turnos/turnos.service.ts` devuelve 1, `rg -c "telefono: t\.paciente\?\.telefono" frontend/src/app/dashboard/turnos/page.tsx` devuelve 1 y las 3 copias de `CalendarEvent` declaran el campo
    - `cd frontend && npx tsc --noEmit` sale con código 0
    - **Comportamiento anti-regresión crítico (D-13)**: en el calendario, abrir el detalle de un turno de un paciente CON teléfono y CON opt-in → el botón "WhatsApp" está HABILITADO. Si aparece deshabilitado, el dato no está fluyendo y el plan 02 no está bien aplicado
    - Comportamiento: detalle de un turno de un paciente SIN teléfono → botón deshabilitado con tooltip `El paciente no tiene teléfono cargado`
    - Comportamiento: paciente CON teléfono y SIN opt-in → deshabilitado con el texto de opt-in
  </acceptance_criteria>
  <done>El atajo WhatsApp del detalle de turno se deshabilita sólo cuando corresponde: habilitado para pacientes con teléfono y opt-in, con el motivo correcto en los dos casos de bloqueo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → API (`POST /whatsapp/...`) | Los dos controles abren `SendWAMessageModal`, que dispara el envío. Este plan agrega guard de UX antes del cruce; el cruce no cambia. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-13 | Elevation of Privilege | Deshabilitar botones tomado como control de autorización | transfer | La autoridad sigue siendo `requireTelefonoParaEnvio()` en `WhatsappService` (67 D-03/D-10), server-side sobre los 4 paths de envío. Este plan no toca ninguna validación de servidor. |
| T-69-14 | Denial of Service | Guard aplicado sin el dato del backend → botón deshabilitado para TODOS los pacientes del calendario | mitigate | `depends_on: 69-02` + wave 3 garantizan que el `select`, el mapeo y las 3 interfaces estén aplicados antes. La Task 2 además verifica ambas precondiciones con `rg` antes de editar y tiene un criterio de aceptación anti-regresión explícito sobre un paciente con teléfono y opt-in. |
| T-69-15 | Information Disclosure | `event.telefono` presente en el estado del cliente del calendario | accept | El campo lo introdujo el plan 02 (ver T-69-03/T-69-04). Este plan sólo lo lee para un booleano de UI: no lo renderiza, no lo loguea y no lo envía a ningún lado. |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala paquetes: consume el helper local y componentes shadcn/ui ya presentes. Sin superficie de supply chain. |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` sale 0
2. `cd frontend && npm run lint` sin errores nuevos en los dos archivos
3. El texto de opt-in ya no está duplicado en ningún control WA: `rg -lF 'El paciente no tiene opt-in para WhatsApp' frontend/src` devuelve únicamente `frontend/src/lib/telefono.ts`
4. Los 5 controles de D-11 usan el helper: `rg -l "getMotivoBloqueoWhatsApp" frontend/src` lista exactamente `PacienteDetails.tsx`, `AppointmentDetailModal.tsx`, `PresupuestosView.tsx` y `WAThreadView.tsx` (4 archivos, 5 controles)
5. `SendWAMessageModal.tsx` sigue sin guard propio y sin recibir teléfono (D-11)
</verification>

<success_criteria>
- Criterio de éxito #4 del ROADMAP: los botones de envío por WhatsApp aparecen deshabilitados con tooltip explicativo cuando el paciente no tiene teléfono
- El botón del calendario sigue HABILITADO para pacientes con teléfono y opt-in (sin falso negativo total)
- Los 5 controles de la lista cerrada de D-11 salen del mismo helper, sin textos hardcodeados
</success_criteria>

<output>
Create `.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-06-SUMMARY.md` when done
</output>
