---
phase: 69-consistencia-de-tel-fono-opcional-frontend
plan: 07
type: execute
wave: 2
depends_on: ["69-09", "69-10"]
files_modified:
  - backend/src/modules/turnos/turnos.service.ts
  - backend/src/modules/turnos/turnos.service.spec.ts
  - frontend/src/hooks/useTurnosRangos.ts
  - frontend/src/app/dashboard/turnos/page.tsx
  - frontend/src/app/dashboard/turnos/__tests__/AppointmentDetailModal.test.tsx
autonomous: true
gap_closure: true
requirements: [ENVIO-03]

must_haves:
  truths:
    - "D-11: el control 5 de la lista cerrada — el atajo WhatsApp del detalle de turno del calendario — EXISTE en el DOM en la app corriendo; hoy no renderiza nunca porque `event.pacienteId` es siempre `undefined`"
    - "D-13: la cadena de plumbing del calendario queda completa de punta a punta: `select` de `obtenerTurnosPorRango` → respuesta HTTP → tipo `TurnoRango` → mapeo de `turnos/page.tsx` → `CalendarEvent.pacienteId` y `CalendarEvent.telefono`; el guard visual del plan 06 ya está escrito y sólo le faltaba el dato"
    - "D-15: `getMotivoBloqueoWhatsApp(event.telefono, event.whatsappOptIn)` — ya invocado por el plan 06 — pasa de ser código muerto a evaluarse en cada apertura del detalle de turno"
    - "D-17: el patrón `Tooltip > TooltipTrigger asChild > <span> > <Button disabled>` del atajo NO se toca en este plan; el JSX del control ya es correcto, lo que se repara es su alcanzabilidad"
    - "El límite de la API del calendario vuelve a estar tipado: sin `as any`, el compilador es quien impide que un campo leído por el mapeo falte en el `select` del backend (esta es la regresión que dejó pasar el bug original, WR-01)"
  artifacts:
    - path: "backend/src/modules/turnos/turnos.service.ts"
      provides: "el `select` de obtenerTurnosPorRango devuelve el FK escalar pacienteId"
      contains: "pacienteId: true"
    - path: "backend/src/modules/turnos/turnos.service.spec.ts"
      provides: "test de regresión que falla si alguien vuelve a sacar pacienteId/telefono del select"
      contains: "obtenerTurnosPorRango"
    - path: "frontend/src/hooks/useTurnosRangos.ts"
      provides: "tipo TurnoRango que espeja exactamente la respuesta del endpoint (pacienteId, paciente.telefono, paciente.whatsappOptIn, esSobreturno)"
      contains: "pacienteId: string"
    - path: "frontend/src/app/dashboard/turnos/page.tsx"
      provides: "mapeo turnos → CalendarEvent sin cast, con pacienteId poblado"
      contains: "t.pacienteId ?? t.paciente?.id"
  key_links:
    - from: "backend/src/modules/turnos/turnos.service.ts"
      to: "frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx"
      via: "select.pacienteId → TurnoRango.pacienteId → CalendarEvent.pacienteId → condición de visibilidad del atajo WhatsApp"
      pattern: "pacienteId: t\\.pacienteId \\?\\? t\\.paciente\\?\\.id"
    - from: "frontend/src/app/dashboard/turnos/page.tsx"
      to: "frontend/src/hooks/useTurnosRangos.ts"
      via: "el mapeo consume TurnoRango sin cast — el chequeo de tipos está encendido en el límite de la API"
      pattern: "turnosRango\\s*$|turnosRango\\n"
---

<objective>
Cerrar CR-01: hacer que el atajo de WhatsApp del detalle de turno **exista en el DOM**. Hoy el guard del plan 06 está bien escrito pero es inalcanzable, porque todo el bloque cuelga de `{event.pacienteId && (...)}` y ese campo es siempre `undefined`.

Purpose: es el gap bloqueante #1 del `69-VERIFICATION.md` contra el criterio de éxito #4 / ENVIO-03. Sin esto, el control 5 de la lista cerrada de D-11 es código muerto en producción y el `telefono: true` que el plan 02 agregó al `select` expone PII sin comprar ningún beneficio funcional.
Output: `pacienteId` viaja del `select` de Prisma al `CalendarEvent`, el límite de la API queda tipado sin `as any`, y un test de jest impide que el `select` vuelva a perder los campos.

**Ampliación de alcance deliberada (no forzada — elegida):** al retirar el cast `as any[]`, cada campo que el mapeo lee tiene que existir en `TurnoRango`. Uno de ellos, `esSobreturno` (`turnos/page.tsx:299`), tampoco está en el `select` y hoy es siempre `undefined`, por lo que el borde punteado naranja que `CalendarGrid.tsx:589,596,625` renderiza para los sobreturnos **nunca aparece**.

Hay tres salidas posibles y el compilador acepta las tres — esto **no** es una consecuencia forzada:
1. Declarar `esSobreturno?: boolean` opcional en `TurnoRango`. Compila y preserva el comportamiento actual, pero deja permanentemente en el tipo un campo que el endpoint nunca manda: exactamente el type drift que esta fase vino a matar, y el mismo patrón que produjo CR-01.
2. Borrar la línea `esSobreturno` del mapeo. Compila, pero elimina en silencio una feature que la UI sí sabe dibujar y que alguien escribió a propósito.
3. **La elegida:** seleccionarlo en el backend, alineando el tipo con la respuesta real.

Se elige (3) porque (1) reproduce la clase de defecto que estamos cerrando y (2) borra funcionalidad sin decidirlo explícitamente. **Consecuencia visible y buscada:** los turnos marcados como sobreturno pasan a mostrarse con su borde punteado naranja — un cambio de UI que no estaba en los gaps del `69-VERIFICATION.md`. Está verificado por su propio criterio de aceptación de comportamiento en la Task 3, con el mismo rigor que se le exige al botón de WhatsApp.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-CONTEXT.md
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-VERIFICATION.md
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-02-SUMMARY.md
@.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-06-SUMMARY.md
</context>

<interfaces>
Estado real verificado del código (leído durante el planning, 2026-08-21):

`backend/src/modules/turnos/turnos.service.ts:561-589` — el `select` de `obtenerTurnosPorRango` tiene hoy exactamente: `id`, `inicio`, `fin`, `estado`, `observaciones`, `paciente: { select: { id, nombreCompleto, whatsappOptIn, telefono } }`, `tipoTurno: { select: { id, nombre, flujoPaciente } }`, `entradaHC: { select: { tipoEntrada, contenido } }`. **No** trae `pacienteId` ni `esSobreturno`.

`backend/src/modules/turnos/turnos.service.ts:593-605` — el `.map()` final destructura `entradaHC` afuera y spreadea `...rest`, agregando `ultimoTratamiento`, `tratamientos` y `tipoEntradaHC`. Cualquier campo escalar que se agregue al `select` viaja solo por el spread: no hay que tocar el mapper del service.

`backend/src/prisma/schema.prisma` (modelo `Turno`) — `pacienteId String` (NO nullable), `esSobreturno Boolean @default(false)`, relación `paciente` obligatoria. El enum `EstadoTurno` (línea 1126) tiene exactamente los 7 miembros `PENDIENTE | CONFIRMADO | CANCELADO | AUSENTE | FINALIZADO | EN_ESPERA | SIENDO_ATENDIDO`, que coinciden 1:1 con la unión declarada en `CalendarEvent.estado`.

`frontend/src/hooks/useTurnosRangos.ts:4-16` — `TurnoRango` declara hoy: `id`, `inicio`, `fin`, `estado: string`, `observaciones?`, `paciente: { id; nombreCompleto }`, `tipoTurno: { id; nombre; flujoPaciente? }`, `ultimoTratamiento?`, `tratamientos?`, `tipoEntradaHC?`. Le faltan **cuatro** de los campos que el mapeo lee: `pacienteId`, `paciente.whatsappOptIn`, `paciente.telefono`, `esSobreturno`.

`frontend/src/app/dashboard/turnos/page.tsx:283-300` — el mapeo. Línea 283: `(turnosRango as any[])`. Línea 290: `pacienteId: t.pacienteId ?? undefined`. Líneas 291-292: `whatsappOptIn` y `telefono` desde `t.paciente?.*` (esos sí llegan con dato real). Línea 299: `esSobreturno: t.esSobreturno ?? false`.

Otros consumidores de `TurnoRango` (verificados: no construyen el objeto, sólo lo consumen; ensanchar el tipo es aditivo para ellos):
- `frontend/src/app/dashboard/components/QuickAppointment.tsx:113,121` — compara `estado` contra `"CANCELADO"` y `"AUSENTE"` (ambos en la unión).
- `frontend/src/components/live-turno/tabs/NuevoTurnoTab.tsx:106,113` — idem.
- `frontend/src/app/dashboard/pacientes/components/TratamientosTab.tsx:58,91,234,260` — usa `estado` como clave de un acumulador y lo pasa a `getEstadoTurnoChip(estado: string)` (`frontend/src/lib/estadoTurno.ts:6`), que acepta `string`: una unión de literales es asignable.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Devolver pacienteId (y esSobreturno) desde obtenerTurnosPorRango, con test de regresión</name>
  <files>backend/src/modules/turnos/turnos.service.ts, backend/src/modules/turnos/turnos.service.spec.ts</files>
  <read_first>
    - backend/src/modules/turnos/turnos.service.ts (líneas 531-606: la firma, la validación de fechas, el `select` completo y el `.map()` que spreadea `...rest`)
    - backend/src/modules/turnos/turnos.service.spec.ts (líneas 60-120: el `beforeEach` que arma el mock de `PrismaService` con `jest.fn()` por modelo y el `Test.createTestingModule`; y cualquier `describe` existente para copiar el registro de nombres en español con referencia a la decisión)
    - backend/src/modules/turnos/turnos.controller.ts (líneas 22-24 y 122-139: el `@Auth('ADMIN','PROFESIONAL','SECRETARIA')` de clase y el handler `obtenerPorRango`, que NO llama `resolveScope` — relevante para el threat model, no se modifica)
    - backend/src/prisma/schema.prisma (modelo `Turno`: confirmar que `pacienteId` es `String` no nullable y `esSobreturno` es `Boolean @default(false)`)
  </read_first>
  <action>
En el `select` de `obtenerTurnosPorRango` (`turnos.service.ts`, ~línea 561), agregar dos campos escalares **a nivel raíz del turno**, no dentro del `select` anidado de `paciente`:
- `pacienteId: true` — es el FK escalar. Es el campo del que depende la condición de visibilidad `{event.pacienteId && (...)}` en `AppointmentDetailModal.tsx:371`. El `paciente.id` anidado que ya se selecciona NO llega a esa condición porque el mapeo del frontend lee la raíz.
- `esSobreturno: true` — leído por el mapeo en `turnos/page.tsx:299` y renderizado por `CalendarGrid.tsx:589,596,625`. Ver la nota de efecto colateral en `<objective>`.

No tocar nada más del `select`: `telefono: true` dentro de `paciente.select` ya está (lo puso el plan 02) y se conserva. No tocar el `.map()` de las líneas 593-605 — los escalares nuevos viajan solos por el `...rest`.

En `turnos.service.spec.ts`, agregar `findMany: jest.fn()` al mock del modelo `turno` dentro del objeto de mocks del `beforeEach` (hoy ese modelo sólo mockea `findFirst`, `findUnique`, `create`, `update`). Es aditivo: no cambia el comportamiento de los `describe` existentes.

Agregar un `describe` nuevo — sugerido `obtenerTurnosPorRango — el select devuelve el FK pacienteId (CR-01 / D-13)` — con dos `it`:
1. **Test del contrato del select:** mockear `prisma.turno.findMany` para resolver `[]`, invocar `service.obtenerTurnosPorRango('prof-1', '2026-01-01', '2026-01-31')`, y assertar sobre el primer argumento con el que se llamó a `findMany` que `arg.select.pacienteId === true`, `arg.select.esSobreturno === true` y `arg.select.paciente.select.telefono === true`. Este test es la protección de regresión: falla si alguien vuelve a angostar el `select`.
2. **Test del passthrough:** mockear `findMany` para resolver un array con una fila con la forma del select real (`id`, `inicio`, `fin`, `estado`, `observaciones`, `pacienteId`, `esSobreturno`, `paciente: { id, nombreCompleto, whatsappOptIn, telefono }`, `tipoTurno: { id, nombre, flujoPaciente }`, `entradaHC: null`) y assertar que el elemento devuelto por el service conserva `pacienteId` y `telefono` — es decir, que el `...rest` no los pierde.

Usar fechas en formato `yyyy-MM-dd` en ambos tests: el service valida el formato con regex antes de tocar Prisma y tira `BadRequestException` si no matchea.
  </action>
  <verify>
    <automated>cd backend && npx jest src/modules/turnos/turnos.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "pacienteId: true" backend/src/modules/turnos/turnos.service.ts` devuelve al menos 1, y esa línea está dentro del `select` de `obtenerTurnosPorRango` (verificable con `rg -n "async obtenerTurnosPorRango" -A 60 backend/src/modules/turnos/turnos.service.ts | rg -n "pacienteId: true"`)
    - `rg -n "async obtenerTurnosPorRango" -A 60 backend/src/modules/turnos/turnos.service.ts | rg -c "esSobreturno: true"` devuelve 1
    - El `telefono: true` del plan 02 sigue en pie: `rg -n "async obtenerTurnosPorRango" -A 60 backend/src/modules/turnos/turnos.service.ts | rg -c "telefono: true"` devuelve 1
    - `cd backend && npx jest src/modules/turnos/turnos.service.spec.ts` sale con código 0 y reporta más tests que antes del cambio (los 2 nuevos incluidos)
    - Prueba negativa del test de regresión: si se quita temporalmente `pacienteId: true` del `select`, el primer `it` nuevo FALLA. (Ejecutar esta comprobación, restaurar la línea, y dejar constancia del resultado en el SUMMARY. Si el test pasa igual con el campo removido, el test no sirve y hay que rehacerlo.)
    - Los `describe` preexistentes de `turnos.service.spec.ts` siguen pasando: el conteo de tests fallidos es 0
    - `backend/src/modules/turnos/turnos.controller.ts` NO fue modificado: `git diff --name-only -- backend/src/modules/turnos/turnos.controller.ts` no devuelve nada
  </acceptance_criteria>
  <done>El endpoint `GET /turnos/rango` devuelve `pacienteId` y `esSobreturno` a nivel raíz de cada turno, y hay un test automatizado que falla si el `select` vuelve a perderlos.</done>
</task>

<task type="auto">
  <name>Task 2: Tipar el límite de la API del calendario y poblar pacienteId en el mapeo</name>
  <files>frontend/src/hooks/useTurnosRangos.ts, frontend/src/app/dashboard/turnos/page.tsx</files>
  <read_first>
    - frontend/src/hooks/useTurnosRangos.ts (el tipo `TurnoRango` completo, líneas 4-16, y la `queryKey` del hook)
    - frontend/src/app/dashboard/turnos/page.tsx (líneas 41-55: la copia local de `interface CalendarEvent`, incluida la unión de `estado` y `esSobreturno?: boolean`; y líneas 275-303: el `useEffect` del mapeo con el cast de la línea 283)
    - frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx (líneas 52-64 y 366-390: confirmar que `CalendarEvent.pacienteId` es el campo del que cuelga el atajo y que el guard `motivoBloqueoWA` ya está escrito — este archivo NO se modifica)
    - frontend/src/app/dashboard/components/QuickAppointment.tsx (líneas 113-125: consumidor de `TurnoRango`, verificar que las comparaciones de `estado` siguen compilando)
    - frontend/src/components/live-turno/tabs/NuevoTurnoTab.tsx (líneas 106-115: idem)
    - frontend/src/app/dashboard/pacientes/components/TratamientosTab.tsx (líneas 50-100 y 230-262: idem, incluye `getEstadoTurnoChip(turno.estado)`)
  </read_first>
  <action>
**Precondición dura:** la Task 1 tiene que estar aplicada. Verificar con `rg -n "async obtenerTurnosPorRango" -A 60 backend/src/modules/turnos/turnos.service.ts | rg -n "pacienteId: true"`. Si no aparece, detener el plan: tipar `pacienteId` como obligatorio sin que el backend lo mande deja el campo `undefined` en runtime con el compilador diciendo que existe — peor que el bug actual (D-13: el dato va antes que el consumidor).

En `frontend/src/hooks/useTurnosRangos.ts`, ensanchar `TurnoRango` para que espeje **exactamente** la respuesta del endpoint tal como quedó tras la Task 1. Campos a agregar/ajustar:
- `pacienteId: string` — obligatorio, no opcional. En Prisma `Turno.pacienteId` es `String` no nullable, así que el endpoint siempre lo manda. Declararlo obligatorio es lo que convierte al compilador en la red de seguridad de CR-01.
- `esSobreturno: boolean` — obligatorio (`@default(false)` en el schema, siempre presente).
- Dentro de `paciente`: agregar `whatsappOptIn: boolean` y `telefono: string | null`. El `telefono` es nullable en el schema desde la Phase 67 — `string | null`, sin `?`, sin `as any` (D-07).
- `estado`: pasa de `string` a la unión literal de los 7 estados del enum `EstadoTurno`, para que `estado: t.estado` compile contra `CalendarEvent.estado` sin cast. Exportar esa unión como tipo con nombre desde el mismo archivo (sugerido `EstadoTurnoRango`) en vez de inlinearla, así los demás consumidores pueden referenciarla. Los 7 miembros, en el orden del enum: `PENDIENTE`, `CONFIRMADO`, `CANCELADO`, `AUSENTE`, `FINALIZADO`, `EN_ESPERA`, `SIENDO_ATENDIDO`.

No inventar campos que el endpoint no devuelve. Si al compilar aparece un campo leído por algún consumidor que no está en el `select` del backend, eso es un hallazgo del mismo tipo que CR-01: reportarlo en el SUMMARY en vez de taparlo con un campo opcional de relleno.

En `frontend/src/app/dashboard/turnos/page.tsx`:
- Línea 283: eliminar el cast `as any[]`. El `.filter(...).map(...)` pasa a correr sobre `TurnoRango[]` tipado. Este cast es WR-01 y es la razón por la que el compilador no detectó que `t.pacienteId` no existía; retirarlo es protección de regresión, no limpieza cosmética.
- Línea 290: cambiar `pacienteId: t.pacienteId ?? undefined` por `pacienteId: t.pacienteId ?? t.paciente?.id ?? undefined`. El fallback a `paciente.id` es cinturón y tirantes por si algún despliegue del backend queda atrás respecto del frontend.
- No tocar las líneas 291-292 (`whatsappOptIn` y `telefono`): el plan 02 ya las dejó bien y ahora quedan además verificadas por tipos.
- No tocar `esSobreturno: t.esSobreturno ?? false` (línea 299): con el tipo obligatorio el `?? false` queda redundante pero es inofensivo; dejarlo evita ruido en el diff.

Si el `tsc` marca errores en `QuickAppointment.tsx`, `NuevoTurnoTab.tsx` o `TratamientosTab.tsx` por el angostamiento de `estado`, arreglarlos en esos archivos (todas sus comparaciones verificadas usan literales que pertenecen a la unión, así que no debería haber ninguno) y agregar esos archivos al SUMMARY. **No** revertir el angostamiento ni reintroducir un cast para silenciarlos.

No modificar `AppointmentDetailModal.tsx` en este plan: su guard y su JSX ya son correctos (D-17); lo único que les faltaba era el dato.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` sale con código 0
    - El cast murió: `rg -c "turnosRango as any" frontend/src/app/dashboard/turnos/page.tsx` devuelve 0
    - No quedó ningún `as any` en el mapeo: `rg -n "as any" frontend/src/app/dashboard/turnos/page.tsx` no devuelve ninguna línea dentro del bloque 275-303
    - El fallback está: `rg -c "t.pacienteId \?\? t.paciente\?\.id" frontend/src/app/dashboard/turnos/page.tsx` devuelve 1
    - El tipo es obligatorio, no opcional: `rg -c "pacienteId: string;" frontend/src/hooks/useTurnosRangos.ts` devuelve 1 y `rg -c "pacienteId\?" frontend/src/hooks/useTurnosRangos.ts` devuelve 0
    - `telefono` en el tipo es nullable sin `?`: `rg -c "telefono: string \| null" frontend/src/hooks/useTurnosRangos.ts` devuelve 1
    - **Prueba de que el chequeo de tipos volvió a estar encendido (falsable):** quitar temporalmente `pacienteId: true` del `select` del backend NO alcanza para que `tsc` falle (son proyectos separados), así que la prueba equivalente es: renombrar temporalmente `pacienteId` a `pacienteIdX` en `TurnoRango` y confirmar que `npx tsc --noEmit` AHORA FALLA en `turnos/page.tsx`. Restaurar y dejar constancia del resultado en el SUMMARY. Antes de este plan esa mutación no producía ningún error.
    - `AppointmentDetailModal.tsx` no fue modificado: `git diff --name-only -- frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx` no devuelve nada
    - La verificación de comportamiento del control (que el botón EXISTE en el DOM, habilitado o deshabilitado según el teléfono) es responsabilidad de la Task 3 — este task no se da por cerrado sin ella
  </acceptance_criteria>
  <done>El atajo de WhatsApp del detalle de turno renderiza para todo turno del calendario, y el mapeo `turnos → CalendarEvent` está tipado de punta a punta sin cast.</done>
</task>

<task type="auto">
  <name>Task 3: Test de render que prueba que el atajo de WhatsApp EXISTE en el DOM</name>
  <files>frontend/src/app/dashboard/turnos/__tests__/AppointmentDetailModal.test.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx (el componente bajo test: su lista de props, la copia local de `CalendarEvent`, la condición `{event.pacienteId && (...)}` de `:371`, el `disabled` y el `TooltipContent` del botón `:370-390`, y qué providers/contexts necesita para montar)
    - frontend/src/lib/telefono.ts (`MOTIVO_SIN_TELEFONO` — el texto exacto a assertar; importarlo, no hardcodearlo)
    - frontend/vitest.config.ts (creado por el plan 09: confirmar que `test.include` alcanza a `src/**/__tests__/*.test.tsx`)
    - frontend/src/lib/__tests__/telefono.test.ts (creado por el plan 09: la convención de tests del repo)
  </read_first>
  <action>
**Precondición dura:** el plan 09 tiene que estar aplicado (`cd frontend && npm test` debe correr). Si no, detener — este task no tiene runner sobre el cual apoyarse.

Crear `frontend/src/app/dashboard/turnos/__tests__/AppointmentDetailModal.test.tsx` que monte `AppointmentDetailModal` con Testing Library y pruebe la **alcanzabilidad** del atajo de WhatsApp — la propiedad que ningún `rg` puede verificar y cuya ausencia es exactamente el gap CR-01.

Tres casos, todos con un `event` que tenga `pacienteId` poblado (el estado post-fix):
1. **Paciente CON teléfono y CON opt-in:** el botón de WhatsApp **está en el documento** y **no** está deshabilitado.
2. **Paciente SIN teléfono** (`telefono: null`): el botón **está en el documento** y **sí** está deshabilitado. Assertar además que el texto de `MOTIVO_SIN_TELEFONO` es alcanzable — importar la constante de `@/lib/telefono` en vez de escribir el string a mano, para que un cambio del copy no deje el test verde por accidente.
3. **`pacienteId` ausente** (`undefined`) — el estado PRE-fix: el botón **no** está en el documento. Este caso es el que documenta el bug que se está cerrando y falla si alguien vuelve a romper la cadena.

Localizar el botón por rol y nombre accesible (`getByRole('button', { name: /whatsapp/i })`) y usar `queryByRole` para el caso 3, donde la ausencia es lo que se afirma. **No** localizar por `data-testid` ni por clase CSS: el punto del test es que el control sea alcanzable como lo alcanza un usuario.

Montar sólo los providers que el componente realmente necesite — leer sus imports primero. Si requiere `QueryClientProvider`, envolver con un `QueryClient` nuevo por test. Mockear con `vi.mock` los módulos que hagan red (`@/lib/api`) para que el test no dependa del backend. Si el tooltip de Radix necesita un `TooltipProvider`, incluirlo; si el contenido del tooltip sólo aparece en hover, alcanza con assertar el estado `disabled` del botón y la presencia del texto vía el atributo accesible que el patrón D-17 ya expone — no forzar una interacción de hover frágil.

Este task **no** modifica `AppointmentDetailModal.tsx`: su JSX ya es correcto (D-17), lo que se agrega es la prueba de que ahora se alcanza.
  </action>
  <verify>
    <automated>cd frontend && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test` sale con código 0 e incluye los 3 tests nuevos de `AppointmentDetailModal`
    - El texto del tooltip no está hardcodeado: `rg -c "MOTIVO_SIN_TELEFONO" frontend/src/app/dashboard/turnos/__tests__/AppointmentDetailModal.test.tsx` devuelve al menos 1, y `rg -c "El paciente no tiene teléfono cargado" frontend/src/app/dashboard/turnos/__tests__/AppointmentDetailModal.test.tsx` devuelve 0
    - El control se busca por rol accesible, no por testid: `rg -c "getByRole|queryByRole" frontend/src/app/dashboard/turnos/__tests__/AppointmentDetailModal.test.tsx` devuelve al menos 3 y `rg -c "data-testid" frontend/src/app/dashboard/turnos/__tests__/AppointmentDetailModal.test.tsx` devuelve 0
    - **Prueba negativa (falsable) — la que prueba que el test detecta el bug original:** revertir temporalmente la Task 2 (volver `pacienteId: t.pacienteId ?? undefined` sin el fallback, o forzar `pacienteId: undefined` en el fixture del caso 1) y confirmar que el caso 1 FALLA porque el botón no está en el documento. Restaurar y registrar el resultado en el SUMMARY. Si el caso 1 pasa con `pacienteId` ausente, el test no prueba alcanzabilidad y hay que rehacerlo
    - `AppointmentDetailModal.tsx` sigue sin modificarse: `git diff --name-only -- frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx` no devuelve nada
    - `cd frontend && npx tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Hay un test automatizado que falla si el atajo de WhatsApp vuelve a no renderizar — la propiedad exacta que el `69-VERIFICATION.md` encontró rota y que ningún grep detecta.</done>
</task>

<task type="auto">
  <name>Task 4: Verificar el efecto colateral declarado del borde de sobreturnos</name>
  <files>frontend/src/app/dashboard/turnos/__tests__/AppointmentDetailModal.test.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/turnos/CalendarGrid.tsx (líneas ~585-630: dónde se decide el borde punteado a partir de `esSobreturno`, y qué props necesita `CalendarGrid` para montar)
    - frontend/src/app/dashboard/turnos/__tests__/AppointmentDetailModal.test.tsx (el archivo creado en la Task 3, para seguir su convención)
  </read_first>
  <action>
La ampliación de alcance declarada en `<objective>` (seleccionar `esSobreturno` en el backend, lo que hace aparecer el borde punteado naranja de los sobreturnos) tiene que quedar verificada con el mismo rigor que el botón de WhatsApp — es un cambio de UI que no estaba en los gaps y no puede entrar sin prueba.

Agregar al archivo de tests un `describe` que monte el componente que decide ese borde (`CalendarGrid`, o el subcomponente de tarjeta que aplica la clase, lo que sea montable con menos superficie) con dos eventos: uno con `esSobreturno: true` y otro con `esSobreturno: false`. Assertar que el elemento del primero lleva la clase/estilo de borde punteado y el del segundo no.

Si `CalendarGrid` resulta impráctico de montar (demasiados providers, dependencia de layout), **no** forzarlo: en ese caso extraer la aserción al nivel más bajo que sí sea testeable, o —si tampoco es viable— **revertir la ampliación de alcance**, es decir sacar `esSobreturno: true` del `select` de la Task 1 y declarar el campo como opcional en `TurnoRango`, dejando el hallazgo registrado en el SUMMARY para una fase futura. Un cambio de UI sin verificación no entra: esa es precisamente la regla que esta fase falló.

Documentar en el SUMMARY cuál de los tres caminos se tomó y por qué.
  </action>
  <verify>
    <automated>cd frontend && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test` sale con código 0
    - Se dio UNO de estos tres resultados, y cuál fue está explícito en el SUMMARY: (a) hay un test que distingue `esSobreturno: true` de `false` y pasa; (b) se testeó a un nivel más bajo y ese test pasa; (c) se revirtió la ampliación — en cuyo caso `rg -n "async obtenerTurnosPorRango" -A 60 backend/src/modules/turnos/turnos.service.ts | rg -c "esSobreturno: true"` devuelve 0 y `rg -c "esSobreturno\?" frontend/src/hooks/useTurnosRangos.ts` devuelve 1
    - No quedó el estado intermedio prohibido: es decir, **no** puede pasar que `esSobreturno: true` esté en el `select` del backend y al mismo tiempo no exista ningún test que verifique el borde. Verificable como: si `rg -c "esSobreturno: true"` sobre el select devuelve 1, entonces `rg -c "esSobreturno" frontend/src/app/dashboard/turnos/__tests__/` debe devolver al menos 1
    - `cd frontend && npx tsc --noEmit` sale 0 y `cd backend && npm run build` sale 0
  </acceptance_criteria>
  <done>El borde de sobreturnos está verificado por test, o la ampliación de alcance fue revertida — pero no queda entrando sin prueba.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → `GET /turnos/rango` | Entrada no confiable (`profesionalId`, `desde`, `hasta` por query string) que devuelve datos de pacientes: `nombreCompleto`, `telefono`, `whatsappOptIn` y —tras este plan— `pacienteId`. |
| service → Prisma | El `select` es la única frontera que decide qué PII sale del modelo `Paciente` hacia el cliente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-16 | Information Disclosure / Broken Access Control | `obtenerPorRango` (`turnos.controller.ts:122-139`) toma `profesionalId` del query string y lo pasa directo al service **sin `resolveScope`**, a diferencia de `findAll` (`:37-49`) que sí lo llama | **mitigate — mitigado por el plan 69-10** | **Exposición real, verificada leyendo el código:** la ruta exige sesión válida vía `@Auth('ADMIN','PROFESIONAL','SECRETARIA')` (`turnos.controller.ts:22`) y excluye `PACIENTE`/`FACTURADOR`, pero no acota horizontalmente. Sin `resolveScope`, un `PROFESIONAL` autenticado puede pedir el rango de agenda de otro profesional y leer nombres + teléfonos de pacientes ajenos. Este plan **agrava** la exposición al sumar `pacienteId` al `select` que el plan 02 ya había ensanchado con `telefono`, así que dejarla abierta no es defendible. **El plan `69-10` la cierra** aplicando `resolveScope` a este handler, con un test que falla si alguien lo revierte. Una versión anterior de este plan la registraba como `accept` argumentando riesgo de regresión para `SECRETARIA`; ese argumento era **falso**: `resolve-scope.ts:27-31` devuelve el `requestedProfesionalId` tal cual para todo rol distinto de `PROFESIONAL`, así que `ADMIN` y `SECRETARIA` no cambian de comportamiento. El executor no tiene que hacer nada acá — sólo verificar que `69-10` esté aplicado antes de dar la fase por cerrada. |
| T-69-17 | Information Disclosure | El plan agrega `pacienteId` al payload del calendario | accept | `pacienteId` es un identificador opaco (cuid) del mismo paciente cuyo `nombreCompleto` y `telefono` ese payload ya devuelve: no ensancha la clase de datos expuesta ni permite enumerar pacientes fuera del rango pedido. Es estrictamente menos sensible que lo que ya viaja. Queda bajo el mismo límite de T-69-16. |
| T-69-18 | Denial of Service (funcional) | Angostar `TurnoRango.estado` a una unión de literales podría romper el build de tres consumidores no relacionados | mitigate | Los tres consumidores fueron leídos durante el planning y todas sus comparaciones usan literales que pertenecen a la unión; `getEstadoTurnoChip` acepta `string`. La red es `npx tsc --noEmit` como `<automated>` de la Task 2, que falla el plan antes de mergear si algún consumidor rompe. |
| T-69-19 | Tampering | El test de regresión del `select` podría escribirse de forma que pase siempre (assertion vacía), reproduciendo el fallo de esta fase: "código presente ≠ comportamiento correcto" | mitigate | La Task 1 exige una prueba negativa explícita: quitar `pacienteId: true` tiene que hacer fallar el test, y el resultado de esa comprobación se registra en el SUMMARY. |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | El plan no instala paquetes: usa `jest`/`ts-jest` ya presentes en `backend/package.json` y no toca dependencias del frontend. Sin superficie de supply chain, no aplica el gate de legitimidad. |
</threat_model>

<verification>
1. `cd backend && npx jest src/modules/turnos/turnos.service.spec.ts` sale 0, con los 2 tests nuevos incluidos y 0 fallos preexistentes
2. `cd frontend && npm test` sale 0, con los 3 tests de alcanzabilidad de la Task 3 incluidos
3. `cd frontend && npx tsc --noEmit` sale 0
4. `cd frontend && npm run lint` sin errores nuevos en `useTurnosRangos.ts` ni en `turnos/page.tsx`
5. La cadena completa es grep-eable de punta a punta: `pacienteId: true` en el `select` del service → `pacienteId: string` en `TurnoRango` → `t.pacienteId ?? t.paciente?.id` en el mapeo → `pacienteId?: string` en las 3 copias de `CalendarEvent` — pero el grep es el complemento, **no** la prueba: la prueba es el test de render de la Task 3
6. Ningún `as any` sobrevive en el mapeo de `turnos/page.tsx`
7. `AppointmentDetailModal.tsx` y `turnos.controller.ts` sin cambios
8. La ampliación de alcance del borde de sobreturnos quedó verificada por test o revertida (Task 4) — no entra sin prueba
9. T-69-16 (el IDOR de `obtenerPorRango`) está mitigado por el plan `69-10`: confirmar que ese plan está aplicado antes de dar la fase por cerrada
</verification>

<success_criteria>
- El atajo de WhatsApp del detalle de turno del calendario **renderiza** (habilitado o deshabilitado según corresponda), cerrando el gap CR-01 del `69-VERIFICATION.md`
- Un paciente con teléfono y opt-in ve el botón habilitado desde el calendario; uno sin teléfono lo ve deshabilitado con el tooltip de D-16
- El límite de la API del calendario está tipado: la próxima vez que un campo del mapeo falte en el `select`, lo caza el compilador y no una verificación manual
</success_criteria>

<output>
Create `.planning/phases/69-consistencia-de-tel-fono-opcional-frontend/69-07-SUMMARY.md` when done.

Incluir obligatoriamente en el SUMMARY:
- El resultado de las tres pruebas negativas (test de jest sin `pacienteId: true`; `tsc` con `pacienteId` renombrado en `TurnoRango`; test de render con `pacienteId` ausente en el fixture)
- Cuál de los tres caminos de la Task 4 se tomó para el borde de sobreturnos, y por qué
- La confirmación de que el plan `69-10` (mitigación de T-69-16) está aplicado
</output>
