---
phase: 13-wsaa-token-service
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - backend/src/modules/afip-config/afip-config.service.ts
  - backend/src/modules/afip-config/afip-config.module.ts
  - backend/src/app.module.ts
autonomous: true
requirements:
  - CAE-01

must_haves:
  truths:
    - "AfipConfigService.saveCert() uses WsaaService.getTicketTransient() instead of openssl subprocess"
    - "After cert save, WsaaService.getTicket() is called to warm the Redis cache — first invoice hits cache, not WSAA"
    - "No openssl subprocess, child_process import, or /tmp file writes remain in AfipConfigService"
    - "WsaaModule is registered in app.module.ts and importable by any module"
    - "Backend compiles and existing afip-config tests still pass"
  artifacts:
    - path: "backend/src/modules/afip-config/afip-config.service.ts"
      provides: "AfipConfigService using WsaaService instead of openssl subprocess"
      contains: "wsaaService.getTicketTransient"
    - path: "backend/src/modules/afip-config/afip-config.module.ts"
      provides: "Module importing WsaaModule"
      contains: "WsaaModule"
  key_links:
    - from: "backend/src/modules/afip-config/afip-config.service.ts"
      to: "backend/src/modules/wsaa/wsaa.service.ts"
      via: "@Inject(WSAA_SERVICE) wsaaService: WsaaServiceInterface"
      pattern: "WSAA_SERVICE"
    - from: "backend/src/modules/afip-config/afip-config.service.ts"
      to: "WsaaService.getTicket()"
      via: "warm cache call after cert save"
      pattern: "wsaaService\\.getTicket"
---

<objective>
Rewire AfipConfigService to use WsaaService (from Plan 01), eliminating the openssl subprocess entirely. After cert save, warm the Redis cache by calling getTicket() so the first invoice emission hits cache.

Purpose: Completes the openssl elimination (security fix) and ensures the cert-save flow validates via the same node-forge path used for production invoice emission. The warm cache ensures zero cold-start latency for the first invoice after cert configuration.
Output: AfipConfigService fully integrated with WsaaModule. No openssl subprocess anywhere in the codebase's AFIP path.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-wsaa-token-service/13-CONTEXT.md
@.planning/phases/13-wsaa-token-service/13-01-SUMMARY.md

@backend/src/modules/afip-config/afip-config.service.ts
@backend/src/modules/afip-config/afip-config.module.ts
@backend/src/app.module.ts

<interfaces>
<!-- From Plan 01 outputs -->

From backend/src/modules/wsaa/wsaa.interfaces.ts:
```typescript
export interface AccessTicket {
  token: string;
  sign: string;
  expiresAt: Date;
}

export interface WsaaServiceInterface {
  getTicket(profesionalId: string, service: string): Promise<AccessTicket>;
  getTicketTransient(certPem: string, keyPem: string, ambiente: AmbienteAFIP, service: string): Promise<AccessTicket>;
}
```

From backend/src/modules/wsaa/wsaa.constants.ts:
```typescript
export const WSAA_SERVICE = 'WSAA_SERVICE';
export const WSAA_REDIS_CLIENT = 'WSAA_REDIS_CLIENT';
```

From backend/src/modules/wsaa/wsaa.module.ts:
```typescript
export class WsaaModule {}
// Exports: WSAA_SERVICE token
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire AfipConfigService to use WsaaService and eliminate openssl</name>
  <files>
    backend/src/modules/afip-config/afip-config.service.ts,
    backend/src/modules/afip-config/afip-config.module.ts
  </files>
  <action>
1. Update `AfipConfigModule` (`afip-config.module.ts`):
   - Add `WsaaModule` to imports array
   - No need to add WSAA_SERVICE to providers — it comes from WsaaModule exports

2. Update `AfipConfigService` (`afip-config.service.ts`):
   - Add constructor parameter: `@Inject(WSAA_SERVICE) private readonly wsaaService: WsaaServiceInterface`
   - Import `Inject` from `@nestjs/common`, `WSAA_SERVICE` from `../wsaa/wsaa.constants`, `WsaaServiceInterface` from `../wsaa/wsaa.interfaces`

3. Replace `getWsaaTicketTransient()` calls in `saveCert()`:
   - BEFORE: `const { token, sign } = await this.getWsaaTicketTransient(dto.certPem, dto.keyPem, dto.ambiente);`
   - AFTER: `const { token, sign } = await this.wsaaService.getTicketTransient(dto.certPem, dto.keyPem, dto.ambiente, 'wsfe');`

4. Replace `getWsaaTicketTransient()` call in `saveBillingConfig()`:
   - BEFORE: `const { token, sign } = await this.getWsaaTicketTransient(certPem, keyPem, dto.ambiente);`
   - AFTER: `const { token, sign } = await this.wsaaService.getTicketTransient(certPem, keyPem, dto.ambiente, 'wsfe');`

5. Add warm cache call in `saveCert()` AFTER the upsert completes (before `return this.getStatus`):
   ```typescript
   // Warm Redis cache — first invoice emission will hit cache, not WSAA
   try {
     await this.wsaaService.getTicket(profesionalId, 'wsfe');
   } catch (err) {
     this.logger.warn('Cache warm failed after cert save — non-blocking', err);
   }
   ```
   This is wrapped in try/catch because it is non-critical — cert save already succeeded. If warm fails (e.g., Redis down), the first invoice call will just call WSAA directly.

6. DELETE the entire `private async getWsaaTicketTransient()` method (lines ~168-257) — it is fully replaced.

7. DELETE the `private buildTra()` method (lines ~259-264) — it now lives in WsaaService.

8. REMOVE the `import * as crypto from 'crypto'` if no longer used (check: `extractCertInfo` uses `crypto.X509Certificate` — keep it).

9. REMOVE all `eslint-disable` comments related to require('child_process'), require('fs'), require('os'), require('path') — they are no longer needed.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npx tsc --noEmit 2>&1 | head -20 && echo "---" && grep -rn "execSync\|child_process\|openssl\|tmpDir\|mkdtemp" src/modules/afip-config/ 2>&1 || echo "No openssl traces found - GOOD"</automated>
  </verify>
  <done>AfipConfigService uses WsaaService for all WSAA interactions. Zero references to openssl, child_process, or /tmp in afip-config module. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Register WsaaModule in app.module.ts and verify full backend compilation</name>
  <files>
    backend/src/app.module.ts
  </files>
  <action>
1. Read `backend/src/app.module.ts` to understand current module imports structure.

2. Add `WsaaModule` to the `imports` array in AppModule. Place it near AfipConfigModule since they are related.
   - Import: `import { WsaaModule } from './modules/wsaa/wsaa.module';`

3. Verify the full backend compiles:
   - `npx tsc --noEmit` — zero errors
   - `npx jest --testPathPattern=wsaa --no-coverage` — all wsaa tests still pass
   - `npx jest --testPathPattern=afip-config --no-coverage` — existing afip-config tests still pass (if any exist)

4. Verify no circular dependency:
   - WsaaModule imports AfipConfigModule (for AfipConfigService to load cert+key)
   - AfipConfigModule imports WsaaModule (for WsaaService)
   - This IS a circular dependency. To resolve: WsaaService should NOT import AfipConfigModule. Instead, WsaaService should inject PrismaService + EncryptionService directly (both are global or imported via WhatsappModule).
   - If circular dependency is detected, FIX IT: remove AfipConfigModule from WsaaModule imports. WsaaService uses PrismaService (global) and EncryptionService (from WhatsappModule). Add WhatsappModule to WsaaModule imports instead.
   - Update the WsaaModule imports accordingly and re-verify compilation.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npx tsc --noEmit 2>&1 | head -20 && echo "---" && npx jest --testPathPattern=wsaa --no-coverage --verbose 2>&1 | tail -20</automated>
  </verify>
  <done>WsaaModule registered in AppModule. No circular dependencies. Full backend compiles. All wsaa and afip-config tests pass. No openssl subprocess traces remain in the afip-config module.</done>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` — zero errors
2. `cd backend && npx jest --no-coverage` — full test suite passes
3. `grep -rn "execSync\|child_process\|openssl\|mkdtemp" backend/src/modules/afip-config/` — zero matches
4. `grep -rn "WSAA_SERVICE" backend/src/modules/afip-config/afip-config.service.ts` — present (injection)
5. `grep -rn "wsaaService.getTicket" backend/src/modules/afip-config/afip-config.service.ts` — present (warm cache)
6. `grep -rn "WsaaModule" backend/src/app.module.ts` — present (registration)
</verification>

<success_criteria>
- AfipConfigService delegates all WSAA calls to WsaaService (no openssl, no child_process, no /tmp)
- Cert save flow: getTicketTransient() for validation, then getTicket() for warm cache
- WsaaModule registered in AppModule without circular dependencies
- Full backend test suite passes
- Ready for Phase 14 to import WsaaModule in FinanzasModule and call getTicket()
</success_criteria>

<output>
After completion, create `.planning/phases/13-wsaa-token-service/13-02-SUMMARY.md`
</output>
