---
phase: 22-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/schema.prisma
  - backend/src/prisma/migrations/20260416000000_flujo_paciente/migration.sql
autonomous: true
requirements: [TIPOS-01, FLUJO-06]

must_haves:
  truths:
    - "schema.prisma contiene el enum FlujoPaciente con valores CIRUGIA, TRATAMIENTO, PENDIENTE"
    - "El modelo Paciente tiene el campo flujo: FlujoPaciente? @default(PENDIENTE)"
    - "El modelo TipoTurno tiene el campo flujoPaciente: FlujoPaciente? (nullable, sin default)"
    - "Existe un archivo migration.sql con DDL y lógica transaccional de migración de datos lista para ejecutar"
  artifacts:
    - path: "backend/src/prisma/schema.prisma"
      provides: "Enum FlujoPaciente + campo Paciente.flujo + campo TipoTurno.flujoPaciente"
      contains: "enum FlujoPaciente"
    - path: "backend/src/prisma/migrations/20260416000000_flujo_paciente/migration.sql"
      provides: "SQL completo: DDL + migración transaccional de TipoTurno + backfill Paciente.flujo"
      contains: "FlujoPaciente"
  key_links:
    - from: "schema.prisma enum FlujoPaciente"
      to: "migration.sql CREATE TYPE FlujoPaciente"
      via: "Prisma migrate dev lee schema y ejecuta migration.sql"
      pattern: "FlujoPaciente"
    - from: "schema.prisma Paciente.flujo"
      to: "migration.sql ALTER TABLE Paciente ADD COLUMN flujo"
      via: "DDL en migration.sql"
      pattern: "ADD COLUMN.*flujo"
---

<objective>
Añadir el enum FlujoPaciente y los dos campos nuevos a schema.prisma, y escribir el archivo SQL de migración completo con DDL + lógica transaccional de reemplazo de TipoTurno + backfill de Paciente.flujo.

Purpose: Establece la fundación de schema que todas las fases siguientes consumen. Sin este enum y estos campos, ninguna lógica de negocio de flujo de pacientes puede construirse.
Output: schema.prisma actualizado + migration.sql listo para `prisma migrate dev`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-schema-foundation/22-CONTEXT.md
@.planning/phases/22-schema-foundation/22-RESEARCH.md

<interfaces>
<!-- Estado actual del schema que se modifica. Extraído de backend/src/prisma/schema.prisma -->

model Paciente {
  // ... (campos existentes hasta línea 203)
  etapaCRM        EtapaCRM?
  temperatura     TemperaturaPaciente?
  scoreConversion Int               @default(0)
  motivoPerdida   MotivoPerdidaCRM?
  // ... (relaciones)
  @@index([etapaCRM])
  @@index([profesionalId, etapaCRM])
}

model TipoTurno {
  id              String                  @id @default(uuid())
  nombre          String                  @unique
  descripcion     String?
  mensajeBase     String?
  instrucciones   String?
  duracionDefault Int?
  esCirugia       Boolean                 @default(false)
  turnos          Turno[]
  configProfesionales TipoTurnoProfesional[]
}

model TipoTurnoProfesional {
  id              String      @id @default(uuid())
  profesionalId   String
  tipoTurnoId     String
  // ...
  @@unique([profesionalId, tipoTurnoId])
}

-- Enums existentes como referencia de formato:
enum EtapaCRM {
  PRIMER_CONTACTO
  CONSULTA_AGENDADA
  // ...
}

enum TemperaturaPaciente {
  FRIO
  TIBIO
  CALIENTE
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir enum FlujoPaciente y campos a schema.prisma</name>
  <files>backend/src/prisma/schema.prisma</files>
  <action>
Realizar dos adiciones en backend/src/prisma/schema.prisma:

**A) Enum FlujoPaciente** — Añadir después del bloque de enums CRM existentes (buscar la sección donde están EtapaCRM, TemperaturaPaciente, MotivoPerdidaCRM) y agregar:

```prisma
enum FlujoPaciente {
  CIRUGIA
  TRATAMIENTO
  PENDIENTE
}
```

**B) Campo Paciente.flujo** — En el modelo Paciente, agregar después del campo `motivoPerdida`:

```prisma
flujo  FlujoPaciente?  @default(PENDIENTE)
```

También agregar índice al final del modelo Paciente (junto a los @@index existentes):

```prisma
@@index([flujo])
@@index([profesionalId, flujo])
```

**C) Campo TipoTurno.flujoPaciente** — En el modelo TipoTurno, agregar después del campo `esCirugia`:

```prisma
flujoPaciente  FlujoPaciente?
```

Notas críticas:
- `Paciente.flujo` es nullable (`?`) con `@default(PENDIENTE)` — el `@default` aplica a nivel de aplicación (new inserts), no SQL-level default en la migración
- `TipoTurno.flujoPaciente` es nullable sin ningún default
- El enum `FlujoPaciente` debe declararse ANTES de los modelos que lo usan (Prisma parsea top-to-bottom); colocarlo junto a los demás enums, NO al final del archivo
- NO añadir `@default` al campo de Paciente como DEFAULT SQL en la migración — la migración lo maneja explícitamente (ver Task 2)
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && grep -c "FlujoPaciente" src/prisma/schema.prisma</automated>
  </verify>
  <done>
- `grep "enum FlujoPaciente" schema.prisma` devuelve match
- `grep "flujo.*FlujoPaciente" schema.prisma` devuelve match en modelo Paciente
- `grep "flujoPaciente.*FlujoPaciente" schema.prisma` devuelve match en modelo TipoTurno
- Al menos 3 ocurrencias de "FlujoPaciente" en el archivo
  </done>
</task>

<task type="auto">
  <name>Task 2: Escribir migration.sql con DDL + migración transaccional</name>
  <files>backend/src/prisma/migrations/20260416000000_flujo_paciente/migration.sql</files>
  <action>
Crear el directorio `backend/src/prisma/migrations/20260416000000_flujo_paciente/` y dentro el archivo `migration.sql` con el siguiente contenido exacto:

```sql
-- Migration: flujo_paciente
-- Phase 22: Schema Foundation

-- ============================================================
-- PART A: DDL — Create enum and add columns
-- ============================================================

CREATE TYPE "FlujoPaciente" AS ENUM ('CIRUGIA', 'TRATAMIENTO', 'PENDIENTE');

-- Add flujo to Paciente WITHOUT SQL-level default so existing rows = NULL
-- @default(PENDIENTE) in schema.prisma handles application-level new inserts
ALTER TABLE "Paciente" ADD COLUMN "flujo" "FlujoPaciente";

-- Add flujoPaciente to TipoTurno (nullable, no default)
ALTER TABLE "TipoTurno" ADD COLUMN "flujoPaciente" "FlujoPaciente";

-- ============================================================
-- PART B: Data migration (transactional)
-- ============================================================

BEGIN;

-- Step 1: UPDATE existing 'Control' row to add flujoPaciente = NULL and ensure esCirugia = false
-- (Control keeps its nombre — unique constraint means we can't INSERT a new one)
UPDATE "TipoTurno"
SET "flujoPaciente" = NULL, "esCirugia" = false
WHERE "nombre" = 'Control';

-- Step 2: INSERT the 4 genuinely new TipoTurno records
-- (Control is reused via UPDATE above, so only 4 are inserted)
INSERT INTO "TipoTurno" ("id", "nombre", "esCirugia", "flujoPaciente")
VALUES
  (gen_random_uuid(), 'Consulta para cirugía',                    false, 'CIRUGIA'::"FlujoPaciente"),
  (gen_random_uuid(), 'Consulta para tratamiento en consultorio', false, 'TRATAMIENTO'::"FlujoPaciente"),
  (gen_random_uuid(), 'Pre-operatorio',                           false, 'CIRUGIA'::"FlujoPaciente"),
  (gen_random_uuid(), 'Consulta pendiente',                       false, NULL);

-- Step 3: Reasign Turno.tipoTurnoId from old types to new equivalents
-- 'Consulta Inicial' → 'Consulta pendiente'
UPDATE "Turno"
SET "tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE "nombre" = 'Consulta pendiente' LIMIT 1)
WHERE "tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE "nombre" = 'Consulta Inicial' LIMIT 1);

-- 'Post-Operatorio' → 'Pre-operatorio'
UPDATE "Turno"
SET "tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE "nombre" = 'Pre-operatorio' LIMIT 1)
WHERE "tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE "nombre" = 'Post-Operatorio' LIMIT 1);

-- 'Procedimiento' → 'Consulta para tratamiento en consultorio'
UPDATE "Turno"
SET "tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE "nombre" = 'Consulta para tratamiento en consultorio' LIMIT 1)
WHERE "tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE "nombre" = 'Procedimiento' LIMIT 1);

-- 'Control' → 'Control' (same nombre, no UPDATE needed)

-- Step 4: Delete TipoTurnoProfesional configs for old types (must run BEFORE deleting TipoTurno)
DELETE FROM "TipoTurnoProfesional"
WHERE "tipoTurnoId" IN (
  SELECT id FROM "TipoTurno"
  WHERE "nombre" IN ('Consulta Inicial', 'Post-Operatorio', 'Procedimiento')
);

-- Step 5: Delete old TipoTurno records (Control is kept/reused, not deleted)
DELETE FROM "TipoTurno"
WHERE "nombre" IN ('Consulta Inicial', 'Post-Operatorio', 'Procedimiento');

-- Step 6: Backfill Paciente.flujo
-- Patients with surgery history (esCirugia turno OR active etapaCRM) → CIRUGIA
UPDATE "Paciente" p
SET "flujo" = 'CIRUGIA'::"FlujoPaciente"
WHERE
  EXISTS (
    SELECT 1 FROM "Turno" t
    WHERE t."pacienteId" = p.id AND t."esCirugia" = true
  )
  OR p."etapaCRM" IS NOT NULL;

-- All other existing patients stay NULL (legacy/unclassified)
-- New patients created via application get PENDIENTE from Prisma @default

COMMIT;
```

Notas críticas del orden de operaciones:
- El UPDATE de 'Control' va PRIMERO (antes del INSERT) para evitar cualquier confusión
- El INSERT solo incluye 4 tipos (Control se reutiliza)
- El DELETE de TipoTurnoProfesional va ANTES del DELETE de TipoTurno (FK constraint ON DELETE RESTRICT)
- El backfill no tiene un segundo UPDATE para null — los rows no-CIRUGIA ya están NULL porque la columna se agregó sin DEFAULT SQL
  </action>
  <verify>
    <automated>ls /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend/src/prisma/migrations/20260416000000_flujo_paciente/migration.sql && grep -c "FlujoPaciente" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend/src/prisma/migrations/20260416000000_flujo_paciente/migration.sql</automated>
  </verify>
  <done>
- El archivo `migration.sql` existe en el directorio correcto
- Contiene `CREATE TYPE "FlujoPaciente"`
- Contiene `BEGIN;` y `COMMIT;` marcando el bloque transaccional
- Los 4 pasos de migración de TipoTurno están en el orden correcto: UPDATE Control → INSERT 4 tipos → UPDATE Turno FKs → DELETE TipoTurnoProfesional → DELETE TipoTurno viejos
- El backfill de Paciente.flujo solo actualiza a CIRUGIA; no hay segundo UPDATE a NULL (innecesario)
  </done>
</task>

</tasks>

<verification>
```bash
# Verificar que el schema tiene los 3 cambios
grep "enum FlujoPaciente" backend/src/prisma/schema.prisma
grep "flujo.*FlujoPaciente" backend/src/prisma/schema.prisma
grep "flujoPaciente.*FlujoPaciente" backend/src/prisma/schema.prisma

# Verificar que el migration.sql existe y tiene estructura correcta
cat backend/src/prisma/migrations/20260416000000_flujo_paciente/migration.sql
```
</verification>

<success_criteria>
- schema.prisma contiene `enum FlujoPaciente { CIRUGIA TRATAMIENTO PENDIENTE }`
- schema.prisma modelo Paciente contiene `flujo  FlujoPaciente?  @default(PENDIENTE)`
- schema.prisma modelo TipoTurno contiene `flujoPaciente  FlujoPaciente?`
- migration.sql existe con DDL completo + lógica transaccional de 4 pasos para TipoTurno + backfill CIRUGIA
- Ninguno de los 5 tipos en el migration.sql tiene `esCirugia = true`
- El archivo NO requiere tocar para ejecutarse — está listo para `prisma migrate dev`
</success_criteria>

<output>
After completion, create `.planning/phases/22-schema-foundation/22-01-SUMMARY.md`
</output>
