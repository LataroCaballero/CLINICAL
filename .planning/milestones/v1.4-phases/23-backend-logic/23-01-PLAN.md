---
phase: 23-backend-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/turnos/turnos.service.ts
autonomous: true
requirements:
  - FLUJO-01
  - FLUJO-02
  - FLUJO-03
  - FLUJO-04

must_haves:
  truths:
    - "Al crear un turno cuyo TipoTurno.flujoPaciente = CIRUGIA para un paciente PENDIENTE, Paciente.flujo cambia a CIRUGIA"
    - "Al crear un turno cuyo TipoTurno.flujoPaciente = TRATAMIENTO para un paciente PENDIENTE, Paciente.flujo cambia a TRATAMIENTO"
    - "Crear el mismo turno para un paciente con flujo = CIRUGIA o TRATAMIENTO no modifica su flujo (monotónico)"
    - "TipoTurno con flujoPaciente = null (Control, Consulta pendiente) no modifica el flujo del paciente en ningún caso"
    - "Si el flujo update falla, el turno se crea igualmente y el error se swallow con logger.warn"
  artifacts:
    - path: "backend/src/modules/turnos/turnos.service.ts"
      provides: "crearTurno() con step 5.5 flujo auto-update best-effort"
      contains: "FlujoPaciente.PENDIENTE"
  key_links:
    - from: "TurnosService.crearTurno()"
      to: "prisma.paciente.update flujo"
      via: "step 5.5 .catch() chain after CRM auto-transition"
      pattern: "flujoPaciente.*PENDIENTE"
---

<objective>
Implementar auto-update de Paciente.flujo al crear un turno clasificatorio.

Purpose: Cuando una secretaria agenda un "Consulta para cirugía" o "Consulta para tratamiento en consultorio", el sistema clasifica automáticamente al paciente sin requerir acción manual. Los tipos sin flujoPaciente (Control, Consulta pendiente) no afectan el flujo — el guard `if (tipoTurno.flujoPaciente && paciente.flujo === 'PENDIENTE')` los cortocircuita.

Output: turnos.service.ts con step 5.5 insertado en crearTurno(), Logger clase property añadido, FlujoPaciente importado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-backend-logic/23-CONTEXT.md
@.planning/phases/23-backend-logic/23-RESEARCH.md
</context>

<interfaces>
<!-- Estado actual de turnos.service.ts relevante para este plan. Extraído del codebase. -->
<!-- El executor NO necesita explorar el repo — toda la información está aquí. -->

From backend/src/modules/turnos/turnos.service.ts (imports, líneas 1–18):
```typescript
import {
  BadRequestException,
  Injectable,
  NotFoundException,
} from '@nestjs/common';
// Logger NO está importado — necesita agregarse
import {
  EstadoTurno,
  RolUsuario,
  TipoMovimiento,
  EstadoCirugia,
  EtapaCRM,
  TipoContacto,
} from '@prisma/client';
// FlujoPaciente NO está importado — necesita agregarse
```

From crearTurno() — tipoTurno findUnique select (línea 47-50):
```typescript
this.prisma.tipoTurno.findUnique({
  where: { id: dto.tipoTurnoId },
  select: { id: true, duracionDefault: true },  // agregar: flujoPaciente: true
})
```

From crearTurno() — step 5 CRM auto-transition (líneas 125–136, plantilla exacta):
```typescript
// 5) CRM auto-transition
const pacienteCRM = await this.prisma.paciente.findUnique({
  where: { id: dto.pacienteId },
  select: { etapaCRM: true, profesionalId: true },  // agregar: flujo: true
});
const etapasIniciales: (EtapaCRM | null)[] = [null, EtapaCRM.NUEVO_LEAD];
if (etapasIniciales.includes(pacienteCRM?.etapaCRM ?? null)) {
  await this.prisma.paciente.update({
    where: { id: dto.pacienteId },
    data: { etapaCRM: EtapaCRM.TURNO_AGENDADO },
  });
}
// Auto-contacto viene después (línea 138)
```

TurnosService class — constructor (línea 24-29):
```typescript
@Injectable()
export class TurnosService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly cuentasCorrientesService: CuentasCorrientesService,
  ) {}
  // private readonly logger NO existe — agregar como class property
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expandir selects y agregar Logger + FlujoPaciente import en turnos.service.ts</name>
  <files>backend/src/modules/turnos/turnos.service.ts</files>
  <action>
Realizar 4 ediciones quirúrgicas en turnos.service.ts:

1. **Imports — @nestjs/common**: agregar `Logger` al import existente:
   ```typescript
   import {
     BadRequestException,
     Injectable,
     Logger,
     NotFoundException,
   } from '@nestjs/common';
   ```

2. **Imports — @prisma/client**: agregar `FlujoPaciente` al import existente:
   ```typescript
   import {
     EstadoTurno,
     RolUsuario,
     TipoMovimiento,
     EstadoCirugia,
     EtapaCRM,
     TipoContacto,
     FlujoPaciente,
   } from '@prisma/client';
   ```

3. **Class property Logger** — agregar después del `constructor` (dentro de la clase, antes del primer método `crearTurno`):
   ```typescript
   private readonly logger = new Logger(TurnosService.name);
   ```

4. **tipoTurno findUnique select** (línea ~49) — agregar `flujoPaciente: true`:
   ```typescript
   select: { id: true, duracionDefault: true, flujoPaciente: true },
   ```

5. **pacienteCRM findUnique select** (línea ~128) — agregar `flujo: true`:
   ```typescript
   select: { etapaCRM: true, profesionalId: true, flujo: true },
   ```

NO cambiar ninguna lógica existente en este task — solo imports, class property, y dos líneas de select. Compilar (`cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -20`) para verificar que no hay errores antes de continuar.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build exits sin errores TypeScript. tipoTurno select incluye flujoPaciente, paciente select incluye flujo, Logger importado y definido como class property.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insertar step 5.5 — flujo auto-update best-effort en crearTurno()</name>
  <files>backend/src/modules/turnos/turnos.service.ts</files>
  <action>
Insertar el bloque step 5.5 en crearTurno() DESPUÉS del bloque CRM auto-transition (step 5, que termina en el `}` que cierra el `if (etapasIniciales.includes(...))`) y ANTES del bloque de auto-contacto (la línea `const profIdParaContacto = ...`).

Bloque exacto a insertar:
```typescript
    // 5.5) Flujo auto-update (best-effort — no bloquea creación del turno)
    if (tipoTurno.flujoPaciente && pacienteCRM?.flujo === FlujoPaciente.PENDIENTE) {
      this.prisma.paciente
        .update({
          where: { id: dto.pacienteId },
          data: { flujo: tipoTurno.flujoPaciente },
        })
        .catch((err: unknown) =>
          this.logger.warn(
            `flujo auto-update failed — turnoId=${turno.id} pacienteId=${dto.pacienteId}: ${String(err)}`,
          ),
        );
    }
```

Orden final dentro de crearTurno():
1. Crear turno (step 4)
2. CRM auto-transition (step 5) — `await`
3. **Step 5.5 flujo auto-update** — fire-and-forget `.catch()`, NO `await`
4. Auto-contacto log — `await`
5. `return turno;`

Usar `.catch()` en cadena, NO try/catch con await. El `await` está ausente deliberadamente para que no bloquee la respuesta.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build exitoso. turnos.service.ts contiene el bloque step 5.5 con guard `tipoTurno.flujoPaciente && pacienteCRM?.flujo === FlujoPaciente.PENDIENTE`, update fire-and-forget con .catch(), y logger.warn con turnoId+pacienteId. El update NO usa await.</done>
</task>

</tasks>

<verification>
1. `cd backend && npm run build` — 0 errores TypeScript
2. `grep -n "flujoPaciente\|FlujoPaciente\|step 5.5\|flujo auto-update" backend/src/modules/turnos/turnos.service.ts` — debe mostrar todas las líneas relevantes
3. `grep -n "await.*5\.5\|await.*flujo.*update" backend/src/modules/turnos/turnos.service.ts` — NO debe retornar nada (confirma que el update es fire-and-forget)
</verification>

<success_criteria>
- turnos.service.ts compila sin errores
- crearTurno() tiene step 5.5 después del CRM auto-transition y antes del contacto log
- El update de flujo es fire-and-forget (.catch, no await)
- Guard correcto: solo actualiza si `tipoTurno.flujoPaciente` existe Y `paciente.flujo === PENDIENTE`
- Logger importado y disponible como class property
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-logic/23-01-SUMMARY.md`
</output>
