---
phase: 24-liveturno-banner
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/store/live-turno.store.ts
  - frontend/src/hooks/useLiveTurnoActions.ts
autonomous: true
requirements:
  - LIVT-01
  - LIVT-02
  - LIVT-03

must_haves:
  truths:
    - "When a PENDIENTE patient's session starts, pacienteFlujo is captured in the store"
    - "bannerDismissed resets to false on every startSession() call"
    - "bannerDismissed is NOT included in the Zustand partialize config (does not survive page reload)"
    - "pacienteFlujo is part of LiveTurnoSession (IS persisted, survives recovery dialog)"
    - "The store exposes a dismissBanner() action that sets bannerDismissed: true"
    - "TypeScript has no errors — flujo is correctly typed as 'PENDIENTE' | 'CIRUGIA' | 'TRATAMIENTO' | null"
  artifacts:
    - path: "frontend/src/store/live-turno.store.ts"
      provides: "Extended LiveTurnoSession + LiveTurnoState with banner fields and dismissBanner action"
      contains: "pacienteFlujo"
    - path: "frontend/src/hooks/useLiveTurnoActions.ts"
      provides: "flujo mapped from IniciarSesionResponse into sessionData"
      contains: "pacienteFlujo: data.paciente.flujo"
  key_links:
    - from: "frontend/src/hooks/useLiveTurnoActions.ts"
      to: "frontend/src/store/live-turno.store.ts"
      via: "startSession(sessionData) with pacienteFlujo field"
      pattern: "pacienteFlujo.*data\\.paciente\\.flujo"
---

<objective>
Extend the LiveTurno data layer to carry patient flujo classification state through the session lifecycle.

Purpose: The banner component (Plan 02) needs to know (a) whether the current patient is PENDIENTE and (b) whether the banner has been dismissed in this session. Both must be in the store before the banner can be built.

Output: Extended store interface with pacienteFlujo and bannerDismissed fields; IniciarSesionResponse typed with flujo; sessionData mapping updated to populate pacienteFlujo on session start.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Current live-turno.store.ts interfaces — executor modifies these in place -->

export interface LiveTurnoSession {
  turnoId: string;
  pacienteId: string;
  pacienteNombre: string;
  pacienteTelefono?: string;
  pacienteEmail?: string;
  pacienteObraSocial?: string;
  pacientePlan?: string;
  profesionalId: string;
  tipoTurno: string;
  tipoTurnoId: string;
  startedAt: string;
  scheduledStart: string;
  // ADD: pacienteFlujo: 'PENDIENTE' | 'CIRUGIA' | 'TRATAMIENTO' | null;
}

interface LiveTurnoState {
  session: LiveTurnoSession | null;
  draftData: LiveTurnoDraftData;
  isMinimized: boolean;
  isPanelOpen: boolean;
  activeTab: LiveTurnoTab;
  showRecoveryDialog: boolean;
  _hasHydrated: boolean;
  // ADD: bannerDismissed: boolean;
  // ADD: dismissBanner: () => void;
  startSession: (session: LiveTurnoSession) => void;
  endSession: () => void;
  // ... (rest of existing actions unchanged)
}

// partialize config (CRITICAL — bannerDismissed must NOT appear here):
partialize: (state) => ({
  session: state.session,
  draftData: state.draftData,
  isMinimized: state.isMinimized,
  activeTab: state.activeTab,
}),

<!-- Current useLiveTurnoActions.ts interfaces — executor modifies these in place -->

interface IniciarSesionResponse {
  id: string;
  inicioReal: string;
  inicio: string;
  paciente: {
    id: string;
    nombreCompleto: string;
    telefono?: string;
    email?: string;
    plan?: string;
    obraSocial?: { id: string; nombre: string };
    // ADD: flujo: 'PENDIENTE' | 'CIRUGIA' | 'TRATAMIENTO' | null;
  };
  profesional: { id: string; usuario: { nombre: string; apellido: string } };
  tipoTurno: { id: string; nombre: string };
}

// sessionData mapping in onSuccess — ADD:
// pacienteFlujo: data.paciente.flujo ?? null,
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend LiveTurnoStore with banner fields</name>
  <files>frontend/src/store/live-turno.store.ts</files>
  <action>
Make the following targeted changes to live-turno.store.ts:

1. In `LiveTurnoSession` interface — add after `scheduledStart`:
   ```
   pacienteFlujo: 'PENDIENTE' | 'CIRUGIA' | 'TRATAMIENTO' | null;
   ```

2. In `LiveTurnoState` interface — add after `_hasHydrated: boolean`:
   ```
   bannerDismissed: boolean;
   dismissBanner: () => void;
   ```

3. In `initialState` object — add:
   ```
   bannerDismissed: false,
   ```

4. In `startSession` action inside `set({...})` — add:
   ```
   bannerDismissed: false,
   ```
   This is the critical reset: every new session starts with the banner un-dismissed.

5. Add new `dismissBanner` action alongside the other actions (after `acknowledgeRecovery`):
   ```
   dismissBanner: () => set({ bannerDismissed: true }),
   ```

6. In `partialize` config — do NOT add `bannerDismissed`. The current partialize block covers `session, draftData, isMinimized, activeTab`. Leave it exactly as is.

CRITICAL: `pacienteFlujo` IS part of `LiveTurnoSession` which is already in the partialize list via `session: state.session`. It WILL be persisted. That is intentional — it needs to survive recovery dialog scenarios. `bannerDismissed` is NOT in partialize and must NOT be added — dismiss is per-session only.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "live-turno\.store|error TS" | head -20</automated>
  </verify>
  <done>
    - LiveTurnoSession has pacienteFlujo typed as 'PENDIENTE' | 'CIRUGIA' | 'TRATAMIENTO' | null
    - LiveTurnoState has bannerDismissed: boolean and dismissBanner: () => void
    - initialState has bannerDismissed: false
    - startSession() resets bannerDismissed to false
    - dismissBanner() action sets bannerDismissed: true
    - partialize config unchanged (bannerDismissed excluded)
    - No TypeScript errors in store file
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire flujo into IniciarSesionResponse and sessionData</name>
  <files>frontend/src/hooks/useLiveTurnoActions.ts</files>
  <action>
Make the following targeted changes to useLiveTurnoActions.ts:

1. In `IniciarSesionResponse.paciente` object — add after `obraSocial?`:
   ```
   flujo: 'PENDIENTE' | 'CIRUGIA' | 'TRATAMIENTO' | null;
   ```

2. In the `onSuccess` callback, in the `sessionData` object mapping — add after `scheduledStart: data.inicio`:
   ```
   pacienteFlujo: data.paciente.flujo ?? null,
   ```

The backend `iniciarSesion` method already returns `flujo` in the response body (confirmed: Prisma include returns all paciente columns). This change simply makes TypeScript aware of the field and maps it into the store.

No other changes needed in this file. `cerrarSesion`, `cerrarSesionPorId`, and `useSesionActiva` are untouched.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "useLiveTurnoActions|error TS" | head -20</automated>
  </verify>
  <done>
    - IniciarSesionResponse.paciente has flujo typed as 'PENDIENTE' | 'CIRUGIA' | 'TRATAMIENTO' | null
    - sessionData mapping includes pacienteFlujo: data.paciente.flujo ?? null
    - No TypeScript errors in this file or downstream (LiveTurnoSession now requires pacienteFlujo)
    - Full tsc --noEmit passes without errors related to these two files
  </done>
</task>

</tasks>

<verification>
Run full TypeScript check across the frontend to confirm no type errors were introduced:

```bash
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npx tsc --noEmit 2>&1 | grep "error TS" | head -30
```

Expected: zero errors. If errors appear, they are most likely in Plan 02's files (LiveTurnoFlujoBanner.tsx) which don't exist yet — those are acceptable at this stage.
</verification>

<success_criteria>
- LiveTurnoSession interface includes pacienteFlujo: 'PENDIENTE' | 'CIRUGIA' | 'TRATAMIENTO' | null
- LiveTurnoState interface includes bannerDismissed: boolean + dismissBanner action
- startSession() resets bannerDismissed to false on every call
- bannerDismissed is excluded from partialize (session-only lifetime)
- useLiveTurnoActions.ts IniciarSesionResponse.paciente exposes flujo
- sessionData maps pacienteFlujo from response into store on session start
- TypeScript compiles without errors in these two files
</success_criteria>

<output>
After completion, create `.planning/phases/24-liveturno-banner/24-01-SUMMARY.md`
</output>
