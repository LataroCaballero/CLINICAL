---
phase: 32-schema-backend-estados-extendidos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/schema.prisma
  - backend/prisma/migrations/
autonomous: true
requirements:
  - EST-01
must_haves:
  truths:
    - "La base de datos acepta turnos con estado EN_ESPERA sin errores de constraint"
    - "La base de datos acepta turnos con estado SIENDO_ATENDIDO sin errores de constraint"
    - "El cliente Prisma generado exporta EstadoTurno.EN_ESPERA y EstadoTurno.SIENDO_ATENDIDO"
  artifacts:
    - path: "backend/src/prisma/schema.prisma"
      provides: "EstadoTurno enum con EN_ESPERA y SIENDO_ATENDIDO"
      contains: "EN_ESPERA"
    - path: "backend/prisma/migrations/"
      provides: "Migration SQL con ALTER TYPE ... ADD VALUE"
  key_links:
    - from: "backend/src/prisma/schema.prisma"
      to: "PostgreSQL enum EstadoTurno"
      via: "npx prisma migrate dev"
      pattern: "EN_ESPERA|SIENDO_ATENDIDO"
---

<objective>
Agregar EN_ESPERA y SIENDO_ATENDIDO al enum EstadoTurno en el schema de Prisma y aplicar la migración a la base de datos.

Purpose: Todos los endpoints de estado nuevos (EST-02..05) dependen de que estos valores existan en el enum de Postgres y en el cliente Prisma generado. Sin esta migración, cualquier intento de escribir esos estados genera un error de constraint.

Output: Schema actualizado, migration SQL generada, cliente Prisma regenerado, build del backend compila sin errores.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-schema-backend-estados-extendidos/32-CONTEXT.md

<interfaces>
<!-- Estado actual del enum en schema.prisma -->
<!-- Extraído de backend/src/prisma/schema.prisma línea 1094 -->

```prisma
enum EstadoTurno {
  PENDIENTE
  CONFIRMADO
  CANCELADO
  AUSENTE
  FINALIZADO
}
```

<!-- Notas importantes:
- AUSENTE ya existe — NO agregar de nuevo
- Solo agregar EN_ESPERA y SIENDO_ATENDIDO
- El modelo Turno usa: estado EstadoTurno @default(PENDIENTE) (línea 804)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar EN_ESPERA y SIENDO_ATENDIDO al enum EstadoTurno</name>
  <files>backend/src/prisma/schema.prisma</files>
  <action>
    En backend/src/prisma/schema.prisma, localizar el enum EstadoTurno (línea ~1094) y agregar EN_ESPERA y SIENDO_ATENDIDO. El enum resultante debe quedar:

    ```prisma
    enum EstadoTurno {
      PENDIENTE
      CONFIRMADO
      CANCELADO
      AUSENTE
      FINALIZADO
      EN_ESPERA
      SIENDO_ATENDIDO
    }
    ```

    Colocar EN_ESPERA y SIENDO_ATENDIDO al final para que el ALTER TYPE generado por Prisma use ADD VALUE (PostgreSQL solo permite agregar valores al final de un enum existente sin recrearlo). No mover ni reordenar los valores existentes.

    NO modificar ningún otro campo del schema — solo el enum.
  </action>
  <verify>
    <automated>grep -A9 "enum EstadoTurno" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend/src/prisma/schema.prisma</automated>
  </verify>
  <done>El enum tiene exactamente 7 valores: PENDIENTE, CONFIRMADO, CANCELADO, AUSENTE, FINALIZADO, EN_ESPERA, SIENDO_ATENDIDO</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar migración y regenerar cliente Prisma</name>
  <files>backend/prisma/migrations/</files>
  <action>
    Desde el directorio backend/, ejecutar los siguientes comandos en orden:

    1. `npx prisma migrate dev --name add-estados-extendidos-turno`
       - Esto genera el SQL de la migración (ALTER TYPE "EstadoTurno" ADD VALUE 'EN_ESPERA'; ALTER TYPE "EstadoTurno" ADD VALUE 'SIENDO_ATENDIDO';) y lo aplica a la base de datos.
       - Si la DB no está disponible localmente, el ejecutor debe usar `npx prisma migrate dev` sin --name o ajustar según el entorno.

    2. `npx prisma generate`
       - Regenera el cliente Prisma para que EstadoTurno.EN_ESPERA y EstadoTurno.SIENDO_ATENDIDO estén disponibles en TypeScript.

    3. Verificar que el build del backend compila sin errores:
       `npm run build`

    Si `npm run build` falla únicamente por referencias a EstadoTurno.EN_ESPERA o EstadoTurno.SIENDO_ATENDIDO en archivos que aún no existen (porque Plan 02 los crea), ignorar esos errores — el build del servicio completo se valida después de Plan 02.

    Si falla por otros motivos no relacionados con los nuevos estados, investigar y corregir antes de marcar la tarea como done.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npx prisma generate && node -e "const { EstadoTurno } = require('./node_modules/.prisma/client'); console.log(EstadoTurno.EN_ESPERA, EstadoTurno.SIENDO_ATENDIDO);"</automated>
  </verify>
  <done>npx prisma generate termina sin errores y node confirma que EstadoTurno.EN_ESPERA === 'EN_ESPERA' y EstadoTurno.SIENDO_ATENDIDO === 'SIENDO_ATENDIDO'</done>
</task>

</tasks>

<verification>
- `grep -c "EN_ESPERA\|SIENDO_ATENDIDO" backend/src/prisma/schema.prisma` devuelve 2
- El archivo de migración generado contiene ADD VALUE para ambos estados
- `npx prisma generate` termina sin errores
- El cliente Prisma en node_modules/.prisma/client expone los nuevos valores
</verification>

<success_criteria>
La base de datos y el cliente Prisma conocen EN_ESPERA y SIENDO_ATENDIDO. Cualquier `prisma.turno.update({ data: { estado: 'EN_ESPERA' } })` ejecuta sin error de constraint de Postgres. Plan 02 puede importar EstadoTurno.EN_ESPERA y EstadoTurno.SIENDO_ATENDIDO directamente desde @prisma/client.
</success_criteria>

<output>
After completion, create `.planning/phases/32-schema-backend-estados-extendidos/32-01-SUMMARY.md`
</output>
