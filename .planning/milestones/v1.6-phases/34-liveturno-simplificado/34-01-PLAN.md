---
phase: 34-liveturno-simplificado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/live-turno/LiveTurnoHeader.tsx
  - frontend/src/components/live-turno/LiveTurnoFooter.tsx
  - frontend/src/components/live-turno/LiveTurnoIndicator.tsx
  - frontend/src/hooks/useLiveTurnoTimer.ts
autonomous: true
requirements:
  - LT-01
  - LT-03

must_haves:
  truths:
    - "El header del panel de consulta activa NO muestra ningún reloj ni contador de tiempo"
    - "El footer NO muestra el label 'Duracion: 05:23' ni ninguna referencia a tiempo"
    - "El AlertDialog de 'Finalizar sesion' NO menciona la duración de la sesion"
    - "El indicator minimizado muestra nombre del paciente y badge de tipo de turno — sin timer"
    - "El botón 'Cerrar sin guardar entrada de HC' existe en el footer entre Minimizar y Finalizar sesion"
    - "Confirmar el dialog de 'Cerrar sin guardar entrada de HC' llama cerrarSesion.mutateAsync sin auto-guardar el borrador de HC"
    - "El archivo useLiveTurnoTimer.ts ya no existe en el repositorio"
  artifacts:
    - path: "frontend/src/components/live-turno/LiveTurnoHeader.tsx"
      provides: "Header sin Clock widget, sin import useLiveTurnoTimer, conserva dot 'En curso'"
      contains: "En curso"
    - path: "frontend/src/components/live-turno/LiveTurnoFooter.tsx"
      provides: "Footer con botón 'Cerrar sin guardar entrada de HC' y AlertDialog propio"
      contains: "Cerrar sin guardar entrada de HC"
    - path: "frontend/src/components/live-turno/LiveTurnoIndicator.tsx"
      provides: "Indicator minimizado con nombre + badge de tipo de turno, sin timer"
      contains: "session.tipoTurno"
  key_links:
    - from: "LiveTurnoFooter.tsx (nuevo botón)"
      to: "cerrarSesion.mutateAsync"
      via: "useLiveTurnoActions"
      pattern: "cerrarSesion\\.mutateAsync"
    - from: "LiveTurnoHeader.tsx"
      to: "useLiveTurnoTimer"
      via: "NO debe existir este import"
      pattern: "NO debe contener: useLiveTurnoTimer"
---

<objective>
Eliminar el timer de toda la UI del panel de consulta activa (LT-01) y agregar el camino explícito para cerrar la sesión sin guardar HC (LT-03).

Purpose: Reducir fricción cognitiva en el panel de consulta — el timer distrae y el bloqueo al salir sin HC fuerza una acción que no siempre aplica.
Output: Header limpio, Footer con botón de escape explícito, Indicator minimizado sin tiempo, hook de timer eliminado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-liveturno-simplificado/34-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From frontend/src/hooks/useLiveTurnoActions.ts:
```typescript
export function useLiveTurnoActions(): {
  iniciarSesion: UseMutationResult<..., string>;
  cerrarSesion: UseMutationResult<..., CerrarSesionDto | undefined>;
  cerrarSesionPorId: UseMutationResult<..., string>;
}

// cerrarSesion.mutateAsync({ entradaHCId?: string }) — llama PATCH /turnos/:id/cerrar-sesion
// cerrarSesion.isPending — boolean para deshabilitar botones durante la llamada
```

From frontend/src/store/live-turno.store.ts:
```typescript
export interface LiveTurnoSession {
  pacienteNombre: string;
  tipoTurno: string;         // nombre del tipo, ej. "Consulta", "Control"
  tipoTurnoId: string;
  // ...otros campos no relevantes para esta tarea
}

export interface HCFormDraft {
  saved?: boolean;
  // ...otros campos
}

export interface LiveTurnoDraftData {
  hcEntryId?: string;
  hcFormDraft?: HCFormDraft;
}

// En useLiveTurnoStore:
// const { minimize, draftData, session } = useLiveTurnoStore();
// draftData.hcFormDraft — para chequear borrador
// draftData.hcEntryId — se pasa a cerrarSesion
```

From frontend/src/components/ui/alert-dialog.tsx (ya usado en LiveTurnoFooter.tsx):
```typescript
// Patrón ya presente en el archivo — mismo import, mismo uso:
import {
  AlertDialog, AlertDialogAction, AlertDialogCancel,
  AlertDialogContent, AlertDialogDescription, AlertDialogFooter,
  AlertDialogHeader, AlertDialogTitle,
} from '@/components/ui/alert-dialog';
```

From frontend/src/components/live-turno/LiveTurnoHeader.tsx (actual):
```typescript
// Imports a remover:
import { useLiveTurnoTimer, formatTimer } from '@/hooks/useLiveTurnoTimer';
// Variables a remover:
const elapsed = useLiveTurnoTimer();
// Bloque a remover (el Clock widget, líneas 57-64):
{/* Timer */}
<div className="flex items-center gap-2 px-4 py-2 bg-gray-100 rounded-lg">
  <Clock className="w-5 h-5 text-gray-600" />
  <span className="font-mono text-xl font-semibold text-gray-900">
    {formatTimer(elapsed)}
  </span>
</div>
// Bloque a CONSERVAR (el dot "En curso", líneas 49-55):
<div className="flex items-center gap-2 px-3 py-1.5 bg-green-50 rounded-full">
  <span className="relative flex h-2.5 w-2.5">
    <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75" />
    <span className="relative inline-flex rounded-full h-2.5 w-2.5 bg-green-500" />
  </span>
  <span className="text-sm font-medium text-green-700">En curso</span>
</div>
// También remover import Clock de lucide-react (ya no se usa tras quitar el widget)
```

From frontend/src/components/live-turno/LiveTurnoFooter.tsx (actual):
```typescript
// Imports a remover:
import { useLiveTurnoTimer, formatTimer } from '@/hooks/useLiveTurnoTimer';
// Variable a remover:
const elapsed = useLiveTurnoTimer();
// Label a remover del JSX (línea 73-75):
<div className="text-sm text-gray-500">
  Duracion: <span className="font-mono">{formatTimer(elapsed)}</span>
</div>
// Del AlertDialog existente, remover referencia a duración (líneas 100-107):
// El texto actual menciona "se registrara la duracion de {formatTimer(elapsed)}"
// Reemplazar con texto sin duración
```

From frontend/src/components/live-turno/LiveTurnoIndicator.tsx (actual):
```typescript
// Imports a remover:
import { useLiveTurnoTimer, formatTimerCompact } from '@/hooks/useLiveTurnoTimer';
// Variable a remover:
const elapsed = useLiveTurnoTimer();
// Bloque timer a remover (líneas 33-35):
<span className="font-mono text-sm bg-green-700 px-2 py-0.5 rounded">
  {formatTimerCompact(elapsed)}
</span>
// Agregar badge de tipo de turno en su lugar:
// session.tipoTurno — usar Badge component o span styled
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Limpiar LiveTurnoHeader y LiveTurnoIndicator — remover timer</name>
  <files>
    frontend/src/components/live-turno/LiveTurnoHeader.tsx
    frontend/src/components/live-turno/LiveTurnoIndicator.tsx
  </files>
  <action>
**LiveTurnoHeader.tsx** — hacer los siguientes cambios:
1. Remover el import de `useLiveTurnoTimer` y `formatTimer` de `@/hooks/useLiveTurnoTimer`
2. Remover `import { Clock }` de `lucide-react` (ya no se usa; conservar `User`, `Phone`, `Mail`, `CreditCard`)
3. Remover la línea `const elapsed = useLiveTurnoTimer();`
4. Remover el bloque JSX del Clock widget completo — el `<div>` que contiene `<Clock />` y `{formatTimer(elapsed)}` (el bloque de "Timer" comentado, líneas 57-64 aproximadamente)
5. Conservar intacto el bloque "En curso" con el dot animado verde

El resultado del bloque superior derecho debe quedar solo con el dot "En curso":
```tsx
{/* Session Status */}
<div className="flex items-center gap-6">
  {/* Live Indicator */}
  <div className="flex items-center gap-2 px-3 py-1.5 bg-green-50 rounded-full">
    <span className="relative flex h-2.5 w-2.5">
      <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75" />
      <span className="relative inline-flex rounded-full h-2.5 w-2.5 bg-green-500" />
    </span>
    <span className="text-sm font-medium text-green-700">En curso</span>
  </div>
</div>
```

**LiveTurnoIndicator.tsx** — hacer los siguientes cambios:
1. Remover el import de `useLiveTurnoTimer` y `formatTimerCompact` de `@/hooks/useLiveTurnoTimer`
2. Remover la línea `const elapsed = useLiveTurnoTimer();`
3. Remover el bloque del timer compacto (`<span className="font-mono text-sm bg-green-700 ...">`)
4. Agregar un badge de tipo de turno en su lugar. Usar un `<span>` con estilo similar al existente (bg-green-700) que muestre `session.tipoTurno`:
```tsx
<span className="text-xs bg-green-700 px-2 py-0.5 rounded font-medium">
  {session.tipoTurno}
</span>
```
5. Conservar el pulsing indicator, el nombre del paciente (truncado), y el ícono Maximize2
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "LiveTurnoHeader|LiveTurnoIndicator|useLiveTurnoTimer" | head -20</automated>
  </verify>
  <done>
    - LiveTurnoHeader.tsx no importa ni referencia useLiveTurnoTimer ni formatTimer
    - LiveTurnoHeader.tsx conserva el dot "En curso"
    - LiveTurnoIndicator.tsx no importa ni referencia useLiveTurnoTimer ni formatTimerCompact
    - LiveTurnoIndicator.tsx muestra session.tipoTurno en lugar del timer
    - TypeScript no reporta errores en estos archivos
  </done>
</task>

<task type="auto">
  <name>Task 2: Reescribir LiveTurnoFooter — remover timer, agregar botón "Cerrar sin guardar entrada de HC" (LT-03)</name>
  <files>
    frontend/src/components/live-turno/LiveTurnoFooter.tsx
    frontend/src/hooks/useLiveTurnoTimer.ts
  </files>
  <action>
**LiveTurnoFooter.tsx** — reescribir completamente con los siguientes cambios:

1. Remover los imports: `useLiveTurnoTimer`, `formatTimer` de `@/hooks/useLiveTurnoTimer`
2. Remover la variable `const elapsed = useLiveTurnoTimer();`
3. Agregar estado para el nuevo dialog: `const [showCloseNoHcDialog, setShowCloseNoHcDialog] = useState(false);`
4. Remover el `<div className="text-sm text-gray-500">Duracion: ...</div>` del lado izquierdo del footer
5. Actualizar el AlertDialog existente de "Finalizar sesion" para eliminar la mención de duración. El `AlertDialogDescription` debe decir:
   `"¿Cerrar la sesión sin registrar entrada de HC? El turno quedará finalizado."`
   Espera — ese copy es para el NUEVO dialog. Para el AlertDialog existente de "Finalizar sesion" cambiar description a:
   `"La sesión será finalizada y el turno quedará marcado como completado."`
6. Agregar el botón "Cerrar sin guardar entrada de HC" entre Minimizar y Finalizar sesión:
```tsx
<Button
  variant="outline"
  onClick={() => setShowCloseNoHcDialog(true)}
  disabled={cerrarSesion.isPending}
>
  Cerrar sin guardar entrada de HC
</Button>
```
7. Agregar el `AlertDialog` para el nuevo botón. Handler: llama `cerrarSesion.mutateAsync({})` SIN el bloque de auto-guardado de HC (pasar objeto vacío — no pasar `entradaHCId`):

```tsx
<AlertDialog open={showCloseNoHcDialog} onOpenChange={setShowCloseNoHcDialog}>
  <AlertDialogContent>
    <AlertDialogHeader>
      <AlertDialogTitle>Cerrar sin registrar HC</AlertDialogTitle>
      <AlertDialogDescription>
        ¿Cerrar la sesión sin registrar entrada de HC? El turno quedará finalizado.
      </AlertDialogDescription>
    </AlertDialogHeader>
    <AlertDialogFooter>
      <AlertDialogCancel>Cancelar</AlertDialogCancel>
      <AlertDialogAction
        onClick={async () => {
          try {
            await cerrarSesion.mutateAsync({});
            setShowCloseNoHcDialog(false);
          } catch {
            // toast de error ya manejado por el mutation onError
          }
        }}
      >
        {cerrarSesion.isPending ? (
          <Loader2 className="w-4 h-4 mr-2 animate-spin" />
        ) : null}
        Cerrar sesión
      </AlertDialogAction>
    </AlertDialogFooter>
  </AlertDialogContent>
</AlertDialog>
```

8. Layout final del footer (sin el label de duración a la izquierda):
```tsx
<div className="flex items-center justify-end gap-3 px-6 py-4 border-t bg-gray-50">
  <Button variant="outline" onClick={minimize}>
    <Minimize2 className="w-4 h-4 mr-2" />
    Minimizar
  </Button>
  <Button
    variant="outline"
    onClick={() => setShowCloseNoHcDialog(true)}
    disabled={cerrarSesion.isPending}
  >
    Cerrar sin guardar entrada de HC
  </Button>
  <Button
    variant="destructive"
    onClick={() => setShowEndDialog(true)}
    disabled={cerrarSesion.isPending}
  >
    {cerrarSesion.isPending ? (
      <Loader2 className="w-4 h-4 mr-2 animate-spin" />
    ) : (
      <Square className="w-4 h-4 mr-2" />
    )}
    Finalizar sesión
  </Button>
</div>
```
Nota: el div cambia de `justify-between` a `justify-end` porque ya no hay contenido a la izquierda.

**useLiveTurnoTimer.ts** — ELIMINAR el archivo. Usar `git rm` o `rm`:
```bash
rm frontend/src/hooks/useLiveTurnoTimer.ts
```
Verificar que ningún otro archivo lo importa antes de eliminarlo (ejecutar: `grep -r "useLiveTurnoTimer" frontend/src/ --include="*.ts" --include="*.tsx"`). Tras los cambios en Task 1 y este task, no debe haber ningún caller restante.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && grep -r "useLiveTurnoTimer\|formatTimer\|formatTimerCompact" src/ --include="*.ts" --include="*.tsx" 2>/dev/null; echo "---"; npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <done>
    - `grep` no devuelve ninguna línea (cero referencias a useLiveTurnoTimer en el codebase)
    - El archivo `frontend/src/hooks/useLiveTurnoTimer.ts` no existe
    - LiveTurnoFooter.tsx contiene exactamente el texto "Cerrar sin guardar entrada de HC"
    - LiveTurnoFooter.tsx contiene exactamente el texto "Cerrar sin registrar HC" (título del dialog)
    - El AlertDialog existente de "Finalizar sesion" no menciona duración ni timer
    - TypeScript no reporta errores
  </done>
</task>

</tasks>

<verification>
```bash
# 1. Sin referencias al timer en todo el codebase frontend
grep -r "useLiveTurnoTimer\|formatTimer\|formatTimerCompact" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/ --include="*.ts" --include="*.tsx"
# Esperado: ninguna salida

# 2. El archivo timer hook no existe
ls /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/hooks/useLiveTurnoTimer.ts 2>&1
# Esperado: "No such file or directory"

# 3. TypeScript compila sin errores en los archivos modificados
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20
# Esperado: sin errores

# 4. Textos clave presentes
grep "Cerrar sin guardar entrada de HC" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/components/live-turno/LiveTurnoFooter.tsx
grep "Cerrar sin registrar HC" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/components/live-turno/LiveTurnoFooter.tsx
grep "En curso" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/components/live-turno/LiveTurnoHeader.tsx
# Esperado: cada grep devuelve al menos una línea
```
</verification>

<success_criteria>
- LT-01: El panel de consulta activa no muestra ningún contador de tiempo transcurrido — cero menciones de timer en Header, Footer ni Indicator
- LT-03: El footer tiene el botón "Cerrar sin guardar entrada de HC" que al confirmarse llama cerrarSesion sin auto-save del borrador de HC
- El hook useLiveTurnoTimer.ts eliminado del repositorio
- TypeScript compila sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/34-liveturno-simplificado/34-01-SUMMARY.md`
</output>
