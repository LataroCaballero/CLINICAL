---
phase: 35-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/pacientes/pacientes.service.ts
autonomous: true
requirements:
  - CRM-01

must_haves:
  truths:
    - "Un PATCH manual a CONFIRMADO es aceptado sin error aunque el paciente no tenga presupuesto ACEPTADO"
    - "El PATCH a PERDIDO sin motivoPerdida sigue siendo rechazado (integridad de datos)"
    - "El endpoint GET /kanban incluye el campo flujo en cada paciente de la respuesta"
  artifacts:
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "updateEtapaCRM sin validación CONFIRMADO; getKanban con campo flujo"
      contains: "flujo: p.flujo"
  key_links:
    - from: "pacientes.controller.ts PATCH /pacientes/:id/etapa-crm"
      to: "pacientes.service.ts updateEtapaCRM"
      via: "sin cambios en el controller"
      pattern: "updateEtapaCRM"
    - from: "pacientes.controller.ts GET /kanban"
      to: "pacientes.service.ts getKanban"
      via: "flujo: true en select + flujo: p.flujo en mapping"
      pattern: "flujo"
---

<objective>
Remover la validación de negocio que bloquea el movimiento manual a CONFIRMADO y exponer el campo `flujo` en el endpoint GET /kanban.

Purpose: El profesional debe poder mover un paciente a cualquier etapa del kanban sin restricciones — la única validación restante es la integridad de datos (motivoPerdida requerida para PERDIDO). El campo flujo en /kanban habilita el FlujoBadge del sheet en Phase 37.

Output: `pacientes.service.ts` modificado con dos cambios quirúrgicos: (1) eliminación de las líneas 519-528 y limpieza del select en findUnique; (2) adición de `flujo: true` en el select de getKanban y `flujo: p.flujo ?? null` en el response mapping.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/src/modules/pacientes/pacientes.service.ts
</context>

<interfaces>
<!-- Código actual relevante extraído del codebase — ejecutor no necesita explorar -->

De pacientes.service.ts — updateEtapaCRM (líneas 503-600):

```typescript
async updateEtapaCRM(
  id: string,
  dto: { etapaCRM: EtapaCRM; motivoPerdida?: MotivoPerdidaCRM },
) {
  const paciente = await this.prisma.paciente.findUnique({
    where: { id },
    select: { id: true, profesionalId: true, presupuestos: { select: { estado: true } } },
    //                                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    //                                        ELIMINAR — solo servía para la validación de abajo
  });
  if (!paciente) throw new NotFoundException('Paciente no encontrado');

  if (dto.etapaCRM === EtapaCRM.PERDIDO && !dto.motivoPerdida) {
    throw new BadRequestException('Se requiere motivoPerdida al mover a etapa PERDIDO');
  }

  // ELIMINAR ESTE BLOQUE COMPLETO (líneas 519-528):
  if (dto.etapaCRM === EtapaCRM.CONFIRMADO) {
    const tienePresupuestoAceptado = paciente.presupuestos.some(
      (p) => p.estado === EstadoPresupuesto.ACEPTADO,
    );
    if (!tienePresupuestoAceptado) {
      throw new BadRequestException(
        'El paciente debe tener al menos un presupuesto ACEPTADO para pasar a CONFIRMADO',
      );
    }
  }
  // FIN DEL BLOQUE A ELIMINAR

  const updated = await this.prisma.paciente.update({ ... });
  // auto-logs etapaNotaMap (NUEVO_LEAD, CONSULTADO, PRESUPUESTO_ENVIADO) — NO TOCAR
  // tareaSeguimiento createMany — NO TOCAR
}
```

De pacientes.service.ts — getKanban select (líneas 609-641):

```typescript
select: {
  id: true,
  nombreCompleto: true,
  fotoUrl: true,
  etapaCRM: true,
  temperatura: true,
  scoreConversion: true,
  tratamiento: true,
  lugarIntervencion: true,
  updatedAt: true,
  enListaEspera: true,
  comentarioListaEspera: true,
  // AGREGAR: flujo: true,
  presupuestos: { ... },
  turnos: { ... },
  contactos: { ... },
  autorizaciones: { ... },
}
```

De pacientes.service.ts — getKanban response mapping (líneas 667-695):

```typescript
pacientes: items.map((p) => ({
  id: p.id,
  nombreCompleto: p.nombreCompleto,
  fotoUrl: p.fotoUrl,
  etapaCRM: p.etapaCRM,
  temperatura: p.temperatura,
  scoreConversion: p.scoreConversion,
  procedimiento: p.tratamiento ?? p.lugarIntervencion ?? null,
  ultimoContactoNota: p.contactos[0]?.nota ?? null,
  ultimoContactoFecha: p.contactos[0]?.fecha ?? null,
  ultimoTurno: p.turnos[0]?.inicio ?? null,
  presupuesto: p.presupuestos[0] ? { ... } : null,
  diasDesdePresupuesto: ...,
  enListaEspera: p.enListaEspera,
  comentarioListaEspera: p.comentarioListaEspera,
  pendingAutorizaciones: p.autorizaciones.length,
  // AGREGAR: flujo: p.flujo ?? null,
})),
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remover validación CONFIRMADO de updateEtapaCRM</name>
  <files>backend/src/modules/pacientes/pacientes.service.ts</files>
  <action>
En `updateEtapaCRM` (línea 503):

1. En el `findUnique` (línea 507-510), cambiar el `select` para eliminar el nested `presupuestos`:
   - De: `select: { id: true, profesionalId: true, presupuestos: { select: { estado: true } } }`
   - A: `select: { id: true, profesionalId: true }`

2. Eliminar el bloque completo de las líneas 519-528:
   ```typescript
   if (dto.etapaCRM === EtapaCRM.CONFIRMADO) {
     const tienePresupuestoAceptado = paciente.presupuestos.some(
       (p) => p.estado === EstadoPresupuesto.ACEPTADO,
     );
     if (!tienePresupuestoAceptado) {
       throw new BadRequestException(
         'El paciente debe tener al menos un presupuesto ACEPTADO para pasar a CONFIRMADO',
       );
     }
   }
   ```

3. NO tocar: la validación de `motivoPerdida` (líneas 513-517), los auto-logs del `etapaNotaMap` (líneas 539-553), ni las `tareaSeguimiento` (líneas 555-578).

4. Si tras la eliminación el import `EstadoPresupuesto` queda sin uso en el archivo, eliminarlo del import.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>El build de TypeScript pasa sin errores. El método updateEtapaCRM ya no contiene el bloque `if (dto.etapaCRM === EtapaCRM.CONFIRMADO)`. El select de findUnique no incluye `presupuestos`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Agregar campo flujo al endpoint GET /kanban</name>
  <files>backend/src/modules/pacientes/pacientes.service.ts</files>
  <action>
En el método `getKanban` (línea 603):

1. En el `select` del `findMany` (línea 609-641), agregar `flujo: true` junto al resto de campos escalares (después de `comentarioListaEspera: true`, antes de la relación `presupuestos`).

2. En el response mapping del `items.map` (líneas 667-695), agregar `flujo: p.flujo ?? null` como último campo del objeto mapeado (después de `pendingAutorizaciones`).

No tocar: el WHERE filter (mantiene `CIRUGIA + null legacy`), las columnas del kanban agrupadas, ni el controller.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>El build de TypeScript pasa sin errores. La respuesta de getKanban incluye `flujo` en cada paciente del mapeado. El campo `flujo: true` está presente en el select del findMany.</done>
</task>

</tasks>

<verification>
Verificación integral al finalizar ambas tareas:

```bash
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build
```

Verificación manual de la lógica de negocio (búsqueda en el archivo resultante):
```bash
grep -n "CONFIRMADO\|motivoPerdida\|flujo" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend/src/modules/pacientes/pacientes.service.ts | head -30
```

Resultado esperado:
- NO debe aparecer la línea `'El paciente debe tener al menos un presupuesto ACEPTADO para pasar a CONFIRMADO'`
- DEBE aparecer `motivoPerdida` (validación de PERDIDO — intacta)
- DEBE aparecer `flujo: true` (en el select de getKanban)
- DEBE aparecer `flujo: p.flujo` (en el mapping de getKanban)
</verification>

<success_criteria>
- Build TypeScript sin errores
- `updateEtapaCRM` acepta movimiento a CONFIRMADO sin validar presupuestos
- `updateEtapaCRM` sigue rechazando PERDIDO sin motivoPerdida
- `getKanban` devuelve `flujo` por paciente en el response
- Los auto-contact logs del `etapaNotaMap` permanecen intactos
</success_criteria>

<output>
After completion, create `.planning/phases/35-backend-foundation/35-01-SUMMARY.md`
</output>
