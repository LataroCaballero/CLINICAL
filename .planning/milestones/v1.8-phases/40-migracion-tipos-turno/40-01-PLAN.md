---
phase: 40-migracion-tipos-turno
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/migrations/20260608000000_migracion_tipos_turno_v18/migration.sql
  - backend/src/modules/tipos-turno/tipos-turno.service.ts
autonomous: true
requirements:
  - TIPO-01
  - TIPO-02
  - TIPO-03
  - TIPO-04
  - TIPO-05
  - TIPO-06

must_haves:
  truths:
    - "GET /tipos-turno devuelve exactamente 4 tipos: Consulta, Control, Pre-Quirúrgico, Tratamiento (no Cirugía)"
    - "Los turnos históricos que referenciaban 'Consulta para cirugía' o 'Consulta pendiente' ahora apuntan al tipo 'Consulta'"
    - "El tipo 'Tratamiento' (ex 'Consulta para tratamiento en consultorio') mantiene flujoPaciente=TRATAMIENTO"
    - "El tipo 'Pre-Quirúrgico' (ex 'Pre-operatorio') mantiene flujoPaciente=CIRUGIA"
    - "El tipo 'Cirugía' (esCirugia=true) sigue en DB y crearTurnoCirugia() funciona sin cambios"
  artifacts:
    - path: "backend/src/prisma/migrations/20260608000000_migracion_tipos_turno_v18/migration.sql"
      provides: "Data migration SQL transaccional: INSERT Consulta, UPDATE Turno FKs, UPSERT TipoTurnoProfesional, DELETE tipos obsoletos, renames"
      contains: "BEGIN; ... COMMIT;"
    - path: "backend/src/modules/tipos-turno/tipos-turno.service.ts"
      provides: "findAll() filtrado con where: { esCirugia: false }"
      contains: "esCirugia: false"
  key_links:
    - from: "TiposTurnoService.findAll()"
      to: "GET /tipos-turno response"
      via: "where: { esCirugia: false } en prisma.tipoTurno.findMany()"
      pattern: "esCirugia.*false"
    - from: "migration.sql PART B"
      to: "Turno.tipoTurnoId"
      via: "UPDATE Turno SET tipoTurnoId = subquery WHERE tipoTurnoId = subquery"
      pattern: "UPDATE.*Turno.*SET.*tipoTurnoId"
---

<objective>
Migrar los registros TipoTurno en la base de datos de 6 tipos legacy a 5 tipos v1.8 (4 públicos + 1 interno), y filtrar el tipo interno del endpoint público.

Purpose: Que el selector de tipos al agendar muestre exactamente 4 opciones claras sin tipos legacy confusos. Los turnos históricos se preservan reasignando sus FK.
Output: Archivo de migración SQL aplicado, `tipos-turno.service.ts` con filtro esCirugia=false.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/40-migracion-tipos-turno/40-CONTEXT.md
@.planning/phases/40-migracion-tipos-turno/40-RESEARCH.md

<interfaces>
<!-- Código actual relevante. El executor usa esto directamente — sin exploración del repo. -->

De backend/src/modules/tipos-turno/tipos-turno.service.ts (línea 9-21):
```typescript
findAll() {
  return this.prisma.tipoTurno.findMany({
    orderBy: { nombre: 'asc' },
    select: {
      id: true,
      nombre: true,
      descripcion: true,
      duracionDefault: true,
      flujoPaciente: true,
      esCirugia: true,
    },
  });
}
```

De backend/src/prisma/migrations/20260416000000_flujo_paciente/migration.sql (patrón a seguir):
```sql
-- Migration: <name>
-- Phase <N>: <description>

-- PART A: DDL — <description or "ningún cambio de schema">

-- PART B: Data migration (transactional)

BEGIN;
-- ... operaciones ...
COMMIT;
```

Tipos actuales en DB (pre-migración):
- "Consulta para cirugía"   (flujoPaciente=CIRUGIA, esCirugia=false)
- "Consulta pendiente"      (flujoPaciente=NULL, esCirugia=false)
- "Consulta para tratamiento en consultorio" (flujoPaciente=TRATAMIENTO, esCirugia=false)
- "Pre-operatorio"          (flujoPaciente=CIRUGIA, esCirugia=false)
- "Control"                 (flujoPaciente=NULL, esCirugia=false)
- "Cirugía"                 (flujoPaciente=NULL, esCirugia=true)

Tipos objetivo post-migración (4 públicos + 1 interno):
- "Consulta"      (flujoPaciente=NULL, esCirugia=false)   ← NUEVO
- "Control"       (flujoPaciente=NULL, esCirugia=false)   ← sin cambio
- "Pre-Quirúrgico" (flujoPaciente=CIRUGIA, esCirugia=false) ← rename de "Pre-operatorio"
- "Tratamiento"   (flujoPaciente=TRATAMIENTO, esCirugia=false) ← rename
- "Cirugía"       (flujoPaciente=NULL, esCirugia=true)    ← sin cambio (interno)

TipoTurno tiene nombre @unique (línea 771 del schema: `nombre String @unique`).
TipoTurnoProfesional tiene @@unique([profesionalId, tipoTurnoId]) (línea 794).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear migración SQL transaccional de datos TipoTurno</name>
  <files>backend/src/prisma/migrations/20260608000000_migracion_tipos_turno_v18/migration.sql</files>
  <action>
Crear el directorio y archivo de migración manualmente (no ejecutar `prisma migrate dev` — Phase 40 no tiene cambios DDL y el comando puede no generar un archivo nuevo).

Crear `backend/src/prisma/migrations/20260608000000_migracion_tipos_turno_v18/migration.sql` con el siguiente contenido exacto:

```sql
-- Migration: migracion_tipos_turno_v18
-- Phase 40: Reorganización de TipoTurno para v1.8

-- ============================================================
-- PART A: DDL — ningún cambio de schema en esta fase
-- ============================================================

-- ============================================================
-- PART B: Data migration (transactional)
-- ============================================================

BEGIN;

-- Step 1: Crear el nuevo tipo "Consulta" (flujoPaciente=NULL, esCirugia=false)
INSERT INTO "TipoTurno" ("id", "nombre", "esCirugia", "flujoPaciente")
VALUES (gen_random_uuid(), 'Consulta', false, NULL);

-- Step 2: Migrar turnos de "Consulta para cirugía" → "Consulta"
UPDATE "Turno"
SET "tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE "nombre" = 'Consulta' LIMIT 1)
WHERE "tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE "nombre" = 'Consulta para cirugía' LIMIT 1);

-- Step 3: Migrar turnos de "Consulta pendiente" → "Consulta"
UPDATE "Turno"
SET "tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE "nombre" = 'Consulta' LIMIT 1)
WHERE "tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE "nombre" = 'Consulta pendiente' LIMIT 1);

-- Step 4: Transferir configs TipoTurnoProfesional de "Consulta para cirugía" → "Consulta"
-- ON CONFLICT DO NOTHING evita violación de unique constraint [profesionalId, tipoTurnoId]
INSERT INTO "TipoTurnoProfesional" (
  "id", "profesionalId", "tipoTurnoId", "duracionMinutos", "colorHex", "createdAt", "updatedAt"
)
SELECT
  gen_random_uuid(),
  ttp."profesionalId",
  (SELECT id FROM "TipoTurno" WHERE nombre = 'Consulta' LIMIT 1),
  ttp."duracionMinutos",
  ttp."colorHex",
  now(),
  now()
FROM "TipoTurnoProfesional" ttp
WHERE ttp."tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE nombre = 'Consulta para cirugía' LIMIT 1)
ON CONFLICT ("profesionalId", "tipoTurnoId") DO NOTHING;

-- Step 5: Eliminar configs de "Consulta pendiente" (no se transfieren)
DELETE FROM "TipoTurnoProfesional"
WHERE "tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE "nombre" = 'Consulta pendiente' LIMIT 1);

-- Step 6: Eliminar configs de "Consulta para cirugía" (ya transferidas al Step 4)
DELETE FROM "TipoTurnoProfesional"
WHERE "tipoTurnoId" = (SELECT id FROM "TipoTurno" WHERE "nombre" = 'Consulta para cirugía' LIMIT 1);

-- Step 7: Eliminar TipoTurno "Consulta pendiente" (ya sin Turnos ni TipoTurnoProfesional)
DELETE FROM "TipoTurno" WHERE "nombre" = 'Consulta pendiente';

-- Step 8: Eliminar TipoTurno "Consulta para cirugía" (ya sin Turnos ni TipoTurnoProfesional)
DELETE FROM "TipoTurno" WHERE "nombre" = 'Consulta para cirugía';

-- Step 9: Rename "Consulta para tratamiento en consultorio" → "Tratamiento"
-- El id no cambia, TipoTurnoProfesional existentes se preservan automáticamente
UPDATE "TipoTurno"
SET "nombre" = 'Tratamiento'
WHERE "nombre" = 'Consulta para tratamiento en consultorio';

-- Step 10: Rename "Pre-operatorio" → "Pre-Quirúrgico"
-- El id no cambia, TipoTurnoProfesional existentes se preservan automáticamente
UPDATE "TipoTurno"
SET "nombre" = 'Pre-Quirúrgico'
WHERE "nombre" = 'Pre-operatorio';

COMMIT;
```

Luego aplicar la migración desde `backend/`:
```bash
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npx prisma migrate deploy
```

Si `prisma migrate deploy` falla porque el entorno es dev, usar:
```bash
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npx prisma migrate dev --name migracion_tipos_turno_v18
```
En ese caso, Prisma puede generar un archivo nuevo — copiar el SQL de PART B al archivo generado y borrar el archivo creado manualmente para evitar duplicados.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npx prisma migrate status 2>&1 | grep -E "Database schema is up to date|migracion_tipos_turno"</automated>
  </verify>
  <done>
    - Archivo migration.sql existe en `migrations/20260608000000_migracion_tipos_turno_v18/`
    - `prisma migrate status` reporta la migración como aplicada
    - DB tiene 5 tipos de turno: Consulta, Control, Pre-Quirúrgico, Tratamiento, Cirugía
    - Ningún Turno apunta a "Consulta para cirugía" o "Consulta pendiente" (ya no existen)
  </done>
</task>

<task type="auto">
  <name>Task 2: Agregar filtro esCirugia=false en TiposTurnoService.findAll()</name>
  <files>backend/src/modules/tipos-turno/tipos-turno.service.ts</files>
  <action>
En `backend/src/modules/tipos-turno/tipos-turno.service.ts`, modificar el método `findAll()` agregando `where: { esCirugia: false }` como primera propiedad del objeto pasado a `findMany()`.

El método debe quedar:
```typescript
findAll() {
  return this.prisma.tipoTurno.findMany({
    where: { esCirugia: false },  // excluye Cirugía del selector público
    orderBy: { nombre: 'asc' },
    select: {
      id: true,
      nombre: true,
      descripcion: true,
      duracionDefault: true,
      flujoPaciente: true,
      esCirugia: true,
    },
  });
}
```

No tocar ningún otro método del service. En particular:
- `getConfigByProfesional()` — no cambiar (opera sobre TipoTurnoProfesional, no TipoTurno)
- `saveConfigByProfesional()` — no cambiar
- `crearTurnoCirugia()` en `turnos.service.ts` usa `findFirst({ where: { esCirugia: true } })` independientemente — no se ve afectado por este cambio
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `tipos-turno.service.ts` contiene `where: { esCirugia: false }` en `findAll()`
    - `npm run build` pasa sin errores TypeScript
    - GET /tipos-turno en un servidor levantado devuelve 4 registros (Consulta, Control, Pre-Quirúrgico, Tratamiento) — sin Cirugía
  </done>
</task>

</tasks>

<verification>
Con el servidor backend corriendo (`npm run start:dev` desde `backend/`):

```bash
# Verificar que GET /tipos-turno devuelve exactamente 4 tipos (requiere token JWT válido)
curl -s -H "Authorization: Bearer <token>" http://localhost:3001/tipos-turno | jq 'length, .[].nombre'
# Esperado: 4 y los nombres Consulta, Control, Pre-Quirúrgico, Tratamiento

# Verificar que Cirugía NO aparece en el response
curl -s -H "Authorization: Bearer <token>" http://localhost:3001/tipos-turno | jq '.[].nombre' | grep -i cirug
# Esperado: sin output (Cirugía filtrada)
```

Verificar en DB (psql o Prisma Studio):
```sql
SELECT nombre, "flujoPaciente", "esCirugia" FROM "TipoTurno" ORDER BY nombre;
-- Esperado: 5 filas (Cirugía, Consulta, Control, Pre-Quirúrgico, Tratamiento)

SELECT COUNT(*) FROM "Turno" t
JOIN "TipoTurno" tt ON tt.id = t."tipoTurnoId"
WHERE tt.nombre IN ('Consulta para cirugía', 'Consulta pendiente');
-- Esperado: 0 (todos migrados a "Consulta")
```
</verification>

<success_criteria>
- DB tiene exactamente 5 tipos de turno post-migración: Consulta (NULL/false), Control (NULL/false), Pre-Quirúrgico (CIRUGIA/false), Tratamiento (TRATAMIENTO/false), Cirugía (NULL/true)
- GET /tipos-turno devuelve 4 tipos (Cirugía excluida por filtro backend)
- Cero turnos apuntando a tipos eliminados ("Consulta para cirugía", "Consulta pendiente")
- `npm run build` pasa sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/40-migracion-tipos-turno/40-01-SUMMARY.md`
</output>
