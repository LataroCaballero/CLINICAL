---
phase: 40-migracion-tipos-turno
plan: "02"
type: execute
wave: 2
depends_on:
  - "40-01"
files_modified:
  - backend/src/prisma/seed-tipos-turno.ts
  - backend/package.json
  - frontend/src/app/dashboard/turnos/CalendarGrid.tsx
autonomous: true
requirements:
  - TIPO-01
  - TIPO-06

must_haves:
  truths:
    - "El seed seed-tipos-turno.ts es idempotente y puede ejecutarse en un fresh environment para crear los 4 tipos públicos + Cirugía"
    - "Re-ejecutar el seed no genera errores ni duplica registros"
    - "Los turnos Pre-Quirúrgico aparecen en color naranja/orange en el CalendarGrid, no en verde"
    - "El selector de tipos al crear un turno desde el frontend muestra los 4 tipos correctos"
  artifacts:
    - path: "backend/src/prisma/seed-tipos-turno.ts"
      provides: "Seed idempotente para 5 tipos de turno v1.8"
      contains: "prisma.tipoTurno.upsert"
    - path: "backend/package.json"
      provides: "Script seed:tipos para ejecutar el seed"
      contains: "seed:tipos"
    - path: "frontend/src/app/dashboard/turnos/CalendarGrid.tsx"
      provides: "Color orange para Pre-Quirúrgico en getEventStyle()"
      contains: "pre-quir"
  key_links:
    - from: "getEventStyle() in CalendarGrid.tsx"
      to: "color orange para Pre-Quirúrgico"
      via: "tipo.includes('pre-quir') || tipo.includes('pre-') branch ANTES de default green"
      pattern: "pre-quir|pre-"
    - from: "seed-tipos-turno.ts"
      to: "TipoTurno table"
      via: "prisma.tipoTurno.upsert con where: { nombre }"
      pattern: "tipoTurno.upsert"
---

<objective>
Crear el seed reproducible para fresh environments y corregir el color de Pre-Quirúrgico en el calendario.

Purpose: Que la aplicación pueda inicializarse desde cero con los 4 tipos correctos, y que el CalendarGrid muestre Pre-Quirúrgico en naranja (no en verde por caída al default).
Output: `seed-tipos-turno.ts` ejecutable, script en package.json, corrección de color en CalendarGrid.tsx.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-migracion-tipos-turno/40-CONTEXT.md
@.planning/phases/40-migracion-tipos-turno/40-01-SUMMARY.md

<interfaces>
<!-- Patrón de seed existente del repo — usar como base para seed-tipos-turno.ts -->

De backend/src/prisma/seed-users.ts (estructura a replicar):
```typescript
import { PrismaClient, RolUsuario } from '@prisma/client';
import * as bcrypt from 'bcrypt';

const prisma = new PrismaClient();

async function main() {
  console.log('...');
  // lógica aquí
}

main()
  .catch((e) => { console.error(e); process.exit(1); })
  .finally(async () => { await prisma.$disconnect(); });
```

De frontend/src/app/dashboard/turnos/CalendarGrid.tsx — función getEventStyle() líneas 87-128 (lógica actual):
```typescript
function getEventStyle(
  event: CalendarEvent,
  colorMap?: Record<string, string>,
  fm?: boolean
): { bg: string; border: string; text: string } {
  const customColor = colorMap?.[event.tipoTurnoId];
  if (customColor) { ... }

  const tipo = event.tipo.toLowerCase();

  if (fm) {
    if (tipo.includes("consulta inicial"))   return { bg: "#1e1b4b", border: "#818cf8", text: "#c7d2fe" }; // indigo dark
    if (tipo.includes("consulta") || tipo.includes("control"))
                                             return { bg: "#0c2a4a", border: "#38bdf8", text: "#bae6fd" }; // sky dark
    if (tipo.includes("cirug"))              return { bg: "#4c0519", border: "#fb7185", text: "#fecdd3" }; // rose dark
    if (tipo.includes("procedimiento") || tipo.includes("tratamiento"))
                                             return { bg: "#2e1065", border: "#a78bfa", text: "#ddd6fe" }; // violet dark
    if (tipo.includes("emergencia") || tipo.includes("urgencia"))
                                             return { bg: "#450a0a", border: "#f87171", text: "#fecaca" }; // red dark
    return { bg: "#052e16", border: "#4ade80", text: "#bbf7d0" }; // green dark (default)
  }

  // Light theme
  if (tipo.includes("consulta inicial"))     return { bg: "#EEF2FF", border: "#6366F1", text: "#3730A3" }; // indigo
  if (tipo.includes("consulta") || tipo.includes("control"))
                                             return { bg: "#F0F9FF", border: "#0EA5E9", text: "#0C4A6E" }; // sky
  if (tipo.includes("cirug"))                return { bg: "#FFF1F2", border: "#F43F5E", text: "#881337" }; // rose
  if (tipo.includes("procedimiento") || tipo.includes("tratamiento"))
                                             return { bg: "#F5F3FF", border: "#8B5CF6", text: "#4C1D95" }; // violet
  if (tipo.includes("emergencia") || tipo.includes("urgencia"))
                                             return { bg: "#FEF2F2", border: "#EF4444", text: "#7F1D1D" }; // red

  return { bg: "#F0FDF4", border: "#22C55E", text: "#14532D" }; // green (default)
}
```

Post-migración, "Pre-Quirúrgico".toLowerCase() = "pre-quirúrgico"
- `tipo.includes("pre-quir")` matchea "pre-quirúrgico" ✓ (usar este como primer check)
- `tipo.includes("pre-")` también matchea ✓ (fallback)
- Sin este branch, cae al default green (incorrecto — debe ser orange)

Colores orange esperados (consistentes con UpcomingAppointments.tsx):
- Dark mode: `{ bg: "#431407", border: "#fb923c", text: "#fed7aa" }`
- Light mode: `{ bg: "#FFF7ED", border: "#F97316", text: "#7C2D12" }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear seed-tipos-turno.ts e incorporar script en package.json</name>
  <files>
    backend/src/prisma/seed-tipos-turno.ts
    backend/package.json
  </files>
  <action>
**1. Crear `backend/src/prisma/seed-tipos-turno.ts`:**

```typescript
/**
 * SEED: Tipos de Turno v1.8
 * Crea los 4 tipos públicos + 1 interno (Cirugía) de forma idempotente.
 * Re-ejecutable sin errores en entornos existentes o fresh.
 */

import { PrismaClient, FlujoPaciente } from '@prisma/client';

const prisma = new PrismaClient();

const TIPOS_TURNO = [
  { nombre: 'Consulta',       flujoPaciente: null as FlujoPaciente | null,       esCirugia: false },
  { nombre: 'Control',        flujoPaciente: null as FlujoPaciente | null,       esCirugia: false },
  { nombre: 'Pre-Quirúrgico', flujoPaciente: FlujoPaciente.CIRUGIA,              esCirugia: false },
  { nombre: 'Tratamiento',    flujoPaciente: FlujoPaciente.TRATAMIENTO,          esCirugia: false },
  { nombre: 'Cirugía',        flujoPaciente: null as FlujoPaciente | null,       esCirugia: true  },
];

async function main() {
  console.log('Seed: tipos de turno v1.8');
  for (const tipo of TIPOS_TURNO) {
    await prisma.tipoTurno.upsert({
      where: { nombre: tipo.nombre },
      update: { flujoPaciente: tipo.flujoPaciente, esCirugia: tipo.esCirugia },
      create: tipo,
    });
    console.log(`  upserted: ${tipo.nombre}`);
  }
  console.log('Seed tipos de turno completado.');
}

main()
  .catch((e) => {
    console.error('Error en seed tipos-turno:', e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

**2. Agregar script en `backend/package.json`:**

En la sección `"scripts"`, agregar como último script antes del cierre `}`:
```json
"seed:tipos": "ts-node src/prisma/seed-tipos-turno.ts"
```

ts-node ya está en devDependencies (^10.9.1) — no instalar nada nuevo.

**3. Verificar el seed ejecutando:**
```bash
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run seed:tipos
```

Debe imprimir "upserted: Consulta", "upserted: Control", etc. y terminar sin errores.
Ejecutar una segunda vez para confirmar idempotencia (mismos prints, sin errores de unique constraint).
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run seed:tipos 2>&1 | tail -8</automated>
  </verify>
  <done>
    - `seed-tipos-turno.ts` existe en `backend/src/prisma/`
    - `npm run seed:tipos` ejecuta sin errores e imprime 5 líneas "upserted: ..."
    - Re-ejecutar `npm run seed:tipos` no produce errores (idempotencia confirmada)
    - `backend/package.json` contiene el script `"seed:tipos"`
  </done>
</task>

<task type="auto">
  <name>Task 2: Corregir color de Pre-Quirúrgico en CalendarGrid.tsx</name>
  <files>frontend/src/app/dashboard/turnos/CalendarGrid.tsx</files>
  <action>
En `frontend/src/app/dashboard/turnos/CalendarGrid.tsx`, dentro de la función `getEventStyle()`, agregar un branch para "Pre-Quirúrgico" (color orange) ANTES de los branches de "consulta inicial" y "consulta/control".

El branch debe insertarse como el PRIMER if de tipo (después de la comprobación `customColor` y ANTES del `if (tipo.includes("consulta inicial"))`).

**En el bloque `if (fm)` (dark mode), agregar como primera condición:**
```typescript
if (tipo.includes("pre-quir") || tipo.includes("pre-"))
  return { bg: "#431407", border: "#fb923c", text: "#fed7aa" }; // orange dark
```

**En el bloque light mode (después del `// Light theme` comment), agregar como primera condición:**
```typescript
if (tipo.includes("pre-quir") || tipo.includes("pre-"))
  return { bg: "#FFF7ED", border: "#F97316", text: "#7C2D12" }; // orange
```

El nombre "Pre-Quirúrgico".toLowerCase() = "pre-quirúrgico", que matchea `tipo.includes("pre-quir")`. El branch `pre-` es fallback. Insertar ANTES de "consulta" para evitar que "pre-" matchee inadvertidamente otros tipos que no existen actualmente — esto es seguro dado el catálogo de 5 tipos.

No modificar ningún otro branch de la función. No tocar la lógica de `customColor`, `fm`, `getStatusDotColor`, ni ningún otro helper.

Verificar que el archivo compila correctamente con:
```bash
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npm run build 2>&1 | grep -E "error|Error|compiled" | head -10
```
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npm run build 2>&1 | grep -E "error TS|Build error|Failed" | head -5</automated>
  </verify>
  <done>
    - `CalendarGrid.tsx` contiene branch `tipo.includes("pre-quir")` retornando colores orange en ambos modos (fm y light)
    - El branch está insertado ANTES de "consulta inicial" en ambos bloques
    - `npm run build` del frontend pasa sin errores TypeScript
    - Los turnos de tipo "Pre-Quirúrgico" mostrarán border naranja (#F97316) en el calendario
  </done>
</task>

</tasks>

<verification>
Verificación manual final (flujo de smoke test):

1. Con el backend corriendo (`npm run start:dev`), abrir el selector de tipo de turno al crear un turno nuevo desde la UI
2. Confirmar que aparecen exactamente 4 opciones: Consulta, Control, Pre-Quirúrgico, Tratamiento
3. Confirmar que NO aparece "Cirugía", "Consulta para cirugía", "Consulta pendiente" ni "Consulta para tratamiento en consultorio"
4. En el CalendarGrid (vista de semana o día), un turno de tipo Pre-Quirúrgico debe mostrar borde naranja, no verde

Verificación de seed en fresh DB:
```bash
# Desde backend/ — simula fresh environment
npm run seed:tipos
# Luego verificar en DB: SELECT nombre FROM "TipoTurno" ORDER BY nombre;
# Esperado: 5 filas
```
</verification>

<success_criteria>
- `npm run seed:tipos` ejecuta correctamente, idempotente en múltiples ejecuciones
- CalendarGrid.tsx tiene branch orange para Pre-Quirúrgico en ambos modos (dark/light)
- Frontend compila sin errores TypeScript
- Smoke test visual: selector de tipos muestra exactamente 4 opciones (sin Cirugía ni tipos legacy)
</success_criteria>

<output>
After completion, create `.planning/phases/40-migracion-tipos-turno/40-02-SUMMARY.md`
</output>
