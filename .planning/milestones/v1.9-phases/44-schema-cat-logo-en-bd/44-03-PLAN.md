---
phase: 44-schema-cat-logo-en-bd
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/catalogo-hc.ts
  - frontend/src/hooks/useCatalogoHC.ts
autonomous: true
requirements: [ZONA-01]

must_haves:
  truths:
    - "Existe el hook useCatalogoHC que consulta GET /catalogo-hc vía TanStack Query, listo para que Phase 45 lo consuma"
    - "Los tipos del catálogo (ZonaHC con diagnósticos y tratamientos anidados, precio resuelto) están tipados en frontend/src/types"
    - "El hook no dispara la query hasta tener profesionalId cuando se requiere (enabled guard)"
  artifacts:
    - path: "frontend/src/types/catalogo-hc.ts"
      provides: "Tipos ZonaHC/DiagnosticoHC/TratamientoHC del response"
      exports: ["ZonaHC", "DiagnosticoHC", "TratamientoHC"]
    - path: "frontend/src/hooks/useCatalogoHC.ts"
      provides: "Hook TanStack Query + query key exportado"
      exports: ["useCatalogoHC", "CATALOGO_HC_QUERY_KEY"]
  key_links:
    - from: "frontend/src/hooks/useCatalogoHC.ts"
      to: "/catalogo-hc"
      via: "api.get (axios instance lib/api.ts)"
      pattern: "api\\.get\\(['\"]/catalogo-hc"
---

<objective>
Crear el hook frontend `useCatalogoHC` y los tipos TypeScript del catálogo de zonas HC, siguiendo el patrón de `useCirugiasCatalogo`. Phase 45 (rediseño de PrimeraConsultaForm) consume este hook.

Purpose: completar la capa de acceso al catálogo (ZONA-01) — el contrato del endpoint queda tipado y consumible desde el frontend sin tocar todavía el formulario.
Output: hook + tipos compilando contra el contrato del GET /catalogo-hc.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/hooks/useCirugiasCatalogo.ts
@frontend/src/lib/api.ts

<interfaces>
<!-- CONTRATO del response GET /catalogo-hc (definido en plan 44-02 — implementar contra este shape, no explorar el backend): -->

```typescript
// Array ordenado por orden asc; "Otros" siempre última zona (orden 9999)
type Response = ZonaHC[];

interface DiagnosticoHC {
  id: string;
  nombre: string;
  orden: number;
  esSistema: boolean; // ítems "Otros" protegidos
}

interface TratamientoHC {
  id: string;
  nombre: string;
  orden: number;
  esSistema: boolean;
  tratamientoId: string | null; // FK al catálogo de precios (null = sin match)
  precio: number | null;        // precio resuelto por join en el backend
}

interface ZonaHC {
  id: string;
  nombre: string;
  orden: number;
  esSistema: boolean;
  diagnosticos: DiagnosticoHC[];
  tratamientos: TratamientoHC[];
}
```

<!-- Patrón existente a seguir (useCirugiasCatalogo.ts): -->

```typescript
import { useQuery } from '@tanstack/react-query';
import { api } from '@/lib/api';

const QUERY_KEY = 'cirugias-catalogo';

export function useCirugiasCatalogo(profesionalId?: string, includeInactive = false) {
  return useQuery<CirugiaCatalogo[], Error>({
    queryKey: [QUERY_KEY, profesionalId, includeInactive],
    queryFn: async () => {
      const { data } = await api.get('/cirugias-catalogo', {
        params: { ...(profesionalId ? { profesionalId } : {}) },
      });
      return data;
    },
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos catalogo-hc + hook useCatalogoHC</name>
  <files>frontend/src/types/catalogo-hc.ts, frontend/src/hooks/useCatalogoHC.ts</files>
  <action>
1. Crear `frontend/src/types/catalogo-hc.ts` con las interfaces `ZonaHC`, `DiagnosticoHC`, `TratamientoHC` exactamente como el contrato del bloque interfaces (exportadas).

2. Crear `frontend/src/hooks/useCatalogoHC.ts`:

```typescript
import { useQuery } from '@tanstack/react-query';
import { api } from '@/lib/api';
import { ZonaHC } from '@/types/catalogo-hc';

export const CATALOGO_HC_QUERY_KEY = 'catalogo-hc';

export function useCatalogoHC(profesionalId?: string, options?: { enabled?: boolean }) {
  return useQuery<ZonaHC[], Error>({
    queryKey: [CATALOGO_HC_QUERY_KEY, profesionalId],
    queryFn: async () => {
      const { data } = await api.get('/catalogo-hc', {
        params: profesionalId ? { profesionalId } : {},
      });
      return data;
    },
    enabled: options?.enabled ?? true,
  });
}
```

Notas:
- Exportar `CATALOGO_HC_QUERY_KEY` como constante — Phase 46/47 invalidarán por key prefix (patrón onSettled del repo)
- El parámetro `enabled` permite que consumidores SECRETARIA/ADMIN esperen a tener profesionalId del professional context antes de disparar (pitfall: sin profesionalId el backend lanza ForbiddenException para esos roles). PROFESIONAL puede llamar sin profesionalId (se resuelve por JWT)
- Solo query GET en esta fase — las mutaciones (renombrar/eliminar) son de Phase 47, no crearlas ahora
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>tsc --noEmit pasa; useCatalogoHC y tipos exportados conforme al contrato; query key exportado para invalidación futura</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` sin errores
- `cd frontend && npm run lint` sin errores nuevos
- El hook sigue el patrón del repo (axios instance de lib/api.ts, query key con profesionalId)
- No se modificó PrimeraConsultaForm ni zonas-diagnostico.ts (eso es Phase 45)
</verification>

<success_criteria>
- Capa de acceso frontend al catálogo lista para Phase 45: tipos + hook compilando contra el contrato del GET /catalogo-hc
- Ningún consumidor actual roto (archivos nuevos únicamente)
</success_criteria>

<output>
After completion, create `.planning/phases/44-schema-cat-logo-en-bd/44-03-SUMMARY.md`
</output>
