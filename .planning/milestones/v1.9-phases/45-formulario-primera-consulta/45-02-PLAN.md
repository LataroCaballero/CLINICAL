---
phase: 45-formulario-primera-consulta
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx
  - frontend/src/components/live-turno/tabs/hc/HCCreatorForm.tsx
  - frontend/src/app/dashboard/components/TurnoHCModal.tsx
  - frontend/src/hooks/useCreateHistoriaClinicaEntry.ts
autonomous: true
requirements: [FORM-01, FORM-02, FORM-04]

must_haves:
  truths:
    - "Al abrir Primera Consulta el profesional ve primero los chips de zonas (Abdomen, Mamas, Nariz, Facial, Locales, Otros) desde el catálogo BD, sin diagnósticos ni tratamientos visibles"
    - "Seleccionar una zona despliega su grupo de diagnósticos y su grupo de tratamientos; las zonas no seleccionadas no muestran nada"
    - "Con 2+ zonas seleccionadas, cada grupo lleva la etiqueta de su zona visible encima"
    - "Seleccionar un tratamiento resuelve su precio desde el catálogo y Generar Presupuesto arma los items con esos precios"
  artifacts:
    - path: "frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx"
      provides: "Formulario rediseñado con zona como eje, consumiendo useCatalogoHC"
      contains: "useCatalogoHC"
      min_lines: 200
    - path: "frontend/src/hooks/useCreateHistoriaClinicaEntry.ts"
      provides: "ZonaSeleccionDto + campo zonas en CreateEntradaDto"
      contains: "zonas?: ZonaSeleccionDto[]"
  key_links:
    - from: "frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx"
      to: "frontend/src/hooks/useCatalogoHC.ts"
      via: "useCatalogoHC(profesionalId)"
      pattern: "useCatalogoHC\\("
    - from: "frontend/src/components/live-turno/tabs/hc/HCCreatorForm.tsx"
      to: "POST /pacientes/:id/historia-clinica/entradas"
      via: "createEntry.mutateAsync con dto.zonas"
      pattern: "zonas: pvState"
    - from: "frontend/src/app/dashboard/components/TurnoHCModal.tsx"
      to: "PrimeraConsultaForm"
      via: "prop profesionalId desde useEffectiveProfessionalId"
      pattern: "useEffectiveProfessionalId"
---

<objective>
Rediseñar PrimeraConsultaForm con la zona como eje: chips de zonas primero (desde el catálogo BD de Phase 44), grupos desplegables de diagnósticos y tratamientos por zona seleccionada con etiqueta de zona visible, lookup de precios preservado; y cablear los dos consumidores (HCCreatorForm y TurnoHCModal) al nuevo estado y al DTO `zonas[]`.

Purpose: FORM-01/02 (UX zona-céntrica) y FORM-04 (precios + presupuesto intactos). Es el corazón del milestone v1.9.
Output: Formulario nuevo funcionando en LiveTurno, PatientDrawer y TurnoHCModal; build de frontend verde.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx
@frontend/src/components/live-turno/tabs/hc/HCCreatorForm.tsx
@frontend/src/app/dashboard/components/TurnoHCModal.tsx
@frontend/src/hooks/useCreateHistoriaClinicaEntry.ts
@frontend/src/hooks/useCatalogoHC.ts
@frontend/src/types/catalogo-hc.ts

<interfaces>
<!-- Contratos existentes (Phase 44) — usar directamente, no explorar. -->

From frontend/src/types/catalogo-hc.ts:
```typescript
export interface DiagnosticoHC { id: string; nombre: string; orden: number; esSistema: boolean; }
export interface TratamientoHC {
  id: string; nombre: string; orden: number; esSistema: boolean;
  tratamientoId: string | null; // FK al catálogo de precios (null = sin match)
  precio: number | null;        // precio resuelto por join en el backend
}
export interface ZonaHC {
  id: string; nombre: string; orden: number; esSistema: boolean;
  diagnosticos: DiagnosticoHC[];
  tratamientos: TratamientoHC[];
}
```

From frontend/src/hooks/useCatalogoHC.ts:
```typescript
export const CATALOGO_HC_QUERY_KEY = 'catalogo-hc';
export function useCatalogoHC(profesionalId?: string, options?: { enabled?: boolean }): UseQueryResult<ZonaHC[], Error>;
// GET /catalogo-hc — PROFESIONAL resuelve por JWT; SECRETARIA/ADMIN requieren profesionalId (sin él → 403)
```

From frontend/src/hooks/useEffectiveProfessionalId.ts:
```typescript
export function useEffectiveProfessionalId(): string | null;
// PROFESIONAL → su propio id; ADMIN/SECRETARIA → selectedProfessionalId del store (puede ser null)
```

Contrato NUEVO compartido del milestone (idéntico al backend en 45-01):
```typescript
// En useCreateHistoriaClinicaEntry.ts
export interface ZonaSeleccionDto {
  zonaId: string;        // ZonaHC.id
  zona: string;          // nombre snapshot, ej. "Abdomen"
  diagnosticos: string[]; // nombres de diagnósticos seleccionados
  otroTexto?: string;    // texto libre cuando la zona es "Otros"
  tratamientos: TratamientoItemDto[]; // { nombre, tratamientoId?, precio } — tipo ya existente
}
// CreateEntradaDto gana: zonas?: ZonaSeleccionDto[]  (legacy diagnostico/tratamientos se mantienen)
```

Nuevo estado del formulario (export desde PrimeraConsultaForm.tsx):
```typescript
export interface PrimeraConsultaFormState {
  zonas: ZonaSeleccionDto[];
  comentario: string;
  autorizacion: AutorizacionFormState | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rediseñar PrimeraConsultaForm con zona como eje</name>
  <files>frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx, frontend/src/hooks/useCreateHistoriaClinicaEntry.ts</files>
  <action>
    **useCreateHistoriaClinicaEntry.ts** (primero — define el contrato): agregar `export interface ZonaSeleccionDto` (ver interfaces) y `zonas?: ZonaSeleccionDto[]` a CreateEntradaDto. NO eliminar `diagnostico`/`tratamientos` legacy (LiveTurnoFooter y live-turno.store los siguen usando — fuera de scope tocarlos; el backend mantiene fallback legacy).

    **PrimeraConsultaForm.tsx** — reescritura:
    - Props nuevas: `{ profesionalId?: string | null; onChange: (state: PrimeraConsultaFormState) => void; onGenerarPresupuesto: (items: PresupuestoItemInput[]) => void; obraSocialId?: string | null }`. Exportar el nuevo `PrimeraConsultaFormState { zonas: ZonaSeleccionDto[]; comentario: string; autorizacion: AutorizacionFormState | null }` (importar ZonaSeleccionDto desde useCreateHistoriaClinicaEntry).
    - Datos: `const { data: catalogo = [], isLoading } = useCatalogoHC(profesionalId ?? undefined, { enabled: !!profesionalId })`. Eliminar TODO import de `@/lib/zonas-diagnostico` (ZONAS, CATEGORIAS_TRATAMIENTO, getSubzonas, getTratamientosCategoria) y el mapa CATEGORIA_LABELS. Mantener `useTratamientosProfesional()` SOLO como fallback de precio (ver abajo).
    - Estado: `zonasSeleccionadas: ZonaSeleccionDto[]` + comentario + mostrarComentario + autorizacion (estos tres se mantienen como hoy). Eliminar subzonas/categorías.
    - **Render — FORM-01:** sección "Zonas" arriba con chips (componente Chip existente, reutilizar) para cada zona del catálogo ordenadas por `orden`. Debajo, por cada zona SELECCIONADA (en orden de `orden` del catálogo), un bloque de grupo: borde izquierdo/contenedor estilo `pl-2 border-l-2 border-blue-200` como los grupos actuales, con **etiqueta de la zona visible como encabezado del bloque** (ej. `<p className="text-xs font-semibold uppercase">{zona.nombre}</p>`) — siempre visible, lo que satisface FORM-02 con 2+ zonas. Dentro del bloque: sub-sección "Diagnósticos" (chips toggle de `zona.diagnosticos`, ordenados por `orden`) y sub-sección "Tratamientos" (botones chip con icono Plus cuando no agregados, estilo idéntico a los sub-tratamientos actuales, toggle add/remove). Zonas no seleccionadas: solo el chip, ningún contenido (colapsadas).
    - toggleZona(z: ZonaHC): agrega `{ zonaId: z.id, zona: z.nombre, diagnosticos: [], tratamientos: [] }` o quita la entrada completa (descarta sus selecciones).
    - toggleDiagnostico(zonaId, nombre): agrega/quita el nombre en `diagnosticos` de esa zona.
    - toggleTratamiento(zonaId, t: TratamientoHC) — **FORM-04 lookup de precio:** `const fallback = tratamientosProfesional.find(tp => tp.nombre.toLowerCase() === t.nombre.toLowerCase()); const item = { nombre: t.nombre, tratamientoId: t.tratamientoId ?? fallback?.id, precio: t.precio ?? fallback?.precio ?? 0 }`. El catálogo ya resuelve precio por join backend; el fallback cubre items sin FK.
    - Zona "Otros" (chip seleccionado cuyo nombre === 'Otros'): mostrar `<Input placeholder="Describir zona..." />` dentro de su bloque, guardando en `otroTexto` de esa entrada de zona (paridad con comportamiento actual). Los items "Otros" de diagnósticos/tratamientos son chips seleccionables normales — los inputs de texto para ellos llegan en Phase 46 (NO implementarlos acá).
    - emitChange en cada mutación: `onChange({ zonas, comentario, autorizacion })`. Mantener comentario (botón "Agregar comentario" + Textarea) y `<AutorizacionCodigosForm>` exactamente como hoy. Mantener la prop `onGenerarPresupuesto` (la usan los consumidores); eliminar el handler interno muerto `handleGenerarPresupuesto` si queda sin uso.
    - Loading: si `isLoading`, mostrar loader pequeño (Loader2 spin) en lugar de los chips de zonas; si catálogo vacío y no loading, texto muted "Sin catálogo disponible".
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "HCCreatorForm\|TurnoHCModal" | head -20</automated>
  </verify>
  <done>PrimeraConsultaForm no importa zonas-diagnostico; renderiza zonas del catálogo primero; grupos por zona con etiqueta; precio resuelto desde catálogo con fallback. (Errores de tipos en consumidores se resuelven en Task 2.)</done>
</task>

<task type="auto">
  <name>Task 2: Cablear HCCreatorForm y TurnoHCModal al nuevo estado</name>
  <files>frontend/src/components/live-turno/tabs/hc/HCCreatorForm.tsx, frontend/src/app/dashboard/components/TurnoHCModal.tsx</files>
  <action>
    **HCCreatorForm.tsx:**
    - Pasar `profesionalId={profesionalId}` (prop ya existente y requerida) a `<PrimeraConsultaForm>`.
    - canSave para primera_vez: `pvState !== null && pvState.zonas.length > 0` (antes: diagnostico.zonas.length || tratamientos.length).
    - handleSave primera_vez: en el dto reemplazar `diagnostico: pvState.diagnostico, tratamientos: pvState.tratamientos` por `zonas: pvState.zonas`. El resto (tipoEntrada, comentario, presupuestoId/Total, autorizaciones, fecha) sin cambios.
    - Footer "Generar Presupuesto": calcular `const tratamientosPlanos = pvState?.zonas.flatMap(z => z.tratamientos) ?? []`; mostrar el botón cuando `tratamientosPlanos.length > 0` y armar items `{ descripcion: t.nombre, precioTotal: t.precio }` desde la lista aplanada. Comportamiento y modal GenerarPresupuestoModal sin cambios (FORM-04).

    **TurnoHCModal.tsx (solo lado creador — el renderer de historial se toca en plan 45-03):**
    - `const efectivoProfesionalId = useEffectiveProfessionalId();` (import desde `@/hooks/useEffectiveProfessionalId`) y pasar `profesionalId={efectivoProfesionalId}` a `<PrimeraConsultaForm>`. OJO: el hook debe llamarse arriba del early return `if (!turno) return null` (regla de hooks).
    - canSave primera_vez: `pvState !== null && pvState.zonas.length > 0` (línea ~115-118).
    - handleSave primera_vez: `zonas: pvState.zonas` en lugar de `diagnostico`/`tratamientos` (líneas ~132-133).

    NO tocar LiveTurnoFooter.tsx ni live-turno.store.ts — el path de auto-guardado de borrador usa la forma legacy que el backend sigue aceptando.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>
  <done>Frontend compila y buildea verde; ambos consumidores envían `zonas[]`; botón Generar Presupuesto funciona con tratamientos aplanados de todas las zonas.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — 0 errores
- `cd frontend && npm run build` — build verde
- `rg "zonas-diagnostico" frontend/src/components frontend/src/app` — sin matches (el lib se borra en 45-03 cuando se confirme que no quedan imports)
</verification>

<success_criteria>
- Plantilla Primera Consulta muestra zonas primero, desde GET /catalogo-hc (FORM-01)
- Grupos de diagnósticos/tratamientos por zona con etiqueta visible; zonas no seleccionadas colapsadas (FORM-01/FORM-02)
- Precio resuelto desde catálogo (con fallback a tratamientosProfesional) y flujo Generar Presupuesto intacto (FORM-04)
- HCCreatorForm (LiveTurno + PatientDrawer) y TurnoHCModal envían el DTO con `zonas[]`
</success_criteria>

<output>
After completion, create `.planning/phases/45-formulario-primera-consulta/45-02-SUMMARY.md`
</output>
