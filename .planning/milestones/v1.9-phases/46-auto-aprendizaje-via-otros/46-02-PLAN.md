---
phase: 46-auto-aprendizaje-via-otros
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - backend/src/modules/catalogo-hc/catalogo-hc.service.ts
  - backend/src/modules/catalogo-hc/catalogo-hc.aprendizaje.service.spec.ts
  - backend/src/modules/historia-clinica/historia-clinica.service.ts
  - backend/src/modules/historia-clinica/historia-clinica.module.ts
autonomous: true
requirements: [APR-01, APR-02, APR-03, APR-04]

must_haves:
  truths:
    - "Guardar una HC primera_vez con zonas[] persiste en BD las zonas/diagnósticos/tratamientos nuevos seleccionados, atribuidos al profesionalId efectivo de la entrada"
    - "Un tratamiento aprendido sin match en el catálogo de precios crea un Tratamiento con precio 0 y queda vinculado vía FK tratamientoId"
    - "Un tratamiento aprendido CON match insensible en el catálogo de precios se vincula al Tratamiento existente sin crear duplicado"
    - "Si el aprendizaje falla, crearEntrada igual devuelve la entrada creada (best-effort, warning logueado)"
    - "El shape legacy (sin zonas[]) no dispara aprendizaje"
  artifacts:
    - path: "backend/src/modules/catalogo-hc/catalogo-hc.service.ts"
      provides: "aprenderDesdeZonas(profesionalId, zonas) aplicando AccionesAprendizaje con Prisma"
      contains: "aprenderDesdeZonas"
    - path: "backend/src/modules/catalogo-hc/catalogo-hc.aprendizaje.service.spec.ts"
      provides: "Tests de aprenderDesdeZonas con PrismaService mockeado"
      min_lines: 80
    - path: "backend/src/modules/historia-clinica/historia-clinica.service.ts"
      provides: "Llamada best-effort al aprendizaje tras crear la entrada primera_vez"
      contains: "aprenderDesdeZonas"
  key_links:
    - from: "backend/src/modules/historia-clinica/historia-clinica.service.ts"
      to: "backend/src/modules/catalogo-hc/catalogo-hc.service.ts"
      via: "inyección de CatalogoHCService + try/catch alrededor de aprenderDesdeZonas"
      pattern: "aprenderDesdeZonas"
    - from: "backend/src/modules/historia-clinica/historia-clinica.module.ts"
      to: "backend/src/modules/catalogo-hc/catalogo-hc.module.ts"
      via: "imports: [CatalogoHCModule]"
      pattern: "CatalogoHCModule"
    - from: "backend/src/modules/catalogo-hc/catalogo-hc.service.ts"
      to: "prisma.tratamiento"
      via: "match normalizado contra catálogo de precios + create precio 0 (APR-04)"
      pattern: "prisma\\.tratamiento\\.(findMany|create)"
---

<objective>
Aplicar el aprendizaje en BD: `CatalogoHCService.aprenderDesdeZonas()` carga el snapshot del catálogo (incluyendo inactivos), corre `detectarAprendizaje` (46-01) y ejecuta las acciones — crear zonas vía `crearZona()`, crear/reactivar diagnósticos y tratamientos, y para cada tratamiento nuevo resolver el FK al catálogo de precios (match insensible o creación de Tratamiento a precio 0). Luego, cablear la llamada best-effort en `historia-clinica.service.crearEntrada()`.

Purpose: cierra APR-01/02/03/04 en el backend — el catálogo aprende al guardar la HC, sin bloquear nunca el guardado.
Output: aprendizaje funcionando end-to-end en el backend con tests de servicio.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-auto-aprendizaje-via-otros/46-CONTEXT.md
@.planning/phases/46-auto-aprendizaje-via-otros/46-01-SUMMARY.md
@backend/src/modules/catalogo-hc/catalogo-hc.service.ts
@backend/src/modules/catalogo-hc/catalogo-hc.aprendizaje.helpers.ts
@backend/src/modules/historia-clinica/historia-clinica.service.ts
@backend/src/modules/historia-clinica/historia-clinica.module.ts

<interfaces>
<!-- Contratos clave. Usar directamente, sin exploración. -->

From backend/src/modules/catalogo-hc/catalogo-hc.aprendizaje.helpers.ts (creado en 46-01):
```typescript
export interface SnapshotItem { id: string; nombre: string; activo: boolean }
export interface SnapshotZona extends SnapshotItem { diagnosticos: SnapshotItem[]; tratamientos: SnapshotItem[] }
export interface ZonaAprendizajeInput { zona: string; diagnosticos: string[]; tratamientos: { nombre: string }[] }
export interface AccionesAprendizaje {
  zonasACrear: string[];
  zonasAReactivar: string[];
  diagnosticosACrear: { zonaNombre: string; nombre: string }[];
  diagnosticosAReactivar: string[];
  tratamientosACrear: { zonaNombre: string; nombre: string }[];
  tratamientosAReactivar: string[];
}
export function detectarAprendizaje(catalogo: SnapshotZona[], zonas: ZonaAprendizajeInput[]): AccionesAprendizaje;
```

From backend/src/modules/catalogo-hc/catalogo-hc.service.ts (existente):
```typescript
// Idempotente; crea ZonaHC + DiagnosticoHC "Otros" + TratamientoHC "Otros" en $transaction.
// Diseñado en Phase 44 para que APR-01 lo reutilice.
async crearZona(profesionalId: string, nombre: string, orden: number, esSistema = false): Promise<ZonaHC>;
```

From backend/src/modules/catalogo-hc/catalogo-hc.seed-data.ts:
```typescript
export function normalizarNombre(nombre: string): string;
```

From backend/src/modules/historia-clinica/historia-clinica.service.ts (punto de enganche, ~línea 73):
```typescript
async crearEntrada(pacienteId: string, dto: CreateEntradaDto, profesionalIdFromJwt?: string)
// resuelve profesionalId al inicio (JWT o findFirst fallback);
// dto.tipo === 'primera_vez' && dto.zonas?.length > 0 → shape nuevo (el único que aprende)
```

Modelos Prisma:
- ZonaHC @@unique([nombre, profesionalId]); DiagnosticoHC/TratamientoHC @@unique([nombre, zonaId]); todos con activo, esSistema, orden ("Otros" = 9999)
- TratamientoHC.tratamientoId String? → Tratamiento (ON DELETE SET NULL)
- Tratamiento: { nombre, precio Decimal @default(0), profesionalId, activo } @@unique([nombre, profesionalId])
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CatalogoHCService.aprenderDesdeZonas con tests (mock Prisma)</name>
  <files>backend/src/modules/catalogo-hc/catalogo-hc.service.ts, backend/src/modules/catalogo-hc/catalogo-hc.aprendizaje.service.spec.ts</files>
  <behavior>
    Tests con PrismaService mockeado (patrón de los *.service.spec.ts existentes del repo, ej. afip-config.service.spec.ts):
    - Test 1: zona nueva en input → llama crearZona con orden = max(orden de zonas no-sistema) + 1 (con catálogo seed: 6)
    - Test 2: dx nuevo en zona existente → diagnosticoHC.create con zonaId correcto, profesionalId, esSistema: false, orden = max orden no-sistema de esa zona + 1
    - Test 3 (APR-04 sin match): tx nuevo sin match en prisma.tratamiento → tratamiento.create({ nombre formateado, precio: 0, profesionalId }) y tratamientoHC.create con tratamientoId del recién creado
    - Test 4 (APR-04 con match): tx nuevo "rinoplastia" con Tratamiento "Rinoplastia" existente → NO crea Tratamiento; tratamientoHC.create con tratamientoId del existente
    - Test 5: ítems a reactivar → updateMany({ where: { id: { in: ids } }, data: { activo: true } }) en el modelo correspondiente; no se crea nada
    - Test 6: input sin novedades (todo matchea activo) → ninguna escritura
    - Test 7: dx/tx de zona nueva → se crean colgando del id devuelto por crearZona
  </behavior>
  <action>
    Agregar a CatalogoHCService:

    `async aprenderDesdeZonas(profesionalId: string, zonas: ZonaAprendizajeInput[]): Promise<void>`

    Orden de operaciones (escrituras secuenciales fuera de transacción larga — patrón pgBouncer del repo; crearZona ya es transaccional internamente):
    1. Snapshot: `prisma.zonaHC.findMany({ where: { profesionalId }, include: { diagnosticos: true, tratamientos: true } })` — SIN filtrar activo (la detección necesita inactivos para reactivar). Mapear a SnapshotZona[].
    2. `const acciones = detectarAprendizaje(snapshot, zonas)` — early return si todas las listas están vacías.
    3. Reactivaciones: zonaHC/diagnosticoHC/tratamientoHC.updateMany con { activo: true } por lista de ids (solo si la lista no está vacía).
    4. Zonas nuevas: para cada nombre, `this.crearZona(profesionalId, nombre, ordenSiguiente++)` donde ordenSiguiente arranca en max(orden de zonas no-sistema del snapshot, 0) + 1. Guardar nombre→zona.id en un map junto con las zonas del snapshot (key: normalizarNombre(nombre)).
    5. Diagnósticos nuevos: resolver zonaId vía el map por zonaNombre normalizado; `diagnosticoHC.create({ nombre, orden, esSistema: false, zonaId, profesionalId })` con orden = max orden no-sistema de los dx de esa zona + 1 (para zonas nuevas: 1), incrementando si hay varios en la misma zona.
    6. Tratamientos nuevos (APR-04): cargar UNA vez `prisma.tratamiento.findMany({ where: { profesionalId }, select: { id, nombre } })` y armar matchMap con normalizarNombre (mismo patrón que seedCatalogoInicial). Para cada tx nuevo: si hay match → usar ese id como tratamientoId; si no → `prisma.tratamiento.create({ data: { nombre, precio: 0, profesionalId } })`, agregar al matchMap (evita duplicado si el mismo tx nuevo aparece en dos zonas) y usar su id. Luego `tratamientoHC.create({ nombre, orden, esSistema: false, zonaId, profesionalId, tratamientoId })`.

    No envolver en try/catch acá — el best-effort lo maneja el caller (Task 2). Importar tipos y detectarAprendizaje desde './catalogo-hc.aprendizaje.helpers'.

    Escribir el spec primero (RED), luego implementar (GREEN) — commits test → feat.
  </action>
  <verify>
    <automated>cd backend && npx jest catalogo-hc --silent</automated>
  </verify>
  <done>Los 7 tests pasan; suite catalogo-hc completa en verde; aprenderDesdeZonas exportado en CatalogoHCService.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring best-effort en historia-clinica.crearEntrada</name>
  <files>backend/src/modules/historia-clinica/historia-clinica.service.ts, backend/src/modules/historia-clinica/historia-clinica.module.ts</files>
  <action>
    1. `historia-clinica.module.ts`: agregar `imports: [CatalogoHCModule]` (CatalogoHCModule ya exporta CatalogoHCService; sin dependencia circular — CatalogoHCModule no importa nada).
    2. `historia-clinica.service.ts`:
       - Inyectar `private catalogoHc: CatalogoHCService` en el constructor; agregar `private readonly logger = new Logger(HistoriaClinicaService.name)` (el servicio hoy no tiene logger).
       - En `crearEntrada`, DESPUÉS de que la transacción de creación de la entrada haya commiteado exitosamente y ANTES del return final:
         ```typescript
         if (dto.tipo === 'primera_vez' && Array.isArray(dto.zonas) && dto.zonas.length > 0) {
           try {
             await this.catalogoHc.aprenderDesdeZonas(
               profesionalId,
               dto.zonas.map((z) => ({
                 zona: z.zona,
                 diagnosticos: z.diagnosticos ?? [],
                 tratamientos: (z.tratamientos ?? []).map((t) => ({ nombre: t.nombre })),
               })),
             );
           } catch (e) {
             this.logger.warn(`Aprendizaje de catálogo HC falló (no bloqueante): ${e?.message ?? e}`);
           }
         }
         ```
       - Razones del diseño (NO cambiar): aprender después del commit garantiza que solo HC efectivamente guardadas enriquecen el catálogo; el shape legacy (sin zonas[]) NO aprende (decisión de CONTEXT); profesionalId es el ya resuelto al inicio de crearEntrada (atribución correcta cuando guarda una secretaria); el JSONB guarda nombres como snapshot — los zonaId temporales "nueva-*" de zonas nuevas quedan tal cual en contenido y es aceptable (los lectores de historial renderizan por nombre, no por id).
    3. NO tocar `construirContenidoPrimeraVez` ni el DTO — el aprendizaje es server-side por comparación de nombres, sin flags del cliente.
  </action>
  <verify>
    <automated>cd backend && npm run build && npx jest catalogo-hc historia-clinica --silent</automated>
  </verify>
  <done>Backend compila; tests existentes de historia-clinica y catalogo-hc en verde; crearEntrada llama aprenderDesdeZonas dentro de try/catch solo en el path primera_vez + zonas[].</done>
</task>

</tasks>

<verification>
- `cd backend && npm run build` — sin errores de tipos
- `cd backend && npx jest catalogo-hc historia-clinica --silent` — todo verde
- `rg -n "aprenderDesdeZonas" backend/src/modules/historia-clinica/historia-clinica.service.ts` — wiring presente dentro de try/catch
- `rg -n "CatalogoHCModule" backend/src/modules/historia-clinica/historia-clinica.module.ts` — import presente
</verification>

<success_criteria>
- POST de una HC primera_vez con zonas[] que incluye nombres nuevos deja en BD: ZonaHC nueva (con sus "Otros" hijos vía crearZona), DiagnosticoHC/TratamientoHC nuevos en sus zonas, y Tratamiento precio 0 vinculado por FK cuando no hay match de precios
- Nombres existentes (insensible a mayúsculas/tildes) se reutilizan o reactivan — cero duplicados
- Un fallo del aprendizaje nunca impide el guardado de la HC
</success_criteria>

<output>
After completion, create `.planning/phases/46-auto-aprendizaje-via-otros/46-02-SUMMARY.md`
</output>
