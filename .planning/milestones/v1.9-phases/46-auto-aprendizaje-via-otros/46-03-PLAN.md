---
phase: 46-auto-aprendizaje-via-otros
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx
  - frontend/src/hooks/useCreateHistoriaClinicaEntry.ts
autonomous: true
requirements: [APR-01, APR-02, APR-03]

must_haves:
  truths:
    - "Enter en el input de la zona 'Otros' convierte el texto en una zona completa al instante: chip seleccionado con grupos Diagnósticos/Tratamientos (solo 'Otros' como opción), usable en la misma consulta"
    - "Click en el chip 'Otros' de diagnósticos o tratamientos de cualquier zona despliega un input debajo; Enter convierte el texto en chip seleccionado y deja el input libre para otro ítem"
    - "Los chips nuevos (zona/dx/tx) tienen distinción visual de borde punteado y pueden deseleccionarse antes de guardar"
    - "Un tratamiento nuevo entra con precio del catálogo de precios si hay match por nombre, o precio 0 editable en el flujo de presupuesto"
    - "Al guardar la HC se invalida la query 'catalogo-hc' — el próximo formulario trae el catálogo actualizado"
  artifacts:
    - path: "frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx"
      provides: "UX Enter→chip para zona/dx/tx nuevos con distinción visual"
      contains: "border-dashed"
    - path: "frontend/src/hooks/useCreateHistoriaClinicaEntry.ts"
      provides: "Invalidación de catalogo-hc en onSuccess"
      contains: "CATALOGO_HC_QUERY_KEY"
  key_links:
    - from: "frontend/src/hooks/useCreateHistoriaClinicaEntry.ts"
      to: "frontend/src/hooks/useCatalogoHC.ts"
      via: "invalidateQueries con prefijo CATALOGO_HC_QUERY_KEY"
      pattern: "invalidateQueries.*CATALOGO_HC_QUERY_KEY|CATALOGO_HC_QUERY_KEY.*invalidateQueries"
    - from: "frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx"
      to: "useTratamientosProfesional"
      via: "lookup de precio por nombre para tratamientos nuevos"
      pattern: "tratamientosProfesional"
---

<objective>
Implementar la captura de ítems nuevos en PrimeraConsultaForm: zona nueva instantánea desde el input de "Otros" (Enter→zona completa client-side), inputs Enter→chip para diagnósticos y tratamientos "Otros" en cualquier zona, distinción visual de chips nuevos, y la invalidación de useCatalogoHC al guardar.

Purpose: es la mitad frontend de APR-01/02/03 — lo que el profesional escribe viaja como selección normal en zonas[] (el backend detecta lo nuevo por nombre, sin flags en el DTO), y la próxima consulta ya muestra el catálogo enriquecido.
Output: PrimeraConsultaForm con UX Enter→chip + invalidación de catálogo.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-auto-aprendizaje-via-otros/46-CONTEXT.md
@frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx
@frontend/src/hooks/useCreateHistoriaClinicaEntry.ts
@frontend/src/hooks/useCatalogoHC.ts
@frontend/src/types/catalogo-hc.ts

<interfaces>
<!-- Contratos existentes. Usar directamente. -->

From frontend/src/types/catalogo-hc.ts:
```typescript
export interface DiagnosticoHC { id: string; nombre: string; orden: number; esSistema: boolean }
export interface TratamientoHC { id: string; nombre: string; orden: number; esSistema: boolean; tratamientoId: string | null; precio: number | null }
export interface ZonaHC { id: string; nombre: string; orden: number; esSistema: boolean; diagnosticos: DiagnosticoHC[]; tratamientos: TratamientoHC[] }
```

From frontend/src/hooks/useCatalogoHC.ts:
```typescript
export const CATALOGO_HC_QUERY_KEY = 'catalogo-hc';
// queryKey: [CATALOGO_HC_QUERY_KEY, profesionalId]
```

From frontend/src/hooks/useCreateHistoriaClinicaEntry.ts:
```typescript
export interface TratamientoItemDto { nombre: string; tratamientoId?: string; precio: number }
export interface ZonaSeleccionDto { zonaId: string; zona: string; diagnosticos: string[]; otroTexto?: string; tratamientos: TratamientoItemDto[] }
// useCreateHistoriaClinicaEntry: useMutation con onSuccess que ya invalida historia-clinica, paciente, pacientes, contactos, crm-kanban, autorizaciones, turnos/rango
```

Estado actual del form (PrimeraConsultaForm.tsx):
- `zonasSeleccionadas: ZonaSeleccionDto[]` + `emitChange` hacia el padre
- Zona "Otros" (detectada por `zona.nombre.toLowerCase() === 'otros'`) muestra `<Input>` que escribe `otroTexto` vía `handleOtroTextoChange`
- Dx: chips toggle de strings; Tx: botones que arman TratamientoItemDto con fallback de precio en `tratamientosProfesional` (match lowercase por nombre)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UX Enter→chip en PrimeraConsultaForm (zona nueva + dx/tx nuevos)</name>
  <files>frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx</files>
  <action>
    Helper local (espejo de la convención backend): `const formatearNombre = (raw: string) => { const t = raw.trim(); return t.charAt(0).toUpperCase() + t.slice(1); }`.

    **A. Zona nueva instantánea (APR-01):**
    - Nuevo estado `zonasNuevas: ZonaHC[]` — zonas creadas client-side con id temporal `` `nueva-${Date.now()}-${i}` ``, `esSistema: false`, `diagnosticos: []`, `tratamientos: []` (el grupo renderiza la opción "Otros" sintética, ver C).
    - El input de la zona "Otros" cambia de comportamiento: en `onKeyDown` Enter con texto no vacío → `formatearNombre(texto)`; si ese nombre ya existe (lowercase/trim) en `catalogo` o `zonasNuevas`, seleccionar la existente en vez de duplicar; si no, push a `zonasNuevas`, agregar `{ zonaId: tempId, zona: nombre, diagnosticos: [], tratamientos: [] }` a `zonasSeleccionadas` (queda seleccionada al instante), limpiar el input (queda libre para otra zona). `e.preventDefault()` en Enter para no submitear forms ancestros.
    - Render: merge de `zonasNuevas` en `zonasOrdenadas` (insertarlas antes de la zona "Otros" sistema). Chip de zona nueva con estilo punteado (ver D). Deseleccionar una zona nueva la saca de `zonasSeleccionadas` (su grupo desaparece; el chip punteado puede permanecer visible deseleccionado).
    - El path `otroTexto`/`handleOtroTextoChange` para la zona "Otros" deja de emitirse al presionar Enter (el texto se consume al crear la zona); mantener `otroTexto` en el DTO sin uso nuevo (compatibilidad).

    **B. Diagnósticos nuevos (APR-02):**
    - El chip "Otros" de diagnósticos (ítem con `esSistema === true` o nombre normalizado 'otros') ya NO togglea el string "Otros" en `diagnosticos`: ahora alterna la visibilidad de un `<Input placeholder="Describir diagnóstico..." />` debajo del grupo (estado `dxInputAbierto: Record<zonaId, boolean>` + `dxInputTexto: Record<zonaId, string>`).
    - Enter con texto no vacío → `formatearNombre`; si el nombre ya existe en los dx de la zona (lowercase) → solo seleccionarlo; si no → agregarlo a `selState.diagnosticos` (seleccionado) y trackearlo en `dxNuevos: Record<zonaId, string[]>` para renderizarlo como chip punteado; limpiar el input y dejarlo abierto para cargar otro.
    - Render de chips dx: los del catálogo + los de `dxNuevos[zona.id]` (punteados, toggleables igual que los demás).

    **C. Tratamientos nuevos (APR-03):**
    - Mismo patrón que B con el chip "Otros" de tratamientos (`txInputAbierto`, `txInputTexto`, `txNuevos: Record<zonaId, TratamientoItemDto[]>`), placeholder "Describir tratamiento...".
    - Al crear: lookup de precio reutilizando el patrón existente — `tratamientosProfesional.find(tp => tp.nombre.toLowerCase() === nombre.toLowerCase())`; armar `TratamientoItemDto { nombre, tratamientoId: fallback?.id, precio: fallback?.precio ?? 0 }`, push a `selState.tratamientos` (seleccionado) y a `txNuevos[zonaId]`.
    - Las zonas nuevas (A) renderizan sus grupos Dx/Tx con SOLO la opción "Otros" sintética (objeto local `{ id: 'otros-' + zonaId, nombre: 'Otros', esSistema: true, orden: 9999 }`) — habilita cargar dx/tx nuevos de esa zona en la misma consulta. No renderizar los headers "Diagnósticos"/"Tratamientos" condicionados a `length > 0` del catálogo para estas zonas: siempre mostrar ambos grupos con el chip "Otros".

    **D. Distinción visual (decisión de CONTEXT, discreción resuelta: borde punteado):**
    - Chips nuevos (zona/dx/tx): agregar `border-dashed` manteniendo el resto del estilo de selección. Ej. seleccionado: `bg-blue-600 text-white border-2 border-dashed border-blue-300`; deseleccionado: `border-2 border-dashed border-gray-400`. Extender el componente `Chip` con prop opcional `dashed?: boolean` en lugar de duplicar markup.

    Restricciones: NO cambiar `ZonaSeleccionDto` ni el payload (la detección de lo nuevo es server-side por nombre); NO tocar HCCreatorForm/TurnoHCModal (el contrato `onChange(PrimeraConsultaFormState)` no cambia); preservar intactos comentario, autorización y el flujo de presupuesto existente.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint -- --quiet</automated>
  </verify>
  <done>tsc sin errores; lint limpio; el form permite crear zona nueva por Enter, dx/tx nuevos por Enter en cualquier zona (incluidas zonas nuevas), con chips punteados deseleccionables y precio resuelto por lookup o 0.</done>
</task>

<task type="auto">
  <name>Task 2: Invalidación de catalogo-hc al guardar la HC</name>
  <files>frontend/src/hooks/useCreateHistoriaClinicaEntry.ts</files>
  <action>
    En `useCreateHistoriaClinicaEntry`, importar `CATALOGO_HC_QUERY_KEY` desde `@/hooks/useCatalogoHC` y agregar al `onSuccess` existente:
    `qc.invalidateQueries({ queryKey: [CATALOGO_HC_QUERY_KEY] });`
    (prefijo sin profesionalId — patrón de invalidación por key prefix del repo; cubre cualquier profesional cacheado). Sin toast ni feedback extra: el aprendizaje es silencioso por decisión de CONTEXT.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && rg -n "CATALOGO_HC_QUERY_KEY" src/hooks/useCreateHistoriaClinicaEntry.ts</automated>
  </verify>
  <done>onSuccess invalida ['catalogo-hc']; tsc sin errores.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — 0 errores (nota: `npm run build` falla por Node 18 vs Next.js >= 20.9 — preexistente del entorno, NO es criterio de este plan; usar tsc)
- `rg -n "border-dashed" frontend/src/components/live-turno/tabs/hc/PrimeraConsultaForm.tsx` — distinción visual presente
- `rg -n "CATALOGO_HC_QUERY_KEY" frontend/src/hooks/useCreateHistoriaClinicaEntry.ts` — invalidación cableada
</verification>

<success_criteria>
- Zona "Otros" + Enter crea zona completa seleccionada al instante con grupos Dx/Tx (solo "Otros"), usable en la misma consulta
- Chip "Otros" de dx/tx despliega input; Enter agrega chip seleccionado punteado y permite cargar múltiples ítems
- Deseleccionar un chip nuevo antes de guardar lo excluye del payload (no se aprende — decisión "solo se aprende lo seleccionado")
- El payload sigue siendo ZonaSeleccionDto[] sin cambios de shape; al guardar se invalida la query catalogo-hc
</success_criteria>

<output>
After completion, create `.planning/phases/46-auto-aprendizaje-via-otros/46-03-SUMMARY.md`
</output>
