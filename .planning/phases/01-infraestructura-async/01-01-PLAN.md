---
phase: 01-infraestructura-async
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/src/app.module.ts
  - backend/src/modules/whatsapp/whatsapp.module.ts
  - backend/src/modules/whatsapp/whatsapp.service.ts
  - backend/src/modules/whatsapp/whatsapp.controller.ts
  - backend/src/modules/whatsapp/processors/whatsapp-message.processor.ts
  - backend/src/modules/whatsapp/dto/save-waba-config.dto.ts
  - backend/src/modules/whatsapp/crypto/encryption.service.ts
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "Un job encolado en BullMQ se procesa (log visible en consola) y no bloquea la request HTTP"
    - "El módulo BullMQ arranca sin errores con NestJS v10"
    - "Ante fallo simulado, el job reintenta con backoff exponencial (3 intentos)"
  artifacts:
    - path: "backend/src/modules/whatsapp/whatsapp.module.ts"
      provides: "Módulo NestJS con BullMQ queue registrada"
      contains: "BullModule.registerQueue"
    - path: "backend/src/app.module.ts"
      provides: "BullModule.forRootAsync() registrado globalmente con Redis"
      contains: "BullModule.forRootAsync"
    - path: "backend/src/modules/whatsapp/processors/whatsapp-message.processor.ts"
      provides: "WorkerHost processor para la queue whatsapp-messages"
      contains: "WorkerHost"
  key_links:
    - from: "backend/src/app.module.ts"
      to: "Redis"
      via: "BullModule.forRootAsync con ConfigService"
      pattern: "BullModule\\.forRootAsync"
    - from: "backend/src/modules/whatsapp/whatsapp.module.ts"
      to: "backend/src/app.module.ts"
      via: "import en AppModule"
      pattern: "WhatsappModule"
---

<objective>
Instalar BullMQ + @nestjs/bullmq, crear el módulo WhatsApp con la queue `whatsapp-messages`, registrar el worker processor con WorkerHost pattern, y verificar con un smoke test que jobs se encolan y procesan correctamente en NestJS v10.

Purpose: La compatibilidad BullMQ v11 + NestJS v10 es una incógnita marcada en STATE.md como concern. Este plan valida la infraestructura async ANTES de construir lógica encima de ella.
Output: Módulo WhatsApp funcional con queue BullMQ operativa, smoke test verificado, AppModule actualizado con conexión Redis global.
</objective>

<execution_context>
@/Users/laucaballero/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infraestructura-async/01-RESEARCH.md

Codebase relevante:
@backend/src/app.module.ts
@backend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependencias BullMQ y crear módulo WhatsApp con queue + processor</name>
  <files>
    backend/package.json
    backend/src/modules/whatsapp/whatsapp.module.ts
    backend/src/modules/whatsapp/processors/whatsapp-message.processor.ts
    backend/src/modules/whatsapp/crypto/encryption.service.ts
    backend/src/modules/whatsapp/dto/save-waba-config.dto.ts
    backend/src/modules/whatsapp/whatsapp.service.ts
    backend/src/modules/whatsapp/whatsapp.controller.ts
    backend/src/app.module.ts
  </files>
  <action>
**1. Instalar paquetes (desde backend/):**
```bash
npm install --save @nestjs/bullmq bullmq
```

**2. Crear `backend/src/modules/whatsapp/crypto/encryption.service.ts`:**
Servicio AES-256-GCM usando Node.js built-in `crypto` (sin dependencias externas):
- Constructor: lee `ENCRYPTION_KEY` de ConfigService, valida que sea 64 hex chars, falla con error claro si no
- `encrypt(plaintext: string): string` — genera IV random de 12 bytes, cifra con aes-256-gcm, retorna `iv:authTag:ciphertext` en base64
- `decrypt(stored: string): string` — invierte el proceso
- Exportar como `@Injectable()` provider

**3. Crear `backend/src/modules/whatsapp/dto/save-waba-config.dto.ts`:**
```typescript
export class SaveWabaConfigDto {
  @IsString() @IsNotEmpty() phoneNumberId: string;
  @IsString() @IsNotEmpty() accessToken: string;
  @IsString() @IsOptional() wabaId?: string;
}
```

**4. Crear `backend/src/modules/whatsapp/processors/whatsapp-message.processor.ts`:**
Usar EXACTAMENTE el WorkerHost pattern del RESEARCH.md (NO usar `@Process()` decorator — no existe en @nestjs/bullmq):
```typescript
export const WHATSAPP_QUEUE = 'whatsapp-messages';

@Processor(WHATSAPP_QUEUE)
export class WhatsappMessageProcessor extends WorkerHost {
  private readonly logger = new Logger(WhatsappMessageProcessor.name);

  async process(job: Job): Promise<void> {
    switch (job.name) {
      case 'send-whatsapp-message':
        // TODO Phase 4: implementar envío real
        this.logger.log(`[Phase 1 smoke test] job procesado: ${JSON.stringify(job.data)}`);
        break;
      case 'test-job':
        this.logger.log(`[Smoke test] test-job procesado: ${JSON.stringify(job.data)}`);
        break;
      default:
        throw new Error(`Job desconocido: ${job.name}`);
    }
  }

  @OnWorkerEvent('failed')
  onFailed(job: Job) {
    this.logger.error(`Job ${job.id} falló: ${job.failedReason}`);
  }

  @OnWorkerEvent('completed')
  onCompleted(job: Job) {
    this.logger.log(`Job ${job.id} completado`);
  }
}
```

**5. Crear `backend/src/modules/whatsapp/whatsapp.service.ts`:**
- Inyectar `@InjectQueue(WHATSAPP_QUEUE) private readonly whatsappQueue: Queue`
- Método `async enqueueTestJob(): Promise<{ queued: boolean; jobId: string }>` que encola un `test-job`
- Método `async validateWABACredentials(phoneNumberId: string, accessToken: string)` — placeholder que lanza `NotImplementedException` (se implementa en Task 2 del próximo plan)

**6. Crear `backend/src/modules/whatsapp/whatsapp.controller.ts`:**
- Endpoint `POST /whatsapp/test-queue` con guard `@Roles('ADMIN')` — llama `whatsappService.enqueueTestJob()` y retorna el resultado
- Este endpoint es TEMPORAL para smoke test — agregar comentario `// TODO: remover antes de Phase 4 o mover a admin-only route`

**7. Crear `backend/src/modules/whatsapp/whatsapp.module.ts`:**
```typescript
@Module({
  imports: [
    BullModule.registerQueue({ name: WHATSAPP_QUEUE }),
  ],
  providers: [WhatsappService, WhatsappMessageProcessor, EncryptionService],
  controllers: [WhatsappController],
  exports: [BullModule, WhatsappService, EncryptionService],
})
export class WhatsappModule {}
```
Exportar `BullModule` para que otros módulos puedan inyectar la queue en Phase 4.

**8. Actualizar `backend/src/app.module.ts`:**
Agregar `BullModule.forRootAsync()` usando el patrón exacto del RESEARCH.md:
```typescript
BullModule.forRootAsync({
  inject: [ConfigService],
  useFactory: (config: ConfigService) => ({
    connection: {
      host: config.get('REDIS_HOST', 'localhost'),
      port: config.get<number>('REDIS_PORT', 6379),
      maxRetriesPerRequest: null, // CRÍTICO para workers — ver RESEARCH.md pitfall 4
    },
    defaultJobOptions: {
      attempts: 3,
      backoff: { type: 'exponential', delay: 2000 },
      removeOnComplete: 1000,
      removeOnFail: 5000,
    },
  }),
}),
```
Agregar `WhatsappModule` al array `imports`.

**IMPORTANTE:** El proyecto ya usa `ConfigModule.forRoot({ isGlobal: true })` en AppModule. NO agregar otro `ConfigModule.forRoot()`.

**IMPORTANTE:** Si `PacientesModule` tiene `ScheduleModule.forRoot()`, NO agregar otro en `AppModule` — verificar con grep antes.
  </action>
  <verify>
```bash
# Desde backend/
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend
npm run build 2>&1 | tail -20
```
Build debe completar sin errores de TypeScript.

Luego verificar que el módulo NestJS arranca (requiere Redis corriendo localmente o REDIS_HOST en .env):
```bash
# Solo si Redis está disponible — si no, el error de conexión es esperado y no bloquea
npm run start:dev 2>&1 | head -30
```
  </verify>
  <done>
- `npm run build` pasa sin errores TypeScript relacionados al módulo whatsapp
- Los archivos del módulo existen con el contenido correcto
- `AppModule` tiene `BullModule.forRootAsync()` y `WhatsappModule` importados
- `WhatsappMessageProcessor` extiende `WorkerHost` (NO usa @Process decorator)
- `EncryptionService` usa Node.js built-in `crypto` (no dependencias externas)
  </done>
</task>

<task type="auto">
  <name>Task 2: Smoke test — verificar compatibilidad BullMQ v11 + NestJS v10</name>
  <files>
    backend/src/modules/whatsapp/whatsapp.service.ts
  </files>
  <action>
Verificar la compatibilidad BullMQ v11 + NestJS v10 identificada como concern en STATE.md:

**1. Verificar versiones instaladas:**
```bash
cd backend && node -e "const b = require('./node_modules/bullmq/package.json'); const nb = require('./node_modules/@nestjs/bullmq/package.json'); console.log('bullmq:', b.version, '@nestjs/bullmq:', nb.version)"
```

**2. Verificar peer dependencies:**
```bash
node -e "const nb = require('./node_modules/@nestjs/bullmq/package.json'); console.log('peerDeps:', JSON.stringify(nb.peerDependencies, null, 2))"
```

**3. Si hay conflicto de peer dependencies** (ej: `@nestjs/bullmq` requiere NestJS v11 pero el proyecto tiene v10):
- Intentar primero con la versión actual
- Si falla en runtime con errores DI: hacer downgrade a `@nestjs/bullmq@10.x` con `npm install --save @nestjs/bullmq@10`
- Documentar la versión que funcionó en el SUMMARY

**4. Actualizar `whatsapp.service.ts` con método de smoke test completo:**
```typescript
async enqueueTestJob(): Promise<{ queued: boolean; jobId: string }> {
  const job = await this.whatsappQueue.add(
    'test-job',
    { test: true, timestamp: new Date().toISOString() },
    {
      attempts: 3,
      backoff: { type: 'exponential', delay: 1000 },
      removeOnComplete: false, // Mantener para inspección
    },
  );
  return { queued: true, jobId: job.id as string };
}
```

**5. Verificar el build final:**
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend
npm run build 2>&1 | grep -E "(error|Error|warning)" | head -20
```

**Resultado esperado para el SUMMARY:**
Documentar en el SUMMARY qué versión de `@nestjs/bullmq` se instaló y si hubo algún ajuste de compatibilidad necesario. Esto es crítico para Phase 4.
  </action>
  <verify>
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend
npm run build 2>&1 | tail -5
```
Build limpio. Si hay errores de TypeScript residuales del pre-existente `test/app.e2e-spec.ts` (documentado en MEMORY.md como pre-existing), son ignorables — solo errores del módulo whatsapp son bloqueantes.
  </verify>
  <done>
- Versiones de BullMQ y @nestjs/bullmq documentadas (o ajuste de versión realizado)
- `npm run build` pasa sin errores en los nuevos archivos
- Concern de compatibilidad de STATE.md resuelto con evidencia documentada en SUMMARY
  </done>
</task>

</tasks>

<verification>
1. `backend/src/modules/whatsapp/` existe con: `whatsapp.module.ts`, `whatsapp.service.ts`, `whatsapp.controller.ts`, `processors/whatsapp-message.processor.ts`, `crypto/encryption.service.ts`, `dto/save-waba-config.dto.ts`
2. `backend/src/app.module.ts` contiene `BullModule.forRootAsync` y `WhatsappModule`
3. `npm run build` en backend/ pasa sin errores en archivos nuevos
4. `WhatsappMessageProcessor` extiende `WorkerHost`, no usa `@Process()` decorator
5. `EncryptionService` usa `import * as crypto from 'crypto'` — sin npm packages externos
</verification>

<success_criteria>
- Módulo WhatsApp con BullMQ registrado y compilando sin errores TypeScript
- Compatibilidad BullMQ v11 + NestJS v10 verificada (o versión compatible instalada)
- `maxRetriesPerRequest: null` configurado en la conexión Redis de BullMQ
- Processor WorkerHost registrado para la queue `whatsapp-messages`
- EncryptionService disponible para ser inyectado en plan 01-02
</success_criteria>

<output>
Después de completar, crear `.planning/phases/01-infraestructura-async/01-01-SUMMARY.md` con:
- Versión final de @nestjs/bullmq instalada y resultado del smoke test de compatibilidad
- Si se requirió downgrade de versión, documentarlo aquí para Phase 4
- Archivos creados y cambios en AppModule
</output>
