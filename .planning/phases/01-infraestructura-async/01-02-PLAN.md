---
phase: 01-infraestructura-async
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/src/prisma/schema.prisma
  - backend/src/prisma/migrations/20260223000001_whatsapp_infra/migration.sql
  - backend/src/modules/pacientes/pacientes.controller.ts
  - backend/src/modules/pacientes/pacientes.service.ts
  - backend/src/modules/whatsapp/whatsapp.service.ts
  - backend/src/modules/whatsapp/dto/save-waba-config.dto.ts
  - backend/src/modules/whatsapp/dto/waba-config-response.dto.ts
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "La migración Prisma se aplica sin errores y la base de datos tiene las nuevas tablas"
    - "El endpoint PATCH /pacientes/:id/whatsapp-opt-in actualiza whatsappOptIn y registra timestamp cuando optIn=true"
    - "El endpoint POST /whatsapp/config guarda las credenciales WABA con el access token cifrado (nunca en texto plano)"
    - "El endpoint GET /whatsapp/config retorna { phoneNumberId, displayPhone, verifiedName, activo } — sin exponer el access token ni el campo encriptado"
  artifacts:
    - path: "backend/src/prisma/schema.prisma"
      provides: "Modelos ConfiguracionWABA, MensajeWhatsApp, enums EstadoMensajeWA/TipoMensajeWA, campos opt-in en Paciente"
      contains: "ConfiguracionWABA"
    - path: "backend/src/modules/whatsapp/whatsapp.service.ts"
      provides: "CRUD de configuración WABA con validación Meta + cifrado"
      exports: ["saveWabaConfig", "getWabaConfig"]
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "updateWhatsappOptIn con lógica timestamp"
      exports: ["updateWhatsappOptIn"]
  key_links:
    - from: "backend/src/modules/whatsapp/whatsapp.service.ts"
      to: "backend/src/modules/whatsapp/crypto/encryption.service.ts"
      via: "inyección DI de EncryptionService"
      pattern: "EncryptionService"
    - from: "backend/src/modules/whatsapp/whatsapp.service.ts"
      to: "Meta Graph API"
      via: "axios GET /{phone-number-id}?fields=display_phone_number,verified_name"
      pattern: "graph\\.facebook\\.com"
    - from: "backend/src/modules/pacientes/pacientes.controller.ts"
      to: "backend/src/modules/pacientes/pacientes.service.ts"
      via: "PATCH :id/whatsapp-opt-in"
      pattern: "updateWhatsappOptIn"
---

<objective>
Aplicar la migración Prisma con los modelos ConfiguracionWABA y MensajeWhatsApp, los enums de estado/tipo de mensaje, y los campos whatsappOptIn en Paciente. Implementar los endpoints de backend para gestionar opt-in y configuración WABA (con cifrado y validación Meta).

Purpose: Sin estos modelos no existe la persistencia que INFRA-02, INFRA-03 e INFRA-04 requieren. Este plan puede ejecutarse en paralelo con 01-01 porque no depende de BullMQ.
Output: Schema Prisma actualizado con migración aplicada, endpoints de opt-in y WABA config operativos.
</objective>

<execution_context>
@/Users/laucaballero/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infraestructura-async/01-RESEARCH.md

Codebase relevante:
@backend/src/prisma/schema.prisma
@backend/src/modules/pacientes/pacientes.controller.ts
@backend/src/modules/pacientes/pacientes.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar schema Prisma y aplicar migración</name>
  <files>
    backend/src/prisma/schema.prisma
  </files>
  <action>
Agregar al final de `backend/src/prisma/schema.prisma` los siguientes bloques (DESPUÉS del último model/enum existente):

**1. Nuevos enums:**
```prisma
enum EstadoMensajeWA {
  PENDIENTE
  ENVIADO
  ENTREGADO
  LEIDO
  FALLIDO
}

enum TipoMensajeWA {
  PRESUPUESTO
  RECORDATORIO_TURNO
  SEGUIMIENTO
  CUSTOM
}
```

**2. Nuevo modelo ConfiguracionWABA:**
```prisma
model ConfiguracionWABA {
  id                   String      @id @default(uuid())
  profesionalId        String      @unique
  phoneNumberId        String
  wabaId               String?
  accessTokenEncrypted String      // AES-256-GCM cifrado — NUNCA retornar en respuestas API
  displayPhone         String      // Número formateado (ej: "+54 9 11 1234-5678") — retornado por Meta en validación
  verifiedName         String?     // Nombre del negocio desde Meta
  activo               Boolean     @default(true)
  createdAt            DateTime    @default(now())
  updatedAt            DateTime    @updatedAt
  profesional          Profesional @relation(fields: [profesionalId], references: [id])
}
```

**3. Nuevo modelo MensajeWhatsApp:**
```prisma
model MensajeWhatsApp {
  id            String          @id @default(uuid())
  waMessageId   String?         // ID de Meta (nullable hasta que se envíe)
  pacienteId    String
  profesionalId String
  tipo          TipoMensajeWA
  contenido     String?         // Nombre del template o cuerpo del mensaje
  estado        EstadoMensajeWA @default(PENDIENTE)
  errorMsg      String?
  sentAt        DateTime?
  deliveredAt   DateTime?
  readAt        DateTime?
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt
  paciente      Paciente        @relation(fields: [pacienteId], references: [id])
  profesional   Profesional     @relation(fields: [profesionalId], references: [id])

  @@index([pacienteId, createdAt])
  @@index([estado])
  @@index([waMessageId])
}
```

**4. Agregar campos en el modelo `Paciente` existente** (antes de los `@@index` al final del modelo):
```prisma
  whatsappOptIn    Boolean   @default(false)
  whatsappOptInAt  DateTime?
```

**5. Agregar relaciones en `Profesional`** (antes del cierre del modelo):
```prisma
  configuracionWABA ConfiguracionWABA?
  mensajesWhatsApp  MensajeWhatsApp[]
```

**6. Agregar relación en `Paciente`** (antes de los `@@index`):
```prisma
  mensajesWhatsApp MensajeWhatsApp[]
```

**7. Ejecutar la migración:**
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend
npx prisma migrate dev --name whatsapp_infra
npx prisma generate
```

Si la DB no está disponible en el entorno de desarrollo, ejecutar al menos `npx prisma generate` para regenerar el cliente y verificar que el schema no tiene errores de sintaxis.
  </action>
  <verify>
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend
npx prisma validate 2>&1
```
Debe retornar "The schema at .../schema.prisma is valid" sin errores.
  </verify>
  <done>
- `npx prisma validate` pasa sin errores
- Schema contiene: modelo `ConfiguracionWABA`, modelo `MensajeWhatsApp`, enums `EstadoMensajeWA` y `TipoMensajeWA`
- Paciente tiene campos `whatsappOptIn Boolean @default(false)` y `whatsappOptInAt DateTime?`
- Relaciones bidireccionales correctas entre ConfiguracionWABA↔Profesional, MensajeWhatsApp↔Paciente, MensajeWhatsApp↔Profesional
  </done>
</task>

<task type="auto">
  <name>Task 2: Backend — endpoints opt-in y WABA config con validación Meta + cifrado</name>
  <files>
    backend/src/modules/pacientes/pacientes.controller.ts
    backend/src/modules/pacientes/pacientes.service.ts
    backend/src/modules/whatsapp/whatsapp.service.ts
    backend/src/modules/whatsapp/dto/save-waba-config.dto.ts
    backend/src/modules/whatsapp/dto/waba-config-response.dto.ts
    backend/src/modules/whatsapp/whatsapp.controller.ts
  </files>
  <action>
**1. DTO `save-waba-config.dto.ts`** (crear o actualizar):
```typescript
import { IsString, IsNotEmpty, IsOptional } from 'class-validator';

export class SaveWabaConfigDto {
  @IsString() @IsNotEmpty()
  phoneNumberId: string;

  @IsString() @IsNotEmpty()
  accessToken: string; // Nunca persiste — se cifra antes de guardar

  @IsString() @IsOptional()
  wabaId?: string;
}
```

**2. DTO `waba-config-response.dto.ts`** (crear):
```typescript
export class WabaConfigResponseDto {
  phoneNumberId: string;
  displayPhone: string;
  verifiedName?: string;
  activo: boolean;
  // NO incluir: accessTokenEncrypted, wabaId (opcional)
}
```

**3. DTO `update-whatsapp-opt-in.dto.ts`** (crear en pacientes/dto/ o whatsapp/dto/):
```typescript
import { IsBoolean } from 'class-validator';

export class UpdateWhatsappOptInDto {
  @IsBoolean()
  optIn: boolean;
}
```

**4. Actualizar `pacientes.service.ts`** — agregar método:
```typescript
async updateWhatsappOptIn(id: string, optIn: boolean) {
  return this.prisma.paciente.update({
    where: { id },
    data: {
      whatsappOptIn: optIn,
      whatsappOptInAt: optIn ? new Date() : null,
    },
    select: {
      id: true,
      nombreCompleto: true,
      whatsappOptIn: true,
      whatsappOptInAt: true,
    },
  });
}
```

**5. Actualizar `pacientes.controller.ts`** — agregar endpoint:
```typescript
@Patch(':id/whatsapp-opt-in')
@Roles('ADMIN', 'PROFESIONAL', 'SECRETARIA')
async updateWhatsappOptIn(
  @Param('id') id: string,
  @Body() dto: UpdateWhatsappOptInDto,
) {
  return this.pacientesService.updateWhatsappOptIn(id, dto.optIn);
}
```

**6. Actualizar `whatsapp.service.ts`** — implementar WABA config:
```typescript
// Constante de versión parameterizada (anti-pitfall 5 de RESEARCH.md)
private readonly META_API_VERSION = 'v21.0';

async validateWABACredentials(phoneNumberId: string, accessToken: string) {
  const url = `https://graph.facebook.com/${this.META_API_VERSION}/${phoneNumberId}`;
  try {
    const response = await axios.get(url, {
      params: { fields: 'display_phone_number,verified_name' },
      headers: { Authorization: `Bearer ${accessToken}` },
    });
    return {
      displayPhone: response.data.display_phone_number as string,
      verifiedName: response.data.verified_name as string | undefined,
    };
  } catch (err: any) {
    const metaMsg = err.response?.data?.error?.message ?? 'Credenciales inválidas';
    const metaCode = err.response?.data?.error?.code;
    if (metaCode === 190) throw new BadRequestException(`Token inválido: ${metaMsg}`);
    if (metaCode === 100) throw new BadRequestException(`Phone Number ID inválido: ${metaMsg}`);
    throw new BadRequestException(`Meta API: ${metaMsg}`);
  }
}

async saveWabaConfig(profesionalId: string, dto: SaveWabaConfigDto): Promise<WabaConfigResponseDto> {
  // 1. Validar credenciales contra Meta ANTES de cifrar/guardar
  const { displayPhone, verifiedName } = await this.validateWABACredentials(dto.phoneNumberId, dto.accessToken);

  // 2. Cifrar el access token — NUNCA persistir en texto plano
  const accessTokenEncrypted = this.encryptionService.encrypt(dto.accessToken);

  // 3. Upsert (crear o actualizar) la configuración del profesional
  const config = await this.prisma.configuracionWABA.upsert({
    where: { profesionalId },
    create: {
      profesionalId,
      phoneNumberId: dto.phoneNumberId,
      wabaId: dto.wabaId,
      accessTokenEncrypted,
      displayPhone,
      verifiedName,
      activo: true,
    },
    update: {
      phoneNumberId: dto.phoneNumberId,
      wabaId: dto.wabaId,
      accessTokenEncrypted,
      displayPhone,
      verifiedName,
      activo: true,
    },
  });

  // 4. Retornar solo campos seguros — NUNCA el token cifrado ni el wabaId si no es necesario
  return {
    phoneNumberId: config.phoneNumberId,
    displayPhone: config.displayPhone,
    verifiedName: config.verifiedName ?? undefined,
    activo: config.activo,
  };
}

async getWabaConfig(profesionalId: string): Promise<WabaConfigResponseDto | null> {
  const config = await this.prisma.configuracionWABA.findUnique({
    where: { profesionalId },
    select: { phoneNumberId: true, displayPhone: true, verifiedName: true, activo: true },
    // IMPORTANTE: NO seleccionar accessTokenEncrypted
  });
  if (!config) return null;
  return {
    phoneNumberId: config.phoneNumberId,
    displayPhone: config.displayPhone,
    verifiedName: config.verifiedName ?? undefined,
    activo: config.activo,
  };
}
```

Inyectar en el constructor de WhatsappService: `PrismaService` y `EncryptionService`.

**7. Actualizar `whatsapp.controller.ts`** — agregar endpoints de config:
```typescript
@Get('config')
@Roles('ADMIN', 'PROFESIONAL')
async getConfig(@Request() req) {
  // req.user.profesionalId del JWT payload — ajustar según patrón del proyecto
  const profesionalId = req.user.profesionalId;
  return this.whatsappService.getWabaConfig(profesionalId);
}

@Post('config')
@Roles('ADMIN', 'PROFESIONAL')
async saveConfig(@Request() req, @Body() dto: SaveWabaConfigDto) {
  const profesionalId = req.user.profesionalId;
  return this.whatsappService.saveWabaConfig(profesionalId, dto);
}
```

Verificar cómo los otros controllers del proyecto obtienen el `profesionalId` del JWT (buscar en `profesionales.controller.ts` o `turnos.controller.ts` el patrón actual) y replicarlo exactamente.

**IMPORTANTE:** Agregar `PrismaModule` a los imports de `WhatsappModule` si no está ya disponible globalmente, o inyectar `PrismaService` directamente.
  </action>
  <verify>
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend
npm run build 2>&1 | grep -E "error TS" | grep -v "e2e-spec" | head -20
```
Cero errores TypeScript en archivos nuevos. Los errores pre-existentes en `test/app.e2e-spec.ts` son ignorables (documentados en MEMORY.md).
  </verify>
  <done>
- `PATCH /pacientes/:id/whatsapp-opt-in` existe y actualiza whatsappOptIn + whatsappOptInAt
- `POST /whatsapp/config` valida contra Meta antes de guardar, cifra el token, retorna WabaConfigResponseDto
- `GET /whatsapp/config` retorna solo { phoneNumberId, displayPhone, verifiedName, activo } — sin access token
- Ninguna respuesta API expone el campo `accessTokenEncrypted`
- Build TypeScript limpio
  </done>
</task>

</tasks>

<verification>
1. `npx prisma validate` pasa sin errores en `backend/`
2. Schema contiene: `ConfiguracionWABA`, `MensajeWhatsApp`, `EstadoMensajeWA`, `TipoMensajeWA`, `whatsappOptIn` en Paciente
3. `npm run build` sin errores TypeScript en archivos nuevos
4. `WabaConfigResponseDto` no incluye `accessTokenEncrypted` ni el token en ninguna forma
5. `saveWabaConfig` llama `validateWABACredentials` ANTES de cifrar y persistir
6. `updateWhatsappOptIn` setea `whatsappOptInAt = new Date()` cuando optIn=true, null cuando false
</verification>

<success_criteria>
- Migración Prisma aplicada con los 3 cambios: ConfiguracionWABA, MensajeWhatsApp, campos opt-in en Paciente
- Endpoint opt-in operativo con timestamp correcto
- Endpoint WABA config con validación Meta live + cifrado AES-256-GCM
- Zero leakage del access token en respuestas API
- Prisma client regenerado y TypeScript compilando
</success_criteria>

<output>
Después de completar, crear `.planning/phases/01-infraestructura-async/01-02-SUMMARY.md` con:
- Resultado de `npx prisma validate` y si la migración se aplicó o quedó pendiente (para aplicar en deploy)
- Nota sobre la versión de Meta API usada (v21.0) y la constante parametrizada
- Resultado del build TypeScript
</output>
