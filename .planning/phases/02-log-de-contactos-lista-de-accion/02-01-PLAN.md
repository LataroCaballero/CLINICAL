---
phase: 02-log-de-contactos-lista-de-accion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/schema.prisma
  - backend/src/modules/pacientes/dto/create-contacto.dto.ts
  - backend/src/modules/pacientes/pacientes.controller.ts
  - backend/src/modules/pacientes/pacientes.service.ts
autonomous: true
requirements: [LOG-01, LOG-02, LOG-03, LOG-04, ACCION-01, ACCION-02, ACCION-04]

must_haves:
  truths:
    - "POST /pacientes/:id/contactos crea un ContactoLog y opcionalmente actualiza etapaCRM + temperatura del paciente en la misma transacción"
    - "GET /pacientes/:id/contactos devuelve el historial de interacciones ordenado por fecha desc"
    - "GET /pacientes/lista-accion devuelve pacientes priorizados con diasSinContacto calculado server-side, excluyendo a los contactados hoy"
    - "Los pacientes con etapaCRM CONFIRMADO o PERDIDO no aparecen en la lista de acción"
  artifacts:
    - path: "backend/src/prisma/schema.prisma"
      provides: "modelo ContactoLog + enum TipoContacto + relaciones a Paciente y Profesional"
      contains: "model ContactoLog"
    - path: "backend/src/modules/pacientes/dto/create-contacto.dto.ts"
      provides: "DTO validado con class-validator"
      exports: ["CreateContactoDto"]
    - path: "backend/src/modules/pacientes/pacientes.controller.ts"
      provides: "3 endpoints: GET lista-accion, GET :id/contactos, POST :id/contactos"
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "getListaAccion(), getContactosByPaciente(), createContacto()"
  key_links:
    - from: "PacientesController.createContacto"
      to: "PacientesService.createContacto"
      via: "Prisma $transaction"
      pattern: "prisma\\.\\$transaction"
    - from: "GET /pacientes/lista-accion"
      to: "PacientesService.getListaAccion"
      via: "Controller route ABOVE @Get(':id')"
      pattern: "@Get\\('lista-accion'\\)"
    - from: "ContactoLog"
      to: "Paciente"
      via: "FK pacienteId"
      pattern: "pacienteId.*String"
---

<objective>
Implementar la capa de datos y API para el log de contactos: nuevo modelo Prisma `ContactoLog`, migración, DTO, y tres endpoints en el módulo `pacientes` existente.

Purpose: El coordinador puede registrar interacciones con pacientes y la plataforma puede calcular prioridades de seguimiento server-side.
Output: Migration aplicada + 3 endpoints funcionales + servicio con lógica de prioridad.
</objective>

<execution_context>
@/Users/laucaballero/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-log-de-contactos-lista-de-accion/02-RESEARCH.md
@backend/src/prisma/schema.prisma
@backend/src/modules/pacientes/pacientes.controller.ts
@backend/src/modules/pacientes/pacientes.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema — modelo ContactoLog + migración</name>
  <files>backend/src/prisma/schema.prisma</files>
  <action>
Agregar al schema.prisma:

1. Nuevo enum `TipoContacto` con valores: `LLAMADA`, `MENSAJE`, `PRESENCIAL`

2. Nuevo modelo `ContactoLog`:
```prisma
model ContactoLog {
  id                  String               @id @default(uuid())
  pacienteId          String
  profesionalId       String
  tipo                TipoContacto
  nota                String?
  fecha               DateTime             @default(now())
  etapaCRMPost        EtapaCRM?
  temperaturaPost     TemperaturaPaciente?
  proximaAccionFecha  DateTime?
  createdAt           DateTime             @default(now())

  paciente    Paciente    @relation(fields: [pacienteId], references: [id], onDelete: Cascade)
  profesional Profesional @relation(fields: [profesionalId], references: [id])

  @@index([pacienteId, fecha])
  @@index([profesionalId, fecha])
}
```

3. Agregar relación a `Paciente`: `contactos   ContactoLog[]`
4. Agregar relación a `Profesional`: `contactos   ContactoLog[]`

Luego ejecutar desde `backend/`:
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
npx prisma migrate dev --name contact_log
npx prisma generate
```

Verificar que la migración se aplica sin errores y que el cliente Prisma se regenera con el nuevo tipo `TipoContacto`.
  </action>
  <verify>
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend
npx prisma validate
```
Debe salir sin errores. También verificar que existe `migrations/20260223*_contact_log/` en `backend/src/prisma/migrations/`.
  </verify>
  <done>Schema válido, migración aplicada, `ContactoLog` y `TipoContacto` disponibles en el cliente Prisma generado.</done>
</task>

<task type="auto">
  <name>Task 2: DTO + Controller endpoints + Service methods</name>
  <files>
    backend/src/modules/pacientes/dto/create-contacto.dto.ts
    backend/src/modules/pacientes/pacientes.controller.ts
    backend/src/modules/pacientes/pacientes.service.ts
  </files>
  <action>
**A. Crear `backend/src/modules/pacientes/dto/create-contacto.dto.ts`:**

```typescript
import { IsEnum, IsOptional, IsString, IsDateString } from 'class-validator';
import { TipoContacto, EtapaCRM, TemperaturaPaciente } from '@prisma/client';

export class CreateContactoDto {
  @IsEnum(TipoContacto)
  tipo: TipoContacto;

  @IsOptional()
  @IsString()
  nota?: string;

  @IsOptional()
  @IsDateString()
  fecha?: string;

  @IsOptional()
  @IsEnum(EtapaCRM)
  etapaCRM?: EtapaCRM;

  @IsOptional()
  @IsEnum(TemperaturaPaciente)
  temperatura?: TemperaturaPaciente;

  @IsOptional()
  @IsDateString()
  proximaAccionFecha?: string;
}
```

**B. Agregar a `pacientes.controller.ts`:**

Importar `CreateContactoDto` y agregar tres endpoints. CRITICAL: colocar `GET 'lista-accion'` ANTES de `@Get(':id')` (mismo patrón que `@Get('kanban')` ya existente):

```typescript
// Agregar import
import { CreateContactoDto } from './dto/create-contacto.dto';

// Endpoints nuevos — colocar DESPUÉS de @Get('kanban') y ANTES de @Get(':id')

@Get('lista-accion')
getListaAccion(
  @Query('profesionalId') profesionalId: string,
) {
  return this.pacientesService.getListaAccion(profesionalId);
}

@Get(':id/contactos')
getContactosByPaciente(
  @Param('id') pacienteId: string,
  @Query('limit') limit?: string,
) {
  return this.pacientesService.getContactosByPaciente(
    pacienteId,
    limit ? parseInt(limit, 10) : undefined,
  );
}

@Post(':id/contactos')
createContacto(
  @Param('id') pacienteId: string,
  @Body() dto: CreateContactoDto,
  @Req() req: any,
) {
  const profesionalId = req.user?.profesionalId as string;
  return this.pacientesService.createContacto(pacienteId, profesionalId, dto);
}
```

**C. Agregar a `pacientes.service.ts`:**

Importar `TipoContacto`, `EtapaCRM`, `TemperaturaPaciente` de `@prisma/client` y `CreateContactoDto`.

Agregar función auxiliar privada y tres métodos públicos:

```typescript
// Helper privado — calcular score de prioridad
private calcularScore(diasSinContacto: number, temperatura: TemperaturaPaciente | null, etapa: EtapaCRM | null): number {
  const diasScore = Math.min(diasSinContacto, 30);
  const tempWeight: Record<string, number> = { CALIENTE: 3, TIBIO: 2, FRIO: 1 };
  const etapaWeight: Record<string, number> = {
    SEGUIMIENTO_ACTIVO: 3,
    CALIENTE: 3,
    PRESUPUESTO_ENVIADO: 2,
    CONSULTA_REALIZADA: 2,
    CONSULTA_AGENDADA: 1,
    NUEVO_LEAD: 1,
  };
  const tw = tempWeight[temperatura ?? 'TIBIO'] ?? 1;
  const ew = etapaWeight[etapa ?? 'NUEVO_LEAD'] ?? 1;
  return diasScore * tw * ew;
}

// Helper — calcular días sin contacto
private calcularDiasSinContacto(ultimoContacto: Date | null, createdAt: Date): number {
  const base = ultimoContacto ?? createdAt;
  const diff = Date.now() - base.getTime();
  return Math.floor(diff / (1000 * 60 * 60 * 24));
}

// Método público: getListaAccion
async getListaAccion(profesionalId: string) {
  // Calcular inicio del día en UTC-3 (Argentina) para ACCION-04
  const offset = -3 * 60;
  const hoyInicio = new Date(Date.now() + offset * 60 * 1000);
  hoyInicio.setUTCHours(0, 0, 0, 0);
  hoyInicio.setTime(hoyInicio.getTime() - offset * 60 * 1000); // convertir de vuelta a UTC

  // Contar contactados hoy (para el widget contador)
  const contactadosHoy = await this.prisma.contactoLog.count({
    where: { profesionalId, fecha: { gte: hoyInicio } },
  });

  // Pacientes activos del profesional, excluyendo los contactados hoy, CONFIRMADO y PERDIDO
  const pacientes = await this.prisma.paciente.findMany({
    where: {
      profesionalId,
      etapaCRM: { notIn: ['CONFIRMADO', 'PERDIDO'] as any },
      NOT: {
        contactos: { some: { fecha: { gte: hoyInicio } } },
      },
    },
    include: {
      contactos: {
        orderBy: { fecha: 'desc' },
        take: 1,
      },
    },
  });

  // Mapear con score de prioridad
  const items = pacientes.map((p) => {
    const ultimoContacto = p.contactos[0]?.fecha ?? null;
    const diasSinContacto = this.calcularDiasSinContacto(ultimoContacto, p.createdAt);
    const score = this.calcularScore(diasSinContacto, p.temperatura as any, p.etapaCRM as any);
    return {
      id: p.id,
      nombreCompleto: p.nombreCompleto,
      telefono: p.telefono,
      etapaCRM: p.etapaCRM,
      temperatura: p.temperatura,
      diasSinContacto,
      score,
      ultimoContactoFecha: ultimoContacto,
    };
  });

  // Ordenar por score desc; secundario: nombreCompleto asc (estable)
  items.sort((a, b) => b.score - a.score || a.nombreCompleto.localeCompare(b.nombreCompleto, 'es'));

  return { items, contactadosHoy, total: items.length };
}

// Método público: getContactosByPaciente
async getContactosByPaciente(pacienteId: string, limit?: number) {
  const contactos = await this.prisma.contactoLog.findMany({
    where: { pacienteId },
    orderBy: { fecha: 'desc' },
    take: limit,
  });

  // Calcular diasSinContacto global del paciente (basado en el contacto más reciente)
  const paciente = await this.prisma.paciente.findUnique({
    where: { id: pacienteId },
    select: { createdAt: true },
  });
  const ultimoContacto = contactos[0]?.fecha ?? null;
  const diasSinContacto = this.calcularDiasSinContacto(ultimoContacto, paciente?.createdAt ?? new Date());

  return { contactos, diasSinContacto, total: contactos.length };
}

// Método público: createContacto
async createContacto(pacienteId: string, profesionalId: string, dto: CreateContactoDto) {
  return this.prisma.$transaction(async (tx) => {
    const contacto = await tx.contactoLog.create({
      data: {
        pacienteId,
        profesionalId,
        tipo: dto.tipo as any,
        nota: dto.nota,
        fecha: dto.fecha ? new Date(dto.fecha) : new Date(),
        etapaCRMPost: dto.etapaCRM as any ?? null,
        temperaturaPost: dto.temperatura as any ?? null,
        proximaAccionFecha: dto.proximaAccionFecha ? new Date(dto.proximaAccionFecha) : null,
      },
    });

    if (dto.etapaCRM || dto.temperatura) {
      await tx.paciente.update({
        where: { id: pacienteId },
        data: {
          ...(dto.etapaCRM && { etapaCRM: dto.etapaCRM as any }),
          ...(dto.temperatura && { temperatura: dto.temperatura as any }),
        },
      });
    }

    return contacto;
  });
}
```

Verificar que no hay errores de TypeScript ni imports faltantes. Los casts `as any` son aceptables hasta que los enum types de Prisma estén completamente alineados.
  </action>
  <verify>
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend
npm run build 2>&1 | grep -E "error TS|Error"
```
Build debe completar sin errores TS relevantes (ignorar `test/app.e2e-spec.ts` — pre-existing).

Smoke test manual (con el backend corriendo):
```bash
# GET lista-accion (debe devolver { items: [], contactadosHoy: 0, total: 0 } para profesional sin pacientes)
curl -s -H "Authorization: Bearer <token>" "http://localhost:3001/pacientes/lista-accion?profesionalId=<id>" | jq .
```
  </verify>
  <done>Build limpio. Tres endpoints responden correctamente. `POST /pacientes/:id/contactos` persiste el contacto y actualiza opcionalemente etapaCRM/temperatura del paciente en una sola transacción.</done>
</task>

</tasks>

<verification>
1. `npx prisma validate` pasa sin errores desde `backend/`
2. Migration `*_contact_log` existe en `backend/src/prisma/migrations/`
3. `npm run build` en backend completa sin errores TS en archivos modificados
4. Los tres endpoints están en el controlador con orden correcto (`lista-accion` antes de `:id`)
5. El servicio implementa la lógica de prioridad con pesos correctos y exclusión UTC-3 de "contactados hoy"
</verification>

<success_criteria>
- ContactoLog model existe en schema.prisma con todos los campos especificados
- Migration aplicada exitosamente
- DTO con validación class-validator creado
- 3 endpoints en controller en el orden correcto (GET lista-accion ANTES de GET :id)
- Service: createContacto usa $transaction, getListaAccion excluye etapas CONFIRMADO/PERDIDO y contactados hoy, score calculado server-side
</success_criteria>

<output>
After completion, create `.planning/phases/02-log-de-contactos-lista-de-accion/02-01-SUMMARY.md`
</output>
