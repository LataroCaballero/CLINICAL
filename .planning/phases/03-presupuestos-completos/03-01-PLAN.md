---
phase: 03-presupuestos-completos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/schema.prisma
  - backend/src/prisma/migrations/20260224_presupuestos_completos/migration.sql
  - backend/src/modules/presupuestos/dto/create-presupuesto.dto.ts
  - backend/src/modules/presupuestos/presupuestos.service.ts
autonomous: true
requirements:
  - PRES-01
  - PRES-05
  - PRES-06
  - PRES-07
  - PRES-08
  - CRM-03
  - CRM-04

must_haves:
  truths:
    - "El schema compila sin errores después de la migración"
    - "PresupuestoItem tiene `precioTotal` y NO tiene `cantidad` ni `precioUnitario`"
    - "Presupuesto tiene campos `tokenAceptacion`, `moneda`, `fechaValidez`"
    - "ConfigClinica existe como modelo Prisma vinculado a Profesional"
    - "EstadoPresupuesto enum incluye VENCIDO"
    - "rechazar() actualiza etapaCRM=PERDIDO y motivoPerdida en el paciente"
    - "CreatePresupuestoDto acepta moneda, fechaValidez; PresupuestoItemDto usa precioTotal"
  artifacts:
    - path: "backend/src/prisma/schema.prisma"
      provides: "Schema con ConfigClinica, PresupuestoItem simplificado, Presupuesto ampliado"
      contains: "model ConfigClinica"
    - path: "backend/src/modules/presupuestos/dto/create-presupuesto.dto.ts"
      provides: "DTO actualizado: precioTotal por ítem, moneda, fechaValidez"
      exports: ["PresupuestoItemDto", "CreatePresupuestoDto"]
    - path: "backend/src/modules/presupuestos/presupuestos.service.ts"
      provides: "rechazar() con transacción que actualiza paciente.etapaCRM=PERDIDO"
  key_links:
    - from: "backend/src/modules/presupuestos/presupuestos.service.ts"
      to: "prisma.paciente.update"
      via: "$transaction en rechazar()"
      pattern: "etapaCRM.*PERDIDO"
    - from: "backend/src/modules/presupuestos/dto/create-presupuesto.dto.ts"
      to: "presupuestos.service.ts create()"
      via: "precioTotal field mapping"
      pattern: "precioTotal"
---

<objective>
Migrar el schema de Prisma para soportar el flujo completo de presupuestos: simplificar PresupuestoItem (precioTotal solamente), ampliar Presupuesto (token de aceptación, moneda, fecha validez), crear ConfigClinica para branding por profesional, y agregar VENCIDO al enum EstadoPresupuesto. Actualizar DTO y corregir el gap crítico en rechazar() que no propagaba CRM.

Purpose: Sin este plan, los planes de backend PDF/email y el frontend no tienen la estructura de datos correcta. Es el fundamento bloqueante de toda la fase.
Output: Migration aplicada, schema actualizado, DTO corregido, service.rechazar() con CRM completo.
</objective>

<execution_context>
@/Users/laucaballero/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/prisma/schema.prisma
@backend/src/modules/presupuestos/dto/create-presupuesto.dto.ts
@backend/src/modules/presupuestos/presupuestos.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma migration — schema changes for presupuestos completos</name>
  <files>backend/src/prisma/schema.prisma</files>
  <action>
Modificar `backend/src/prisma/schema.prisma` con los siguientes cambios exactos:

**1. Nuevo modelo ConfigClinica** (insertar después del bloque `model Presupuesto`):

```prisma
model ConfigClinica {
  id               String      @id @default(uuid())
  profesionalId    String      @unique
  nombreClinica    String?
  logoUrl          String?
  direccion        String?
  telefono         String?
  emailContacto    String?
  web              String?
  piePaginaTexto   String?
  smtpHost         String?
  smtpPort         Int?
  smtpUser         String?
  smtpPassEncrypted String?
  smtpFrom         String?
  createdAt        DateTime    @default(now())
  updatedAt        DateTime    @updatedAt
  profesional      Profesional @relation(fields: [profesionalId], references: [id])
}
```

**2. Agregar relación en model Profesional** — agregar `configClinica ConfigClinica?` a la lista de relaciones del modelo Profesional (después de la última relación existente antes de `@@index` o `@@unique`).

**3. Modificar model Presupuesto** — agregar estos campos antes de `createdAt`:
```prisma
  tokenAceptacion   String?           @unique
  moneda            String            @default("ARS")
  fechaValidez      DateTime?
```

**4. Modificar model PresupuestoItem** — reemplazar los campos `cantidad`, `precioUnitario`, `total` por un único campo `precioTotal`:
- Eliminar: `cantidad Int @default(1)` y `precioUnitario Decimal @db.Decimal(10, 2)`
- Renombrar `total` → `precioTotal`: `precioTotal Decimal @db.Decimal(10, 2)`

El modelo PresupuestoItem resultante debe ser:
```prisma
model PresupuestoItem {
  id            String      @id @default(uuid())
  presupuestoId String
  descripcion   String
  precioTotal   Decimal     @db.Decimal(10, 2)
  orden         Int         @default(0)
  presupuesto   Presupuesto @relation(fields: [presupuestoId], references: [id])
}
```

**5. Modificar enum EstadoPresupuesto** — agregar `VENCIDO` al final:
```prisma
enum EstadoPresupuesto {
  BORRADOR
  ENVIADO
  ACEPTADO
  RECHAZADO
  CANCELADO
  VENCIDO
}
```

Después de editar el schema, ejecutar desde `backend/`:
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
npx prisma migrate dev --name presupuestos_completos
npx prisma generate
```

Si la migración falla por datos existentes en `PresupuestoItem` (la columna `total` se renombra a `precioTotal`), agregar `--create-only` para revisar el SQL generado antes de aplicar.

**IMPORTANTE:** Si hay datos existentes que usan `cantidad × precioUnitario`, el SQL de migración debe hacer:
```sql
ALTER TABLE "PresupuestoItem" ADD COLUMN "precioTotal" DECIMAL(10,2);
UPDATE "PresupuestoItem" SET "precioTotal" = "total";
ALTER TABLE "PresupuestoItem" DROP COLUMN "cantidad";
ALTER TABLE "PresupuestoItem" DROP COLUMN "precioUnitario";
ALTER TABLE "PresupuestoItem" DROP COLUMN "total";
ALTER TABLE "PresupuestoItem" ALTER COLUMN "precioTotal" SET NOT NULL;
```
Prisma puede generar esto automáticamente — verificar el SQL antes de aplicar.
  </action>
  <verify>
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
cd backend && npx prisma validate
```
Debe retornar sin errores. También verificar:
```bash
grep -n "model ConfigClinica\|precioTotal\|tokenAceptacion\|VENCIDO\|moneda" backend/src/prisma/schema.prisma
```
Todos deben aparecer en el output.
  </verify>
  <done>
`npx prisma validate` pasa sin errores. Schema contiene: ConfigClinica model, precioTotal en PresupuestoItem (sin cantidad/precioUnitario), tokenAceptacion/moneda/fechaValidez en Presupuesto, VENCIDO en EstadoPresupuesto. Migration aplicada y Prisma client regenerado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Actualizar DTO + corregir service rechazar() con CRM completo</name>
  <files>
    backend/src/modules/presupuestos/dto/create-presupuesto.dto.ts
    backend/src/modules/presupuestos/presupuestos.service.ts
  </files>
  <action>
**A. Actualizar `backend/src/modules/presupuestos/dto/create-presupuesto.dto.ts`:**

Reemplazar completamente el archivo con:
```typescript
import {
  IsArray,
  IsDateString,
  IsIn,
  IsNumber,
  IsOptional,
  IsPositive,
  IsString,
  IsUUID,
  Min,
  ValidateNested,
} from 'class-validator';
import { Type } from 'class-transformer';

export class PresupuestoItemDto {
  @IsString()
  descripcion: string;

  @IsNumber()
  @IsPositive()
  precioTotal: number;
}

export class CreatePresupuestoDto {
  @IsUUID()
  pacienteId: string;

  @IsUUID()
  profesionalId: string;

  @IsArray()
  @ValidateNested({ each: true })
  @Type(() => PresupuestoItemDto)
  items: PresupuestoItemDto[];

  @IsOptional()
  @IsNumber()
  @Min(0)
  descuentos?: number;

  @IsOptional()
  @IsIn(['ARS', 'USD'])
  moneda?: string;

  @IsOptional()
  @IsDateString()
  fechaValidez?: string;
}
```

**B. Actualizar `backend/src/modules/presupuestos/presupuestos.service.ts`:**

1. **Método `create()`** — actualizar el mapeo de items (reemplazar el bloque `itemsConTotal`):
```typescript
// Reemplazar el cálculo de itemsConTotal con:
const itemsConTotal = dto.items.map((item, index) => ({
  descripcion: item.descripcion,
  precioTotal: item.precioTotal,
  orden: index,
}));

const subtotal = itemsConTotal.reduce((acc, item) => acc + item.precioTotal, 0);
const descuentos = dto.descuentos || 0;
const total = subtotal - descuentos;
```

Y actualizar el `data` del `create` para incluir moneda y fechaValidez:
```typescript
const presupuesto = await this.prisma.presupuesto.create({
  data: {
    pacienteId: dto.pacienteId,
    profesionalId: dto.profesionalId,
    subtotal,
    descuentos,
    total,
    moneda: dto.moneda ?? 'ARS',
    fechaValidez: dto.fechaValidez ? new Date(dto.fechaValidez) : null,
    estado: EstadoPresupuesto.BORRADOR,
    items: {
      create: itemsConTotal,
    },
  },
  // include sin cambios
});
```

2. **Método `rechazar()`** — reemplazar la implementación para usar `$transaction` y actualizar el paciente:
```typescript
async rechazar(id: string, dto: { motivoRechazo?: string }) {
  const presupuesto = await this.prisma.presupuesto.findUnique({
    where: { id },
    select: { id: true, pacienteId: true, estado: true },
  });

  if (!presupuesto) throw new NotFoundException('Presupuesto no encontrado');

  if (
    presupuesto.estado !== EstadoPresupuesto.BORRADOR &&
    presupuesto.estado !== EstadoPresupuesto.ENVIADO
  ) {
    throw new BadRequestException(
      `No se puede rechazar un presupuesto en estado ${presupuesto.estado}`,
    );
  }

  const [updated] = await this.prisma.$transaction([
    this.prisma.presupuesto.update({
      where: { id },
      data: {
        estado: EstadoPresupuesto.RECHAZADO,
        fechaRechazado: new Date(),
        motivoRechazo: dto.motivoRechazo ?? null,
      },
      include: {
        items: { orderBy: { orden: 'asc' } },
        paciente: { select: { id: true, nombreCompleto: true } },
      },
    }),
    this.prisma.paciente.update({
      where: { id: presupuesto.pacienteId },
      data: {
        etapaCRM: EtapaCRM.PERDIDO,
        motivoPerdida: dto.motivoRechazo ?? null,
      },
    }),
  ]);

  return this.formatPresupuesto(updated);
}
```

3. **Método `formatPresupuesto()`** — actualizar el mapeo de items:
```typescript
items: presupuesto.items?.map((item: any) => ({
  id: item.id,
  descripcion: item.descripcion,
  precioTotal: Number(item.precioTotal),
})),
```
También agregar `moneda` y `fechaValidez` al objeto de retorno:
```typescript
moneda: presupuesto.moneda,
fechaValidez: presupuesto.fechaValidez,
```

4. **Agregar import de `MotivoPerdidaCRM` si `motivoPerdida` es del tipo enum** — revisar el schema Prisma. Si `motivoPerdida` en el modelo `Paciente` es un enum `MotivoPerdidaCRM`, usar el valor correcto. Si es un `String?`, pasar el string directamente como está en el dto.

**NOTA:** El campo `motivoPerdida` en Paciente es de tipo `MotivoPerdidaCRM?` (enum, según MEMORY.md). Revisar los valores del enum en schema.prisma. En `rechazar()`, `dto.motivoRechazo` es un string del coordinador/paciente — guardarlo en `presupuesto.motivoRechazo` (String) pero para `paciente.motivoPerdida` (enum), usar el valor más cercano o null si no matchea. Dado que la aceptación pública registra el motivo como texto libre, se decide: dejar `paciente.motivoPerdida` como null en rechazar() del servicio (el endpoint público lo setea con el enum correcto). Solo actualizar `etapaCRM = PERDIDO`.

Corrección final para rechazar():
```typescript
this.prisma.paciente.update({
  where: { id: presupuesto.pacienteId },
  data: { etapaCRM: EtapaCRM.PERDIDO },
}),
```
  </action>
  <verify>
```bash
export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"
cd backend && npm run build 2>&1 | grep -E "error TS|SyntaxError" | head -20
```
No debe haber errores TypeScript relacionados con los archivos modificados. Si hay errores de tipo en el Prisma client (por campos eliminados), ejecutar `npx prisma generate` primero.
  </verify>
  <done>
`npm run build` sin errores TS en `presupuestos.service.ts` ni `create-presupuesto.dto.ts`. El método `rechazar()` usa `$transaction` y actualiza `etapaCRM: EtapaCRM.PERDIDO` en el paciente. `PresupuestoItemDto` tiene `precioTotal` (sin `cantidad`/`precioUnitario`). `formatPresupuesto` retorna `moneda`, `fechaValidez`, y `items[].precioTotal`.
  </done>
</task>

</tasks>

<verification>
1. `cd backend && npx prisma validate` — sin errores
2. `grep -n "ConfigClinica\|precioTotal\|tokenAceptacion\|VENCIDO\|moneda" backend/src/prisma/schema.prisma` — todos presentes
3. `cd backend && npm run build 2>&1 | grep "error TS" | head -10` — sin errores en módulo presupuestos
4. La migration existe en `backend/src/prisma/migrations/` con timestamp reciente
</verification>

<success_criteria>
Schema migrado y compilado sin errores. PresupuestoItem usa precioTotal. Presupuesto tiene tokenAceptacion/moneda/fechaValidez. ConfigClinica existe en DB. rechazar() propaga etapaCRM=PERDIDO al paciente. DTOs actualizados con nuevos campos.
</success_criteria>

<output>
After completion, create `.planning/phases/03-presupuestos-completos/03-01-SUMMARY.md` with:
- What was changed in schema (exact fields added/removed)
- Whether migration applied cleanly or required manual SQL
- Any TypeScript errors encountered and resolved
- Exact enum values of MotivoPerdidaCRM (needed by Plan 03-02 for public controller)
</output>
