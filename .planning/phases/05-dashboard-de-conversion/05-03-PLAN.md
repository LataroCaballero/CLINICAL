---
phase: 05-dashboard-de-conversion
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - frontend/src/app/dashboard/components/CRMFunnelWidget.tsx
  - frontend/src/app/dashboard/components/CRMKpiCards.tsx
  - frontend/src/app/dashboard/components/LossReasonsWidget.tsx
  - frontend/src/app/dashboard/components/PipelineIncomeWidget.tsx
  - frontend/src/app/dashboard/components/CoordinatorPerformanceWidget.tsx
  - frontend/src/app/dashboard/page.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "El embudo muestra barras decrecientes de arriba a abajo (trapecio vertical) con conteo de pacientes por etapa"
    - "La tasa de paso aparece entre cada par de etapas, mostrando '—' cuando la tasa es null"
    - "Los perdidos aparecen en tarjeta separada al lado del embudo, NO dentro del funnel"
    - "CRMKpiCards muestra nuevos pacientes, confirmados y tasa de conversión con su propio PeriodoSelector"
    - "LossReasonsWidget muestra los motivos de pérdida con porcentajes y su propio PeriodoSelector"
    - "PipelineIncomeWidget muestra el total de ingresos potenciales, visible solo para PROFESIONAL y ADMIN"
    - "CoordinatorPerformanceWidget muestra una tabla con columnas Nombre | Interacciones registradas | Pacientes contactados | % Conversión personal, visible solo para PROFESIONAL y ADMIN"
    - "Cada widget persiste su período seleccionado en localStorage con su propia storage key"
  artifacts:
    - path: "frontend/src/app/dashboard/components/CRMFunnelWidget.tsx"
      provides: "Embudo trapecio vertical con perdidos en tarjeta lateral"
      min_lines: 80
    - path: "frontend/src/app/dashboard/components/CRMKpiCards.tsx"
      provides: "4 KPI cards con PeriodoSelector integrado"
      min_lines: 60
    - path: "frontend/src/app/dashboard/components/LossReasonsWidget.tsx"
      provides: "Widget de motivos de pérdida con porcentajes y PeriodoSelector"
    - path: "frontend/src/app/dashboard/components/PipelineIncomeWidget.tsx"
      provides: "Widget de ingreso potencial del pipeline con PeriodoSelector"
    - path: "frontend/src/app/dashboard/components/CoordinatorPerformanceWidget.tsx"
      provides: "Tabla HTML con columnas Nombre | Interacciones | Pacientes contactados | % Conversión personal, visible solo PROFESIONAL/ADMIN"
    - path: "frontend/src/app/dashboard/page.tsx"
      provides: "Dashboard integrado con todos los widgets nuevos en layout 12-col"
  key_links:
    - from: "frontend/src/app/dashboard/components/CRMFunnelWidget.tsx"
      to: "frontend/src/hooks/useCRMFunnel.ts"
      via: "import useCRMFunnel"
      pattern: "useCRMFunnel"
    - from: "frontend/src/app/dashboard/components/CRMKpiCards.tsx"
      to: "frontend/src/hooks/useCRMKpis.ts"
      via: "import useCRMKpis (reemplaza useCRMMetrics)"
      pattern: "useCRMKpis"
    - from: "frontend/src/app/dashboard/components/CoordinatorPerformanceWidget.tsx"
      to: "frontend/src/hooks/useCurrentUser.ts"
      via: "rol check PROFESIONAL|ADMIN"
      pattern: "useCurrentUser"
    - from: "frontend/src/app/dashboard/page.tsx"
      to: "frontend/src/app/dashboard/components/LossReasonsWidget.tsx"
      via: "import + render en columna izquierda"
      pattern: "LossReasonsWidget"
---

<objective>
Construir los 5 widgets del dashboard de conversión y actualizar `dashboard/page.tsx` para integrarlos en el layout existente. `CRMFunnelWidget` se reescribe como trapecio vertical clásico. `CRMKpiCards` se reescribe con filtro de período. Se crean 3 widgets nuevos.

Purpose: Este es el plan que hace visible el valor de la fase — transforma el dashboard de datos estáticos a métricas interactivas con filtros por período.
Output: Dashboard de conversión completo con embudo trapecio, KPIs con período, motivos de pérdida, pipeline financiero y performance del coordinador.
</objective>

<execution_context>
@/Users/laucaballero/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-de-conversion/05-02-SUMMARY.md
@.planning/phases/05-dashboard-de-conversion/05-CONTEXT.md
@.planning/phases/05-dashboard-de-conversion/05-RESEARCH.md
@frontend/src/app/dashboard/page.tsx
@frontend/src/app/dashboard/components/CRMFunnelWidget.tsx
@frontend/src/app/dashboard/components/CRMKpiCards.tsx
@frontend/src/app/dashboard/components/KpiCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir CRMFunnelWidget (trapecio) y reescribir CRMKpiCards (con período) + crear LossReasonsWidget y PipelineIncomeWidget</name>
  <files>
    frontend/src/app/dashboard/components/CRMFunnelWidget.tsx
    frontend/src/app/dashboard/components/CRMKpiCards.tsx
    frontend/src/app/dashboard/components/LossReasonsWidget.tsx
    frontend/src/app/dashboard/components/PipelineIncomeWidget.tsx
  </files>
  <action>
**`CRMFunnelWidget.tsx` — REEMPLAZAR completamente:**

Trapecio vertical clásico. Estructura: tarjeta blanca con `flex gap-4` — lado izquierdo el embudo, lado derecho la tarjeta de perdidos.

```tsx
"use client";
import { useCRMFunnel } from "@/hooks/useCRMFunnel";
import { useEffectiveProfessionalId } from "@/hooks/useEffectiveProfessionalId";
import { Skeleton } from "@/components/ui/skeleton";

const ETAPAS_LABELS: Record<string, string> = {
  NUEVO_LEAD: "Nuevo lead",
  TURNO_AGENDADO: "Turno agendado",
  CONSULTADO: "Consultado",
  PRESUPUESTO_ENVIADO: "Presupuesto enviado",
  CONFIRMADO: "Confirmado",
};

const MOTIVO_LABELS: Record<string, string> = {
  PRECIO: "Precio",
  TIEMPO: "Tiempo",
  MIEDO_CIRUGIA: "Miedo a cirugía",
  PREFIERE_OTRO_PROFESIONAL: "Otro profesional",
  NO_CANDIDATO_MEDICO: "No candidato",
  NO_RESPONDIO: "Sin respuesta",
  OTRO: "Otro",
  SIN_MOTIVO: "Sin motivo registrado",
};

export default function CRMFunnelWidget() {
  const profId = useEffectiveProfessionalId();
  const { data, isLoading } = useCRMFunnel(profId);

  if (isLoading) return <Skeleton className="h-64 w-full rounded-xl" />;
  if (!data) return null;

  const maxCount = Math.max(...data.etapas.map(e => e.count), 1);

  return (
    <div className="bg-white rounded-xl border p-4">
      <h3 className="text-sm font-semibold text-gray-700 mb-4">Embudo de conversión</h3>
      <div className="flex gap-6">
        {/* Trapecio izquierdo */}
        <div className="flex-1 flex flex-col gap-1">
          {data.etapas.map((etapa, i) => {
            const widthPct = maxCount > 0 ? Math.round((etapa.count / maxCount) * 100) : 0;
            const tasaPaso = data.tasasPaso[i]; // tasa HACIA esta etapa desde la anterior
            return (
              <div key={etapa.etapa}>
                {/* Tasa de paso entre etapas (antes de cada etapa excepto la primera) */}
                {i > 0 && (
                  <div className="text-center text-xs text-gray-400 py-0.5">
                    ↓ {tasaPaso?.tasa != null ? `${tasaPaso.tasa}%` : "—"}
                  </div>
                )}
                {/* Barra del trapecio */}
                <div className="flex items-center gap-2">
                  <div
                    className="h-8 rounded flex items-center justify-center transition-all"
                    style={{
                      width: `${widthPct}%`,
                      minWidth: '2rem',
                      backgroundColor: i === data.etapas.length - 1 ? '#10b981' : '#3b82f6',
                      opacity: 1 - i * 0.1,
                    }}
                  >
                    <span className="text-white text-xs font-bold px-2">{etapa.count}</span>
                  </div>
                  <span className="text-xs text-gray-500 whitespace-nowrap">
                    {ETAPAS_LABELS[etapa.etapa] ?? etapa.etapa}
                  </span>
                </div>
              </div>
            );
          })}
        </div>

        {/* Tarjeta de perdidos (separada) */}
        <div className="w-40 shrink-0 border border-red-100 rounded-lg p-3 bg-red-50/50 flex flex-col gap-2">
          <div className="text-xs font-semibold text-red-600 uppercase tracking-wide">Perdidos</div>
          <div className="text-2xl font-bold text-red-600">{data.perdidos.total}</div>
          {data.perdidos.porMotivo.length > 0 && (
            <ul className="space-y-1">
              {data.perdidos.porMotivo.slice(0, 4).map(m => (
                <li key={m.motivo} className="flex justify-between text-xs text-gray-600">
                  <span>{MOTIVO_LABELS[m.motivo] ?? m.motivo}</span>
                  <span className="font-medium">{m.count}</span>
                </li>
              ))}
            </ul>
          )}
          {data.perdidos.porMotivo.length === 0 && (
            <p className="text-xs text-gray-400">Sin motivos registrados</p>
          )}
        </div>
      </div>
    </div>
  );
}
```

**`CRMKpiCards.tsx` — REEMPLAZAR completamente:**

Reemplazar `useCRMMetrics` por `useCRMKpis`. El `PeriodoSelector` NO va dentro de este componente — va en `dashboard/page.tsx` como una fila header encima del grid de KPIs (ver Task 2). Este componente solo exporta el estado `[periodo, setPeriodo]` necesario para que page.tsx pueda pasárselo a `PeriodoSelector` y a este mismo componente, O alternativamente el componente acepta `periodo` como prop.

DECISION DE IMPLEMENTACIÓN: Para mantener encapsulamiento, el hook `usePeriodoFilter` vive DENTRO de `CRMKpiCards`. El componente acepta un `ref`-like o expone el período a través de un callback. La forma más limpia es: `CRMKpiCards` maneja su propio `usePeriodoFilter` internamente y recibe una prop `onPeriodoChange?: (p: Periodo) => void` — así `page.tsx` puede renderizar el `PeriodoSelector` fuera del grid usando el estado propio del componente expuesto hacia arriba.

IMPLEMENTACIÓN REAL (la más simple sin prop drilling complejo): Mover el `usePeriodoFilter` a `page.tsx` y pasar `periodo` + `setPeriodo` como props a `CRMKpiCards`. Así `page.tsx` tiene el estado y puede renderizar `PeriodoSelector` antes del grid.

```tsx
"use client";
import KpiCard from "./KpiCard";
import { useCRMKpis } from "@/hooks/useCRMKpis";
import { useEffectiveProfessionalId } from "@/hooks/useEffectiveProfessionalId";
import { Periodo } from "@/hooks/usePeriodoFilter";

interface Props {
  isLoading?: boolean;
  periodo: Periodo;
}

export default function CRMKpiCards({ isLoading: parentLoading, periodo }: Props) {
  const profId = useEffectiveProfessionalId();
  const { data, isLoading } = useCRMKpis(profId, periodo);

  const loading = parentLoading || isLoading;

  return (
    <>
      <KpiCard
        title="Nuevos pacientes"
        value={data ? String(data.nuevos) : "—"}
        subtitle={`${periodo === 'semana' ? 'esta semana' : periodo === 'mes' ? 'este mes' : 'este trimestre'}`}
        isLoading={loading}
      />
      <KpiCard
        title="Cirugías confirmadas"
        value={data ? String(data.confirmados) : "—"}
        subtitle="en el período"
        trend="up"
        isLoading={loading}
      />
      <KpiCard
        title="Tasa de conversión"
        value={data ? `${data.tasaConversion}%` : "—"}
        subtitle={data ? `${data.confirmados} de ${data.totalActivos} activos` : "sin datos"}
        trend={
          data && data.tasaConversion > 30
            ? "up"
            : data && data.tasaConversion > 0
            ? "neutral"
            : "down"
        }
        isLoading={loading}
      />
      <KpiCard
        title="Pacientes activos"
        value={data ? String(data.totalActivos) : "—"}
        subtitle="excluye perdidos"
        isLoading={loading}
      />
    </>
  );
}
```

NO usar `headerAction` en ningún `KpiCard` — la interfaz de `KpiCard` no incluye esa prop y el build fallaría. El `PeriodoSelector` se renderiza en `dashboard/page.tsx` en un `div` de col-span-4 ANTES del grid de KPI cards (ver Task 2).

**`LossReasonsWidget.tsx` — NUEVO:**

```tsx
"use client";
import { useCRMLossReasons } from "@/hooks/useCRMLossReasons";
import { useEffectiveProfessionalId } from "@/hooks/useEffectiveProfessionalId";
import { usePeriodoFilter } from "@/hooks/usePeriodoFilter";
import { PeriodoSelector } from "@/components/crm/PeriodoSelector";
import { Skeleton } from "@/components/ui/skeleton";

const MOTIVO_LABELS: Record<string, string> = {
  PRECIO: "Precio",
  TIEMPO: "Tiempo",
  MIEDO_CIRUGIA: "Miedo a cirugía",
  PREFIERE_OTRO_PROFESIONAL: "Otro profesional",
  NO_CANDIDATO_MEDICO: "No candidato médico",
  NO_RESPONDIO: "Sin respuesta",
  OTRO: "Otro",
  SIN_MOTIVO: "Sin motivo registrado",
};

export default function LossReasonsWidget() {
  const profId = useEffectiveProfessionalId();
  const [periodo, setPeriodo] = usePeriodoFilter('crm-loss-reasons-periodo', 'mes');
  const { data, isLoading } = useCRMLossReasons(profId, periodo);

  if (isLoading) return <Skeleton className="h-40 w-full rounded-xl" />;

  return (
    <div className="bg-white rounded-xl border p-4">
      <div className="flex items-center justify-between mb-3">
        <h3 className="text-sm font-semibold text-gray-700">Motivos de pérdida</h3>
        <PeriodoSelector value={periodo} onChange={setPeriodo} />
      </div>
      {!data || data.total === 0 ? (
        <p className="text-sm text-gray-400 text-center py-4">Sin pacientes perdidos en el período</p>
      ) : (
        <div className="space-y-2">
          <p className="text-xs text-gray-500 mb-2">{data.total} pacientes perdidos</p>
          {data.motivos.map(m => (
            <div key={m.motivo} className="flex items-center gap-2">
              <div className="flex-1">
                <div className="flex justify-between text-xs mb-0.5">
                  <span className="text-gray-600">{MOTIVO_LABELS[m.motivo] ?? m.motivo}</span>
                  <span className="font-medium text-gray-800">{m.porcentaje}%</span>
                </div>
                <div className="h-1.5 bg-gray-100 rounded-full overflow-hidden">
                  <div
                    className="h-full bg-red-400 rounded-full"
                    style={{ width: `${m.porcentaje}%` }}
                  />
                </div>
              </div>
              <span className="text-xs text-gray-400 w-6 text-right">{m.count}</span>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
```

**`PipelineIncomeWidget.tsx` — NUEVO:**

```tsx
"use client";
import { useCRMPipelineIncome } from "@/hooks/useCRMPipelineIncome";
import { useEffectiveProfessionalId } from "@/hooks/useEffectiveProfessionalId";
import { usePeriodoFilter } from "@/hooks/usePeriodoFilter";
import { PeriodoSelector } from "@/components/crm/PeriodoSelector";
import { Skeleton } from "@/components/ui/skeleton";
import { useCurrentUser } from "@/hooks/useCurrentUser";

function formatMoney(n: number) {
  if (n >= 1_000_000) return `$${(n / 1_000_000).toFixed(1)}M`;
  if (n >= 1_000) return `$${(n / 1_000).toFixed(0)}K`;
  return "$" + Math.round(n).toLocaleString("es-AR");
}

export default function PipelineIncomeWidget() {
  const { data: user } = useCurrentUser();
  const profId = useEffectiveProfessionalId();
  const [periodo, setPeriodo] = usePeriodoFilter('crm-pipeline-income-periodo', 'mes');
  const { data, isLoading } = useCRMPipelineIncome(profId, periodo);

  // Solo visible para PROFESIONAL y ADMIN
  // TODO: tier gating — cuando se implemente el modelo de tiers/suscripción en ConfigClinica,
  // agregar condición: clinica.tier === 'premium'. Por ahora se usa visibilidad por rol como proxy.
  // La lógica de tier está DIFERIDA (ver 05-CONTEXT.md sección Deferred Ideas).
  if (user && user.rol !== 'PROFESIONAL' && user.rol !== 'ADMIN') return null;

  if (isLoading) return <Skeleton className="h-24 w-full rounded-xl" />;

  return (
    <div className="bg-white rounded-xl border p-4">
      <div className="flex items-center justify-between mb-2">
        <h3 className="text-sm font-semibold text-gray-700">Pipeline potencial</h3>
        <PeriodoSelector value={periodo} onChange={setPeriodo} />
      </div>
      <div className="mt-1">
        <div className="text-2xl font-bold text-emerald-600">
          {data ? formatMoney(data.total) : "—"}
        </div>
        <p className="text-xs text-gray-400 mt-0.5">
          {data ? `${data.count} presupuesto${data.count !== 1 ? 's' : ''} enviado${data.count !== 1 ? 's' : ''} · pacientes calientes` : "presupuestos enviados · pacientes calientes"}
        </p>
      </div>
    </div>
  );
}
```

NOTA SOBRE `useCurrentUser`: Si el hook no existe con ese nombre, buscar el hook que retorna el usuario autenticado en el proyecto (puede ser `useMe`, `useAuthUser`, `useUser`) y usar ese. Verificar con `ls frontend/src/hooks/` antes de escribir el import.
  </action>
  <verify>
```bash
ls /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/app/dashboard/components/LossReasonsWidget.tsx /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/app/dashboard/components/PipelineIncomeWidget.tsx
```
Los 4 archivos existen (2 reescritos + 2 nuevos).
  </verify>
  <done>CRMFunnelWidget reescrito con trapecio vertical y tarjeta de perdidos. CRMKpiCards reescrito con useCRMKpis + PeriodoSelector. LossReasonsWidget y PipelineIncomeWidget creados.</done>
</task>

<task type="auto">
  <name>Task 2: Crear CoordinatorPerformanceWidget e integrar todos los widgets en dashboard/page.tsx</name>
  <files>
    frontend/src/app/dashboard/components/CoordinatorPerformanceWidget.tsx
    frontend/src/app/dashboard/page.tsx
  </files>
  <action>
**`CoordinatorPerformanceWidget.tsx` — NUEVO:**

Renderiza una tabla HTML con columnas: Nombre | Interacciones registradas | Pacientes contactados | % Conversión personal. Per user decision in CONTEXT.md: "Tabla con columnas: Nombre | Interacciones registradas | Pacientes contactados | % Conversión personal".

```tsx
"use client";
import { useCoordinatorPerformance } from "@/hooks/useCoordinatorPerformance";
import { useEffectiveProfessionalId } from "@/hooks/useEffectiveProfessionalId";
import { usePeriodoFilter } from "@/hooks/usePeriodoFilter";
import { PeriodoSelector } from "@/components/crm/PeriodoSelector";
import { Skeleton } from "@/components/ui/skeleton";
import { useCurrentUser } from "@/hooks/useCurrentUser";

export default function CoordinatorPerformanceWidget() {
  const { data: user } = useCurrentUser();
  const profId = useEffectiveProfessionalId();
  const [periodo, setPeriodo] = usePeriodoFilter('coordinator-performance-periodo', 'semana');
  const { data, isLoading } = useCoordinatorPerformance(profId, periodo);

  // Solo visible para PROFESIONAL y ADMIN (vista gerencial)
  if (user && user.rol !== 'PROFESIONAL' && user.rol !== 'ADMIN') return null;

  if (isLoading) return <Skeleton className="h-28 w-full rounded-xl" />;

  return (
    <div className="bg-white rounded-xl border p-4">
      <div className="flex items-center justify-between mb-3">
        <h3 className="text-sm font-semibold text-gray-700">Performance de seguimiento</h3>
        <PeriodoSelector value={periodo} onChange={setPeriodo} />
      </div>
      {!data || data.coordinadores.length === 0 ? (
        <p className="text-sm text-gray-400 text-center py-3">Sin interacciones registradas en el período</p>
      ) : (
        <table className="w-full text-xs">
          <thead>
            <tr className="border-b">
              <th className="text-left pb-2 font-medium text-gray-500">Nombre</th>
              <th className="text-right pb-2 font-medium text-gray-500">Interacciones</th>
              <th className="text-right pb-2 font-medium text-gray-500">Pacientes contactados</th>
              <th className="text-right pb-2 font-medium text-gray-500">% Conversión</th>
            </tr>
          </thead>
          <tbody>
            {data.coordinadores.map((coord) => (
              <tr key={coord.nombre} className="border-b last:border-0">
                <td className="py-2 text-gray-700">{coord.nombre}</td>
                <td className="py-2 text-right text-gray-800 font-medium">{coord.interacciones}</td>
                <td className="py-2 text-right text-gray-800 font-medium">{coord.pacientesContactados}</td>
                <td className="py-2 text-right font-medium">
                  {coord.porcentajeConversion != null
                    ? <span className="text-emerald-600">{coord.porcentajeConversion}%</span>
                    : <span className="text-gray-400">—</span>
                  }
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      )}
    </div>
  );
}
```

**`dashboard/page.tsx` — actualizar importaciones y layout:**

1. Agregar imports:
   ```tsx
   import LossReasonsWidget from "./components/LossReasonsWidget";
   import PipelineIncomeWidget from "./components/PipelineIncomeWidget";
   import CoordinatorPerformanceWidget from "./components/CoordinatorPerformanceWidget";
   import { usePeriodoFilter } from "@/hooks/usePeriodoFilter";
   import { PeriodoSelector } from "@/components/crm/PeriodoSelector";
   ```

2. En el body del componente de la página (dentro de la función, antes del return), agregar el estado del período de los KPI cards:
   ```tsx
   const [kpiPeriodo, setKpiPeriodo] = usePeriodoFilter('crm-kpis-periodo', 'mes');
   ```

3. En el layout normal (la sección `// ── Normal layout ──`), reemplazar el bloque de KPI cards con el header de período + grid:

```tsx
{/* Bloque KPIs CRM con su selector de período propio */}
<div>
  <div className="flex items-center justify-between mb-3">
    <h3 className="text-sm font-semibold text-gray-600">Métricas de conversión</h3>
    <PeriodoSelector value={kpiPeriodo} onChange={setKpiPeriodo} />
  </div>
  <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
    <CRMKpiCards isLoading={kpisLoading} periodo={kpiPeriodo} />
  </div>
</div>

{/* Mini-embudo del mes — trapecio */}
<CRMFunnelWidget />

{/* Dashboard de conversión — segunda fila */}
<div className="grid grid-cols-1 md:grid-cols-2 gap-4">
  <LossReasonsWidget />
  <PipelineIncomeWidget />
</div>

{/* Performance del coordinador */}
<CoordinatorPerformanceWidget />

{/* KPIs financieros — tercera fila */}
```

4. Mantener TODOS los widgets y KPIs financieros existentes intactos. Solo se agregan los 3 nuevos componentes en la columna izquierda.

5. La prop `periodo` es REQUERIDA en `CRMKpiCards` (per la nueva interfaz del componente). Siempre pasar `kpiPeriodo`.

NOTA SOBRE `useCurrentUser`: Antes de escribir el import en los widgets, verificar el nombre real del hook con:
```bash
ls frontend/src/hooks/ | grep -i "user\|auth\|me"
```
Usar el nombre exacto que existe en el proyecto.
  </action>
  <verify>
```bash
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && bash -c 'export PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH"; npm run build 2>&1 | tail -30'
```
Build exitoso. Si hay errores de tipo, corregirlos antes de continuar.
  </verify>
  <done>
- CoordinatorPerformanceWidget.tsx creado y visible solo para PROFESIONAL/ADMIN
- dashboard/page.tsx importa y renderiza los 3 nuevos widgets
- El dashboard compila sin errores de TypeScript
- Los widgets existentes (UpcomingAppointments, AlertsWidget, etc.) no fueron alterados
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificación visual del dashboard de conversión</name>
  <what-built>
Dashboard de conversión completo con:
- CRMFunnelWidget: trapecio vertical con barras decrecientes, tasas de paso entre etapas, tarjeta de perdidos separada
- CRMKpiCards: nuevos pacientes, confirmados, tasa de conversión, activos — con filtro de período propio
- LossReasonsWidget: barras de porcentaje de motivos de pérdida con filtro de período
- PipelineIncomeWidget: total de ingresos potenciales (presupuestos ENVIADOS · pacientes CALIENTES) — visible solo PROFESIONAL/ADMIN
- CoordinatorPerformanceWidget: interacciones y pacientes únicos del período — visible solo PROFESIONAL/ADMIN
- Cada widget persiste su período en localStorage
  </what-built>
  <how-to-verify>
1. Iniciar el servidor: `cd frontend && npm run dev` + `cd backend && npm run start:dev`
2. Abrir http://localhost:3000/dashboard como usuario PROFESIONAL o ADMIN
3. Verificar que el embudo se muestra como trapecio (barras más anchas arriba, más angostas abajo)
4. Verificar que hay tasas de paso entre etapas (ej: "↓ 67%" o "↓ —" si no hay datos)
5. Verificar que los perdidos están en tarjeta separada A LA DERECHA del embudo, no integrados en el funnel
6. Verificar que el PeriodoSelector de los KPI cards está ENCIMA del grid de 4 KPIs (no dentro de ningún KpiCard)
7. Verificar que CoordinatorPerformanceWidget muestra una TABLA con columnas: Nombre | Interacciones registradas | Pacientes contactados | % Conversión personal
8. Cambiar el período de un widget y recargar la página — verificar que persiste
9. Iniciar sesión como SECRETARIA y verificar que PipelineIncomeWidget y CoordinatorPerformanceWidget NO se ven
10. Verificar que todos los widgets financieros y de turnos del dashboard original siguen funcionando
  </how-to-verify>
  <resume-signal>Escribí "approved" si todo se ve correctamente, o describí los problemas encontrados</resume-signal>
  <action>Verificación manual requerida — ver instrucciones en how-to-verify</action>
  <verify>Abrir http://localhost:3000/dashboard y validar todos los puntos listados en how-to-verify</verify>
  <done>Usuario confirma que el dashboard de conversión se visualiza correctamente con trapecio, perdidos separados, filtros de período funcionales y visibilidad por rol correcta</done>
</task>

</tasks>

<verification>
1. Los 5 widgets están implementados y en el dashboard
2. El embudo es trapecio vertical (NO horizontal con flechas)
3. Los perdidos están en tarjeta lateral (NO dentro del funnel)
4. Cada widget usa su propia storage key en localStorage
5. PipelineIncomeWidget y CoordinatorPerformanceWidget retornan `null` para SECRETARIA
6. `npm run build` del frontend pasa sin errores de tipo
7. `CRMKpiCards` NO usa prop `headerAction` en ningún `KpiCard` — el `PeriodoSelector` está en `dashboard/page.tsx` encima del grid
8. `CoordinatorPerformanceWidget` renderiza una `<table>` con columnas: Nombre | Interacciones registradas | Pacientes contactados | % Conversión personal
9. `CRMKpiCards` acepta prop `periodo: Periodo` (no gestiona el estado del período internamente)
</verification>

<success_criteria>
- 5 componentes de widgets en `frontend/src/app/dashboard/components/`
- `dashboard/page.tsx` importa y renderiza los 3 widgets nuevos
- El checkpoint de verificación visual es aprobado por el usuario
- Todos los requirements DASH-01 a DASH-05 tienen cobertura visual en el dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-de-conversion/05-03-SUMMARY.md`
</output>
