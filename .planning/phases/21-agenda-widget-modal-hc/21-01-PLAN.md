---
phase: 21-agenda-widget-modal-hc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/hc-templates/dto/create-entry.dto.ts
  - backend/src/modules/hc-templates/hc-templates.service.ts
  - frontend/src/types/hc-templates.ts
autonomous: true
requirements: [HC-03]

must_haves:
  truths:
    - "POST /pacientes/:id/hc/entries acepta campo fecha opcional y la entrada queda con esa fecha en DB"
    - "El campo fecha no puede ser futuro (retorna 400 si lo es)"
    - "Si fecha no se provee, la entrada usa DB default(now()) sin regresión"
  artifacts:
    - path: "backend/src/modules/hc-templates/dto/create-entry.dto.ts"
      provides: "CreateEntryDto con campo fecha?: string opcional"
      contains: "IsOptional"
    - path: "backend/src/modules/hc-templates/hc-templates.service.ts"
      provides: "createEntry valida y persiste fecha retroactiva"
      contains: "fechaFinal"
    - path: "frontend/src/types/hc-templates.ts"
      provides: "CreateEntryDto frontend con fecha?: string"
      contains: "fecha?"
  key_links:
    - from: "frontend/src/hooks/useHCEntries.ts"
      to: "POST /pacientes/:id/hc/entries"
      via: "CreateEntryDto.fecha"
      pattern: "fecha"
---

<objective>
Agregar soporte de fecha retroactiva al endpoint POST /pacientes/:id/hc/entries (template-based HC entries). Actualmente el backend CreateEntryDto y el service no aceptan fecha — la entrada siempre usa DB default(now()). El modal TurnoHCModal necesita pasar la fecha del turno histórico para que las entradas queden correctamente fechadas.

Purpose: Cumplir HC-03 — la entrada retroactiva queda fechada en el día del turno, no en hoy.
Output: Backend DTO + service con fecha opcional validada; tipo frontend actualizado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-backend-data-fixes/20-01-SUMMARY.md

<interfaces>
<!-- Patrón de fecha retroactiva ya establecido en Phase 20 — replicar exactamente -->

From backend/src/modules/historia-clinica/dto/crear-entrada.dto.ts (referencia del patrón):
```typescript
@IsOptional()
@IsDateString()
fecha?: string; // ISO date string para entradas retroactivas
```

From backend/src/modules/historia-clinica/historia-clinica.service.ts (patrón de validación):
```typescript
// Validar que la fecha no sea futura
const hoy = new Date();
hoy.setHours(23, 59, 59, 999);
if (fechaFinal > hoy) {
  throw new BadRequestException('La fecha no puede ser futura');
}
// ... en Prisma create:
...(fechaFinal && { fecha: fechaFinal }),
```

From backend/src/modules/hc-templates/dto/create-entry.dto.ts (estado actual — SIN fecha):
```typescript
export class CreateEntryDto {
  @IsUUID()
  @IsString()
  templateId: string;

  @IsUUID()
  @IsString()
  templateVersionId: string;
}
```

From backend/src/modules/hc-templates/hc-templates.service.ts createEntry (línea 289 — sin fecha):
```typescript
return this.prisma.historiaClinicaEntrada.create({
  data: {
    historiaClinicaId: historiaClinica.id,
    templateId: dto.templateId,
    templateVersionId: dto.templateVersionId,
    status: EstadoEntradaHC.DRAFT,
    answers: {},
    computed: {},
  },
  ...
});
```

From frontend/src/types/hc-templates.ts (estado actual — SIN fecha):
```typescript
export interface CreateEntryDto {
  templateId: string;
  templateVersionId: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar fecha opcional a CreateEntryDto backend y validar en createEntry service</name>
  <files>backend/src/modules/hc-templates/dto/create-entry.dto.ts, backend/src/modules/hc-templates/hc-templates.service.ts</files>
  <action>
    1. En `backend/src/modules/hc-templates/dto/create-entry.dto.ts`:
       - Agregar imports: `IsOptional, IsDateString` de `class-validator`
       - Agregar campo al final de la clase: `@IsOptional() @IsDateString() fecha?: string;`

    2. En `backend/src/modules/hc-templates/hc-templates.service.ts`, método `createEntry`:
       - Agregar import `BadRequestException` si no existe (ya está importado — verificar)
       - Antes del bloque `prisma.historiaClinica.findFirst`, agregar validación de fecha (replicar exactamente el patrón de Phase 20):
         ```typescript
         let fechaFinal: Date | undefined;
         if (dto.fecha) {
           fechaFinal = new Date(dto.fecha);
           if (isNaN(fechaFinal.getTime())) {
             throw new BadRequestException('Fecha inválida');
           }
           const hoy = new Date();
           hoy.setHours(23, 59, 59, 999);
           if (fechaFinal > hoy) {
             throw new BadRequestException('La fecha no puede ser futura');
           }
         }
         ```
       - En el bloque `prisma.historiaClinicaEntrada.create`, en `data`, agregar al final:
         `...(fechaFinal && { fecha: fechaFinal }),`

    NO cambiar la firma pública del método — `createEntry(pacienteId, profesionalId, dto)` permanece igual.
    NO modificar otros métodos del service.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript compila sin errores; CreateEntryDto tiene campo fecha opcional; createEntry valida y persiste fecha.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar tipo CreateEntryDto en frontend con campo fecha opcional</name>
  <files>frontend/src/types/hc-templates.ts</files>
  <action>
    En `frontend/src/types/hc-templates.ts`, localizar la interfaz `CreateEntryDto` (línea ~288) y agregar el campo opcional:

    ```typescript
    export interface CreateEntryDto {
      templateId: string;
      templateVersionId: string;
      fecha?: string; // ISO date string YYYY-MM-DD para entradas retroactivas
    }
    ```

    NO modificar otras interfaces del archivo.
    NO cambiar `useCreateHCEntry` — el campo ya pasará transparentemente porque el hook envía `dto` directamente al POST.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>Interfaz CreateEntryDto tiene campo fecha?: string; compilación frontend sin errores.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sin errores en backend y frontend
- `CreateEntryDto` backend tiene `@IsOptional() @IsDateString() fecha?: string`
- `createEntry` service valida fecha no futura con patrón idéntico al Phase 20
- Prisma create incluye `...(fechaFinal && { fecha: fechaFinal })`
- Interfaz frontend `CreateEntryDto` tiene `fecha?: string`
</verification>

<success_criteria>
Backend acepta POST con `{ templateId, templateVersionId, fecha: "2026-03-15" }` y la entrada queda con `fecha = 2026-03-15` en DB. POST con fecha futura retorna 400. POST sin fecha usa `@default(now())`.
</success_criteria>

<output>
After completion, create `.planning/phases/21-agenda-widget-modal-hc/21-01-SUMMARY.md`
</output>
