---
phase: 21-agenda-widget-modal-hc
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/dashboard/components/UpcomingAppointments.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]

must_haves:
  truths:
    - "Al abrir el dashboard, el widget carga los turnos del día actual (agenda completa, no solo futuros)"
    - "Los botones de flecha navegan día anterior / siguiente y la lista se actualiza con /turnos/agenda"
    - "El selector de calendario permite seleccionar cualquier fecha y la lista se actualiza"
    - "Para hoy y días pasados se muestra la tira de métricas (total, finalizados, cirugías, ausentes, cancelados)"
    - "Cada turno FINALIZADO de hoy o días pasados muestra botón 'Ver HC' que abre TurnoHCModal"
  artifacts:
    - path: "frontend/src/app/dashboard/components/UpcomingAppointments.tsx"
      provides: "Widget agenda-first con nav día-a-día, métricas, y botón Ver HC en pasados/hoy"
      contains: "agenda"
  key_links:
    - from: "UpcomingAppointments.tsx"
      to: "GET /turnos/agenda"
      via: "useQuery con fecha param"
      pattern: "turnos/agenda"
    - from: "UpcomingAppointments.tsx"
      to: "TurnoHCModal"
      via: "setHcTurno + estado FINALIZADO"
      pattern: "setHcTurno"
---

<objective>
Reescribir la lógica de estado de `UpcomingAppointments.tsx` para que sea agenda-first: por defecto muestra hoy vía `/turnos/agenda`, los botones de flecha navegan día-a-día (incrementando/decrementando la fecha base), el botón "Ver HC" se muestra para turnos FINALIZADOS en hoy y días pasados (no solo cuando hay `pickedDate`).

El componente ya existe en el working tree con estructura y UI completa, pero tiene tres gaps de comportamiento que este plan cierra.

Purpose: Cumplir DASH-01 (hoy por defecto), DASH-02 (nav anterior/siguiente), DASH-03 (calendario — ya funciona), DASH-04 (métricas — ya funciona cuando hay pickedDate), DASH-05 (Ver HC en hoy/pasados sin requerir pickedDate).
Output: UpcomingAppointments.tsx con lógica de fecha unificada.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Estado actual del componente — leer el archivo completo antes de editar -->
<!-- Path: frontend/src/app/dashboard/components/UpcomingAppointments.tsx -->

Resumen del estado actual:
- Tiene dos modos: "upcoming" (usa /turnos/proximos, agrupa por fecha) y "picked" (usa /turnos/agenda con fecha elegida)
- dateIndex navega por grupos en modo upcoming
- pickedDate activa el modo picked y el calendario
- Las métricas solo aparecen cuando `pickedDate` es truthy
- "Ver HC" solo aparece cuando `pickedDate` es truthy Y el turno es FINALIZADO
- Los botones de flecha en modo upcoming navegan groups[], no días

Cambios requeridos (mínimos — no reescribir lo que funciona):

GAP 1 — DASH-01: Modo por defecto debe ser hoy vía /turnos/agenda
  - Inicializar `selectedDate` con `new Date()` (hoy) en lugar de null
  - Eliminar el query de /turnos/proximos (o mantenerlo pero no como modo default)
  - La query de /turnos/agenda debe estar habilitada con `selectedDate` siempre (no solo cuando pickedDate != null)

GAP 2 — DASH-02: Nav flechas debe cambiar selectedDate ± 1 día
  - Los botones ChevronLeft/ChevronRight deben llamar a setSelectedDate(addDays(selectedDate, -1)) y setSelectedDate(addDays(selectedDate, +1))
  - Siempre habilitados (no hay límite de navegación salvo fecha razonable)
  - El botón X ("Volver a próximos") puede mantenerse para resetear a hoy o eliminarse si no tiene sentido

GAP 3 — DASH-05: "Ver HC" en hoy y días pasados sin necesitar pickedDate
  - La condición `pickedDate && t.estado === "FINALIZADO"` debe cambiar a:
    `isHoyOPasado(selectedDate) && t.estado === "FINALIZADO"`
  - donde isHoyOPasado(d) = d <= new Date() (comparación con inicio del día)

Refactoring de estado:
  - Reemplazar la dualidad upcoming/picked por un solo estado: `selectedDate: Date` (inicializado en hoy)
  - `pickedDate` para el calendario puede mantenerse como alias de selectedDate o colapsar en uno
  - El query siempre usa /turnos/agenda con selectedDate como fecha
  - Eliminar el query de /turnos/proximos si ya no se necesita (simplifica el componente)

IMPORTANTE: Mantener toda la UI existente (cards, table, theme tokens, focusMode, TurnoHCModal wiring). Solo cambiar la lógica de estado y queries.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactorizar UpcomingAppointments a modo agenda-first unificado</name>
  <files>frontend/src/app/dashboard/components/UpcomingAppointments.tsx</files>
  <action>
    Leer el archivo completo primero. Luego aplicar los siguientes cambios quirúrgicos:

    **1. Estado de fecha unificado**
    Reemplazar:
    ```typescript
    const [dateIndex, setDateIndex] = useState(0);
    const [pickedDate, setPickedDate] = useState<Date | null>(null);
    ```
    Por:
    ```typescript
    const [selectedDate, setSelectedDate] = useState<Date>(new Date());
    const [calendarOpen, setCalendarOpen] = useState(false);
    ```
    (eliminar `dateIndex` y `pickedDate` como estados separados)

    **2. Query única usando /turnos/agenda**
    Eliminar el query de `upcoming` (`/turnos/proximos`). Mantener solo el query de agenda y habilitarlo siempre:
    ```typescript
    const { data: turnos = [], isLoading } = useQuery({
      queryKey: ["turnos", "agenda", profesionalId, yyyyMmDd(selectedDate)],
      queryFn: async () => {
        const res = await api.get<TurnoAgenda[]>("/turnos/agenda", {
          params: { profesionalId, fecha: yyyyMmDd(selectedDate) },
        });
        return res.data ?? [];
      },
      enabled: !!profesionalId,
    });
    ```

    **3. Header derivado de selectedDate**
    ```typescript
    const hoyStr = yyyyMmDd(new Date());
    const isToday = yyyyMmDd(selectedDate) === hoyStr;
    const isPast = selectedDate < new Date(new Date().setHours(0, 0, 0, 0));
    const dayLabel = capitalize(format(selectedDate, "EEEE d 'de' MMMM", { locale: es }));
    const header = isToday
      ? "Turnos del día"
      : isPast
      ? `Historial — ${dayLabel}`
      : `Turnos — ${dayLabel}`;
    ```

    **4. Botones de navegación día-a-día**
    Los botones ChevronLeft/ChevronRight siempre visibles (no dependen de `pickedDate`):
    ```typescript
    <button
      className={navBtnCls}
      onClick={() => setSelectedDate(d => { const n = new Date(d); n.setDate(n.getDate() - 1); return n; })}
      aria-label="Día anterior"
    >
      <ChevronLeft className="w-4 h-4" />
    </button>
    <button
      className={navBtnCls}
      onClick={() => setSelectedDate(d => { const n = new Date(d); n.setDate(n.getDate() + 1); return n; })}
      aria-label="Día siguiente"
    >
      <ChevronRight className="w-4 h-4" />
    </button>
    ```
    Eliminar el indicador "1 / N" de grupos (ya no aplica). El botón X puede resetear a hoy:
    ```typescript
    {!isToday && (
      <button className={navBtnCls} onClick={() => setSelectedDate(new Date())} aria-label="Volver a hoy">
        <X className="w-4 h-4" />
      </button>
    )}
    ```

    **5. Calendario**
    El Calendar picker actualiza `selectedDate`:
    ```typescript
    onSelect={(d) => {
      if (d) setSelectedDate(d);
      setCalendarOpen(false);
    }}
    selected={selectedDate}
    ```

    **6. Métricas: mostrar para hoy y pasados**
    ```typescript
    const isHoyOPasado = isToday || isPast;
    const metrics = isHoyOPasado ? {
      total: turnos.length,
      finalizados: turnos.filter((t) => t.estado === "FINALIZADO").length,
      cirugias: turnos.filter(isCirugia).length,
      ausentes: turnos.filter((t) => t.estado === "AUSENTE").length,
      cancelados: turnos.filter((t) => t.estado === "CANCELADO").length,
    } : null;
    ```
    Reemplazar la condición de render de métricas: `{metrics && isHoyOPasado && (...)}` → `{metrics && (...)}`

    **7. Botón "Ver HC": hoy y pasados sin necesitar pickedDate**
    Cambiar:
    ```typescript
    {pickedDate && t.estado === "FINALIZADO" && (
    ```
    Por:
    ```typescript
    {isHoyOPasado && t.estado === "FINALIZADO" && (
    ```

    **8. TurnoHCModal — pasar selectedDate**
    ```typescript
    <TurnoHCModal
      turno={hcTurno}
      open={!!hcTurno}
      onClose={() => setHcTurno(null)}
      selectedDate={selectedDate}
    />
    ```

    **9. Eliminar referencias huérfanas** a `pickedDate`, `dateIndex`, `upcomingLoading`, `agendaLoading`, `data?.dates`, `data?.grouped`, `canPrev`, `canNext`, `pickedDateStr`. El componente simplificado no las necesita.

    NO cambiar: tipos TurnoAgenda, funciones helper (yyyyMmDd, hhmm, capitalize, tipoTurnoClass, estadoUi, isCirugia), theme tokens, estructura de tabla, lógica de observaciones/tooltip.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - TypeScript compila sin errores
    - Componente usa un solo estado selectedDate (inicializado en new Date())
    - Query única apunta a /turnos/agenda con fecha=yyyyMmDd(selectedDate)
    - Flechas prev/next modifican selectedDate ± 1 día
    - Métricas visibles cuando isToday || isPast
    - "Ver HC" visible cuando isHoyOPasado && FINALIZADO
    - selectedDate pasado al TurnoHCModal
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sin errores en frontend
- No hay referencias a `pickedDate`, `dateIndex`, `upcomingLoading`, `agendaLoading` en el archivo
- El queryKey incluye yyyyMmDd(selectedDate) (no pickedDateStr)
- El query está enabled: !!profesionalId (sin condición de pickedDate)
</verification>

<success_criteria>
El widget UpcomingAppointments carga /turnos/agenda con la fecha de hoy por defecto. Las flechas incrementan/decrementan la fecha. El calendario actualiza la fecha. Las métricas y "Ver HC" aparecen para hoy y días pasados.
</success_criteria>

<output>
After completion, create `.planning/phases/21-agenda-widget-modal-hc/21-02-SUMMARY.md`
</output>
