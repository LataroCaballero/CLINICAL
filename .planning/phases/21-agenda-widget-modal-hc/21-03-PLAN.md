---
phase: 21-agenda-widget-modal-hc
plan: 03
type: execute
wave: 2
depends_on: [21-01, 21-02]
files_modified:
  - frontend/src/app/dashboard/components/TurnoHCModal.tsx
autonomous: false
requirements: [HC-01, HC-02, HC-03]

must_haves:
  truths:
    - "El modal muestra las entradas HC del día del turno en modo solo-lectura con indicador legal de inmutabilidad"
    - "El modal permite agregar una nueva entrada HC usando el selector de plantilla + wizard"
    - "La entrada nueva queda fechada en el día histórico del turno (no en hoy)"
  artifacts:
    - path: "frontend/src/app/dashboard/components/TurnoHCModal.tsx"
      provides: "Modal HC con fecha retroactiva wired en createEntry"
      contains: "selectedDate"
  key_links:
    - from: "TurnoHCModal.tsx handleSelectTemplate"
      to: "useCreateHCEntry mutation"
      via: "dto.fecha = yyyyMmDd(selectedDate)"
      pattern: "fecha.*selectedDate"
---

<objective>
Conectar la fecha retroactiva en TurnoHCModal: cuando el profesional crea una nueva entrada HC desde el modal, el DTO debe incluir `fecha: yyyyMmDd(selectedDate)` para que la entrada quede fechada en el día histórico del turno. El Plan 01 ya agregó `fecha?` al backend DTO y al tipo frontend. Este plan solo wirea el campo en la llamada de mutación.

También verificar HC-01: que el indicador visual de inmutabilidad legal ya está presente (lo está — el componente `EntryReadOnly` ya incluye el badge de Lock). Confirmar y completar si falta algo.

Purpose: Cerrar HC-03 en frontend — la entrada retroactiva queda con la fecha correcta.
Output: TurnoHCModal.tsx con fecha wired; checkpoint humano confirma el flujo completo.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-agenda-widget-modal-hc/21-01-SUMMARY.md
@.planning/phases/21-agenda-widget-modal-hc/21-02-SUMMARY.md

<interfaces>
<!-- Estado del TurnoHCModal luego de los planes anteriores -->

Props del componente (no cambian):
```typescript
type Props = {
  turno: TurnoAgenda | null;
  open: boolean;
  onClose: () => void;
  selectedDate: Date;  // fecha del turno seleccionado — puede ser histórica
};
```

Función yyyyMmDd ya existe en el archivo:
```typescript
function yyyyMmDd(d: Date) {
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}`;
}
```

Llamada actual en handleSelectTemplate (SIN fecha — esto es el gap):
```typescript
const entry = await createTemplateEntry.mutateAsync({
  pacienteId,
  dto: {
    templateId: template.id,
    templateVersionId: template.currentVersion.id,
  },
});
```

Llamada corregida (CON fecha):
```typescript
const entry = await createTemplateEntry.mutateAsync({
  pacienteId,
  dto: {
    templateId: template.id,
    templateVersionId: template.currentVersion.id,
    fecha: yyyyMmDd(selectedDate),
  },
});
```

Tipo CreateEntryDto (ya actualizado en Plan 01):
```typescript
export interface CreateEntryDto {
  templateId: string;
  templateVersionId: string;
  fecha?: string; // ISO date string YYYY-MM-DD para entradas retroactivas
}
```

El componente EntryReadOnly ya tiene:
- Badge de Lock con texto "Entrada finalizada — no puede modificarse por razones legales" (HC-01 ya cumplido)
- Renderizado de answers, contenido legacy, metadata (HC-01 ya cumplido)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wiring de fecha retroactiva en handleSelectTemplate del TurnoHCModal</name>
  <files>frontend/src/app/dashboard/components/TurnoHCModal.tsx</files>
  <action>
    Leer el archivo completo primero. Luego un cambio quirúrgico en `handleSelectTemplate`:

    Localizar el bloque de mutación (línea ~111):
    ```typescript
    const entry = await createTemplateEntry.mutateAsync({
      pacienteId,
      dto: {
        templateId: template.id,
        templateVersionId: template.currentVersion.id,
      },
    });
    ```

    Reemplazar por:
    ```typescript
    const entry = await createTemplateEntry.mutateAsync({
      pacienteId,
      dto: {
        templateId: template.id,
        templateVersionId: template.currentVersion.id,
        fecha: yyyyMmDd(selectedDate),
      },
    });
    ```

    Solo este cambio. NO modificar ningún otro bloque del archivo.

    Verificar que `selectedDate` es accesible en el scope de `handleSelectTemplate` (viene de las Props del componente — sí, está en scope).

    Verificar que `yyyyMmDd` está definida en el archivo (sí, línea ~65) y produce YYYY-MM-DD que es lo que el backend `@IsDateString()` acepta.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - TypeScript compila sin errores
    - handleSelectTemplate pasa `fecha: yyyyMmDd(selectedDate)` en el dto
    - El campo fecha es una string YYYY-MM-DD derivada del turno seleccionado, no de new Date()
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificar flujo completo agenda + modal HC</name>
  <action>Pausa para verificación humana del flujo end-to-end. Todo el código está implementado — este checkpoint confirma que funciona correctamente en el navegador.</action>
  <verify>Usuario confirma que el flujo funciona navegando el dashboard y creando una entrada HC retroactiva.</verify>
  <done>Usuario escribe "aprobado" tras verificar los 10 pasos del flujo.</done>
  <what-built>
    Flujo completo del widget de agenda + modal HC:
    - Plan 01: Backend y tipo frontend aceptan fecha retroactiva en /hc/entries
    - Plan 02: Widget carga hoy por defecto, navega día-a-día con flechas, métricas en pasados/hoy, "Ver HC" en FINALIZADOS
    - Plan 03: Modal crea entradas con fecha del turno histórico
  </what-built>
  <how-to-verify>
    1. Abrir el dashboard (http://localhost:3000/dashboard)
    2. Verificar que el widget de agenda carga los turnos del DÍA ACTUAL (no solo futuros)
    3. Hacer click en la flecha izquierda — verificar que cambia al día anterior y carga turnos de ese día
    4. Hacer click en la flecha derecha — verificar que vuelve a hoy
    5. Si hay turnos en días pasados: verificar que aparece la tira de métricas (total, finalizados, cirugías, ausentes, cancelados)
    6. Si hay un turno FINALIZADO en hoy o días pasados: hacer click en "Ver HC"
    7. Verificar que el modal abre y muestra las entradas HC existentes con el badge de candado (inmutabilidad legal)
    8. Hacer click en "Nueva entrada HC" y seleccionar una plantilla
    9. Completar y finalizar la entrada
    10. Verificar que la entrada creada tiene como fecha el día del turno histórico (no hoy), revisando en la historia clínica del paciente
  </how-to-verify>
  <resume-signal>Escribir "aprobado" si el flujo funciona, o describir el problema encontrado</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compila sin errores en frontend y backend
- `handleSelectTemplate` incluye `fecha: yyyyMmDd(selectedDate)` en el dto
- Checkpoint humano confirma flujo end-to-end
</verification>

<success_criteria>
Al abrir el modal desde un turno FINALIZADO histórico y crear una nueva entrada HC, la entrada queda registrada con la fecha del día del turno seleccionado. El modal muestra el indicador de inmutabilidad legal en entradas existentes.
</success_criteria>

<output>
After completion, create `.planning/phases/21-agenda-widget-modal-hc/21-03-SUMMARY.md`
</output>
