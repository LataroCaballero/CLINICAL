---
phase: 22-schema-foundation
plan: 03
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - backend/src/modules/pacientes/dto/update-flujo.dto.ts
  - backend/src/modules/pacientes/pacientes.service.ts
  - backend/src/modules/pacientes/pacientes.controller.ts
autonomous: true
requirements: [TIPOS-02]

must_haves:
  truths:
    - "PATCH /pacientes/:id/flujo acepta { flujo: 'CIRUGIA' | 'TRATAMIENTO' | 'PENDIENTE' } y persiste el valor"
    - "El endpoint rechaza valores inválidos con 400 (Bad Request)"
    - "El endpoint devuelve 404 si el paciente no existe"
    - "Solo ADMIN, PROFESIONAL y SECRETARIA pueden acceder al endpoint"
    - "FACTURADOR no puede acceder al endpoint (rol excluido explícitamente)"
  artifacts:
    - path: "backend/src/modules/pacientes/dto/update-flujo.dto.ts"
      provides: "DTO con @IsEnum(FlujoPaciente) para validar el body"
      exports: ["UpdateFlujoDto"]
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "Método updateFlujo(id, flujo) con verificación de existencia + update"
      contains: "updateFlujo"
    - path: "backend/src/modules/pacientes/pacientes.controller.ts"
      provides: "Ruta PATCH :id/flujo con @Auth a nivel de método"
      contains: "flujo"
  key_links:
    - from: "PATCH /pacientes/:id/flujo"
      to: "PacientesService.updateFlujo()"
      via: "Controller delega a service"
      pattern: "updateFlujo"
    - from: "UpdateFlujoDto.flujo"
      to: "@IsEnum(FlujoPaciente)"
      via: "class-validator global ValidationPipe"
      pattern: "IsEnum.*FlujoPaciente"
---

<objective>
Crear el endpoint PATCH /pacientes/:id/flujo completo: DTO con validación enum, método de servicio con verificación de existencia, y ruta de controller con @Auth a nivel de método para restringir a ADMIN + PROFESIONAL + SECRETARIA.

Purpose: Permite clasificar o reclasificar manualmente el flujo de un paciente desde cualquier parte de la UI. Base para el banner LiveTurno (Phase 24) y la corrección de clasificaciones erróneas.
Output: Endpoint funcional accesible por los 3 roles autorizados.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-schema-foundation/22-02-SUMMARY.md
@.planning/phases/22-schema-foundation/22-CONTEXT.md

<interfaces>
<!-- Patrón establecido en pacientes.controller.ts (línea ~183-189) — template directo -->

// Controller — PATCH :id/temperatura (template a replicar para /flujo)
@Patch(':id/temperatura')
updateTemperatura(
  @Param('id') id: string,
  @Body() body: { temperatura: TemperaturaPaciente },
) {
  return this.pacientesService.updateTemperatura(id, body.temperatura);
}

// El controller tiene @Auth('ADMIN', 'PROFESIONAL', 'SECRETARIA', 'FACTURADOR') a nivel de clase.
// El nuevo endpoint /flujo excluye FACTURADOR → necesita @Auth a nivel de método.
// Verificar si el decorator @Auth permite override a nivel de método en este proyecto.
// Si no, usar @UseGuards + @Roles desde common/ o wrapping manual.

<!-- Patrón de DTO existente — backend/src/modules/pacientes/dto/update-whatsapp-opt-in.dto.ts -->
import { IsBoolean } from 'class-validator';
export class UpdateWhatsappOptInDto {
  @IsBoolean()
  optIn: boolean;
}

<!-- Método en service (template: updateTemperatura) -->
async updateTemperatura(id: string, temperatura: TemperaturaPaciente) {
  const exists = await this.prisma.paciente.findUnique({
    where: { id },
    select: { id: true },
  });
  if (!exists) throw new NotFoundException('Paciente no encontrado');
  return this.prisma.paciente.update({
    where: { id },
    data: { temperatura },
  });
}

<!-- Imports actuales en pacientes.service.ts — ya tiene EtapaCRM, TemperaturaPaciente desde @prisma/client -->
<!-- Añadir FlujoPaciente al mismo import -->

<!-- Imports en pacientes.controller.ts — ya tiene TemperaturaPaciente, EtapaCRM desde @prisma/client -->
<!-- Añadir FlujoPaciente + UpdateFlujoDto al mismo import -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear UpdateFlujoDto y método updateFlujo en service</name>
  <files>
    backend/src/modules/pacientes/dto/update-flujo.dto.ts
    backend/src/modules/pacientes/pacientes.service.ts
  </files>
  <action>
**A) Crear DTO `backend/src/modules/pacientes/dto/update-flujo.dto.ts`:**

```typescript
import { IsEnum } from 'class-validator';
import { FlujoPaciente } from '@prisma/client';

export class UpdateFlujoDto {
  @IsEnum(FlujoPaciente)
  flujo: FlujoPaciente;
}
```

Notas:
- `@IsEnum(FlujoPaciente)` valida que el valor sea uno de: 'CIRUGIA', 'TRATAMIENTO', 'PENDIENTE'
- El endpoint no acepta `null` — si en el futuro se necesita limpiar el flujo, habrá un endpoint separado
- `FlujoPaciente` se importa desde `@prisma/client` (disponible tras la migración del Plan 02)

**B) Añadir método `updateFlujo` en `backend/src/modules/pacientes/pacientes.service.ts`:**

1. Añadir `FlujoPaciente` al import de `@prisma/client` (junto a `EtapaCRM`, `TemperaturaPaciente`, etc.)

2. Añadir al final de la clase `PacientesService` (después de `updateListaEspera` o similar):

```typescript
async updateFlujo(id: string, flujo: FlujoPaciente): Promise<Paciente> {
  const exists = await this.prisma.paciente.findUnique({
    where: { id },
    select: { id: true },
  });
  if (!exists) throw new NotFoundException('Paciente no encontrado');

  return this.prisma.paciente.update({
    where: { id },
    data: { flujo },
  });
}
```

Notas:
- El método sigue exactamente el mismo patrón que `updateTemperatura` (findUnique + NotFoundException + update)
- NO usar `updatePacienteSection` — ese método usa `data: Record<string, any>` sin tipado; el nuevo método es fuertemente tipado
- `NotFoundException` ya está importado en el service (usado por métodos existentes)
  </action>
  <verify>
    <automated>ls /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend/src/modules/pacientes/dto/update-flujo.dto.ts && grep "updateFlujo" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend/src/modules/pacientes/pacientes.service.ts</automated>
  </verify>
  <done>
- Existe el archivo `dto/update-flujo.dto.ts` con `@IsEnum(FlujoPaciente)`
- `pacientes.service.ts` tiene el método `async updateFlujo(id: string, flujo: FlujoPaciente)`
- El método lanza `NotFoundException` si el paciente no existe
  </done>
</task>

<task type="auto">
  <name>Task 2: Añadir ruta PATCH :id/flujo al controller con @Auth restrictivo</name>
  <files>backend/src/modules/pacientes/pacientes.controller.ts</files>
  <action>
Editar `backend/src/modules/pacientes/pacientes.controller.ts`.

**Paso 1 — Verificar cómo funciona @Auth a nivel de método en este proyecto:**

Antes de escribir código, leer la definición del decorator `@Auth` en `backend/src/common/` para determinar si soporta override a nivel de método sobre el guard de clase. Ejecutar:

```bash
grep -r "export.*Auth\|SetMetadata\|Reflector" backend/src/common/ | head -20
```

**Paso 2 — Añadir imports:**

Añadir al bloque de imports del controller:
- `FlujoPaciente` desde `@prisma/client` (si no está ya)
- `UpdateFlujoDto` desde `./dto/update-flujo.dto`
- `Auth` decorator ya importado (está en uso a nivel de clase)

**Paso 3 — Añadir el método al controller:**

Añadir antes del cierre de la clase `PacientesController` (después del último @Patch existente):

```typescript
// Flujo — Clasificar o reclasificar el flujo del paciente
@Auth('ADMIN', 'PROFESIONAL', 'SECRETARIA')
@Patch(':id/flujo')
updateFlujo(
  @Param('id') id: string,
  @Body() dto: UpdateFlujoDto,
) {
  return this.pacientesService.updateFlujo(id, dto.flujo);
}
```

**Paso 4 — Si @Auth a nivel de método NO soporta override:**

Si el decorator no permite override (el guard de clase ya tiene FACTURADOR), investigar alternativas en el orden:
1. Ver si hay un decorator `@Roles` separado en common/
2. Ver si el guard usa `Reflector.getAllAndOverride` (que daría prioridad al método sobre la clase)
3. Si ninguna funciona, documentar la limitación en el SUMMARY y dejar el método con el guard de clase (FACTURADOR incluido) para resolver en un fix posterior

**Decisión del usuario (CONTEXT.md):** ADMIN + PROFESIONAL + SECRETARIA. FACTURADOR explícitamente excluido. El ejecutor DEBE intentar implementar esta restricción; si hay una limitación técnica, documentarla claramente.

Notas adicionales:
- El decorador `@Patch(':id/flujo')` debe ir ANTES de cualquier `@Patch(':id/:section')` si existe uno, para evitar conflictos de routing (Express/Fastify matchea el más específico primero si está definido antes)
- No añadir `@ApiOperation` ni Swagger decorators — no son parte del proyecto actual
  </action>
  <verify>
    <automated>grep -A5 "Patch.*id.*flujo" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend/src/modules/pacientes/pacientes.controller.ts</automated>
  </verify>
  <done>
- El controller tiene `@Patch(':id/flujo')` con el método `updateFlujo`
- El método usa `UpdateFlujoDto` tipado
- Se intenta restringir a ADMIN + PROFESIONAL + SECRETARIA (con nota en SUMMARY si no fue posible)
- `PATCH /pacientes/[id-valido]/flujo` con body `{ "flujo": "CIRUGIA" }` devuelve 200 con el paciente actualizado
- `PATCH /pacientes/[id-valido]/flujo` con body `{ "flujo": "INVALIDO" }` devuelve 400
- `PATCH /pacientes/[id-inexistente]/flujo` con body válido devuelve 404
  </done>
</task>

</tasks>

<verification>
```bash
# Build TypeScript completo para confirmar sin errores de tipos
cd backend && npm run build 2>&1 | tail -30

# Verificar que el endpoint está registrado
grep -n "flujo" src/modules/pacientes/pacientes.controller.ts
grep -n "flujo" src/modules/pacientes/pacientes.service.ts
ls src/modules/pacientes/dto/update-flujo.dto.ts

# Test funcional con curl (requiere servidor corriendo y token válido)
# curl -X PATCH http://localhost:3001/pacientes/{id}/flujo \
#   -H "Authorization: Bearer {token}" \
#   -H "Content-Type: application/json" \
#   -d '{"flujo": "CIRUGIA"}'
```
</verification>

<success_criteria>
- `npm run build` en backend pasa sin errores TypeScript
- `dto/update-flujo.dto.ts` existe con `@IsEnum(FlujoPaciente)`
- `pacientes.service.ts` tiene `updateFlujo(id, flujo)` con NotFoundException para paciente inexistente
- `pacientes.controller.ts` tiene `PATCH :id/flujo` delegando a `updateFlujo`
- Valores inválidos en body → 400 (class-validator ValidationPipe global rechaza)
- ID de paciente inexistente → 404
- Roles FACTURADOR y PACIENTE → 403 (restringido por @Auth)
</success_criteria>

<output>
After completion, create `.planning/phases/22-schema-foundation/22-03-SUMMARY.md`
</output>
