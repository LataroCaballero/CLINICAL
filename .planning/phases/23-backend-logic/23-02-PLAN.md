---
phase: 23-backend-logic
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/pacientes/pacientes.service.ts
  - backend/src/modules/reportes/services/crm-dashboard.service.ts
  - backend/src/modules/reportes/services/crm-metrics.service.ts
autonomous: true
requirements:
  - CRM-01
  - CRM-02
  - CRM-03

must_haves:
  truths:
    - "El kanban CRM muestra solo pacientes con flujo = CIRUGIA y pacientes legacy con flujo = null"
    - "La lista de acción diaria muestra solo pacientes con flujo = CIRUGIA y flujo = null"
    - "getFunnelSnapshot, getKpis, getMotivosPerdida, getPipelineIncome, getCoordinatorPerformance filtran por flujo CIRUGIA o null"
    - "getCRMMetrics filtra pacientes por flujo CIRUGIA o null"
    - "Pacientes con flujo = TRATAMIENTO no aparecen en ninguna vista CRM"
    - "Pacientes legacy (flujo IS NULL) con cualquier etapaCRM siguen visibles (sin condición extra sobre etapaCRM)"
  artifacts:
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "getKanban() y getListaAccion() con flujo filter"
      contains: "FlujoPaciente.CIRUGIA"
    - path: "backend/src/modules/reportes/services/crm-dashboard.service.ts"
      provides: "5 métodos CRM con flujo filter"
      contains: "FlujoPaciente"
    - path: "backend/src/modules/reportes/services/crm-metrics.service.ts"
      provides: "getCRMMetrics() con flujo filter"
      contains: "FlujoPaciente"
  key_links:
    - from: "getKanban() WHERE"
      to: "Paciente.flujo"
      via: "OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }]"
      pattern: "OR.*flujo.*CIRUGIA"
    - from: "getFunnelSnapshot() groupBy WHERE"
      to: "Paciente.flujo"
      via: "OR flujo filter en el groupBy principal"
      pattern: "OR.*flujo.*CIRUGIA"
---

<objective>
Agregar el filtro `WHERE (flujo = 'CIRUGIA' OR flujo IS NULL)` a todas las vistas CRM: getKanban, getListaAccion, y los 5+1 métodos de crm-dashboard.service.ts / crm-metrics.service.ts.

Purpose: Los pacientes con flujo = TRATAMIENTO no deben contaminar el embudo CRM de cirugías. Los pacientes legacy (flujo IS NULL) permanecen visibles en todas las vistas para no vaciar el kanban al hacer deploy de v1.4.

Output: 3 archivos de servicio modificados, filter uniforme en 8 call sites, build verde.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-backend-logic/23-CONTEXT.md
@.planning/phases/23-backend-logic/23-RESEARCH.md
</context>

<interfaces>
<!-- Estado actual de los 3 archivos modificados. Extraído del codebase. -->
<!-- El executor NO necesita explorar el repo — toda la información está aquí. -->

**pacientes.service.ts** — imports actuales (FlujoPaciente YA ESTÁ importado):
```typescript
import {
  EstadoPaciente,
  RolUsuario,
  EtapaCRM,
  TemperaturaPaciente,
  FlujoPaciente,
  // ...otros
} from '@prisma/client';
```

**pacientes.service.ts** — getKanban() WHERE actual (línea 619):
```typescript
where: { profesionalId },
```

**pacientes.service.ts** — getListaAccion() WHERE actual (línea 812-819):
```typescript
where: {
  profesionalId,
  etapaCRM: { notIn: ['CONFIRMADO', 'PERDIDO'] as EtapaCRM[] },
  NOT: {
    contactos: { some: { fecha: { gte: hoyInicio } } },
  },
},
```

---

**crm-dashboard.service.ts** — imports actuales (FlujoPaciente NO está importado):
```typescript
import { EtapaCRM, TemperaturaPaciente, EstadoPresupuesto } from '@prisma/client';
// Necesita agregar FlujoPaciente
```

**getFunnelSnapshot()** — dos groupBy afectados:
1. groupBy principal: `where: { profesionalId }` (línea 54)
2. motivosGrupos groupBy: `where: { profesionalId, etapaCRM: EtapaCRM.PERDIDO, motivoPerdida: { not: null } }` (líneas 88-93)

**getKpis()** — 3 `prisma.paciente.count` afectados:
1. nuevos: `where: { profesionalId, createdAt: { gte, lte } }` (línea 113)
2. confirmados: `where: { profesionalId, etapaCRM: EtapaCRM.CONFIRMADO, updatedAt: { gte, lte } }` (línea 119)
3. totalActivos: `where: { profesionalId, etapaCRM: { not: EtapaCRM.PERDIDO } }` (línea 126)

**getMotivosPerdida()** — 1 groupBy:
`where: { profesionalId, etapaCRM: EtapaCRM.PERDIDO, updatedAt: { gte, lte } }` (línea 152)

**getPipelineIncome()** — presupuesto findMany con relación paciente:
```typescript
where: {
  profesionalId,
  estado: EstadoPresupuesto.ENVIADO,
  fechaEnviado: { gte, lte },
  paciente: { temperatura: TemperaturaPaciente.CALIENTE },
}
```
Filter va dentro de `paciente: { temperatura: ..., OR: [...] }`.

**getCoordinatorPerformance()** — contactoLog findMany:
`where: { profesionalId, fecha: { gte, lte } }` (línea 201)
Filter va como nested: `paciente: { OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }] }`.

---

**crm-metrics.service.ts** — imports actuales (FlujoPaciente NO está importado):
```typescript
import { EtapaCRM, EstadoPresupuesto } from '@prisma/client';
// Necesita agregar FlujoPaciente
```

**getCRMMetrics()** — paciente findMany WHERE actual (línea 26):
```typescript
where: { profesionalId },
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Agregar flujo filter a pacientes.service.ts (getKanban + getListaAccion)</name>
  <files>backend/src/modules/pacientes/pacientes.service.ts</files>
  <action>
FlujoPaciente ya está importado en pacientes.service.ts — no agregar import duplicado.

**getKanban() — modificar WHERE** (línea ~619):
```typescript
// Antes:
where: { profesionalId },

// Después:
where: {
  profesionalId,
  OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }],
},
```

**getListaAccion() — modificar WHERE** (línea ~812):
```typescript
// Antes:
where: {
  profesionalId,
  etapaCRM: { notIn: ['CONFIRMADO', 'PERDIDO'] as EtapaCRM[] },
  NOT: {
    contactos: { some: { fecha: { gte: hoyInicio } } },
  },
},

// Después — agregar OR al mismo nivel que las otras condiciones:
where: {
  profesionalId,
  etapaCRM: { notIn: ['CONFIRMADO', 'PERDIDO'] as EtapaCRM[] },
  NOT: {
    contactos: { some: { fecha: { gte: hoyInicio } } },
  },
  OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }],
},
```

Nota de semántica: un paciente PERDIDO + flujo IS NULL satisface el `OR flujo` pero falla el `etapaCRM notIn PERDIDO` — es excluido correctamente por el AND implícito de Prisma. Comportamiento esperado.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build verde. getKanban y getListaAccion tienen OR flujo filter. No hay imports duplicados de FlujoPaciente.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Agregar flujo filter a crm-dashboard.service.ts y crm-metrics.service.ts</name>
  <files>
    backend/src/modules/reportes/services/crm-dashboard.service.ts
    backend/src/modules/reportes/services/crm-metrics.service.ts
  </files>
  <action>
**crm-dashboard.service.ts — agregar FlujoPaciente al import** (línea 3):
```typescript
import { EtapaCRM, TemperaturaPaciente, EstadoPresupuesto, FlujoPaciente } from '@prisma/client';
```

**getFunnelSnapshot() — 2 groupBy**:

1. groupBy principal (línea ~54):
```typescript
where: {
  profesionalId,
  OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }],
},
```

2. motivosGrupos groupBy (líneas ~88-93) — este filtra por PERDIDO, agregar OR igualmente:
```typescript
where: {
  profesionalId,
  etapaCRM: EtapaCRM.PERDIDO,
  motivoPerdida: { not: null },
  OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }],
},
```

**getKpis() — 3 count**:

1. nuevos:
```typescript
where: {
  profesionalId,
  createdAt: { gte: inicio, lte: fin },
  OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }],
},
```

2. confirmados:
```typescript
where: {
  profesionalId,
  etapaCRM: EtapaCRM.CONFIRMADO,
  updatedAt: { gte: inicio, lte: fin },
  OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }],
},
```

3. totalActivos:
```typescript
where: {
  profesionalId,
  etapaCRM: { not: EtapaCRM.PERDIDO },
  OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }],
},
```

**getMotivosPerdida() — 1 groupBy**:
```typescript
where: {
  profesionalId,
  etapaCRM: EtapaCRM.PERDIDO,
  updatedAt: { gte: inicio, lte: fin },
  OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }],
},
```

**getPipelineIncome() — presupuesto findMany, filter nested en paciente**:
```typescript
where: {
  profesionalId,
  estado: EstadoPresupuesto.ENVIADO,
  fechaEnviado: { gte: inicio, lte: fin },
  paciente: {
    temperatura: TemperaturaPaciente.CALIENTE,
    OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }],
  },
},
```

**getCoordinatorPerformance() — contactoLog findMany, filter nested en paciente**:
```typescript
where: {
  profesionalId,
  fecha: { gte: inicio, lte: fin },
  paciente: { OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }] },
},
```

---

**crm-metrics.service.ts — agregar FlujoPaciente al import** (línea 3):
```typescript
import { EtapaCRM, EstadoPresupuesto, FlujoPaciente } from '@prisma/client';
```

**getCRMMetrics() — paciente findMany WHERE** (línea ~26):
```typescript
where: {
  profesionalId,
  OR: [{ flujo: FlujoPaciente.CIRUGIA }, { flujo: null }],
},
```
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build verde. Los 8 call sites tienen OR flujo filter. FlujoPaciente importado en crm-dashboard.service.ts y crm-metrics.service.ts. Ningún método de reportes-dashboard.service.ts fue modificado.</done>
</task>

</tasks>

<verification>
1. `cd backend && npm run build` — 0 errores TypeScript
2. `grep -rn "OR.*flujo.*CIRUGIA\|flujo.*null" backend/src/modules/pacientes/pacientes.service.ts backend/src/modules/reportes/services/crm-dashboard.service.ts backend/src/modules/reportes/services/crm-metrics.service.ts` — debe mostrar al menos 8 líneas
3. `grep -n "reportes-dashboard\|getDashboardKPIs" backend/src/modules/reportes/services/reportes-dashboard.service.ts` — confirmar que ese archivo NO fue modificado
</verification>

<success_criteria>
- Build TypeScript sin errores
- getKanban() y getListaAccion() tienen OR flujo filter en su WHERE
- Los 5 métodos de crm-dashboard.service.ts tienen OR flujo filter (6 call sites en total)
- getCRMMetrics() tiene OR flujo filter
- Total: 8 call sites modificados
- FlujoPaciente importado en crm-dashboard.service.ts y crm-metrics.service.ts
- reportes-dashboard.service.ts NO modificado
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-logic/23-02-SUMMARY.md`
</output>
