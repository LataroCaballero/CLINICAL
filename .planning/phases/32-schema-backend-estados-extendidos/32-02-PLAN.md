---
phase: 32-schema-backend-estados-extendidos
plan: 02
type: execute
wave: 2
depends_on:
  - "32-01"
files_modified:
  - backend/src/modules/turnos/turnos.service.ts
  - backend/src/modules/turnos/turnos.controller.ts
  - frontend/src/app/dashboard/turnos/CalendarGrid.tsx
  - frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx
  - frontend/src/app/dashboard/turnos/page.tsx
  - frontend/src/app/dashboard/components/QuickAppointment.tsx
  - frontend/src/app/dashboard/components/UpcomingAppointments.tsx
  - frontend/src/app/dashboard/components/NextPatientCard.tsx
autonomous: true
requirements:
  - EST-02
  - EST-03
  - EST-04
  - EST-05
must_haves:
  truths:
    - "PATCH /:id/marcar-en-espera transiciona el turno de PENDIENTE o CONFIRMADO a EN_ESPERA"
    - "PATCH /:id/marcar-ausente transiciona el turno de PENDIENTE, EN_ESPERA o CONFIRMADO a AUSENTE"
    - "PATCH /:id/reactivar transiciona el turno de AUSENTE a PENDIENTE"
    - "iniciarSesion establece el estado del turno a SIENDO_ATENDIDO (no CONFIRMADO)"
    - "Los 3 nuevos endpoints retornan 400 si el turno ya está en estado terminal (CANCELADO, FINALIZADO)"
    - "El frontend compila sin errores de TypeScript — los tipos inline incluyen EN_ESPERA y SIENDO_ATENDIDO"
  artifacts:
    - path: "backend/src/modules/turnos/turnos.service.ts"
      provides: "Métodos marcarEnEspera, marcarAusente, reactivar + iniciarSesion actualizado"
      exports: ["marcarEnEspera", "marcarAusente", "reactivar"]
    - path: "backend/src/modules/turnos/turnos.controller.ts"
      provides: "Endpoints PATCH /:id/marcar-en-espera, /:id/marcar-ausente, /:id/reactivar"
  key_links:
    - from: "backend/src/modules/turnos/turnos.controller.ts"
      to: "turnos.service.ts"
      via: "this.turnosService.marcarEnEspera(id)"
      pattern: "marcarEnEspera|marcarAusente|reactivar"
    - from: "backend/src/modules/turnos/turnos.service.ts"
      to: "prisma.turno.update"
      via: "estado: EstadoTurno.EN_ESPERA"
      pattern: "EstadoTurno\\.EN_ESPERA|EstadoTurno\\.SIENDO_ATENDIDO"
---

<objective>
Implementar los tres endpoints de transición de estado (marcar-en-espera, marcar-ausente, reactivar) en el módulo de turnos, actualizar iniciarSesion para establecer SIENDO_ATENDIDO, y actualizar los tipos inline del frontend para incluir los nuevos estados.

Purpose: Completa el ciclo operativo del backend para EST-02..05. Las fases 33 y 34 dependen de que estos endpoints existan y de que el frontend compile con los nuevos estados.

Output: 3 nuevos endpoints PATCH, iniciarSesion corregido, tipos frontend actualizados.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-schema-backend-estados-extendidos/32-CONTEXT.md
@.planning/phases/32-schema-backend-estados-extendidos/32-01-SUMMARY.md

<interfaces>
<!-- Patrones de implementación existentes en turnos.service.ts -->
<!-- Estos son los patrones a replicar para los nuevos métodos -->

Patrón de servicio (confirmarTurno como referencia):
```typescript
async confirmarTurno(turnoId: string) {
  const turno = await this.prisma.turno.findUnique({
    where: { id: turnoId },
    select: { id: true, estado: true },
  });
  if (!turno) {
    throw new NotFoundException('Turno no encontrado.');
  }
  if (
    turno.estado === EstadoTurno.CANCELADO ||
    turno.estado === EstadoTurno.FINALIZADO
  ) {
    throw new BadRequestException(
      `No se puede confirmar un turno en estado ${turno.estado}.`,
    );
  }
  return this.prisma.turno.update({
    where: { id: turnoId },
    data: { estado: EstadoTurno.CONFIRMADO },
  });
}
```

Patrón de controller (cancelar como referencia):
```typescript
@Patch(':id/cancelar')
cancelar(@Param('id') id: string) {
  return this.turnosService.cancelarTurno(id);
}
```

El controlador tiene a nivel de clase: @Auth('ADMIN', 'PROFESIONAL', 'SECRETARIA')
Los nuevos endpoints usan el mismo rol — no necesitan decorador adicional.

iniciarSesion actual (línea ~721-738, solo la parte relevante):
```typescript
return this.prisma.turno.update({
  where: { id: turnoId },
  data: {
    inicioReal,
    estado: EstadoTurno.CONFIRMADO,  // <-- cambiar a SIENDO_ATENDIDO
  },
  include: { ... }
});
```

Tipos inline en frontend (6 archivos tienen esta misma unión):
```typescript
estado: "PENDIENTE" | "CONFIRMADO" | "CANCELADO" | "AUSENTE" | "FINALIZADO";
// Cambiar a:
estado: "PENDIENTE" | "CONFIRMADO" | "CANCELADO" | "AUSENTE" | "FINALIZADO" | "EN_ESPERA" | "SIENDO_ATENDIDO";
```

CalendarGrid.tsx switch de colores (líneas 133-137) — agregar casos:
```typescript
case "EN_ESPERA":    return "#A78BFA"; // violet — paciente llegó, espera ser llamado
case "SIENDO_ATENDIDO": return "#0EA5E9"; // sky blue — en consulta activa
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar marcarEnEspera, marcarAusente y reactivar en TurnosService</name>
  <files>backend/src/modules/turnos/turnos.service.ts</files>
  <behavior>
    - marcarEnEspera: origen válido PENDIENTE o CONFIRMADO → estado EN_ESPERA; origen inválido (AUSENTE, FINALIZADO, CANCELADO, ya EN_ESPERA) → BadRequestException
    - marcarAusente: origen válido PENDIENTE, EN_ESPERA o CONFIRMADO → estado AUSENTE; origen FINALIZADO, CANCELADO o ya AUSENTE → BadRequestException
    - reactivar: origen válido AUSENTE solamente → estado PENDIENTE; origen CANCELADO o FINALIZADO → BadRequestException; origen PENDIENTE, CONFIRMADO, EN_ESPERA → BadRequestException (no es AUSENTE)
    - Todos los métodos: turno no encontrado → NotFoundException
  </behavior>
  <action>
    En backend/src/modules/turnos/turnos.service.ts, agregar tres métodos nuevos después de confirmarTurno (~línea 290). Seguir exactamente el patrón de confirmarTurno:

    **marcarEnEspera(turnoId: string)**
    - findUnique donde id = turnoId, select { id, estado }
    - NotFoundException si no existe
    - BadRequestException si estado es CANCELADO, FINALIZADO, o ya EN_ESPERA
    - BadRequestException si estado no es PENDIENTE ni CONFIRMADO (mensaje: `No se puede marcar en espera un turno en estado ${turno.estado}.`)
    - prisma.turno.update → estado: EstadoTurno.EN_ESPERA, sin include (igual que confirmarTurno)

    **marcarAusente(turnoId: string)**
    - findUnique donde id = turnoId, select { id, estado }
    - NotFoundException si no existe
    - BadRequestException si estado es CANCELADO, FINALIZADO, o ya AUSENTE
    - BadRequestException si estado no es PENDIENTE, EN_ESPERA ni CONFIRMADO (mensaje: `No se puede marcar ausente un turno en estado ${turno.estado}.`)
    - prisma.turno.update → estado: EstadoTurno.AUSENTE, sin include

    **reactivar(turnoId: string)**
    - findUnique donde id = turnoId, select { id, estado }
    - NotFoundException si no existe
    - BadRequestException si estado es CANCELADO o FINALIZADO
    - BadRequestException si estado no es AUSENTE (mensaje: `Solo se puede reactivar un turno en estado AUSENTE. Estado actual: ${turno.estado}.`)
    - prisma.turno.update → estado: EstadoTurno.PENDIENTE, sin include

    TAMBIÉN en iniciarSesion (~línea 721): cambiar `estado: EstadoTurno.CONFIRMADO` a `estado: EstadoTurno.SIENDO_ATENDIDO`. Esta es la única línea que cambia en iniciarSesion — no tocar nada más de ese método.

    No agregar DTOs — ninguno de los métodos existentes comparables (cancelar, confirmar) los usa.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npx tsc --noEmit 2>&1 | grep -E "turnos\.service|error TS" | head -20</automated>
  </verify>
  <done>
    - tsc --noEmit no reporta errores en turnos.service.ts
    - grep confirma que los 3 métodos existen: `grep -n "marcarEnEspera\|marcarAusente\|reactivar" backend/src/modules/turnos/turnos.service.ts`
    - grep confirma que iniciarSesion usa SIENDO_ATENDIDO: `grep "SIENDO_ATENDIDO" backend/src/modules/turnos/turnos.service.ts`
  </done>
</task>

<task type="auto">
  <name>Task 2: Agregar endpoints al controller y actualizar tipos frontend</name>
  <files>
    backend/src/modules/turnos/turnos.controller.ts,
    frontend/src/app/dashboard/turnos/CalendarGrid.tsx,
    frontend/src/app/dashboard/turnos/AppointmentDetailModal.tsx,
    frontend/src/app/dashboard/turnos/page.tsx,
    frontend/src/app/dashboard/components/QuickAppointment.tsx,
    frontend/src/app/dashboard/components/UpcomingAppointments.tsx,
    frontend/src/app/dashboard/components/NextPatientCard.tsx
  </files>
  <action>
    **turnos.controller.ts** — Agregar 3 endpoints después de @Patch(':id/confirmar') (~línea 72):

    ```typescript
    @Patch(':id/marcar-en-espera')
    marcarEnEspera(@Param('id') id: string) {
      return this.turnosService.marcarEnEspera(id);
    }

    @Patch(':id/marcar-ausente')
    marcarAusente(@Param('id') id: string) {
      return this.turnosService.marcarAusente(id);
    }

    @Patch(':id/reactivar')
    reactivar(@Param('id') id: string) {
      return this.turnosService.reactivar(id);
    }
    ```

    No agregar decoradores de rol individuales — el @Auth a nivel de clase ya cubre ADMIN, PROFESIONAL, SECRETARIA.

    **Frontend — 6 archivos con tipos inline**: En cada archivo, buscar la línea con el tipo inline de estado y agregar "EN_ESPERA" y "SIENDO_ATENDIDO" al union type. Archivos y líneas aproximadas:
    - CalendarGrid.tsx línea 25: `estado: "PENDIENTE" | "CONFIRMADO" | "CANCELADO" | "AUSENTE" | "FINALIZADO"`
    - AppointmentDetailModal.tsx línea 60: mismo union
    - page.tsx línea 51: mismo union
    - QuickAppointment.tsx: referencia inline sin definición de tipo separada (no tiene declaración de tipo con ese union, pero usa los valores — verificar y actualizar si hay un switch/case)
    - UpcomingAppointments.tsx línea 44: mismo union
    - NextPatientCard.tsx línea 18: mismo union

    En todos los archivos que tienen el union type, reemplazar por:
    `estado: "PENDIENTE" | "CONFIRMADO" | "CANCELADO" | "AUSENTE" | "FINALIZADO" | "EN_ESPERA" | "SIENDO_ATENDIDO"`

    **CalendarGrid.tsx — switch de colores** (líneas ~133-137): Agregar dos casos después del case "FINALIZADO":
    ```typescript
    case "EN_ESPERA":       return "#A78BFA"; // violet
    case "SIENDO_ATENDIDO": return "#0EA5E9"; // sky blue
    ```

    No hay que agregar lógica de renderizado adicional para los nuevos estados en este plan — eso corresponde a Phase 33 (Widget Agenda Operativo). Solo asegurar que los tipos compilan y el switch de colores no falla con los nuevos valores.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npx tsc --noEmit 2>&1 | grep -E "EN_ESPERA|SIENDO_ATENDIDO|error TS" | head -20</automated>
  </verify>
  <done>
    - `grep -n "SIENDO_ATENDIDO" backend/src/modules/turnos/turnos.controller.ts` muestra los 3 nuevos métodos (o que al menos están conectados)
    - `grep -rn "EN_ESPERA" frontend/src/app/dashboard/turnos/ frontend/src/app/dashboard/components/` confirma que los 6 archivos tienen el tipo actualizado
    - `cd frontend && npx tsc --noEmit` termina sin errores de tipo relacionados con EstadoTurno
    - `cd backend && npm run build` compila sin errores
  </done>
</task>

</tasks>

<verification>
Smoke tests manuales opcionales (si el entorno está disponible):
- `curl -X PATCH http://localhost:3001/turnos/{id}/marcar-en-espera -H "Authorization: Bearer {token}"` → 200 con turno en EN_ESPERA
- `curl -X PATCH http://localhost:3001/turnos/{id}/marcar-ausente -H "Authorization: Bearer {token}"` → 200 con turno en AUSENTE
- `curl -X PATCH http://localhost:3001/turnos/{id}/reactivar -H "Authorization: Bearer {token}"` → 200 con turno en PENDIENTE
- `curl -X POST http://localhost:3001/turnos/{id}/iniciar-sesion -H "Authorization: Bearer {token}"` → turno.estado === "SIENDO_ATENDIDO"

Build verification:
- `cd backend && npm run build` → exit 0
- `cd frontend && npx tsc --noEmit` → exit 0
</verification>

<success_criteria>
1. Existen 3 nuevos endpoints en el controller: PATCH /:id/marcar-en-espera, /:id/marcar-ausente, /:id/reactivar
2. iniciarSesion establece SIENDO_ATENDIDO (confirmado via grep en el service)
3. Las reglas de la máquina de estados se aplican: estados fuente incorrectos retornan 400
4. El build de backend compila sin errores
5. El frontend compila sin errores de TypeScript — los tipos inline incluyen EN_ESPERA y SIENDO_ATENDIDO
</success_criteria>

<output>
After completion, create `.planning/phases/32-schema-backend-estados-extendidos/32-02-SUMMARY.md`
</output>
