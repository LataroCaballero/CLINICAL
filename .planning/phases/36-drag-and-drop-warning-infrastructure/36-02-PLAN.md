---
phase: 36-drag-and-drop-warning-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "36-01"
files_modified:
  - frontend/src/components/crm/KanbanBoard.tsx
autonomous: false
requirements:
  - CRM-01
  - CRM-02
  - CRM-03

must_haves:
  truths:
    - "El usuario puede arrastrar una card a cualquier columna (excepto PERDIDO que sigue el flujo modal) y el movimiento se persiste"
    - "Al soltar en PRESUPUESTO_ENVIADO sin presupuesto, aparece toast amber 'No hay presupuesto enviado a este paciente' y el move se completa igual"
    - "Al soltar en CONFIRMADO sin presupuesto aceptado, aparece toast amber 'Ningún presupuesto fue aceptado — verificá antes de confirmar' y el move se completa igual"
    - "Si el backend falla, la card hace snap-back y aparece toast.error 'No se pudo guardar el movimiento. Intentá de nuevo.'"
    - "El snap-back funciona via onSettled limpiando pendingMoves — no se rompe con la incorporación del warning"
  artifacts:
    - path: "frontend/src/components/crm/KanbanBoard.tsx"
      provides: "handleDragEnd con warning logic integrado y texto de error actualizado"
      contains: "getEtapaWarning"
  key_links:
    - from: "frontend/src/components/crm/KanbanBoard.tsx"
      to: "frontend/src/lib/crm-warnings.ts"
      via: "import { getEtapaWarning }"
      pattern: "getEtapaWarning"
    - from: "KanbanBoard.tsx:handleDragEnd"
      to: "toast.warning"
      via: "if (warning) toast.warning(warning)"
      pattern: "toast\\.warning"
---

<objective>
Integrar la función `getEtapaWarning` en `handleDragEnd` de `KanbanBoard.tsx`, disparando el toast amber inmediatamente después del move optimista para PRESUPUESTO_ENVIADO y CONFIRMADO. Actualizar también el texto del `toast.error` en `onError` para que no mencione "requisitos" (el backend ya no tiene restricciones).

Purpose: Este es el único cambio visible para el usuario en Phase 36. Plan 01 creó los contratos; este plan los conecta al flujo de drag-and-drop.
Output: `KanbanBoard.tsx` con warning logic integrado en `handleDragEnd` + texto de error actualizado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-drag-and-drop-warning-infrastructure/36-CONTEXT.md
@.planning/phases/36-drag-and-drop-warning-infrastructure/36-RESEARCH.md
@.planning/phases/36-drag-and-drop-warning-infrastructure/36-01-SUMMARY.md

<interfaces>
<!-- Código actual de KanbanBoard.tsx — el executor modifica handleDragEnd precisamente -->

From frontend/src/components/crm/KanbanBoard.tsx — función handleDragEnd ACTUAL (antes de Phase 36):
```typescript
function handleDragEnd(event: DragEndEvent) {
  setActivePatient(null);
  setActiveColumn(null);

  const { over, active } = event;
  if (!over) return;

  const targetColumn = over.id as EtapaCRM;
  const { patient, fromColumn } = active.data.current as {
    patient: KanbanPatient;
    fromColumn: string;
  };

  if (targetColumn === fromColumn) return;

  if (targetColumn === "PERDIDO") {
    setPendingDrop({ pacienteId: patient.id, targetColumn });
    return;
  }

  // Mover optimistamente
  setPendingMoves((prev) => ({ ...prev, [patient.id]: targetColumn }));
  updateEtapa(
    { pacienteId: patient.id, etapaCRM: targetColumn },
    {
      onSettled: () =>
        setPendingMoves((prev) => {
          const next = { ...prev };
          delete next[patient.id];
          return next;
        }),
      onError: () =>
        toast.error("No se pudo mover el paciente. Verificá los requisitos."),
        // ^^^^^ CAMBIAR a: "No se pudo guardar el movimiento. Intentá de nuevo."
    }
  );
}
```

<!-- getEtapaWarning — creada en Plan 01, disponible para importar -->
From frontend/src/lib/crm-warnings.ts (creado en Plan 01):
```typescript
import { KanbanPatient, EtapaCRM } from "@/hooks/useCRMKanban";

export function getEtapaWarning(
  patient: KanbanPatient,
  targetEtapa: EtapaCRM
): string | null {
  if (targetEtapa === "PRESUPUESTO_ENVIADO" && patient.presupuesto === null) {
    return "No hay presupuesto enviado a este paciente";
  }
  if (targetEtapa === "CONFIRMADO" && patient.presupuesto?.estado !== "ACEPTADO") {
    return "Ningún presupuesto fue aceptado — verificá antes de confirmar";
  }
  return null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrar getEtapaWarning en handleDragEnd de KanbanBoard.tsx</name>
  <files>frontend/src/components/crm/KanbanBoard.tsx</files>
  <action>
    Hacer DOS cambios en `frontend/src/components/crm/KanbanBoard.tsx`:

    **Cambio 1 — Agregar import de getEtapaWarning** al bloque de imports existente, junto a los otros imports de `@/lib/`:
    ```typescript
    import { getEtapaWarning } from "@/lib/crm-warnings";
    ```

    **Cambio 2 — Modificar `handleDragEnd`**: reemplazar el bloque "Mover optimistamente" completo:

    ANTES:
    ```typescript
    // Mover optimistamente
    setPendingMoves((prev) => ({ ...prev, [patient.id]: targetColumn }));
    updateEtapa(
      { pacienteId: patient.id, etapaCRM: targetColumn },
      {
        onSettled: () =>
          setPendingMoves((prev) => {
            const next = { ...prev };
            delete next[patient.id];
            return next;
          }),
        onError: () =>
          toast.error("No se pudo mover el paciente. Verificá los requisitos."),
      }
    );
    ```

    DESPUÉS:
    ```typescript
    // Mover optimistamente
    setPendingMoves((prev) => ({ ...prev, [patient.id]: targetColumn }));

    // Warning no bloqueante — simultáneo con el move optimista (CRM-02, CRM-03)
    const warning = getEtapaWarning(patient, targetColumn);
    if (warning) toast.warning(warning);

    updateEtapa(
      { pacienteId: patient.id, etapaCRM: targetColumn },
      {
        onSettled: () =>
          setPendingMoves((prev) => {
            const next = { ...prev };
            delete next[patient.id];
            return next;
          }),
        onError: () =>
          toast.error("No se pudo guardar el movimiento. Intentá de nuevo."),
      }
    );
    ```

    IMPORTANTE — qué NO cambiar:
    - NO modificar `handleLossConfirm` (el texto `"No se pudo actualizar el estado"` en esa función es correcto)
    - NO modificar la lógica de PERDIDO (`setPendingDrop`)
    - NO mover el `onSettled` a `onError` — el rollback depende de `onSettled` siempre ejecutarse
    - NO agregar `onSuccess` callback
    - NO agregar lógica de deduplicación de toasts (deferred per CONTEXT.md)

    El toast debe dispararse ANTES del `updateEtapa` call (misma línea síncrona que `setPendingMoves`).
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npx tsc --noEmit 2>&1 | grep KanbanBoard || echo "NO_TYPE_ERRORS_IN_KANBAN"</automated>
  </verify>
  <done>
    KanbanBoard.tsx importa getEtapaWarning, llama a toast.warning con el resultado cuando no es null, y el toast.error en onError usa el texto actualizado. Verificar:
    - `grep "getEtapaWarning" frontend/src/components/crm/KanbanBoard.tsx` muestra 2 líneas (import + uso)
    - `grep "Intentá de nuevo" frontend/src/components/crm/KanbanBoard.tsx` muestra 1 línea
    - `grep "Verificá los requisitos" frontend/src/components/crm/KanbanBoard.tsx` muestra 0 líneas
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar warnings en kanban drag-and-drop</name>
  <files>frontend/src/components/crm/KanbanBoard.tsx</files>
  <action>Verificar manualmente en el browser que los warnings de drag-and-drop funcionan correctamente (ver how-to-verify).</action>
  <verify>
    <automated>grep -c 'getEtapaWarning' /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/components/crm/KanbanBoard.tsx</automated>
  </verify>
  <done>Los tres tests de drag-and-drop pasan (CRM-02, CRM-03, movimiento normal) y el checkpoint es aprobado por el usuario.</done>
  <what-built>
    Sistema completo de warnings no bloqueantes en el kanban drag-and-drop:
    - `frontend/src/lib/crm-warnings.ts`: función getEtapaWarning con los dos warnings requeridos
    - `frontend/src/hooks/useCRMKanban.ts`: KanbanPatient con campo flujo tipado
    - `frontend/src/components/crm/KanbanBoard.tsx`: handleDragEnd con warning integrado y texto de error actualizado
  </what-built>
  <how-to-verify>
    1. Iniciar frontend: `cd frontend && npm run dev`
    2. Navegar al kanban CRM
    3. **Test CRM-02:** Arrastrar un paciente SIN presupuesto a la columna "Presupuesto Enviado"
       - Esperado: la card se mueve a la columna destino Y aparece un toast amber con "No hay presupuesto enviado a este paciente"
       - El movimiento debe persistirse (no rollback)
    4. **Test CRM-03:** Arrastrar un paciente cuyo presupuesto NO está en estado ACEPTADO a la columna "Confirmado"
       - Esperado: la card se mueve Y aparece toast amber "Ningún presupuesto fue aceptado — verificá antes de confirmar"
    5. **Test movimiento normal:** Arrastrar cualquier paciente entre dos columnas normales (ej. Nuevo Lead a Turno Agendado)
       - Esperado: la card se mueve SIN toast de warning
    6. **Test PERDIDO sin cambios:** Arrastrar a PERDIDO sigue abriendo el modal de motivo de pérdida como antes
  </how-to-verify>
  <resume-signal>Escribir "aprobado" si todos los tests pasan, o describir qué no funciona para corregir</resume-signal>
</task>

</tasks>

<verification>
Compilación global limpia al finalizar:
```bash
cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend && npx tsc --noEmit 2>&1 | head -20
```

Verificar integración completa:
```bash
grep -n "getEtapaWarning" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/components/crm/KanbanBoard.tsx
grep -c "Verificá los requisitos" /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/frontend/src/components/crm/KanbanBoard.tsx
# Debe retornar 0 (texto reemplazado)
```
</verification>

<success_criteria>
1. `KanbanBoard.tsx` importa `getEtapaWarning` desde `@/lib/crm-warnings`
2. `handleDragEnd` llama `toast.warning(warning)` cuando `getEtapaWarning` retorna string no nulo
3. El texto `"Verificá los requisitos"` no aparece en ningún archivo del proyecto
4. El texto `"Intentá de nuevo"` aparece en `onError` de `handleDragEnd`
5. `onSettled` sigue siendo el único punto de cleanup de `pendingMoves` (snap-back intacto)
6. Checkpoint de verificación aprobado por el usuario
</success_criteria>

<output>
After completion, create `.planning/phases/36-drag-and-drop-warning-infrastructure/36-02-SUMMARY.md`
</output>
