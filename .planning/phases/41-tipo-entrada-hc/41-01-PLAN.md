---
phase: 41-tipo-entrada-hc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/schema.prisma
  - backend/prisma/migrations/20260608100000_add_tipo_entrada_hc/migration.sql
  - backend/src/modules/historia-clinica/dto/crear-entrada.dto.ts
  - backend/src/modules/historia-clinica/historia-clinica.service.ts
  - backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts
autonomous: true
requirements: [HC-01, HC-03, HC-04]
user_setup: []

must_haves:
  truths:
    - "La entrada de HC persiste un campo tipoEntrada con uno de: CONSULTA_CIRUGIA, TRATAMIENTO, CONTROL, SEGUIMIENTO, PREOPERATORIO"
    - "Al crear entrada con tipoEntrada=CONSULTA_CIRUGIA y paciente.flujo=PENDIENTE, el flujo pasa a CIRUGIA"
    - "Al crear entrada con tipoEntrada=TRATAMIENTO y paciente.flujo=PENDIENTE, el flujo pasa a TRATAMIENTO"
    - "Al crear entrada con tipoEntrada=TRATAMIENTO y paciente.flujo=CIRUGIA, el flujo NO cambia (dual-state)"
    - "CONTROL, SEGUIMIENTO, PREOPERATORIO no modifican flujo ni etapaCRM"
    - "Cuando dto.turnoId apunta a un turno con esCirugia=true, el cambio de flujo por tipoEntrada se omite"
    - "cerrarSesion (turnos.service.ts) conserva su lógica de etapaCRM/PROCEDIMIENTO_REALIZADO intacta"
  artifacts:
    - path: "backend/src/prisma/schema.prisma"
      provides: "enum TipoEntradaHC + campo tipoEntrada en HistoriaClinicaEntrada"
      contains: "enum TipoEntradaHC"
    - path: "backend/prisma/migrations/20260608100000_add_tipo_entrada_hc/migration.sql"
      provides: "Migración Prisma que crea el enum y la columna"
      contains: "TipoEntradaHC"
    - path: "backend/src/modules/historia-clinica/dto/crear-entrada.dto.ts"
      provides: "Campo tipoEntrada en CreateEntradaDto"
      contains: "tipoEntrada"
    - path: "backend/src/modules/historia-clinica/historia-clinica.service.ts"
      provides: "Persistencia de tipoEntrada + transición de flujo dentro de la transacción de crearEntrada"
      contains: "tipoEntrada"
    - path: "backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts"
      provides: "Tests unitarios de la lógica de transición de flujo"
      contains: "describe"
  key_links:
    - from: "crearEntrada (historia-clinica.service.ts)"
      to: "tx.paciente.update({ flujo })"
      via: "rama por tipoEntrada dentro de prisma.$transaction"
      pattern: "flujo:\\s*'?(CIRUGIA|TRATAMIENTO)'?"
    - from: "crearEntrada"
      to: "turno.esCirugia guard"
      via: "lookup de turno cuando dto.turnoId está presente"
      pattern: "esCirugia"
---

<objective>
Backend de la clasificación clínica de entradas de HC. Crea el enum Prisma `TipoEntradaHC`, el campo `tipoEntrada` en `HistoriaClinicaEntrada`, la migración, el campo en el DTO, y la lógica de transición de flujo dentro de `crearEntrada` (punto único usado por LiveTurno y PatientDrawer).

Purpose: Materializa la "clasificación tardía" de v1.8 — el tipo de turno "Consulta" no clasifica al agendar (flujoPaciente=NULL); la clasificación ocurre al guardar la entrada de HC vía `tipoEntrada`, y de ahí derivan los cambios de `Paciente.flujo`.

Output: Enum + columna migrados, DTO extendido, lógica de flujo (con guard `esCirugia`) en `crearEntrada`, y tests unitarios de las reglas de transición.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-tipo-entrada-hc/41-CONTEXT.md

@backend/src/prisma/schema.prisma
@backend/src/modules/historia-clinica/historia-clinica.service.ts
@backend/src/modules/historia-clinica/dto/crear-entrada.dto.ts

<interfaces>
<!-- Contratos verificados en el código. El executor usa esto directo, sin explorar. -->

Enums existentes (schema.prisma ~1137-1166) — el nuevo enum sigue este patrón:
```prisma
enum FlujoPaciente {
  CIRUGIA
  TRATAMIENTO
  PENDIENTE
}
enum EtapaCRM {
  NUEVO_LEAD
  TURNO_AGENDADO
  CONSULTADO
  PRESUPUESTO_ENVIADO
  PROCEDIMIENTO_REALIZADO
  CONFIRMADO
  PERDIDO
}
```

Paciente (schema.prisma ~193-197): `etapaCRM EtapaCRM?` y `flujo FlujoPaciente? @default(PENDIENTE)`.

HistoriaClinicaEntrada (schema.prisma ~276): ya tiene `contenido Json?`, `templateId`, `status EstadoEntradaHC`, etc. El nuevo campo va junto a estos.

CreateEntradaDto (crear-entrada.dto.ts): campo discriminador EXISTENTE `tipo: 'primera_vez' | 'pre_quirurgico' | 'control' | 'practica' | 'tratamiento_en_consultorio' | 'libre'`.
⚠ ESTE `tipo` ES EL SELECTOR DE PLANTILLA — NO confundir con el nuevo `tipoEntrada` (clasificación clínica). Son ejes distintos.

crearEntrada (historia-clinica.service.ts:65) — firma:
`async crearEntrada(pacienteId: string, dto: CreateEntradaDto, profesionalIdFromJwt?: string)`
- Toda la escritura ocurre en `return this.prisma.$transaction(async (tx) => { ... })` (~línea 181).
- Ya hace `tx.paciente.update(...)` para diagnostico/tratamiento — el update de `flujo` se suma ahí.
- `dto.turnoId` está presente desde LiveTurno, ausente desde PatientDrawer.

cerrarSesion (turnos.service.ts:873) — NO se toca en este plan. Su lógica de etapaCRM
(`esCirugia → PROCEDIMIENTO_REALIZADO`, `TURNO_AGENDADO → CONSULTADO`) ya cumple HC-03 (avance a CONSULTADO).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enum TipoEntradaHC + campo tipoEntrada + migración</name>
  <files>backend/src/prisma/schema.prisma, backend/prisma/migrations/20260608100000_add_tipo_entrada_hc/migration.sql</files>
  <action>
    En `backend/src/prisma/schema.prisma`:
    1. Agregar un nuevo enum junto a los demás enums CRM (cerca de `FlujoPaciente`, ~línea 1162):
       ```prisma
       enum TipoEntradaHC {
         CONSULTA_CIRUGIA
         TRATAMIENTO
         CONTROL
         SEGUIMIENTO
         PREOPERATORIO
       }
       ```
       Nombre `TipoEntradaHC` deliberadamente distinto del tipo frontend `TipoEntrada` (plantilla) para evitar colisión — ver CONTEXT "Colisión de nombres a evitar".
    2. En `model HistoriaClinicaEntrada` (~línea 276), agregar el campo OPCIONAL (las entradas legacy no tienen valor; out-of-scope hacer backfill):
       ```prisma
       tipoEntrada       TipoEntradaHC?
       ```
       Colocarlo junto a los campos de plantilla (después de `computed Json?`).

    Crear la migración manual (mismo patrón que `20260608000000_migracion_tipos_turno_v18`) en
    `backend/prisma/migrations/20260608100000_add_tipo_entrada_hc/migration.sql`:
       ```sql
       -- CreateEnum
       CREATE TYPE "TipoEntradaHC" AS ENUM ('CONSULTA_CIRUGIA', 'TRATAMIENTO', 'CONTROL', 'SEGUIMIENTO', 'PREOPERATORIO');

       -- AlterTable
       ALTER TABLE "HistoriaClinicaEntrada" ADD COLUMN "tipoEntrada" "TipoEntradaHC";
       ```
    Luego regenerar el client: `cd backend && npx prisma generate`.
    NO ejecutar `prisma migrate dev` automáticamente si requiere DB viva sin confirmar — basta con que el archivo exista y `prisma generate` compile el client con el nuevo tipo. Verificá nombre exacto de la columna en la DB target con `\d "HistoriaClinicaEntrada"` solo si hay acceso.
  </action>
  <verify>
    <automated>cd backend && npx prisma generate && npx prisma validate</automated>
  </verify>
  <done>schema.prisma contiene `enum TipoEntradaHC` y `tipoEntrada TipoEntradaHC?` en HistoriaClinicaEntrada; el archivo de migración existe con el CREATE TYPE + ALTER TABLE; `prisma validate` pasa y el client regenerado expone el enum.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tipoEntrada en DTO + lógica de transición de flujo en crearEntrada</name>
  <files>backend/src/modules/historia-clinica/dto/crear-entrada.dto.ts, backend/src/modules/historia-clinica/historia-clinica.service.ts, backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts</files>
  <behavior>
    Función pura de decisión de flujo (extraer como helper exportable, p.ej. `resolverNuevoFlujo(tipoEntrada, flujoActual, esCirugia): FlujoPaciente | null`):
    - Test 1: ('CONSULTA_CIRUGIA', 'PENDIENTE', false) → 'CIRUGIA'
    - Test 2: ('CONSULTA_CIRUGIA', 'CIRUGIA', false) → null (sin cambio)
    - Test 3: ('TRATAMIENTO', 'PENDIENTE', false) → 'TRATAMIENTO'
    - Test 4: ('TRATAMIENTO', 'CIRUGIA', false) → null (dual-state preservado, HC-04)
    - Test 5: ('TRATAMIENTO', 'TRATAMIENTO', false) → null
    - Test 6: ('CONTROL', 'PENDIENTE', false) → null
    - Test 7: ('SEGUIMIENTO', 'PENDIENTE', false) → null
    - Test 8: ('PREOPERATORIO', 'PENDIENTE', false) → null
    - Test 9: esCirugia guard — ('CONSULTA_CIRUGIA', 'PENDIENTE', true) → null (turno cirugía omite el cambio)
    - Test 10: tipoEntrada undefined → null (entradas legacy / sin clasificar)
  </behavior>
  <action>
    1. En `crear-entrada.dto.ts`: agregar al `CreateEntradaDto`:
       ```ts
       @IsOptional()
       @IsEnum(['CONSULTA_CIRUGIA', 'TRATAMIENTO', 'CONTROL', 'SEGUIMIENTO', 'PREOPERATORIO'])
       tipoEntrada?: 'CONSULTA_CIRUGIA' | 'TRATAMIENTO' | 'CONTROL' | 'SEGUIMIENTO' | 'PREOPERATORIO';
       ```
       (importar `IsEnum` de class-validator). Opcional a nivel transporte; el form lo hace obligatorio en UI (Plan 02). Las entradas legacy/sin clasificar quedan sin valor.

    2. En `historia-clinica.service.ts`, crear el helper puro (exportado para test), arriba o al final del archivo:
       ```ts
       export function resolverNuevoFlujo(
         tipoEntrada: string | undefined,
         flujoActual: string | null | undefined,
         esCirugia: boolean,
       ): 'CIRUGIA' | 'TRATAMIENTO' | null {
         if (esCirugia) return null;                 // Criterio 5: turnos cirugía omiten cambio
         if (!tipoEntrada) return null;
         if (tipoEntrada === 'CONSULTA_CIRUGIA') {
           return flujoActual === 'PENDIENTE' ? 'CIRUGIA' : null;   // HC-03
         }
         if (tipoEntrada === 'TRATAMIENTO') {
           return flujoActual === 'PENDIENTE' ? 'TRATAMIENTO' : null; // HC-04 (CIRUGIA → null, dual)
         }
         return null; // CONTROL / SEGUIMIENTO / PREOPERATORIO: no-op
       }
       ```

    3. En `crearEntrada`, persistir `tipoEntrada` y aplicar el flujo DENTRO de `this.prisma.$transaction`:
       - Antes de la transacción (o al inicio de ella), si `dto.turnoId` está presente, leer `esCirugia`:
         `const turnoCtx = dto.turnoId ? await tx.turno.findUnique({ where: { id: dto.turnoId }, select: { esCirugia: true } }) : null;`
         (preferible dentro de tx por consistencia; queda a discreción — el repo evita queries anidadas, podés hacer el lookup fuera de tx como con los otros pre-fetch).
       - Al crear la entrada, incluir el campo:
         `data: { historiaClinicaId: historia.id, contenido, tipoEntrada: dto.tipoEntrada ?? undefined, ...(fechaFinal && { fecha: fechaFinal }) }`
       - Leer el flujo actual del paciente (en la misma tx o reutilizar un select previo):
         `const pac = await tx.paciente.findUnique({ where: { id: pacienteId }, select: { flujo: true } });`
       - Calcular `const nuevoFlujo = resolverNuevoFlujo(dto.tipoEntrada, pac?.flujo, turnoCtx?.esCirugia ?? false);`
       - Si `nuevoFlujo`, sumarlo al `tx.paciente.update` existente (o crear uno):
         `await tx.paciente.update({ where: { id: pacienteId }, data: { ...(diagnosticoStr !== null && { diagnostico: diagnosticoStr }), ...(tratamientoStr !== null && { tratamiento: tratamientoStr }), ...(nuevoFlujo && { flujo: nuevoFlujo }) } });`
         OJO: el `tx.paciente.update` actual está guardado por `if (diagnosticoStr !== null || tratamientoStr !== null)`. Ajustar la condición para que también dispare cuando hay `nuevoFlujo`, p.ej. `if (diagnosticoStr !== null || tratamientoStr !== null || nuevoFlujo)`.
       - NO tocar `etapaCRM` acá (vive en cerrarSesion). NO duplicar lógica de PROCEDIMIENTO_REALIZADO.

    4. Crear `historia-clinica.flujo.spec.ts` con los 10 casos de <behavior> sobre `resolverNuevoFlujo` (test unitario puro, sin DB, sin mocks de Prisma).
  </action>
  <verify>
    <automated>cd backend && npx jest historia-clinica.flujo --silent && npm run build</automated>
  </verify>
  <done>`resolverNuevoFlujo` pasa los 10 tests; `crearEntrada` persiste `tipoEntrada` y aplica el flujo dentro de la transacción respetando el guard esCirugia; el build de NestJS compila sin errores de tipo; `cerrarSesion` queda sin modificar.</done>
</task>

</tasks>

<verification>
- `npx prisma validate` pasa y el client expone `TipoEntradaHC`.
- `npx jest historia-clinica.flujo` verde (10 casos cubren HC-03, HC-04 y guard esCirugia / criterio 5).
- `npm run build` (backend) compila sin errores.
- Grep de control: `cerrarSesion` en turnos.service.ts no fue modificado (sigue conteniendo `PROCEDIMIENTO_REALIZADO` y `TURNO_AGENDADO → CONSULTADO` sin lógica de flujo).
</verification>

<success_criteria>
- HC-01: campo `tipoEntrada` (enum `TipoEntradaHC`) persistido en `HistoriaClinicaEntrada`.
- HC-03: CONSULTA_CIRUGIA + PENDIENTE → CIRUGIA (avance a CONSULTADO lo cubre cerrarSesion intacto).
- HC-04: TRATAMIENTO + PENDIENTE → TRATAMIENTO; TRATAMIENTO + CIRUGIA → sin cambio.
- Criterio 5: turnos con `esCirugia=true` omiten el cambio de flujo por tipoEntrada; PROCEDIMIENTO_REALIZADO intacto.
</success_criteria>

<output>
After completion, create `.planning/phases/41-tipo-entrada-hc/41-01-SUMMARY.md`
</output>
