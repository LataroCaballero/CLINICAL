---
phase: 41-tipo-entrada-hc
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - frontend/src/hooks/useCreateHistoriaClinicaEntry.ts
  - frontend/src/components/live-turno/tabs/hc/HCCreatorForm.tsx
autonomous: false
requirements: [HC-02]
user_setup: []

must_haves:
  truths:
    - "El HCCreatorForm muestra un selector obligatorio 'Tipo de consulta' con 5 opciones: Consulta para cirugía, Tratamiento, Control, Seguimiento, Pre-operatorio"
    - "El selector aparece tanto en LiveTurno como en PatientDrawer (mismo HCCreatorForm reutilizable)"
    - "El valor del selector se auto-deriva del botón de plantilla elegido pero es editable (override manual)"
    - "Al guardar, el payload incluye tipoEntrada y llega a crearEntrada"
    - "No se puede guardar sin un tipoEntrada seleccionado"
  artifacts:
    - path: "frontend/src/hooks/useCreateHistoriaClinicaEntry.ts"
      provides: "Campo tipoEntrada en CreateEntradaDto del hook"
      contains: "tipoEntrada"
    - path: "frontend/src/components/live-turno/tabs/hc/HCCreatorForm.tsx"
      provides: "Selector 'Tipo de consulta' con mapeo plantilla→tipoEntrada y envío en payload"
      contains: "tipoEntrada"
  key_links:
    - from: "HCCreatorForm selector"
      to: "createEntry.mutateAsync({ dto: { tipoEntrada } })"
      via: "estado tipoEntradaHC incluido en cada rama de handleSave"
      pattern: "tipoEntrada"
    - from: "botón de plantilla (TIPOS)"
      to: "valor inicial del selector"
      via: "mapeo PLANTILLA_TO_TIPO_ENTRADA"
      pattern: "primera_vez.*CONSULTA_CIRUGIA"
---

<objective>
Frontend del selector "Tipo de consulta" (HC-02). Agrega el selector obligatorio y editable en `HCCreatorForm` (compartido por LiveTurno y PatientDrawer), con auto-derivación desde el botón de plantilla, y envía `tipoEntrada` en el payload del hook.

Purpose: Permite al profesional clasificar la consulta al cerrar la sesión. La clasificación viaja a `crearEntrada` (Plan 01) que aplica la transición de flujo.

Output: Selector visible/editable con 5 opciones, mapeo plantilla→tipoEntrada, payload extendido, validación de obligatoriedad.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/41-tipo-entrada-hc/41-CONTEXT.md
@.planning/phases/41-tipo-entrada-hc/41-01-PLAN.md

@frontend/src/components/live-turno/tabs/hc/HCCreatorForm.tsx
@frontend/src/hooks/useCreateHistoriaClinicaEntry.ts

<interfaces>
<!-- Contratos verificados en el código. -->

⚠ COLISIÓN DE NOMBRES (CONTEXT): en este archivo YA existe
`type TipoEntrada = 'primera_vez' | 'pre_quirurgico' | 'control' | 'practica' | 'tratamiento_en_consultorio' | 'libre'`
(importado en HCCreatorForm como `type TipoBoton = TipoEntrada`). Ese es el tipo de PLANTILLA.
El NUEVO campo clínico se llama `tipoEntrada` y usa los valores del enum backend
('CONSULTA_CIRUGIA' | 'TRATAMIENTO' | 'CONTROL' | 'SEGUIMIENTO' | 'PREOPERATORIO').
Para el tipo frontend nuevo usar un alias distinto, p.ej. `type TipoEntradaHCValue` — NO reusar el nombre `TipoEntrada`.

HCCreatorForm.tsx (verificado):
- `const TIPOS: { id: TipoBoton; label: string }[]` (~línea 35): plantillas (primera_vez, pre_quirurgico, control, tratamiento_en_consultorio). 'libre'/'practica' no están como botones pero existen como valores.
- `const [tipoSeleccionado, setTipoSeleccionado] = useState<TipoBoton | null>(null)` (~línea 51).
- Render del selector de plantilla: `.flex.gap-2.flex-wrap` con `TIPOS.map(...)` (~línea 196). El onClick hace `setTipoSeleccionado(id)`.
- `handleSave` (~línea 97) tiene 3 ramas que llaman `createEntry.mutateAsync({ pacienteId, dto: {...} })`: 'primera_vez', 'tratamiento_en_consultorio', y el `else` (libre/control/pre_quirurgico).
- `canSave` (~línea 90) guarda la habilitación del botón Guardar.
- shadcn Select disponible en `frontend/src/components/ui/select.tsx`.

useCreateHistoriaClinicaEntry.ts (verificado):
- `export interface CreateEntradaDto { tipo: TipoEntrada; ...; turnoId?: string }` — agregar `tipoEntrada?`.

Mapeo plantilla → tipoEntrada default (de CONTEXT "Implementation Decisions"):
  primera_vez → CONSULTA_CIRUGIA
  pre_quirurgico → PREOPERATORIO
  control → CONTROL
  tratamiento_en_consultorio → TRATAMIENTO
  libre / practica → CONTROL (default seguro, editable)
SEGUIMIENTO no tiene plantilla; solo se alcanza eligiéndolo manualmente.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tipoEntrada en el hook CreateEntradaDto</name>
  <files>frontend/src/hooks/useCreateHistoriaClinicaEntry.ts</files>
  <action>
    Agregar al `interface CreateEntradaDto` (sin tocar el `type TipoEntrada` de plantilla existente):
    ```ts
    // Clasificación clínica (Phase 41) — distinto del `tipo` de plantilla
    tipoEntrada?: 'CONSULTA_CIRUGIA' | 'TRATAMIENTO' | 'CONTROL' | 'SEGUIMIENTO' | 'PREOPERATORIO';
    ```
    Opcionalmente exportar un alias `export type TipoEntradaHCValue = NonNullable<CreateEntradaDto['tipoEntrada']>;` para reutilizar en el form. No renombrar ni romper el `type TipoEntrada` existente (lo importan otros archivos).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>`CreateEntradaDto.tipoEntrada` existe con los 5 valores; `tsc --noEmit` pasa; el `type TipoEntrada` de plantilla queda intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Selector 'Tipo de consulta' con auto-derivación en HCCreatorForm</name>
  <files>frontend/src/components/live-turno/tabs/hc/HCCreatorForm.tsx</files>
  <action>
    1. Importar el Select de shadcn (`Select, SelectTrigger, SelectValue, SelectContent, SelectItem` de `@/components/ui/select`) y el alias `type TipoEntradaHCValue` del hook.
    2. Definir constantes a nivel módulo:
       ```ts
       const TIPO_ENTRADA_OPTIONS: { value: TipoEntradaHCValue; label: string }[] = [
         { value: 'CONSULTA_CIRUGIA', label: 'Consulta para cirugía' },
         { value: 'TRATAMIENTO',      label: 'Tratamiento' },
         { value: 'CONTROL',          label: 'Control' },
         { value: 'SEGUIMIENTO',      label: 'Seguimiento' },
         { value: 'PREOPERATORIO',    label: 'Pre-operatorio' },
       ];
       const PLANTILLA_TO_TIPO_ENTRADA: Record<string, TipoEntradaHCValue> = {
         primera_vez: 'CONSULTA_CIRUGIA',
         pre_quirurgico: 'PREOPERATORIO',
         control: 'CONTROL',
         tratamiento_en_consultorio: 'TRATAMIENTO',
         libre: 'CONTROL',
         practica: 'CONTROL',
       };
       ```
    3. Estado: `const [tipoEntradaHC, setTipoEntradaHC] = useState<TipoEntradaHCValue | null>(null);`
    4. Auto-derivación: en el onClick de los botones de plantilla (~línea 200, donde se hace `setTipoSeleccionado(id)`), agregar `setTipoEntradaHC(PLANTILLA_TO_TIPO_ENTRADA[id] ?? 'CONTROL');`. Esto pre-carga el selector pero queda editable.
    5. Render del selector: agregar un bloque visible cuando `tipoSeleccionado !== null` (debajo de los botones de plantilla, antes del área de form). Label "Tipo de consulta" + `<Select value={tipoEntradaHC ?? undefined} onValueChange={(v) => setTipoEntradaHC(v as TipoEntradaHCValue)}>` mapeando `TIPO_ENTRADA_OPTIONS`. Marcar visualmente como requerido.
    6. Obligatoriedad: en `canSave` (~línea 90) agregar `&& tipoEntradaHC !== null`.
    7. Envío: en las TRES ramas de `handleSave` que llaman `createEntry.mutateAsync`, agregar `tipoEntrada: tipoEntradaHC ?? undefined` dentro del objeto `dto`. (Está garantizado no-null por canSave, pero usar `?? undefined` por seguridad de tipos.)
    8. Reset: en el `setTimeout` de limpieza al final de `handleSave` (~línea 150), agregar `setTipoEntradaHC(null);`.
    NO modificar la lógica de plantillas, presupuesto, ni insumos. Solo sumar el eje `tipoEntrada`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>El selector "Tipo de consulta" aparece con 5 opciones al elegir una plantilla, pre-cargado según el mapeo y editable; `canSave` exige tipoEntradaHC; las 3 ramas de handleSave envían `tipoEntrada`; el reset lo limpia; `tsc --noEmit` y `lint` pasan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — verificación end-to-end del flujo clínico</name>
  <action>
    [CHECKPOINT — verificación humana. Claude ya automatizó el selector + payload (Tasks 1-2); este paso confirma el comportamiento end-to-end con backend (Plan 01) corriendo.]

    Qué se construyó: selector obligatorio "Tipo de consulta" en HCCreatorForm (5 opciones), auto-derivado desde el botón de plantilla y editable, conectado a crearEntrada (Plan 01) que aplica la transición de flujo.

    Cómo verificar — con backend (`cd backend && npm run start:dev`) y frontend (`cd frontend && npm run dev`) corriendo:
    1. LiveTurno de un turno tipo "Consulta" de un paciente con flujo PENDIENTE: abrí la pestaña HC, elegí la plantilla "Primera Consulta" → el selector "Tipo de consulta" debe aparecer pre-cargado en "Consulta para cirugía". Guardá y cerrá sesión. Verificá que el paciente pasó a flujo CIRUGIA y, si estaba en TURNO_AGENDADO, su etapa CRM avanzó a CONSULTADO. (Criterios 1, 2)
    2. Otro turno "Consulta" de un paciente PENDIENTE: elegí la plantilla "Tratamiento en Consultorio" → selector en "Tratamiento". Guardá. El paciente debe pasar a flujo TRATAMIENTO sin cambiar etapa CRM. (Criterio 3)
    3. Cambiá manualmente el selector a "Seguimiento" antes de guardar para confirmar que es editable (override). El flujo no debe cambiar para CONTROL/SEGUIMIENTO/PREOPERATORIO.
    4. PatientDrawer (sin turno): el selector debe aparecer igual y ser obligatorio.
    5. Un paciente con flujo CIRUGIA + plantilla "Tratamiento": guardá y confirmá que el flujo NO cambia (sigue CIRUGIA, dual-state). (Criterio 4)
  </action>
  <verify>
    <automated>MISSING — verificación manual; criterios 2-4 cubiertos por jest en Plan 01 (resolverNuevoFlujo). Este checkpoint confirma la integración UI→backend.</automated>
  </verify>
  <done>El usuario escribe "approved" tras confirmar criterios 1-4 end-to-end, o reporta problemas a corregir.</done>
  <resume-signal>Escribí "approved" o describí los problemas encontrados.</resume-signal>
</task>

</tasks>

<verification>
- `tsc --noEmit` y `npm run lint` (frontend) pasan.
- El selector muestra exactamente 5 opciones con los labels especificados.
- El payload de cada rama de handleSave incluye `tipoEntrada`.
- Checkpoint humano confirma los criterios 1–4 end-to-end.
</verification>

<success_criteria>
- HC-02: HCCreatorForm incluye selector obligatorio "Tipo de consulta" en LiveTurno y PatientDrawer.
- Criterio 1: 5 opciones con labels correctos; auto-derivado y editable.
- El valor llega a crearEntrada (Plan 01) cerrando el flujo end-to-end (criterios 2–4 verificados en checkpoint).
</success_criteria>

<output>
After completion, create `.planning/phases/41-tipo-entrada-hc/41-02-SUMMARY.md`
</output>
