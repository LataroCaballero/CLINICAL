---
phase: 42-estado-dual-y-tratamientostab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/turnos/turnos.service.ts
  - frontend/src/hooks/useTurnosRangos.ts
  - frontend/src/app/dashboard/pacientes/components/TratamientosTab.tsx
autonomous: true
requirements: [DUAL-01, DUAL-02, DUAL-03]

must_haves:
  truths:
    - "Un paciente flujo=CIRUGIA con un turno tipo 'Tratamiento' aparece en la planilla de tratamientos del mes (fuente A)"
    - "Un paciente flujo=CIRUGIA con un turno tipo 'Consulta' cuya entrada HC tiene tipoEntrada=TRATAMIENTO aparece en la planilla (fuente B)"
    - "La columna 'Tipo de turno' muestra 'Consulta → Tratamiento' para filas de fuente B y el tipoTurno.nombre para fuente A"
    - "El contador del header cuenta A + B como un total combinado, ordenado por inicio asc, sin desglose por fuente"
    - "El endpoint /turnos/rango expone tipoEntradaHC por turno sin queries N+1 (vía select de la relación entradaHC directa)"
  artifacts:
    - path: "backend/src/modules/turnos/turnos.service.ts"
      provides: "findRango devuelve tipoEntradaHC por turno desde entradaHC.tipoEntrada"
      contains: "entradaHC"
    - path: "frontend/src/hooks/useTurnosRangos.ts"
      provides: "TurnoRango type incluye tipoEntradaHC"
      contains: "tipoEntradaHC"
    - path: "frontend/src/app/dashboard/pacientes/components/TratamientosTab.tsx"
      provides: "Predicado de filtrado fuente A OR fuente B + render 'Consulta → Tratamiento'"
      contains: "Consulta"
  key_links:
    - from: "backend findRango select"
      to: "Turno.entradaHC.tipoEntrada"
      via: "Prisma nested select on direct relation"
      pattern: "entradaHC[\\s\\S]*tipoEntrada"
    - from: "TratamientosTab filter predicate"
      to: "t.tipoEntradaHC === 'TRATAMIENTO'"
      via: "client-side OR clause extending flujoPaciente filter"
      pattern: "tipoEntradaHC"
---

<objective>
Hacer que la planilla de tratamientos (TratamientosTab) muestre dos fuentes de registros: (A) turnos cuyo tipoTurno.flujoPaciente === TRATAMIENTO (comportamiento actual), y (B) turnos de tipo "Consulta" cuya entrada HC vinculada tiene tipoEntrada === TRATAMIENTO. Así un paciente flujo=CIRUGIA que también recibe tratamientos aparece tanto en el kanban CRM como en la planilla, sin cambiar su flujo ni su etapa.

Purpose: Cumplir DUAL-01/02/03 — estado dual visible. Phase 42 es SOLO LECTURA: no muta flujo ni etapaCRM, no crea endpoints nuevos.
Output: findRango extendido para exponer tipoEntradaHC por turno; type TurnoRango actualizado; TratamientosTab con predicado dual y columna "Consulta → Tratamiento".
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-estado-dual-y-tratamientostab/42-CONTEXT.md

@backend/src/modules/turnos/turnos.service.ts
@frontend/src/hooks/useTurnosRangos.ts
@frontend/src/app/dashboard/pacientes/components/TratamientosTab.tsx

<interfaces>
<!-- Contratos clave extraídos del codebase. El executor usa estos directamente. -->

Prisma schema (backend/src/prisma/schema.prisma):
- model Turno tiene relación DIRECTA: `entradaHCId String?` + `entradaHC HistoriaClinicaEntrada? @relation(...)`.
  => Se puede traer `entradaHC.tipoEntrada` con un nested select en el mismo findMany (NO hay N+1).
- model HistoriaClinicaEntrada tiene `tipoEntrada TipoEntradaHC?`.
- enum TipoEntradaHC { CONSULTA_CIRUGIA, TRATAMIENTO, CONTROL, SEGUIMIENTO, PREOPERATORIO }

findRango actual (turnos.service.ts:504) hace:
  prisma.turno.findMany({ where: { profesionalId, inicio: {gte, lte} }, orderBy: { inicio: 'asc' },
    select: { id, inicio, fin, estado, observaciones, paciente{...}, tipoTurno{ id, nombre, flujoPaciente } } })
  ...luego enriquece cada turno con `ultimoTratamiento` (patrón Phase 30) y retorna `turnos.map(t => ({ ...t, ultimoTratamiento }))`.

TurnoRango type actual (useTurnosRangos.ts):
  type TurnoRango = {
    id; inicio; fin; estado; observaciones?;
    paciente: { id; nombreCompleto };
    tipoTurno: { id; nombre; flujoPaciente? };
    ultimoTratamiento?: string | null;
  }

TratamientosTab filtro actual (línea ~66):
  const tratamientoTurnos = (turnosData ?? []).filter(t => t.tipoTurno.flujoPaciente === "TRATAMIENTO");
Render columna Tipo (línea ~233-235):
  <td ...>{turno.tipoTurno.nombre}</td>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exponer tipoEntradaHC por turno en findRango</name>
  <files>backend/src/modules/turnos/turnos.service.ts</files>
  <action>
    En findRango (turnos.service.ts:504), extender el `select` del `prisma.turno.findMany` para incluir la relación directa `entradaHC` con solo `tipoEntrada`:

      entradaHC: { select: { tipoEntrada: true } },

    Agregarlo dentro del objeto `select` existente (junto a paciente/tipoTurno), NO en un query separado — la relación `Turno.entradaHC` es directa (entradaHCId), así que esto NO produce N+1.

    Luego en el `return turnos.map((t) => ({ ... }))` final (línea ~570), aplanar el valor a un campo plano `tipoEntradaHC` y NO devolver el objeto anidado `entradaHC` en la respuesta (mantener la forma plana que consume el frontend):

      return turnos.map((t) => {
        const { entradaHC, ...rest } = t;
        return {
          ...rest,
          ultimoTratamiento: ultimoTratamientoMap.get(t.paciente.id) ?? null,
          tipoEntradaHC: entradaHC?.tipoEntrada ?? null,
        };
      });

    NO cambiar el `where`, el `orderBy`, ni la lógica de ultimoTratamiento. NO mutar nada (read-only). NO crear endpoints ni DTOs nuevos. El scope multi-tenant (profesionalId) ya está cubierto por el where existente.
  </action>
  <verify>
    <automated>cd backend && npx tsc -p tsconfig.build.json --noEmit</automated>
  </verify>
  <done>findRango compila; el select incluye entradaHC.tipoEntrada; cada turno retornado expone tipoEntradaHC: TipoEntradaHC | null; no hay query adicional para HC entries (sigue habiendo solo el findMany de turnos + el findMany de historias para ultimoTratamiento que ya existía).</done>
</task>

<task type="auto">
  <name>Task 2: Predicado dual + columna "Consulta → Tratamiento" en TratamientosTab</name>
  <files>frontend/src/hooks/useTurnosRangos.ts, frontend/src/app/dashboard/pacientes/components/TratamientosTab.tsx</files>
  <action>
    PASO A — Actualizar el type TurnoRango (useTurnosRangos.ts): agregar el campo plano que ahora devuelve el backend:

      tipoEntradaHC?: string | null;

    (al lado de ultimoTratamiento). No cambiar el queryKey ni la lógica del hook.

    PASO B — TratamientosTab.tsx, extender el predicado de filtrado (línea ~66). Definir una constante helper para fuente B y reemplazar el filtro:

      // Fuente A: tipoTurno de flujo TRATAMIENTO (comportamiento actual)
      // Fuente B: turno tipo "Consulta" con entrada HC tipoEntrada=TRATAMIENTO (Phase 42 dual)
      const isFuenteB = (t: TurnoRango) =>
        t.tipoTurno.nombre === "Consulta" && t.tipoEntradaHC === "TRATAMIENTO";

      const tratamientoTurnos = (turnosData ?? []).filter(
        (t) => t.tipoTurno.flujoPaciente === "TRATAMIENTO" || isFuenteB(t)
      );

    Importar el type TurnoRango desde "@/hooks/useTurnosRangos" si se usa en la firma del helper (o tipar el param como el elemento del array). Mantener el orden por inicio asc (ya viene ordenado del backend; el filter preserva orden) — A y B quedan intercaladas por fecha automáticamente. El contador del header (totalCount / countByEstado) ya opera sobre tratamientoTurnos => cuenta A + B combinado sin cambios.

    PASO C — Render de la columna "Tipo de turno" (línea ~233-235). Mostrar "Consulta → Tratamiento" para fuente B, y tipoTurno.nombre para el resto:

      <td className={cn("py-2 px-3", fm ? "text-[var(--fc-text-secondary)]" : "text-gray-600")}>
        {isFuenteB(turno) ? "Consulta → Tratamiento" : turno.tipoTurno.nombre}
      </td>

    PASO D — Filtro dropdown (Claude's Discretion): el dropdown deriva tiposEnMes de tratamientoTurnos por tipoTurno.id. Para que las filas de fuente B sean filtrables coherentemente, agregar una entrada sintética al dropdown cuando exista al menos una fila fuente B en el mes: opción con value "CONSULTA_TRATAMIENTO" y label "Consulta → Tratamiento". Ajustar el predicado de `visibleTurnos` para que: si filterTipoId === "CONSULTA_TRATAMIENTO" muestre solo filas isFuenteB; si filterTipoId es un id de tipoTurno muestre solo ese tipo Y que NO sean fuente B (para no duplicar Consulta→Tratamiento bajo el tipo "Consulta"); si es null muestre todo. Mantenerlo simple — no agregar más controles.

    NO agregar badge de origen por fila, NO FlujoBadge, NO columna de etapa CRM (decisión locked: lista homogénea). NO mutar estado. NO tocar PatientDrawer ni la columna "Último tratamiento".
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>TurnoRango incluye tipoEntradaHC; tratamientoTurnos incluye fuente A OR fuente B; la columna Tipo muestra "Consulta → Tratamiento" para fuente B; el header cuenta A+B; el dropdown filtra coherentemente ambas fuentes; tsc y lint pasan sin errores nuevos.</done>
</task>

</tasks>

<verification>
- `cd backend && npx tsc -p tsconfig.build.json --noEmit` pasa.
- `cd frontend && npx tsc --noEmit && npm run lint` pasa.
- Inspección de findRango: el select incluye `entradaHC: { select: { tipoEntrada: true } }` y la respuesta plana expone `tipoEntradaHC`; no se agregó un segundo query por HC entries.
- Inspección de TratamientosTab: predicado A OR B; columna Tipo condicional; sin mutaciones de estado.
</verification>

<success_criteria>
- DUAL-01: un paciente flujo=CIRUGIA con turno "Tratamiento" aparece en la planilla (fuente A, ya funcionaba — confirmado no-regresión).
- DUAL-02: un paciente flujo=CIRUGIA con turno "Consulta" + entrada HC tipoEntrada=TRATAMIENTO aparece en la planilla (fuente B, nuevo).
- DUAL-03: la planilla muestra ambas fuentes intercaladas por fecha, con la columna Tipo delatando el origen via "Consulta → Tratamiento".
- Garantía dual read-only: ningún cambio escribe flujo/etapaCRM.
</success_criteria>

<output>
After completion, create `.planning/phases/42-estado-dual-y-tratamientostab/42-01-SUMMARY.md`
</output>
