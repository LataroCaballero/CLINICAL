---
phase: 44-schema-cat-logo-en-bd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/schema.prisma
  - backend/src/prisma/migrations/20260612000000_add_catalogo_hc/migration.sql
autonomous: true
requirements: [ZONA-01]

must_haves:
  truths:
    - "El Prisma client compila con los modelos ZonaHC, DiagnosticoHC y TratamientoHC"
    - "La migración DDL aplica limpia sobre la DB de desarrollo (migrate deploy)"
    - "TratamientoHC.tratamientoId es FK nullable al catálogo de precios Tratamiento con ON DELETE SET NULL"
  artifacts:
    - path: "backend/src/prisma/schema.prisma"
      provides: "Modelos ZonaHC, DiagnosticoHC, TratamientoHC + relaciones inversas"
      contains: "model ZonaHC"
    - path: "backend/src/prisma/migrations/20260612000000_add_catalogo_hc/migration.sql"
      provides: "DDL puro: 3 CREATE TABLE + FKs + índices"
      contains: "CREATE TABLE \"ZonaHC\""
  key_links:
    - from: "backend/src/prisma/schema.prisma (ZonaHC)"
      to: "Profesional"
      via: "relation profesionalId"
      pattern: "profesional\\s+Profesional\\s+@relation"
    - from: "backend/src/prisma/schema.prisma (TratamientoHC)"
      to: "Tratamiento"
      via: "FK opcional tratamientoId"
      pattern: "tratamiento\\s+Tratamiento\\?\\s+@relation"
---

<objective>
Crear la fundación de schema para el catálogo de zonas/diagnósticos/tratamientos de HC por profesional: tres modelos Prisma nuevos (ZonaHC, DiagnosticoHC, TratamientoHC) y la migración SQL manual (DDL puro, sin data).

Purpose: ZONA-01 — el catálogo de HC pasa del JSON hardcodeado a PostgreSQL por profesional. Este plan crea las tablas; el plan 44-02 las puebla (seed) y las expone (API).
Output: schema.prisma actualizado + migración aplicada + Prisma client regenerado.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-schema-cat-logo-en-bd/44-CONTEXT.md
@.planning/phases/44-schema-cat-logo-en-bd/44-RESEARCH.md
@backend/src/prisma/schema.prisma

<interfaces>
<!-- Blueprint existente en schema.prisma a replicar (modelo Tratamiento, línea ~891): -->

```prisma
model Tratamiento {
  id              String      @id @default(uuid())
  nombre          String
  precio          Decimal     @default(0) @db.Decimal(10, 2)
  profesionalId   String
  profesional     Profesional @relation(fields: [profesionalId], references: [id])
  activo          Boolean     @default(true)
  createdAt       DateTime    @default(now())
  updatedAt       DateTime    @updatedAt
  // ...otros campos

  @@unique([nombre, profesionalId])
  @@index([profesionalId, activo])
}
```

<!-- Patrón de migración del repo (20260422000000_add_insumos_catalogs): ids son TEXT NOT NULL
     (Prisma genera el uuid en el cliente — NO usar UUID DEFAULT gen_random_uuid() como tipo de columna). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar modelos ZonaHC, DiagnosticoHC y TratamientoHC a schema.prisma</name>
  <files>backend/src/prisma/schema.prisma</files>
  <action>
Agregar al final de la sección de catálogos (cerca de Tratamiento/CirugiaCatalogo) los tres modelos:

```prisma
model ZonaHC {
  id            String          @id @default(uuid())
  nombre        String
  orden         Int
  activo        Boolean         @default(true)
  esSistema     Boolean         @default(false)
  profesionalId String
  profesional   Profesional     @relation(fields: [profesionalId], references: [id])
  diagnosticos  DiagnosticoHC[]
  tratamientos  TratamientoHC[]
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt

  @@unique([nombre, profesionalId])
  @@index([profesionalId, activo])
}

model DiagnosticoHC {
  id            String   @id @default(uuid())
  nombre        String
  orden         Int      @default(0)
  activo        Boolean  @default(true)
  esSistema     Boolean  @default(false)
  zonaId        String
  zona          ZonaHC   @relation(fields: [zonaId], references: [id], onDelete: Cascade)
  profesionalId String
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@unique([nombre, zonaId])
  @@index([zonaId, activo])
  @@index([profesionalId])
}

model TratamientoHC {
  id            String       @id @default(uuid())
  nombre        String
  orden         Int          @default(0)
  activo        Boolean      @default(true)
  esSistema     Boolean      @default(false)
  zonaId        String
  zona          ZonaHC       @relation(fields: [zonaId], references: [id], onDelete: Cascade)
  profesionalId String
  tratamientoId String?
  tratamiento   Tratamiento? @relation(fields: [tratamientoId], references: [id], onDelete: SetNull)
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt

  @@unique([nombre, zonaId])
  @@index([zonaId, activo])
  @@index([profesionalId])
}
```

Notas importantes:
- `profesionalId` en DiagnosticoHC y TratamientoHC es scalar denormalizado SIN @relation (solo ZonaHC tiene la relación real a Profesional). Esto evita 2 relaciones inversas extra en Profesional y simplifica queries per-profesional.
- `esSistema` va en los 3 modelos (decisión: ítems "Otros" protegidos en los 3 niveles para Phase 47).
- `activo` soft delete en los 3 modelos (patrón Tratamiento/CirugiaCatalogo; Phase 47 define semántica).
- `onDelete: SetNull` en tratamientoId — si se borra el Tratamiento de precios, el ítem HC queda sin precio (pitfall 5 del research).

Relaciones inversas OBLIGATORIAS (Prisma falla sin ellas — pitfall 3 del research):
- En `model Profesional`: agregar `zonasHC ZonaHC[]`
- En `model Tratamiento`: agregar `tratamientosHC TratamientoHC[]`

Luego correr `npx prisma generate` desde backend/.
  </action>
  <verify>
    <automated>cd backend && npx prisma validate && npx prisma generate</automated>
  </verify>
  <done>prisma validate y generate pasan sin errores; el client tipado expone prisma.zonaHC, prisma.diagnosticoHC, prisma.tratamientoHC</done>
</task>

<task type="auto">
  <name>Task 2: Crear migración SQL manual (DDL puro) y aplicarla</name>
  <files>backend/src/prisma/migrations/20260612000000_add_catalogo_hc/migration.sql</files>
  <action>
Crear la carpeta `backend/src/prisma/migrations/20260612000000_add_catalogo_hc/` con `migration.sql` de DDL puro (sin data migration — el seed vive en TypeScript, plan 44-02). Seguir el formato exacto del repo (ver 20260422000000_add_insumos_catalogs/migration.sql): ids como `TEXT NOT NULL` (NO `UUID DEFAULT gen_random_uuid()` — Prisma genera el uuid en el cliente).

```sql
-- CreateTable: ZonaHC
CREATE TABLE "ZonaHC" (
    "id" TEXT NOT NULL,
    "nombre" TEXT NOT NULL,
    "orden" INTEGER NOT NULL,
    "activo" BOOLEAN NOT NULL DEFAULT true,
    "esSistema" BOOLEAN NOT NULL DEFAULT false,
    "profesionalId" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "ZonaHC_pkey" PRIMARY KEY ("id")
);

-- CreateTable: DiagnosticoHC
CREATE TABLE "DiagnosticoHC" (
    "id" TEXT NOT NULL,
    "nombre" TEXT NOT NULL,
    "orden" INTEGER NOT NULL DEFAULT 0,
    "activo" BOOLEAN NOT NULL DEFAULT true,
    "esSistema" BOOLEAN NOT NULL DEFAULT false,
    "zonaId" TEXT NOT NULL,
    "profesionalId" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "DiagnosticoHC_pkey" PRIMARY KEY ("id")
);

-- CreateTable: TratamientoHC
CREATE TABLE "TratamientoHC" (
    "id" TEXT NOT NULL,
    "nombre" TEXT NOT NULL,
    "orden" INTEGER NOT NULL DEFAULT 0,
    "activo" BOOLEAN NOT NULL DEFAULT true,
    "esSistema" BOOLEAN NOT NULL DEFAULT false,
    "zonaId" TEXT NOT NULL,
    "profesionalId" TEXT NOT NULL,
    "tratamientoId" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "TratamientoHC_pkey" PRIMARY KEY ("id")
);

-- AddForeignKey
ALTER TABLE "ZonaHC" ADD CONSTRAINT "ZonaHC_profesionalId_fkey"
  FOREIGN KEY ("profesionalId") REFERENCES "Profesional"("id") ON DELETE RESTRICT ON UPDATE CASCADE;

ALTER TABLE "DiagnosticoHC" ADD CONSTRAINT "DiagnosticoHC_zonaId_fkey"
  FOREIGN KEY ("zonaId") REFERENCES "ZonaHC"("id") ON DELETE CASCADE ON UPDATE CASCADE;

ALTER TABLE "TratamientoHC" ADD CONSTRAINT "TratamientoHC_zonaId_fkey"
  FOREIGN KEY ("zonaId") REFERENCES "ZonaHC"("id") ON DELETE CASCADE ON UPDATE CASCADE;

ALTER TABLE "TratamientoHC" ADD CONSTRAINT "TratamientoHC_tratamientoId_fkey"
  FOREIGN KEY ("tratamientoId") REFERENCES "Tratamiento"("id") ON DELETE SET NULL ON UPDATE CASCADE;

-- CreateIndex
CREATE UNIQUE INDEX "ZonaHC_nombre_profesionalId_key" ON "ZonaHC"("nombre", "profesionalId");
CREATE INDEX "ZonaHC_profesionalId_activo_idx" ON "ZonaHC"("profesionalId", "activo");

CREATE UNIQUE INDEX "DiagnosticoHC_nombre_zonaId_key" ON "DiagnosticoHC"("nombre", "zonaId");
CREATE INDEX "DiagnosticoHC_zonaId_activo_idx" ON "DiagnosticoHC"("zonaId", "activo");
CREATE INDEX "DiagnosticoHC_profesionalId_idx" ON "DiagnosticoHC"("profesionalId");

CREATE UNIQUE INDEX "TratamientoHC_nombre_zonaId_key" ON "TratamientoHC"("nombre", "zonaId");
CREATE INDEX "TratamientoHC_zonaId_activo_idx" ON "TratamientoHC"("zonaId", "activo");
CREATE INDEX "TratamientoHC_profesionalId_idx" ON "TratamientoHC"("profesionalId");
```

Aplicar con `npx prisma migrate deploy` (NUNCA `migrate dev` — patrón pgBouncer del repo desde v1.2). Verificar el DDL contra el schema con `npx prisma migrate status` (debe quedar "up to date").
  </action>
  <verify>
    <automated>cd backend && npx prisma migrate deploy && npx prisma migrate status</automated>
  </verify>
  <done>Migración 20260612000000_add_catalogo_hc aplicada; migrate status reporta "Database schema is up to date"; las 3 tablas existen en la DB de desarrollo</done>
</task>

</tasks>

<verification>
- `npx prisma validate` pasa (relaciones bidireccionales correctas)
- `npx prisma generate` produce client con los 3 modelos nuevos
- `npx prisma migrate status` reporta schema al día tras `migrate deploy`
- `cd backend && npm run build` sigue pasando (ningún código existente roto)
</verification>

<success_criteria>
- Tablas ZonaHC, DiagnosticoHC, TratamientoHC existen en PostgreSQL con FK al profesional (criterio 1 de la fase, parte schema)
- TratamientoHC.tratamientoId nullable con ON DELETE SET NULL hacia Tratamiento
- Migración DDL pura (sin data) siguiendo el patrón manual del repo
</success_criteria>

<output>
After completion, create `.planning/phases/44-schema-cat-logo-en-bd/44-01-SUMMARY.md`
</output>
