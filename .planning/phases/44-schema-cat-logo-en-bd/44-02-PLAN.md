---
phase: 44-schema-cat-logo-en-bd
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - backend/src/modules/catalogo-hc/catalogo-hc.seed-data.ts
  - backend/src/modules/catalogo-hc/catalogo-hc.seed-data.spec.ts
  - backend/src/modules/catalogo-hc/catalogo-hc.service.ts
  - backend/src/modules/catalogo-hc/catalogo-hc.controller.ts
  - backend/src/modules/catalogo-hc/catalogo-hc.module.ts
  - backend/src/app.module.ts
  - backend/src/modules/usuarios/usuarios.service.ts
  - backend/src/modules/usuarios/usuarios.module.ts
autonomous: true
requirements: [ZONA-01, ZONA-02, ZONA-03]

must_haves:
  truths:
    - "El primer GET /catalogo-hc de un profesional sin catálogo siembra las 6 zonas (Abdomen, Mamas, Nariz, Facial, Locales, Otros) y devuelve el catálogo anidado"
    - "Un segundo GET /catalogo-hc no duplica filas (seed idempotente)"
    - "Facial y Locales tienen exactamente 1 diagnóstico: 'Otros' (esSistema=true); sus tratamientos vienen de tratamiento_facial y lunar_cirugia_local"
    - "Crear un usuario con rol PROFESIONAL siembra su catálogo automáticamente"
    - "Cada TratamientoHC del response incluye tratamientoId y precio resueltos por join (null si no hubo match por nombre normalizado)"
  artifacts:
    - path: "backend/src/modules/catalogo-hc/catalogo-hc.seed-data.ts"
      provides: "SEED_ZONAS constante normalizada + normalizarNombre()"
      exports: ["SEED_ZONAS", "normalizarNombre"]
    - path: "backend/src/modules/catalogo-hc/catalogo-hc.service.ts"
      provides: "getCatalogoConSeed, seedCatalogoInicial, crearZona (helper reutilizable Phase 46)"
      exports: ["CatalogoHCService"]
      min_lines: 80
    - path: "backend/src/modules/catalogo-hc/catalogo-hc.controller.ts"
      provides: "GET /catalogo-hc con getProfesionalId() helper"
      contains: "@Controller('catalogo-hc')"
    - path: "backend/src/modules/catalogo-hc/catalogo-hc.seed-data.spec.ts"
      provides: "Tests del seed data: 6 zonas, ZONA-03, nombres corregidos, normalización"
  key_links:
    - from: "backend/src/app.module.ts"
      to: "CatalogoHCModule"
      via: "imports array"
      pattern: "CatalogoHCModule"
    - from: "backend/src/modules/catalogo-hc/catalogo-hc.controller.ts"
      to: "CatalogoHCService.getCatalogoConSeed"
      via: "GET handler"
      pattern: "getCatalogoConSeed"
    - from: "backend/src/modules/usuarios/usuarios.service.ts"
      to: "CatalogoHCService.seedCatalogoInicial"
      via: "hook post-creación de Profesional"
      pattern: "seedCatalogoInicial"
    - from: "backend/src/modules/catalogo-hc/catalogo-hc.service.ts"
      to: "prisma.zonaHC / prisma.tratamiento"
      via: "queries Prisma"
      pattern: "prisma\\.(zonaHC|tratamiento)\\."
---

<objective>
Crear el módulo backend `catalogo-hc/`: servicio con seed idempotente desde los datos del JSON (normalizados), helper `crearZona()` que garantiza el invariante "toda zona nace con Otros", match de precios insensible a tildes contra el catálogo Tratamiento, endpoint GET con lazy seed, y hook de seed al crear un profesional.

Purpose: ZONA-01/02/03 — el catálogo vive en BD por profesional, pre-cargado con los datos actuales corregidos. Phase 45 consume el GET; Phase 46 reutiliza crearZona().
Output: módulo NestJS completo registrado en app.module + hook en usuarios.service.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-schema-cat-logo-en-bd/44-CONTEXT.md
@.planning/phases/44-schema-cat-logo-en-bd/44-RESEARCH.md
@backend/src/modules/tratamientos/tratamientos.controller.ts
@backend/src/modules/usuarios/usuarios.service.ts
@frontend/src/lib/zonas-diagnostico.json

<interfaces>
<!-- Modelos Prisma creados en 44-01 (client ya generado): -->

```
prisma.zonaHC        — { id, nombre, orden, activo, esSistema, profesionalId, createdAt, updatedAt }
                       relaciones: profesional, diagnosticos[], tratamientos[]
                       unique: [nombre, profesionalId]
prisma.diagnosticoHC — { id, nombre, orden(=0), activo, esSistema, zonaId, profesionalId }
                       unique: [nombre, zonaId]; zona onDelete: Cascade
prisma.tratamientoHC — { id, nombre, orden(=0), activo, esSistema, zonaId, profesionalId, tratamientoId? }
                       unique: [nombre, zonaId]; tratamiento? → Tratamiento (precio Decimal)
```

<!-- Punto de creación de Profesional (usuarios.service.ts, método crear(), dentro de $transaction): -->

```typescript
if (dto.rol === RolUsuario.PROFESIONAL) {
  await tx.profesional.create({ data: { usuarioId: usuario.id } });
}
```

<!-- Helper getProfesionalId a copiar (tratamientos.controller.ts líneas 30-58):
     SECRETARIA/ADMIN pasan ?profesionalId=, PROFESIONAL se resuelve por JWT via
     prisma.profesional.findUnique({ where: { usuarioId: user.userId } }).
     PrismaModule es @Global() — PrismaService se inyecta sin imports. -->

<!-- CONTRATO del response GET /catalogo-hc (Phase 45 y plan 44-03 dependen de este shape exacto): -->

```typescript
type ZonaHCResponse = {
  id: string;
  nombre: string;
  orden: number;
  esSistema: boolean;
  diagnosticos: { id: string; nombre: string; orden: number; esSistema: boolean }[];
  tratamientos: {
    id: string;
    nombre: string;
    orden: number;
    esSistema: boolean;
    tratamientoId: string | null;
    precio: number | null; // resuelto desde tratamiento.precio (Decimal → number)
  }[];
}[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed data normalizado + normalizarNombre con tests</name>
  <files>backend/src/modules/catalogo-hc/catalogo-hc.seed-data.ts, backend/src/modules/catalogo-hc/catalogo-hc.seed-data.spec.ts</files>
  <behavior>
    - SEED_ZONAS tiene exactamente 6 zonas en orden: Abdomen(1), Mamas(2), Nariz(3), Facial(4), Locales(5), Otros(9999, esSistema=true)
    - Facial y Locales tienen diagnosticos = [] (el "Otros" lo agrega crearZona, NO el seed data) — ZONA-03
    - Abdomen y Mamas contienen ambos "Hernia" (duplicado intencional entre zonas)
    - Nombres corregidos presentes: "Dermolipectomía", "Reducción de volumen", "Ácido hialurónico labios", "Armonización facial" (con tildes); diagnósticos capitalizados: "Piel", "Dorso alto", "Hipomastia"
    - Ninguna lista de SEED_ZONAS contiene "Otros"/"otros" (los ítems de sistema los inyecta crearZona — no duplicar el "otros" del JSON)
    - normalizarNombre("Dermolipectomía") === normalizarNombre("dermolipectomia") (lowercase + sin diacríticos + trim)
  </behavior>
  <action>
Crear `catalogo-hc.seed-data.ts` exportando:

1. `normalizarNombre(nombre: string): string` — `nombre.toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '').trim()` (Node nativo, sin dependencias).

2. `SEED_ZONAS: SeedZona[]` con tipo `{ nombre: string; orden: number; esSistema: boolean; diagnosticos: string[]; tratamientos: string[] }`. Datos exactos (transformación del JSON `frontend/src/lib/zonas-diagnostico.json` con las correcciones locked en CONTEXT.md):

- **Abdomen** (orden 1): diagnosticos: Piel, Musculo, Grasa, Pared, Hernia. tratamientos: Dermolipectomía, Dermoliposucción, Minidermoliposucción, Gluteoplastia
- **Mamas** (orden 2): diagnosticos: Hipomastia, Hipertrofia, Ptosis, Contractura capsular, Hernia. tratamientos: Aumento de volumen, Reducción de volumen, Levantamiento con implante, Levantamiento sin implante, Reconstrucción de mama, Recambio de implante
- **Nariz** (orden 3): diagnosticos: Dorso alto, Dorso bajo, Giba, Dorso ancho, Punta ancha, Punta caída, Punta indefinida, Punta rotada, Base ancha, Base colapsada, Laterorrinia. tratamientos: Rinoplastia, Rinoplastia estructural
- **Facial** (orden 4): diagnosticos: [] (ZONA-03 — arranca solo con "Otros" vía crearZona). tratamientos: Botox frente, Botox entrecejo, Botox patas de gallo, Ácido hialurónico rinomodelación, Ácido hialurónico labios, Ácido hialurónico surco nasogeniano, Ácido hialurónico pómulos, Ácido hialurónico mentón, Armonización facial
- **Locales** (orden 5): diagnosticos: [] (ZONA-03). tratamientos: Electrocauterio, Resección y plastia
- **Otros** (orden 9999, esSistema: true): diagnosticos: [], tratamientos: [] (queda solo con sus ítems "Otros" de sistema)

IMPORTANTE: las listas NO incluyen "Otros"/"otros" — el ítem de sistema "Otros" (orden 9999, esSistema=true) lo crea siempre `crearZona()` en Task 2. El "otros" del JSON original ES ese ítem de sistema, no duplicar.

Crear `catalogo-hc.seed-data.spec.ts` (Jest, funciones puras, sin NestJS ni DB — patrón historia-clinica.flujo.spec.ts) con los tests del behavior.
  </action>
  <verify>
    <automated>cd backend && npm test -- --testPathPattern=catalogo-hc</automated>
  </verify>
  <done>Tests pasan; SEED_ZONAS y normalizarNombre exportados y verificados contra ZONA-02/ZONA-03</done>
</task>

<task type="auto">
  <name>Task 2: CatalogoHCService — crearZona, seedCatalogoInicial, getCatalogoConSeed</name>
  <files>backend/src/modules/catalogo-hc/catalogo-hc.service.ts</files>
  <action>
Crear `CatalogoHCService` (inyecta PrismaService) con tres métodos públicos:

**`crearZona(profesionalId, nombre, orden, esSistema = false)`** — unidad atómica que garantiza el invariante "toda zona nace con diagnóstico 'Otros' + tratamiento 'Otros'" (Phase 46 la reutiliza para APR-01):
- Idempotente: `findUnique` por `nombre_profesionalId`; si existe, retornarla sin crear nada
- Si no existe, en `prisma.$transaction`: crear ZonaHC, luego DiagnosticoHC `{ nombre: 'Otros', orden: 9999, esSistema: true, zonaId, profesionalId }` y TratamientoHC `{ nombre: 'Otros', orden: 9999, esSistema: true, zonaId, profesionalId, tratamientoId: null }`

**`seedCatalogoInicial(profesionalId)`** — seed completo desde SEED_ZONAS:
- Guard de idempotencia: si `prisma.zonaHC.count({ where: { profesionalId } }) > 0`, retornar sin hacer nada (cuenta sin filtro activo — un catálogo con zonas desactivadas NO debe re-sembrarse)
- Cargar el catálogo de precios una sola vez: `prisma.tratamiento.findMany({ where: { profesionalId, activo: true }, select: { id: true, nombre: true } })` y armar `Map<normalizarNombre(nombre), id>`
- Por cada SeedZona: invocar `crearZona(...)`, luego `createMany` de diagnósticos (orden 1..N, esSistema false) y de tratamientos (orden 1..N, esSistema false, `tratamientoId: matchMap.get(normalizarNombre(nombre)) ?? null`) con `skipDuplicates: true`
- NO crear filas Tratamiento nuevas si no hay match — la FK queda null (los Tratamiento a precio 0 son exclusivos de Phase 46 APR-04)

**`getCatalogoConSeed(profesionalId)`** — lazy seed + catálogo anidado:
- Si `count === 0` para el profesional → `await this.seedCatalogoInicial(profesionalId)`
- Query: `zonaHC.findMany({ where: { profesionalId, activo: true }, orderBy: { orden: 'asc' }, include: { diagnosticos: { where: { activo: true }, orderBy: { orden: 'asc' } }, tratamientos: { where: { activo: true }, orderBy: { orden: 'asc' }, include: { tratamiento: { select: { id: true, precio: true } } } } } })`
- Mapear al contrato ZonaHCResponse (ver interfaces): cada tratamiento HC sale como `{ id, nombre, orden, esSistema, tratamientoId, precio: t.tratamiento ? Number(t.tratamiento.precio) : null }` — convertir Decimal a number, no devolver el objeto tratamiento anidado ni campos internos (activo, zonaId, timestamps fuera del response)
  </action>
  <verify>
    <automated>cd backend && npm run build && npm test -- --testPathPattern=catalogo-hc</automated>
  </verify>
  <done>Service compila; crearZona garantiza el invariante Otros; seedCatalogoInicial idempotente con match de precios por nombre normalizado; getCatalogoConSeed devuelve el contrato anidado con precio resuelto</done>
</task>

<task type="auto">
  <name>Task 3: Controller GET + módulo + registro en app.module + hook en usuarios.service</name>
  <files>backend/src/modules/catalogo-hc/catalogo-hc.controller.ts, backend/src/modules/catalogo-hc/catalogo-hc.module.ts, backend/src/app.module.ts, backend/src/modules/usuarios/usuarios.service.ts, backend/src/modules/usuarios/usuarios.module.ts</files>
  <action>
**Controller** (`catalogo-hc.controller.ts`): `@Controller('catalogo-hc')` + `@Auth('ADMIN', 'PROFESIONAL', 'SECRETARIA')`. Copiar el helper privado `getProfesionalId(user, targetProfesionalId?)` de `tratamientos.controller.ts:30-58` tal cual (patrón del repo: se copia, no se extrae a shared). Único endpoint:

```typescript
@Get()
async getCatalogo(@Req() req: any, @Query('profesionalId') profesionalId?: string) {
  const pid = await this.getProfesionalId(req.user, profesionalId);
  return this.service.getCatalogoConSeed(pid);
}
```

**Módulo** (`catalogo-hc.module.ts`): `controllers: [CatalogoHCController]`, `providers: [CatalogoHCService]`, `exports: [CatalogoHCService]` (UsuariosModule y Phase 46 lo inyectan). PrismaModule es @Global — no requiere imports.

**app.module.ts**: agregar `CatalogoHCModule` al array de imports (junto a TratamientosModule/CirugiasCatalogoModule).

**usuarios.module.ts**: agregar `CatalogoHCModule` a imports.

**usuarios.service.ts** (método `crear()`): hoy el `$transaction` hace `await tx.profesional.create({ data: { usuarioId: usuario.id } })` sin capturar el resultado. Cambios:
1. Capturar: `const profesional = await tx.profesional.create(...)` y hacer que la transacción retorne `{ usuario, profesionalId: profesional?.id }` (profesionalId undefined para otros roles)
2. Después de que la transacción commitee (fuera del callback), si `dto.rol === RolUsuario.PROFESIONAL`: `try { await this.catalogoHCService.seedCatalogoInicial(profesionalId) } catch { /* log warn — el lazy seed del GET cubre el fallo */ }`. El seed NO va dentro de la transacción (usa su propio cliente prisma) y su fallo NO debe romper la creación del usuario
3. Mantener el shape de retorno actual del método (el objeto `usuario` con el select existente)
4. Inyectar `CatalogoHCService` en el constructor

Verificar que los profesionales existentes en producción NO necesitan backfill: se pueblan vía lazy seed al primer GET (decisión locked).
  </action>
  <verify>
    <automated>cd backend && npm run build && npm run lint && npm test -- --testPathPattern=catalogo-hc</automated>
  </verify>
  <done>GET /catalogo-hc registrado y protegido por roles; CatalogoHCModule en app.module; crear usuario PROFESIONAL dispara el seed sin romper la transacción de creación</done>
</task>

</tasks>

<verification>
Verificación end-to-end con el backend corriendo (`npm run start:dev`) y un JWT de PROFESIONAL (o ADMIN + ?profesionalId=):

1. `curl -H "Authorization: Bearer $TOKEN" http://localhost:3001/catalogo-hc` → 200 con array de 6 zonas en orden Abdomen, Mamas, Nariz, Facial, Locales, Otros; cada zona con diagnosticos y tratamientos anidados
2. Repetir el mismo curl → mismo resultado, sin duplicados (idempotencia; sin error P2002 en logs)
3. En el response: Facial y Locales tienen exactamente 1 diagnóstico ("Otros", esSistema=true); Facial tiene 10 tratamientos (9 + Otros), Locales tiene 3 (2 + Otros)
4. Tratamientos con match en el catálogo de precios traen `tratamientoId` y `precio` numérico; sin match traen ambos null
5. Cada zona termina con diagnóstico "Otros" y tratamiento "Otros" al final (orden 9999)
6. SQL spot-check: `SELECT z.nombre, count(d.id) FROM "ZonaHC" z LEFT JOIN "DiagnosticoHC" d ON d."zonaId" = z.id GROUP BY z.nombre;` → Abdomen 6, Mamas 6, Nariz 12, Facial 1, Locales 1, Otros 1
</verification>

<success_criteria>
- Criterio 2 de la fase: profesional nuevo (creado vía usuarios.crear) y profesional existente (vía lazy seed en GET) obtienen las 6 zonas con sus diagnósticos/tratamientos; seed idempotente
- Criterio 3: Facial y Locales con diagnósticos = [Otros]; tratamientos de tratamiento_facial y lunar_cirugia_local presentes
- Criterio 4: GET /catalogo-hc devuelve el catálogo completo anidado con precios resueltos por join
- crearZona() exportado vía CatalogoHCService para reutilización en Phase 46
</success_criteria>

<output>
After completion, create `.planning/phases/44-schema-cat-logo-en-bd/44-02-SUMMARY.md`
</output>
