---
phase: 45-formulario-primera-consulta
plan: "03"
type: execute
wave: 2
depends_on: ["45-01", "45-02"]
files_modified:
  - frontend/src/components/live-turno/tabs/hc/HistorialClinicoPanel.tsx
  - frontend/src/components/patient/PatientDrawer/views/HistoriaClinica.tsx
  - frontend/src/app/dashboard/components/TurnoHCModal.tsx
  - frontend/src/lib/zonas-diagnostico.ts
  - frontend/src/lib/zonas-diagnostico.json
autonomous: false
requirements: [FORM-02, FORM-03]

must_haves:
  truths:
    - "Una entrada de HC guardada con el formulario nuevo se muestra agrupada por zona en HistorialClinicoPanel, PatientDrawer y TurnoHCModal"
    - "Las entradas de HC históricas (forma legacy diagnostico/tratamientos) se siguen mostrando exactamente como hoy"
    - "El JSON hardcodeado zonas-diagnostico ya no existe en el frontend (catálogo BD es la única fuente)"
    - "El flujo completo (seleccionar zonas → diagnósticos/tratamientos → presupuesto → guardar → ver en historial) funciona de punta a punta"
  artifacts:
    - path: "frontend/src/components/live-turno/tabs/hc/HistorialClinicoPanel.tsx"
      provides: "Render dual-shape: zonas[] agrupado + legacy"
      contains: "zonas"
    - path: "frontend/src/components/patient/PatientDrawer/views/HistoriaClinica.tsx"
      provides: "Preview y detalle de entrada con grupos por zona + legacy"
      contains: "zonas"
  key_links:
    - from: "frontend/src/components/live-turno/tabs/hc/HistorialClinicoPanel.tsx"
      to: "contenido.zonas"
      via: "branch dual-shape en el render de primera_vez"
      pattern: "contenido.*zonas|c\\.zonas"
---

<objective>
Actualizar los tres lectores de `contenido` de primera_vez para renderizar la nueva forma agrupada por zona (manteniendo el branch legacy para entradas históricas), eliminar el JSON hardcodeado ya sin consumidores, y verificar el flujo completo con el usuario.

Purpose: cerrar FORM-03 de punta a punta — sin esto, las entradas nuevas se verían vacías o rotas en el historial. El checkpoint valida los 5 criterios de éxito de la fase.
Output: Historial consistente con ambas formas, lib zonas-diagnostico borrado, fase verificada visualmente.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/45-formulario-primera-consulta/45-01-SUMMARY.md
@.planning/phases/45-formulario-primera-consulta/45-02-SUMMARY.md
@frontend/src/components/live-turno/tabs/hc/HistorialClinicoPanel.tsx
@frontend/src/components/patient/PatientDrawer/views/HistoriaClinica.tsx
@frontend/src/app/dashboard/components/TurnoHCModal.tsx

<interfaces>
<!-- Forma del contenido (definida en 45-01, emitida por el form de 45-02). -->

Contenido NUEVO de entradas primera_vez:
```typescript
{
  tipo: 'primera_vez',
  zonas: Array<{
    zonaId: string;
    zona: string;              // "Abdomen"
    diagnosticos: string[];    // nombres seleccionados
    otroTexto?: string;        // texto libre de zona "Otros"
    tratamientos: Array<{ nombre: string; tratamientoId?: string; precio: number }>;
  }>,
  comentario: string,
  presupuestoId: string | null,
  presupuestoTotal: number,
}
```

Contenido LEGACY (entradas históricas — NO migradas, deben seguir renderizando igual):
```typescript
{
  tipo: 'primera_vez',
  diagnostico: { zonas: string[]; subzonas: string[]; otroTexto?: string },
  tratamientos: Array<{ nombre: string; precio?: number }>,
  comentario, presupuestoId, presupuestoTotal,
}
```

Detección: `if (Array.isArray(contenido.zonas))` → forma nueva; else → legacy.

Puntos de render actuales:
- HistorialClinicoPanel.tsx: tipo EntradaPrimeraVez (líneas 13-18) + render badges (líneas 77-102)
- PatientDrawer/views/HistoriaClinica.tsx: ContenidoPrimeraVez (líneas 387-394), FreeEntryPreview (líneas 494-513), FreeEntryFullContent (líneas 517-599)
- TurnoHCModal.tsx: renderer de entradas del historial (~líneas 330-385, isPrimeraVez branch con contenido.diagnostico.zonas)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render dual-shape en los tres lectores de historial</name>
  <files>frontend/src/components/live-turno/tabs/hc/HistorialClinicoPanel.tsx, frontend/src/components/patient/PatientDrawer/views/HistoriaClinica.tsx, frontend/src/app/dashboard/components/TurnoHCModal.tsx</files>
  <action>
    En los tres lectores, agregar branch para la forma nueva ANTES del branch legacy, sin modificar el render legacy existente:

    **HistorialClinicoPanel.tsx:** extender `EntradaPrimeraVez` con `zonas?: Array<{ zona: string; diagnosticos: string[]; otroTexto?: string; tratamientos: Array<{ nombre: string; precio: number }> }>`. Si `pvContenido.zonas` es array: por cada zona, una fila compacta con Badge de zona (variant secondary, capitalize) seguida de badges outline para sus diagnósticos y badges azules (estilo actual de tratamientos) para sus tratamientos. Si no, render legacy actual sin cambios.

    **PatientDrawer/views/HistoriaClinica.tsx:**
    - Extender `ContenidoPrimeraVez` con el campo `zonas?` (misma forma).
    - `FreeEntryPreview`: si `c.zonas` → `parts` por zona: `"Abdomen: Diástasis · Abdominoplastia"` o al menos `Zonas: ...` + `Tratamientos: ...` aplanados — mantener una línea truncada legible.
    - `FreeEntryFullContent`: si `c.zonas` → una sección por zona con encabezado h4 con el nombre de la zona; dentro, bloque de diagnósticos (lista o texto unido por comas, estilo bg-muted/40 actual), `otroTexto` si existe, y la lista de tratamientos con precio formateado ARS (reutilizar el patrón Intl.NumberFormat existente). Mantener comentario y presupuestoTotal como hoy (viven en el nivel raíz del contenido en ambas formas).

    **TurnoHCModal.tsx (renderer de historial únicamente — el lado creador ya fue migrado en 45-02):** en el branch isPrimeraVez, si `contenido.zonas` es array → grupos por zona (Badge de zona + badges de diagnósticos + badges de tratamientos, mismo estilo que el render actual); else legacy sin cambios.

    En los tres casos las HC históricas no se modifican ni re-interpretan (Out of Scope del milestone): solo se agrega el branch nuevo.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Entradas nuevas se muestran agrupadas por zona en los tres lectores; entradas legacy renderizan idéntico a hoy; tipos sin `any` nuevos en las firmas extendidas.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar zonas-diagnostico hardcodeado</name>
  <files>frontend/src/lib/zonas-diagnostico.ts, frontend/src/lib/zonas-diagnostico.json</files>
  <action>
    Verificar que no quedan imports: `rg "zonas-diagnostico" frontend/src --glob '!lib/zonas-diagnostico*'` debe devolver vacío (45-02 eliminó el único consumidor, PrimeraConsultaForm). Si aparece algún import residual, migrarlo al catálogo BD antes de borrar. Luego eliminar ambos archivos (`git rm frontend/src/lib/zonas-diagnostico.ts frontend/src/lib/zonas-diagnostico.json`). Los datos ya viven en `backend/src/modules/catalogo-hc/catalogo-hc.seed-data.ts` (SEED_ZONAS, Phase 44).
  </action>
  <verify>
    <automated>cd frontend && test ! -f src/lib/zonas-diagnostico.json && npm run build</automated>
  </verify>
  <done>Ambos archivos eliminados; build de frontend verde; cero referencias a zonas-diagnostico en el código.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual del flujo completo</name>
  <action>Presentar al usuario la verificación visual del flujo completo de la fase (pasos en how-to-verify) y esperar aprobación antes de cerrar la fase.</action>
  <what-built>Plantilla Primera Consulta rediseñada con zona como eje (catálogo BD), guardado agrupado por zona, lookup de precios y presupuesto preservados, historial con render agrupado.</what-built>
  <how-to-verify>
    Con backend (`cd backend && npm run start:dev`) y frontend (`cd frontend && npm run dev`) corriendo, logueado como PROFESIONAL:

    1. Abrir un paciente (PatientDrawer → Historia Clínica → nueva entrada) y elegir "Primera Consulta". **Esperado:** se ven solo los chips de zonas (Abdomen, Mamas, Nariz, Facial, Locales, Otros) — ningún diagnóstico ni tratamiento visible todavía.
    2. Seleccionar "Abdomen". **Esperado:** se despliega un bloque con etiqueta "Abdomen" con sus diagnósticos y sus tratamientos; las demás zonas siguen colapsadas.
    3. Seleccionar también "Mamas". **Esperado:** dos bloques, cada uno con la etiqueta de su zona visible encima de sus diagnósticos/tratamientos.
    4. Seleccionar un tratamiento con precio cargado en Configuración → Tratamientos. **Esperado:** al tocar "Generar Presupuesto", el modal muestra el item con ese precio. Crear el presupuesto.
    5. Seleccionar la zona "Otros". **Esperado:** aparece el input "Describir zona..." dentro de su bloque.
    6. Guardar la HC. **Esperado:** toast "HC guardada." y la entrada aparece en el historial mostrando los diagnósticos/tratamientos agrupados por zona (etiqueta de zona visible).
    7. Abrir una entrada de Primera Consulta ANTERIOR al cambio (si existe). **Esperado:** se ve igual que antes (zonas/subzonas/tratamientos planos).
    8. Repetir el alta rápida desde la agenda (widget → TurnoHCModal → Primera Consulta) para confirmar que el segundo punto de entrada también funciona.
  </how-to-verify>
  <resume-signal>Escribí "approved" o describí los problemas encontrados</resume-signal>
  <verify>Respuesta "approved" del usuario tras ejecutar los 8 pasos</verify>
  <done>Usuario confirmó los 5 criterios de éxito de la fase: zonas primero, despliegue por zona, agrupación visual multi-zona, guardado agrupado visible en historial, presupuesto con precio correcto</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit && npm run build` — verde
- `rg "zonas-diagnostico" frontend/src` — sin resultados
- Checkpoint aprobado por el usuario (criterios 1-5 de la fase)
</verification>

<success_criteria>
- Entradas nuevas de primera_vez se muestran agrupadas por zona en HistorialClinicoPanel, PatientDrawer y TurnoHCModal (FORM-02/FORM-03)
- Entradas históricas renderizan sin cambios
- zonas-diagnostico.{ts,json} eliminados — el catálogo BD es la única fuente
- Usuario aprobó el flujo completo de punta a punta
</success_criteria>

<output>
After completion, create `.planning/phases/45-formulario-primera-consulta/45-03-SUMMARY.md`
</output>
