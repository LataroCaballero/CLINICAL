---
phase: 51-schema-foundation-chat-fix
plan: 02
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - backend/src/prisma/migrations/
  - backend/src/modules/pacientes/seguimiento-scheduler.service.ts
autonomous: true
requirements: [CHAT-01, CHAT-02]
must_haves:
  truths:
    - "A single versioned migration applies ALL v1.12 schema changes AND the CHAT-02 cleanup DELETE atomically"
    - "After deploy, the scheduler creates a 'Seguimiento CRM' message only once per task, never again until completed (SC#1)"
    - "The historical esSistema=true flood is removed from the chat (SC#2)"
    - "The cleanup DELETE and the notificada guard ship in the same migration/release — no window (SC#3, D-06)"
    - "All existing screens load after migration; no regressions from new columns (SC#4)"
  artifacts:
    - path: "backend/src/prisma/migrations/"
      provides: "Versioned big-bang migration: DDL + ALTER autorId DROP NOT NULL + DELETE cleanup"
      contains: "DELETE FROM \"MensajeInterno\" WHERE \"esSistema\" = true"
    - path: "backend/src/modules/pacientes/seguimiento-scheduler.service.ts"
      provides: "CHAT-01 dedupe guard"
      contains: "notificada: false"
  key_links:
    - from: "seguimiento-scheduler.service.ts findMany"
      to: "TareaSeguimiento.notificada"
      via: "WHERE notificada: false"
      pattern: "notificada:\\s*false"
    - from: "seguimiento-scheduler.service.ts after mensajeInterno.create"
      to: "TareaSeguimiento.update"
      via: "set notificada: true, notificadaEn"
      pattern: "notificada:\\s*true"
    - from: "migration.sql"
      to: "MensajeInterno rows"
      via: "DELETE WHERE esSistema=true (cascade-safe via MensajeLectura.onDelete: Cascade)"
      pattern: "DELETE FROM \"MensajeInterno\""
---

<objective>
Apply the schema authored in plan 01 as a single versioned big-bang migration, hand-append the CHAT-02 cleanup `DELETE`, regenerate the Prisma client, and fix the CHAT-01 scheduler so a task is alerted exactly once. This plan IS the atomic release unit: the cleanup and the dedupe guard ship together, so the flood cannot regrow within a cron cycle (D-06 / SC#3).

Purpose: Stop the daily "Seguimiento CRM" spam permanently and wipe the historical flood in one migration, while landing the full milestone schema.
Output: One applied migration (DDL + DELETE), regenerated client, fixed scheduler service.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/51-schema-foundation-chat-fix/51-CONTEXT.md
@.planning/phases/51-schema-foundation-chat-fix/51-PATTERNS.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Exact current scheduler body the executor patches (seguimiento-scheduler.service.ts). -->

Current buggy findMany WHERE (lines 21-25):
  where: {
    completada: false,
    fechaProgramada: { lte: ahora },
  },

Current no-op block after mensajeInterno.create (lines 61-66) — a log-only comment that must be REPLACED with a real update:
  // Marcar tarea como "notificada" — el usuario la completa manualmente
  // ... (solo registramos en log por ahora)
  this.logger.log(`Alerta creada para paciente ${tarea.paciente.nombreCompleto} — ${tarea.tipo}`);

`ahora` (const ahora = new Date()) is already in scope at line 19 — reuse it for notificadaEn.
The per-item try/catch (lines 37-69) already wraps create — the new update goes INSIDE the same try.

Cascade fact: MensajeLectura.mensaje has onDelete: Cascade (schema.prisma line 244) → DELETE of a MensajeInterno auto-removes its read-receipts; no orphan MensajeLectura, no manual pre-delete.
Cleanup safety fact (D-01): the ONLY producer of esSistema=true in production is this scheduler. Presupuesto messages (presupuestos.service.ts) use esSistema=false → untouched.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Generate, hand-edit, and apply the big-bang migration (CHAT-02 cleanup)</name>
  <files>backend/src/prisma/migrations/ (new versioned migration dir)</files>
  <read_first>
    - backend/src/prisma/schema.prisma (the edited schema from plan 01 — source of the migration DDL)
    - .planning/phases/51-schema-foundation-chat-fix/51-CONTEXT.md §D-01, §D-02, §D-03, §D-06 (DELETE scope, cascade-safety, idempotency, off-peak timing)
    - .planning/research/PITFALLS.md Pitfall 11 (destructive cleanup partial-application; off-peak; do NOT delete TareaSeguimiento rows)
    - backend/src/prisma/migrations/migration_lock.toml and the latest existing migration dir (confirm provider = postgresql, naming convention)
  </read_first>
  <action>
    This task is BLOCKING and mandatory — the phase cannot pass verification without the migration applied (build/type checks pass from schema config even without a live migration, a false-positive trap). Steps, in order:

    1. Scaffold without applying: `cd backend && npx prisma migrate dev --create-only --name v1_12_schema_foundation_chat_fix`. This generates the migration directory and `migration.sql` with all DDL Prisma derives from plan 01's schema, including the auto-generated `ALTER TABLE "MensajeInterno" ALTER COLUMN "autorId" DROP NOT NULL;` (because autorId became `String?`).
    2. Confirm the generated SQL contains `ALTER COLUMN "autorId" DROP NOT NULL` and the two `CREATE TABLE "AlergiaCatalogoPro"` / `CREATE TABLE "MedicamentoCatalogoPro"` statements and the new Paciente/TareaSeguimiento/HistoriaClinicaEntrada columns. If any expected DDL is missing, stop and report (schema/plan-01 mismatch).
    3. Using the Edit/Write tool (never a heredoc), APPEND to the end of that `migration.sql`, after all DDL, the CHAT-02 cleanup with a deploy-timing comment:
       a comment line documenting D-06: run off-peak (between 23:00 and 07:00 ART), never at 09:00 when the cron fires, to avoid lock contention;
       then the statement `DELETE FROM "MensajeInterno" WHERE "esSistema" = true;`
       Do NOT add any DELETE against `TareaSeguimiento` (legitimate business data — PITFALL 11). Do NOT pre-delete `MensajeLectura` rows (DB-level cascade handles them — D-02). The DELETE is idempotent (re-run on empty set is a no-op — D-02) and captures both dated "Seguimiento CRM" and PERSONALIZADA system alerts (D-03 — text LIKE filter rejected).
    4. Apply the edited migration: `cd backend && npx prisma migrate dev`. This applies the pending migration including the appended DELETE.
    5. Regenerate the client so plan-02 Task 2 typechecks against the new `notificada` field: `cd backend && npx prisma generate`.
  </action>
  <acceptance_criteria>
    - `cd backend && npx prisma migrate status` reports the `v1_12_schema_foundation_chat_fix` migration as applied / database schema up to date.
    - The migration's `migration.sql` contains `DELETE FROM "MensajeInterno" WHERE "esSistema" = true;` AND `ALTER COLUMN "autorId" DROP NOT NULL`.
    - The migration's `migration.sql` contains `CREATE TABLE "AlergiaCatalogoPro"` and `CREATE TABLE "MedicamentoCatalogoPro"`.
    - `migration.sql` contains NO `DELETE FROM "TareaSeguimiento"` and NO manual `DELETE FROM "MensajeLectura"`.
    - After apply, `SELECT count(*) FROM "MensajeInterno" WHERE "esSistema" = true;` returns 0 (verifiable via `npx prisma db execute` or psql against DATABASE_URL).
    - Re-querying does not error and a hypothetical second run of the DELETE affects 0 rows (idempotency — DELETE WHERE on empty set).
    - `cd backend && npx prisma generate` completes; the generated client exposes `tareaSeguimiento.notificada`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx prisma migrate status && grep -q 'DELETE FROM "MensajeInterno" WHERE "esSistema" = true' src/prisma/migrations/*v1_12_schema_foundation_chat_fix*/migration.sql && echo OK</automated>
  </verify>
  <done>Versioned migration applied with DDL + ALTER autorId DROP NOT NULL + cleanup DELETE; esSistema=true count is 0; client regenerated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the seguimiento scheduler dedupe guard (CHAT-01)</name>
  <files>backend/src/modules/pacientes/seguimiento-scheduler.service.ts</files>
  <read_first>
    - backend/src/modules/pacientes/seguimiento-scheduler.service.ts (full file, 72 lines — the WHERE at 21-25, the create at 51-59, the no-op log block at 61-66, the try/catch at 37-69)
    - .planning/phases/51-schema-foundation-chat-fix/51-CONTEXT.md §D-04, §D-05 (one alert per task then silence; mechanism = notificada flag)
    - .planning/phases/51-schema-foundation-chat-fix/51-PATTERNS.md "CHAT-01 guard fix" section (exact before/after)
  </read_first>
  <behavior>
    - A TareaSeguimiento with `completada: false, notificada: false, fechaProgramada <= now` is selected and produces exactly one MensajeInterno, then the task is updated to `notificada: true`.
    - A TareaSeguimiento already `notificada: true` (and still `completada: false`) is NOT selected by the scheduler → produces zero new messages no matter how many cron cycles pass (SC#1).
    - The task remains `completada: false` after notification (the professional closes it manually — D-05); only `notificada`/`notificadaEn` change.
  </behavior>
  <action>
    Edit backend/src/modules/pacientes/seguimiento-scheduler.service.ts (CHAT-01 / D-05):

    1. In the `findMany` WHERE (currently `completada: false, fechaProgramada: { lte: ahora }`), add `notificada: false` so already-alerted tasks are skipped.
    2. Replace the log-only no-op block (the comment "Marcar tarea como notificada ... solo registramos en log por ahora" and the trailing `this.logger.log('Alerta creada ...')`) with a real `await this.prisma.tareaSeguimiento.update({ where: { id: tarea.id }, data: { notificada: true, notificadaEn: ahora } })`, placed immediately after the existing `mensajeInterno.create`, inside the same per-item try block so an update failure is caught by the existing catch. Reuse the in-scope `ahora` constant for `notificadaEn`. Do NOT set `completada` (the task stays open for manual completion — D-05). Keep the `@Cron(CronExpression.EVERY_DAY_AT_9AM)` decorator unchanged.

    If a unit test harness for this service does not already exist, create `seguimiento-scheduler.service.spec.ts` with a mocked PrismaService asserting the two behaviors above (notified task skipped; new task marks notificada:true). If adding a test is disproportionate to the 3-line change, set the automated verify to the grep gate below and note the assertion in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -E "notificada:\s*false" backend/src/modules/pacientes/seguimiento-scheduler.service.ts` matches inside the findMany WHERE.
    - `grep -E "notificada:\s*true" backend/src/modules/pacientes/seguimiento-scheduler.service.ts` matches inside a `tareaSeguimiento.update` call that also sets `notificadaEn`.
    - The file no longer contains the string `solo registramos en log por ahora`.
    - No `completada: true` is written by the scheduler (`grep -v '^\s*//' | grep -c "completada: true"` returns 0).
    - `cd backend && npm run build` compiles with no type errors (relies on the regenerated client from Task 1).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npm run build && grep -Eq "notificada:\s*false" src/modules/pacientes/seguimiento-scheduler.service.ts && grep -Eq "notificada:\s*true" src/modules/pacientes/seguimiento-scheduler.service.ts && echo OK</automated>
  </verify>
  <done>Scheduler selects only un-notified overdue tasks and marks each notificada:true after one alert; build passes against regenerated client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy operator → migration DML | The hand-appended DELETE runs at deploy time with trusted DB credentials; scope must be exact |
| cron scheduler → MensajeInterno | Internal daily job; the only producer of esSistema=true rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering / Information disclosure | Cleanup `DELETE FROM "MensajeInterno"` scope | mitigate | WHERE is exactly `"esSistema" = true`. D-01 verified the only esSistema=true producer is the buggy scheduler; presupuesto messages use esSistema=false. Acceptance: `SELECT count(*) FROM "MensajeInterno" WHERE "esSistema" = false` is unchanged across the migration (legitimate messages preserved). No text-LIKE filter (D-03 — would miss PERSONALIZADA). |
| T-51-02 | Denial of service | Cascade delete + cron lock contention | mitigate | Cascade is bounded to `MensajeLectura` rows of the deleted MensajeInterno (onDelete: Cascade, line 244) — no manual pre-delete. D-06: run off-peak (23:00–07:00 ART), never at 09:00 cron time; documented as a comment in migration.sql. DELETE on TareaSeguimiento explicitly forbidden (PITFALL 11). |
| T-51-05 | Tampering | Migration false-positive (schema config compiles without a live DB) | mitigate | Task 1 is [BLOCKING] and runs `prisma migrate dev` + `migrate status`; verification asserts applied state and esSistema=true count 0, not just a passing build. |
| T-51-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Phase 51 installs ZERO new packages. No package-legitimacy gate required. |
</threat_model>

<verification>
- `npx prisma migrate status` shows the migration applied.
- `SELECT count(*) FROM "MensajeInterno" WHERE "esSistema" = true` returns 0; legitimate (esSistema=false) messages unchanged.
- migration.sql contains the DDL, the ALTER autorId DROP NOT NULL, and the appended DELETE; no TareaSeguimiento/MensajeLectura deletes.
- Scheduler WHERE includes `notificada: false`; post-create `tareaSeguimiento.update` sets `notificada: true, notificadaEn`.
- `npm run build` passes against the regenerated client.
- Manual/regression smoke (SC#4): existing chat views (`mensajes-internos.service.ts` findChats / findByPaciente / unread counters) still load — no broken read-receipts after the cascade purge.
</verification>

<success_criteria>
The big-bang migration is applied (schema + cleanup DELETE), the historical esSistema=true flood is gone, and the scheduler alerts each task exactly once — all in one release with no window where messages are cleaned but the guard is inactive (SC#1–SC#3). The app builds and existing screens load (SC#4).
</success_criteria>

<output>
Create `.planning/phases/51-schema-foundation-chat-fix/51-02-SUMMARY.md` when done
</output>
