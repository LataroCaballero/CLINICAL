---
phase: 52-preop-hc-form-chip-catalogs
plan: 05
type: execute
wave: 3
depends_on: ["52-02"]
files_modified:
  - frontend/src/hooks/useAntecedentesCatalogo.ts
  - frontend/src/hooks/useAlergiasCatalogo.ts
  - frontend/src/hooks/useMedicamentosCatalogo.ts
  - frontend/src/hooks/useCreateHistoriaClinicaEntry.ts
autonomous: true
requirements: [PREOP-03, PREOP-04, PREOP-06, PREOP-07]
must_haves:
  truths:
    - "The form can load each per-professional catalog (antecedentes/alergias/medicamentos) via dedicated hooks"
    - "After saving a PREOP entry, the three flat catalogs are invalidated so learned Otro values appear next open"
    - "The create-entry hook accepts the PREOP DTO fields"
  artifacts:
    - path: "frontend/src/hooks/useAntecedentesCatalogo.ts"
      provides: "TanStack Query hook for antecedentes catalog"
      contains: "antecedentes-catalogo"
    - path: "frontend/src/hooks/useCreateHistoriaClinicaEntry.ts"
      provides: "PREOP DTO fields + catalog invalidation"
      contains: "consentimientoInformado"
  key_links:
    - from: "useCreateHistoriaClinicaEntry onSuccess"
      to: "antecedentes/alergias/medicamentos catalog query keys"
      via: "invalidateQueries"
      pattern: "antecedentes-catalogo"
---

<objective>
Add the three flat-catalog TanStack Query hooks consumed by the prequirúrgico form, and extend `useCreateHistoriaClinicaEntry` with the PREOP DTO fields plus invalidation of the three catalog query keys so newly-learned "Otro" values surface on the next form open (PREOP-04).

Purpose: Frontend data layer for PREOP-03/04/06/07 chips + learning refresh.
Output: 3 new hooks (mirroring useCatalogoHC) + DTO/invalidation additions.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-preop-hc-form-chip-catalogs/52-PATTERNS.md

<interfaces>
From useCatalogoHC.ts (full pattern, 22 lines): useQuery({ queryKey:[KEY, profesionalId], queryFn: api.get('/catalogo-hc', {params: profesionalId?{profesionalId}:{}}), enabled, staleTime:5min, gcTime:30min })
Backend endpoints (Plan 02): GET /catalogo-hc/antecedentes, /catalogo-hc/alergias, /catalogo-hc/medicamentos — each returns CatalogoItem[] { id, nombre, esSistema }
From useCreateHistoriaClinicaEntry.ts: DTO type (around line 60) with tipo/zonas/etc; onSuccess invalidation block (lines 82-95) already invalidates ['catalogo-hc'] etc.
api client: frontend/src/lib/api.ts (axios with JWT interceptor)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the three flat-catalog hooks</name>
  <read_first>
    - frontend/src/hooks/useCatalogoHC.ts (full file — verbatim pattern)
    - frontend/src/lib/api.ts (axios instance)
  </read_first>
  <action>Create useAntecedentesCatalogo.ts exporting `ANTECEDENTES_CATALOGO_QUERY_KEY = 'antecedentes-catalogo'`, a `CatalogoItem` interface `{ id: string; nombre: string; esSistema: boolean }`, and `useAntecedentesCatalogo(profesionalId?)` calling `api.get('/catalogo-hc/antecedentes', { params: profesionalId ? { profesionalId } : {} })`, `enabled: !!profesionalId`, staleTime 5min, gcTime 30min. Create useAlergiasCatalogo.ts ('alergias-catalogo', '/catalogo-hc/alergias') and useMedicamentosCatalogo.ts ('medicamentos-catalogo', '/catalogo-hc/medicamentos') the same way. Export each query-key constant so Plan 05 Task 2 and Plan 06 can reuse them.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "antecedentes-catalogo" src/hooks/useAntecedentesCatalogo.ts && grep -q "alergias-catalogo" src/hooks/useAlergiasCatalogo.ts && grep -q "medicamentos-catalogo" src/hooks/useMedicamentosCatalogo.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Three hook files exist, each exporting its QUERY_KEY constant and a useXxxCatalogo(profesionalId?) hook hitting the matching backend endpoint
    - Each query is enabled only when profesionalId is present
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Catalog hooks compile and match the useCatalogoHC pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Extend useCreateHistoriaClinicaEntry with PREOP fields + invalidation</name>
  <read_first>
    - frontend/src/hooks/useCreateHistoriaClinicaEntry.ts (full file — DTO type ~line 60, onSuccess invalidation 82-95)
  </read_first>
  <action>In useCreateHistoriaClinicaEntry.ts extend the create DTO type with optional PREOP fields: `antecedentes?: string[]`, `alergias?: string[]`, `medicacion?: string[]`, `estudiosComplementarios?: { laboratorio: boolean; ecg: boolean; imagenes: string[] }`, `consentimientoInformado?: boolean`. In the `onSuccess` callback add three `qc.invalidateQueries({ queryKey: ['antecedentes-catalogo'] })`, `['alergias-catalogo']`, `['medicamentos-catalogo']` (import the exported key constants from the Task-1 hooks rather than hardcoding if convenient). Do not remove existing invalidations.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "consentimientoInformado" src/hooks/useCreateHistoriaClinicaEntry.ts && grep -q "antecedentes-catalogo" src/hooks/useCreateHistoriaClinicaEntry.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - DTO type accepts antecedentes/alergias/medicacion/estudiosComplementarios/consentimientoInformado
    - onSuccess invalidates antecedentes-catalogo, alergias-catalogo, medicamentos-catalogo in addition to existing keys
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Create-entry hook carries PREOP fields and refreshes catalogs post-save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| frontend → catalog API | Read-only; auth handled by axios JWT interceptor |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-14 | Spoofing | catalog requests | mitigate | All requests via the shared api client that attaches the JWT; backend re-derives scope server-side (Plan 02) |
| T-52-15 | Information Disclosure | profesionalId in query | accept | profesionalId is a non-secret scoping hint; backend authorizes it server-side, client value is advisory only |
| T-52-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds
</verification>

<success_criteria>
- Three catalog hooks available and typed
- Create-entry hook carries PREOP fields and invalidates the flat catalogs
</success_criteria>

<output>
Create `.planning/phases/52-preop-hc-form-chip-catalogs/52-05-SUMMARY.md` when done.
</output>
