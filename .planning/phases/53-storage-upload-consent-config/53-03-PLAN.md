---
phase: 53-storage-upload-consent-config
plan: 03
type: execute
wave: 3
depends_on: [53-02]
files_modified:
  - frontend/src/types/consentimientos.ts
  - frontend/src/hooks/useConsentimientos.ts
  - frontend/src/hooks/useConsentimientosMutations.ts
  - frontend/src/app/dashboard/configuracion/components/GestionConsentimientos.tsx
  - frontend/src/app/dashboard/configuracion/page.tsx
autonomous: false
requirements: [CONS-01, CONS-02]

must_haves:
  truths:
    - "A 'Consentimientos' tab appears in Configuración for both the PROFESIONAL and SECRETARIA views"
    - "The tab lists the professional's ZonaHC zonas"
    - "Per zona, the user can upload a consent PDF and see the current vigente filename + upload date"
    - "Per zona, the user can enter and save an indicaciones URL that persists after a page reload"
  artifacts:
    - path: "frontend/src/types/consentimientos.ts"
      provides: "ZonaConConsentimiento + ConsentimientoZonaArchivo types"
      contains: "ZonaConConsentimiento"
    - path: "frontend/src/hooks/useConsentimientos.ts"
      provides: "Query hook for /consentimientos/zonas"
      contains: "useConsentimientos"
    - path: "frontend/src/hooks/useConsentimientosMutations.ts"
      provides: "Upload PDF + update indicaciones mutations"
      contains: "useUploadConsentimiento"
    - path: "frontend/src/app/dashboard/configuracion/components/GestionConsentimientos.tsx"
      provides: "Per-zona consent upload + indicaciones URL UI"
      contains: "GestionConsentimientos"
  key_links:
    - from: "frontend/src/app/dashboard/configuracion/components/GestionConsentimientos.tsx"
      to: "useConsentimientos / useConsentimientosMutations"
      via: "hook calls"
      pattern: "useConsentimientos|useUploadConsentimiento|useUpdateIndicaciones"
    - from: "frontend/src/app/dashboard/configuracion/page.tsx"
      to: "GestionConsentimientos"
      via: "TabsContent value=consentimientos"
      pattern: "consentimientos"
---

<objective>
Add the "Consentimientos" tab to Configuración (D-12): a new top-level tab (NOT nested under "Catálogo HC") that lists the professional's surgical zonas and, per zona, lets the user upload a consent PDF (showing the current vigente file) and save an indicaciones URL.

Purpose: Front-end completion of CONS-01 (upload consent PDF) and CONS-02 (indicaciones link), consuming the endpoints from 53-02 and satisfying SC#1 (visible upload) and SC#3 (indicaciones persist after reload).
Output: types, two hooks, the GestionConsentimientos component, and the tab wiring in both PROFESIONAL and SECRETARIA views.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-storage-upload-consent-config/53-CONTEXT.md
@.planning/phases/53-storage-upload-consent-config/53-PATTERNS.md
@.planning/phases/53-storage-upload-consent-config/53-02-SUMMARY.md

<interfaces>
<!-- Backend contract from 53-02 -->
GET   /consentimientos/zonas                       → ZonaConConsentimiento[]
POST  /consentimientos/zonas/:zonaId/pdf           (multipart 'file', max 10MB)
PATCH /catalogo-hc/zonas/:id/indicaciones          body { indicacionesUrl: string | null }

ZonaConConsentimiento:
  { id, nombre, orden, esSistema, indicacionesUrl: string | null,
    consentimientoVigente: { id, zonaId, path, nombreOriginal, uploadedAt, vigente, url? } | null }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + TanStack Query hooks for consentimientos</name>
  <files>frontend/src/types/consentimientos.ts, frontend/src/hooks/useConsentimientos.ts, frontend/src/hooks/useConsentimientosMutations.ts</files>
  <read_first>
    - frontend/src/types/catalogo-hc.ts (lines 1-24 — type shape to mirror)
    - frontend/src/hooks/useCatalogoHC.ts (lines 1-22 — query hook pattern incl. queryKey with profesionalId, staleTime)
    - frontend/src/hooks/useCatalogoHCMutations.ts (lines 1-96 — mutation + invalidation pattern)
    - frontend/src/lib/api.ts (lines 3-6, 27-35 — axios instance, JWT auto-attach, multipart header note)
    - .planning/phases/53-storage-upload-consent-config/53-PATTERNS.md (types lines 657-679; useConsentimientos lines 575-599; useConsentimientosMutations lines 603-653)
  </read_first>
  <action>
    Create `frontend/src/types/consentimientos.ts` with `ConsentimientoZonaArchivo` ({ id, zonaId, path, nombreOriginal, uploadedAt: string, vigente, url?: string }) and `ZonaConConsentimiento` ({ id, nombre, orden, esSistema, indicacionesUrl: string | null, consentimientoVigente: ConsentimientoZonaArchivo | null }) mirroring catalogo-hc.ts.
    Create `useConsentimientos.ts`: export `CONSENTIMIENTOS_QUERY_KEY = 'consentimientos-zonas'` and a `useConsentimientos(profesionalId?, options?)` hook calling `api.get('/consentimientos/zonas', { params: profesionalId ? { profesionalId } : {} })`, queryKey `[CONSENTIMIENTOS_QUERY_KEY, profesionalId]`, staleTime 5min, gcTime 30min, enabled default true — exactly the useCatalogoHC pattern.
    Create `useConsentimientosMutations.ts` with two mutations:
      - `useUploadConsentimiento(profesionalId?)`: mutationFn builds FormData (append 'file'), POSTs `/consentimientos/zonas/${zonaId}/pdf` with header `'Content-Type': 'multipart/form-data'` and params profesionalId; onSuccess invalidate `[CONSENTIMIENTOS_QUERY_KEY]`.
      - `useUpdateIndicaciones(profesionalId?)`: mutationFn PATCHes `/catalogo-hc/zonas/${zonaId}/indicaciones` body `{ indicacionesUrl }` with params profesionalId; onSuccess invalidate BOTH `[CONSENTIMIENTOS_QUERY_KEY]` and `[CATALOGO_HC_QUERY_KEY]` (indicacionesUrl lives on ZonaHC).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: consentimientos.ts exports `ZonaConConsentimiento` and `ConsentimientoZonaArchivo`.
    - SOURCE: useConsentimientos.ts queryKey includes profesionalId so SECRETARIA's selected scope is respected.
    - SOURCE: useConsentimientosMutations.ts upload uses FormData with `multipart/form-data`; update invalidates both CONSENTIMIENTOS_QUERY_KEY and CATALOGO_HC_QUERY_KEY.
    - `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
  <done>Types and three hooks (query + two mutations) exist following the catalogo-hc conventions and typecheck cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: GestionConsentimientos component + Configuración tab wiring</name>
  <files>frontend/src/app/dashboard/configuracion/components/GestionConsentimientos.tsx, frontend/src/app/dashboard/configuracion/page.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/configuracion/components/GestionCatalogoHC.tsx (lines 1-37 imports; line 48 props; lines 153-167 loading/error/empty; lines 168-315 zone iteration; lines 103-106 toast)
    - frontend/src/app/dashboard/configuracion/page.tsx (lines 11-21 imports; lines 108-120 PROFESIONAL TabsList grid-cols-11; line 211 SECRETARIA TabsList grid-cols-6; both Tabs bodies)
    - frontend/src/hooks/useCatalogoHC.ts (zone listing — reused to enumerate zonas) and this plan's Task 1 hooks
    - .planning/phases/53-storage-upload-consent-config/53-PATTERNS.md (page.tsx tab lines 500-519; GestionConsentimientos lines 523-571)
    - .planning/phases/53-storage-upload-consent-config/53-CONTEXT.md (D-12)
  </read_first>
  <action>
    Create `GestionConsentimientos({ profesionalId }: { profesionalId?: string })` ('use client') modeled on GestionCatalogoHC. Fetch zonas-with-consent via `useConsentimientos(profesionalId)` (this already carries indicacionesUrl + consentimientoVigente; no need to also call useCatalogoHC unless a zona has no consent row — useConsentimientos returns all active zonas regardless). Use `useUploadConsentimiento(profesionalId)` and `useUpdateIndicaciones(profesionalId)`.
    Render loading (Loader2 spinner), error (red text), and empty ("No hay zonas configuradas") states copied from GestionCatalogoHC lines 153-167. For each zona render a Card with:
      - The zona nombre (capitalize) as title.
      - An indicaciones row: a URL `<Input>` initialized from `zona.indicacionesUrl ?? ''` held in local state keyed by zona id, plus a Save button that calls useUpdateIndicaciones.mutate({ zonaId, indicacionesUrl }) and toasts success/error.
      - A consent row: `<input type="file" accept="application/pdf,.pdf">`, an Upload button that calls useUploadConsentimiento.mutate({ zonaId, file }) (disable while pending, show Loader2), and a line showing the current vigente file: `consentimientoVigente?.nombreOriginal` + formatted `uploadedAt`, or "Sin consentimiento cargado" when null. If `consentimientoVigente?.url` exists, render it as an anchor (target _blank) so the user can open the current PDF.
    Use `toast.success`/`toast.error` from sonner per the GestionCatalogoHC pattern.
    In page.tsx: import GestionConsentimientos. In the PROFESIONAL TabsList add `<TabsTrigger value="consentimientos">Consentimientos</TabsTrigger>` and bump the grid-cols count from 11 to 12; add `<TabsContent value="consentimientos" className="mt-6"><GestionConsentimientos profesionalId={...} /></TabsContent>` passing the same profesionalId prop the sibling tabs use. Mirror the addition in the SECRETARIA TabsList (bump grid-cols-6 → 7) and its Tabs body. Do NOT nest this into the "Catálogo HC" tab (D-12).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npm run lint</automated>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: page.tsx contains a `TabsTrigger value="consentimientos"` and a `TabsContent value="consentimientos"` rendering `<GestionConsentimientos />` in BOTH the PROFESIONAL and SECRETARIA views; the respective grid-cols counts were incremented (D-12).
    - SOURCE: GestionConsentimientos.tsx is a separate top-level tab component (not inside GestionCatalogoHC) and calls useConsentimientos + useUploadConsentimiento + useUpdateIndicaciones.
    - SOURCE: the file input restricts to PDF (`accept` includes pdf); the vigente filename/date is rendered when present.
    - `npx tsc --noEmit`, `npm run lint`, and `npm run build` pass.
  </acceptance_criteria>
  <done>Consentimientos tab live in both Configuración views, listing zonas with per-zona consent upload (current vigente shown) and indicaciones URL save.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Consentimientos tab end-to-end</name>
  <action>Pause for human verification of the Consentimientos tab. All code is implemented in Tasks 1-2 — this checkpoint confirms the upload, validation, and persistence behaviors work in the browser for both roles.</action>
  <verify>User confirms the tab lists zonas and that consent upload, non-PDF rejection, and indicaciones persistence all work in both PROFESIONAL and SECRETARIA views.</verify>
  <done>User types "approved" after confirming all six verification steps below.</done>
  <what-built>The "Consentimientos" tab in Configuración: lists surgical zonas, per zona allows uploading a consent PDF (showing the current vigente file) and saving an indicaciones URL.</what-built>
  <how-to-verify>
    1. Run backend (`cd backend && npm run start:dev`) and frontend (`cd frontend && npm run dev`); log in as a PROFESIONAL.
    2. Open Configuración → "Consentimientos" tab. Confirm your surgical zonas are listed.
    3. For one zona, upload a real PDF. Confirm the current file name + date appears after upload (SC#1). Open the PDF link — the browser should download it (Content-Disposition: attachment) rather than render inline.
    4. Try uploading a non-PDF (e.g. a .png renamed to .pdf or a plain .txt). Confirm an error toast and that no file is shown as vigente (SC#2).
    5. Enter an indicaciones URL for a zona and Save. Reload the page and confirm the URL is still there (SC#3).
    6. Log in as SECRETARIA (with a selected professional) and confirm the tab also appears and is scoped to that professional.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (missing tab, upload fails, non-PDF accepted, URL not persisting, scope wrong).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → upload endpoint | File selected client-side; real validation is server-side (53-02 magic bytes) |
| SECRETARIA scope → profesionalId param | The selected-professional scope must flow through to the hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-12 | Tampering | client file-type filter | accept | `accept=".pdf"` is UX-only; the authoritative magic-byte check is server-side in 53-02 (T-53-06) — client cannot bypass server validation |
| T-53-13 | Elevation of Privilege | profesionalId scope in hooks | mitigate | profesionalId is passed as a query param only; the backend resolves/authorizes via JWT + getProfesionalId — the frontend value cannot widen access |
| T-53-SC | Tampering | npm installs | accept | No new frontend package required (TanStack Query, sonner, shadcn already present) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npm run lint && npm run build` — all pass.
- Human checkpoint: upload visible (SC#1), non-PDF rejected (SC#2), indicaciones URL persists after reload (SC#3), tab present in both PROFESIONAL and SECRETARIA views (D-12).
</verification>

<success_criteria>
- Consentimientos tab present in Configuración (both roles), not nested in Catálogo HC (D-12).
- Per-zona consent PDF upload works and shows the current vigente file (CONS-01, SC#1).
- Per-zona indicaciones URL saves and persists after reload (CONS-02, SC#3).
</success_criteria>

<output>
Create `.planning/phases/53-storage-upload-consent-config/53-03-SUMMARY.md` when done.
</output>
