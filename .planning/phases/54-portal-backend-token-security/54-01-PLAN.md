---
phase: 54-portal-backend-token-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/schema.prisma
  - backend/src/modules/paciente-portal/dto/update-contacto-portal.dto.ts
  - backend/src/modules/paciente-portal/dto/update-salud-staged.dto.ts
  - backend/src/modules/paciente-portal/strategies/portal-jwt.strategy.ts
  - backend/src/modules/paciente-portal/guards/portal-jwt.guard.ts
autonomous: true
requirements: [PORTAL-04, PORTAL-06]
must_haves:
  truths:
    - "The Paciente table has two new persistent columns for failed-attempt tracking"
    - "A staff JWT cannot satisfy a portal route (separate named strategy + scope claim)"
    - "The contact DTO declares only contact fields; the staged-health DTO declares only *AutoReportad* fields"
  artifacts:
    - path: "backend/src/prisma/schema.prisma"
      provides: "portalIntentosFallidos + portalBloqueadoHasta on Paciente"
      contains: "portalIntentosFallidos"
    - path: "backend/src/modules/paciente-portal/dto/update-contacto-portal.dto.ts"
      provides: "Narrow contact DTO (all optional, no clinical fields)"
    - path: "backend/src/modules/paciente-portal/dto/update-salud-staged.dto.ts"
      provides: "Narrow staged-health DTO (only *AutoReportad* keys)"
    - path: "backend/src/modules/paciente-portal/strategies/portal-jwt.strategy.ts"
      provides: "Named 'portal-jwt' passport strategy enforcing scope=portal-paciente"
    - path: "backend/src/modules/paciente-portal/guards/portal-jwt.guard.ts"
      provides: "PortalJwtGuard extending AuthGuard('portal-jwt')"
  key_links:
    - from: "portal-jwt.strategy.ts"
      to: "scope claim"
      via: "validate() rejects payload.scope !== 'portal-paciente'"
      pattern: "portal-paciente"
---

<objective>
Lay the foundation for the patient-portal backend: the persistent brute-force tracking columns, the two narrow write DTOs that define the safe write surface, and the portal-scoped JWT strategy/guard that keeps staff tokens out. No controller or service logic yet — these are the contracts the downstream plans build against.

Purpose: Establish the schema columns (needed for the lock logic to type-check), the DTO shapes (the SC#3/SC#4 protection surface), and the portal authentication primitive before any endpoint consumes them.
Output: 2 new Paciente columns + migration, 2 narrow DTOs, 1 named passport strategy + guard.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/54-portal-backend-token-security/54-CONTEXT.md
@.planning/phases/54-portal-backend-token-security/54-PATTERNS.md

<interfaces>
<!-- Existing Paciente staging/portal block (schema.prisma:217-224) — the new columns must match this column-aligned style -->
portalToken                       String?  @unique
portalTokenGeneradoAt             DateTime?
portalTokenCifrado                String?
alergiasAutoReportadas            String[]
antecedentesAutoReportados        Json?
medicacionAutoReportada           String[]
tratamientosPreviosAutoReportados String?

<!-- Staff strategy to mirror (auth/strategies/jwt.strategy.ts) — UNNAMED PassportStrategy(Strategy); portal one MUST be NAMED 'portal-jwt' -->
<!-- Staff guard to mirror (auth/guards/jwt-auth.guard.ts): export class JwtAuthGuard extends AuthGuard('jwt') {} -->
<!-- PrismaService is @Global (prisma/prisma.module.ts) — inject directly, import path mirrors auth jwt.strategy.ts depth -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: Add brute-force tracking columns + run Prisma migration</name>
  <files>backend/src/prisma/schema.prisma</files>
  <read_first>
    - backend/src/prisma/schema.prisma (the Paciente model, lines 152-235; place the new fields next to the existing portal block at 217-224)
    - CLAUDE.md (Development Commands — this project uses `npx prisma migrate dev`, not `db push`)
    - .planning/phases/54-portal-backend-token-security/54-PATTERNS.md (§schema.prisma — exact field style + the "ONLY migration of the phase" note)
  </read_first>
  <action>Add two fields to the `Paciente` model (D-01), column-aligned with the existing portal block at schema.prisma:217-224: `portalIntentosFallidos Int @default(0)` and `portalBloqueadoHasta DateTime?`. No new index — the lookup stays on the existing `@unique portalToken`. Then run the migration: `npx prisma migrate dev --name portal_intentos_bloqueo` followed by `npx prisma generate`. Both columns are additive and safe (non-null with default, plus nullable) so `migrate dev` will NOT prompt for data loss. This task is [BLOCKING]: build and type checks pass WITHOUT the migration because Prisma types come from the schema/generated client, not the live DB — so this must run before any verification of downstream plans. If Prisma unexpectedly emits an interactive prompt, STOP and flag for manual intervention rather than guessing.</action>
  <acceptance_criteria>
    - Source: `grep -E "portalIntentosFallidos|portalBloqueadoHasta" backend/src/prisma/schema.prisma` returns both lines.
    - Migration applied: `cd backend && npx prisma migrate status` reports the schema up to date (a new migration directory named `*portal_intentos_bloqueo` exists under `backend/prisma/migrations/` or the project's migrations path).
    - Generated client current: `cd backend && npx prisma generate` exits 0 and the generated `PacienteSelect`/`PacienteUpdateInput` types include `portalIntentosFallidos` and `portalBloqueadoHasta`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && grep -q portalIntentosFallidos src/prisma/schema.prisma && grep -q portalBloqueadoHasta src/prisma/schema.prisma && npx prisma migrate status</automated>
  </verify>
  <done>Both columns exist in schema and DB; client regenerated; migrate status clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create the two narrow write DTOs</name>
  <files>backend/src/modules/paciente-portal/dto/update-contacto-portal.dto.ts, backend/src/modules/paciente-portal/dto/update-salud-staged.dto.ts</files>
  <read_first>
    - backend/src/modules/pacientes/dto/create-contacto.dto.ts (class-validator + @IsOptional decorator pattern)
    - backend/src/prisma/schema.prisma (Paciente fields 158-178 for contact names; 221-224 for staging names)
    - .planning/phases/54-portal-backend-token-security/54-PATTERNS.md (§DTO — exact field lists for both DTOs and the no-global-pipe caveat)
  </read_first>
  <action>Create `UpdateContactoPortalDto` (D-11, PORTAL-04) with ONLY contact fields, every one optional: `telefono?`, `telefonoAlternativo?`, `email?`, `direccion?`, `contactoEmergenciaNombre?`, `contactoEmergenciaTelefono?`, `contactoEmergenciaRelacion?` — each `@IsOptional()` + `@IsString()`, with `email` using `@IsEmail()`. It MUST NOT declare `obraSocialId`, `alergias`, `condiciones`, `etapaCRM`, `flujo`, or `dni`. Create `UpdateSaludStagedDto` (D-13, PORTAL-06) with ONLY the `*AutoReportad*` keys, all optional: `alergiasAutoReportadas?: string[]` (`@IsOptional() @IsArray() @IsString({ each: true })`), `antecedentesAutoReportados?` (Json — `@IsOptional() @IsObject()`; shape is Claude's discretion per D, keep consistent with what F55 will send), `medicacionAutoReportada?: string[]` (same array decorators), `tratamientosPreviosAutoReportados?: string` (`@IsOptional() @IsString()`). It MUST NOT declare the curated `alergias`/`condiciones`/`medicacion`. These DTOs only enforce SC#3/SC#4 once a per-route `ValidationPipe({ whitelist: true })` is wired in Plan 03 — there is NO global ValidationPipe in this project.</action>
  <acceptance_criteria>
    - Source: `update-contacto-portal.dto.ts` contains `telefono`, `contactoEmergenciaTelefono` and does NOT contain `obraSocial`, `alergias`, `etapaCRM`, `flujo` (`grep -E "obraSocial|alergias|condiciones|etapaCRM|flujo|dni" backend/src/modules/paciente-portal/dto/update-contacto-portal.dto.ts` returns nothing).
    - Source: `update-salud-staged.dto.ts` contains `alergiasAutoReportadas`, `antecedentesAutoReportados`, `medicacionAutoReportada`, `tratamientosPreviosAutoReportados` and contains NO bare `alergias`/`condiciones`/`medicacion` keys (only the `*AutoReportad*` forms).
    - Build: `cd backend && npm run build` compiles with no type errors.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && ! grep -Eq "obraSocial|etapaCRM|flujo" src/modules/paciente-portal/dto/update-contacto-portal.dto.ts && grep -q alergiasAutoReportadas src/modules/paciente-portal/dto/update-salud-staged.dto.ts && npm run build</automated>
  </verify>
  <done>Both DTOs exist with only their allowed fields; project builds.</done>
</task>

<task type="auto">
  <name>Task 3: Create portal-jwt passport strategy + guard</name>
  <files>backend/src/modules/paciente-portal/strategies/portal-jwt.strategy.ts, backend/src/modules/paciente-portal/guards/portal-jwt.guard.ts</files>
  <read_first>
    - backend/src/modules/auth/strategies/jwt.strategy.ts (whole file — extraction/secret/validate shape and import depth)
    - backend/src/modules/auth/guards/jwt-auth.guard.ts (the 4-line guard pattern)
    - .planning/phases/54-portal-backend-token-security/54-PATTERNS.md (§guards/portal-jwt.guard.ts — the named-strategy + scope-check requirement)
  </read_first>
  <action>Create `PortalJwtStrategy extends PassportStrategy(Strategy, 'portal-jwt')` — the SECOND argument NAMES the strategy so it never collides with the staff `'jwt'` strategy (D-05, threat: token-as-write-credential / staff-token reuse). Use the same extraction and secret as the staff strategy: `jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken()`, `secretOrKey: process.env.JWT_SECRET`, `ignoreExpiration: false` (so an expired portal JWT yields 401 → re-verify DNI per D-06). In `validate(payload)`: FIRST reject when `payload.scope !== 'portal-paciente'` by returning `null` (→ 401), then load the `Paciente` by `payload.sub` via the injected `PrismaService` (selecting only `id`) and return `{ pacienteId: paciente.id }` (return `null` if not found). The handler will read `req.user.pacienteId` — scope derived from the authenticated JWT, never the body (pitfall 12). Create `PortalJwtGuard extends AuthGuard('portal-jwt')` mirroring the staff `jwt-auth.guard.ts`. Mirror the PrismaService import depth from the analog strategy file.</action>
  <acceptance_criteria>
    - Source: `portal-jwt.strategy.ts` contains the named-strategy literal `'portal-jwt'` and the scope check against `'portal-paciente'`.
    - Source: `portal-jwt.guard.ts` contains `extends AuthGuard('portal-jwt')`.
    - Source: strategy returns a `pacienteId` (not `userId`/`rol`) — it does NOT load `Usuario`.
    - Build: `cd backend && npm run build` compiles.
    - Lint clean: `cd backend && npm run lint` exits 0 (CLAUDE.md quality bar).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && grep -q "'portal-jwt'" src/modules/paciente-portal/strategies/portal-jwt.strategy.ts && grep -q "portal-paciente" src/modules/paciente-portal/strategies/portal-jwt.strategy.ts && grep -q "AuthGuard('portal-jwt')" src/modules/paciente-portal/guards/portal-jwt.guard.ts && npm run build && npm run lint</automated>
  </verify>
  <done>Named portal strategy enforces scope and returns pacienteId; guard wraps it; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → portal write route | Untrusted patient body crosses here; must not reach curated clinical fields |
| staff JWT → portal route | A staff-issued JWT must NOT authenticate a portal route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Elevation | portal-jwt.strategy.ts | mitigate | Named `'portal-jwt'` strategy + `payload.scope !== 'portal-paciente'` → null (401); staff `'jwt'` strategy cannot satisfy portal routes |
| T-54-02 | Tampering | update-*-portal DTOs | mitigate | DTOs declare ONLY contact / `*AutoReportad*` fields; clinical/obraSocial/dni keys absent (enforced as whitelist in Plan 03) |
| T-54-03 | Spoofing | portal JWT TTL | mitigate | `ignoreExpiration: false` so expired token → 401 → DNI re-verify (D-06); no refresh token |
| T-54-SC | Tampering | npm installs | accept | No new package installs this phase — all deps (@nestjs/jwt, passport-jwt, class-validator) already present in package.json |
</threat_model>

<verification>
- `cd backend && npx prisma migrate status` clean; both new columns present in schema.
- `cd backend && npm run build` compiles and `npm run lint` is clean with the new DTOs, strategy, and guard.
- DTOs contain no clinical/obra-social/dni keys; strategy enforces scope + returns pacienteId.
</verification>

<success_criteria>
- Paciente has `portalIntentosFallidos` (Int default 0) and `portalBloqueadoHasta` (DateTime?) applied via migration.
- Narrow contact and staged-health DTOs exist with only their allowed fields.
- Named portal-jwt strategy + guard exist and reject non-portal scopes.
</success_criteria>

<output>
Create `.planning/phases/54-portal-backend-token-security/54-01-SUMMARY.md` when done.
</output>
