---
phase: 55-portal-frontend
plan: 03
type: execute
wave: 2
depends_on: [55-02]
files_modified:
  - frontend/src/schemas/portalContacto.schema.ts
  - frontend/src/components/portal/PortalInfoBasica.tsx
  - frontend/src/components/portal/SaludChips.tsx
  - frontend/src/components/portal/PortalSalud.tsx
autonomous: true
requirements: [PORTAL-03, PORTAL-05]
must_haves:
  truths:
    - "En Info basica el paciente ve pre-cargados telefono, email, direccion y contacto de emergencia y puede corregirlos; al guardar se persisten via PATCH /datos-personales"
    - "En Salud el paciente ve chips pre-cargados desde los valores staged y puede seleccionar sugerencias comunes o agregar 'otro' en 4 categorias"
    - "La escritura de salud va SOLO a los campos *AutoReportad* via PATCH /salud, nunca a los campos clinicos curados"
    - "El guardado ocurre al salir/avanzar de la seccion o con un boton Guardar (D-08)"
  artifacts:
    - path: "frontend/src/schemas/portalContacto.schema.ts"
      provides: "Zod schema de contacto (email + telefono + emergencia) mapeado al UpdateContactoPortalDto"
      contains: "z.object"
    - path: "frontend/src/components/portal/PortalInfoBasica.tsx"
      provides: "Form RHF+Zod de contacto con pre-fill y guardado via useUpdateContacto"
      contains: "useUpdateContacto"
    - path: "frontend/src/components/portal/SaludChips.tsx"
      provides: "Componente de chips + sugerencias + campo otro que emite string[]"
      contains: "PREDEFINED"
    - path: "frontend/src/components/portal/PortalSalud.tsx"
      provides: "4 categorias de salud pre-cargadas de staged, guardado via useUpdateSalud"
      contains: "useUpdateSalud"
  key_links:
    - from: "PortalInfoBasica.tsx"
      to: "PATCH /paciente-portal/public/datos-personales"
      via: "useUpdateContacto().mutateAsync"
      pattern: "useUpdateContacto"
    - from: "PortalSalud.tsx"
      to: "PATCH /paciente-portal/public/salud"
      via: "useUpdateSalud().mutateAsync con campos *AutoReportad*"
      pattern: "AutoReportad"
---

<objective>
Implementar las dos secciones editables del portal sobre los stubs del Plan 02: Info basica (PORTAL-03 — telefono, email, direccion, contacto de emergencia via RHF+Zod → PATCH /datos-personales) y Salud (PORTAL-05 — chips + sugerencias comunes + campo "otro" en 4 categorias, pre-cargados desde los valores staged, escribiendo SOLO a los campos `*AutoReportad*` via PATCH /salud). Guardado al salir/avanzar de la seccion o con boton Guardar (D-08).

Purpose: Cubre PORTAL-03 y PORTAL-05 (SC#2 y SC#3). Reusa el patron de chips del staff (F52) adaptado a mobile y respeta la separacion staff-vs-paciente garantizada por los DTOs narrow de F54.
Output: schema Zod de contacto, PortalInfoBasica (form), SaludChips (componente reutilizable de chips), PortalSalud (4 categorias).
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-portal-frontend/55-CONTEXT.md
@.planning/phases/55-portal-frontend/55-PATTERNS.md

<interfaces>
<!-- Contratos del Plan 02 que este plan consume -->
De frontend/src/hooks/usePortalDatos.ts:
- useUpdateContacto() -> mutation, payload = UpdateContactoPayload (7 campos opcionales de contacto), PATCH /datos-personales
- useUpdateSalud() -> mutation, payload = UpdateSaludPayload, PATCH /salud
De frontend/src/types/portal.ts:
- PortalDatos["contacto"]: { telefono, telefonoAlternativo, email, direccion, contactoEmergenciaNombre, contactoEmergenciaTelefono, contactoEmergenciaRelacion } (todos string|null)
- PortalDatos["saludAutoReportada"]: { alergiasAutoReportadas: string[]|null, antecedentesAutoReportados: Record<string,unknown>|null, medicacionAutoReportada: string[]|null, tratamientosPreviosAutoReportados: string|null }

Firmas de los stubs (Plan 02) a reemplazar:
- PortalInfoBasica({ datos }: { datos: PortalDatos })
- PortalSalud({ salud }: { salud: PortalDatos["saludAutoReportada"] })

Backend DTO shapes (destino del PATCH):
- UpdateContactoPortalDto: telefono?, telefonoAlternativo?, email? (IsEmail), direccion?, contactoEmergenciaNombre?, contactoEmergenciaTelefono?, contactoEmergenciaRelacion? (todos string opcionales). NOTA F54: un `null` explicito se trata como campo ausente (pickPresent ignora null) — para "limpiar" un valor usar string vacio solo si el backend lo permite; preferir no enviar el campo.
- UpdateSaludStagedDto: alergiasAutoReportadas?: string[]; antecedentesAutoReportados?: object; medicacionAutoReportada?: string[]; tratamientosPreviosAutoReportados?: string.
  Mapeo de categorias (D-04/D-05): condiciones/enfermedades -> antecedentesAutoReportados (OBJECT, no array); alergias -> alergiasAutoReportadas (string[]); medicacion -> medicacionAutoReportada (string[]); tratamientos previos -> tratamientosPreviosAutoReportados (STRING).

Analogs:
- frontend/src/schemas/createTurno.schema.ts (z.object + z.infer)
- frontend/src/components/crm/ContactoSheet.tsx lineas 93-121 (useForm + zodResolver + handleSubmit + mutateAsync + toast)
- frontend/src/components/AlergiasChips.tsx (patron chips: PREDEFINED const, allChips = Array.from(new Set([...PREDEFINED,...selected])), toggle, Input+Plus addCustom). IMPORTANTE: el analog serializa a string con .join(", ") — el portal debe EMITIR ARRAYS (saltear el join) para las categorias array.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema Zod de contacto + PortalInfoBasica</name>
  <files>frontend/src/schemas/portalContacto.schema.ts, frontend/src/components/portal/PortalInfoBasica.tsx</files>
  <read_first>
    - frontend/src/schemas/createTurno.schema.ts (patron z.object + z.infer)
    - frontend/src/components/crm/ContactoSheet.tsx lineas 93-121 (useForm + zodResolver + handleSubmit + mutateAsync + toast)
    - frontend/src/hooks/usePortalDatos.ts (useUpdateContacto — creado Plan 02)
    - frontend/src/types/portal.ts (PortalDatos["contacto"])
    - frontend/src/components/portal/PortalInfoBasica.tsx (stub Plan 02 con firma { datos: PortalDatos })
  </read_first>
  <behavior>
    - Un email invalido bloquea el submit con mensaje de error de campo.
    - Un email vacio es aceptado (campo opcional — no se envia si esta vacio).
    - Al enviar valores validos se llama `useUpdateContacto().mutateAsync` con los campos de contacto y se muestra toast de exito.
    - Los campos aparecen pre-cargados desde `datos.contacto`.
  </behavior>
  <action>
  1) `portalContacto.schema.ts`: `z.object` con `telefono` (string opcional), `email` (`z.string().email("Email invalido").optional().or(z.literal(""))`), `direccion` (string opcional), `contactoEmergenciaNombre`/`contactoEmergenciaTelefono`/`contactoEmergenciaRelacion` (string opcionales). Exportar `type PortalContactoValues = z.infer<...>`. Los nombres de campo deben mapear 1:1 a `UpdateContactoPortalDto`.
  2) `PortalInfoBasica.tsx` (reemplazar stub, mantener firma `{ datos }: { datos: PortalDatos }`): `useForm<PortalContactoValues>({ resolver: zodResolver(portalContactoSchema), defaultValues: { ...prefill desde datos.contacto (mapear null -> "") } })`. Render mobile-first: labels + `Input` (de @/components/ui/input) para cada campo, agrupando "Contacto de emergencia" (nombre + telefono + relacion). `onSubmit` = construir el payload dejando fuera los strings vacios (no enviar campos vacios, consistente con pickPresent), `await useUpdateContacto().mutateAsync(payload)`, `toast.success("Guardamos tus datos")` / `toast.error("No pudimos guardar, proba de nuevo")`. Boton "Guardar" full-width. Copy en tuteo, `text-base`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -Ec "portalContacto|PortalInfoBasica" | grep -q '^0$' && grep -q "useUpdateContacto" src/components/portal/PortalInfoBasica.tsx && grep -q "email" src/schemas/portalContacto.schema.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `portalContacto.schema.ts` exporta un `z.object` con validacion `.email()` y los 6 campos de contacto (telefono, email, direccion, 3 de emergencia) mapeando a `UpdateContactoPortalDto`.
    - `PortalInfoBasica.tsx` usa `useForm` + `zodResolver`, pre-carga desde `datos.contacto`, y en submit llama `useUpdateContacto().mutateAsync` + toast.
    - El payload de submit omite strings vacios (no envia campos vacios).
    - `npx tsc --noEmit` sin errores.
  </acceptance_criteria>
  <done>Info basica editable con validacion Zod, pre-fill y guardado via PATCH /datos-personales (PORTAL-03, SC#2).</done>
</task>

<task type="auto">
  <name>Task 2: SaludChips (chips + sugerencias + otro, emite arrays)</name>
  <files>frontend/src/components/portal/SaludChips.tsx</files>
  <read_first>
    - frontend/src/components/AlergiasChips.tsx (patron completo de chips; contrastar: NO usar .join(", ") — emitir array)
    - frontend/src/components/CondicionesChips.tsx (segundo ejemplo del patron chips)
  </read_first>
  <action>Crear `SaludChips` (`"use client"`) reusable por categoria. Props: `{ label: string; sugerencias: string[]; value: string[]; onChange: (next: string[]) => void }`. Logica copiada de AlergiasChips PERO trabajando con arrays: `allChips = Array.from(new Set([...sugerencias, ...value]))`; `toggle(item)` agrega/quita de `value` y llama `onChange(nextArray)` (SIN join a string); `Input` + boton `Plus` para agregar "otro" custom → `onChange(Array.from(new Set([...value, custom.trim()])))`. Render mobile-first: `Badge variant="outline"` con clases activo/inactivo via `cn()`, `onClick={toggle}`, texto `text-base`. Exponer el `label` como titulo de la categoria.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "SaludChips" | grep -q '^0$' && grep -q "onChange" src/components/portal/SaludChips.tsx && grep -Lq "join(" src/components/portal/SaludChips.tsx; grep -q "string\[\]" src/components/portal/SaludChips.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `SaludChips.tsx` exporta un componente con props `{ label, sugerencias, value: string[], onChange: (next: string[]) => void }`.
    - `onChange` recibe `string[]` (NO un string joineado); el componente no usa `.join(", ")` para emitir el valor.
    - Incluye chips de sugerencias seleccionables + un `Input`/`Plus` para agregar valores custom ("otro").
    - `npx tsc --noEmit` sin errores.
  </acceptance_criteria>
  <done>Componente de chips mobile que emite arrays, base para las 4 categorias de salud.</done>
</task>

<task type="auto">
  <name>Task 3: PortalSalud (4 categorias, pre-fill staged, guardado)</name>
  <files>frontend/src/components/portal/PortalSalud.tsx</files>
  <read_first>
    - frontend/src/components/portal/SaludChips.tsx (Task 2)
    - frontend/src/hooks/usePortalDatos.ts (useUpdateSalud — Plan 02)
    - frontend/src/types/portal.ts (PortalDatos["saludAutoReportada"])
    - frontend/src/components/portal/PortalSalud.tsx (stub Plan 02, firma { salud })
    - backend/src/modules/paciente-portal/dto/update-salud-staged.dto.ts (shape exacto: array vs object vs string por campo)
  </read_first>
  <action>Reemplazar el stub `PortalSalud` (mantener firma `{ salud }: { salud: PortalDatos["saludAutoReportada"] }`). Estado local por categoria inicializado desde `salud` (D-05 pre-fill). Cuatro secciones usando `SaludChips`:
  - Alergias -> value `salud.alergiasAutoReportadas ?? []`, sugerencias comunes (Penicilina, Iodo, Latex, AINEs, ...), emite a `alergiasAutoReportadas: string[]`.
  - Medicacion -> value `salud.medicacionAutoReportada ?? []`, sugerencias (Anticoagulantes, Antihipertensivos, ...), emite a `medicacionAutoReportada: string[]`.
  - Condiciones/enfermedades -> chips que representan claves; al guardar mapear el array de condiciones a `antecedentesAutoReportados` como OBJECT (p.ej. `{ condiciones: string[] }` o `Object.fromEntries(items.map(i => [i, true]))` — elegir y documentar; debe ser un objeto, no array).
  - Tratamientos previos -> como es STRING en el DTO, usar un `Textarea` (o SaludChips + join solo para esta categoria) que emita a `tratamientosPreviosAutoReportados: string`.
  Boton "Guardar" y/o guardado al colapsar la seccion (D-08): `await useUpdateSalud().mutateAsync(payload)` con los 4 campos `*AutoReportad*` + toast exito/error. NUNCA enviar campos clinicos curados (alergias/condiciones/medicacion sin sufijo) — solo `*AutoReportad*`. Mobile-first, tuteo, `text-base`.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "PortalSalud" | grep -q '^0$' && grep -q "useUpdateSalud" src/components/portal/PortalSalud.tsx && grep -q "alergiasAutoReportadas" src/components/portal/PortalSalud.tsx && grep -q "antecedentesAutoReportados" src/components/portal/PortalSalud.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `PortalSalud.tsx` renderiza 4 categorias (alergias, medicacion, condiciones, tratamientos previos) usando `SaludChips` (y Textarea para tratamientos si aplica).
    - Cada categoria se pre-carga desde `salud` (staged pre-fill, D-05).
    - En guardado llama `useUpdateSalud().mutateAsync` con payload cuyos campos son SOLO `alergiasAutoReportadas` (string[]), `medicacionAutoReportada` (string[]), `antecedentesAutoReportados` (object), `tratamientosPreviosAutoReportados` (string). El payload NO contiene claves `alergias`/`condiciones`/`medicacion` sin sufijo.
    - `npx tsc --noEmit` sin errores.
  </acceptance_criteria>
  <done>Seccion Salud con 4 categorias pre-cargadas escribiendo solo a campos staged (PORTAL-05, SC#3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paciente (form del portal) -> PATCH /datos-personales y /salud | Input editable del paciente cruza al backend (ya autenticado por JWT portal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-10 | Tampering (escribir clinico curado) | PortalSalud payload | mitigate | El form emite SOLO campos `*AutoReportad*`; el backend F54 (DTO narrow + ValidationPipe whitelist + pickPresent) descarta cualquier campo clinico curado — el cliente refuerza no enviarlos (D-06) |
| T-55-11 | Information Disclosure | pre-fill de datos | accept | Los datos mostrados (contacto + staged) provienen del GET /paciente-portal/public ya acotado por el backend; el cliente solo los renderiza |
| T-55-12 | Input validation | email/telefono | mitigate | Validacion cliente Zod (email) + validacion server IsEmail (F54); doble capa |
| T-55-13 | Injection (XSS) | render de valores del paciente/staged | mitigate | Render solo como texto (React escapa); sin dangerouslySetInnerHTML; no se renderiza indicacionesUrl (CR-01) |
| T-55-SC | Tampering (supply chain) | npm installs | accept | Sin dependencias npm nuevas (react-hook-form, @hookform/resolvers, zod, sonner ya presentes) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` sin errores en schemas/portalContacto, components/portal/PortalInfoBasica, SaludChips, PortalSalud.
- `cd frontend && npm run build` compila.
- Grep de aseveracion: el payload de salud usa exclusivamente los 4 nombres `*AutoReportad*`.
</verification>

<success_criteria>
- PORTAL-03: contacto editable pre-cargado, guardado via PATCH /datos-personales al guardar/avanzar (SC#2).
- PORTAL-05: 4 categorias de salud con chips+sugerencias+"otro", pre-cargadas de staged, guardadas SOLO a campos `*AutoReportad*` via PATCH /salud (SC#3).
- Sin escritura posible a campos clinicos curados desde el portal.
</success_criteria>

<output>
Create `.planning/phases/55-portal-frontend/55-03-SUMMARY.md` when done
</output>
