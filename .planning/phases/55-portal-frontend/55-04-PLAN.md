---
phase: 55-portal-frontend
plan: 04
type: execute
wave: 2
depends_on: [55-01, 55-02]
files_modified:
  - frontend/src/schemas/portalConsulta.schema.ts
  - frontend/src/components/portal/PortalConsultas.tsx
autonomous: true
requirements: [CHAT-04]
must_haves:
  truths:
    - "El paciente puede escribir una consulta (o elegir una sugerencia comun) y enviarla al medico desde la seccion Consultas"
    - "Al enviar, la consulta llega al chat interno del staff via POST /consulta y se muestra un banner de confirmacion"
    - "Es one-way: el paciente NO ve respuestas del staff en el portal (no hay endpoint de lectura ni polling) (D-02)"
    - "Consultas es opcional y siempre disponible (puede enviar varias en distintas visitas), no bloquea el completado (D-11)"
  artifacts:
    - path: "frontend/src/schemas/portalConsulta.schema.ts"
      provides: "Zod schema del mensaje de consulta (1..2000)"
      contains: "z.object"
    - path: "frontend/src/components/portal/PortalConsultas.tsx"
      provides: "UI de envio de consulta con sugerencias + textarea + confirmacion, via useEnviarConsulta"
      contains: "useEnviarConsulta"
  key_links:
    - from: "PortalConsultas.tsx"
      to: "POST /paciente-portal/public/consulta"
      via: "useEnviarConsulta().mutateAsync({ mensaje })"
      pattern: "useEnviarConsulta"
---

<objective>
Implementar la seccion "Consultas" (CHAT-04, frontend) sobre el stub del Plan 02: una caja donde el paciente escribe una pregunta al medico (con sugerencias comunes predefinidas + textarea libre) y la envia via `POST /paciente-portal/public/consulta` (endpoint del Plan 01). Es one-way (D-02): el paciente envia y ve una confirmacion, pero NO ve respuestas del staff dentro del portal. Consultas es opcional (D-11) y siempre disponible como canal de contacto.

Purpose: Cierra CHAT-04 del lado frontend (SC#4): la consulta del paciente aparece en el chat interno del staff diferenciada (origenPaciente=true, seteado por el backend del Plan 01).
Output: schema Zod de la consulta y el componente PortalConsultas con envio + banner de confirmacion.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-portal-frontend/55-CONTEXT.md
@.planning/phases/55-portal-frontend/55-PATTERNS.md

<interfaces>
<!-- Contratos que este plan consume -->
De frontend/src/hooks/usePortalDatos.ts (Plan 02):
- useEnviarConsulta() -> mutation, payload { mensaje: string }, POST /paciente-portal/public/consulta, retorna { id, createdAt }
Backend (Plan 01):
- POST /paciente-portal/public/consulta (PortalJwtGuard) body { mensaje: string } (1..2000) -> crea MensajeInterno origenPaciente=true. Cualquier campo extra en el body es descartado (whitelist).
Firma del stub (Plan 02) a reemplazar:
- PortalConsultas({}) (sin props; usa useEnviarConsulta internamente)
Analogs:
- frontend/src/app/presupuesto/[token]/page.tsx lineas 58-64 (DUDAS_PREDEFINIDAS const) + lineas 433-495 (panel "Tengo una duda": chips predefinidos + "Otra consulta..." + Textarea + submit) + lineas 299-306 (banner verde de confirmacion). Template directo para la seccion Consultas one-way.
- frontend/src/components/crm/ContactoSheet.tsx lineas 104-121 (try/mutateAsync/toast).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema Zod de consulta + PortalConsultas one-way</name>
  <files>frontend/src/schemas/portalConsulta.schema.ts, frontend/src/components/portal/PortalConsultas.tsx</files>
  <read_first>
    - frontend/src/app/presupuesto/[token]/page.tsx lineas 58-64 (DUDAS_PREDEFINIDAS) + 433-495 (panel de duda: chips + textarea + submit) + 299-306 (banner de confirmacion verde)
    - frontend/src/hooks/usePortalDatos.ts (useEnviarConsulta — Plan 02)
    - frontend/src/components/portal/PortalConsultas.tsx (stub Plan 02, sin props)
    - frontend/src/schemas/createTurno.schema.ts (patron z.object + z.infer)
  </read_first>
  <behavior>
    - Un mensaje vacio no se puede enviar (boton deshabilitado o validacion Zod min 1).
    - Un mensaje de mas de 2000 caracteres es rechazado por Zod (coincide con el backend MaxLength 2000).
    - Al enviar un mensaje valido se llama `useEnviarConsulta().mutateAsync({ mensaje })`; en exito se muestra un banner de confirmacion y se limpia el textarea.
    - No se renderiza ninguna respuesta del staff (one-way, D-02).
  </behavior>
  <action>
  1) `portalConsulta.schema.ts`: `z.object({ mensaje: z.string().min(1, "Escribi tu consulta").max(2000, "La consulta es muy larga") })` + `type PortalConsultaValues = z.infer<...>`. El limite 2000 debe coincidir con el `@MaxLength(2000)` del backend (Plan 01).
  2) `PortalConsultas.tsx` (reemplazar stub, sin props): definir `CONSULTAS_SUGERIDAS` (const de preguntas comunes en tuteo, p.ej. "Tengo una duda sobre la fecha de mi cirugia", "Que tengo que llevar el dia de la operacion?", "Puedo tomar mi medicacion habitual?"). Estado local `mensaje`. Render mobile-first: chips de sugerencias que al clickear rellenan/append al `Textarea`; un `Textarea` libre; un `Button` "Enviar consulta" full-width. `onSubmit`: validar con el schema (o min length), `await useEnviarConsulta().mutateAsync({ mensaje })`, en exito mostrar banner verde de confirmacion (patron presupuesto lineas 299-306) "Enviamos tu consulta al equipo medico" + `toast.success`, limpiar el textarea; en error `toast.error("No pudimos enviar, proba de nuevo")`. La caja permanece disponible tras enviar (puede mandar otra, D-11). NO renderizar ni pedir respuestas del staff (one-way). Copy en tuteo, `text-base`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -Ec "portalConsulta|PortalConsultas" | grep -q '^0$' && grep -q "useEnviarConsulta" src/components/portal/PortalConsultas.tsx && grep -q "max(2000" src/schemas/portalConsulta.schema.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `portalConsulta.schema.ts` valida `mensaje` con `.min(1)` y `.max(2000)` (coincide con el backend del Plan 01).
    - `PortalConsultas.tsx` renderiza sugerencias + `Textarea` + boton de envio y llama `useEnviarConsulta().mutateAsync({ mensaje })`.
    - En exito muestra un banner de confirmacion y limpia el textarea; la caja sigue disponible para otra consulta.
    - El componente NO contiene logica de lectura/render de respuestas del staff (one-way).
    - `npx tsc --noEmit` sin errores.
  </acceptance_criteria>
  <done>Seccion Consultas funcional: el paciente envia preguntas al chat del staff via POST /consulta, one-way, con confirmacion (CHAT-04, SC#4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paciente (textarea del portal) -> POST /paciente-portal/public/consulta | Texto libre del paciente cruza al backend y luego se persiste/renderiza en el chat del staff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-14 | Tampering (mass-assignment) | payload de consulta | mitigate | El cliente envia SOLO `{ mensaje }`; el backend (Plan 01) descarta campos extra con ValidationPipe whitelist y deriva pacienteId del JWT |
| T-55-15 | Injection (stored-XSS) | mensaje -> render en chat staff | mitigate | El texto se envia como dato; el limite Zod max 2000 acota el payload; el render seguro es del renderer del chat staff (React escapa; sin dangerouslySetInnerHTML). Documentar en el SUMMARY que el chat staff debe tratar `origenPaciente` como texto no confiable |
| T-55-16 | Denial of Service (flood de consultas) | envio repetido | accept | ThrottlerGuard global + @Throttle de la clase paciente-portal (F54/Plan 01) acotan el flood server-side |
| T-55-SC | Tampering (supply chain) | npm installs | accept | Sin dependencias npm nuevas (zod, sonner, lucide-react ya presentes) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` sin errores en portalConsulta.schema.ts y PortalConsultas.tsx.
- `cd frontend && npm run build` compila.
- Manual (con backend + JWT valido): escribir una consulta y enviarla -> aparece un MensajeInterno con origenPaciente=true en el chat interno del profesional; el portal muestra confirmacion y no muestra respuestas.
</verification>

<success_criteria>
- CHAT-04 (frontend): el paciente envia consultas al medico desde el portal; llegan al chat interno del staff diferenciadas (origenPaciente=true).
- One-way (D-02): sin lectura de respuestas en el portal.
- Consultas opcional y reutilizable (D-11), con confirmacion de envio.
</success_criteria>

<output>
Create `.planning/phases/55-portal-frontend/55-04-SUMMARY.md` when done
</output>
