---
phase: 56-signed-consent-chat-badge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/schema.prisma
  - backend/prisma/migrations
autonomous: true
requirements: [CONS-03, CONS-05, CONS-06]
must_haves:
  truths:
    - "The database has a ConsentimientoFirmado table with the 10 forensic columns"
    - "ConsentimientoZonaArchivo has a version Int column, backfilled per zona by uploadedAt"
    - "Cirugia has a cirugiaCatalogoId FK enabling the D-09 resolver chain"
    - "prisma generate produces a client that types the new model and fields"
  artifacts:
    - path: "backend/src/prisma/schema.prisma"
      provides: "ConsentimientoFirmado model + version field + Cirugia FK + back-relations"
      contains: "model ConsentimientoFirmado"
    - path: "backend/prisma/migrations"
      provides: "Applied migration with additive columns + backfill SQL"
  key_links:
    - from: "Cirugia.cirugiaCatalogoId"
      to: "CirugiaCatalogo.id"
      via: "Prisma relation (onDelete: SetNull)"
      pattern: "cirugiaCatalogoId"
    - from: "ConsentimientoFirmado.consentimientoZonaArchivoId"
      to: "ConsentimientoZonaArchivo.id"
      via: "Prisma relation (onDelete: Restrict)"
      pattern: "consentimientoZonaArchivoId"
---

<objective>
Add the Phase 56 schema foundation: the new `ConsentimientoFirmado` forensic model, a `version Int` field on `ConsentimientoZonaArchivo`, and the critical `cirugiaCatalogoId` FK on `Cirugia` that makes the D-09 consent-resolution chain possible. Apply the migration (additive columns + version backfill) and regenerate the Prisma client.

Purpose: Every backend capability in this phase (stamping persistence, resolver, forensic audit) depends on these models existing in the live database and in the generated client.
Output: Extended `schema.prisma`, an applied migration, and a regenerated Prisma client.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-signed-consent-chat-badge/56-RESEARCH.md
@.planning/phases/56-signed-consent-chat-badge/56-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the three schema deltas + back-relations to schema.prisma</name>
  <files>backend/src/prisma/schema.prisma</files>
  <read_first>
    - backend/src/prisma/schema.prisma — existing `ConsentimientoZonaArchivo` (~1404), `Cirugia` (~854, `procedimiento String?` ~861), `CirugiaCatalogo` (~943), `Paciente` (~152, flag `consentimientoFirmado` ~172 / `consentimientoFirmadoAt` ~216), `ZonaHC` (~1384)
    - .planning/phases/56-signed-consent-chat-badge/56-RESEARCH.md §"Prisma Schema Deltas" (lines 665-747) and §"Critical Schema Gap" (lines 631-661) — exact model/field shapes to copy verbatim
    - .planning/phases/56-signed-consent-chat-badge/56-PATTERNS.md §schema.prisma (lines 38-50)
  </read_first>
  <action>
    Apply the three deltas exactly as specified in RESEARCH §"Prisma Schema Deltas": (1) add `version Int @default(1)` plus back-relation `consentimientosFirmados ConsentimientoFirmado[]` to `ConsentimientoZonaArchivo` (D-03). (2) Add the new `ConsentimientoFirmado` model with the 10 fields `pacienteId, zonaId, consentimientoZonaArchivoId, pdfFirmadoPath, ip, userAgent, versionNumero, hashSha256, firmadoAt @default(now()), indicacionesLeidasAt`, three relations, and indexes `@@index([pacienteId])`, `@@index([zonaId])`, `@@index([pacienteId, zonaId])` (D-04); set `onDelete: Restrict` on the `consentimientoZonaArchivo` relation (immutability, D-06 / RESEARCH Assumption A4). (3) Add `cirugiaCatalogoId String?` plus relation `cirugiaCatalogo CirugiaCatalogo? @relation(fields: [cirugiaCatalogoId], references: [id], onDelete: SetNull)` to `Cirugia`, and the inverse `cirugias Cirugia[]` to `CirugiaCatalogo` (D-09). Add inverse `consentimientosFirmados ConsentimientoFirmado[]` back-relations to `Paciente` and `ZonaHC`. All additions are additive — do not rename or remove existing fields.
  </action>
  <acceptance_criteria>
    - `grep -c "model ConsentimientoFirmado" backend/src/prisma/schema.prisma` returns 1
    - `grep -c "cirugiaCatalogoId" backend/src/prisma/schema.prisma` returns >= 2 (field + inverse usage)
    - `grep -c "version Int @default(1)" backend/src/prisma/schema.prisma` returns 1
    - `grep "onDelete: Restrict" backend/src/prisma/schema.prisma` matches the consentimientoZonaArchivo relation line
    - `cd backend && npx prisma validate` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx prisma validate</automated>
  </verify>
  <done>schema.prisma validates and contains ConsentimientoFirmado, version, and Cirugia.cirugiaCatalogoId with the specified relations and indexes.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migration with version backfill + regenerate client</name>
  <files>backend/prisma/migrations, backend/src/prisma/schema.prisma</files>
  <read_first>
    - .planning/phases/56-signed-consent-chat-badge/56-RESEARCH.md §"Prisma Schema Deltas" backfill SQL (lines 689-703)
    - .planning/STATE.md §Accumulated Context — prior migration decisions [51-02], [53-02], [54-01]: on this Supabase/pgBouncer database `prisma migrate dev` is blocked by a pre-existing timestamp drift; migrations are applied via `prisma migrate diff --script` → `prisma db execute` → `prisma migrate resolve --applied`
    - backend/prisma/migrations — inspect the most recent migration folder (e.g. 20260630000000_portal_intentos_bloqueo) to mirror the naming + file structure
  </read_first>
  <action>
    Apply the schema deltas to the live database using this repo's proven non-interactive pattern (NOT `migrate dev`, which hangs on pgBouncer drift per STATE.md): create a migration folder `backend/prisma/migrations/{timestamp}_signed_consent_forensic/migration.sql`; generate the DDL with `npx prisma migrate diff --from-schema-datasource prisma/schema.prisma --to-schema-datamodel prisma/schema.prisma --script` (or diff from the current DB state) so it emits the additive `ALTER TABLE`/`CREATE TABLE` statements for the three deltas; append the version backfill SQL from RESEARCH lines 689-703 (ROW_NUMBER() OVER PARTITION BY "zonaId" ORDER BY "uploadedAt") AFTER the `ALTER TABLE ... ADD COLUMN version`; apply with `npx prisma db execute --file migration.sql --schema prisma/schema.prisma`; mark applied with `npx prisma migrate resolve --applied {migration_name}`; then run `npx prisma generate`. New columns are additive so existing rows get `cirugiaCatalogoId = NULL` (handled as D-10 empty-state downstream) and existing consent archives get sequential version numbers per zona. If the non-interactive apply cannot complete, STOP and surface the DB error rather than leaving a partial migration.
  </action>
  <acceptance_criteria>
    - `cd backend && npx prisma migrate status` reports no pending/failed migrations for the new folder
    - The generated Prisma client types `prisma.consentimientoFirmado` — assert with: `cd backend && node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();if(typeof p.consentimientoFirmado?.create!=='function')process.exit(1);console.log('ok')"`
    - The migration.sql file contains the backfill `ROW_NUMBER() OVER` statement
    - `cd backend && npx prisma validate` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx prisma generate && node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();if(typeof p.consentimientoFirmado?.create!=='function')process.exit(1);console.log('ok')"</automated>
  </verify>
  <done>Migration is applied to the live DB (additive columns + backfilled versions), and the regenerated Prisma client exposes `consentimientoFirmado`, `version`, and `cirugiaCatalogoId`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Database | Schema/DDL changes and forensic record writes cross into the persistence tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Tampering | ConsentimientoFirmado rows | mitigate | Model is write-once by design (D-06); `onDelete: Restrict` on `consentimientoZonaArchivoId` blocks DB-level deletion of a signed template version |
| T-56-02 | Repudiation | Forensic audit record | mitigate | `firmadoAt @default(now())` uses server/DB time (UTC), not client-supplied; the 5 forensic columns are structurally required (non-null) |
| T-56-03 | Denial of Service | Migration on production DB | accept | Additive-only DDL (ADD COLUMN / CREATE TABLE) plus a single bounded backfill UPDATE; no destructive locks on hot paths; applied via proven non-interactive pattern |
</threat_model>

<verification>
- `npx prisma validate` passes.
- `npx prisma migrate status` shows the new migration applied, none failed.
- Generated client exposes `prisma.consentimientoFirmado`, `ConsentimientoZonaArchivo.version`, and `Cirugia.cirugiaCatalogoId`.
</verification>

<success_criteria>
The three schema deltas exist in `schema.prisma`, the migration is applied to the live database with versions backfilled per zona, and `prisma generate` produces a client that types the new model and fields — unblocking all downstream backend plans.
</success_criteria>

<output>
Create `.planning/phases/56-signed-consent-chat-badge/56-01-SUMMARY.md` when done.
</output>
