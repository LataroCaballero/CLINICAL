---
phase: 56-signed-consent-chat-badge
plan: 04
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - backend/src/modules/paciente-portal/paciente-portal.service.ts
  - backend/src/modules/paciente-portal/paciente-portal.module.ts
  - backend/src/modules/paciente-portal/paciente-portal.controller.ts
autonomous: true
requirements: [CONS-03]
must_haves:
  truths:
    - "The portal resolves each pending surgery's zone to its vigente consent PDF (D-09 chain)"
    - "Each of the six D-10 empty/already-signed states is distinguished and returned without a signable payload"
    - "GET /consentimiento is guarded by PortalJwtGuard and derives pacienteId from the JWT only"
    - "StorageService is injected into PacientePortalService (StorageModule imported by this plan) so getPublicUrl resolves and the module compiles in isolation"
  artifacts:
    - path: "backend/src/modules/paciente-portal/paciente-portal.service.ts"
      provides: "getConsentimientosParaFirmar resolver + StorageService injection"
      contains: "getConsentimientosParaFirmar"
    - path: "backend/src/modules/paciente-portal/paciente-portal.module.ts"
      provides: "StorageModule import so StorageService is injectable"
      contains: "StorageModule"
    - path: "backend/src/modules/paciente-portal/paciente-portal.controller.ts"
      provides: "guarded GET consentimiento route"
      contains: "consentimiento"
  key_links:
    - from: "getConsentimientosParaFirmar"
      to: "ConsentimientoZonaArchivo (vigente)"
      via: "Cirugia → cirugiaCatalogo → zona → consent include"
      pattern: "vigente: true"
    - from: "PacientePortalService constructor"
      to: "StorageService.getPublicUrl"
      via: "DI (StorageModule import)"
      pattern: "StorageService"
---

<objective>
Add the portal read path that resolves which consent(s) a patient must sign: wire `StorageService` into the portal module/service, walk the D-09 chain (pending Cirugia → `cirugiaCatalogoId` → `CirugiaCatalogo.zonaId` → vigente `ConsentimientoZonaArchivo`), classify each of the six D-10 states (four empty-states, already-signed, happy path), and expose it via a `PortalJwtGuard`-protected GET endpoint whose identity comes from the JWT only.

Purpose: CONS-03 — the patient can see/download the correct PDF, and the frontend gets a per-zone status it can render without inferring anything client-side. This plan owns the `StorageModule` import + `StorageService` injection (it is the first plan to need `getPublicUrl`), so it compiles standalone; Plan 05 reuses that injected `StorageService` without re-importing.
Output: StorageModule import, StorageService injection, one resolver service method, and one guarded GET route.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/56-signed-consent-chat-badge/56-RESEARCH.md
@.planning/phases/56-signed-consent-chat-badge/56-PATTERNS.md
@.planning/phases/56-signed-consent-chat-badge/56-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs — no codebase exploration required. -->

StorageService (from backend/src/modules/storage/storage.service.ts, exported by ../storage/storage.module):
- injected via `imports: [StorageModule]` in the module + `private readonly storage: StorageService` in the constructor
- `getPublicUrl(path: string): string` — builds the public attachment URL used for `pdfUrl`
(Import shape mirrors consentimientos.module.ts:4/7 + consentimientos.service.ts:8/16.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire StorageService (import StorageModule + inject) and implement getConsentimientosParaFirmar (D-09 + D-10 states)</name>
  <files>backend/src/modules/paciente-portal/paciente-portal.service.ts, backend/src/modules/paciente-portal/paciente-portal.module.ts</files>
  <read_first>
    - backend/src/modules/paciente-portal/paciente-portal.service.ts — imports (lines 1-13), constructor DI (lines 36-39: `private prisma`, `private jwt`), `getDatos` (lines 86-152, Cirugia read shape ~112-120), `crearConsulta` (lines 311-334)
    - backend/src/modules/paciente-portal/paciente-portal.module.ts (whole file) — `imports: [PassportModule, JwtModule.register(...)]` (lines 23-26); PrismaService is global (no import)
    - backend/src/modules/consentimientos/consentimientos.module.ts (lines 4, 7) and consentimientos.service.ts (lines 8, 16) — the exact `StorageModule` import + `StorageService` injection pattern to mirror
    - .planning/phases/56-signed-consent-chat-badge/56-RESEARCH.md §"Pending-Surgery → Zone → Template Resolution" (lines 751-801) — the full findMany include + the six-state table
    - .planning/phases/56-signed-consent-chat-badge/56-UI-SPEC.md §"Empty States" (lines 125-154) — the exact state discriminators the frontend expects
    - .planning/phases/56-signed-consent-chat-badge/56-PATTERNS.md §paciente-portal.service.ts (lines 141-159)
  </read_first>
  <action>
    First wire StorageService so this plan compiles in isolation: in `paciente-portal.module.ts` add `StorageModule` to the `imports` array (import `{ StorageModule } from '../storage/storage.module'`, mirroring consentimientos.module.ts); in `paciente-portal.service.ts` import `{ StorageService } from '../storage/storage.service'` and add `private readonly storage: StorageService` to the constructor (alongside `prisma`/`jwt`). Then add `async getConsentimientosParaFirmar(pacienteId: string)`. Read the patient's pending cirugias with `estado in ['PROGRAMADA','EN_CURSO']`, nested-including `cirugiaCatalogo.zona` → `consentimientoArchivos` (where `vigente: true`, orderBy `uploadedAt desc`, take 1) and `zona.consentimientosFirmados` (where `{ pacienteId }`, take 1) per RESEARCH lines 762-789. Return a per-zone array where each item carries a discriminator matching the six states: `SIN_CIRUGIA` (empty cirugias), `SIN_CATALOGO` (`cirugiaCatalogoId` null), `SIN_ZONA` (`CirugiaCatalogo.zonaId` null), `SIN_PDF` (no vigente archivo), `YA_FIRMADO` (a `ConsentimientoFirmado` exists for this pacienteId+zonaId, includes firmadoAt), or `PARA_FIRMAR` (returns `{ zonaId, zonaNombre, pdfUrl` via `this.storage.getPublicUrl`, `consentimientoZonaArchivoId, version, indicacionesUrl }`). `pacienteId` is the method arg (from JWT) — never from a body. Do NOT return the PDF url for non-`PARA_FIRMAR` states.
  </action>
  <acceptance_criteria>
    - `grep "StorageModule" backend/src/modules/paciente-portal/paciente-portal.module.ts` matches an import + imports-array entry
    - `grep "StorageService" backend/src/modules/paciente-portal/paciente-portal.service.ts` matches the import + a constructor member
    - `grep -c "getConsentimientosParaFirmar" backend/src/modules/paciente-portal/paciente-portal.service.ts` returns >= 1
    - The query includes `vigente: true` and a `consentimientosFirmados` include scoped to the patient
    - The return distinguishes all six states (grep the discriminator literals: SIN_CIRUGIA, SIN_CATALOGO, SIN_ZONA, SIN_PDF, YA_FIRMADO, PARA_FIRMAR)
    - `cd backend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit</automated>
  </verify>
  <done>StorageModule is imported and StorageService injected; the resolver returns a per-zone status covering all six D-09/D-10 cases, with the signable payload (incl. pdfUrl via StorageService) only for PARA_FIRMAR.</done>
</task>

<task type="auto">
  <name>Task 2: Add guarded GET /consentimiento route</name>
  <files>backend/src/modules/paciente-portal/paciente-portal.controller.ts</files>
  <read_first>
    - backend/src/modules/paciente-portal/paciente-portal.controller.ts — `getDatos` guarded read (lines 69-73), `enviarConsulta` (lines 124-132), `PortalRequest` type (lines 20-21), class docblock about per-route guards (lines 23-39)
    - .planning/phases/56-signed-consent-chat-badge/56-PATTERNS.md §paciente-portal.controller.ts (lines 109-138), Shared Patterns (lines 358-371)
  </read_first>
  <action>
    Add a `@UseGuards(PortalJwtGuard) @Get('consentimiento')` route `getConsentimiento(@Req() req: PortalRequest)` that returns `this.service.getConsentimientosParaFirmar(req.user.pacienteId)`. Mirror the exact route base path used by `enviarConsulta`/`getDatos` (the same `@Controller` prefix). The guard is per-route, NEVER class-level (public preVerify/verificar routes must stay open). `pacienteId` comes from `req.user` only (pitfall 12).
  </action>
  <acceptance_criteria>
    - `grep -c "@Get('consentimiento')" backend/src/modules/paciente-portal/paciente-portal.controller.ts` returns 1
    - The route is decorated with `@UseGuards(PortalJwtGuard)` and passes `req.user.pacienteId`
    - No `pacienteId` is read from `@Body`/`@Param`/`@Query` on this route
    - `cd backend && npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npm run build</automated>
  </verify>
  <done>GET /consentimiento returns the resolver output under PortalJwtGuard with JWT-scoped identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Patient browser → portal read API | The resolver must scope strictly to the JWT patient; PDF urls must not leak across tenants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-09 | Elevation/IDOR | getConsentimiento route | mitigate | PortalJwtGuard supplies `pacienteId` from the token; the resolver never accepts patient/zona identity from the request (pitfall 12); query scoped by `pacienteId` |
| T-56-10 | Information disclosure | pdfUrl exposure | mitigate | `pdfUrl` returned only for PARA_FIRMAR zones belonging to the JWT patient; empty/already-signed states carry no url; url is the F53 UUID-guarded attachment path |
| T-56-11 | Repudiation | Already-signed detection | mitigate | YA_FIRMADO derives from a `ConsentimientoFirmado` row scoped to pacienteId+zonaId, preventing a second signable payload (D-08) |
</threat_model>

<verification>
- `npm run build` passes (module compiles standalone with StorageService injected).
- Resolver distinguishes all six states; PARA_FIRMAR alone carries pdfUrl + version + archivo id.
- GET route is guarded and JWT-scoped.
</verification>

<success_criteria>
The portal can tell the patient exactly which consent(s) to sign (or why none is available) and hand the frontend the correct PDF url + version for each signable zone — all strictly scoped to the authenticated patient, with StorageService wired in this plan so it compiles independently.
</success_criteria>

<output>
Create `.planning/phases/56-signed-consent-chat-badge/56-04-SUMMARY.md` when done.
</output>
