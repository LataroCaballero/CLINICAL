---
phase: 56-signed-consent-chat-badge
plan: 06
type: execute
wave: 4
depends_on: ["56-05"]
files_modified:
  - frontend/src/hooks/usePortalConsentimiento.ts
  - frontend/src/components/portal/PortalConsentimiento.tsx
  - frontend/src/app/portal/[token]/page.tsx
autonomous: false
requirements: [CONS-03, CONS-04, CONS-07]
must_haves:
  truths:
    - "The patient can view/download the consent PDF from the portal Consentimiento step"
    - "D-11: The patient can draw a signature; Confirmar firma is reactively disabled until indicaciones is checked AND the canvas is non-empty"
    - "Each of the six resolver states renders exactly the UI-SPEC copy/layout (empty states, already-signed, happy path, post-sign, fallback)"
    - "indicacionesUrl and pdfUrl are only ever used as href values, gated by ^https?:// at render"
  artifacts:
    - path: "frontend/src/hooks/usePortalConsentimiento.ts"
      provides: "GET consent query + POST firmar mutation on portalApi"
      contains: "useFirmarConsentimiento"
    - path: "frontend/src/components/portal/PortalConsentimiento.tsx"
      provides: "signature canvas + states + gate"
      contains: "SignaturePad"
      min_lines: 80
    - path: "frontend/src/app/portal/[token]/page.tsx"
      provides: "placeholder replaced by PortalConsentimiento"
      contains: "PortalConsentimiento"
  key_links:
    - from: "PortalConsentimiento.tsx"
      to: "/paciente-portal consent endpoints"
      via: "usePortalConsentimiento hook on portalApi"
      pattern: "usePortalConsentimiento|useFirmarConsentimiento"
---

<objective>
Build the portal "Consentimiento" step: a TanStack Query hook against the new portal endpoints, the `PortalConsentimiento` client component (PDF download, indicaciones gate, signature canvas with reactive-disable, all six states, post-sign success, canvas fallback, XSS-safe links), and wire it into the portal page in place of the placeholder.

Purpose: CONS-03/04/07 — the patient-facing act of viewing the consent and drawing a signature. UI-SPEC is the LOCKED visual/copy contract; follow it verbatim.
Output: New hook, new component, one page edit, and a human verification of the signing flow.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/56-signed-consent-chat-badge/56-UI-SPEC.md
@.planning/phases/56-signed-consent-chat-badge/56-RESEARCH.md
@.planning/phases/56-signed-consent-chat-badge/56-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create usePortalConsentimiento hook (query + mutation)</name>
  <files>frontend/src/hooks/usePortalConsentimiento.ts</files>
  <read_first>
    - frontend/src/hooks/usePortalDatos.ts (lines 1-62) — useQuery + useMutation on portalApi + invalidateQueries analog
    - frontend/src/lib/portal-api.ts (lines 1-19) — portalApi axios instance (sessionStorage portalToken; baseURL is NEXT_PUBLIC_API_URL, so include the full `/paciente-portal/...` path)
    - .planning/phases/56-signed-consent-chat-badge/56-PATTERNS.md §usePortalConsentimiento (lines 268-296)
  </read_first>
  <action>
    Create `useConsentimientosParaFirmar(enabled)` — `useQuery` GET to the same base path as `usePortalDatos` plus `/consentimiento` (mirror the exact route base the controller uses, e.g. `/paciente-portal/public/consentimiento`), queryKey `["portal-consentimiento"]`. Create `useFirmarConsentimiento()` — `useMutation` POST to `.../consentimiento/firmar` with body `{ zonaId, signaturePngDataUrl, indicacionesLeidas }`, `onSuccess` invalidating `["portal-consentimiento"]`. Type the query result to the resolver's per-zone status shape (state discriminator + PARA_FIRMAR payload).
  </action>
  <acceptance_criteria>
    - `grep -c "useFirmarConsentimiento" frontend/src/hooks/usePortalConsentimiento.ts` returns >= 1
    - Hook uses `portalApi` (not the staff `api` instance) and posts `signaturePngDataUrl`
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Hook exposes the consent query and the firmar mutation over portalApi.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build PortalConsentimiento component (states + canvas + gate)</name>
  <files>frontend/src/components/portal/PortalConsentimiento.tsx</files>
  <read_first>
    - .planning/phases/56-signed-consent-chat-badge/56-UI-SPEC.md §"Surface 1" (lines 111-342) — LOCKED copy, layout, states, canvas, gate, typography (3 sizes/2 weights), colors; §"Interaction Contracts" (lines 496-520); §"Security Contracts" (lines 524-534)
    - frontend/src/components/portal/PortalConsultas.tsx (lines 1-60) — 'use client' shell, hook usage, Loader2/CheckCircle, toast, text-base
    - .planning/phases/56-signed-consent-chat-badge/56-RESEARCH.md §"Pattern 3" signature_pad capture/fallback/resize (lines 356-442) and §"Pattern 5" frontend safe render (lines 528-545)
    - frontend/package.json — signature_pad ^5.1.1, lucide-react ^0.553.0 already installed
  </read_first>
  <action>
    Create the `'use client'` `PortalConsentimiento` component rendering the hook data. For each zone status render exactly per UI-SPEC: loading (Loader2 indigo), API error copy, the four empty-state gray text blocks (SIN_CIRUGIA/SIN_CATALOGO/SIN_ZONA/SIN_PDF copy verbatim), already-signed (CheckCircle2 teal + date), and the happy-path zone card (border rounded-xl) containing in order: PDF download `<a href={pdfUrl}>` (FileDown), the indicaciones check, the signature canvas, Limpiar + Confirmar buttons, inline error. Initialize `SignaturePad` on the canvasRef with transparent background + black pen; implement canvas fallback (`typeof HTMLCanvasElement === 'undefined' || typeof PointerEvent === 'undefined'` → fallback copy), and canvas resize handling (devicePixelRatio) with empty-state re-evaluation. Wire the reactive-disable gate: subscribe to the pad's stroke-end event, mirror `isEmpty()` into React state, and set Confirmar `disabled={!indicacionesLeidas || canvasEmpty || isSubmitting || !canvasSupported}`. On confirm, `pad.toDataURL('image/png')` unmodified → call `useFirmarConsentimiento` with `{ zonaId (from server response, not user input), signaturePngDataUrl, indicacionesLeidas: true }`; on success replace the card with the post-sign success block; on 409 show already-signed. Compute `safeIndicacionesUrl = indicacionesUrl && /^https?:\/\//i.test(indicacionesUrl) ? indicacionesUrl : null` and render the with-link variant only when non-null, else the generic check. Use ONLY the 3 allowed sizes (text-2xl/text-base/text-xs) and 2 weights (font-normal/font-semibold); teal reserved per UI-SPEC; primary CTA is bg-gray-900 (NOT indigo).
  </action>
  <acceptance_criteria>
    - `grep -c "SignaturePad" frontend/src/components/portal/PortalConsentimiento.tsx` returns >= 1
    - `grep "toDataURL('image/png')" frontend/src/components/portal/PortalConsentimiento.tsx` present and passed unmodified to the mutation
    - `grep "safeIndicacionesUrl" frontend/src/components/portal/PortalConsentimiento.tsx` present with the `^https?:` test
    - No `dangerouslySetInnerHTML` in the file (grep returns 0); `indicacionesUrl`/`pdfUrl` only used as `href`
    - No prohibited typography: `grep -E "text-sm|text-\[10px\]|font-bold|font-medium" frontend/src/components/portal/PortalConsentimiento.tsx` returns 0
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>PortalConsentimiento renders all six states per UI-SPEC, gates Confirmar reactively, sends the unmodified PNG data URL, and renders links XSS-safely.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the portal Consentimiento placeholder with the component</name>
  <files>frontend/src/app/portal/[token]/page.tsx</files>
  <read_first>
    - frontend/src/app/portal/[token]/page.tsx — the `value="consentimiento"` AccordionItem (lines 345-361), placeholder AccordionContent (lines 356-360), the existing `<PortalConsultas />` usage (line 375) for the import pattern
    - .planning/phases/56-signed-consent-chat-badge/56-PATTERNS.md §portal page (lines 300-303)
  </read_first>
  <action>
    Replace the placeholder inside the `consentimiento` AccordionContent with `<PortalConsentimiento />` (import mirroring the `PortalConsultas` import). Keep the AccordionItem wrapper, the teal `FileSignature` trigger icon, and the `font-semibold text-base` trigger unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "PortalConsentimiento" frontend/src/app/portal/[token]/page.tsx` returns >= 2 (import + usage)
    - The "Próximamente vas a poder firmar" placeholder text is gone (grep returns 0)
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>The portal Consentimiento step renders PortalConsentimiento; the placeholder is removed; build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify the portal signing flow end to end</name>
  <files>frontend/src/components/portal/PortalConsentimiento.tsx</files>
  <what-built>The full portal Consentimiento step: PDF view/download, indicaciones gate, signature canvas, and signed-PDF generation with forensic archival.</what-built>
  <read_first>
    - .planning/phases/56-signed-consent-chat-badge/56-UI-SPEC.md §"Surface 1" (states + gate) and §"Interaction Contracts"
  </read_first>
  <action>Pause after the build passes and hand the running flow to the human for end-to-end verification of the signing + forensic artifact. Do not mark the plan complete until the human approves.</action>
  <how-to-verify>
    1. Open a patient portal link (DNI-gated) for a patient with a PROGRAMADA cirugia linked to a CirugiaCatalogo whose zona has a vigente consent PDF; open the "Consentimiento" step.
    2. Confirm "Descargar / ver el consentimiento" opens the PDF; confirm Confirmar firma stays disabled until BOTH the indicaciones checkbox is checked AND you draw on the canvas.
    3. Draw a signature, check indicaciones, click "Confirmar firma"; confirm the success state ("Tu firma fue registrada.").
    4. On the server/storage, open the generated signed PDF: the signature + forensic box (fecha UTC / IP / userAgent / versión) must be visible on the last page and the hash must NOT appear in the PDF.
    5. Reload the step: the zone shows the already-signed state (no canvas).
    6. Try a patient with no pending surgery / unlinked cirugia: confirm the correct empty-state copy and NO canvas.
  </how-to-verify>
  <verify><human-check>Human confirms the end-to-end signing flow, the visible forensic box (no printed hash), the reactive gate, the already-signed terminal state, and the empty-state copy.</human-check></verify>
  <acceptance_criteria>Human confirms all six verification steps pass, or reports specific issues to fix.</acceptance_criteria>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
  <done>Human approved the end-to-end signing flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server data → patient DOM | `indicacionesUrl` / `pdfUrl` from the API are rendered as links in the patient browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-17 | XSS | indicacionesUrl / pdfUrl render | mitigate | URLs used ONLY as `href`, gated by `^https?://` at render (defense-in-depth atop the Plan 02 write-point fix); no `dangerouslySetInnerHTML` |
| T-56-18 | Tampering | zonaId in POST body | mitigate | `zonaId` sourced from the server GET response, never from user-visible input or the URL token param |
| T-56-19 | Integrity | signaturePngDataUrl | mitigate | Sent as the raw `toDataURL('image/png')` output, not re-encoded client-side; backend re-validates magic bytes + size |
</threat_model>

<verification>
- `npm run build` and `npx tsc --noEmit` pass in frontend.
- Reactive-disable gate holds (indicaciones AND non-empty canvas).
- No prohibited typography utilities; links only as href with protocol guard.
- Human checkpoint confirms the end-to-end signing + forensic box.
</verification>

<success_criteria>
The patient can view/download the consent, draw a signature behind a mandatory indicaciones gate, and produce a signed PDF with a visible forensic box — with all copy/typography/colors matching the LOCKED UI-SPEC and all URLs rendered XSS-safely.
</success_criteria>

<output>
Create `.planning/phases/56-signed-consent-chat-badge/56-06-SUMMARY.md` when done.
</output>
