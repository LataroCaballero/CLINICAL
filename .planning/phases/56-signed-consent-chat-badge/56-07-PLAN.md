---
phase: 56-signed-consent-chat-badge
plan: 07
type: execute
wave: 2
depends_on: ["56-02"]
files_modified:
  - frontend/src/hooks/useMensajesPaciente.ts
  - frontend/src/components/mensajes/MessageBubble.tsx
  - frontend/src/components/mensajes/ChatView.tsx
  - frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx
  - frontend/src/types/pacients.ts
autonomous: true
requirements: [CHAT-03, CONS-08]
must_haves:
  truths:
    - "Patient-origin chat messages render a teal 'Paciente' bubble distinct from sistema (Bot) and staff bubbles"
    - "The staff patient drawer shows an emerald 'Consentimiento firmado' date badge when consentimientoFirmadoAt is set"
  artifacts:
    - path: "frontend/src/components/mensajes/MessageBubble.tsx"
      provides: "origenPaciente prop + teal branch"
      contains: "origenPaciente"
    - path: "frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx"
      provides: "emerald consent date badge"
      contains: "consentimientoFirmadoAt"
  key_links:
    - from: "ChatView.tsx"
      to: "MessageBubble origenPaciente branch"
      via: "origenPaciente={mensaje.origenPaciente} prop"
      pattern: "origenPaciente={mensaje.origenPaciente}"
---

<objective>
Add the two staff-facing badges: a teal "Paciente" branch in the chat `MessageBubble` for `origenPaciente` messages (CHAT-03), and an emerald "Consentimiento firmado" date badge in the patient drawer (CONS-08). Thread `origenPaciente` through the Mensaje type + ChatView, and add `consentimientoFirmadoAt` to the frontend Paciente type.

Purpose: CHAT-03/CONS-08 — staff can distinguish real patient messages from noise, and see the signed-consent status with its date.
Output: Chat type + render edits and a drawer badge edit.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/56-signed-consent-chat-badge/56-UI-SPEC.md
@.planning/phases/56-signed-consent-chat-badge/56-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add teal Paciente branch to MessageBubble + thread the prop</name>
  <files>frontend/src/components/mensajes/MessageBubble.tsx, frontend/src/hooks/useMensajesPaciente.ts, frontend/src/components/mensajes/ChatView.tsx</files>
  <read_first>
    - frontend/src/components/mensajes/MessageBubble.tsx — lucide import (line 8), `MessageBubbleProps` (lines 10-23), the `esSistema` early-return branch (lines 39-60), general avatar return (~64)
    - frontend/src/hooks/useMensajesPaciente.ts — `Mensaje` interface (lines 4-20), `esSistema`/`esPropio` fields
    - frontend/src/components/mensajes/ChatView.tsx — the MessageBubble render/prop spread (lines 118-127)
    - .planning/phases/56-signed-consent-chat-badge/56-UI-SPEC.md §"Surface 2" (lines 346-423) — LOCKED teal branch JSX, tokens, icon, 16px bubble body, branch order
  </read_first>
  <action>
    Add `origenPaciente: boolean` to the `Mensaje` interface (useMensajesPaciente.ts) and to `MessageBubbleProps`. In ChatView.tsx pass `origenPaciente={mensaje.origenPaciente}` in the MessageBubble prop spread. In MessageBubble.tsx add `UserRound` to the lucide-react import and insert a NEW branch BETWEEN the `esSistema` return and the general avatar return: `if (origenPaciente && !esSistema)` returns the left-aligned teal bubble exactly per UI-SPEC Surface 2 — avatar circle `bg-teal-100` with `UserRound text-teal-600`, a `text-xs font-semibold text-teal-700` "Paciente" label + PriorityBadge, a `bg-teal-50 border-teal-200 text-teal-900 text-base` bubble, and a `text-xs text-muted-foreground` timestamp (no leido tick). Branch order stays sistema → paciente → staff. Use only teal tokens listed; bubble body is `text-base` (16px) per D-13.
  </action>
  <acceptance_criteria>
    - `grep -c "origenPaciente" frontend/src/components/mensajes/MessageBubble.tsx` returns >= 2 (prop + branch condition)
    - `grep "origenPaciente={mensaje.origenPaciente}" frontend/src/components/mensajes/ChatView.tsx` present
    - `grep "origenPaciente" frontend/src/hooks/useMensajesPaciente.ts` present in the Mensaje interface
    - `grep "UserRound" frontend/src/components/mensajes/MessageBubble.tsx` present in the import + avatar
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Patient-origin messages render the teal "Paciente" bubble; the prop is typed and threaded from the hook through ChatView.</done>
</task>

<task type="auto">
  <name>Task 2: Add emerald consent date badge to DatosCompletos (CONS-08)</name>
  <files>frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx, frontend/src/types/pacients.ts</files>
  <read_first>
    - frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx — the `FieldRow label="Consentimiento firmado"` block (~lines 820-831), lucide imports (lines 1-11)
    - frontend/src/types/pacients.ts (lines 20, 47) — Paciente type currently has `consentimientoFirmado: boolean` but NOT `consentimientoFirmadoAt`
    - .planning/phases/56-signed-consent-chat-badge/56-UI-SPEC.md §"Surface 3" (lines 426-463) — LOCKED emerald badge JSX, tokens, es-AR date, drawer-only placement
  </read_first>
  <action>
    Add `consentimientoFirmadoAt: string | null` to the Paciente type in pacients.ts (both relevant shapes at ~lines 20 and 47 that carry `consentimientoFirmado`). In DatosCompletos.tsx add `CheckCircle2` to the lucide import; wrap the existing consent `FieldRow` value in `<div className="flex items-center gap-3 flex-wrap">` keeping the existing `EditableCheckbox`, and append — only when `paciente.consentimientoFirmadoAt` is non-null — the emerald pill exactly per UI-SPEC: `inline-flex items-center gap-1 text-xs font-semibold text-emerald-700 bg-emerald-50 border border-emerald-300 rounded-md px-2 py-0.5` with a `CheckCircle2 w-3.5 h-3.5` and `new Date(paciente.consentimientoFirmadoAt).toLocaleDateString('es-AR')`. Emerald (NOT teal) is deliberate to avoid collision with the chat badge. No backend change needed (findOne already returns the field).
  </action>
  <acceptance_criteria>
    - `grep "consentimientoFirmadoAt" frontend/src/types/pacients.ts` present
    - `grep -c "consentimientoFirmadoAt" frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx` returns >= 1 with the emerald pill guarded by a non-null check
    - `grep "toLocaleDateString('es-AR')" frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx` present
    - `grep -E "text-teal" frontend/src/components/patient/PatientDrawer/views/DatosCompletos.tsx` returns 0 (badge is emerald, not teal)
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>The drawer shows an emerald date badge when the patient completed the portal flow; the frontend type carries consentimientoFirmadoAt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API DTO → staff DOM | Message text and consent metadata render in the staff UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-20 | XSS | Patient message body render | mitigate | Message text rendered as React text content (`{mensaje}` in a `<p>`), never as innerHTML; React escapes by default |
| T-56-21 | Spoofing (visual) | origin badge trust | mitigate | `origenPaciente` is set server-side only by the portal write path (crearConsulta), not settable by staff; the badge reflects a server-authoritative flag |
| T-56-22 | Information disclosure | consent date badge | accept | `consentimientoFirmadoAt` is non-sensitive status already visible to staff for this patient in the drawer detail view |
</threat_model>

<verification>
- `npm run build` and `npx tsc --noEmit` pass in frontend.
- Teal Paciente branch renders for origenPaciente messages; prop threaded from hook → ChatView → bubble.
- Emerald date badge shows only when consentimientoFirmadoAt is set; type updated.
</verification>

<success_criteria>
Staff can visually distinguish patient-origin chat messages (teal "Paciente" bubble) from sistema/staff, and see the signed-consent status with its date (emerald badge) in the patient drawer.
</success_criteria>

<output>
Create `.planning/phases/56-signed-consent-chat-badge/56-07-SUMMARY.md` when done.
</output>
