---
phase: 57-backend-foundation-etapa-y-payload-enriquecido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/pacientes/crm-steps.helper.ts
  - backend/src/modules/pacientes/crm-steps.helper.spec.ts
  - backend/src/modules/pacientes/pacientes.service.ts
autonomous: true
requirements: [EMBUDO-02]
must_haves:
  truths:
    - "getKanban devuelve por cada paciente un objeto `pasos` con los 5 pasos (hc, presupuesto, cirugia, consentimiento, indicacionesPreop), cada uno 'completo' | 'pendiente' (D-04, D-05)"
    - "getKanban devuelve `todosCompletos` (boolean) por paciente — todos los 5 pasos en 'completo' (D-05; consumido por EMBUDO-04 en fase 58)"
    - "Los pacientes con etapaCRM PROCEDIMIENTO_REALIZADO ya NO caen a SIN_CLASIFICAR — el agrupador de columnas los expone en su propia clave (EMBUDO-02)"
    - "La lógica de estado de pasos está centralizada en una función pura testeable (D-04)"
  artifacts:
    - path: "backend/src/modules/pacientes/crm-steps.helper.ts"
      provides: "Función pura computePasosCrm(paciente) que deriva pasos + todosCompletos"
      contains: "export function computePasosCrm"
    - path: "backend/src/modules/pacientes/crm-steps.helper.spec.ts"
      provides: "Tests unitarios de las 5 reglas de paso + todosCompletos"
      contains: "computePasosCrm"
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "getKanban extendido: select enriquecido + payload de pasos + columna PROCEDIMIENTO_REALIZADO"
      contains: "computePasosCrm"
  key_links:
    - from: "backend/src/modules/pacientes/pacientes.service.ts (getKanban)"
      to: "computePasosCrm"
      via: "import + llamada dentro del items.map"
      pattern: "computePasosCrm\\("
---

<objective>
Enriquecer `getKanban` (`pacientes.service.ts:619`) para que exponga, por cada paciente, el estado computado de los 5 pasos canónicos del stepper (D-04, D-05) y un flag `todosCompletos`, y para que los pacientes en `PROCEDIMIENTO_REALIZADO` dejen de caer a `SIN_CLASIFICAR` (EMBUDO-02). La lógica de negocio de estado de pasos vive en una función pura testeable (`crm-steps.helper.ts`).

Purpose: dar a las fases 58 (indicadores/columnas) y 59 (stepper) todos los datos que necesitan sin recomputar en el frontend. El frontend solo pinta verde/naranja.
Output: nuevo helper puro + su spec, y `getKanban` extendido (select + map + agrupador).
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-backend-foundation-etapa-y-payload-enriquecido/57-CONTEXT.md
@.planning/phases/57-backend-foundation-etapa-y-payload-enriquecido/57-PATTERNS.md

<interfaces>
<!-- Contratos confirmados desde schema.prisma y getKanban actual. El ejecutor NO necesita explorar. -->

getKanban select ACTUAL (pacientes.service.ts:626-658) ya trae:
  presupuestos: { total, estado, fechaEnviado } where estado != RECHAZADO, orderBy createdAt desc
  turnos: { inicio } take 1
  contactos, autorizaciones

Modelos fuente de los 5 pasos (schema.prisma):
  Paciente (:152): consentimientoFirmado Boolean, indicacionesEnviadas Boolean (flags legacy pre-v1.12)
  Paciente.cirugias -> Cirugia[] (:193)
  Paciente.historiasClinicas -> HistoriaClinica[] (:186); HistoriaClinica.entradas -> HistoriaClinicaEntrada[] (:293)
  Paciente.consentimientosFirmados -> ConsentimientoFirmado[] (:227)

  Cirugia (:855): fecha DateTime (requerido), estado EstadoCirugia (PROGRAMADA|EN_CURSO|COMPLETADA|CANCELADA|SUSPENDIDA :885)
  HistoriaClinicaEntrada (:296): status EstadoEntradaHC (DRAFT|FINALIZED :1140), tipoEntrada TipoEntradaHC? (CONSULTA_CIRUGIA|TRATAMIENTO|CONTROL|SEGUIMIENTO|PREOPERATORIO :1198)
  ConsentimientoFirmado (:1426): firmadoAt DateTime (default now), indicacionesLeidasAt DateTime (requerido, seteado al firmar)
  EstadoPresupuesto (:1071): incluye ENVIADO, ACEPTADO, RECHAZADO
  EtapaCRM (:1166): PROCEDIMIENTO_REALIZADO existe (orden 6 en ETAPA_ORDEN, ver PATTERNS.md)

Analog de helper puro (mismo patrón a seguir): backend/src/modules/historia-clinica/historia-clinica.contenido.helpers.ts (export function stateless, sin @Injectable)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear helper puro computePasosCrm + spec (TDD)</name>
  <files>backend/src/modules/pacientes/crm-steps.helper.ts, backend/src/modules/pacientes/crm-steps.helper.spec.ts</files>
  <read_first>
    - backend/src/modules/historia-clinica/historia-clinica.contenido.helpers.ts (patrón de helper puro: `export function`, sin `@Injectable`)
    - backend/src/prisma/schema.prisma:855-891 (Cirugia + EstadoCirugia), :296-323 (HistoriaClinicaEntrada), :1071-1078 (EstadoPresupuesto), :1140-1143 (EstadoEntradaHC), :1198-1204 (TipoEntradaHC), :1426-1446 (ConsentimientoFirmado)
    - .planning/phases/57-backend-foundation-etapa-y-payload-enriquecido/57-PATTERNS.md ("Data sources per step" table)
  </read_first>
  <behavior>
    computePasosCrm(p) recibe un objeto estructural con: presupuestos[] ({ estado }), cirugias[] ({ fecha, estado }), historiasClinicas[] ({ entradas: [{ status, tipoEntrada }] }), consentimientosFirmados[] ({ firmadoAt, indicacionesLeidasAt }), consentimientoFirmado (boolean legacy), indicacionesEnviadas (boolean legacy). Devuelve { pasos: { hc, presupuesto, cirugia, consentimiento, indicacionesPreop }, todosCompletos }. Cada paso es literal 'completo' | 'pendiente'.
    - Test hc: 'completo' cuando existe al menos una entrada con status === 'FINALIZED' y tipoEntrada === 'CONSULTA_CIRUGIA'; 'pendiente' si no hay entradas, o solo DRAFT, o solo otros tipoEntrada.
    - Test presupuesto: 'completo' cuando algún presupuesto tiene estado 'ENVIADO' o 'ACEPTADO'; 'pendiente' con array vacío o solo otros estados.
    - Test cirugia: 'completo' cuando cirugias.length > 0 (fecha es requerida en el modelo, cualquier registro cuenta); 'pendiente' con array vacío.
    - Test consentimiento: 'completo' cuando existe algún ConsentimientoFirmado con firmadoAt seteado O el flag legacy consentimientoFirmado === true; 'pendiente' si ninguno.
    - Test indicacionesPreop: 'completo' cuando existe algún ConsentimientoFirmado con indicacionesLeidasAt presente O el flag legacy indicacionesEnviadas === true; 'pendiente' si ninguno.
    - Test todosCompletos: true solo cuando los 5 pasos === 'completo'; false si al menos uno 'pendiente'. Incluir un caso all-complete y un caso con exactamente un pendiente.
    - Test robustez: arrays undefined/null y flags undefined tratados como 'pendiente' sin lanzar.
  </behavior>
  <action>
    Escribir PRIMERO `crm-steps.helper.spec.ts` con los casos del bloque behavior (RED), luego `crm-steps.helper.ts` (GREEN). Exportar `export function computePasosCrm(p: PacientePasosInput): { pasos: PasosCrm; todosCompletos: boolean }` y un tipo `PasosCrm` con claves `hc | presupuesto | cirugia | consentimiento | indicacionesPreop` de tipo `'completo' | 'pendiente'`. Definir `PacientePasosInput` como interface estructural mínima (solo los campos que el helper lee) para no acoplar al tipo Prisma completo. Precedencia consentimiento/indicaciones per D-05: fuente primaria `ConsentimientoFirmado` (v1.12), fallback OR a los booleans legacy de Paciente. Mantenerlo como módulo con `export function` sin `@Injectable` (patrón `historia-clinica.contenido.helpers.ts`). No fenced code en el plan; el ejecutor implementa. Sin dependencias nuevas.
  </action>
  <verify>
    <automated>cd backend && npm run test -- crm-steps.helper.spec</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/modules/pacientes/crm-steps.helper.ts` exporta `computePasosCrm` y el tipo `PasosCrm`.
    - `npm run test -- crm-steps.helper.spec` pasa con todos los casos del bloque behavior (incluye all-complete, un-pendiente, y robustez con null/undefined).
    - El helper es una `export function` sin decorador `@Injectable`.
    - Cada valor de paso es exactamente `'completo'` o `'pendiente'` (no boolean).
  </acceptance_criteria>
  <done>El helper puro existe, cubre las 5 reglas + todosCompletos con precedencia v1.12→legacy, y su spec pasa en verde.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar computePasosCrm y columna PROCEDIMIENTO_REALIZADO en getKanban</name>
  <files>backend/src/modules/pacientes/pacientes.service.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.ts:619-722 (getKanban completo: select :626-658, columnas :663-671, output map :685-720)
    - backend/src/modules/pacientes/crm-steps.helper.ts (contrato de computePasosCrm, creado en Task 1)
  </read_first>
  <action>
    Extender el `select` de getKanban (dentro de `findMany`, :626-658) agregando SOLO estos campos, sin quitar los existentes: `consentimientoFirmado: true`, `indicacionesEnviadas: true`, `cirugias: { select: { fecha: true, estado: true }, orderBy: { fecha: 'desc' } }`, `historiasClinicas: { select: { entradas: { select: { status: true, tipoEntrada: true } } } }`, `consentimientosFirmados: { select: { firmadoAt: true, indicacionesLeidasAt: true }, take: 1, orderBy: { firmadoAt: 'desc' } }`. En el objeto `columnas` (:663-671) agregar la clave `PROCEDIMIENTO_REALIZADO: []` (ubicarla después de `CONFIRMADO` y antes de `PERDIDO`) para que esos pacientes dejen de caer al bucket SIN_CLASIFICAR (EMBUDO-02); actualizar el comentario `:662` que dice que está excluido. En el `items.map((p) => { ... return {...} })` (:685-720) importar `computePasosCrm` desde `./crm-steps.helper` y hacer spread de su resultado en el objeto devuelto: agregar `pasos` y `todosCompletos` al payload por paciente, junto a las claves existentes (no reemplazar ninguna). Respeta el scoping existente (`where profesionalId`, `crmArchivado: false`) — no ampliarlo. Sin migración de schema (los modelos ya existen).
  </action>
  <verify>
    <automated>cd backend && npm run build && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PROCEDIMIENTO_REALIZADO" backend/src/modules/pacientes/pacientes.service.ts` muestra la clave dentro del objeto `columnas` de getKanban.
    - `grep -n "computePasosCrm" backend/src/modules/pacientes/pacientes.service.ts` muestra el import y la llamada dentro del map.
    - El `select` incluye `cirugias`, `historiasClinicas`, `consentimientosFirmados`, `consentimientoFirmado`, `indicacionesEnviadas`.
    - `npm run build` compila sin errores de tipo (el shape seleccionado satisface `PacientePasosInput`).
    - El objeto devuelto por paciente conserva todas las claves previas (id, nombreCompleto, etapaCRM, presupuesto, diasDesdePresupuesto, etc.) y suma `pasos` + `todosCompletos`.
  </acceptance_criteria>
  <done>getKanban devuelve por paciente `pasos` (5 llaves 'completo'|'pendiente') + `todosCompletos`, y agrupa a los pacientes PROCEDIMIENTO_REALIZADO en su propia columna sin ocultarlos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente (profesional) → GET /pacientes/kanban | El profesional autenticado consulta su embudo; el payload se amplía con datos clínicos por paso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01-I | Information Disclosure | getKanban select enriquecido (cirugias, historiasClinicas, consentimientosFirmados) | mitigate | Las nuevas relaciones son selects anidados bajo el `paciente` ya filtrado por `where: { profesionalId, crmArchivado: false }` (:621-624). No se agrega ninguna query fuera de ese scope. No exponer PDFs/paths de consentimiento: el select trae solo `firmadoAt` e `indicacionesLeidasAt` (timestamps), nunca `pdfFirmadoPath`, `hashSha256`, `ip` ni `userAgent`. |
| T-57-02-I | Information Disclosure | payload `pasos` computado | accept | El payload solo devuelve estados 'completo'|'pendiente' derivados (sin datos crudos clínicos), reduciendo superficie respecto a exponer flags/fechas. Riesgo residual bajo: mismo dueño de datos que ya recibía el kanban. |
| T-57-SC | Tampering | npm installs | accept | No se agregan dependencias en este plan (helper puro + edición de servicio). Sin superficie de supply-chain nueva. |
</threat_model>

<verification>
- `cd backend && npm run test -- crm-steps.helper.spec` pasa.
- `cd backend && npm run build` compila.
- `grep -c "'completo'\\|'pendiente'" backend/src/modules/pacientes/crm-steps.helper.ts` > 0.
- El select de getKanban no incluye `pdfFirmadoPath`, `hashSha256`, `ip`, `userAgent` (verificar por grep que no aparecen bajo `consentimientosFirmados`).
</verification>

<success_criteria>
- getKanban expone `pasos` (hc, presupuesto, cirugia, consentimiento, indicacionesPreop) + `todosCompletos` por paciente.
- Pacientes PROCEDIMIENTO_REALIZADO aparecen en su propia columna (no en SIN_CLASIFICAR).
- Lógica centralizada en `computePasosCrm`, cubierta por tests unitarios verdes.
- Sin cambios de schema, sin dependencias nuevas.
</success_criteria>

<output>
Create `.planning/phases/57-backend-foundation-etapa-y-payload-enriquecido/57-01-SUMMARY.md` when done
</output>
