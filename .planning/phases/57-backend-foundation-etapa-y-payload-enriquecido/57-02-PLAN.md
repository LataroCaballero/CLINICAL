---
phase: 57-backend-foundation-etapa-y-payload-enriquecido
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/pacientes/cirugia-realizada-scheduler.service.ts
  - backend/src/modules/pacientes/cirugia-realizada-scheduler.service.spec.ts
  - backend/src/modules/pacientes/pacientes.module.ts
  - backend/src/modules/turnos/turnos.service.ts
autonomous: true
requirements: [EMBUDO-02, EMBUDO-05]
must_haves:
  truths:
    - "Un job diario mueve a PROCEDIMIENTO_REALIZADO a los pacientes cuya Cirugia tiene fecha pasada, solo hacia adelante (no toca PERDIDO ni etapas de orden >= 6), y persiste en Paciente.etapaCRM (D-03, EMBUDO-02)"
    - "El scheduler ignora cirugías CANCELADA/SUSPENDIDA y aísla fallos por-item con try/catch + Logger (patrón seguimiento-scheduler)"
    - "Al crear un turno, el paciente SIEMPRE pasa a TURNO_AGENDADO desde cualquier etapa, incluidas PERDIDO y PROCEDIMIENTO_REALIZADO (reactivación total del embudo) (D-09, EMBUDO-05)"
    - "El guard isAutoTransitionBlocked se elimina de turnos.service.ts (queda sin uso al bypassear) pero SIGUE intacto y activo en presupuestos.service.ts (D-10)"
  artifacts:
    - path: "backend/src/modules/pacientes/cirugia-realizada-scheduler.service.ts"
      provides: "@Cron diario que auto-mueve a PROCEDIMIENTO_REALIZADO por fecha de cirugía pasada"
      contains: "@Cron"
    - path: "backend/src/modules/pacientes/cirugia-realizada-scheduler.service.spec.ts"
      provides: "Tests del forward-only gate + exclusión de PERDIDO + exclusión de cirugías canceladas"
      contains: "CirugiaRealizadaSchedulerService"
    - path: "backend/src/modules/pacientes/pacientes.module.ts"
      provides: "Registro del nuevo scheduler en providers"
      contains: "CirugiaRealizadaSchedulerService"
    - path: "backend/src/modules/turnos/turnos.service.ts"
      provides: "crearTurno con auto-transición incondicional a TURNO_AGENDADO (guard bypass)"
      contains: "TURNO_AGENDADO"
  key_links:
    - from: "cirugia-realizada-scheduler.service.ts"
      to: "prisma.cirugia.findMany / prisma.paciente.update"
      via: "sweep diario + update etapaCRM"
      pattern: "prisma\\.paciente\\.update"
    - from: "pacientes.module.ts (providers)"
      to: "CirugiaRealizadaSchedulerService"
      via: "registro en providers array"
      pattern: "CirugiaRealizadaSchedulerService"
---

<objective>
Implementar las reglas de transición CRM de la fase 57: (1) un job diario que mueve a `PROCEDIMIENTO_REALIZADO` a los pacientes cuya `Cirugia` ya pasó de fecha, forward-only y persistido en DB (D-03, EMBUDO-02); y (2) el bypass del guard forward-only en la creación de turno para que un nuevo turno reactive el embudo desde cualquier etapa incluidas PERDIDO y PROCEDIMIENTO_REALIZADO (D-09, EMBUDO-05).

Purpose: poblar la etapa "Cirugía Realizada" automáticamente y permitir el re-engagement (segunda cirugía o lead recuperado) sin ser bloqueado por el guard, manteniendo el guard intacto para las auto-transiciones de presupuestos (D-10).
Output: nuevo scheduler + spec, su registro en el módulo, y la relajación del guard en `crearTurno`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-backend-foundation-etapa-y-payload-enriquecido/57-CONTEXT.md
@.planning/phases/57-backend-foundation-etapa-y-payload-enriquecido/57-PATTERNS.md

<interfaces>
<!-- Contratos confirmados. El ejecutor NO necesita explorar. -->

Analog EXACTO del scheduler: backend/src/modules/pacientes/seguimiento-scheduler.service.ts
  - @Injectable + private logger = new Logger(...)
  - constructor(private readonly prisma: PrismaService)
  - @Cron(CronExpression.EVERY_DAY_AT_9AM) async metodo(): Promise<void>
  - loop con try/catch por item + this.logger.error; logs de start/count fuera del loop
Analog del spec: backend/src/modules/pacientes/seguimiento-scheduler.service.spec.ts

Modelo Cirugia (schema.prisma:855): fecha DateTime (requerido), estado EstadoCirugia (:885 PROGRAMADA|EN_CURSO|COMPLETADA|CANCELADA|SUSPENDIDA), pacienteId, relación paciente
EtapaCRM (:1166): PROCEDIMIENTO_REALIZADO existe. Orden CRM (fuente de verdad, PATTERNS.md):
  SIN_CLASIFICAR:0 NUEVO_LEAD:1 TURNO_AGENDADO:2 CONSULTADO:3 PRESUPUESTO_ENVIADO:4 CONFIRMADO:5 PROCEDIMIENTO_REALIZADO:6
  PERDIDO NO está en el mapa (manejo especial explícito).

pacientes.module.ts ACTUAL (:1-14): ScheduleModule.forRoot() ya está en imports; providers = [PacientesService, SeguimientoSchedulerService, PortalEmailService]

turnos.service.ts guard ACTUAL:
  - Definiciones a ELIMINAR (:27-50): ETAPA_ORDEN, etapaOrden, isAutoTransitionBlocked. Único uso está en :162.
  - Bloque a relajar (:156-168): findUnique de pacienteCRM (select etapaCRM, profesionalId, flujo) seguido de `if (!isAutoTransitionBlocked(...)) { paciente.update etapaCRM=TURNO_AGENDADO }`.
  - `pacienteCRM` se sigue usando en :171-173 (flujo) → conservar el findUnique.
  - `EtapaCRM.TURNO_AGENDADO` se sigue usando en el update → conservar el import EtapaCRM.
  - presupuestos.service.ts tiene su PROPIA copia del guard (:25-45, usos en :207/:275/:681) → NO TOCAR (D-10).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear CirugiaRealizadaSchedulerService (@Cron diario) + spec</name>
  <files>backend/src/modules/pacientes/cirugia-realizada-scheduler.service.ts, backend/src/modules/pacientes/cirugia-realizada-scheduler.service.spec.ts</files>
  <read_first>
    - backend/src/modules/pacientes/seguimiento-scheduler.service.ts (patrón estructural EXACTO: @Cron, Logger, try/catch por item)
    - backend/src/modules/pacientes/seguimiento-scheduler.service.spec.ts (patrón de spec con PrismaService mockeado)
    - backend/src/prisma/schema.prisma:855-891 (Cirugia + EstadoCirugia)
    - .planning/phases/57-backend-foundation-etapa-y-payload-enriquecido/57-PATTERNS.md ("cirugia-realizada-scheduler" + "Shared Patterns")
  </read_first>
  <behavior>
    moverCirugiasRealizadas() (método @Cron) mueve pacientes a PROCEDIMIENTO_REALIZADO cuando su cirugía ya pasó, forward-only:
    - Test: paciente con Cirugia fecha pasada y etapaCRM CONFIRMADO (orden 5 < 6) → prisma.paciente.update con data.etapaCRM = PROCEDIMIENTO_REALIZADO llamado con su id.
    - Test: paciente ya en PROCEDIMIENTO_REALIZADO (orden 6) → NO se llama update (gate >= 6).
    - Test: paciente en PERDIDO → NO se llama update (PERDIDO excluido explícitamente; no está en ETAPA_ORDEN).
    - Test: cirugía con fecha futura → no entra en el findMany (where fecha < ahora); no update.
    - Test: cirugía con estado CANCELADA o SUSPENDIDA → excluida del where; no update.
    - Test resiliencia: si un update lanza, el loop continúa con el siguiente item y se llama logger.error (un fallo no aborta el sweep).
  </behavior>
  <action>
    Escribir PRIMERO el spec (RED) mockeando PrismaService (siguiendo `seguimiento-scheduler.service.spec.ts`), luego el servicio (GREEN). Copiar la estructura verbatim de `seguimiento-scheduler.service.ts`: `@Injectable()`, `private readonly logger = new Logger(CirugiaRealizadaSchedulerService.name)`, `constructor(private readonly prisma: PrismaService)`, método `@Cron(CronExpression.EVERY_DAY_AT_6AM) async moverCirugiasRealizadas(): Promise<void>`. Query: `prisma.cirugia.findMany({ where: { fecha: { lt: new Date() }, estado: { notIn: ['CANCELADA', 'SUSPENDIDA'] } }, select: { pacienteId: true, paciente: { select: { etapaCRM: true } } } })`. Definir inline dentro del archivo un `ETAPA_ORDEN` y `etapaOrden` con el bloque canónico de PATTERNS.md (incluir el comentario de ADVERTENCIA de que el orden NO se deriva del enum; PROCEDIMIENTO_REALIZADO = 6). Loop con try/catch por item: `continue` si `etapaCRM === 'PERDIDO'`; `continue` si `etapaOrden(etapaCRM) >= 6`; caso contrario `prisma.paciente.update({ where: { id: pacienteId }, data: { etapaCRM: EtapaCRM.PROCEDIMIENTO_REALIZADO } })`. Loggear start + count fuera del loop y `logger.error` dentro del catch. Importar `EtapaCRM` desde `@prisma/client`. Sin dependencias nuevas (`@nestjs/schedule` ya está en package.json). No fenced code en el plan.
  </action>
  <verify>
    <automated>cd backend && npm run test -- cirugia-realizada-scheduler.service.spec</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/modules/pacientes/cirugia-realizada-scheduler.service.ts` exporta `CirugiaRealizadaSchedulerService` con un método decorado `@Cron`.
    - `npm run test -- cirugia-realizada-scheduler.service.spec` pasa todos los casos del bloque behavior.
    - El `findMany` filtra `fecha: { lt: <now> }` y `estado: { notIn: ['CANCELADA', 'SUSPENDIDA'] }`.
    - El loop salta pacientes PERDIDO y pacientes con `etapaOrden >= 6`, y solo entonces llama `prisma.paciente.update` con `etapaCRM: PROCEDIMIENTO_REALIZADO`.
    - Hay `try/catch` por item con `logger.error` (un fallo no aborta el sweep).
  </acceptance_criteria>
  <done>El scheduler diario auto-mueve por fecha de cirugía pasada, forward-only, excluyendo PERDIDO y cirugías canceladas, con resiliencia por item; su spec pasa en verde.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar el scheduler en PacientesModule</name>
  <files>backend/src/modules/pacientes/pacientes.module.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.module.ts (registro actual de SeguimientoSchedulerService en providers)
    - backend/src/modules/pacientes/cirugia-realizada-scheduler.service.ts (creado en Task 1)
  </read_first>
  <action>
    Agregar el import `CirugiaRealizadaSchedulerService` desde `./cirugia-realizada-scheduler.service` y sumarlo al array `providers` junto a `SeguimientoSchedulerService`. NO tocar `imports` — `ScheduleModule.forRoot()` ya está presente y basta para activar los `@Cron`. No cambiar controllers ni exports.
  </action>
  <verify>
    <automated>cd backend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CirugiaRealizadaSchedulerService" backend/src/modules/pacientes/pacientes.module.ts` muestra el import y su presencia en `providers`.
    - `imports` conserva `ScheduleModule.forRoot()` sin duplicarlo.
    - `npm run build` compila sin errores.
  </acceptance_criteria>
  <done>El scheduler queda registrado como provider y sus @Cron se activan vía el ScheduleModule ya presente.</done>
</task>

<task type="auto">
  <name>Task 3: Bypass del guard forward-only en crearTurno (reactivación total)</name>
  <files>backend/src/modules/turnos/turnos.service.ts</files>
  <read_first>
    - backend/src/modules/turnos/turnos.service.ts:27-50 (definiciones ETAPA_ORDEN/etapaOrden/isAutoTransitionBlocked a eliminar)
    - backend/src/modules/turnos/turnos.service.ts:156-179 (bloque de auto-transición a relajar; uso de pacienteCRM.flujo en :171-173)
    - .planning/phases/57-backend-foundation-etapa-y-payload-enriquecido/57-PATTERNS.md ("turnos.service.ts — bypass guard")
  </read_first>
  <action>
    En el bloque de auto-transición de `crearTurno` (:156-168): conservar el `findUnique` de `pacienteCRM` (select `etapaCRM, profesionalId, flujo` — `flujo` se usa después en :171-173). Reemplazar el `if (!isAutoTransitionBlocked(...)) { ... update ... }` por un `prisma.paciente.update` INCONDICIONAL que setea `data: { etapaCRM: EtapaCRM.TURNO_AGENDADO }` (D-09: un nuevo turno SIEMPRE reactiva a TURNO_AGENDADO desde cualquier etapa, incluidas PERDIDO y PROCEDIMIENTO_REALIZADO). Como `isAutoTransitionBlocked` queda sin usos en este archivo, ELIMINAR también las definiciones ahora huérfanas `ETAPA_ORDEN`, `etapaOrden`, `isAutoTransitionBlocked` (:27-50) y su comentario asociado, para no dejar código muerto (lint). Conservar el import de `EtapaCRM` (sigue usado en el update). NO tocar `presupuestos.service.ts` — su copia del guard permanece activa (D-10). Actualizar el comentario del bloque (:156) para reflejar que ahora la reactivación es incondicional.
  </action>
  <verify>
    <automated>cd backend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isAutoTransitionBlocked" backend/src/modules/turnos/turnos.service.ts` no devuelve resultados (definición y uso eliminados).
    - `grep -c "isAutoTransitionBlocked" backend/src/modules/presupuestos/presupuestos.service.ts` sigue siendo > 0 (guard de presupuestos intacto, D-10).
    - En `crearTurno`, el `prisma.paciente.update` a `TURNO_AGENDADO` ya no está envuelto en un `if` de guard (es incondicional).
    - `npm run build` y `npm run lint` pasan sin errores de variable/función no usada en `turnos.service.ts`.
  </acceptance_criteria>
  <done>Un nuevo turno mueve incondicionalmente a TURNO_AGENDADO (reactivación desde cualquier etapa), el código muerto del guard se eliminó de turnos.service.ts, y el guard de presupuestos permanece intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron interno (sin usuario) → Paciente.etapaCRM | El scheduler corre sin request; barre todos los tenants globalmente |
| cliente (profesional/secretaria) → POST /turnos | La creación de turno dispara la reactivación del embudo del paciente objetivo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03-E | Elevation of Privilege / Tampering | scheduler sweep global (tenant-agnostic) | mitigate | El sweep solo actualiza `Paciente.etapaCRM` del `pacienteId` que es dueño de cada `Cirugia` (FK garantiza correspondencia paciente↔cirugía). No cruza datos entre tenants: cada paciente se evalúa contra SU propia cirugía. No modifica ninguna otra fila ni campo. |
| T-57-04-T | Tampering (regresión de etapa) | scheduler forward-only gate | mitigate | Gate explícito: `continue` si `etapaCRM === 'PERDIDO'` y si `etapaOrden >= 6`; filtro `estado notIn [CANCELADA, SUSPENDIDA]` evita mover por cirugías anuladas. Evita degradar o mover indebidamente estados terminales/superiores. |
| T-57-05-E | Elevation / abuso de reactivación | bypass del guard en crearTurno | mitigate | El bypass solo afecta la etapaCRM del `dto.pacienteId` del turno que se está creando; no hay movimiento cross-tenant. Las validaciones de existencia de paciente/profesional/tipoTurno (:68-79) se conservan; la reactivación no crea acceso nuevo, solo reordena el embudo del propio paciente. Las stats reales (fase 60) no dependen de etapaCRM (D-10), así que la reactivación no infla conteos. |
| T-57-SC | Tampering | npm installs | accept | No se agregan dependencias (`@nestjs/schedule` ya presente en package.json ^6.1.0). Sin superficie de supply-chain nueva. |
</threat_model>

<verification>
- `cd backend && npm run test -- cirugia-realizada-scheduler.service.spec` pasa.
- `cd backend && npm run build && npm run lint` pasan.
- `grep -n "isAutoTransitionBlocked" backend/src/modules/turnos/turnos.service.ts` sin resultados.
- `grep -c "isAutoTransitionBlocked" backend/src/modules/presupuestos/presupuestos.service.ts` > 0.
- `grep -n "CirugiaRealizadaSchedulerService" backend/src/modules/pacientes/pacientes.module.ts` muestra import + provider.
</verification>

<success_criteria>
- Job diario auto-mueve a PROCEDIMIENTO_REALIZADO por fecha de cirugía pasada (forward-only, excluye PERDIDO y cirugías canceladas), persistido en DB.
- Scheduler registrado y activo vía ScheduleModule ya presente.
- Un nuevo turno reactiva incondicionalmente a TURNO_AGENDADO desde cualquier etapa (EMBUDO-05).
- Guard de presupuestos intacto (D-10); sin código muerto en turnos.service.ts; sin dependencias nuevas.
</success_criteria>

<output>
Create `.planning/phases/57-backend-foundation-etapa-y-payload-enriquecido/57-02-SUMMARY.md` when done
</output>
