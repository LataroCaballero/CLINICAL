---
phase: 58-kanban-board-columnas-tarjetas-y-etiquetas
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useCRMKanban.ts
  - frontend/src/components/crm/KanbanBoard.tsx
  - frontend/src/components/crm/PatientCard.tsx
autonomous: true
requirements: [EMBUDO-01, EMBUDO-03, EMBUDO-04, CONTACTO-01, CONTACTO-02]

must_haves:
  truths:
    - "La columna 'Sin clasificar' aparece como ultima columna del board"
    - "La columna 'Cirugia Realizada' aparece inmediatamente despues de 'Confirmado'"
    - "Un paciente en 'Cirugia Realizada' con al menos un paso pendiente muestra un indicador naranja en su tarjeta"
    - "Un paciente con los 5 pasos completos (todosCompletos) no aparece en ninguna columna del board"
    - "Una tarjeta en 'Confirmado' sin turno de cirugia muestra la etiqueta 'Espera fecha'"
    - "Una tarjeta en 'Confirmado' con turno de cirugia muestra la etiqueta 'Cirugia programada'"
  artifacts:
    - path: "frontend/src/hooks/useCRMKanban.ts"
      provides: "KanbanPatient extendido con pasos + todosCompletos; ETAPA_ORDER reordenado (SIN_CLASIFICAR last, PROCEDIMIENTO_REALIZADO tras CONFIRMADO); ETAPA_LABELS PROCEDIMIENTO_REALIZADO = 'Cirugia Realizada'"
      contains: "PROCEDIMIENTO_REALIZADO"
    - path: "frontend/src/components/crm/KanbanBoard.tsx"
      provides: "Filtro que oculta pacientes con todosCompletos del board (EMBUDO-04)"
      contains: "todosCompletos"
    - path: "frontend/src/components/crm/PatientCard.tsx"
      provides: "Indicador naranja en tarjetas de Cirugia Realizada + etiqueta de contacto Espera fecha / Cirugia programada en Confirmado"
      contains: "Cirugia programada"
  key_links:
    - from: "frontend/src/components/crm/KanbanBoard.tsx"
      to: "patient.todosCompletos"
      via: "filter en displayedColumns"
      pattern: "todosCompletos"
    - from: "frontend/src/components/crm/PatientCard.tsx"
      to: "patient.pasos.cirugia"
      via: "seleccion de etiqueta de contacto en columna CONFIRMADO"
      pattern: "pasos\\.cirugia"
    - from: "frontend/src/hooks/useCRMKanban.ts"
      to: "ETAPA_ORDER"
      via: "orden de columnas del board"
      pattern: "PROCEDIMIENTO_REALIZADO"
---

<objective>
Actualizar el board del Kanban CRM (frontend puro) para reflejar el nuevo orden de columnas, mostrar la columna "Cirugia Realizada" con su indicador naranja, ocultar pacientes con todos los pasos completos, y agregar la etiqueta de contacto ("Espera fecha" / "Cirugia programada") en las tarjetas de "Confirmado".

Purpose: El backend (Phase 57) ya expone por paciente `pasos` (5 pasos 'completo'|'pendiente') y `todosCompletos`, y ya incluye `PROCEDIMIENTO_REALIZADO` como columna propia en `getKanban`. El frontend todavia excluye esa columna, ordena "Sin clasificar" primero, y no consume `pasos`/`todosCompletos`. Este plan cablea el payload existente a la UI.

Output: 3 archivos frontend modificados. Cero cambios de backend (el payload de Phase 57 ya trae todo lo necesario).
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-backend-foundation-etapa-y-payload-enriquecido/57-01-SUMMARY.md

<interfaces>
<!-- Contrato del payload getKanban ya emitido por el backend (Phase 57). El executor NO debe explorar el backend: usar estos nombres de campo tal cual. -->

Backend getKanban (backend/src/modules/pacientes/pacientes.service.ts:619) hace spread de computePasosCrm(...) en cada paciente, por lo que el objeto paciente incluye:

pasos: {
  hc: 'completo' | 'pendiente';
  presupuesto: 'completo' | 'pendiente';
  cirugia: 'completo' | 'pendiente';        // 'completo' = existe registro Cirugia (D-07 = turno de cirugia)
  consentimiento: 'completo' | 'pendiente';
  indicacionesPreop: 'completo' | 'pendiente';
};
todosCompletos: boolean;                      // true solo cuando los 5 pasos son 'completo' (EMBUDO-04)

Tipo fuente de verdad: backend/src/modules/pacientes/crm-steps.helper.ts (PasosCrm / PasoEstado).

Estado ACTUAL del frontend (a modificar):
- frontend/src/hooks/useCRMKanban.ts: interface KanbanPatient NO declara pasos ni todosCompletos.
  ETAPA_ORDER = [SIN_CLASIFICAR, NUEVO_LEAD, TURNO_AGENDADO, CONSULTADO, PRESUPUESTO_ENVIADO, CONFIRMADO, PERDIDO]
  (SIN_CLASIFICAR primero; PROCEDIMIENTO_REALIZADO excluido a proposito por comentario).
  ETAPA_LABELS.PROCEDIMIENTO_REALIZADO = "Procedimiento Realizado".
- KanbanBoard.tsx: sortedColumns filtra por ETAPA_ORDER.includes y ordena por ETAPA_ORDER.indexOf; displayedColumns aplica pendingMoves + orden por temperatura.
- PatientCard.tsx: recibe prop columnId:string; ya renderiza badges (lista espera, autorizaciones). No consume pasos.
- KanbanColumn.tsx: usa ETAPA_LABELS para el header (no requiere cambios).
- EtapaStepper.tsx: usa STEPPER_CHAIN propio para ordenar (NO ETAPA_ORDER) y ETAPA_LABELS solo para texto.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender el contrato del board en useCRMKanban.ts (interface + orden + label)</name>
  <read_first>
    - frontend/src/hooks/useCRMKanban.ts (archivo a modificar: interface KanbanPatient, ETAPA_ORDER, ETAPA_LABELS)
    - backend/src/modules/pacientes/crm-steps.helper.ts (fuente de verdad del shape PasosCrm/PasoEstado que el payload hace spread)
    - frontend/src/components/crm/EtapaStepper.tsx (consumidor de ETAPA_LABELS; usa STEPPER_CHAIN propio, NO ETAPA_ORDER — confirmar que agregar PROCEDIMIENTO_REALIZADO a ETAPA_ORDER no lo afecta)
  </read_first>
  <action>
    En frontend/src/hooks/useCRMKanban.ts realizar tres cambios:

    1. Extender la interface KanbanPatient con los dos campos que el payload de Phase 57 ya devuelve: un campo `pasos` objeto con las 5 keys exactas `hc`, `presupuesto`, `cirugia`, `consentimiento`, `indicacionesPreop`, cada una tipada como union `'completo' | 'pendiente'` (definir un type alias `PasoEstado = 'completo' | 'pendiente'` y `PasosCrm` con esas 5 keys, espejando crm-steps.helper.ts), y un campo `todosCompletos: boolean`. Colocarlos junto a los campos existentes de la interface.

    2. Reemplazar el arreglo ETAPA_ORDER (y borrar el comentario "PROCEDIMIENTO_REALIZADO intentionally excluded — hidden from kanban") por el nuevo orden EXACTO: `NUEVO_LEAD`, `TURNO_AGENDADO`, `CONSULTADO`, `PRESUPUESTO_ENVIADO`, `CONFIRMADO`, `PROCEDIMIENTO_REALIZADO`, `PERDIDO`, `SIN_CLASIFICAR`. Esto pone PROCEDIMIENTO_REALIZADO inmediatamente despues de CONFIRMADO (SC2 / EMBUDO context) y SIN_CLASIFICAR como ultima columna (SC1 / EMBUDO-01).

    3. Cambiar ETAPA_LABELS.PROCEDIMIENTO_REALIZADO de "Procedimiento Realizado" a "Cirugia Realizada" (con acento: "Cirugía Realizada"). No tocar los otros labels. Nota: EtapaStepper.tsx tambien lee ETAPA_LABELS para texto — el cambio de label es intencional y deseado (consistencia de naming); EtapaStepper ordena por su STEPPER_CHAIN propio, asi que agregar PROCEDIMIENTO_REALIZADO a ETAPA_ORDER no lo rompe.

    No modificar useCRMKanban (la funcion query) ni la URL del endpoint.
  </action>
  <verify>
    <automated>cd frontend && grep -q "todosCompletos" src/hooks/useCRMKanban.ts && grep -q "Cirugía Realizada" src/hooks/useCRMKanban.ts && node -e "const t=require('fs').readFileSync('src/hooks/useCRMKanban.ts','utf8'); const m=t.match(/ETAPA_ORDER[^\[]*\[([^\]]*)\]/s)[1]; const order=[...m.matchAll(/\"([A-Z_]+)\"/g)].map(x=>x[1]); const ci=order.indexOf('CONFIRMADO'); if(order[ci+1]!=='PROCEDIMIENTO_REALIZADO') throw new Error('PROCEDIMIENTO_REALIZADO no va tras CONFIRMADO: '+order); if(order[order.length-1]!=='SIN_CLASIFICAR') throw new Error('SIN_CLASIFICAR no es ultima: '+order); console.log('ORDER OK',order);"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: la interface KanbanPatient declara `pasos` con las 5 keys `hc|presupuesto|cirugia|consentimiento|indicacionesPreop` (tipo `'completo'|'pendiente'`) y `todosCompletos: boolean`.
    - Behavior assertion: en ETAPA_ORDER, el elemento inmediatamente posterior a `CONFIRMADO` es `PROCEDIMIENTO_REALIZADO` (SC2: "Cirugia Realizada" tras "Confirmado").
    - Behavior assertion: el ultimo elemento de ETAPA_ORDER es `SIN_CLASIFICAR` (SC1 / EMBUDO-01: "Sin clasificar" al final).
    - Source assertion: ETAPA_LABELS.PROCEDIMIENTO_REALIZADO === "Cirugía Realizada".
    - CLI: el comando de verify pasa (grep + assert de orden) sin lanzar error.
  </acceptance_criteria>
  <done>KanbanPatient expone pasos + todosCompletos; ETAPA_ORDER coloca PROCEDIMIENTO_REALIZADO tras CONFIRMADO y SIN_CLASIFICAR al final; el label es "Cirugía Realizada".</done>
</task>

<task type="auto">
  <name>Task 2: Ocultar pacientes con todos los pasos completos del board (EMBUDO-04)</name>
  <read_first>
    - frontend/src/components/crm/KanbanBoard.tsx (archivo a modificar: useMemo displayedColumns / sortedColumns)
    - frontend/src/hooks/useCRMKanban.ts (contrato KanbanPatient.todosCompletos agregado en Task 1)
  </read_first>
  <action>
    En frontend/src/components/crm/KanbanBoard.tsx, dentro del useMemo `displayedColumns` (el que mapea columnas y ordena por temperatura), agregar un filtro que EXCLUYA de la lista `pacientes` de cada columna a todo paciente con `p.todosCompletos === true`. Como `todosCompletos` requiere el paso cirugia completo, en la practica solo aplica a operados (EMBUDO-04: "paciente operado con todos los pasos completos se oculta del board"), pero el filtro se aplica a todas las columnas por seguridad.

    Tras filtrar, recalcular `total` de cada columna para que el contador del header refleje los pacientes efectivamente visibles (usar el largo del arreglo filtrado, no el `total` del backend). Mantener intacta la logica existente de `applyPendingMoves` y del orden por temperatura (TEMP_ORDER). El filtro de todosCompletos debe aplicarse sobre el resultado ya procesado por pendingMoves.

    No tocar handleDragEnd, pendingMoves ni la logica de PERDIDO.
  </action>
  <verify>
    <automated>cd frontend && grep -q "todosCompletos" src/components/crm/KanbanBoard.tsx && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `displayedColumns` filtra `pacientes` excluyendo `todosCompletos === true` (patron `todosCompletos` presente en el filtro).
    - Behavior assertion (SC3 / EMBUDO-04): un paciente con los 5 pasos completos no aparece en ninguna columna renderizada del board.
    - Behavior assertion: el contador (`total`) del header de cada columna cuenta solo los pacientes visibles tras el filtro.
    - CLI: `npm run lint` no reporta errores nuevos en KanbanBoard.tsx.
  </acceptance_criteria>
  <done>Los pacientes con todosCompletos quedan ocultos del board y no se cuentan en los headers de columna.</done>
</task>

<task type="auto">
  <name>Task 3: Indicador naranja en Cirugia Realizada + etiqueta de contacto en Confirmado (PatientCard)</name>
  <read_first>
    - frontend/src/components/crm/PatientCard.tsx (archivo a modificar: componente de tarjeta, prop columnId, badges existentes)
    - frontend/src/hooks/useCRMKanban.ts (contrato KanbanPatient.pasos / todosCompletos agregado en Task 1)
    - frontend/src/components/crm/KanbanColumn.tsx (pasa columnId={column.etapa} a PatientCard — confirmar el valor exacto de columnId)
  </read_first>
  <action>
    En frontend/src/components/crm/PatientCard.tsx agregar dos elementos visuales condicionados por `columnId` (que llega con el valor de la etapa, p.ej. "PROCEDIMIENTO_REALIZADO" o "CONFIRMADO"):

    1. Indicador naranja (EMBUDO-03 / SC3): cuando `columnId === "PROCEDIMIENTO_REALIZADO"` y el paciente tiene al menos un paso pendiente (equivalente: `patient.todosCompletos !== true`, o `Object.values(patient.pasos).some(v => v === "pendiente")`), renderizar un indicador visual naranja en la tarjeta — un badge/punto naranja (usar clases Tailwind existentes del repo, p.ej. `bg-orange-500` para el punto y/o `border-orange-400 text-orange-600` para un badge con texto corto "Pasos pendientes"). Debe ser claramente naranja y visible en la tarjeta. Como Task 2 oculta a los pacientes con todosCompletos, cualquier tarjeta mostrada en esta columna tendra el indicador; gatear igual en `patient.todosCompletos !== true` para robustez.

    2. Etiqueta de contacto (CONTACTO-01 / CONTACTO-02 / SC4): cuando `columnId === "CONFIRMADO"`, renderizar una etiqueta de contacto en la tarjeta cuyo texto depende de `patient.pasos.cirugia`:
       - si `patient.pasos.cirugia === "pendiente"` (sin turno de cirugia) → texto EXACTO "Espera fecha".
       - si `patient.pasos.cirugia === "completo"` (con turno de cirugia, D-07) → texto EXACTO "Cirugia programada" (con acento: "Cirugía programada").
       Renderizar como un badge/chip pequeño consistente con los badges existentes de la tarjeta (p.ej. estilo similar a los chips de lista de espera / autorizacion). Usar `patient.pasos?.cirugia` de forma segura por si `pasos` viniera indefinido.

    No eliminar ni alterar los badges existentes (WA unread, lista espera, autorizaciones, ultimo contacto). No cambiar la firma del componente mas alla de leer `patient.pasos`/`patient.todosCompletos` (ya disponibles via el tipo KanbanPatient).
  </action>
  <verify>
    <automated>cd frontend && grep -q "Espera fecha" src/components/crm/PatientCard.tsx && grep -q "Cirugía programada" src/components/crm/PatientCard.tsx && grep -Eq "orange" src/components/crm/PatientCard.tsx && grep -q "PROCEDIMIENTO_REALIZADO" src/components/crm/PatientCard.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion (SC4 / CONTACTO-01): tarjeta en CONFIRMADO con `pasos.cirugia === "pendiente"` muestra el texto exacto "Espera fecha".
    - Behavior assertion (SC4 / CONTACTO-02): tarjeta en CONFIRMADO con `pasos.cirugia === "completo"` muestra el texto exacto "Cirugía programada".
    - Behavior assertion (SC3 / EMBUDO-03): tarjeta en PROCEDIMIENTO_REALIZADO con al menos un paso pendiente muestra un indicador naranja (clase `orange`).
    - Source assertion: la etiqueta de contacto se gatea por `columnId === "CONFIRMADO"` y el indicador naranja por `columnId === "PROCEDIMIENTO_REALIZADO"`.
    - CLI: `npx tsc --noEmit` compila sin errores de tipo (el consumo de `patient.pasos`/`patient.todosCompletos` es valido contra el tipo extendido en Task 1).
  </acceptance_criteria>
  <done>Las tarjetas de Cirugia Realizada muestran indicador naranja cuando hay pasos pendientes; las de Confirmado muestran "Espera fecha" o "Cirugía programada" segun pasos.cirugia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend getKanban → frontend board | El payload ya viene filtrado por `where: { profesionalId, crmArchivado: false }` (multi-tenant). El frontend solo lee/oculta/etiqueta datos ya scopeados. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Information Disclosure | Orden/label de columnas + filtro todosCompletos (frontend) | accept | Solo display logic sobre datos ya scopeados por profesionalId en getKanban (Phase 57). No se introduce ningun endpoint ni query nueva; el frontend no ampl a el conjunto de pacientes, solo lo reordena/oculta. Sin fuga cross-tenant. |
| T-58-02 | Tampering | Etiqueta de contacto e indicador naranja derivados de patient.pasos | accept | Derivados de campos read-only ('completo'|'pendiente') calculados server-side por computePasosCrm; el frontend no escribe ni recomputa reglas de negocio. |
| T-58-03 | Information Disclosure | EMBUDO-04 hide client-side | mitigate | El ocultamiento de pacientes con todosCompletos es solo presentacional; no expone datos adicionales. No se agrega ningun campo clinico nuevo al payload (Phase 57 ya limita el select a timestamps y flags 'completo'/'pendiente'). |
</threat_model>

<verification>
- Los 3 archivos frontend compilan (`cd frontend && npx tsc --noEmit -p tsconfig.json`) y pasan lint (`npm run lint`).
- ETAPA_ORDER: PROCEDIMIENTO_REALIZADO inmediatamente tras CONFIRMADO; SIN_CLASIFICAR ultimo.
- ETAPA_LABELS.PROCEDIMIENTO_REALIZADO === "Cirugía Realizada".
- KanbanBoard oculta pacientes con todosCompletos y ajusta el contador de columna.
- PatientCard: "Espera fecha" / "Cirugía programada" en CONFIRMADO segun pasos.cirugia; indicador naranja en PROCEDIMIENTO_REALIZADO con pasos pendientes.
- No se modifico ningun archivo de backend.
</verification>

<success_criteria>
1. (EMBUDO-01) La columna "Sin clasificar" es la ultima del board.
2. (EMBUDO-02 display) La columna "Cirugía Realizada" aparece inmediatamente despues de "Confirmado".
3. (EMBUDO-03) Un paciente en "Cirugía Realizada" con al menos un paso pendiente muestra indicador naranja en su tarjeta.
4. (EMBUDO-04) Un paciente con los 5 pasos completos no aparece en el board.
5. (CONTACTO-01) Tarjeta en "Confirmado" sin turno de cirugia muestra "Espera fecha".
6. (CONTACTO-02) Tarjeta en "Confirmado" con turno de cirugia muestra "Cirugía programada".
</success_criteria>

<output>
Create `.planning/phases/58-kanban-board-columnas-tarjetas-y-etiquetas/58-01-SUMMARY.md` when done
</output>
