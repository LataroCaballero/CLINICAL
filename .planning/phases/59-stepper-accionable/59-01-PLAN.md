---
phase: 59-stepper-accionable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/crm/EtapaStepper.tsx
autonomous: true
requirements: [STEPPER-01, STEPPER-02, STEPPER-05, STEPPER-06]
must_haves:
  truths:
    - "Un paso mapeado con pasos.<x> === 'completo' muestra el círculo verde y NO muestra botón de acción"
    - "Un paso mapeado con pasos.<x> === 'pendiente' muestra el círculo naranja y SÍ muestra su botón de acción"
    - "El paso Confirmado se muestra verde cuando pasos.cirugia === 'completo' (tiene turno de cirugía) y naranja con botón 'Agendar cirugía' cuando 'pendiente'"
    - "Los pasos NO mapeados (SIN_CLASIFICAR, NUEVO_LEAD, TURNO_AGENDADO, PROCEDIMIENTO_REALIZADO) conservan su coloreo de embudo actual (done/current/pending) y el nodo PERDIDO queda intacto"
    - "Cuando flujo === 'TRATAMIENTO' se ocultan la acción de cirugía y los sub-indicadores consentimiento/indicacionesPreop; se muestran hc y presupuesto"
    - "consentimiento e indicacionesPreop se muestran como sub-indicadores verde/naranja bajo Confirmado, sin botón"
  artifacts:
    - path: "frontend/src/components/crm/EtapaStepper.tsx"
      provides: "Coloreo verde/naranja por estado de paso + botones de acción gateados + sub-indicadores"
      contains: "onCirugiaClick"
  key_links:
    - from: "EtapaStepper.tsx"
      to: "PasosCrm / KanbanPatient.flujo"
      via: "props pasos + flujo importados de @/hooks/useCRMKanban"
      pattern: "pasos\\??: PasosCrm"
---

<objective>
Enriquecer `EtapaStepper.tsx` (NO reescribirlo) para agregar una segunda dimensión de coloreo
por estado de paso (verde=completo / naranja=pendiente) sobre los 3 nodos mapeados, botones de
acción gateados por ese estado, sub-indicadores visuales de consentimiento/indicaciones, y
filtrado por flujo — conservando intactos el embudo de 7 etapas, la navegación manual por click
y el nodo PERDIDO (D-01, D-06).

Purpose: STEPPER-01/02 (coloreo verde/naranja + gating de botón) y el estado visual de
STEPPER-05/06 (Confirmado verde con cirugía / naranja sin cirugía).
Output: `EtapaStepper.tsx` con nuevas props `pasos`, `flujo`, `onCirugiaClick` y lógica de
coloreo + gating + sub-indicadores + filtrado por flujo.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-stepper-accionable/59-CONTEXT.md
@.planning/phases/59-stepper-accionable/59-PATTERNS.md

<interfaces>
<!-- Tipos que el executor consume directamente — NO explorar el codebase para hallarlos. -->

From frontend/src/hooks/useCRMKanban.ts:
- export type PasoEstado = 'completo' | 'pendiente';
- export interface PasosCrm { hc: PasoEstado; presupuesto: PasoEstado; cirugia: PasoEstado; consentimiento: PasoEstado; indicacionesPreop: PasoEstado; }
- export interface KanbanPatient { ...; flujo: 'CIRUGIA' | 'TRATAMIENTO' | 'PENDIENTE' | null; pasos: PasosCrm; todosCompletos: boolean; }
- export type EtapaCRM (incluye 'CONSULTADO' | 'PRESUPUESTO_ENVIADO' | 'CONFIRMADO' | 'PROCEDIMIENTO_REALIZADO' | 'PERDIDO' ...)
- export const ETAPA_LABELS: Record<EtapaCRM, string>

Mapeo paso→etapa (D-02) a codificar como constante junto a STEPPER_CHAIN:
  CONSULTADO          ↔ pasos.hc          → botón onHCClick ("Registrar HC")
  PRESUPUESTO_ENVIADO ↔ pasos.presupuesto → botón onPresupuestoClick ("Ver/Crear presupuesto")
  CONFIRMADO          ↔ pasos.cirugia     → botón onCirugiaClick ("Agendar cirugía")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ampliar props y codificar el mapeo paso→etapa + estado de paso por nodo</name>
  <files>frontend/src/components/crm/EtapaStepper.tsx</files>
  <read_first>
    - frontend/src/components/crm/EtapaStepper.tsx (estado actual: STEPPER_CHAIN, EtapaStepperProps líneas 18-24, render loop líneas 45-146, nodo PERDIDO 152-185)
    - frontend/src/hooks/useCRMKanban.ts (tipos PasoEstado, PasosCrm, KanbanPatient.flujo, EtapaCRM, ETAPA_LABELS)
    - .planning/phases/59-stepper-accionable/59-PATTERNS.md (sección "EtapaStepper.tsx — PRIMARY WORK", D-02/D-03)
  </read_first>
  <action>
    Extender la interface `EtapaStepperProps` con tres props opcionales: `pasos?: PasosCrm`,
    `flujo?: KanbanPatient['flujo']`, y `onCirugiaClick?: () => void` (mirror de
    `onPresupuestoClick`/`onHCClick`). Importar `PasosCrm` y el tipo `KanbanPatient` desde
    `@/hooks/useCRMKanban` (junto a los imports existentes de `ETAPA_LABELS`/`EtapaCRM`).
    Codificar el mapeo D-02 como una constante local `PASO_POR_ETAPA` (Record parcial de EtapaCRM
    a la clave de PasosCrm): `CONSULTADO → 'hc'`, `PRESUPUESTO_ENVIADO → 'presupuesto'`,
    `CONFIRMADO → 'cirugia'`. Dentro del render loop, para cada `etapa`, derivar
    `pasoKey = PASO_POR_ETAPA[etapa]` y `pasoEstado = pasoKey && pasos ? pasos[pasoKey] : undefined`;
    un booleano `esNodoMapeado = !!pasoEstado`. No cambiar aún el coloreo ni los botones — solo
    dejar disponibles `pasoEstado` y `esNodoMapeado` para la Task 2. Mantener STEPPER_CHAIN,
    `activeIndex`, `esPerdido`, `isDone`, `isCurrent` y toda la estructura existente sin tocar
    (D-01/D-06: enriquecer, no reemplazar).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | rg "EtapaStepper" || echo "OK: sin errores de tipos en EtapaStepper"</automated>
  </verify>
  <acceptance_criteria>
    - `EtapaStepper.tsx` importa `PasosCrm` desde `@/hooks/useCRMKanban` (grep: `import.*PasosCrm.*useCRMKanban` o `PasosCrm` presente en un import de ese módulo)
    - La interface `EtapaStepperProps` contiene `pasos?: PasosCrm`, `flujo?:` y `onCirugiaClick?: () => void`
    - Existe una constante que mapea `CONSULTADO`→`'hc'`, `PRESUPUESTO_ENVIADO`→`'presupuesto'`, `CONFIRMADO`→`'cirugia'` (grep del literal `'cirugia'` y `PRESUPUESTO_ENVIADO` en el mismo bloque)
    - `npx tsc --noEmit` no reporta errores nuevos en EtapaStepper.tsx
    - STEPPER_CHAIN sigue conteniendo los 7 valores originales y el bloque del nodo PERDIDO permanece sin cambios funcionales
  </acceptance_criteria>
  <done>Props y mapeo agregados; el componente compila; estructura existente intacta.</done>
</task>

<task type="auto">
  <name>Task 2: Coloreo verde/naranja por estado de paso + gating de botones + botón "Agendar cirugía"</name>
  <files>frontend/src/components/crm/EtapaStepper.tsx</files>
  <read_first>
    - frontend/src/components/crm/EtapaStepper.tsx (circleClass líneas 61-68, hasContextualButton 54-59, botones contextuales 100-142, nodo PERDIDO filled 166-172)
    - .planning/phases/59-stepper-accionable/59-PATTERNS.md (D-03 semántica de color y gating; className exacta del botón contextual)
  </read_first>
  <action>
    Para los nodos mapeados (`esNodoMapeado === true`), el `circleClass` debe usar el estado de
    paso en lugar de la posición de embudo (D-03: el estado del paso manda sobre el embudo para
    estos 3 nodos): verde cuando `pasoEstado === 'completo'` usando clases
    `"border-green-500 bg-green-500"`, naranja cuando `pasoEstado === 'pendiente'` usando
    `"border-orange-500 bg-orange-500"` (espejando la convención de relleno del nodo PERDIDO
    `border-red-500 bg-red-500`). Para los nodos NO mapeados, conservar exactamente el `circleClass`
    actual (isCurrent/isDone/pending funnel). Mantener el tratamiento de label `isCurrent`
    (negrita) sin cambios.
    Gatear los botones de acción por estado de paso (STEPPER-01/02: verde = sin botón, naranja =
    con botón): el botón de `CONSULTADO`/`onHCClick` y el de `PRESUPUESTO_ENVIADO`/`onPresupuestoClick`
    ahora solo se renderizan cuando su `pasoEstado === 'pendiente'` (además del handler presente).
    Agregar el nuevo botón "Agendar cirugía" en el nodo `CONFIRMADO`, copiando EXACTAMENTE la forma
    del botón contextual existente (className
    `"mb-3 flex items-center gap-1.5 text-xs text-muted-foreground border border-border rounded px-2 py-1 hover:bg-muted/50 w-fit"`,
    `e.stopPropagation()` en el onClick para no disparar `onClickEtapa`), visible solo cuando
    `pasos?.cirugia === 'pendiente'` && `onCirugiaClick` presente; usar un ícono de lucide-react
    (p. ej. `CalendarPlus` o `Scissors`) importado junto a los existentes. Extender el predicado
    `hasContextualButton` para incluir el caso cirugía-pendiente, de modo que el padding del label
    (línea 72) siga correcto. NO tocar el botón "Marcar como realizado" de `PROCEDIMIENTO_REALIZADO`
    ni la navegación manual `onClickEtapa` (D-06).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | rg "EtapaStepper" ; npm run lint 2>&1 | rg "EtapaStepper" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `EtapaStepper.tsx` contiene las clases `border-green-500 bg-green-500` y `border-orange-500 bg-orange-500` aplicadas condicionalmente por `pasoEstado`
    - El botón de HC solo se renderiza cuando `pasos?.hc === 'pendiente'` (grep de `pasos?.hc === 'pendiente'` o equivalente sobre la clave `hc`) y análogamente presupuesto sobre `'presupuesto'`
    - Existe un botón "Agendar cirugía" con `onCirugiaClick` y `e.stopPropagation()`, gateado por `pasos?.cirugia === 'pendiente'`
    - `hasContextualButton` incluye el caso cirugía-pendiente
    - El bloque del nodo `PROCEDIMIENTO_REALIZADO` ("Marcar como realizado") y el nodo PERDIDO permanecen sin cambios de comportamiento
    - `npx tsc --noEmit` y `npm run lint` sin errores nuevos en EtapaStepper.tsx
  </acceptance_criteria>
  <done>Los 3 nodos mapeados pintan verde/naranja según pasos; botones gateados; "Agendar cirugía" agregado; resto intacto.</done>
</task>

<task type="auto">
  <name>Task 3: Sub-indicadores consentimiento/indicaciones + filtrado por flujo TRATAMIENTO</name>
  <files>frontend/src/components/crm/EtapaStepper.tsx</files>
  <read_first>
    - frontend/src/components/crm/EtapaStepper.tsx (columna derecha del nodo, flex flex-col ~línea 97)
    - .planning/phases/59-stepper-accionable/59-CONTEXT.md (D-04 sub-indicadores; D-05 filtrado por flujo, edge case PENDIENTE/null)
  </read_first>
  <action>
    Bajo el nodo `CONFIRMADO` (dentro de su columna derecha `flex flex-col`), renderizar dos
    sub-indicadores visuales SOLO de estado (D-04, sin botón): "Consentimiento" y "Indicaciones
    preop", cada uno con un punto pequeño verde cuando `pasos?.consentimiento`/
    `pasos?.indicacionesPreop === 'completo'` y naranja cuando `=== 'pendiente'`, con label en
    texto chico (`text-xs text-muted-foreground`). Usar un dot (`h-2 w-2 rounded-full`) con
    `bg-green-500`/`bg-orange-500`; NO usar `<button>` (son visual-only; sus acciones están
    diferidas). Filtrado por flujo (D-05, client-side): cuando `flujo === 'TRATAMIENTO'`, ocultar
    el botón "Agendar cirugía" del nodo CONFIRMADO y ocultar ambos sub-indicadores
    consentimiento/indicaciones (mostrar solo el estado hc/presupuesto). Para `flujo` distinto de
    'TRATAMIENTO' (incluye 'CIRUGIA', 'PENDIENTE' y `null`): mostrar todo (edge case PENDIENTE/null
    = comportarse como CIRUGIA, discreción del planner en D-05). Implementar como un booleano
    `esTratamiento = flujo === 'TRATAMIENTO'` y gatear con él tanto el botón de cirugía como los
    sub-indicadores.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | rg "EtapaStepper" || echo "OK: tipos EtapaStepper limpios"</automated>
  </verify>
  <acceptance_criteria>
    - Bajo CONFIRMADO se renderizan sub-indicadores para `consentimiento` e `indicacionesPreop` con dot verde/naranja y sin `<button>`
    - Existe un booleano derivado de `flujo === 'TRATAMIENTO'` que oculta el botón "Agendar cirugía" y los sub-indicadores
    - Con `flujo` igual a `'CIRUGIA'`, `'PENDIENTE'` o `null`, el botón de cirugía y los sub-indicadores permanecen visibles (según estado de paso)
    - `npx tsc --noEmit` sin errores nuevos en EtapaStepper.tsx
  </acceptance_criteria>
  <done>Sub-indicadores visuales bajo Confirmado y filtrado por flujo TRATAMIENTO implementados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Este plan es 100% presentacional (frontend). No cruza ningún límite de confianza: no lee ni escribe datos, no llama a la API, no maneja input no confiable. Solo pinta props ya provistas por `getKanban` (endpoint ya autenticado/tenant-scoped en Phase 57). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Information Disclosure | EtapaStepper render de `pasos`/`flujo` | accept | Los datos ya vienen filtrados por profesional/tenant en el payload de `getKanban`; el componente no expone nada nuevo ni agrega endpoints. Sin PII adicional renderizada. |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | Sin instalaciones de paquetes: se reutilizan `cn`, `lucide-react` y tipos ya presentes (REQUIREMENTS "Out of Scope": nuevas dependencias prohibidas). |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` sin errores nuevos.
- `cd frontend && npm run lint` sin errores nuevos en EtapaStepper.tsx.
- Inspección de source: los 3 nodos mapeados usan clases green/orange; botones gateados por pasos; sub-indicadores sin button; filtrado por flujo presente.
</verification>

<success_criteria>
- Un paso completo → círculo verde, sin botón (STEPPER-01).
- Un paso pendiente → círculo naranja, con botón (STEPPER-02).
- Confirmado verde con cirugía / naranja + "Agendar cirugía" sin cirugía (STEPPER-05/06 visual).
- Embudo de 7 etapas, navegación manual y PERDIDO intactos (D-01/D-06).
</success_criteria>

<output>
Create `.planning/phases/59-stepper-accionable/59-01-SUMMARY.md` when done
</output>
