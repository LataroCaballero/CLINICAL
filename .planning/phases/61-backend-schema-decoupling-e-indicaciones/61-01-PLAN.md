---
phase: 61-backend-schema-decoupling-e-indicaciones
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/schema.prisma
  - backend/src/prisma/migrations/20260708000000_add_indicaciones_leidas/migration.sql
  - backend/src/modules/catalogo-hc/catalogo-hc.service.ts
  - backend/src/modules/catalogo-hc/catalogo-hc.controller.ts
autonomous: true
requirements: [INDIC-03]
must_haves:
  truths:
    - "La migración agrega Paciente.indicacionesLeidasAt (DateTime nullable) sin tocar filas existentes de ConsentimientoFirmado"
    - "ConsentimientoFirmado.indicacionesLeidasAt pasa a nullable preservando los timestamps forenses v1.12"
    - "El Prisma client regenerado expone Paciente.indicacionesLeidasAt para el codigo de las Waves 2"
    - "indicacionesUrl se valida server-side (http/https only, maxLength 2048, reject-on-throw) sin comentarios enganosos de @IsUrl"
  artifacts:
    - path: "backend/src/prisma/schema.prisma"
      provides: "Campo Paciente.indicacionesLeidasAt DateTime? + ConsentimientoFirmado.indicacionesLeidasAt DateTime?"
      contains: "indicacionesLeidasAt              DateTime?"
    - path: "backend/src/prisma/migrations/20260708000000_add_indicaciones_leidas/migration.sql"
      provides: "Migracion pgBouncer additive/DROP NOT NULL"
      contains: "ALTER TABLE \"Paciente\" ADD COLUMN \"indicacionesLeidasAt\""
  key_links:
    - from: "schema.prisma"
      to: "prisma client (node_modules/.prisma)"
      via: "npx prisma generate"
      pattern: "indicacionesLeidasAt"
---

<objective>
Entrega la base de schema de v1.14: agrega el campo global set-once `Paciente.indicacionesLeidasAt` (INDIC-03), relaja `ConsentimientoFirmado.indicacionesLeidasAt` a nullable preservando el valor forense v1.12 (D-01), aplica la migracion con el patron pgBouncer (SC#4) y regenera el Prisma client. Cierra ademas el folded todo cr-01 (limpieza del docstring enganoso de la validacion de `indicacionesUrl`, que YA esta implementada en el codigo).

Purpose: Sin este campo y sin el client regenerado, el codigo de la Wave 2 (endpoint de acuse, derivacion CRM) no compila ni corre contra la BD real. La migracion debe aplicarse a la BD viva porque los type-checks pasan solo con schema.prisma (falso positivo).
Output: schema.prisma actualizado, migration.sql aplicado + reconciliado en historial, Prisma client regenerado, cr-01 cerrado.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-CONTEXT.md
@.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-PATTERNS.md
@.planning/todos/pending/cr-01-indicaciones-url-validation.md

<interfaces>
<!-- schema.prisma existing nullable analogs (copy the shape) -->
Paciente model (schema.prisma):
  line 173: indicacionesEnviadas              Boolean                  @default(false)
  line 174: fechaIndicaciones                 DateTime?
  line 216: consentimientoFirmadoAt           DateTime?     <- nearest nullable timestamp analog

ConsentimientoFirmado model (schema.prisma line 1437):
  indicacionesLeidasAt        DateTime // set at signing time, required   <- relax to DateTime?

Datasource (schema.prisma lines 9-10):
  url       = env("DATABASE_URL")   // pgBouncer-pooled
  directUrl = env("DIRECT_URL")     // bypass for schema ops
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editar schema.prisma — agregar campo Paciente + relajar ConsentimientoFirmado</name>
  <files>backend/src/prisma/schema.prisma</files>
  <read_first>
    - backend/src/prisma/schema.prisma (Paciente model 152-236; ConsentimientoFirmado model 1426-1446)
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-PATTERNS.md (seccion 1 schema deltas)
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-CONTEXT.md (D-01)
  </read_first>
  <action>
    Delta (a) INDIC-03: en el modelo `Paciente`, agregar el campo `indicacionesLeidasAt DateTime?` cerca de la linea 216 junto a los otros campos de consentimiento/indicaciones (`consentimientoFirmadoAt`, `fechaIndicaciones`). Comentario: INDIC-03 acuse global set-once. Copiar la forma exacta de los `DateTime?` existentes del mismo modelo.
    Delta (b) D-01: en el modelo `ConsentimientoFirmado` linea 1437, cambiar `indicacionesLeidasAt DateTime` a `indicacionesLeidasAt DateTime?`. NO dropear la columna, NO tocar otras columnas. Actualizar el comentario inline a: legacy v1.12 forensic value; new rows leave NULL (D-01).
    NO ejecutar `prisma generate` todavia (eso va en Task 2 tras la migracion).
  </action>
  <verify>
    <automated>cd backend && grep -q "indicacionesLeidasAt              DateTime?" src/prisma/schema.prisma && grep -q "indicacionesLeidasAt        DateTime?" src/prisma/schema.prisma && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - Schema: `schema.prisma` modelo `Paciente` contiene `indicacionesLeidasAt              DateTime?`
    - Schema: `schema.prisma` modelo `ConsentimientoFirmado` contiene `indicacionesLeidasAt        DateTime?` (nullable, columna NO dropeada)
    - CLI: `npx prisma validate` exits 0
  </acceptance_criteria>
  <done>Ambos deltas aplicados; `prisma validate` pasa; ninguna otra columna alterada.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Crear y aplicar migracion pgBouncer + regenerar client</name>
  <files>backend/src/prisma/migrations/20260708000000_add_indicaciones_leidas/migration.sql</files>
  <read_first>
    - backend/src/prisma/migrations/20260701000000_signed_consent_forensic/migration.sql (analog: header-comment + additive style)
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-PATTERNS.md (seccion 2 migration mechanics)
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-CONTEXT.md (Established Patterns — pgBouncer)
    - .planning/PROJECT.md (patron migracion pgBouncer)
  </read_first>
  <action>
    Crear `backend/src/prisma/migrations/20260708000000_add_indicaciones_leidas/migration.sql` con dos statements comentados por delta, siguiendo el estilo del analog Phase 56:
    Delta 1 (INDIC-03): ALTER TABLE "Paciente" ADD COLUMN "indicacionesLeidasAt" TIMESTAMP(3);  (sin default — metadata-only, no reescribe filas).
    Delta 2 (D-01): ALTER TABLE "ConsentimientoFirmado" ALTER COLUMN "indicacionesLeidasAt" DROP NOT NULL;  (preserva filas forenses v1.12, no las toca).
    Header comment: Phase 61 — Indicaciones acuse + consent decoupling. Sin indice (campo global unico).
    Aplicar SIGUIENDO EL PATRON pgBouncer del proyecto — NUNCA `npx prisma migrate dev` ni `npx prisma db push`:
    1. `npx prisma migrate diff --from-schema-datasource prisma/schema.prisma --to-schema-datamodel prisma/schema.prisma` para verificar que el SQL generado coincide con el escrito (el SQL escrito a mano es la fuente de verdad).
    2. Aplicar el SQL a la BD viva via `npx prisma db execute --file src/prisma/migrations/20260708000000_add_indicaciones_leidas/migration.sql --schema src/prisma/schema.prisma` (usa directUrl/DIRECT_URL para bypassear pgBouncer en schema ops).
    3. Reconciliar historial: `npx prisma migrate resolve --applied 20260708000000_add_indicaciones_leidas`.
    4. Regenerar tipos: `npx prisma generate` (para que Paciente.indicacionesLeidasAt exista en el client antes de las Waves 2).
    Rutas de comando desde `backend/`. Si algun paso pide prompt interactivo que no se puede suprimir, detener y marcar checkpoint (no forzar).
  </action>
  <verify>
    <automated>cd backend && grep -q 'ALTER TABLE "Paciente" ADD COLUMN "indicacionesLeidasAt"' src/prisma/migrations/20260708000000_add_indicaciones_leidas/migration.sql && grep -q 'ALTER COLUMN "indicacionesLeidasAt" DROP NOT NULL' src/prisma/migrations/20260708000000_add_indicaciones_leidas/migration.sql && npx prisma migrate status</automated>
  </verify>
  <acceptance_criteria>
    - Source: `migration.sql` contiene ambos ALTER (ADD COLUMN Paciente + DROP NOT NULL ConsentimientoFirmado), header comment Phase 61
    - CLI: `npx prisma migrate status` reporta la migracion `20260708000000_add_indicaciones_leidas` como aplicada / historial en sync (no drift/pending)
    - Behavior: una query `SELECT is_nullable FROM information_schema.columns WHERE table_name='Paciente' AND column_name='indicacionesLeidasAt'` devuelve una fila `is_nullable=YES` (columna existe en la BD viva)
    - Behavior: NO se uso `migrate dev` ni `db push` (revisar el SUMMARY de comandos ejecutados)
    - Data: filas existentes de `ConsentimientoFirmado` conservan su `indicacionesLeidasAt` (ningun UPDATE/DELETE en el SQL)
  </acceptance_criteria>
  <done>Migracion escrita, aplicada a la BD viva via diff+execute+resolve, historial en sync, client regenerado; registros forenses intactos.</done>
</task>

<task type="auto">
  <name>Task 3: cr-01 — verificar validacion URL existente + limpiar docstring enganoso</name>
  <files>backend/src/modules/catalogo-hc/catalogo-hc.service.ts, backend/src/modules/catalogo-hc/catalogo-hc.controller.ts</files>
  <read_first>
    - backend/src/modules/catalogo-hc/catalogo-hc.service.ts (actualizarIndicacionesUrl 755-788 — validacion YA presente en 766-779)
    - backend/src/modules/catalogo-hc/catalogo-hc.controller.ts (actualizarIndicaciones 115-131)
    - .planning/todos/pending/cr-01-indicaciones-url-validation.md
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-PATTERNS.md (seccion 9 — discrepancy: validacion ya implementada)
  </read_first>
  <action>
    PRIMERO verificar el estado real del codigo (discrepancy del pattern mapper): confirmar que `actualizarIndicacionesUrl` (catalogo-hc.service.ts ~766-779) YA contiene la validacion manual server-side: new URL() con reject-on-throw, protocolos solo http:/https: (rechaza javascript:/data:), maxLength 2048, null permitido para limpiar. NO agregar validacion net-new — ya existe y cubre el cr-01 completo.
    LUEGO limpiar SOLO el docstring enganoso en catalogo-hc.service.ts linea 759 que dice "T-53-11 @IsUrl validated in DTO": reformular para que apunte a la validacion manual real en el service (los decoradores @IsUrl del DTO son dead code: no hay ValidationPipe global). Redaccion sugerida: "Allows null to clear the URL. URL is validated server-side in this method (new URL + http/https-only + maxLength 2048); the DTO class-validator decorators do NOT run (no global ValidationPipe)."
    Opcional: agregar en el docstring del controller (catalogo-hc.controller.ts ~118-120) una nota de que la validacion de URL es server-side en el service.
    NO agregar un ValidationPipe global (Deferred idea — fuera de scope).
  </action>
  <verify>
    <automated>cd backend && grep -q "new URL(indicacionesUrl)" src/modules/catalogo-hc/catalogo-hc.service.ts && grep -q "protocol !== 'http:'" src/modules/catalogo-hc/catalogo-hc.service.ts && ! grep -q "@IsUrl validated in DTO" src/modules/catalogo-hc/catalogo-hc.service.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `catalogo-hc.service.ts` conserva la validacion existente (grep `new URL(indicacionesUrl)` y `protocol !== 'http:'` presentes)
    - Source: `catalogo-hc.service.ts` NO contiene la cadena `@IsUrl validated in DTO` (docstring enganoso removido)
    - Build: `cd backend && npm run build` exits 0
  </acceptance_criteria>
  <done>Validacion confirmada intacta (no net-new); docstring reformulado a la validacion server-side real; build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profesional (staff JWT) -> catalogo-hc API | `indicacionesUrl` es input no confiable que se persiste y se renderizara como `href` en Phase 62 |
| Prisma schema ops -> BD viva (pgBouncer) | DDL debe bypassear el pooler via directUrl para no corromper el pool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering (stored-XSS) | `actualizarIndicacionesUrl` (catalogo-hc.service.ts) | mitigate | Validacion server-side YA presente (766-779): `new URL()` reject-on-throw, solo http/https (rechaza `javascript:`/`data:`), maxLength 2048, null-allowed. Task 3 verifica su presencia y elimina el docstring que falsamente atribuye la proteccion al DTO decorator (dead code). |
| T-61-02 | Tampering | Migracion sobre `ConsentimientoFirmado` (registros forenses append-only) | mitigate | Solo `ALTER COLUMN ... DROP NOT NULL` — sin UPDATE/DELETE; columna preservada; filas v1.12 intactas (SC#4). Acceptance criteria valida ausencia de UPDATE/DELETE en el SQL. |
| T-61-03 | Denial of Service | Migracion sobre tabla caliente `Paciente` | accept | `ADD COLUMN` sin default y `DROP NOT NULL` son metadata-only en Postgres (no reescriben filas, no toman lock largo) — mismo perfil que el analog Phase 56. |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | No hay instalacion de paquetes en esta fase (edicion de schema + SQL + docstring). Sin superficie de supply-chain. |
</threat_model>

<verification>
- `npx prisma validate` pasa con ambos deltas.
- `npx prisma migrate status` sin drift; migracion `20260708000000_add_indicaciones_leidas` aplicada.
- Columna `Paciente.indicacionesLeidasAt` existe y es nullable en la BD viva.
- `ConsentimientoFirmado.indicacionesLeidasAt` es nullable; filas existentes conservan su valor.
- `cd backend && npm run build` exits 0; docstring `@IsUrl validated in DTO` removido.
</verification>

<success_criteria>
- SC#4 (ROADMAP): la migracion sigue el patron pgBouncer (`prisma diff + db execute + migrate resolve`) y NO rompe registros de consentimiento existentes.
- INDIC-03 (parcial): el campo `Paciente.indicacionesLeidasAt` existe en schema + BD viva (la persistencia via endpoint la completa 61-02).
- cr-01 cerrado: validacion server-side confirmada; docstring corregido.
</success_criteria>

<output>
Create `.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-01-SUMMARY.md` when done. Incluir la lista exacta de comandos prisma ejecutados (para trazabilidad del patron pgBouncer).
</output>
