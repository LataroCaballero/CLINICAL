---
phase: 61-backend-schema-decoupling-e-indicaciones
plan: 03
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - backend/src/modules/pacientes/crm-steps.helper.ts
  - backend/src/modules/pacientes/crm-steps.helper.spec.ts
  - backend/src/modules/pacientes/pacientes.service.ts
autonomous: true
requirements: [INDIC-04]
must_haves:
  truths:
    - "computePasosCrm marca indicacionesPreop como completo cuando Paciente.indicacionesLeidasAt != null (fuente primaria nueva)"
    - "Los dos fallbacks legacy (ConsentimientoFirmado.indicacionesLeidasAt, Paciente.indicacionesEnviadas) se conservan para no regresionar pacientes v1.12/pre-v1.12"
    - "getKanban selecciona Paciente.indicacionesLeidasAt y lo pasa a computePasosCrm"
    - "D-04: el paso indicacionesPreop deriva de un OR de 3 fuentes con Paciente.indicacionesLeidasAt como primaria nueva y las dos fuentes legacy como fallback de no-regresion"
    - "D-05: PacientePasosInput agrega indicacionesLeidasAt y el select de getKanban incluye Paciente.indicacionesLeidasAt pasandolo a computePasosCrm"
  artifacts:
    - path: "backend/src/modules/pacientes/crm-steps.helper.ts"
      provides: "PacientePasosInput.indicacionesLeidasAt + OR de 3 fuentes en Paso 5"
      contains: "indicacionesLeidasAt"
    - path: "backend/src/modules/pacientes/pacientes.service.ts"
      provides: "getKanban select + call arg del nuevo campo"
      contains: "indicacionesLeidasAt: true"
  key_links:
    - from: "pacientes.service.ts getKanban select"
      to: "computePasosCrm({ ..., indicacionesLeidasAt })"
      via: "select key -> call arg"
      pattern: "indicacionesLeidasAt: p.indicacionesLeidasAt"
    - from: "computePasosCrm Paso 5"
      to: "indicacionesPreop = p.indicacionesLeidasAt != null || ..."
      via: "OR de 3 fuentes (primaria nueva primero)"
      pattern: "p.indicacionesLeidasAt != null"
---

<objective>
Ajusta `computePasosCrm` para derivar el paso `indicacionesPreop` a partir del acuse en el perfil del paciente (`Paciente.indicacionesLeidasAt`) como fuente primaria, conservando las dos fuentes legacy como fallback de no-regresion (INDIC-04, D-04, SC#3), y hace que `getKanban` seleccione y pase el nuevo campo (D-05).

Purpose: Cerrado el desacople (61-02), el paso de indicaciones del board debe reflejar el acuse real del perfil del paciente, no el acto de firma del consentimiento. Sin backfill, los pacientes v1.12 ya firmados no deben regresionar: por eso el OR con las dos fuentes legacy.
Output: `computePasosCrm` con OR de 3 fuentes, `PacientePasosInput` extendido, `getKanban` seleccionando y pasando `indicacionesLeidasAt`, tests actualizados.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-CONTEXT.md
@.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-PATTERNS.md

<interfaces>
<!-- crm-steps.helper.ts current Paso 5 (extend, don't restructure) -->
PacientePasosInput (28-49) legacy fields analog (46-48):
  /** Legacy Paciente.indicacionesEnviadas boolean (fallback pre-v1.12) */
  indicacionesEnviadas?: boolean | null;

computePasosCrm Paso 5 current (94-99):
  const indicacionesPreopCompleto =
    consentimientosFirmados.some((c) => c.indicacionesLeidasAt != null) ||
    indicacionesEnviadasLegacy === true;

getKanban select current (pacientes.service.ts 659-661):
  consentimientoFirmado: true,
  indicacionesEnviadas: true,
  // <- add indicacionesLeidasAt: true here

computePasosCrm call current (741-748) passes:
  consentimientosFirmados, consentimientoFirmado, indicacionesEnviadas
  // <- add indicacionesLeidasAt: p.indicacionesLeidasAt
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender PacientePasosInput + Paso 5 con OR de 3 fuentes (INDIC-04, D-04, D-05)</name>
  <files>backend/src/modules/pacientes/crm-steps.helper.ts, backend/src/modules/pacientes/crm-steps.helper.spec.ts</files>
  <read_first>
    - backend/src/modules/pacientes/crm-steps.helper.ts (PacientePasosInput 28-49; Paso 5 94-99; header docstring 8-10)
    - backend/src/modules/pacientes/crm-steps.helper.spec.ts (patron emptyInput + describe por paso; Paso 5 tests)
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-PATTERNS.md (seccion 7)
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-CONTEXT.md (D-04, D-05)
  </read_first>
  <behavior>
    - indicacionesPreop es completo cuando `p.indicacionesLeidasAt != null` (fuente primaria nueva), aunque no haya consentimiento firmado ni flag legacy
    - Se conserva completo por fallback 1: algun `consentimientosFirmados[].indicacionesLeidasAt != null` (legacy v1.12)
    - Se conserva completo por fallback 2: `indicacionesEnviadas === true` (legacy pre-v1.12)
    - Es pendiente cuando las tres fuentes estan ausentes/null/false
    - Paso 4 (consentimiento) permanece SIN cambios
  </behavior>
  <action>
    En `PacientePasosInput` (crm-steps.helper.ts 28-49): agregar el campo `indicacionesLeidasAt?: Date | string | null;` con comentario "Paciente.indicacionesLeidasAt (v1.14) — fuente PRIMARIA nueva del paso indicaciones", junto a los otros campos legacy.
    En `computePasosCrm` Paso 5 (94-99): reescribir el OR anteponiendo la fuente primaria nueva, dejando las dos legacy como fallback (D-04, orden exacto):
    indicacionesPreopCompleto = (p.indicacionesLeidasAt != null) || consentimientosFirmados.some((c) => c.indicacionesLeidasAt != null) || (indicacionesEnviadasLegacy === true).
    Leer `p.indicacionesLeidasAt` inline o destructurarlo arriba junto a los otros `const ... = p.xxx ?? ...` (62-67), a eleccion. Actualizar los comentarios del Paso 5 y el header docstring (8-10) para nombrar la nueva fuente primaria v1.14. NO tocar el Paso 4.
    Tests (crm-steps.helper.spec.ts): agregar en el describe de Paso 5: (a) completo solo por `indicacionesLeidasAt` presente (sin consentimiento ni flag); (b) fallback v1.12 sigue completo; (c) fallback indicacionesEnviadas sigue completo; (d) pendiente con las 3 fuentes ausentes. Extender `emptyInput()` con `indicacionesLeidasAt: null` si aplica.
  </action>
  <verify>
    <automated>cd backend && grep -q "p.indicacionesLeidasAt != null" src/modules/pacientes/crm-steps.helper.ts && grep -q "indicacionesLeidasAt?: Date | string | null" src/modules/pacientes/crm-steps.helper.ts && npm test -- crm-steps.helper.spec</automated>
  </verify>
  <acceptance_criteria>
    - Source: `PacientePasosInput` contiene `indicacionesLeidasAt?: Date | string | null`
    - Source: Paso 5 evalua `p.indicacionesLeidasAt != null` como PRIMER operando del OR, seguido de los dos fallbacks legacy (orden D-04)
    - Source: el Paso 4 (consentimiento) no cambio
    - Behavior (test): completo por `indicacionesLeidasAt` solo; ambos fallbacks legacy siguen dando completo; pendiente con las 3 ausentes
    - Test: `cd backend && npm test -- crm-steps.helper.spec` pasa
  </acceptance_criteria>
  <done>OR de 3 fuentes con la nueva primaria primero; input extendido; tests de no-regresion verdes.</done>
</task>

<task type="auto">
  <name>Task 2: getKanban — seleccionar y pasar Paciente.indicacionesLeidasAt (D-05, SC#3)</name>
  <files>backend/src/modules/pacientes/pacientes.service.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.ts (getKanban select 627-678; call computePasosCrm 741-748)
    - backend/src/modules/pacientes/crm-steps.helper.ts (nueva firma de PacientePasosInput tras Task 1)
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-PATTERNS.md (seccion 8)
  </read_first>
  <action>
    En el `select` top-level de `Paciente` en getKanban (pacientes.service.ts ~659-661, junto a `consentimientoFirmado: true` e `indicacionesEnviadas: true`): agregar `indicacionesLeidasAt: true,` con comentario "v1.14 primary source, INDIC-04". Este campo existe en el client tras la migracion+generate de 61-01.
    En la llamada `computePasosCrm({ ... })` (~741-748): agregar el arg `indicacionesLeidasAt: p.indicacionesLeidasAt,` junto a los otros campos que ya se pasan.
    No cambiar nada mas de getKanban (es el unico consumidor de computePasosCrm — CONTEXT integration point).
  </action>
  <verify>
    <automated>cd backend && grep -q "indicacionesLeidasAt: true" src/modules/pacientes/pacientes.service.ts && grep -q "indicacionesLeidasAt: p.indicacionesLeidasAt" src/modules/pacientes/pacientes.service.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `pacientes.service.ts` getKanban select contiene `indicacionesLeidasAt: true`
    - Source: la llamada `computePasosCrm({...})` pasa `indicacionesLeidasAt: p.indicacionesLeidasAt`
    - Build: `cd backend && npm run build` exits 0 (el campo existe en el Prisma client regenerado por 61-01)
  </acceptance_criteria>
  <done>getKanban selecciona y pasa el nuevo campo; build verde con tipos del client regenerado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profesional (staff JWT) -> getKanban | getKanban ya filtra por profesionalId; el nuevo campo no cruza tenants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-08 | Information Disclosure | getKanban select del nuevo campo | accept | `indicacionesLeidasAt` es un timestamp del propio paciente del profesional autenticado; getKanban ya scopa por `profesionalId` en el `where`. Sin cruce de tenant; el campo no expone PII adicional. |
| T-61-09 | Tampering (logica derivada) | computePasosCrm Paso 5 OR | mitigate | Fuente primaria nueva antepuesta sin remover los fallbacks legacy => no-regresion de pacientes v1.12/pre-v1.12 garantizada por los tests de la Task 1 (D-04). Funcion pura, sin efectos secundarios. |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | No hay instalacion de paquetes en esta fase. Sin superficie de supply-chain. |
</threat_model>

<verification>
- `npm test -- crm-steps.helper.spec` pasa (nueva primaria + no-regresion de ambos fallbacks).
- `npm run build` exits 0 (getKanban compila contra el client regenerado en 61-01).
- Paso 5 usa `p.indicacionesLeidasAt != null` como fuente primaria; getKanban lo selecciona y pasa.
</verification>

<success_criteria>
- SC#3 (ROADMAP): `computePasosCrm` marca `indicacionesPreop` completo a partir de `Paciente.indicacionesLeidasAt != null`, no del acto de firma del consentimiento.
- INDIC-04 cubierto; no-regresion de pacientes legacy preservada (D-04, sin backfill).
</success_criteria>

<output>
Create `.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-03-SUMMARY.md` when done.
</output>
