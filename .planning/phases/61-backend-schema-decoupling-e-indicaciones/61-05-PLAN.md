---
phase: 61-backend-schema-decoupling-e-indicaciones
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - backend/src/modules/pacientes/pacientes.service.spec.ts
autonomous: true
requirements: [INDIC-04]
must_haves:
  truths:
    - "Existe un guard que corre automaticamente con `npm test` (Jest) y falla si se reintroduce `take` en el bloque select de consentimientosFirmados de getKanban — sin depender del comando `rg` ad hoc del 61-04-PLAN"
    - "El guard lee la fuente real de pacientes.service.ts (no un mock de Prisma) y por eso detecta la forma real del select, cerrando la limitacion del test de frontera 61-04 que mockea findMany y no ve el take:1"
    - "El guard NO da falso positivo por el comentario 'Sin take:1' que vive ARRIBA del bloque (extrae el bloque por balance de llaves desde la llave de apertura, no por ventana de texto que incluya el comentario)"
    - "El guard confirma tambien que el bloque conserva los 2 campos que computePasosCrm lee (firmadoAt, indicacionesLeidasAt) — sin ampliar el over-fetch (T-61-10)"
  artifacts:
    - path: "backend/src/modules/pacientes/pacientes.service.spec.ts"
      provides: "describe source-shape guard que parsea pacientes.service.ts y asserta ausencia de take en el select de consentimientosFirmados"
      contains: "consentimientosFirmados"
  key_links:
    - from: "pacientes.service.spec.ts source-shape guard"
      to: "backend/src/modules/pacientes/pacientes.service.ts (bloque consentimientosFirmados select)"
      via: "fs.readFileSync + brace-matching desde la llave de apertura del bloque"
      pattern: "consentimientosFirmados"
---

<objective>
Cierra el unico gap remanente de la Fase 61 (61-VERIFICATION.md, 2026-07-17): NO existe un guard durable y automatico contra la reintroduccion de `take:1` en el select `consentimientosFirmados` de `getKanban`. El codigo de produccion es correcto y las 4 Success Criteria del ROADMAP estan VERIFICADAS; el test de frontera de 61-04 (`pacientes.service.spec.ts` lineas 268-381) mockea `prisma.paciente.findMany` con un array hecho a mano, por lo que su `mockResolvedValue` es independiente del `select`/`take` real — reintroducir `take:1` deja los 12 tests en verde. El unico guard actual es un comando `rg` ad hoc dentro del `<verify>` de 61-04-PLAN.md, no cableado a `npm test`, lint ni CI.

Este plan agrega un test source-parsing (Jest, co-ubicado en el spec existente) que lee `pacientes.service.ts` con `fs.readFileSync`, aisla el bloque `consentimientosFirmados: { ... }` por balance de llaves y asserta que no contiene la clave `take`. Corre automaticamente con `npm test` y falla si alguien reintroduce la truncacion. No toca codigo de produccion.

Purpose: Convertir el guard efimero (`rg` manual) en un guard durable que Jest ejecuta en cada corrida, cerrando la limitacion disclosed del test mockeado de 61-04.
Output: Un describe block en `pacientes.service.spec.ts` que parsea la fuente y protege la forma del select contra regresiones de `take`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-CONTEXT.md
@.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-VERIFICATION.md
@.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-04-SUMMARY.md

<interfaces>
Estado actual — el executor NO debe explorar; usar esto directo.

Fuente a proteger (backend/src/modules/pacientes/pacientes.service.ts 674-681):
- Lineas 674-678: comentario multilinea que CONTIENE el texto "take:1" y "Sin take:1" (documentacion). Este comentario esta ARRIBA de la linea del bloque, NO dentro de las llaves del bloque.
- Linea 679: `consentimientosFirmados: {`
- Linea 680: `  select: { firmadoAt: true, indicacionesLeidasAt: true },`
- Linea 681: `},`
Estado correcto (verificado): el bloque NO tiene `take` ni `orderBy`; solo `select` con 2 campos.
IMPLICACION CLAVE: un guard que use una ventana de texto amplia (o un grep de todo el archivo) daria falso positivo por el comentario. El guard DEBE aislar el bloque desde su llave de apertura (la `{` de `consentimientosFirmados:`) por balance de llaves y assertar solo sobre ese substring — asi el comentario (que precede la clave) queda excluido. Este es exactamente el fallo de higiene que la nota "grep -c cuenta comentarios" advierte.

Patron del spec existente (backend/src/modules/pacientes/pacientes.service.spec.ts):
- El archivo ya usa `require('crypto')` inline (linea 32-34) — leer del filesystem / usar modulos node core es un patron aceptado aca; `fs` y `path` se importan igual.
- El TestingModule (lineas 47-74) NO es necesario para este guard: es un test estatico de fuente, no invoca el service. Puede vivir en su propio describe de nivel superior sin beforeEach.
- `mockPrisma.paciente` ya incluye `findMany: jest.fn()` (linea 52) — irrelevante para este guard, no lo toques.

Comando de test targeted (Jest ya es el runner del repo):
`npx jest --runTestsByPath src/modules/pacientes/pacientes.service.spec.ts`
Nota de entorno (documentada en 61-01/61-02/61-03/61-04): el full-suite jest puede colgar; correr SOLO este archivo con la invocacion targeted. Si el worktree no tiene node_modules, restaurar con `npm ci` contra el package-lock.json ya commiteado (patron 61-04, Rule 3, no es install de paquete nuevo).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard source-parsing contra reintroduccion de take en el select consentimientosFirmados (INDIC-04)</name>
  <files>backend/src/modules/pacientes/pacientes.service.spec.ts</files>
  <read_first>
    - backend/src/modules/pacientes/pacientes.service.ts (lineas 655-685: el bloque consentimientosFirmados select 679-681 y el comentario 674-678 que contiene "take:1" — confirmar que el comentario precede la llave de apertura del bloque, no vive dentro de el)
    - backend/src/modules/pacientes/pacientes.service.spec.ts (estructura completa: imports 11-16, uso inline de require('crypto') 31-35, describe/beforeEach 39-75, y el describe de frontera existente 'getKanban -> computePasosCrm boundary' ~268-381 — NO modificar ese describe, solo AGREGAR uno nuevo)
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-VERIFICATION.md (gap frontmatter: truth failed + missing items — la fuente de verdad de este gap-closure)
    - .planning/phases/61-backend-schema-decoupling-e-indicaciones/61-04-SUMMARY.md (secciones 'Decisions Made' y 'TDD Gate Compliance': por que el test mockeado no detecta take:1 — esta es la limitacion que este guard cierra)
  </read_first>
  <behavior>
    - GREEN (estado actual, produccion correcta): el guard PASA. El bloque `consentimientosFirmados: { ... }` aislado por balance de llaves NO matchea `/\btake\b/` ni `/\borderBy\b/`, y SI contiene `firmadoAt` e `indicacionesLeidasAt`.
    - RED (falsificacion — falla si se reintroduce la truncacion): si se agrega `take: 1` (o `orderBy: { firmadoAt: 'desc' }`) dentro del bloque en pacientes.service.ts, el guard FALLA con un assert claro sobre el substring del bloque.
    - Sin falso positivo por comentario: el guard NO debe fallar por el texto "take:1" del comentario de lineas 674-678, porque extrae el bloque DESDE la llave de apertura de `consentimientosFirmados:` y el comentario precede esa llave.
    - El guard es puramente estatico: lee la fuente con fs, no instancia PacientesService, no mockea Prisma, no depende de node_modules de runtime del service.
  </behavior>
  <action>
    Agregar al final de `backend/src/modules/pacientes/pacientes.service.spec.ts` un describe de nivel superior nuevo, nombre exacto: `getKanban consentimientosFirmados select — source-shape guard (take regression, INDIC-04)`. No usa beforeEach ni TestingModule (es un test estatico de fuente).

    Importar `readFileSync` de `fs` y `join` de `path` (agregar los imports arriba junto a los existentes; o usar `require` inline al estilo del `require('crypto')` ya presente — cualquiera consistente con el archivo).

    En el describe:
    1. Resolver la ruta del archivo fuente con `join(__dirname, 'pacientes.service.ts')` y leerlo con `readFileSync(path, 'utf8')` a un string `source`. (Bajo ts-jest, `__dirname` resuelve al directorio del spec, que es el mismo del service.)
    2. Localizar el indice de la subcadena literal `consentimientosFirmados:` en `source` (usar `source.indexOf('consentimientosFirmados:')`; assertar que es distinto de -1 para fallar ruidosamente si alguien renombra/borra el bloque).
    3. Desde ese indice, encontrar la primera llave de apertura `{` (la del objeto del relation) y recorrer hacia adelante llevando un contador de balance de llaves (`depth`): +1 en cada `{`, -1 en cada `}`, capturando el substring hasta que `depth` vuelva a 0. Ese substring `block` es el objeto `{ select: { firmadoAt: true, indicacionesLeidasAt: true } }` completo, SIN incluir el comentario previo. (Implementacion simple con un for sobre los chars; no hace falta un parser AST.)
    4. Asserts:
       - `expect(block).not.toMatch(/\btake\b/)` — falla si se reintroduce `take` (este es el assert de falsificacion; su mensaje debe dejar claro que la truncacion take:N rompe el Fallback 1 legacy v1.12 / WR-01 / SC#3 / INDIC-04).
       - `expect(block).not.toMatch(/\borderBy\b/)` — la truncacion original venia con `orderBy: { firmadoAt: 'desc' }`.
       - `expect(block).toMatch(/firmadoAt/)` y `expect(block).toMatch(/indicacionesLeidasAt/)` — confirma que sigue seleccionando exactamente los 2 campos que computePasosCrm lee (no se degrada el select; T-61-10 se mantiene: no se amplia a campos forenses).

    NO modificar el describe de frontera existente (`getKanban -> computePasosCrm boundary`), ni `mockPrisma`, ni ningun otro test. NO tocar codigo de produccion (`pacientes.service.ts` queda intacto).
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && rg -q "source-shape guard" src/modules/pacientes/pacientes.service.spec.ts && rg -q "readFileSync" src/modules/pacientes/pacientes.service.spec.ts && npx jest --runTestsByPath src/modules/pacientes/pacientes.service.spec.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Source: el spec contiene un describe nuevo con `source-shape guard` en el nombre y usa `readFileSync` + `join(__dirname, 'pacientes.service.ts')` para leer la fuente real (no un mock).
    - Source: el guard aisla el bloque por balance de llaves desde la llave de apertura de `consentimientosFirmados:` — verificado porque en el estado actual (comentario "Sin take:1" presente en lineas 674-678 de la fuente) el guard PASA (no da falso positivo por el comentario).
    - Behavior (GREEN): `npx jest --runTestsByPath src/modules/pacientes/pacientes.service.spec.ts` corre y el nuevo describe pasa junto al resto (suite en verde).
    - Behavior (FALSIFICACION — obligatorio): reintroducir temporalmente `take: 1, orderBy: { firmadoAt: 'desc' }` dentro del bloque `consentimientosFirmados` de pacientes.service.ts, re-correr `npx jest --runTestsByPath src/modules/pacientes/pacientes.service.spec.ts`, y CONFIRMAR que el nuevo guard FALLA (a diferencia del test de frontera de 61-04, que seguia en verde). Luego revertir el scratch edit y confirmar arbol limpio con `git diff --stat` (sin cambios en pacientes.service.ts). Registrar este resultado de falsificacion en el SUMMARY.
    - Source: no se modifico pacientes.service.ts (solo el spec); `git diff --name-only` tras el trabajo lista unicamente pacientes.service.spec.ts.
    - Entorno: si el worktree no tiene node_modules, se restauro con `npm ci` contra el package-lock.json commiteado (patron 61-04) — documentar en SUMMARY si aplica. Si jest full-suite cuelga, se corrio SOLO este archivo con la invocacion targeted.
  </acceptance_criteria>
  <done>Existe un guard Jest durable que corre con npm test, lee la fuente real de getKanban, falla si se reintroduce take/orderBy en el select consentimientosFirmados, y no da falso positivo por el comentario documental — cerrando la limitacion disclosed del test mockeado de 61-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Este plan NO agrega superficie de ataque nueva. El unico artefacto es un test estatico que lee un archivo fuente del propio repo con `fs.readFileSync` sobre una ruta derivada de `__dirname` (constante, no input de usuario). No hay entrada de usuario, no hay red, no hay DB, no hay cruce de tenants, no se instala ningun paquete. No toca ninguna ruta de codigo de produccion.

| Boundary | Description |
|----------|-------------|
| N/A (test-only) | El guard corre en el entorno de CI/local de tests; no expone ni consume datos en runtime de la app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-12 | Tampering (regresion silenciosa) | getKanban consentimientosFirmados select | mitigate | ESTE plan ES la mitigacion: un guard durable en `npm test` falla si se reintroduce `take`/`orderBy`, bloqueando la regresion de INDIC-04/SC#3 (Fallback 1 legacy v1.12) que hoy solo cubria un `rg` manual efimero. |
| T-61-10 | Information Disclosure | consentimientosFirmados select scope | mitigate | El guard asserta positivamente que el bloque sigue seleccionando solo `firmadoAt` e `indicacionesLeidasAt` — si un cambio amplia el select a campos forenses (hash/ip/userAgent/PDF), el intent de no-over-fetch permanece bajo revision del guard (los 2-campos-esperados). |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | No hay instalacion de paquetes nuevos en este gap. `fs`/`path` son modulos node core ya disponibles; un eventual `npm ci` restaura el lockfile ya commiteado (patron 61-04, no es paquete nuevo). Sin superficie supply-chain. |
</threat_model>

<verification>
- El nuevo describe `source-shape guard` corre bajo `npx jest --runTestsByPath src/modules/pacientes/pacientes.service.spec.ts` y pasa en el estado actual (produccion correcta).
- Falsificacion obligatoria: con `take: 1`/`orderBy` reintroducidos en el select de la fuente, el guard FALLA (contraste directo con el test de frontera de 61-04 que permanece verde). Revertido a arbol limpio.
- Sin falso positivo: el guard pasa pese al comentario "Sin take:1" presente arriba del bloque, porque aisla el bloque por balance de llaves.
- No se modifico codigo de produccion: `git diff --name-only` lista solo `pacientes.service.spec.ts`.
</verification>

<success_criteria>
- El gap unico de 61-VERIFICATION.md queda cerrado: existe un guard durable, automatico (Jest / `npm test`), que detecta la reintroduccion de `take` en el select `consentimientosFirmados` de `getKanban` — reemplazando la dependencia del `rg` ad hoc del 61-04-PLAN.
- INDIC-04 mantiene su estado SATISFIED con proteccion de regresion real (no solo el test mockeado, que no veia el select).
- Cero cambios en codigo de produccion; la Fase 61 queda lista para override/cierre.
</success_criteria>

<output>
Create `.planning/phases/61-backend-schema-decoupling-e-indicaciones/61-05-SUMMARY.md` when done.
</output>
