---
phase: 63-flujo-crm-autom-tico-backend
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts
  - backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts
  - backend/src/modules/historia-clinica/historia-clinica.service.ts
autonomous: true
requirements: [EMBUDO-09]
must_haves:
  truths:
    - "D-08: cuando una entrada HC del turno tiene `dto.tipo === 'tratamiento_en_consultorio'`, la entrada se persiste con `tipoEntrada = 'TRATAMIENTO'` de forma forzada (no dependiente de que el cliente mande tipoEntrada), igual que pre_quirurgico fuerza PREOPERATORIO"
    - "D-09: sólo se mueve a `flujo = TRATAMIENTO` si el paciente estaba `flujo = PENDIENTE` (o `null`, caso lead nuevo de EMBUDO-07); un candidato quirúrgico (`flujo = CIRUGIA`) que recibe un tratamiento en consultorio se queda en el board"
    - "D-10: al pasar a `flujo = TRATAMIENTO` el paciente se oculta del board (patrón v1.13, getKanban ya filtra flujo ∈ {CIRUGIA,null}) y se limpia `etapaCRM = null`"
    - "D-10: el tratamiento queda en la planilla con `HistoriaClinicaEntrada.fecha` (soporta fecha retroactiva) — sin cambios de schema"
  artifacts:
    - path: "backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts"
      provides: "resolverTipoEntrada (fuerza TRATAMIENTO/PREOPERATORIO) + resolverNuevoFlujo (TRATAMIENTO desde PENDIENTE|null)"
      contains: "TRATAMIENTO"
    - path: "backend/src/modules/historia-clinica/historia-clinica.service.ts"
      provides: "crearEntrada usa el tipoEntrada resuelto y limpia etapaCRM al mover a TRATAMIENTO"
      contains: "etapaCRM: null"
    - path: "backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts"
      provides: "Tests de forzado de tipoEntrada + transición de flujo"
  key_links:
    - from: "historia-clinica.service.ts::crearEntrada()"
      to: "resolverTipoEntrada + resolverNuevoFlujo"
      via: "tipoEntrada resuelto usado en create data y en la clasificación de flujo"
      pattern: "resolverTipoEntrada"
    - from: "crearEntrada() tx.paciente.update"
      to: "flujo=TRATAMIENTO + etapaCRM=null"
      via: "spread condicional cuando nuevoFlujo==='TRATAMIENTO'"
      pattern: "etapaCRM: null"
---

<objective>
EMBUDO-09: al cargar una entrada HC "Tratamiento en consultorio" (sola o junto a "Primera vez") el paciente sale del kanban (`flujo = TRATAMIENTO`, oculto por el patrón v1.13) y queda en la planilla de tratamientos con la fecha de la entrada.

Purpose: Que un tratamiento en consultorio reclasifique automáticamente al paciente fuera del embudo quirúrgico, sin acción manual, preservando al candidato quirúrgico en el board.
Output: helpers puros (forzado de tipoEntrada + transición de flujo) y el wiring en crearEntrada; tests unitarios.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/63-flujo-crm-autom-tico-backend/63-CONTEXT.md
@.planning/phases/63-flujo-crm-autom-tico-backend/63-PATTERNS.md

<interfaces>
enum FlujoPaciente { CIRUGIA, TRATAMIENTO, PENDIENTE }
enum TipoEntradaHC { CONSULTA_CIRUGIA, TRATAMIENTO, CONTROL, SEGUIMIENTO, PREOPERATORIO }
// dto.tipo (discriminador de UI): 'primera_vez' | 'pre_quirurgico' | 'tratamiento_en_consultorio' | ...

// historia-clinica.flujo.helpers.ts (archivo de helpers puros, doc: "Extracted to enable direct unit testing"):
//   resolverNuevoFlujo(tipoEntrada, flujoActual, esCirugia): 'CIRUGIA'|'TRATAMIENTO'|null
//     · TRATAMIENTO branch HOY: flujoActual === 'PENDIENTE' ? 'TRATAMIENTO' : null
//
// historia-clinica.service.ts::crearEntrada():
//   L248-251: tipoEntrada guardado = dto.tipo==='pre_quirurgico' ? 'PREOPERATORIO' : (dto.tipoEntrada ?? undefined)
//   L268-272: resolverNuevoFlujo(dto.tipoEntrada, pac?.flujo, turnoCtx?.esCirugia ?? false)  ← usa dto.tipoEntrada CRUDO
//   L274-283: tx.paciente.update spread condicional { diagnostico?, tratamiento?, flujo? }
//   Planilla: contenido.tratamientos + entrada.fecha ya persistidos (sin cambios).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helpers puros — forzar tipoEntrada y ampliar transición de flujo</name>
  <files>backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts, backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts</files>
  <read_first>
    - backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts (archivo completo — resolverNuevoFlujo)
    - backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts (patrón de tests unitarios del helper)
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-CONTEXT.md (D-08, D-09)
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-01-PLAN.md (D-03: lead nuevo queda flujo=null → la transición debe cubrir null)
  </read_first>
  <behavior>
    - resolverTipoEntrada: `('tratamiento_en_consultorio', undefined)` → `'TRATAMIENTO'`; `('pre_quirurgico', X)` → `'PREOPERATORIO'`; `('primera_vez', 'CONTROL')` → `'CONTROL'` (respeta dto.tipoEntrada); `('otro', undefined)` → `undefined`.
    - resolverNuevoFlujo: `('TRATAMIENTO', 'PENDIENTE', false)` → `'TRATAMIENTO'`; `('TRATAMIENTO', null, false)` → `'TRATAMIENTO'` (lead nuevo EMBUDO-07); `('TRATAMIENTO', 'CIRUGIA', false)` → `null` (candidato quirúrgico se queda, D-09); `('CONSULTA_CIRUGIA', 'PENDIENTE', false)` → `'CIRUGIA'` (sin regresión).
  </behavior>
  <action>
    Agregar en el archivo de helpers una función pura `resolverTipoEntrada(tipo: string | undefined, tipoEntradaDto: string | undefined): TipoEntradaHC | undefined` que centralice el forzado: `pre_quirurgico → 'PREOPERATORIO'` (comportamiento existente de L248-251), `tratamiento_en_consultorio → 'TRATAMIENTO'` (D-08 nuevo), en cualquier otro caso `tipoEntradaDto ?? undefined`. Modificar el branch TRATAMIENTO de `resolverNuevoFlujo` para aceptar también flujo `null`: `(flujoActual === 'PENDIENTE' || flujoActual == null) ? 'TRATAMIENTO' : null` (cubre el lead nuevo con flujo=null de EMBUDO-07/D-03; el candidato CIRUGIA sigue devolviendo null, D-09). NO tocar el branch CONSULTA_CIRUGIA ni la guarda `esCirugia`. Escribir en el spec los casos de `behavior` y verificar que los tests existentes del helper siguen verdes.
  </action>
  <verify>
    <automated>cd backend && npx jest src/modules/historia-clinica/historia-clinica.flujo.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "resolverTipoEntrada" backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts` devuelve la definición exportada.
    - `grep -n "flujoActual == null" backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts` aparece en el branch TRATAMIENTO.
    - Todos los casos de `behavior` pasan; los tests preexistentes del helper no regresionan.
  </acceptance_criteria>
  <done>Helpers puros cubren el forzado de tipoEntrada (TRATAMIENTO/PREOPERATORIO) y la transición TRATAMIENTO desde PENDIENTE|null, con CIRUGIA preservado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wiring en crearEntrada — usar tipoEntrada resuelto y limpiar etapaCRM (D-08/D-10)</name>
  <files>backend/src/modules/historia-clinica/historia-clinica.service.ts, backend/src/modules/historia-clinica/historia-clinica.flujo.spec.ts</files>
  <read_first>
    - backend/src/modules/historia-clinica/historia-clinica.service.ts (L240-283: create de la entrada, resolución de flujo y paciente.update)
    - backend/src/modules/historia-clinica/historia-clinica.flujo.helpers.ts (resolverTipoEntrada creado en Task 1)
    - backend/src/modules/pacientes/pacientes.service.ts (L1029-1054 updateFlujo — analog del pareo flujo + etapaCRM:null)
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-CONTEXT.md (D-08, D-09, D-10)
    - .planning/phases/63-flujo-crm-autom-tico-backend/63-PATTERNS.md (EMBUDO-09 D-08 bridge y D-10 coupling)
  </read_first>
  <behavior>
    - Test: al crear la entrada con `dto.tipo==='tratamiento_en_consultorio'`, el `tx.historiaClinicaEntrada.create` recibe `tipoEntrada === 'TRATAMIENTO'` (independiente de dto.tipoEntrada).
    - Test: para un paciente `flujo=PENDIENTE`, el `tx.paciente.update` recibe `data.flujo === 'TRATAMIENTO'` y `data.etapaCRM === null` (D-10).
    - Test: para un paciente `flujo=CIRUGIA`, NO se escribe flujo ni etapaCRM (candidato quirúrgico se queda, D-09).
    - Test (no regresión): `dto.tipo==='pre_quirurgico'` sigue guardando `tipoEntrada === 'PREOPERATORIO'`.
  </behavior>
  <action>
    En crearEntrada calcular una vez `const tipoEntradaResuelto = resolverTipoEntrada(dto.tipo, dto.tipoEntrada)` y usarlo en DOS lugares: (1) reemplazar el ternario de L248-251 por `tipoEntrada: tipoEntradaResuelto` en el `tx.historiaClinicaEntrada.create` (preserva pre_quirurgico→PREOPERATORIO y agrega tratamiento_en_consultorio→TRATAMIENTO, D-08); (2) pasar `tipoEntradaResuelto` (en vez de `dto.tipoEntrada` crudo) como primer argumento de `resolverNuevoFlujo` en L268 — así el forzado alcanza la clasificación de flujo. Extender el `tx.paciente.update` de L274-283 para que, cuando `nuevoFlujo === 'TRATAMIENTO'`, además incluya `etapaCRM: null` (D-10, espejo de updateFlujo "sale del board → limpia etapa"); usar spread condicional para no tocar etapaCRM en los demás casos. Importar `resolverTipoEntrada` del helper. NO agregar código para ocultar del board ni para la planilla: getKanban ya filtra flujo=TRATAMIENTO y `entrada.fecha`+`contenido.tratamientos` ya se persisten (D-10, sin cambios de schema). Añadir los tests de `behavior` (nivel helper + aserción del wiring; si testear crearEntrada requiere mock de Prisma, seguir el patrón de TestingModule de los specs existentes o cubrir el forzado vía resolverTipoEntrada).
  </action>
  <verify>
    <automated>cd backend && npx jest src/modules/historia-clinica</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "resolverTipoEntrada" backend/src/modules/historia-clinica/historia-clinica.service.ts` aparece en el import y en crearEntrada.
    - `grep -n "etapaCRM: null" backend/src/modules/historia-clinica/historia-clinica.service.ts` aparece en el update condicionado a `nuevoFlujo === 'TRATAMIENTO'`.
    - El primer argumento de `resolverNuevoFlujo` en crearEntrada es `tipoEntradaResuelto` (no `dto.tipoEntrada`).
    - Tests verdes; sin regresión de pre_quirurgico ni de candidatos CIRUGIA.
  </acceptance_criteria>
  <done>Una entrada "tratamiento en consultorio" fuerza tipoEntrada=TRATAMIENTO, mueve a flujo=TRATAMIENTO + etapaCRM=null sólo desde PENDIENTE|null, y deja al candidato quirúrgico intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente→API `crearEntrada` (HC) | `dto.tipo`/`dto.tipoEntrada` no confiables determinan una transición de estado CRM del Paciente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-08 | Tampering (state integrity) | Forzado de tipoEntrada en crearEntrada | mitigate | El forzado server-side (resolverTipoEntrada) evita que un cliente evada la reclasificación mandando un tipoEntrada distinto; la transición sólo aplica al `pacienteId` del contexto de la entrada. |
| T-63-09 | Tampering (state integrity) | Transición flujo/etapaCRM en la tx | mitigate | La condición D-09 (`resolverNuevoFlujo`) sólo mueve desde PENDIENTE|null y preserva CIRUGIA → no puede sacar del board a un candidato quirúrgico ni cambiar el flujo de otro paciente; escritura dentro de la $transaction existente (atómica). |
| T-63-10 | Information Disclosure | Planilla vía entrada.fecha | accept | Sin cambios de schema ni de exposición; se reutilizan campos ya persistidos y ya scopeados por paciente/profesional. |
| T-63-SC | Tampering | npm installs | accept | No se instalan paquetes (edición de archivos existentes). |
</threat_model>

<verification>
- `cd backend && npx jest src/modules/historia-clinica` → verde.
- `cd backend && npx tsc --noEmit -p tsconfig.build.json` → sin errores de tipo.
- Escenario manual (opcional): cargar entrada "tratamiento en consultorio" sobre un lead PENDIENTE → desaparece del board y queda en la planilla con la fecha de la entrada; sobre un candidato CIRUGIA → permanece en el board.
</verification>

<success_criteria>
- Una entrada "tratamiento en consultorio" fuerza tipoEntrada=TRATAMIENTO (D-08).
- El paciente pasa a flujo=TRATAMIENTO + etapaCRM=null sólo desde PENDIENTE|null (D-09/D-10), oculto del board por el patrón v1.13 y presente en la planilla con la fecha de la entrada (Success Criterion #3).
- El candidato quirúrgico (flujo=CIRUGIA) no se mueve.
</success_criteria>

<output>
Create `.planning/phases/63-flujo-crm-autom-tico-backend/63-03-SUMMARY.md` when done.
</output>
