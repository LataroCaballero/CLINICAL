---
phase: 66-correcciones-de-ui-de-historia-cl-nica-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/patient/PatientDrawer/views/HCEntryContent.tsx
  - frontend/src/components/patient/PatientDrawer/views/HistoriaClinica.tsx
autonomous: false
requirements: [HCUI-01, HCUI-02]

must_haves:
  truths:
    - "HCUI-02: Al abrir el detalle de una entrada 'Pre-quirúrgico' se ven antecedentes, alergias, medicación, estudios complementarios, consentimiento informado y comentario guardados (no una tarjeta vacía)"
    - "HCUI-02 (D-05): Los campos vacíos del pre-quirúrgico se ocultan (no se muestra '—' ni secciones vacías)"
    - "HCUI-02: El título de una entrada pre_quirurgico se muestra como 'Pre-quirúrgico', no como el string crudo 'pre_quirurgico'"
    - "HCUI-01 (D-01): La vista HistoriaClinica ya no muestra el dropdown 'Nueva entrada'; el único punto de entrada de creación es el botón '+ Nueva HC' del header de PatientDrawer (wizard HCCreatorDialog)"
    - "HCUI-01 (D-02): El formulario de texto libre y el selector de plantilla fueron eliminados de HistoriaClinica.tsx; las entradas viejas de texto libre ya guardadas siguen renderizándose igual"
    - "El proyecto compila (tsc --noEmit) y pasa lint sin imports/variables huérfanos"
  artifacts:
    - path: "frontend/src/components/patient/PatientDrawer/views/HCEntryContent.tsx"
      provides: "Interface ContenidoPreQuirurgico + rama de render pre_quirurgico en HCEntryChips y HCEntryFullContent"
      contains: "pre_quirurgico"
    - path: "frontend/src/components/patient/PatientDrawer/views/HistoriaClinica.tsx"
      provides: "Dead-code removido (dropdown/form/modal) + TIPO_LABELS.pre_quirurgico"
      contains: "pre_quirurgico"
  key_links:
    - from: "entrada.contenido.tipo === 'pre_quirurgico'"
      to: "nueva rama de render en HCEntryContent.tsx"
      via: "condicional tipo en HCEntryChips y HCEntryFullContent"
      pattern: "c\\.tipo === \"pre_quirurgico\""
    - from: "PatientDrawer '+ Nueva HC' button"
      to: "HCCreatorDialog -> HCCreatorForm"
      via: "hcDialogOpen (punto de entrada superviviente, no se toca)"
      pattern: "HCCreatorDialog"
---

<objective>
Corregir dos fricciones de UI de Historia Clínica en la ficha del paciente (PatientDrawer), sin rediseñar componentes:

- **HCUI-02:** El detalle de una entrada "Pre-quirúrgico" renderiza el JSONB persistido (antecedentes, alergias, medicación, estudios complementarios, consentimiento informado, comentario) en vez de mostrarse vacío. Incluye título legible en `TIPO_LABELS`.
- **HCUI-01:** Eliminar el dropdown "Nueva entrada" (texto libre / usar plantilla) y su código muerto asociado de la vista `HistoriaClinica`, dejando como único punto de creación el botón "+ Nueva HC" del header de `PatientDrawer` (wizard `HCCreatorDialog` → `HCCreatorForm`, ya cableado).

Purpose: Cargar y revisar HC desde la ficha es consistente con LiveTurno y no deja información sin mostrar.
Output: HCEntryContent.tsx con nueva rama pre_quirurgico; HistoriaClinica.tsx sin el camino viejo de creación y con título legible.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-correcciones-de-ui-de-historia-cl-nica-frontend/66-CONTEXT.md
@.planning/phases/66-correcciones-de-ui-de-historia-cl-nica-frontend/66-PATTERNS.md

<interfaces>
<!-- Forma REAL persistida del contenido pre_quirurgico (fuente de verdad: backend). -->
<!-- NO es el DTO de salida de HCCreatorForm. Los render reciben esta forma via entrada.contenido. -->

From backend/src/modules/historia-clinica/historia-clinica.service.ts:110-120 (construcción del contenido persistido):
- tipo: "pre_quirurgico"
- antecedentes: string[]           (dto.antecedentes ?? [])
- alergias: string[]               (dto.alergias ?? [])
- medicacion: string[]             (dto.medicacion ?? [])
- estudiosComplementarios: { laboratorio: boolean; ecg: boolean; imagenes: string[] } | null
- consentimientoInformadoAt: string (ISO) | null   (null = "No"; timestamp = "Sí")
- comentario: string | null
NO existe campo `zonas` en el contenido persistido de pre_quirurgico. NO renderizar sección de zonas.

Interface a AGREGAR en HCEntryContent.tsx (junto a ContenidoPrimeraVez, línea ~15-25):
  export interface ContenidoPreQuirurgico {
    tipo: "pre_quirurgico";
    antecedentes?: string[];
    alergias?: string[];
    medicacion?: string[];
    estudiosComplementarios?: { laboratorio: boolean; ecg: boolean; imagenes: string[] } | null;
    consentimientoInformadoAt?: string | null;
    comentario?: string | null;
  }
E incluirla en el union ContenidoEntrada (HCEntryContent.tsx:32-36).

From frontend/src/app/dashboard/pacientes/components/PatientDrawer.tsx (punto de entrada SUPERVIVIENTE, NO tocar):
- const [hcDialogOpen, setHcDialogOpen] = useState(false);
- Botón "+ Nueva HC" → setHcDialogOpen(true)
- <HCCreatorDialog open={hcDialogOpen} ... /> → HCCreatorForm (wizard nuevo, showDatePicker=true)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Render de detalle Pre-quirúrgico (HCUI-02) + título legible</name>
  <files>
    frontend/src/components/patient/PatientDrawer/views/HCEntryContent.tsx
    frontend/src/components/patient/PatientDrawer/views/HistoriaClinica.tsx
  </files>
  <read_first>
    - frontend/src/components/patient/PatientDrawer/views/HCEntryContent.tsx (el archivo a modificar; leer las ramas `primera_vez` de HCEntryChips ~línea 55 y de HCEntryFullContent ~línea 174 como patrón de estilo; leer el union ContenidoEntrada líneas 32-36 y la interface ContenidoPrimeraVez líneas 15-25)
    - frontend/src/components/patient/PatientDrawer/views/HistoriaClinica.tsx (solo TIPO_LABELS ~línea 405 y getTituloEntrada; NO tocar dead code en esta task — eso es Task 2)
    - backend/src/modules/historia-clinica/historia-clinica.service.ts (líneas 100-130: forma persistida real de `contenido` para pre_quirurgico — fuente de verdad de los campos)
    - .planning/phases/66-correcciones-de-ui-de-historia-cl-nica-frontend/66-PATTERNS.md (sección "CRITICAL: Data-shape correction" y "Pattern Assignments" para HCEntryContent.tsx)
  </read_first>
  <action>
    Agregar la interface `ContenidoPreQuirurgico` (forma persistida, ver `<interfaces>`) junto a `ContenidoPrimeraVez` en HCEntryContent.tsx, e incluirla en el union `ContenidoEntrada` (líneas 32-36).

    En `HCEntryChips` (variante preview/tarjeta): insertar una rama nueva `if (c.tipo === "pre_quirurgico")` DESPUÉS del bloque `primera_vez` y ANTES del comentario `// ── Texto libre ──` (~línea 131). Renderizar `antecedentes`, `alergias`, `medicacion` como chips `<Badge variant="outline" className="text-xs">` por item (mismo patrón que `z.diagnosticos.map`), cada grupo dentro de `flex flex-wrap gap-1 items-center`, todo en un wrapper `space-y-1.5`. Renderizar `comentario` con el mismo `<p className="text-xs text-muted-foreground whitespace-pre-line pt-1">` que usa la rama primera_vez. Consentimiento: chip legible `Sí` si `consentimientoInformadoAt` es truthy. Si no hay ningún dato, devolver el mismo fallback `(sin contenido)` que el resto del archivo.

    En `HCEntryFullContent` (variante detalle/modal): insertar rama `if (c.tipo === "pre_quirurgico")` DESPUÉS del bloque `primera_vez` y ANTES del comentario `// ── Texto libre ──` (~línea 358). Secciones con header `<h4 className="text-sm font-semibold">`: "Antecedentes", "Alergias", "Medicación" (chips outline por item); "Estudios complementarios" (bloque `p-3 bg-muted/40 rounded-lg`: Laboratorio Sí/No, ECG Sí/No, `imagenes` como chips si length>0); "Consentimiento informado" (Badge `Sí`/`No` según truthiness de `consentimientoInformadoAt`); "Comentario" (mismo `<p className="text-sm whitespace-pre-line p-3 bg-muted/40 rounded-lg">` que primera_vez). Estilo consistente con la rama `primera_vez` (Claude's Discretion sobre orden fino/spacing/íconos).

    Aplicar D-05 (ocultar vacíos): cada sección se renderiza SOLO con guard `.length > 0` para arrays y truthy para escalares/objetos (`estudiosComplementarios`, `consentimientoInformadoAt`, `comentario`). NO usar placeholder `—` (ese patrón vive solo en la rama legacy de primera_vez y NO debe copiarse).

    NO renderizar ninguna sección de `zonas`: el contenido persistido de pre_quirurgico no tiene `zonas` (desviación fundamentada de D-04, ver PATTERNS.md). No usar `<any>` gratuito: castear a `ContenidoPreQuirurgico` para acceso tipado.

    En HistoriaClinica.tsx: agregar `pre_quirurgico: "Pre-quirúrgico"` al objeto `TIPO_LABELS` (~línea 405), para que `getTituloEntrada` devuelva un título legible en vez del string crudo. NO tocar el dead code en esta task.
  </action>
  <verify>
    <automated>cd frontend && grep -c 'c\.tipo === "pre_quirurgico"' src/components/patient/PatientDrawer/views/HCEntryContent.tsx</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'c\.tipo === "pre_quirurgico"' HCEntryContent.tsx` devuelve `2` (una rama en HCEntryChips, otra en HCEntryFullContent).
    - Source: `grep -c 'interface ContenidoPreQuirurgico' HCEntryContent.tsx` devuelve `1`; `ContenidoPreQuirurgico` aparece en el union `ContenidoEntrada`.
    - Source: NO existe render de `zonas` dentro de ninguna rama `pre_quirurgico` (`grep` de `zonas` en las ramas nuevas = 0).
    - Source: `grep -c 'pre_quirurgico: "Pre-quirúrgico"' HistoriaClinica.tsx` devuelve `1`.
    - Behavior: cada campo se renderiza detrás de un guard de longitud/truthiness (sin `—` placeholder en las ramas nuevas).
    - Behavior: `consentimientoInformadoAt` se muestra como `Sí` (truthy) / `No` (null), no como valor crudo ISO.
    - CLI: `npx tsc --noEmit` desde `frontend/` termina con exit code 0.
  </acceptance_criteria>
  <done>
    HCEntryContent.tsx tiene la interface ContenidoPreQuirurgico en el union y dos ramas de render pre_quirurgico (chips + detalle) siguiendo el estilo de primera_vez, sin sección de zonas, ocultando vacíos. HistoriaClinica.tsx tiene el título legible en TIPO_LABELS. tsc pasa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Eliminar dropdown viejo y código muerto de HistoriaClinica (HCUI-01/D-01/D-02)</name>
  <files>
    frontend/src/components/patient/PatientDrawer/views/HistoriaClinica.tsx
  </files>
  <read_first>
    - frontend/src/components/patient/PatientDrawer/views/HistoriaClinica.tsx (archivo a modificar; identificar el bloque DropdownMenu "Nueva entrada" ~195-215, la Card `showForm` de texto libre ~262-281, el `<Dialog>` `showTemplateSelector` ~312-343, los estados/handlers `showForm`/`contenido`/`showTemplateSelector`/`handleGuardar`/`handleSelectTemplate`, y los imports/hooks asociados)
    - frontend/src/app/dashboard/pacientes/components/PatientDrawer.tsx (confirmar que el botón "+ Nueva HC" → HCCreatorDialog es el punto de entrada superviviente; NO modificar)
    - frontend/src/components/patient/PatientDrawer/views/HCCreatorDialog.tsx (wrapper del wizard ya cableado; NO modificar, solo verificar)
    - .planning/phases/66-correcciones-de-ui-de-historia-cl-nica-frontend/66-PATTERNS.md (sección de HistoriaClinica.tsx: lista exacta de qué borrar y qué CONSERVAR)
  </read_first>
  <action>
    Borrar de HistoriaClinica.tsx (D-01/D-02):
    1. El bloque `<DropdownMenu>` "Nueva entrada" del header (~195-215) con sus items "Texto libre" (`onClick setShowForm(true)`) y "Usar plantilla" (`onClick setShowTemplateSelector(true)`). No reemplazar por nada — la acción de crear vive en el botón "+ Nueva HC" de PatientDrawer.tsx.
    2. La Card del formulario de texto libre guardada por `showForm` (~262-281): `<Textarea>` con `value={contenido}` y los botones Cancelar/Guardar.
    3. El `<Dialog open={showTemplateSelector}>` selector de plantilla (~312-343).

    Borrar estado/handlers huérfanos como consecuencia: `showForm`/`setShowForm` (línea 73), `contenido`/`setContenido` (74), `showTemplateSelector`/`setShowTemplateSelector` (75), `handleGuardar` (94-100), `handleSelectTemplate` (102-121).

    Borrar hooks/imports que quedan huérfanos tras lo anterior (confirmar sin otros usos con grep antes de borrar): `useAvailableHCTemplates` import + destructure `templates` (línea 51, 83); `createEntry`/`useCreateHistoriaClinicaEntry` (50, 84) — solo lo usa `handleGuardar`; `createTemplateEntry`/`useCreateHCEntry` (53, 85) — solo lo usa `handleSelectTemplate`. Imports de icono `Plus` y `ChevronDown` (34, 37) — solo usados en el trigger del dropdown borrado. Import `Textarea` (8) — solo usado en la Card borrada. Los cuatro imports de `DropdownMenu*` (18-21) — solo usados en el bloque borrado.

    CONSERVAR (NO borrar, siguen en uso): `FileCode` (usado en badges isTemplateBased ~372/448); el import `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle` (usado por el "ENTRY DETAIL MODAL" ~367-383 — solo se borra la instancia `<Dialog>` del template-selector, no el import); toda la lógica de borradores/wizard: `wizardEntryId`, `wizardSchema`, `handleContinueDraft`, `handleCloseWizard`, `handleFinalizeWizard`, `useHCDraftEntries`, `deleteEntry`/`useDeleteHCEntry`, `DynamicTemplateWizard`; el listado de entradas (`EntryCard`→`ExpandedEntryContent`→`HCEntryChips`/`HCEntryFullContent`) y `TIPO_LABELS`/`getTituloEntrada` (con la adición de Task 1).

    Las entradas viejas de texto libre ya guardadas deben seguir renderizándose igual (no se toca la lógica de listado/detalle).
  </action>
  <verify>
    <automated>cd frontend && grep -c -e 'setShowForm' -e 'setShowTemplateSelector' -e 'handleGuardar' -e 'handleSelectTemplate' src/components/patient/PatientDrawer/views/HistoriaClinica.tsx</automated>
    <automated>cd frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c -e 'setShowForm' -e 'setShowTemplateSelector' -e 'handleGuardar' -e 'handleSelectTemplate' HistoriaClinica.tsx` devuelve `0` (ninguna referencia sobrevive).
    - Source: `grep -c 'Nueva entrada' HistoriaClinica.tsx` devuelve `0` (dropdown eliminado).
    - Source: `grep -c 'DropdownMenu' HistoriaClinica.tsx` devuelve `0` (import y uso eliminados).
    - Source: el import y el uso de `Textarea` fueron eliminados de HistoriaClinica.tsx (`grep -c Textarea` = 0).
    - Behavior: PatientDrawer.tsx y HCCreatorDialog.tsx NO fueron modificados (siguen siendo el punto de entrada de creación).
    - Behavior: el listado y el modal de detalle (`Dialog` del ENTRY DETAIL MODAL, `HCEntryFullContent`) siguen presentes y funcionales; `FileCode` sigue importado y usado.
    - CLI: `npx tsc --noEmit` exit 0 y `npm run lint` sin errores (sin imports/variables huérfanos).
  </acceptance_criteria>
  <done>
    HistoriaClinica.tsx ya no contiene el dropdown "Nueva entrada", el formulario de texto libre ni el selector de plantilla, ni sus estados/handlers/imports huérfanos. El wizard de PatientDrawer queda como único camino de creación. tsc + lint pasan limpios.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    HCUI-01: eliminado el dropdown viejo "Nueva entrada" de la vista HistoriaClinica; el único camino de creación es "+ Nueva HC" (wizard). HCUI-02: nueva rama de render para entradas Pre-quirúrgico en la tarjeta (chips) y en el detalle (modal), con título legible.
  </what-built>
  <how-to-verify>
    1. `cd frontend && npm run dev`, abrir el dashboard y una ficha de paciente que tenga (o crear con "+ Nueva HC" → plantilla Pre-quirúrgico) al menos una entrada de HC "Pre-quirúrgico".
    2. En la vista Historia Clínica del PatientDrawer, confirmar que NO aparece el botón/dropdown "Nueva entrada" en el header de la vista; el único botón de creación es "+ Nueva HC" en el header del drawer.
    3. Click en "+ Nueva HC" → confirmar que abre el wizard nuevo (HCCreatorDialog/HCCreatorForm con selector de fecha), no un textarea de texto libre.
    4. En el listado, abrir el detalle de la entrada "Pre-quirúrgico": confirmar título "Pre-quirúrgico" y que se ven los campos guardados (antecedentes, alergias, medicación, estudios complementarios, consentimiento informado como Sí/No, comentario). Los campos vacíos NO deben mostrar "—" ni secciones vacías.
    5. Abrir una entrada vieja de texto libre existente: confirmar que sigue renderizándose igual que antes.
  </how-to-verify>
  <resume-signal>Escribí "approved" o describí los problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Este phase es un cambio 100% frontend: elimina rutas de UI muertas (HCUI-01) y agrega una rama de render read-only sobre datos JSONB ya persistidos (HCUI-02). No introduce nuevos inputs, endpoints, superficie de auth, ni flujos de datos. No cruza ninguna trust boundary nueva.

| Boundary | Description |
|----------|-------------|
| (ninguna nueva) | Los datos `entrada.contenido` ya se obtienen y renderizan por el mismo pipeline de listado de HC existente; la rama nueva solo formatea campos ya sanitizados/mostrados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Tampering | HCEntryFullContent/HCEntryChips (render de contenido.pre_quirurgico) | accept | Datos ya persistidos y provistos por el mismo pipeline read-only que primera_vez; React escapa el contenido por defecto (sin `dangerouslySetInnerHTML`). Sin nueva superficie de ataque |
| T-66-02 | Information Disclosure | Detalle de HC en PatientDrawer | accept | El acceso a la ficha del paciente y a su HC ya está gateado por la auth/roles existentes; este phase no cambia qué datos se cargan, solo formatea campos ya devueltos por el backend |
| T-66-SC | Tampering | npm/pip installs | n/a | No hay instalación de paquetes en este phase (solo edición de componentes existentes); sin gate de legitimidad requerido |

Block-on severity: high — none present. No high/critical threats introducidos.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` → exit 0 (tipos intactos).
- `cd frontend && npm run lint` → sin errores (sin imports/variables huérfanos tras el borrado de HCUI-01).
- `grep -c 'c\.tipo === "pre_quirurgico"' HCEntryContent.tsx` → 2.
- `grep -c -e 'setShowForm' -e 'setShowTemplateSelector' -e 'handleGuardar' -e 'handleSelectTemplate' HistoriaClinica.tsx` → 0.
- Checkpoint humano: wizard como único camino de creación + detalle Pre-quirúrgico completo y legible + entradas viejas intactas.
</verification>

<success_criteria>
- HCUI-01: Agregar una nueva entrada de HC desde el PatientDrawer abre el wizard nuevo (HCCreatorForm/HCCreatorDialog); el dropdown viejo y su código muerto ya no existen.
- HCUI-02: El detalle de una entrada "Pre-quirúrgico" muestra todos los campos guardados (antecedentes, alergias, medicación, estudios complementarios, consentimiento informado, comentario), ocultando vacíos, con título legible.
- Tipos, lint y las entradas de HC existentes no se rompen.
</success_criteria>

<output>
Create `.planning/phases/66-correcciones-de-ui-de-historia-cl-nica-frontend/66-01-SUMMARY.md` when done
</output>
