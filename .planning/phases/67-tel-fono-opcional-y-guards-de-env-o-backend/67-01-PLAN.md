---
phase: 67-tel-fono-opcional-y-guards-de-env-o-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/schema.prisma
  - backend/src/prisma/migrations/
  - backend/src/modules/pacientes/dto/create-paciente.dto.ts
  - backend/src/modules/pacientes/dto/paciente-lista.dto.ts
  - backend/src/common/types/paciente-suggest.type.ts
  - backend/src/modules/reportes/types/reportes.types.ts
  - backend/src/modules/pacientes/pacientes.service.ts
  - backend/src/modules/reportes/services/reportes-financieros.service.ts
autonomous: true
requirements: [TEL-01]

must_haves:
  truths:
    - "El schema Prisma declara Paciente.telefono como nullable (String?), igual que su hermano telefonoAlternativo"
    - "CreatePacienteDto acepta un payload sin telefono sin fallar la validación de class-validator"
    - "La migración generada relaja el NOT NULL de Paciente.telefono sin borrar ni reescribir teléfonos existentes (SC#2)"
    - "El cliente Prisma regenerado tipa Paciente.telefono como string | null"
    - "npm run build vuelve a salir 0 tras ensanchar las 4 declaraciones de tipo y los 2 genéricos de $queryRaw que declaraban telefono: string"
  artifacts:
    - path: "backend/src/prisma/schema.prisma"
      provides: "Paciente.telefono nullable"
      contains: "telefono                          String?"
    - path: "backend/src/modules/pacientes/dto/create-paciente.dto.ts"
      provides: "telefono opcional en el DTO de alta"
      contains: "telefono?: string"
    - path: "backend/src/prisma/migrations/"
      provides: "Migración SQL que hace DROP NOT NULL sobre Paciente.telefono"
      contains: "DROP NOT NULL"
    - path: "backend/src/common/types/paciente-suggest.type.ts"
      provides: "PacienteSuggest.telefono nullable"
      contains: "telefono: string | null"
    - path: "backend/src/modules/reportes/types/reportes.types.ts"
      provides: "CuentaPorCobrar.telefono y CuentaMorosa.telefono nullables"
      contains: "telefono: string | null"
  key_links:
    - from: "backend/src/prisma/schema.prisma"
      to: "cliente Prisma generado"
      via: "npx prisma migrate dev seguido de npx prisma generate"
      pattern: "telefono.*String\\?"
    - from: "backend/src/modules/pacientes/pacientes.service.ts"
      to: "backend/src/modules/pacientes/dto/paciente-lista.dto.ts"
      via: "obtenerListaPacientes mapea p.telefono (ahora nullable) al DTO"
      pattern: "telefono: string \\| null"
---

<objective>
Hacer `Paciente.telefono` nullable de punta a punta: schema Prisma, migración aplicada a la base viva, DTO de alta, y todas las declaraciones de tipo TypeScript que hoy mienten diciendo `telefono: string`. Al terminar este plan el repo vuelve a compilar con `telefono` nullable — es el cimiento de los otros cuatro planes de la fase.

Purpose: TEL-01 y los success criteria #1 y #2 del ROADMAP. Sin la migración aplicada, `npm run build` pasaría igual (los tipos salen del cliente generado, no de la base) y la verificación daría un falso positivo.
Output: schema + migración + DTO + 4 declaraciones de tipo y 2 genéricos de `$queryRaw` ensanchados; `npm run build` en verde.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-CONTEXT.md
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Estado actual de los tipos que este plan ensancha. El ejecutor no necesita explorar el codebase para encontrarlos. -->

backend/src/common/types/paciente-suggest.type.ts (archivo completo, 8 líneas):
  export type PacienteSuggest = { id; nombreCompleto; dni; telefono: string; fotoUrl: string | null; score: number }
  -> `telefono` pasa a `string | null`

backend/src/modules/pacientes/dto/paciente-lista.dto.ts línea 6:
  export class PacienteListaDto { ... telefono: string; ... }
  -> `telefono` pasa a `string | null`

backend/src/modules/reportes/types/reportes.types.ts:
  línea 194 `export interface CuentaPorCobrar { ... }` con `telefono: string;` en la línea 197
  línea 211 `export interface CuentaMorosa   { ... }` con `telefono: string;` en la línea 214
  -> ambos `telefono` pasan a `string | null`

Genéricos de `$queryRaw` que declaran `telefono: string` y quedan stale:
  backend/src/modules/pacientes/pacientes.service.ts:347-355 (`suggest()`)
  backend/src/modules/reportes/services/reportes-financieros.service.ts:551-558 (`getMorosidad()`)

Sitio ya correcto que sirve de referencia (NO tocar):
  backend/src/modules/presupuestos/presupuesto-pdf.service.ts:15-19 -> `telefono?: string | null`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Schema Prisma nullable + @IsOptional() en CreatePacienteDto</name>
  <files>backend/src/prisma/schema.prisma, backend/src/modules/pacientes/dto/create-paciente.dto.ts</files>
  <read_first>
    - backend/src/prisma/schema.prisma (líneas 150-235: bloque `model Paciente`, con `telefono` en :158, `telefonoAlternativo String?` en :159 y el índice GIN trgm `idx_paciente_telefono_trgm` en :231)
    - backend/src/modules/pacientes/dto/create-paciente.dto.ts (archivo completo: imports en 1-9 con `IsOptional` ya importado, `telefono` en 23-24, `telefonoAlternativo` en 26-28 como plantilla exacta)
    - backend/src/modules/pacientes/dto/update-paciente.dto.ts (confirmar que es `PartialType(CreatePacienteDto)` y por lo tanto NO requiere edición propia)
    - .planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-PATTERNS.md (secciones "schema.prisma" y "create-paciente.dto.ts")
  </read_first>
  <action>
    En `backend/src/prisma/schema.prisma` línea 158, cambiar `telefono String` por `telefono String?`, conservando la alineación de columnas del bloque `model Paciente` (el campo queda idéntico en forma a `telefonoAlternativo String?` de la línea siguiente). NO tocar el índice `@@index([telefono(ops: raw("gin_trgm_ops"))], map: "idx_paciente_telefono_trgm", type: Gin)` de la línea 231: un índice GIN sobre columna nullable es válido en Postgres — las filas con NULL simplemente no se indexan.

    En `backend/src/modules/pacientes/dto/create-paciente.dto.ts` líneas 23-24, el campo pasa de la forma de dos líneas `@IsString()` + `telefono: string;` a la forma de tres líneas `@IsOptional()` + `@IsString()` + `telefono?: string;`, copiando literalmente el shape del campo hermano `telefonoAlternativo` (líneas 26-28). `IsOptional` ya está importado en el bloque de imports (líneas 1-9) — no agregar imports nuevos.

    NO editar `update-paciente.dto.ts`: hereda la opcionalidad vía `PartialType(CreatePacienteDto)`.
    NO editar todavía ningún tipo TypeScript de lectura — eso es la Task 3 de este mismo plan.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && grep -E 'telefono +String\?$' src/prisma/schema.prisma && npx prisma validate 2>&1 | grep -E "error|The schema at" && grep -A2 '@IsOptional()' src/modules/pacientes/dto/create-paciente.dto.ts | grep 'telefono?: string;'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `backend/src/prisma/schema.prisma` contiene exactamente una línea que matchea `telefono +String\?$` dentro de `model Paciente`
    - Source: `backend/src/prisma/schema.prisma` sigue conteniendo `map: "idx_paciente_telefono_trgm"` (índice intacto)
    - Source: `backend/src/modules/pacientes/dto/create-paciente.dto.ts` contiene `telefono?: string;` inmediatamente precedido por `@IsString()` y, antes, `@IsOptional()`
    - Source: `git diff --stat` NO lista `backend/src/modules/pacientes/dto/update-paciente.dto.ts`
    - CLI: `cd backend && npx prisma validate` sale 0
  </acceptance_criteria>
  <done>El schema declara `telefono String?`, el DTO de alta lo acepta como opcional, `prisma validate` pasa.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generar y aplicar la migración Prisma a la base viva</name>
  <files>backend/src/prisma/migrations/</files>
  <read_first>
    - CLAUDE.md (sección "Development Commands" — los comandos Prisma se corren desde `backend/`; el schema vive en `src/prisma/schema.prisma`)
    - backend/package.json (bloque `"prisma": { "schema": "src/prisma/schema.prisma" }` — confirma que los comandos no necesitan `--schema` explícito)
    - backend/src/prisma/migrations/migration_lock.toml (provider en uso)
    - backend/src/prisma/schema.prisma (estado post-Task 1)
  </read_first>
  <action>
    Este task es BLOCKING: sin él, `npm run build` y los type-checks pasarían igual (los tipos salen del cliente generado, no de la base) y la fase quedaría en falso positivo de verificación.

    Desde `backend/`, en este orden exacto:

    1. Capturar la línea base ANTES de migrar. Pre-migración `telefono` es NOT NULL, así que el total de filas equivale al total de pacientes con teléfono. Ejecutar: `node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.paciente.count().then(c=>console.log('TEL_BASELINE',c)).finally(()=>p.$disconnect())"`. Anotar el número devuelto — es el valor de contraste de SC#2.
    2. `npx prisma migrate dev --name telefono_opcional` — genera el SQL y lo aplica.
    3. `npx prisma generate` — regenera el cliente para que TypeScript vea `telefono: string | null`.
    4. Volver a contar, ahora filtrando explícitamente los no-nulos: `node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.paciente.count({where:{NOT:{telefono:null}}}).then(c=>console.log('TEL_AFTER',c)).finally(()=>p.$disconnect())"`. `TEL_AFTER` debe ser idéntico a `TEL_BASELINE`.
    5. Inspeccionar el SQL generado en el directorio nuevo `backend/src/prisma/migrations/<timestamp>_telefono_opcional/migration.sql`: debe contener `ALTER COLUMN "telefono" DROP NOT NULL` (o el equivalente `ALTER TABLE "Paciente" ALTER COLUMN "telefono" DROP NOT NULL`) y NO debe contener `DROP COLUMN`, `DELETE FROM`, `TRUNCATE` ni `DROP TABLE` contra `Paciente`.
    6. Registrar en el SUMMARY los valores `TEL_BASELINE` y `TEL_AFTER`.

    Reglas de aborto, sin excepciones: si `migrate dev` detecta drift y ofrece resetear la base, o pide cualquier confirmación interactiva que no se pueda contestar en este entorno, ABORTAR el task y reportar el prompt textual al usuario para intervención manual. NUNCA ejecutar `npx prisma migrate reset`, `npx prisma db push --accept-data-loss` ni ninguna variante que descarte datos. Si el SQL generado contiene una sentencia destructiva contra `Paciente`, revertir el archivo de migración y escalar.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && ls src/prisma/migrations | grep telefono_opcional && grep -i 'DROP NOT NULL' src/prisma/migrations/*telefono_opcional/migration.sql && ! grep -Ei 'DROP COLUMN|TRUNCATE|DROP TABLE|DELETE FROM' src/prisma/migrations/*telefono_opcional/migration.sql && npx prisma migrate status 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CLI: existe el directorio `backend/src/prisma/migrations/<timestamp>_telefono_opcional/` con un `migration.sql`
    - Source: ese `migration.sql` contiene `DROP NOT NULL` referido a la columna `telefono` de `Paciente`
    - Source: ese `migration.sql` NO contiene ninguno de `DROP COLUMN`, `DELETE FROM`, `TRUNCATE`, `DROP TABLE`
    - CLI: `cd backend && npx prisma migrate status` reporta la base al día (sin migraciones pendientes)
    - Behavior: `TEL_AFTER == TEL_BASELINE` — ningún paciente existente perdió su teléfono (SC#2), ambos valores anotados en el SUMMARY
    - Behavior: en ningún momento se ejecutó `migrate reset` ni `db push --accept-data-loss`
  </acceptance_criteria>
  <done>La columna `Paciente.telefono` acepta NULL en la base viva, el cliente Prisma está regenerado, y el conteo de teléfonos existentes no cambió.</done>
</task>

<task type="auto">
  <name>Task 3: Ensanchar las declaraciones de tipo que afirman telefono: string</name>
  <files>backend/src/modules/pacientes/dto/paciente-lista.dto.ts, backend/src/common/types/paciente-suggest.type.ts, backend/src/modules/reportes/types/reportes.types.ts, backend/src/modules/pacientes/pacientes.service.ts, backend/src/modules/reportes/services/reportes-financieros.service.ts</files>
  <read_first>
    - backend/src/modules/pacientes/dto/paciente-lista.dto.ts (archivo completo — `telefono: string;` en línea 6; nótese cómo `email`, `obraSocialNombre`, etc. ya usan `?: string | null`)
    - backend/src/common/types/paciente-suggest.type.ts (archivo completo, 8 líneas — `telefono: string;` en línea 5, `fotoUrl: string | null` en línea 6 como forma a copiar)
    - backend/src/modules/reportes/types/reportes.types.ts (líneas 194-220 — `CuentaPorCobrar` con `telefono: string;` en :197, `CuentaMorosa` con `telefono: string;` en :214; ambos ya tienen `email: string | null` / campos nullables como referencia)
    - backend/src/modules/pacientes/pacientes.service.ts (líneas 337-399: `suggest()`, con el genérico de `$queryRaw` en 347-355 declarando `telefono: string`)
    - backend/src/modules/reportes/services/reportes-financieros.service.ts (líneas 505-610: mapeo de `CuentaPorCobrar` en :522, genérico de `$queryRaw` de `getMorosidad()` en 551-558 con `telefono: string`, mapeo de `CuentaMorosa` en :599)
    - backend/src/modules/presupuestos/presupuesto-pdf.service.ts (líneas 15-19 y 143 — el precedente ya-correcto de teléfono opcional en este codebase)
  </read_first>
  <action>
    Ensanchar a `string | null` únicamente las declaraciones de tipo que hoy afirman `telefono: string` sobre un teléfono de paciente. Cinco sitios concretos:

    1. `backend/src/modules/pacientes/dto/paciente-lista.dto.ts:6` — `telefono: string;` pasa a `telefono: string | null;`.
    2. `backend/src/common/types/paciente-suggest.type.ts:5` — `telefono: string;` pasa a `telefono: string | null;` (misma forma que `fotoUrl: string | null` una línea abajo).
    3. `backend/src/modules/reportes/types/reportes.types.ts:197` (`CuentaPorCobrar`) y `:214` (`CuentaMorosa`) — ambos `telefono: string;` pasan a `telefono: string | null;`.
    4. `backend/src/modules/pacientes/pacientes.service.ts:347-355` — dentro del genérico `Array<{...}>` de `this.prisma.$queryRaw<...>` de `suggest()`, `telefono: string;` pasa a `telefono: string | null;`. En este task NO se toca el SQL de `suggest()` (el `LIKE` sobre `p.telefono` es materia del plan 67-02) — sólo el genérico TypeScript.
    5. `backend/src/modules/reportes/services/reportes-financieros.service.ts:551-558` — dentro del genérico `Array<{...}>` de `$queryRaw` de `getMorosidad()`, `telefono: string;` pasa a `telefono: string | null;`. El SQL de `getMorosidad()` sólo hace `SELECT pac.telefono` y `GROUP BY ... pac.telefono` (no filtra ni compara contra el teléfono), así que NO requiere cambio de SQL; confirmar leyendo la query antes de decidir.

    NO tocar el teléfono de la clínica (`config.telefono` en los bloques `config:` de los servicios de presupuesto) ni `contactoEmergenciaTelefono` — no son el teléfono del paciente. NO tocar `presupuesto-pdf.service.ts` (ya declara `telefono?: string | null`).

    Cerrar el task corriendo `npm run build` desde `backend/` y arreglando cualquier error de tipo residual que aparezca por la nueva nullabilidad, con la regla de que la corrección va en la declaración de tipo o en el consumo null-safe, nunca con `as any` ni `!` (non-null assertion) nuevos.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -20 && grep -rn 'telefono: string | null' src/common/types/paciente-suggest.type.ts src/modules/pacientes/dto/paciente-lista.dto.ts src/modules/reportes/types/reportes.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `cd backend && npm run build` sale 0
    - Source: `backend/src/common/types/paciente-suggest.type.ts` contiene `telefono: string | null`
    - Source: `backend/src/modules/pacientes/dto/paciente-lista.dto.ts` contiene `telefono: string | null`
    - Source: `backend/src/modules/reportes/types/reportes.types.ts` contiene `telefono: string | null` dos veces (una en `CuentaPorCobrar`, una en `CuentaMorosa`)
    - Source: el genérico de `$queryRaw` de `suggest()` en `pacientes.service.ts` declara `telefono: string | null`
    - Source: el genérico de `$queryRaw` de `getMorosidad()` en `reportes-financieros.service.ts` declara `telefono: string | null`
    - Source: `git diff` no introduce ningún `as any` ni `!.` nuevo sobre `telefono`
    - CLI: `cd backend && npm run lint` sale 0
  </acceptance_criteria>
  <done>El backend compila con `telefono` nullable en las 4 declaraciones de tipo y los 2 genéricos de raw SQL, sin supresiones de tipo nuevas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma CLI -> base de datos productiva/dev | La migración es la única operación de este plan con poder destructivo sobre datos reales de pacientes |
| cliente HTTP -> `POST /pacientes` | Payload no confiable que hasta ahora tenía `telefono` como campo obligatorio por DTO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Tampering | `npx prisma migrate dev` sobre `Paciente` | mitigate | Assert sobre el SQL generado: debe contener `DROP NOT NULL` y no contener `DROP COLUMN`/`DELETE FROM`/`TRUNCATE`/`DROP TABLE`; contraste `TEL_BASELINE` vs `TEL_AFTER`; prohibición explícita de `migrate reset` y `db push --accept-data-loss` |
| T-67-02 | Denial of Service | `npx prisma migrate dev` en modo interactivo | mitigate | Regla de aborto: si el CLI pide confirmación no contestable, abortar y escalar a intervención manual en vez de improvisar un workaround |
| T-67-03 | Tampering | `CreatePacienteDto.telefono` deja de ser obligatorio | mitigate | La invariante de forma no se pierde: se reubica en `normalizeTelefono()` (plan 67-02, D-01/D-06) y en los guards de envío (plan 67-03, D-03). Este plan sólo relaja el DTO; los planes 02 y 03 son dependencias de wave 2 que restauran la validación |
| T-67-04 | Information Disclosure | índice GIN trgm `idx_paciente_telefono_trgm` | accept | El índice queda intacto y las filas NULL simplemente no se indexan; no cambia quién puede leer teléfonos ni el scoping multi-tenant de las queries |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala ni actualiza ninguna dependencia (`package.json` no está en `files_modified`). Si durante la ejecución apareciera la necesidad de instalar un paquete, detener el plan y correr el Package Legitimacy Gate antes de continuar |
</threat_model>

<verification>
- `cd backend && npx prisma validate` sale 0
- `cd backend && npx prisma migrate status` reporta la base al día
- `cd backend && npm run build` sale 0
- `cd backend && npm run lint` sale 0
- `TEL_AFTER == TEL_BASELINE` documentado en el SUMMARY
</verification>

<success_criteria>
- `Paciente.telefono` es nullable en el schema, en la migración aplicada y en el cliente Prisma generado
- `CreatePacienteDto` declara `telefono?: string` con `@IsOptional()`
- Los pacientes existentes conservan su teléfono (SC#2 del ROADMAP), probado por el contraste de conteos
- El backend compila y linta en verde con las 6 declaraciones de tipo ensanchadas
</success_criteria>

<output>
Create `.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-01-SUMMARY.md` when done
</output>

