---
phase: 67-tel-fono-opcional-y-guards-de-env-o-backend
plan: 04
type: execute
wave: 2
depends_on: ["67-01"]
files_modified:
  - backend/src/modules/reportes/services/reportes-financieros.service.ts
  - backend/src/modules/presupuestos/presupuestos.service.ts
  - backend/src/modules/presupuestos/presupuesto-email.service.ts
autonomous: true
requirements: [TEL-01]

must_haves:
  truths:
    - "Los reportes financieros (cuentas por cobrar y morosidad) devuelven pacientes sin teléfono como telefono: null, sin romper el tipado ni el agrupamiento SQL"
    - "El armado del payload del PDF de presupuesto propaga telefono: null sin casts as any y sin non-null assertions"
    - "El PDF de presupuesto omite la línea 'Teléfono:' cuando el paciente no tiene número, en vez de imprimir 'Teléfono: null'"
    - "Los 10 sitios de lectura enumerados en las notas de implementación del ROADMAP quedan auditados con disposición explícita registrada en el SUMMARY"
  artifacts:
    - path: "backend/src/modules/presupuestos/presupuestos.service.ts"
      provides: "generatePdf() propaga telefono nullable sin supresión de tipos"
      contains: "telefono: presupuesto.paciente.telefono"
    - path: "backend/src/modules/reportes/services/reportes-financieros.service.ts"
      provides: "Mapeos de CuentaPorCobrar y CuentaMorosa con telefono nullable"
      contains: "telefono: c.telefono"
  key_links:
    - from: "backend/src/modules/presupuestos/presupuestos.service.ts generatePdf()"
      to: "backend/src/modules/presupuestos/presupuesto-pdf.service.ts PresupuestoPdfData"
      via: "paciente.telefono pasa como string | null al generador de PDF, que ya lo renderiza condicionalmente"
      pattern: "telefono: presupuesto\\.paciente\\.telefono"
---

<objective>
Cerrar la auditoría de lecturas que las notas de implementación del ROADMAP enumeran: los tres sitios de reportes financieros y los dos de presupuestos que hoy consumen `telefono` asumiendo un string. Dejarlos propagando `null` limpiamente, sin `as any` ni non-null assertions, y registrar la disposición de los 10 sitios auditados.

Purpose: TEL-01 — la relajación del NOT NULL no puede degenerar en "null" impreso en un PDF, en un reporte roto o en un cast que tape el problema. `presupuesto-pdf.service.ts:143` ya trata el teléfono como opcional y es el precedente a preservar, no a rehacer.
Output: mapeos de reportes verificados, `generatePdf()` sin casts sobre `telefono`, y la tabla de auditoría en el SUMMARY.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-CONTEXT.md
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-PATTERNS.md
@.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-01-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Estado post plan 67-01. Los tipos ya están ensanchados; acá se audita el consumo. -->

Ya hecho por el plan 67-01 (NO rehacer):
  - `CuentaPorCobrar.telefono` y `CuentaMorosa.telefono` en `reportes/types/reportes.types.ts` (:197 y :214) son `string | null`
  - el genérico de `$queryRaw` de `getMorosidad()` (`reportes-financieros.service.ts:551-558`) declara `telefono: string | null`

Sitios de consumo a auditar en este plan:
  - `reportes-financieros.service.ts:474` — `select: { ... telefono: true }` de `getCuentasPorCobrar()`
  - `reportes-financieros.service.ts:522` — `telefono: c.paciente.telefono` en el `.map()` a `CuentaPorCobrar[]`
  - `reportes-financieros.service.ts:563` y `:572` — `SELECT pac.telefono` y `GROUP BY pac.id, pac."nombreCompleto", pac.telefono, cc."saldoActual"` en el raw SQL de `getMorosidad()`
  - `reportes-financieros.service.ts:599` — `telefono: c.telefono` en el `.map()` a `CuentaMorosa[]`
  - `presupuestos.service.ts:428` — `select: { id, nombreCompleto, dni, email, telefono }` dentro de `generatePdf()`
  - `presupuestos.service.ts:458` — `telefono: (presupuesto.paciente as any).telefono` en el armado de `pdfData.paciente`
  - `presupuesto-email.service.ts:77` — `telefono: presupuesto.paciente.telefono` en el mismo armado, sin cast

Tipo destino, ya correcto (NO editar):
  `presupuesto-pdf.service.ts:15-19` -> `paciente: { nombreCompleto: string; dni?: string | null; email?: string | null; telefono?: string | null }`
  `presupuesto-pdf.service.ts:143` -> `if (paciente.telefono) doc.text(\`Teléfono: ${paciente.telefono}\`, 50, 230);`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Auditar los tres sitios de reportes financieros</name>
  <files>backend/src/modules/reportes/services/reportes-financieros.service.ts</files>
  <read_first>
    - backend/src/modules/reportes/services/reportes-financieros.service.ts (líneas 460-610: `getCuentasPorCobrar()` con el `select` en :474 y el `.map()` a `CuentaPorCobrar[]` en ~:519-528; `getMorosidad()` con el `$queryRaw` en ~:547-575 incluyendo el `SELECT pac.telefono` de :563 y el `GROUP BY` de :572; el `.map()` a `CuentaMorosa[]` en ~:590-605)
    - backend/src/modules/reportes/types/reportes.types.ts (líneas 194-220, estado post plan 67-01)
    - backend/src/modules/reportes/reportes.controller.spec.ts (convención de test del módulo, por si hace falta un assert nuevo)
    - .planning/ROADMAP.md (Phase 67, "Notas de implementación": la lista de reads a auditar)
  </read_first>
  <action>
    Recorrer los tres sitios y dejarlos consumiendo `telefono: string | null` sin supresión de tipos:

    1. `getCuentasPorCobrar()` — el `.map()` de la línea ~522 asigna `telefono: c.paciente.telefono` a `CuentaPorCobrar[]`. Con el tipo ya ensanchado por el plan 67-01, esto debería compilar sin cambios: confirmarlo y NO introducir un `?? ''` ni un placeholder de texto. El placeholder de display es responsabilidad de la Phase 69 (TEL-03) y meterlo acá le sacaría al frontend la capacidad de distinguir "sin teléfono" de "teléfono vacío".
    2. `getMorosidad()` — el raw SQL sólo hace `SELECT pac.telefono` (:563) y `GROUP BY ... pac.telefono` (:572); no filtra ni compara contra el teléfono, y en Postgres `GROUP BY` sobre una columna nullable agrupa los NULL sin error. Confirmar leyendo la query que no hay ningún `LIKE`, `=`, `LOWER()`, `unaccent()` ni `similarity()` aplicado a `pac.telefono`. Si apareciera alguna comparación, envolver la columna en `COALESCE(pac.telefono, '')` igual que se hizo en `suggest()` (plan 67-02), manteniendo los parámetros del template tag de Prisma y sin `Prisma.raw`.
    3. El `.map()` a `CuentaMorosa[]` (~:599) — mismo criterio que el punto 1.

    Preservar en los tres el scoping existente (filtros por profesional/tenant que ya tenga cada query): no relajar ni ampliar ninguna cláusula `WHERE`. No agregar `as any`, `!` ni `?? ''`.

    Si `npm run build` no reporta ningún error en este archivo, el resultado legítimo de esta task puede ser cero líneas modificadas: en ese caso registrar en el SUMMARY que los tres sitios quedaron auditados sin cambios, con el motivo.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && npm run build 2>&1 | tail -10 && ! grep -n "pac.telefono LIKE\|pac.telefono =" src/modules/reportes/services/reportes-financieros.service.ts && ! grep -n "telefono.*as any\|telefono!" src/modules/reportes/services/reportes-financieros.service.ts && npm run test -- reportes 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `cd backend && npm run build` sale 0
    - CLI: `cd backend && npm run test -- reportes` sale 0
    - Source: `reportes-financieros.service.ts` no contiene `as any` ni `!` de non-null assertion aplicados a `telefono`
    - Source: `reportes-financieros.service.ts` no contiene `?? ''` ni ningún literal de placeholder ("—", "Sin teléfono") sobre `telefono`
    - Source: el raw SQL de `getMorosidad()` no aplica `LIKE`, `=`, `LOWER`, `unaccent` ni `similarity` sobre `pac.telefono`; si lo hiciera, la columna queda envuelta en `COALESCE(pac.telefono, '')`
    - Behavior: `getCuentasPorCobrar()` y `getMorosidad()` devuelven `telefono: null` para un paciente sin número, sin lanzar
    - SUMMARY: registra los tres sitios (`:522`, `:563/:572`, `:599`) con su disposición (modificado / auditado sin cambios) y el motivo
  </acceptance_criteria>
  <done>Los reportes financieros propagan `telefono: null` sin casts, sin placeholders prematuros y sin cambios de scoping.</done>
</task>

<task type="auto">
  <name>Task 2: Presupuestos — propagar telefono nullable sin casts y cerrar la tabla de auditoría</name>
  <files>backend/src/modules/presupuestos/presupuestos.service.ts, backend/src/modules/presupuestos/presupuesto-email.service.ts</files>
  <read_first>
    - backend/src/modules/presupuestos/presupuestos.service.ts (líneas 417-476: `generatePdf()` completo — el `include`/`select` del paciente en 421-430 y el bloque `paciente: { nombreCompleto, dni, email, telefono }` con los `as any` en ~454-459)
    - backend/src/modules/presupuestos/presupuesto-email.service.ts (líneas 60-90: `enviarPresupuesto()` armando el mismo payload, con `telefono: presupuesto.paciente.telefono` sin cast en :77)
    - backend/src/modules/presupuestos/presupuesto-pdf.service.ts (líneas 15-19: la interfaz `PresupuestoPdfData` con `telefono?: string | null`; línea 143: `if (paciente.telefono) doc.text(...)` — el render condicional que NO se toca)
    - backend/src/modules/presupuestos/presupuestos.controller.ts (líneas 50-65: la ruta pública `GET /presupuestos/:id/pdf` que Meta consume; contexto de por qué este sitio no es un guard site)
    - .planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-PATTERNS.md (sección `presupuestos.service.ts — generatePdf()`, incluida la "Note for the planner")
  </read_first>
  <action>
    Este NO es un guard site. El guard de envío por WhatsApp del presupuesto (ENVIO-02) vive en `whatsapp.service.ts::sendPresupuestoPdf` y lo implementa el plan 67-03; acá el cambio es estrictamente de propagación de tipos. No agregar ningún `BadRequestException` en estos dos archivos.

    En `presupuestos.service.ts::generatePdf()` (~líneas 454-459): quitar los casts `(presupuesto.paciente as any)` sobre `telefono` —y también sobre `dni` y `email` si el compilador los acepta sin ellos, ya que el `select` de las líneas 421-430 los incluye explícitamente— de modo que el payload quede `telefono: presupuesto.paciente.telefono`, alineado con cómo lo escribe `presupuesto-email.service.ts:77`. El destino `PresupuestoPdfData.paciente.telefono` ya es `string | null | undefined`, así que el `null` fluye sin error. Si al quitar algún cast el compilador se queja por un motivo ajeno a la nullabilidad de `telefono` (por ejemplo el shape del `include`), dejar ese cast puntual y anotarlo en el SUMMARY, pero el cast sobre `telefono` sí debe desaparecer.

    En `presupuesto-email.service.ts:77`: verificar que sigue compilando sin cambios (ya pasa el valor directo). NO editar salvo que el build lo exija.

    En `presupuesto-pdf.service.ts`: NO editar. Es el precedente ya correcto — el `if (paciente.telefono)` de la línea 143 hace que la línea "Teléfono: ..." simplemente no se dibuje cuando no hay número. Confirmar leyendo que el guard sigue ahí.

    Cerrar el task escribiendo en el SUMMARY una tabla de auditoría con los 10 sitios de lectura que enumeran las notas de implementación del ROADMAP —`pacientes.service.ts:166`, `pacientes.service.ts:973`, `reportes-financieros.service.ts:522/563/599`, `paciente-portal.service.ts:142`, `whatsapp.service.ts:229/279/331/456`, `presupuestos.service.ts:458`, `presupuesto-email.service.ts:77`, `presupuesto-pdf.service.ts:143`— indicando para cada uno el plan que lo cubrió (67-01, 67-02, 67-03 o 67-04) y su disposición final.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/CLINICAL/backend && ! grep -n 'paciente as any).telefono' src/modules/presupuestos/presupuestos.service.ts && grep -n 'if (paciente.telefono)' src/modules/presupuestos/presupuesto-pdf.service.ts && ! grep -n 'BadRequestException' src/modules/presupuestos/presupuesto-email.service.ts && npm run build 2>&1 | tail -10 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `presupuestos.service.ts` no contiene `(presupuesto.paciente as any).telefono`; el payload usa `telefono: presupuesto.paciente.telefono`
    - Source: `presupuesto-pdf.service.ts` sigue conteniendo `if (paciente.telefono)` y `git diff --stat` no lo lista como modificado
    - Source: ni `presupuestos.service.ts::generatePdf()` ni `presupuesto-email.service.ts` incorporan un `BadRequestException` nuevo (el guard de ENVIO-02 es del plan 67-03)
    - Behavior: generar el PDF de un presupuesto de un paciente con `telefono: null` no lanza y el PDF no contiene la cadena `Teléfono: null`
    - CLI: `cd backend && npm run build` sale 0 y `npm run lint` sale 0
    - SUMMARY: contiene la tabla con los 10 sitios del ROADMAP, el plan que cubrió cada uno y su disposición
  </acceptance_criteria>
  <done>El payload del PDF propaga `telefono: null` sin casts, el render condicional preexistente sigue intacto, y la auditoría de los 10 sitios queda registrada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /presupuestos/:id/pdf` -> Internet (Meta fetchea el PDF) | Ruta pública cuyo contenido sale del control del sistema una vez servida |
| backend -> reportes financieros expuestos al staff | Datos de contacto de pacientes agregados en listados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-16 | Information Disclosure | PDF de presupuesto servido en una ruta pública | mitigate | Se preserva el render condicional `if (paciente.telefono)` de `presupuesto-pdf.service.ts:143`: no se imprime `Teléfono: null` ni ninguna cadena derivada de un valor ausente. Criterio de aceptación: el PDF generado no contiene `Teléfono: null` |
| T-67-17 | Tampering | casts `as any` sobre el payload del PDF | mitigate | Quitar el cast sobre `telefono` restablece el chequeo de tipos en ese borde en vez de taparlo; prohibido reemplazarlo por `!` o `?? ''` |
| T-67-18 | Information Disclosure | scoping de `getCuentasPorCobrar()` y `getMorosidad()` | mitigate | No se toca ninguna cláusula `WHERE` ni filtro por profesional; criterio de aceptación explícito de "sin cambios de scoping" |
| T-67-19 | Tampering | raw SQL de `getMorosidad()` | mitigate | Si hubiera que envolver la columna, se usa `COALESCE(pac.telefono, '')` sobre la columna y nunca sobre el parámetro; prohibido `Prisma.raw` y la concatenación de strings |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala ni actualiza dependencias. Si apareciera la necesidad, detener y correr el Package Legitimacy Gate antes de continuar |
</threat_model>

<verification>
- `cd backend && npm run build` sale 0
- `cd backend && npm run lint` sale 0
- `cd backend && npm run test -- reportes` sale 0
- La tabla de auditoría de los 10 sitios está en el SUMMARY
</verification>

<success_criteria>
- Los reportes financieros y el payload del PDF de presupuesto propagan `telefono: null` sin casts ni assertions
- El PDF de presupuesto omite la línea de teléfono cuando no hay número
- Ningún placeholder de display se filtró al backend (eso es Phase 69 / TEL-03)
- Los 10 sitios de lectura del ROADMAP quedan auditados con disposición explícita
</success_criteria>

<output>
Create `.planning/phases/67-tel-fono-opcional-y-guards-de-env-o-backend/67-04-SUMMARY.md` when done
</output>
