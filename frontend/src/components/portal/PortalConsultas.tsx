"use client";

/**
 * Seccion "Consultas" del portal del paciente.
 * Wave 2 (Plan 04) implementa el formulario de envio de consultas al medico.
 * Sin props: usa useEnviarConsulta() internamente (CHAT-04 one-way).
 */
export function PortalConsultas() {
  return (
    <div className="space-y-4 text-base">
      <p className="text-gray-500">Cargando el area de consultas...</p>
    </div>
  );
}
